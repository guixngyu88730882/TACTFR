# GTA5 Native 函数 C# 调用参考大全

> 数据来源: [alloc8or/gta5-nativedb-data](https://github.com/alloc8or/gta5-nativedb-data)
> 
> 本文档包含 GTA5 所有 6673 个 Native 函数的 C# 调用参考
> 
> 最后更新: 2025年12月 (游戏版本 3717/1013)

---

## 目录

- [使用说明](#使用说明)
- [命名空间列表](#命名空间列表)
- [BUILTIN](#builtin)
- [APP](#app)
- [AUDIO](#audio)
- [BRAIN](#brain)
- [CAM](#cam)
- [CLOCK](#clock)
- [CUTSCENE](#cutscene)
- [DATAFILE](#datafile)
- [DECORATOR](#decorator)
- [DLC](#dlc)
- [ENTITY](#entity)
- [EVENT](#event)
- [EXTRAMETADATA](#extrametadata)
- [FIRE](#fire)
- [GRAPHICS](#graphics)
- [HUD](#hud)
- [INTERIOR](#interior)
- [ITEMSET](#itemset)
- [LOBBY](#lobby)
- [LOCALIZATION](#localization)
- [MISC](#misc)
- [MOBILE](#mobile)
- [MONEY](#money)
- [NETSHOPPING](#netshopping)
- [NETWORK](#network)
- [OBJECT](#object)
- [PAD](#pad)
- [PATHFIND](#pathfind)
- [PED](#ped)
- [PHYSICS](#physics)
- [PLAYER](#player)
- [RECORDING](#recording)
- [REPLAY](#replay)
- [SAVEMIGRATION](#savemigration)
- [SCRIPT](#script)
- [SECURITY](#security)
- [SHAPETEST](#shapetest)
- [SOCIALCLUB](#socialclub)
- [STATS](#stats)
- [STREAMING](#streaming)
- [TASK](#task)
- [VEHICLE](#vehicle)
- [WATER](#water)
- [WEAPON](#weapon)
- [ZONE](#zone)

---

## 使用说明

### C# 调用 Native 的基本方式

```csharp
// 使用 ScriptHookVDotNet
using GTA;
using GTA.Native;

// 调用 Native 函数示例
// 等待 1000 毫秒
Function.Call(Hash.WAIT, 1000);

// 获取玩家角色
Ped playerPed = Function.Call<Ped>(Hash.GET_PLAYER_PED, Game.Player);

// 生成载具
Vehicle vehicle = Function.Call<Vehicle>(Hash.CREATE_VEHICLE, 
    (uint)VehicleHash.Adder, 
    position.X, position.Y, position.Z, 
    heading, 
    false, false);
```

### 类型对照表

| C++ 类型 | C# 类型 | 说明 |
|---------|--------|------|
| `void` | `void` | 无返回值 |
| `int` | `int` | 整数 |
| `float` | `float` | 浮点数 |
| `bool` / `BOOL` | `bool` | 布尔值 |
| `const char*` | `string` | 字符串 |
| `Hash` | `uint` | 哈希值 |
| `Entity` | `int` | 实体句柄 |
| `Ped` | `int` | 角色句柄 |
| `Vehicle` | `int` | 载具句柄 |
| `Object` | `int` | 物体句柄 |
| `Player` | `int` | 玩家句柄 |
| `Cam` | `int` | 摄像机句柄 |
| `Blip` | `int` | 地图标记句柄 |
| `Any` | `object` | 任意类型 |
| `Any*` | `IntPtr` / `object*` | 指针类型 |
| `Vector3` | `Vector3` | 三维向量 |

---

## 命名空间列表


---

## BUILTIN

**包含 26 个 Native 函数**

### WAIT

**Hash:** `0x4EDE34FBADD967A6` | **JHash:** `0x7715C03B` | **Build:** `323`

**C++ 原型:**
```cpp
void WAIT(int ms)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAIT, ms);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ms` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Pauses execution of the current script, please note this behavior is only seen when called from one of the game script files(ysc). In order to wait an asi script use "static void WAIT(DWORD time);" found in main.h

---

### START_NEW_SCRIPT

**Hash:** `0xE81651AD79516E48` | **JHash:** `0x3F166D0E` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NEW_SCRIPT(const char* scriptName, int stackSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NEW_SCRIPT, scriptName, stackSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `stackSize` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Examples:
 g_384A = SYSTEM::START_NEW_SCRIPT("cellphone_flashhand", 1424);
 l_10D = SYSTEM::START_NEW_SCRIPT("taxiService", 1828);
 SYSTEM::START_NEW_SCRIPT("AM_MP_YACHT", 5000);
 SYSTEM::START_NEW_SCRIPT("emergencycall", 512);
 SYSTEM::START_NEW_SCRIPT("emergencycall", 512); 
 SYSTEM::START_NEW_SCRIPT("FM_maintain_cloud_header_data", 1424);
 SYSTEM::START_NEW_SCRIPT("FM_Mission_Controller", 31000);
 SYSTEM::START_NEW_SCRIPT("tennis_family", 3650);
 SYSTEM::START_NEW_SCRIPT("Celebrations", 3650);

Decompiled examples of usage when starting a script:
 
    SCRIPT::REQUEST_SCRIPT(a_0);
    if (SCRIPT::HAS_SCRIPT_LOADED(a_0)) {
        SYSTEM::START_NEW_SCRIPT(a_0, v_3);
        SCRIPT::SET_SCRIPT_AS_NO_LONGER_NEEDED(a_0);
        return 1;
    }
 
or:

    v_2 = "MrsPhilips2";
    SCRIPT::REQUEST_SCRIPT(v_2);
    while (!SCRIPT::HAS_SCRIPT_LOADED(v_2)) {
    SCRIPT::REQUEST_SCRIPT(v_2);
    SYSTEM::WAIT(0);
    }
    sub_8792(36);
    SYSTEM::START_NEW_SCRIPT(v_2, 17000);
    SCRIPT::SET_SCRIPT_AS_NO_LONGER_NEEDED(v_2);

---

### START_NEW_SCRIPT_WITH_ARGS

**Hash:** `0xB8BA7F44DF1575E1` | **JHash:** `0x4A2100E4` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NEW_SCRIPT_WITH_ARGS(const char* scriptName, Any* args, int argCount, int stackSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NEW_SCRIPT_WITH_ARGS, scriptName, args, argCount, stackSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `args` | `Any*` | `object*` | |
| `argCount` | `int` | `int` | |
| `stackSize` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** return : script thread id, 0 if failed
Pass pointer to struct of args in p1, size of struct goes into p2

---

### START_NEW_SCRIPT_WITH_NAME_HASH

**Hash:** `0xEB1C67C3A5333A92` | **JHash:** `0x8D15BE5D` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NEW_SCRIPT_WITH_NAME_HASH(Hash scriptHash, int stackSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NEW_SCRIPT_WITH_NAME_HASH, scriptHash, stackSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |
| `stackSize` | `int` | `int` | |

**返回值:** `int` → `int`

---

### START_NEW_SCRIPT_WITH_NAME_HASH_AND_ARGS

**Hash:** `0xC4BB298BD441BE78` | **JHash:** `0xE38A3AD4` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NEW_SCRIPT_WITH_NAME_HASH_AND_ARGS(Hash scriptHash, Any* args, int argCount, int stackSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NEW_SCRIPT_WITH_NAME_HASH_AND_ARGS, scriptHash, args, argCount, stackSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |
| `args` | `Any*` | `object*` | |
| `argCount` | `int` | `int` | |
| `stackSize` | `int` | `int` | |

**返回值:** `int` → `int`

---

### TIMERA

**Hash:** `0x83666F9FB8FEBD4B` | **JHash:** `0x45C8C188` | **Build:** `323`

**C++ 原型:**
```cpp
int TIMERA(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TIMERA);
```

**返回值:** `int` → `int`

**说明:** Counts up. Every 1000 is 1 real-time second. Use SETTIMERA(int value) to set the timer (e.g.: SETTIMERA(0)).

---

### TIMERB

**Hash:** `0xC9D9444186B5A374` | **JHash:** `0x330A9C0C` | **Build:** `323`

**C++ 原型:**
```cpp
int TIMERB(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TIMERB);
```

**返回值:** `int` → `int`

---

### SETTIMERA

**Hash:** `0xC1B1E9A034A63A62` | **JHash:** `0x35785333` | **Build:** `323`

**C++ 原型:**
```cpp
void SETTIMERA(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SETTIMERA, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SETTIMERB

**Hash:** `0x5AE11BC36633DE4E` | **JHash:** `0x27C1B7C6` | **Build:** `323`

**C++ 原型:**
```cpp
void SETTIMERB(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SETTIMERB, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TIMESTEP

**Hash:** `0x0000000050597EE2` | **JHash:** `0x50597EE2` | **Build:** `323`

**C++ 原型:**
```cpp
float TIMESTEP(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.TIMESTEP);
```

**返回值:** `float` → `float`

**说明:** Gets the current frame time.

---

### SIN

**Hash:** `0x0BADBFA3B172435F` | **JHash:** `0xBF987F58` | **Build:** `323`

**C++ 原型:**
```cpp
float SIN(float value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.SIN, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `float` → `float`

---

### COS

**Hash:** `0xD0FFB162F40A139C` | **JHash:** `0x00238FE9` | **Build:** `323`

**C++ 原型:**
```cpp
float COS(float value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.COS, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `float` → `float`

---

### SQRT

**Hash:** `0x71D93B57D07F9804` | **JHash:** `0x145C7701` | **Build:** `323`

**C++ 原型:**
```cpp
float SQRT(float value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.SQRT, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `float` → `float`

---

### POW

**Hash:** `0xE3621CC40F31FE2E` | **JHash:** `0x85D134F8` | **Build:** `323`

**C++ 原型:**
```cpp
float POW(float base, float exponent)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.POW, base, exponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `base` | `float` | `float` | |
| `exponent` | `float` | `float` | |

**返回值:** `float` → `float`

---

### LOG10

**Hash:** `0xE816E655DE37FE20` | **Build:** `1493`

**C++ 原型:**
```cpp
float LOG10(float value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.LOG10, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `float` → `float`

---

### VMAG

**Hash:** `0x652D2EEEF1D3E62C` | **JHash:** `0x1FCF1ECD` | **Build:** `323`

**C++ 原型:**
```cpp
float VMAG(float x, float y, float z)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.VMAG, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Calculates the magnitude of a vector.

---

### VMAG2

**Hash:** `0xA8CEACB4F35AE058` | **JHash:** `0xE796E629` | **Build:** `323`

**C++ 原型:**
```cpp
float VMAG2(float x, float y, float z)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.VMAG2, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Calculates the magnitude of a vector but does not perform Sqrt operations. (Its way faster)

---

### VDIST

**Hash:** `0x2A488C176D52CCA5` | **JHash:** `0x3C08ECB7` | **Build:** `323`

**C++ 原型:**
```cpp
float VDIST(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.VDIST, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Calculates distance between vectors.

---

### VDIST2

**Hash:** `0xB7A628320EFF8E47` | **JHash:** `0xC85DEF1F` | **Build:** `323`

**C++ 原型:**
```cpp
float VDIST2(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.VDIST2, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Calculates distance between vectors but does not perform Sqrt operations. (Its way faster)

---

### SHIFT_LEFT

**Hash:** `0xEDD95A39E5544DE8` | **JHash:** `0x314CC6CD` | **Build:** `323`

**C++ 原型:**
```cpp
int SHIFT_LEFT(int value, int bitShift)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SHIFT_LEFT, value, bitShift);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |
| `bitShift` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SHIFT_RIGHT

**Hash:** `0x97EF1E5BCE9DC075` | **JHash:** `0x352633CA` | **Build:** `323`

**C++ 原型:**
```cpp
int SHIFT_RIGHT(int value, int bitShift)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SHIFT_RIGHT, value, bitShift);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |
| `bitShift` | `int` | `int` | |

**返回值:** `int` → `int`

---

### FLOOR

**Hash:** `0xF34EE736CF047844` | **JHash:** `0x32E9BE04` | **Build:** `323`

**C++ 原型:**
```cpp
int FLOOR(float value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.FLOOR, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `int` → `int`

---

### CEIL

**Hash:** `0x11E019C8F43ACC8A` | **JHash:** `0xD536A1DF` | **Build:** `323`

**C++ 原型:**
```cpp
int CEIL(float value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CEIL, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** I'm guessing this rounds a float value up to the next whole number, and FLOOR rounds it down

---

### ROUND

**Hash:** `0xF2DB717A73826179` | **JHash:** `0x323B0E24` | **Build:** `323`

**C++ 原型:**
```cpp
int ROUND(float value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ROUND, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `int` → `int`

---

### TO_FLOAT

**Hash:** `0xBBDA792448DB5A89` | **JHash:** `0x67116627` | **Build:** `323`

**C++ 原型:**
```cpp
float TO_FLOAT(int value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.TO_FLOAT, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `float` → `float`

---

### SET_THIS_THREAD_PRIORITY

**Hash:** `0x42B65DEEF2EDF2A1` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_THIS_THREAD_PRIORITY(int priority)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIS_THREAD_PRIORITY, priority);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `priority` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** THREAD_PRIO_HIGHEST = 0
THREAD_PRIO_NORMAL = 1
THREAD_PRIO_LOWEST = 2
THREAD_PRIO_MANUAL_UPDATE = 100

---


---

## APP

**包含 17 个 Native 函数**

### APP_DATA_VALID

**Hash:** `0x846AA8E7D55EE5B6` | **JHash:** `0x72BDE002` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL APP_DATA_VALID(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.APP_DATA_VALID);
```

**返回值:** `BOOL` → `bool`

---

### APP_GET_INT

**Hash:** `0xD3A58A12C77D9D4B` | **JHash:** `0x2942AAD2` | **Build:** `323`

**C++ 原型:**
```cpp
int APP_GET_INT(const char* property)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.APP_GET_INT, property);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### APP_GET_FLOAT

**Hash:** `0x1514FB24C02C2322` | **JHash:** `0xD87F3A1C` | **Build:** `323`

**C++ 原型:**
```cpp
float APP_GET_FLOAT(const char* property)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.APP_GET_FLOAT, property);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |

**返回值:** `float` → `float`

---

### APP_GET_STRING

**Hash:** `0x749B023950D2311C` | **JHash:** `0x849CEB80` | **Build:** `323`

**C++ 原型:**
```cpp
const char* APP_GET_STRING(const char* property)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.APP_GET_STRING, property);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |

**返回值:** `const char*` → `string`

---

### APP_SET_INT

**Hash:** `0x607E8E3D3E4F9611` | **JHash:** `0x1B509C32` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SET_INT(const char* property, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SET_INT, property, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### APP_SET_FLOAT

**Hash:** `0x25D7687C68E0DAA4` | **JHash:** `0xF3076135` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SET_FLOAT(const char* property, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SET_FLOAT, property, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### APP_SET_STRING

**Hash:** `0x3FF2FCEC4B7721B4` | **JHash:** `0x23DF19A8` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SET_STRING(const char* property, const char* value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SET_STRING, property, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `property` | `const char*` | `string` | |
| `value` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### APP_SET_APP

**Hash:** `0xCFD0406ADAF90D2B` | **JHash:** `0x8BAC4146` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SET_APP(const char* appName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SET_APP, appName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `appName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Called in the gamescripts like:
APP::APP_SET_APP("car");
APP::APP_SET_APP("dog");

---

### APP_SET_BLOCK

**Hash:** `0x262AB456A3D21F93` | **JHash:** `0xC2D54DD9` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SET_BLOCK(const char* blockName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SET_BLOCK, blockName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blockName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### APP_CLEAR_BLOCK

**Hash:** `0x5FE1DF3342DB7DBA` | **JHash:** `0xDAB86A18` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_CLEAR_BLOCK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_CLEAR_BLOCK);
```

**返回值:** `void` → `void`

---

### APP_CLOSE_APP

**Hash:** `0xE41C65E07A5F05FC` | **JHash:** `0x03767C7A` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_CLOSE_APP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_CLOSE_APP);
```

**返回值:** `void` → `void`

---

### APP_CLOSE_BLOCK

**Hash:** `0xE8E3FCF72EAC0EF8` | **JHash:** `0xED97B202` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_CLOSE_BLOCK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_CLOSE_BLOCK);
```

**返回值:** `void` → `void`

---

### APP_HAS_LINKED_SOCIAL_CLUB_ACCOUNT

**Hash:** `0x71EEE69745088DA0` | **JHash:** `0xD368BA15` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL APP_HAS_LINKED_SOCIAL_CLUB_ACCOUNT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.APP_HAS_LINKED_SOCIAL_CLUB_ACCOUNT);
```

**返回值:** `BOOL` → `bool`

---

### APP_HAS_SYNCED_DATA

**Hash:** `0xCA52279A7271517F` | **JHash:** `0x1DE2A63D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL APP_HAS_SYNCED_DATA(const char* appName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.APP_HAS_SYNCED_DATA, appName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `appName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### APP_SAVE_DATA

**Hash:** `0x95C5D356CDA6E85F` | **JHash:** `0x84A3918D` | **Build:** `323`

**C++ 原型:**
```cpp
void APP_SAVE_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APP_SAVE_DATA);
```

**返回值:** `void` → `void`

---

### APP_GET_DELETED_FILE_STATUS

**Hash:** `0xC9853A2BE3DED1A6` | **JHash:** `0x784D550B` | **Build:** `323`

**C++ 原型:**
```cpp
int APP_GET_DELETED_FILE_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.APP_GET_DELETED_FILE_STATUS);
```

**返回值:** `int` → `int`

---

### APP_DELETE_APP_DATA

**Hash:** `0x44151AEA95C8A003` | **JHash:** `0x2A2FBD1C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL APP_DELETE_APP_DATA(const char* appName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.APP_DELETE_APP_DATA, appName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `appName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---


---

## AUDIO

**包含 281 个 Native 函数**

### PLAY_PED_RINGTONE

**Hash:** `0xF9E56683CA8E11A5` | **JHash:** `0x1D530E47` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_PED_RINGTONE(const char* ringtoneName, Ped ped, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PED_RINGTONE, ringtoneName, ped, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ringtoneName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/RFb4GTny

AUDIO::PLAY_PED_RINGTONE("Remote_Ring", PLAYER::PLAYER_PED_ID(), 1);
AUDIO::PLAY_PED_RINGTONE("Dial_and_Remote_Ring", PLAYER::PLAYER_PED_ID(), 1);


---

### IS_PED_RINGTONE_PLAYING

**Hash:** `0x1E8E5E20937E3137` | **JHash:** `0xFE576EE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RINGTONE_PLAYING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RINGTONE_PLAYING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STOP_PED_RINGTONE

**Hash:** `0x6C5AE23EFA885092` | **JHash:** `0xFEEA107C` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PED_RINGTONE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PED_RINGTONE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_MOBILE_PHONE_CALL_ONGOING

**Hash:** `0x7497D2CE2C30D24C` | **JHash:** `0x4ED1400A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOBILE_PHONE_CALL_ONGOING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOBILE_PHONE_CALL_ONGOING);
```

**返回值:** `BOOL` → `bool`

---

### IS_MOBILE_INTERFERENCE_ACTIVE

**Hash:** `0xC8B1B2425604CDD0` | **JHash:** `0x16FB88B5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOBILE_INTERFERENCE_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOBILE_INTERFERENCE_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### GET_CURRENT_TV_SHOW_PLAY_TIME

**Hash:** `0xDD3AA743AB7D4D75` | **Build:** `3095`

**C++ 原型:**
```cpp
int GET_CURRENT_TV_SHOW_PLAY_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_TV_SHOW_PLAY_TIME);
```

**返回值:** `int` → `int`

---

### CREATE_NEW_SCRIPTED_CONVERSATION

**Hash:** `0xD2C91A0B572AAE56` | **JHash:** `0xB2BC25F8` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_NEW_SCRIPTED_CONVERSATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_NEW_SCRIPTED_CONVERSATION);
```

**返回值:** `void` → `void`

---

### ADD_LINE_TO_CONVERSATION

**Hash:** `0xC5EF963405593646` | **JHash:** `0x96CD0513` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_LINE_TO_CONVERSATION(int index, const char* p1, const char* p2, int p3, int p4, BOOL p5, BOOL p6, BOOL p7, BOOL p8, int p9, BOOL p10, BOOL p11, BOOL p12)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_LINE_TO_CONVERSATION, index, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `int` | `int` | |
| `p10` | `BOOL` | `bool` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** NOTE: ones that are -1, 0 - 35 are determined by a function where it gets a TextLabel from a global then runs,
GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME and depending on what the result is it goes in check order of 0 - 9 then A - Z then z (lowercase). So it will then return 0 - 35 or -1 if it's 'z'. The func to handle that ^^ is func_67 in dialog_handler.c atleast in TU27 Xbox360 scripts.

p0 is -1, 0 - 35
p1 is a char or string (whatever you wanna call it)
p2 is Global 10597 + i * 6. 'i' is a while(i < 70) loop
p3 is again -1, 0 - 35 
p4 is again -1, 0 - 35 
p5 is either 0 or 1 (bool ?)
p6 is either 0 or 1 (The func to determine this is bool)
p7 is either 0 or 1 (The func to determine this is bool)
p8 is either 0 or 1 (The func to determine this is bool)
p9 is 0 - 3 (Determined by func_60 in dialogue_handler.c)
p10 is either 0 or 1 (The func to determine this is bool)
p11 is either 0 or 1 (The func to determine this is bool)
p12 is unknown as in TU27 X360 scripts it only goes to p11.

---

### ADD_PED_TO_CONVERSATION

**Hash:** `0x95D9F4BC443956E7` | **JHash:** `0xF8D5EB86` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PED_TO_CONVERSATION(int index, Ped ped, const char* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PED_TO_CONVERSATION, index, ped, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `ped` | `Ped` | `int` | |
| `p2` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** 4 calls in the b617d scripts. The only one with p0 and p2 in clear text:

AUDIO::ADD_PED_TO_CONVERSATION(5, l_AF, "DINAPOLI");

=================================================
One of the 2 calls in dialogue_handler.c p0 is in a while-loop, and so is determined to also possibly be 0 - 15.

---

### SET_POSITION_FOR_NULL_CONV_PED

**Hash:** `0x33E3C6C6F2F0B506` | **JHash:** `0x73C6F979` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POSITION_FOR_NULL_CONV_PED(Any p0, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POSITION_FOR_NULL_CONV_PED, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_FOR_NULL_CONV_PED

**Hash:** `0x892B6AB8F33606F5` | **JHash:** `0x88203DDA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_FOR_NULL_CONV_PED(int p0, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_FOR_NULL_CONV_PED, p0, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_MICROPHONE_POSITION

**Hash:** `0xB6AE90EDDE95C762` | **JHash:** `0xAD7BB191` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MICROPHONE_POSITION(BOOL toggle, float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MICROPHONE_POSITION, toggle, x1, y1, z1, x2, y2, z2, x3, y3, z3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native controls where the game plays audio from. By default the microphone is positioned on the player.
When p0 is true the game will play audio from the 3 positions inputted.
It is recommended to set all 3 positions to the same value as mixing different positions doesn't seem to work well.
The scripts mostly use it with only one position such as in fbi3.c: 
AUDIO::SET_MICROPHONE_POSITION(true, ENTITY::GET_ENTITY_COORDS(iLocal_3091, true), ENTITY::GET_ENTITY_COORDS(iLocal_3091, true), ENTITY::GET_ENTITY_COORDS(iLocal_3091, true));

---

### SET_CONVERSATION_AUDIO_CONTROLLED_BY_ANIM

**Hash:** `0x0B568201DD99F0EB` | **JHash:** `0x1193ED6E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONVERSATION_AUDIO_CONTROLLED_BY_ANIM(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONVERSATION_AUDIO_CONTROLLED_BY_ANIM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CONVERSATION_AUDIO_PLACEHOLDER

**Hash:** `0x61631F5DF50D1C34` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONVERSATION_AUDIO_PLACEHOLDER(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONVERSATION_AUDIO_PLACEHOLDER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_SCRIPT_PHONE_CONVERSATION

**Hash:** `0x252E5F915EABB675` | **JHash:** `0x38E42D07` | **Build:** `323`

**C++ 原型:**
```cpp
void START_SCRIPT_PHONE_CONVERSATION(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_SCRIPT_PHONE_CONVERSATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PRELOAD_SCRIPT_PHONE_CONVERSATION

**Hash:** `0x6004BCB0E226AAEA` | **JHash:** `0x9ACB213A` | **Build:** `323`

**C++ 原型:**
```cpp
void PRELOAD_SCRIPT_PHONE_CONVERSATION(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_SCRIPT_PHONE_CONVERSATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_SCRIPT_CONVERSATION

**Hash:** `0x6B17C62C9635D2DC` | **JHash:** `0xE5DE7D9D` | **Build:** `323`

**C++ 原型:**
```cpp
void START_SCRIPT_CONVERSATION(BOOL p0, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_SCRIPT_CONVERSATION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PRELOAD_SCRIPT_CONVERSATION

**Hash:** `0x3B3CAD6166916D87` | **JHash:** `0xDDF5C579` | **Build:** `323`

**C++ 原型:**
```cpp
void PRELOAD_SCRIPT_CONVERSATION(BOOL p0, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_SCRIPT_CONVERSATION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_PRELOADED_CONVERSATION

**Hash:** `0x23641AFE870AF385` | **JHash:** `0xA170261B` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PRELOADED_CONVERSATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PRELOADED_CONVERSATION);
```

**返回值:** `void` → `void`

---

### GET_IS_PRELOADED_CONVERSATION_READY

**Hash:** `0xE73364DB90778FFA` | **JHash:** `0x336F3D35` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PRELOADED_CONVERSATION_READY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PRELOADED_CONVERSATION_READY);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCRIPTED_CONVERSATION_ONGOING

**Hash:** `0x16754C556D2EDE3D` | **JHash:** `0xCB8FD96F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_CONVERSATION_ONGOING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_CONVERSATION_ONGOING);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCRIPTED_CONVERSATION_LOADED

**Hash:** `0xDF0D54BE7A776737` | **JHash:** `0xE1870EA9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_CONVERSATION_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_CONVERSATION_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### GET_CURRENT_SCRIPTED_CONVERSATION_LINE

**Hash:** `0x480357EE890C295A` | **JHash:** `0x9620E41F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_SCRIPTED_CONVERSATION_LINE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_SCRIPTED_CONVERSATION_LINE);
```

**返回值:** `int` → `int`

---

### PAUSE_SCRIPTED_CONVERSATION

**Hash:** `0x8530AD776CD72B12` | **JHash:** `0xE2C9C6F8` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_SCRIPTED_CONVERSATION(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_SCRIPTED_CONVERSATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RESTART_SCRIPTED_CONVERSATION

**Hash:** `0x9AEB285D1818C9AC` | **JHash:** `0x6CB24B56` | **Build:** `323`

**C++ 原型:**
```cpp
void RESTART_SCRIPTED_CONVERSATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESTART_SCRIPTED_CONVERSATION);
```

**返回值:** `void` → `void`

---

### STOP_SCRIPTED_CONVERSATION

**Hash:** `0xD79DEEFB53455EBA` | **JHash:** `0xAB77DA7D` | **Build:** `323`

**C++ 原型:**
```cpp
int STOP_SCRIPTED_CONVERSATION(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STOP_SCRIPTED_CONVERSATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### SKIP_TO_NEXT_SCRIPTED_CONVERSATION_LINE

**Hash:** `0x9663FE6B7A61EB00` | **JHash:** `0x85C98304` | **Build:** `323`

**C++ 原型:**
```cpp
void SKIP_TO_NEXT_SCRIPTED_CONVERSATION_LINE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SKIP_TO_NEXT_SCRIPTED_CONVERSATION_LINE);
```

**返回值:** `void` → `void`

---

### INTERRUPT_CONVERSATION

**Hash:** `0xA018A12E5C5C2FA6` | **JHash:** `0xF3A67AF3` | **Build:** `323`

**C++ 原型:**
```cpp
void INTERRUPT_CONVERSATION(Ped ped, const char* voiceline, const char* speaker)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INTERRUPT_CONVERSATION, ped, voiceline, speaker);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `voiceline` | `const char*` | `string` | |
| `speaker` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Example from carsteal3.c: AUDIO::INTERRUPT_CONVERSATION(PLAYER::PLAYER_PED_ID(), "CST4_CFAA", "FRANKLIN");
Voicelines can be found in GTAV\x64\audio\sfx in files starting with "SS_" which seems to mean scripted speech.

---

### INTERRUPT_CONVERSATION_AND_PAUSE

**Hash:** `0x8A694D7A68F8DC38` | **JHash:** `0xDD4A3F1F` | **Build:** `323`

**C++ 原型:**
```cpp
void INTERRUPT_CONVERSATION_AND_PAUSE(Ped ped, const char* p1, const char* speaker)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INTERRUPT_CONVERSATION_AND_PAUSE, ped, p1, speaker);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `const char*` | `string` | |
| `speaker` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** One call found in the b617d scripts:

AUDIO::INTERRUPT_CONVERSATION_AND_PAUSE(NETWORK::NET_TO_PED(l_3989._f26F[0/*1*/]), "CONV_INTERRUPT_QUIT_IT", "LESTER");

---

### GET_VARIATION_CHOSEN_FOR_SCRIPTED_LINE

**Hash:** `0xAA19F5572C38B564` | **JHash:** `0xB58B8FF3` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VARIATION_CHOSEN_FOR_SCRIPTED_LINE(Any* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VARIATION_CHOSEN_FOR_SCRIPTED_LINE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### SET_NO_DUCKING_FOR_CONVERSATION

**Hash:** `0xB542DE8C3D1CB210` | **JHash:** `0x789D8C6C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NO_DUCKING_FOR_CONVERSATION(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NO_DUCKING_FOR_CONVERSATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REGISTER_SCRIPT_WITH_AUDIO

**Hash:** `0xC6ED9D5092438D91` | **JHash:** `0xA6203643` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_SCRIPT_WITH_AUDIO(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_SCRIPT_WITH_AUDIO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### UNREGISTER_SCRIPT_WITH_AUDIO

**Hash:** `0xA8638BE228D4751A` | **JHash:** `0x66728EFE` | **Build:** `323`

**C++ 原型:**
```cpp
void UNREGISTER_SCRIPT_WITH_AUDIO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNREGISTER_SCRIPT_WITH_AUDIO);
```

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### REQUEST_MISSION_AUDIO_BANK

**Hash:** `0x7345BDD95E62E0F2` | **JHash:** `0x916E37CA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_MISSION_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_MISSION_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** All occurrences and usages found in b617d: https://pastebin.com/NzZZ2Tmm
Full list of mission audio bank names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/missionAudioBankNames.json
p2 is always -1

---

### REQUEST_AMBIENT_AUDIO_BANK

**Hash:** `0xFE02FFBED8CA9D99` | **JHash:** `0x23C88BC7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_AMBIENT_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_AMBIENT_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** All occurrences and usages found in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/XZ1tmGEz
Full list of ambient audio bank names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientAudioBankNames.json
p2 is always -1

---

### REQUEST_SCRIPT_AUDIO_BANK

**Hash:** `0x2F844A8B08D76685` | **JHash:** `0x21322887` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_SCRIPT_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_SCRIPT_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** All occurrences and usages found in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/AkmDAVn6
Full list of script audio bank names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scriptAudioBankNames.json
p2 is always -1

---

### HINT_MISSION_AUDIO_BANK

**Hash:** `0x40763EA7B9B783E7` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL HINT_MISSION_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HINT_MISSION_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 is always -1

---

### HINT_AMBIENT_AUDIO_BANK

**Hash:** `0x8F8C0E370AE62F5C` | **JHash:** `0xF1850DDC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HINT_AMBIENT_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HINT_AMBIENT_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 is always -1

---

### HINT_SCRIPT_AUDIO_BANK

**Hash:** `0xFB380A29641EC31A` | **JHash:** `0x41FA0E51` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HINT_SCRIPT_AUDIO_BANK(const char* audioBank, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HINT_SCRIPT_AUDIO_BANK, audioBank, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 is always -1

---

### RELEASE_MISSION_AUDIO_BANK

**Hash:** `0x0EC92A1BF0857187` | **JHash:** `0x8E8824C7` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_MISSION_AUDIO_BANK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_MISSION_AUDIO_BANK);
```

**返回值:** `void` → `void`

---

### RELEASE_AMBIENT_AUDIO_BANK

**Hash:** `0x65475A218FFAA93D` | **JHash:** `0x8C938784` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_AMBIENT_AUDIO_BANK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_AMBIENT_AUDIO_BANK);
```

**返回值:** `void` → `void`

---

### RELEASE_NAMED_SCRIPT_AUDIO_BANK

**Hash:** `0x77ED170667F50170` | **JHash:** `0x16707ABC` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_NAMED_SCRIPT_AUDIO_BANK(const char* audioBank)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_NAMED_SCRIPT_AUDIO_BANK, audioBank);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of script audio bank names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scriptAudioBankNames.json

---

### RELEASE_SCRIPT_AUDIO_BANK

**Hash:** `0x7A2D8AD0A9EB9C3F` | **JHash:** `0x22F865E5` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_SCRIPT_AUDIO_BANK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_SCRIPT_AUDIO_BANK);
```

**返回值:** `void` → `void`

---

### UNHINT_AMBIENT_AUDIO_BANK

**Hash:** `0x19AF7ED9B9D23058` | **JHash:** `0xA58BBF4F` | **Build:** `323`

**C++ 原型:**
```cpp
void UNHINT_AMBIENT_AUDIO_BANK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNHINT_AMBIENT_AUDIO_BANK);
```

**返回值:** `void` → `void`

---

### UNHINT_SCRIPT_AUDIO_BANK

**Hash:** `0x9AC92EED5E4793AB` | **JHash:** `0xE67446E9` | **Build:** `323`

**C++ 原型:**
```cpp
void UNHINT_SCRIPT_AUDIO_BANK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNHINT_SCRIPT_AUDIO_BANK);
```

**返回值:** `void` → `void`

---

### UNHINT_NAMED_SCRIPT_AUDIO_BANK

**Hash:** `0x11579D940949C49E` | **Build:** `678`

**C++ 原型:**
```cpp
void UNHINT_NAMED_SCRIPT_AUDIO_BANK(const char* audioBank)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNHINT_NAMED_SCRIPT_AUDIO_BANK, audioBank);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioBank` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### GET_SOUND_ID

**Hash:** `0x430386FE9BF80B45` | **JHash:** `0x6AE0AD56` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SOUND_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SOUND_ID);
```

**返回值:** `int` → `int`

---

### RELEASE_SOUND_ID

**Hash:** `0x353FC880830B88FA` | **JHash:** `0x9C080899` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_SOUND_ID(int soundId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_SOUND_ID, soundId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAY_SOUND

**Hash:** `0x7FF4944CC209192D` | **JHash:** `0xB6E1917F` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_SOUND(int soundId, const char* audioName, const char* audioRef, BOOL p3, Any p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_SOUND, soundId, audioName, audioRef, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `audioName` | `const char*` | `string` | |
| `audioRef` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/A8Ny8AHZ

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### PLAY_SOUND_FRONTEND

**Hash:** `0x67C540AA08E4A6F5` | **JHash:** `0x2E458F74` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_SOUND_FRONTEND(int soundId, const char* audioName, const char* audioRef, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_SOUND_FRONTEND, soundId, audioName, audioRef, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `audioName` | `const char*` | `string` | |
| `audioRef` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** List: https://pastebin.com/DCeRiaLJ

All occurrences as of Cayo Perico Heist DLC (b2189), sorted alphabetically and identical lines removed: https://git.io/JtLxM

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### PLAY_DEFERRED_SOUND_FRONTEND

**Hash:** `0xCADA5A0D0702381E` | **JHash:** `0xC70E6CFA` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_DEFERRED_SOUND_FRONTEND(const char* soundName, const char* soundsetName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_DEFERRED_SOUND_FRONTEND, soundName, soundsetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundName` | `const char*` | `string` | |
| `soundsetName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Only call found in the b617d scripts:

AUDIO::PLAY_DEFERRED_SOUND_FRONTEND("BACK", "HUD_FREEMODE_SOUNDSET");

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### PLAY_SOUND_FROM_ENTITY

**Hash:** `0xE65F427EB70AB1ED` | **JHash:** `0x95AE00F8` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_SOUND_FROM_ENTITY(int soundId, const char* audioName, Entity entity, const char* audioRef, BOOL isNetwork, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_SOUND_FROM_ENTITY, soundId, audioName, entity, audioRef, isNetwork, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `audioName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `audioRef` | `const char*` | `string` | |
| `isNetwork` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/f2A7vTj0 
No changes made in b678d.

gtaforums.com/topic/795622-audio-for-mods

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### PLAY_SOUND_FROM_ENTITY_HASH

**Hash:** `0x5B9853296731E88D` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAY_SOUND_FROM_ENTITY_HASH(int soundId, Hash model, Entity entity, Hash soundSetHash, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_SOUND_FROM_ENTITY_HASH, soundId, model, entity, soundSetHash, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `model` | `Hash` | `uint` | |
| `entity` | `Entity` | `int` | |
| `soundSetHash` | `Hash` | `uint` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Only used with "formation_flying_blips_soundset" and "biker_formation_blips_soundset".
p1 is always the model of p2

---

### PLAY_SOUND_FROM_COORD

**Hash:** `0x8D8686B622B88120` | **JHash:** `0xCAD3E2D5` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_SOUND_FROM_COORD(int soundId, const char* audioName, float x, float y, float z, const char* audioRef, BOOL isNetwork, int range, BOOL p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_SOUND_FROM_COORD, soundId, audioName, x, y, z, audioRef, isNetwork, range, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `audioName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `audioRef` | `const char*` | `string` | |
| `isNetwork` | `BOOL` | `bool` | |
| `range` | `int` | `int` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/eeFc5DiW

gtaforums.com/topic/795622-audio-for-mods

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### UPDATE_SOUND_COORD

**Hash:** `0x7EC3C679D0E7E46B` | **Build:** `678`

**C++ 原型:**
```cpp
void UPDATE_SOUND_COORD(int soundId, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_SOUND_COORD, soundId, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_SOUND

**Hash:** `0xA3B0C41BA5CC0BB5` | **JHash:** `0xCD7F4030` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SOUND(int soundId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SOUND, soundId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_NETWORK_ID_FROM_SOUND_ID

**Hash:** `0x2DE3F0A134FFBC0D` | **JHash:** `0x2576F610` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NETWORK_ID_FROM_SOUND_ID(int soundId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NETWORK_ID_FROM_SOUND_ID, soundId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Could this be used alongside either, 
SET_NETWORK_ID_EXISTS_ON_ALL_MACHINES or _SET_NETWORK_ID_SYNC_TO_PLAYER to make it so other players can hear the sound while online? It'd be a bit troll-fun to be able to play the Zancudo UFO creepy sounds globally.

---

### GET_SOUND_ID_FROM_NETWORK_ID

**Hash:** `0x75262FD12D0A1C84` | **JHash:** `0xD064D4DC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SOUND_ID_FROM_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SOUND_ID_FROM_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_VARIABLE_ON_SOUND

**Hash:** `0xAD6B3148A78AE9B6` | **JHash:** `0x606EE5FA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VARIABLE_ON_SOUND(int soundId, const char* variable, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VARIABLE_ON_SOUND, soundId, variable, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |
| `variable` | `const char*` | `string` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VARIABLE_ON_STREAM

**Hash:** `0x2F9D3834AEB9EF79` | **JHash:** `0xF67BB44C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VARIABLE_ON_STREAM(const char* variable, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VARIABLE_ON_STREAM, variable, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variable` | `const char*` | `string` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** From the scripts, p0:

"ArmWrestlingIntensity",
"INOUT",
"Monkey_Stream",
"ZoomLevel"

---

### OVERRIDE_UNDERWATER_STREAM

**Hash:** `0xF2A9CDABCEA04BD6` | **JHash:** `0x9A083B7E` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_UNDERWATER_STREAM(const char* p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_UNDERWATER_STREAM, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VARIABLE_ON_UNDER_WATER_STREAM

**Hash:** `0x733ADF241531E5C2` | **JHash:** `0x62D026BE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VARIABLE_ON_UNDER_WATER_STREAM(const char* variableName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VARIABLE_ON_UNDER_WATER_STREAM, variableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variableName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** AUDIO::SET_VARIABLE_ON_UNDER_WATER_STREAM("inTunnel", 1.0);
AUDIO::SET_VARIABLE_ON_UNDER_WATER_STREAM("inTunnel", 0.0);

---

### HAS_SOUND_FINISHED

**Hash:** `0xFCBDCE714A7C88E5` | **JHash:** `0xE85AEC2E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SOUND_FINISHED(int soundId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SOUND_FINISHED, soundId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `soundId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLAY_PED_AMBIENT_SPEECH_NATIVE

**Hash:** `0x8E04FEDD28D42462` | **JHash:** `0x5C57B85D` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_PED_AMBIENT_SPEECH_NATIVE(Ped ped, const char* speechName, const char* speechParam, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PED_AMBIENT_SPEECH_NATIVE, ped, speechName, speechParam, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speechName` | `const char*` | `string` | |
| `speechParam` | `const char*` | `string` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Plays ambient speech. See also _0x444180DB.

ped: The ped to play the ambient speech.
speechName: Name of the speech to play, eg. "GENERIC_HI".
speechParam: Can be one of the following:
SPEECH_PARAMS_STANDARD
SPEECH_PARAMS_ALLOW_REPEAT
SPEECH_PARAMS_BEAT
SPEECH_PARAMS_FORCE
SPEECH_PARAMS_FORCE_FRONTEND
SPEECH_PARAMS_FORCE_NO_REPEAT_FRONTEND
SPEECH_PARAMS_FORCE_NORMAL
SPEECH_PARAMS_FORCE_NORMAL_CLEAR
SPEECH_PARAMS_FORCE_NORMAL_CRITICAL
SPEECH_PARAMS_FORCE_SHOUTED
SPEECH_PARAMS_FORCE_SHOUTED_CLEAR
SPEECH_PARAMS_FORCE_SHOUTED_CRITICAL
SPEECH_PARAMS_FORCE_PRELOAD_ONLY
SPEECH_PARAMS_MEGAPHONE
SPEECH_PARAMS_HELI
SPEECH_PARAMS_FORCE_MEGAPHONE
SPEECH_PARAMS_FORCE_HELI
SPEECH_PARAMS_INTERRUPT
SPEECH_PARAMS_INTERRUPT_SHOUTED
SPEECH_PARAMS_INTERRUPT_SHOUTED_CLEAR
SPEECH_PARAMS_INTERRUPT_SHOUTED_CRITICAL
SPEECH_PARAMS_INTERRUPT_NO_FORCE
SPEECH_PARAMS_INTERRUPT_FRONTEND
SPEECH_PARAMS_INTERRUPT_NO_FORCE_FRONTEND
SPEECH_PARAMS_ADD_BLIP
SPEECH_PARAMS_ADD_BLIP_ALLOW_REPEAT
SPEECH_PARAMS_ADD_BLIP_FORCE
SPEECH_PARAMS_ADD_BLIP_SHOUTED
SPEECH_PARAMS_ADD_BLIP_SHOUTED_FORCE
SPEECH_PARAMS_ADD_BLIP_INTERRUPT
SPEECH_PARAMS_ADD_BLIP_INTERRUPT_FORCE
SPEECH_PARAMS_FORCE_PRELOAD_ONLY_SHOUTED
SPEECH_PARAMS_FORCE_PRELOAD_ONLY_SHOUTED_CLEAR
SPEECH_PARAMS_FORCE_PRELOAD_ONLY_SHOUTED_CRITICAL
SPEECH_PARAMS_SHOUTED
SPEECH_PARAMS_SHOUTED_CLEAR
SPEECH_PARAMS_SHOUTED_CRITICAL

Note: A list of Name and Parameters can be found here https://pastebin.com/1GZS5dCL

Full list of speeches and voices names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/speeches.json

---

### PLAY_PED_AMBIENT_SPEECH_AND_CLONE_NATIVE

**Hash:** `0xC6941B4A3A8FBBB9` | **JHash:** `0x444180DB` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_PED_AMBIENT_SPEECH_AND_CLONE_NATIVE(Ped ped, const char* speechName, const char* speechParam, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PED_AMBIENT_SPEECH_AND_CLONE_NATIVE, ped, speechName, speechParam, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speechName` | `const char*` | `string` | |
| `speechParam` | `const char*` | `string` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Plays ambient speech. See also _0x5C57B85D.

See PLAY_PED_AMBIENT_SPEECH_NATIVE for parameter specifications.

Full list of speeches and voices names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/speeches.json

---

### PLAY_PED_AMBIENT_SPEECH_WITH_VOICE_NATIVE

**Hash:** `0x3523634255FC3318` | **JHash:** `0x8386AE28` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_PED_AMBIENT_SPEECH_WITH_VOICE_NATIVE(Ped ped, const char* speechName, const char* voiceName, const char* speechParam, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PED_AMBIENT_SPEECH_WITH_VOICE_NATIVE, ped, speechName, voiceName, speechParam, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speechName` | `const char*` | `string` | |
| `voiceName` | `const char*` | `string` | |
| `speechParam` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This is the same as PLAY_PED_AMBIENT_SPEECH_NATIVE and PLAY_PED_AMBIENT_SPEECH_AND_CLONE_NATIVE but it will allow you to play a speech file from a specific voice file. It works on players and all peds, even animals.

EX (C#):
GTA.Native.Function.Call(Hash.PLAY_PED_AMBIENT_SPEECH_WITH_VOICE_NATIVE, Game.Player.Character, "GENERIC_INSULT_HIGH", "s_m_y_sheriff_01_white_full_01", "SPEECH_PARAMS_FORCE_SHOUTED", 0);

The first param is the ped you want to play it on, the second is the speech name, the third is the voice name, the fourth is the speech param, and the last param is usually always 0.

Full list of speeches and voices names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/speeches.json

---

### PLAY_AMBIENT_SPEECH_FROM_POSITION_NATIVE

**Hash:** `0xED640017ED337E45` | **JHash:** `0xA1A1402E` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_AMBIENT_SPEECH_FROM_POSITION_NATIVE(const char* speechName, const char* voiceName, float x, float y, float z, const char* speechParam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_AMBIENT_SPEECH_FROM_POSITION_NATIVE, speechName, voiceName, x, y, z, speechParam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `speechName` | `const char*` | `string` | |
| `voiceName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `speechParam` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of speeches and voices names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/speeches.json

---

### OVERRIDE_TREVOR_RAGE

**Hash:** `0x13AD665062541A7E` | **JHash:** `0x05B9B5CF` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_TREVOR_RAGE(const char* voiceEffect)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_TREVOR_RAGE, voiceEffect);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `voiceEffect` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** This native enables the audio flag "TrevorRageIsOverridden" and sets the voice effect to `voiceEffect`

---

### RESET_TREVOR_RAGE

**Hash:** `0xE78503B10C4314E0` | **JHash:** `0xE80CF0D4` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_TREVOR_RAGE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_TREVOR_RAGE);
```

**返回值:** `void` → `void`

---

### SET_PLAYER_ANGRY

**Hash:** `0xEA241BB04110F091` | **JHash:** `0x782CA58D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_ANGRY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_ANGRY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAY_PAIN

**Hash:** `0xBC9AE166038A5CEC` | **JHash:** `0x874BD6CB` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_PAIN(Ped ped, int painID, int p1, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PAIN, ped, painID, p1, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `painID` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Needs another parameter [int p2]. The signature is PED::PLAY_PAIN(Ped ped, int painID, int p1, int p2);

Last 2 parameters always seem to be 0.

EX: Function.Call(Hash.PLAY_PAIN, TestPed, 6, 0, 0);

Known Pain IDs
________________________

1 - Doesn't seem to do anything. Does NOT crash the game like previously said. (Latest patch)
6 - Scream (Short)
7 - Scared Scream (Kinda Long)
8 - On Fire


---

### RELEASE_WEAPON_AUDIO

**Hash:** `0xCE4AC0439F607045` | **JHash:** `0x6096504C` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_WEAPON_AUDIO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_WEAPON_AUDIO);
```

**返回值:** `void` → `void`

---

### ACTIVATE_AUDIO_SLOWMO_MODE

**Hash:** `0xD01005D2BA2EB778` | **JHash:** `0x59A3A17D` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_AUDIO_SLOWMO_MODE(const char* mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_AUDIO_SLOWMO_MODE, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mode` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** mode can be any of these: 
SLOWMO_T1_TRAILER_SMASH
SLOWMO_T1_RAYFIRE_EXPLOSION
SLOWMO_PROLOGUE_VAULT
NIGEL_02_SLOWMO_SETTING
JSH_EXIT_TUNNEL_SLOWMO
SLOWMO_BIG_SCORE_JUMP
SLOWMO_FIB4_TRUCK_SMASH
SLOWMO_EXTREME_04
SLOW_MO_METH_HOUSE_RAYFIRE
BARRY_02_SLOWMO
BARRY_01_SLOWMO

---

### DEACTIVATE_AUDIO_SLOWMO_MODE

**Hash:** `0xDDC635D5B3262C56` | **JHash:** `0x0E387BFE` | **Build:** `323`

**C++ 原型:**
```cpp
void DEACTIVATE_AUDIO_SLOWMO_MODE(const char* mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DEACTIVATE_AUDIO_SLOWMO_MODE, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mode` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** see ACTIVATE_AUDIO_SLOWMO_MODE for modes

---

### SET_AMBIENT_VOICE_NAME

**Hash:** `0x6C8065A3B780185B` | **JHash:** `0xBD2EA1A1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_VOICE_NAME(Ped ped, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_VOICE_NAME, ped, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Audio List
gtaforums.com/topic/795622-audio-for-mods/

All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/FTeAj4yZ

---

### SET_AMBIENT_VOICE_NAME_HASH

**Hash:** `0x9A53DED9921DE990` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_AMBIENT_VOICE_NAME_HASH(Ped ped, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_VOICE_NAME_HASH, ped, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_AMBIENT_VOICE_NAME_HASH

**Hash:** `0x5E203DA2BA15D436` | **Build:** `463`

**C++ 原型:**
```cpp
Hash GET_AMBIENT_VOICE_NAME_HASH(Ped ped)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_AMBIENT_VOICE_NAME_HASH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Hash` → `uint`

---

### SET_PED_VOICE_FULL

**Hash:** `0x40CF0D12D142A9E8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VOICE_FULL(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VOICE_FULL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Assigns some ambient voice to the ped.

---

### SET_PED_RACE_AND_VOICE_GROUP

**Hash:** `0x1B7ABE26CBCBF8C7` | **Build:** `372`

**C++ 原型:**
```cpp
void SET_PED_RACE_AND_VOICE_GROUP(Ped ped, int p1, Hash voiceGroup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RACE_AND_VOICE_GROUP, ped, p1, voiceGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `voiceGroup` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_PED_VOICE_GROUP

**Hash:** `0x7CDC8C3B89F661B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VOICE_GROUP(Ped ped, Hash voiceGroupHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VOICE_GROUP, ped, voiceGroupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `voiceGroupHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** From the scripts:

AUDIO::SET_PED_VOICE_GROUP(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("PAIGE_PVG"));
AUDIO::SET_PED_VOICE_GROUP(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("TALINA_PVG"));
AUDIO::SET_PED_VOICE_GROUP(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("FEMALE_LOST_BLACK_PVG"));
AUDIO::SET_PED_VOICE_GROUP(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("FEMALE_LOST_WHITE_PVG"));

---

### SET_PED_VOICE_GROUP_FROM_RACE_TO_PVG

**Hash:** `0x0BABC1345ABBFB16` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_PED_VOICE_GROUP_FROM_RACE_TO_PVG(Ped ped, Hash voiceGroupHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VOICE_GROUP_FROM_RACE_TO_PVG, ped, voiceGroupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `voiceGroupHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Dat151RelType == 29

---

### SET_PED_GENDER

**Hash:** `0xA5342D390CDA41D6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GENDER(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GENDER, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** BOOL p1: 0 = Female; 1 = Male

---

### STOP_CURRENT_PLAYING_SPEECH

**Hash:** `0x7A73D05A607734C7` | **JHash:** `0x5B7463F4` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CURRENT_PLAYING_SPEECH(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CURRENT_PLAYING_SPEECH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### STOP_CURRENT_PLAYING_AMBIENT_SPEECH

**Hash:** `0xB8BEC0CA6F0EDB0F` | **JHash:** `0xBB8E64BF` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CURRENT_PLAYING_AMBIENT_SPEECH(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CURRENT_PLAYING_AMBIENT_SPEECH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_AMBIENT_SPEECH_PLAYING

**Hash:** `0x9072C8B49907BFAD` | **JHash:** `0x1972E8AA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AMBIENT_SPEECH_PLAYING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AMBIENT_SPEECH_PLAYING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_SCRIPTED_SPEECH_PLAYING

**Hash:** `0xCC9AA18DCC7084F4` | **JHash:** `0x2C653904` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_SPEECH_PLAYING(Ped p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_SPEECH_PLAYING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ANY_SPEECH_PLAYING

**Hash:** `0x729072355FA39EC9` | **JHash:** `0x2B74A6D6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_SPEECH_PLAYING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_SPEECH_PLAYING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ANY_POSITIONAL_SPEECH_PLAYING

**Hash:** `0x30CA2EF91D15ADF8` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL IS_ANY_POSITIONAL_SPEECH_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_POSITIONAL_SPEECH_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### DOES_CONTEXT_EXIST_FOR_THIS_PED

**Hash:** `0x49B99BF3FDA89A7A` | **JHash:** `0x8BD5F11E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_CONTEXT_EXIST_FOR_THIS_PED(Ped ped, const char* speechName, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CONTEXT_EXIST_FOR_THIS_PED, ped, speechName, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speechName` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the ped can play the speech or has the speech file, p2 is usually false.

---

### IS_PED_IN_CURRENT_CONVERSATION

**Hash:** `0x049E937F18F4020C` | **JHash:** `0x7B2F0743` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_CURRENT_CONVERSATION(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_CURRENT_CONVERSATION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_IS_DRUNK

**Hash:** `0x95D2D383D5396B8A` | **JHash:** `0xD2EA77A3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_IS_DRUNK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_IS_DRUNK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the ped drunk sounds.  Only works with PLAYER_PED_ID

====================================================

As mentioned above, this only sets the drunk sound to ped/player.

To give the Ped a drunk effect with drunk walking animation try using SET_PED_MOVEMENT_CLIPSET

Below is an example

if (!Function.Call<bool>(Hash.HAS_ANIM_SET_LOADED, "move_m@drunk@verydrunk"))
                {
                    Function.Call(Hash.REQUEST_ANIM_SET, "move_m@drunk@verydrunk");
                }
                Function.Call(Hash.SET_PED_MOVEMENT_CLIPSET, Ped.Handle, "move_m@drunk@verydrunk", 0x3E800000);



And to stop the effect use
RESET_PED_MOVEMENT_CLIPSET

---

### PLAY_ANIMAL_VOCALIZATION

**Hash:** `0xEE066C7006C49C0A` | **JHash:** `0x498849F3` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_ANIMAL_VOCALIZATION(Ped pedHandle, int p1, const char* speechName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_ANIMAL_VOCALIZATION, pedHandle, p1, speechName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedHandle` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `speechName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Plays sounds from a ped with chop model. For example it used to play bark or sniff sounds. p1 is always 3 or 4294967295 in decompiled scripts. By a quick disassembling I can assume that this arg is unused.
This native is works only when you call it on the ped with right model (ac_chop only ?)
Speech Name can be: CHOP_SNIFF_SEQ CHOP_WHINE CHOP_LICKS_MOUTH CHOP_PANT bark GROWL SNARL BARK_SEQ

---

### IS_ANIMAL_VOCALIZATION_PLAYING

**Hash:** `0xC265DF9FB44A9FBD` | **JHash:** `0x0CBAF2EF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANIMAL_VOCALIZATION_PLAYING(Ped pedHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANIMAL_VOCALIZATION_PLAYING, pedHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedHandle` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ANIMAL_MOOD

**Hash:** `0xCC97B29285B1DC3B` | **JHash:** `0x3EA7C6CB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ANIMAL_MOOD(Ped animal, int mood)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ANIMAL_MOOD, animal, mood);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animal` | `Ped` | `int` | |
| `mood` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** mood can be 0 or 1 (it's not a boolean value!). Effects audio of the animal.

---

### PLAY_PED_AUDIO_EVENT_ANIM

**Hash:** `0xAD2191A6E3543189` | **Build:** `3717`

**C++ 原型:**
```cpp
void PLAY_PED_AUDIO_EVENT_ANIM(Ped pedHandle, const char* audioEvent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_PED_AUDIO_EVENT_ANIM, pedHandle, audioEvent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedHandle` | `Ped` | `int` | |
| `audioEvent` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### IS_MOBILE_PHONE_RADIO_ACTIVE

**Hash:** `0xB35CE999E8EF317E` | **JHash:** `0x6E502A5B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOBILE_PHONE_RADIO_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOBILE_PHONE_RADIO_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_MOBILE_PHONE_RADIO_STATE

**Hash:** `0xBF286C554784F3DF` | **JHash:** `0xE1E0ED34` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOBILE_PHONE_RADIO_STATE(BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_PHONE_RADIO_STATE, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_RADIO_STATION_INDEX

**Hash:** `0xE8AF77C4C06ADC93` | **JHash:** `0x1C4946AC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_RADIO_STATION_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_RADIO_STATION_INDEX);
```

**返回值:** `int` → `int`

**说明:** Returns 255 (radio off index) if the function fails.

---

### GET_PLAYER_RADIO_STATION_NAME

**Hash:** `0xF6D733C32076AD03` | **JHash:** `0xD909C107` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_PLAYER_RADIO_STATION_NAME(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_PLAYER_RADIO_STATION_NAME);
```

**返回值:** `const char*` → `string`

**说明:** Returns active radio station name

---

### GET_RADIO_STATION_NAME

**Hash:** `0xB28ECA15046CA8B9` | **JHash:** `0x3DF493BC` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_RADIO_STATION_NAME(int radioStation)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_RADIO_STATION_NAME, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Converts radio station index to string. Use HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION to get the user-readable text.

---

### GET_PLAYER_RADIO_STATION_GENRE

**Hash:** `0xA571991A7FE6CCEB` | **JHash:** `0x872CF0EA` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_RADIO_STATION_GENRE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_RADIO_STATION_GENRE);
```

**返回值:** `int` → `int`

---

### IS_RADIO_RETUNING

**Hash:** `0xA151A7394A214E65` | **JHash:** `0xCF29097B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_RADIO_RETUNING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_RADIO_RETUNING);
```

**返回值:** `BOOL` → `bool`

---

### IS_RADIO_FADED_OUT

**Hash:** `0x0626A247D2405330` | **JHash:** `0x815CAE99` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_RADIO_FADED_OUT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_RADIO_FADED_OUT);
```

**返回值:** `BOOL` → `bool`

---

### SET_RADIO_RETUNE_UP

**Hash:** `0xFF266D1D0EB1195D` | **JHash:** `0x53DB6994` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_RETUNE_UP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_RETUNE_UP);
```

**返回值:** `void` → `void`

**说明:** Tune Forward...

---

### SET_RADIO_RETUNE_DOWN

**Hash:** `0xDD6BCF9E94425DF9` | **JHash:** `0xD70ECC80` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_RETUNE_DOWN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_RETUNE_DOWN);
```

**返回值:** `void` → `void`

**说明:** Tune Backwards...

---

### SET_RADIO_TO_STATION_NAME

**Hash:** `0xC69EDA28699D5107` | **JHash:** `0x7B36E35E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_TO_STATION_NAME(const char* stationName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_TO_STATION_NAME, stationName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `stationName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** List of radio stations that are in the wheel, in clockwise order, as of LS Tuners DLC: https://git.io/J8a3k
An older list including hidden radio stations: https://pastebin.com/Kj9t38KF

---

### SET_VEH_RADIO_STATION

**Hash:** `0x1B9C0099CB942AC6` | **JHash:** `0xE391F55F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEH_RADIO_STATION(Vehicle vehicle, const char* radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEH_RADIO_STATION, vehicle, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `radioStation` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** List of radio stations that are in the wheel, in clockwise order, as of LS Tuners DLC: https://git.io/J8a3k
An older list including hidden radio stations: https://pastebin.com/Kj9t38KF

---

### SET_VEH_HAS_NORMAL_RADIO

**Hash:** `0x3E45765F3FBB582F` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_VEH_HAS_NORMAL_RADIO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEH_HAS_NORMAL_RADIO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_RADIO_ON

**Hash:** `0x0BE4BE946463F917` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_RADIO_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_RADIO_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEH_FORCED_RADIO_THIS_FRAME

**Hash:** `0xC1805D05E6D4FE10` | **JHash:** `0x7ABB89D2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEH_FORCED_RADIO_THIS_FRAME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEH_FORCED_RADIO_THIS_FRAME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_EMITTER_RADIO_STATION

**Hash:** `0xACF57305B12AF907` | **JHash:** `0x87431585` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EMITTER_RADIO_STATION(const char* emitterName, const char* radioStation, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EMITTER_RADIO_STATION, emitterName, radioStation, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `emitterName` | `const char*` | `string` | |
| `radioStation` | `const char*` | `string` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Full list of static emitters by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/staticEmitters.json

---

### SET_STATIC_EMITTER_ENABLED

**Hash:** `0x399D2D3B33F1B8EB` | **JHash:** `0x91F72E92` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STATIC_EMITTER_ENABLED(const char* emitterName, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STATIC_EMITTER_ENABLED, emitterName, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `emitterName` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example:
AUDIO::SET_STATIC_EMITTER_ENABLED((Any*)"LOS_SANTOS_VANILLA_UNICORN_01_STAGE", false);    AUDIO::SET_STATIC_EMITTER_ENABLED((Any*)"LOS_SANTOS_VANILLA_UNICORN_02_MAIN_ROOM", false);    AUDIO::SET_STATIC_EMITTER_ENABLED((Any*)"LOS_SANTOS_VANILLA_UNICORN_03_BACK_ROOM", false);

This turns off surrounding sounds not connected directly to peds.

Full list of static emitters by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/staticEmitters.json

---

### LINK_STATIC_EMITTER_TO_ENTITY

**Hash:** `0x651D3228960D08AF` | **Build:** `505`

**C++ 原型:**
```cpp
void LINK_STATIC_EMITTER_TO_ENTITY(const char* emitterName, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LINK_STATIC_EMITTER_TO_ENTITY, emitterName, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `emitterName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of static emitters by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/staticEmitters.json

---

### SET_RADIO_TO_STATION_INDEX

**Hash:** `0xA619B168B8A8570F` | **JHash:** `0x1D82766D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_TO_STATION_INDEX(int radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_TO_STATION_INDEX, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets radio station by index.

---

### SET_FRONTEND_RADIO_ACTIVE

**Hash:** `0xF7F26C6E9CC9EBB8` | **JHash:** `0xB1172075` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FRONTEND_RADIO_ACTIVE(BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FRONTEND_RADIO_ACTIVE, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### UNLOCK_MISSION_NEWS_STORY

**Hash:** `0xB165AB7C248B2DC1` | **JHash:** `0xCCD9ABE4` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOCK_MISSION_NEWS_STORY(int newsStory)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOCK_MISSION_NEWS_STORY, newsStory);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `newsStory` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** "news" that play on the radio after you've done something in story mode(?)

---

### IS_MISSION_NEWS_STORY_UNLOCKED

**Hash:** `0x66E49BF55B4B1874` | **JHash:** `0x27305D37` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MISSION_NEWS_STORY_UNLOCKED(int newsStory)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MISSION_NEWS_STORY_UNLOCKED, newsStory);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `newsStory` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_AUDIBLE_MUSIC_TRACK_TEXT_ID

**Hash:** `0x50B196FC9ED6545B` | **JHash:** `0xA2B88CA7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_AUDIBLE_MUSIC_TRACK_TEXT_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_AUDIBLE_MUSIC_TRACK_TEXT_ID);
```

**返回值:** `int` → `int`

---

### PLAY_END_CREDITS_MUSIC

**Hash:** `0xCD536C4D33DCC900` | **JHash:** `0x8E88B3CC` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_END_CREDITS_MUSIC(BOOL play)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_END_CREDITS_MUSIC, play);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `play` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SKIP_RADIO_FORWARD

**Hash:** `0x6DDBBDD98E2E9C25` | **JHash:** `0x10D36630` | **Build:** `323`

**C++ 原型:**
```cpp
void SKIP_RADIO_FORWARD(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SKIP_RADIO_FORWARD);
```

**返回值:** `void` → `void`

---

### FREEZE_RADIO_STATION

**Hash:** `0x344F393B027E38C3` | **JHash:** `0x286BF543` | **Build:** `323`

**C++ 原型:**
```cpp
void FREEZE_RADIO_STATION(const char* radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREEZE_RADIO_STATION, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### UNFREEZE_RADIO_STATION

**Hash:** `0xFC00454CF60B91DD` | **JHash:** `0x4D46202C` | **Build:** `323`

**C++ 原型:**
```cpp
void UNFREEZE_RADIO_STATION(const char* radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNFREEZE_RADIO_STATION, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_RADIO_AUTO_UNFREEZE

**Hash:** `0xC1AA9F53CE982990` | **JHash:** `0xA40196BF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_AUTO_UNFREEZE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_AUTO_UNFREEZE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INITIAL_PLAYER_STATION

**Hash:** `0x88795F13FACDA88D` | **JHash:** `0x9B069233` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INITIAL_PLAYER_STATION(const char* radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INITIAL_PLAYER_STATION, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_USER_RADIO_CONTROL_ENABLED

**Hash:** `0x19F21E63AE6EAE4E` | **JHash:** `0x52E054CE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_USER_RADIO_CONTROL_ENABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USER_RADIO_CONTROL_ENABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_RADIO_TRACK

**Hash:** `0xB39786F201FEE30B` | **JHash:** `0x76E96212` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_TRACK(const char* radioStation, const char* radioTrack)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_TRACK, radioStation, radioTrack);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `radioTrack` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Only found this one in the decompiled scripts:

AUDIO::SET_RADIO_TRACK("RADIO_03_HIPHOP_NEW", "ARM1_RADIO_STARTS");


---

### SET_RADIO_TRACK_WITH_START_OFFSET

**Hash:** `0x2CB0075110BE1E56` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_RADIO_TRACK_WITH_START_OFFSET(const char* radioStationName, const char* mixName, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_TRACK_WITH_START_OFFSET, radioStationName, mixName, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStationName` | `const char*` | `string` | |
| `mixName` | `const char*` | `string` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_NEXT_RADIO_TRACK

**Hash:** `0x55ECF4D13D9903B0` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_NEXT_RADIO_TRACK(const char* radioName, const char* radioTrack, const char* p2, const char* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NEXT_RADIO_TRACK, radioName, radioTrack, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioName` | `const char*` | `string` | |
| `radioTrack` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_RADIO_LOUD

**Hash:** `0xBB6F1CAEC68B0BCE` | **JHash:** `0x8D9EDD99` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_RADIO_LOUD(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_RADIO_LOUD, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CAN_VEHICLE_RECEIVE_CB_RADIO

**Hash:** `0x032A116663A4D5AC` | **JHash:** `0xCBA99F4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_VEHICLE_RECEIVE_CB_RADIO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_VEHICLE_RECEIVE_CB_RADIO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_MOBILE_RADIO_ENABLED_DURING_GAMEPLAY

**Hash:** `0x1098355A16064BB3` | **JHash:** `0x990085F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOBILE_RADIO_ENABLED_DURING_GAMEPLAY(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_RADIO_ENABLED_DURING_GAMEPLAY, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DOES_PLAYER_VEH_HAVE_RADIO

**Hash:** `0x109697E2FFBAC8A1` | **JHash:** `0x46B0C696` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PLAYER_VEH_HAVE_RADIO(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PLAYER_VEH_HAVE_RADIO);
```

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_VEH_RADIO_ENABLE

**Hash:** `0x5F43D83FD6738741` | **JHash:** `0x2A3E5E8B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_VEH_RADIO_ENABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_VEH_RADIO_ENABLE);
```

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_RADIO_ENABLED

**Hash:** `0x3B988190C0AA6C0B` | **JHash:** `0x6F812CAB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_RADIO_ENABLED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_RADIO_ENABLED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** can't seem to enable radio on cop cars etc

---

### SET_POSITIONED_PLAYER_VEHICLE_RADIO_EMITTER_ENABLED

**Hash:** `0xDA07819E452FFE8F` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_POSITIONED_PLAYER_VEHICLE_RADIO_EMITTER_ENABLED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POSITIONED_PLAYER_VEHICLE_RADIO_EMITTER_ENABLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CUSTOM_RADIO_TRACK_LIST

**Hash:** `0x4E404A9361F75BB2` | **JHash:** `0x128C3873` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUSTOM_RADIO_TRACK_LIST(const char* radioStation, const char* trackListName, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUSTOM_RADIO_TRACK_LIST, radioStation, trackListName, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `trackListName` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Examples:

AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "END_CREDITS_KILL_MICHAEL", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "END_CREDITS_KILL_MICHAEL", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "END_CREDITS_KILL_TREVOR", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "END_CREDITS_SAVE_MICHAEL_TREVOR", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "OFF_ROAD_RADIO_ROCK_LIST", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_06_COUNTRY", "MAGDEMO2_RADIO_DINGHY", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_16_SILVERLAKE", "SEA_RACE_RADIO_PLAYLIST", 1);
AUDIO::SET_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK", "OFF_ROAD_RADIO_ROCK_LIST", 1);

---

### CLEAR_CUSTOM_RADIO_TRACK_LIST

**Hash:** `0x1654F24A88A8E3FE` | **JHash:** `0x1D766976` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_CUSTOM_RADIO_TRACK_LIST(const char* radioStation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_CUSTOM_RADIO_TRACK_LIST, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** 3 calls in the b617d scripts, removed duplicate.

AUDIO::CLEAR_CUSTOM_RADIO_TRACK_LIST("RADIO_16_SILVERLAKE");
AUDIO::CLEAR_CUSTOM_RADIO_TRACK_LIST("RADIO_01_CLASS_ROCK");

---

### GET_NUM_UNLOCKED_RADIO_STATIONS

**Hash:** `0xF1620ECB50E01DE7` | **JHash:** `0xCC91FCF5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_UNLOCKED_RADIO_STATIONS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_UNLOCKED_RADIO_STATIONS);
```

**返回值:** `int` → `int`

---

### FIND_RADIO_STATION_INDEX

**Hash:** `0x8D67489793FF428B` | **JHash:** `0xECA1512F` | **Build:** `323`

**C++ 原型:**
```cpp
int FIND_RADIO_STATION_INDEX(Hash stationNameHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.FIND_RADIO_STATION_INDEX, stationNameHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `stationNameHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### SET_RADIO_STATION_MUSIC_ONLY

**Hash:** `0x774BD811F656A122` | **JHash:** `0xB1FF7137` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_STATION_MUSIC_ONLY(const char* radioStation, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_STATION_MUSIC_ONLY, radioStation, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** 6 calls in the b617d scripts, removed identical lines:

AUDIO::SET_RADIO_STATION_MUSIC_ONLY("RADIO_01_CLASS_ROCK", 1);
AUDIO::SET_RADIO_STATION_MUSIC_ONLY(AUDIO::GET_RADIO_STATION_NAME(10), 0);
AUDIO::SET_RADIO_STATION_MUSIC_ONLY(AUDIO::GET_RADIO_STATION_NAME(10), 1);

---

### SET_RADIO_FRONTEND_FADE_TIME

**Hash:** `0x2C96CDB04FCA358E` | **JHash:** `0xC8B514E2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_FRONTEND_FADE_TIME(float fadeTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_FRONTEND_FADE_TIME, fadeTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fadeTime` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UNLOCK_RADIO_STATION_TRACK_LIST

**Hash:** `0x031ACB6ABA18C729` | **JHash:** `0xBE998184` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOCK_RADIO_STATION_TRACK_LIST(const char* radioStation, const char* trackListName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOCK_RADIO_STATION_TRACK_LIST, radioStation, trackListName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `trackListName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** AUDIO::UNLOCK_RADIO_STATION_TRACK_LIST("RADIO_16_SILVERLAKE", "MIRRORPARK_LOCKED");

---

### LOCK_RADIO_STATION_TRACK_LIST

**Hash:** `0xFF5E5EA2DCEEACF3` | **Build:** `2372`

**C++ 原型:**
```cpp
void LOCK_RADIO_STATION_TRACK_LIST(const char* radioStation, const char* trackListName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOCK_RADIO_STATION_TRACK_LIST, radioStation, trackListName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `trackListName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### UPDATE_UNLOCKABLE_DJ_RADIO_TRACKS

**Hash:** `0x47AED84213A47510` | **Build:** `1493`

**C++ 原型:**
```cpp
void UPDATE_UNLOCKABLE_DJ_RADIO_TRACKS(BOOL enableMixes)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_UNLOCKABLE_DJ_RADIO_TRACKS, enableMixes);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `enableMixes` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Just a nullsub (i.e. does absolutely nothing) since build 1604.

---

### LOCK_RADIO_STATION

**Hash:** `0x477D9DB48F889591` | **Build:** `1493`

**C++ 原型:**
```cpp
void LOCK_RADIO_STATION(const char* radioStationName, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOCK_RADIO_STATION, radioStationName, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStationName` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables the radio station (hides it from the radio wheel).

---

### SET_RADIO_STATION_AS_FAVOURITE

**Hash:** `0x4CAFEBFA21EC188D` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_RADIO_STATION_AS_FAVOURITE(const char* radioStation, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_STATION_AS_FAVOURITE, radioStation, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Doesn't have an effect in Story Mode.

---

### IS_RADIO_STATION_FAVOURITED

**Hash:** `0x2B1784DB08AFEA79` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL IS_RADIO_STATION_FAVOURITED(const char* radioStation)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_RADIO_STATION_FAVOURITED, radioStation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### GET_NEXT_AUDIBLE_BEAT

**Hash:** `0xC64A06D939F826F5` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL GET_NEXT_AUDIBLE_BEAT(float* out1, float* out2, int* out3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_NEXT_AUDIBLE_BEAT, out1, out2, out3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `out1` | `float*` | `float*` | |
| `out2` | `float*` | `float*` | |
| `out3` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### FORCE_MUSIC_TRACK_LIST

**Hash:** `0x4E0AF9114608257C` | **Build:** `2372`

**C++ 原型:**
```cpp
void FORCE_MUSIC_TRACK_LIST(const char* radioStation, const char* trackListName, int milliseconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_MUSIC_TRACK_LIST, radioStation, trackListName, milliseconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStation` | `const char*` | `string` | |
| `trackListName` | `const char*` | `string` | |
| `milliseconds` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Changes start time of a tracklist (milliseconds)
R* uses a random int: MISC::GET_RANDOM_INT_IN_RANGE(0, 13) * 60000)

---

### GET_CURRENT_TRACK_PLAY_TIME

**Hash:** `0x3E65CDE5215832C1` | **Build:** `1493`

**C++ 原型:**
```cpp
int GET_CURRENT_TRACK_PLAY_TIME(const char* radioStationName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_TRACK_PLAY_TIME, radioStationName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStationName` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### GET_CURRENT_TRACK_SOUND_NAME

**Hash:** `0x34D66BC058019CE0` | **Build:** `1493`

**C++ 原型:**
```cpp
Hash GET_CURRENT_TRACK_SOUND_NAME(const char* radioStationName)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_CURRENT_TRACK_SOUND_NAME, radioStationName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `radioStationName` | `const char*` | `string` | |

**返回值:** `Hash` → `uint`

---

### SET_VEHICLE_MISSILE_WARNING_ENABLED

**Hash:** `0xF3365489E0DD50F9` | **JHash:** `0x8AFC488D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MISSILE_WARNING_ENABLED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MISSILE_WARNING_ENABLED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AMBIENT_ZONE_STATE

**Hash:** `0xBDA07E5950085E46` | **JHash:** `0x2849CAC9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_ZONE_STATE(const char* zoneName, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_ZONE_STATE, zoneName, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneName` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of ambient zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientZones.json

---

### CLEAR_AMBIENT_ZONE_STATE

**Hash:** `0x218DD44AAAC964FF` | **JHash:** `0xCDFF3C82` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AMBIENT_ZONE_STATE(const char* zoneName, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AMBIENT_ZONE_STATE, zoneName, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneName` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This function also has a p2, unknown. Signature AUDIO::CLEAR_AMBIENT_ZONE_STATE(const char* zoneName, bool p1, Any p2);

Still needs more research.

Full list of ambient zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientZones.json

---

### SET_AMBIENT_ZONE_LIST_STATE

**Hash:** `0x9748FA4DE50CCE3E` | **JHash:** `0xBF80B412` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_ZONE_LIST_STATE(const char* ambientZone, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_ZONE_LIST_STATE, ambientZone, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ambientZone` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_AMBIENT_ZONE_LIST_STATE

**Hash:** `0x120C48C614909FA4` | **JHash:** `0x38B9B8D4` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AMBIENT_ZONE_LIST_STATE(const char* ambientZone, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AMBIENT_ZONE_LIST_STATE, ambientZone, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ambientZone` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AMBIENT_ZONE_STATE_PERSISTENT

**Hash:** `0x1D6650420CEC9D3B` | **JHash:** `0xC1FFB672` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_ZONE_STATE_PERSISTENT(const char* ambientZone, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_ZONE_STATE_PERSISTENT, ambientZone, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ambientZone` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of ambient zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientZones.json

---

### SET_AMBIENT_ZONE_LIST_STATE_PERSISTENT

**Hash:** `0xF3638DAE8C4045E1` | **JHash:** `0x5F5A2605` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_ZONE_LIST_STATE_PERSISTENT(const char* ambientZone, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_ZONE_LIST_STATE_PERSISTENT, ambientZone, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ambientZone` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of ambient zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientZones.json

---

### IS_AMBIENT_ZONE_ENABLED

**Hash:** `0x01E2817A479A7F9B` | **JHash:** `0xBFABD872` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AMBIENT_ZONE_ENABLED(const char* ambientZone)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AMBIENT_ZONE_ENABLED, ambientZone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ambientZone` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of ambient zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ambientZones.json

---

### REFRESH_CLOSEST_OCEAN_SHORELINE

**Hash:** `0x5D2BFAAB8D956E0E` | **Build:** `573`

**C++ 原型:**
```cpp
void REFRESH_CLOSEST_OCEAN_SHORELINE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REFRESH_CLOSEST_OCEAN_SHORELINE);
```

**返回值:** `void` → `void`

---

### SET_CUTSCENE_AUDIO_OVERRIDE

**Hash:** `0x3B4BF5F0859204D9` | **JHash:** `0xCE1332B7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_AUDIO_OVERRIDE(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_AUDIO_OVERRIDE, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** All occurrences found in b617d, sorted alphabetically and identical lines removed: 

AUDIO::SET_CUTSCENE_AUDIO_OVERRIDE("_AK");
AUDIO::SET_CUTSCENE_AUDIO_OVERRIDE("_CUSTOM");
AUDIO::SET_CUTSCENE_AUDIO_OVERRIDE("_TOOTHLESS");
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### SET_VARIABLE_ON_SYNCH_SCENE_AUDIO

**Hash:** `0xBCC29F935ED07688` | **JHash:** `0xD63CF33A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VARIABLE_ON_SYNCH_SCENE_AUDIO(const char* variableName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VARIABLE_ON_SYNCH_SCENE_AUDIO, variableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variableName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PLAY_POLICE_REPORT

**Hash:** `0xDFEBD56D9BD1EB16` | **JHash:** `0x3F277B62` | **Build:** `323`

**C++ 原型:**
```cpp
int PLAY_POLICE_REPORT(const char* name, float p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PLAY_POLICE_REPORT, name, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `p1` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Plays the given police radio message.

All found occurrences in b617d, sorted alphabetically and identical lines removed: https://pastebin.com/GBnsQ5hr
Full list of police report names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/policeReportNames.json

---

### CANCEL_ALL_POLICE_REPORTS

**Hash:** `0xB4F90FAF7670B16F` | **Build:** `323`

**C++ 原型:**
```cpp
void CANCEL_ALL_POLICE_REPORTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CANCEL_ALL_POLICE_REPORTS);
```

**返回值:** `void` → `void`

---

### BLIP_SIREN

**Hash:** `0x1B9025BDA76822B6` | **JHash:** `0xC0EB6924` | **Build:** `323`

**C++ 原型:**
```cpp
void BLIP_SIREN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLIP_SIREN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Plays the siren sound of a vehicle which is otherwise activated when fastly double-pressing the horn key.
Only works on vehicles with a police siren.

---

### OVERRIDE_VEH_HORN

**Hash:** `0x3CDC1E622CCE0356` | **JHash:** `0x2ACAB783` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_VEH_HORN(Vehicle vehicle, BOOL override, int hornHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_VEH_HORN, vehicle, override, hornHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `override` | `BOOL` | `bool` | |
| `hornHash` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Overrides the vehicle's horn hash.
When changing this hash on a vehicle, it will not return the 'overwritten' hash. It will still always return the default horn hash (same as GET_VEHICLE_DEFAULT_HORN)

vehicle - the vehicle whose horn should be overwritten

---

### IS_HORN_ACTIVE

**Hash:** `0x9D6BFC12B05C6121` | **JHash:** `0x20E2BDD0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HORN_ACTIVE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HORN_ACTIVE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks whether the horn of a vehicle is currently played.

---

### SET_AGGRESSIVE_HORNS

**Hash:** `0x395BF71085D1B1D9` | **JHash:** `0x01D6EABE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AGGRESSIVE_HORNS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AGGRESSIVE_HORNS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes pedestrians sound their horn longer, faster and more agressive when they use their horn.

---

### SET_RADIO_POSITION_AUDIO_MUTE

**Hash:** `0x02E93C796ABD3A97` | **JHash:** `0x3C395AEE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIO_POSITION_AUDIO_MUTE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIO_POSITION_AUDIO_MUTE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### SET_VEHICLE_CONVERSATIONS_PERSIST

**Hash:** `0x58BB377BEC7CD5F4` | **JHash:** `0x8CE63FA1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CONVERSATIONS_PERSIST(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CONVERSATIONS_PERSIST, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CONVERSATIONS_PERSIST_NEW

**Hash:** `0x9BD7BD55E4533183` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_VEHICLE_CONVERSATIONS_PERSIST_NEW(BOOL p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CONVERSATIONS_PERSIST_NEW, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_STREAM_PLAYING

**Hash:** `0xD11FA52EB849D978` | **JHash:** `0xF1F51A14` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STREAM_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STREAM_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### GET_STREAM_PLAY_TIME

**Hash:** `0x4E72BBDBCA58A3DB` | **JHash:** `0xB4F0AD56` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STREAM_PLAY_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STREAM_PLAY_TIME);
```

**返回值:** `int` → `int`

---

### LOAD_STREAM

**Hash:** `0x1F1F957154EC51DF` | **JHash:** `0x0D89599D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOAD_STREAM(const char* streamName, const char* soundSet)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOAD_STREAM, streamName, soundSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `streamName` | `const char*` | `string` | |
| `soundSet` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:
AUDIO::LOAD_STREAM("CAR_STEAL_1_PASSBY", "CAR_STEAL_1_SOUNDSET");

All found occurrences in the b678d decompiled scripts: https://pastebin.com/3rma6w5w

Stream names often ends with "_MASTER", "_SMALL" or "_STREAM". Also "_IN", "_OUT" and numbers.   

soundSet is often set to 0 in the scripts. These are common to end the soundSets: "_SOUNDS", "_SOUNDSET" and numbers.

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### LOAD_STREAM_WITH_START_OFFSET

**Hash:** `0x59C16B79F53B3712` | **JHash:** `0xE5B5745C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOAD_STREAM_WITH_START_OFFSET(const char* streamName, int startOffset, const char* soundSet)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOAD_STREAM_WITH_START_OFFSET, streamName, startOffset, soundSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `streamName` | `const char*` | `string` | |
| `startOffset` | `int` | `int` | |
| `soundSet` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:
AUDIO::LOAD_STREAM_WITH_START_OFFSET("STASH_TOXIN_STREAM", 2400, "FBI_05_SOUNDS");

Only called a few times in the scripts.

Full list of audio / sound names by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/soundNames.json

---

### PLAY_STREAM_FROM_PED

**Hash:** `0x89049DD63C08B5D1` | **JHash:** `0xA1D7FABE` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_STREAM_FROM_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_STREAM_FROM_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### PLAY_STREAM_FROM_VEHICLE

**Hash:** `0xB70374A758007DFA` | **JHash:** `0xF8E4BDA2` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_STREAM_FROM_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_STREAM_FROM_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### PLAY_STREAM_FROM_OBJECT

**Hash:** `0xEBAA9B64D76356FD` | **JHash:** `0xC5266BF7` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_STREAM_FROM_OBJECT(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_STREAM_FROM_OBJECT, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

**说明:** Used with AUDIO::LOAD_STREAM

Example from finale_heist2b.c4:
TASK::TASK_SYNCHRONIZED_SCENE(l_4C8[2/*14*/], l_4C8[2/*14*/]._f7, l_30A, "push_out_vault_l", 4.0, -1.5, 5, 713, 4.0, 0);
                    PED::SET_SYNCHRONIZED_SCENE_PHASE(l_4C8[2/*14*/]._f7, 0.0);
                    PED::FORCE_PED_AI_AND_ANIMATION_UPDATE(l_4C8[2/*14*/], 0, 0);
                    PED::SET_PED_COMBAT_ATTRIBUTES(l_4C8[2/*14*/], 38, 1);
                    PED::SET_BLOCKING_OF_NON_TEMPORARY_EVENTS(l_4C8[2/*14*/], 1);
                    if (AUDIO::LOAD_STREAM("Gold_Cart_Push_Anim_01", "BIG_SCORE_3B_SOUNDS")) {
                        AUDIO::PLAY_STREAM_FROM_OBJECT(l_36F[0/*1*/]);
                    }

---

### PLAY_STREAM_FRONTEND

**Hash:** `0x58FCE43488F9F5F4` | **JHash:** `0x2C2A16BC` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_STREAM_FRONTEND(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_STREAM_FRONTEND);
```

**返回值:** `void` → `void`

---

### PLAY_STREAM_FROM_POSITION

**Hash:** `0x21442F412E8DE56B` | **JHash:** `0x6FE5D865` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_STREAM_FROM_POSITION(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_STREAM_FROM_POSITION, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_STREAM

**Hash:** `0xA4718A1419D18151` | **JHash:** `0xD1E364DE` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_STREAM(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_STREAM);
```

**返回值:** `void` → `void`

---

### STOP_PED_SPEAKING

**Hash:** `0x9D64D7405520E3D3` | **JHash:** `0xFF92B49D` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PED_SPEAKING(Ped ped, BOOL shaking)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PED_SPEAKING, ped, shaking);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `shaking` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### BLOCK_ALL_SPEECH_FROM_PED

**Hash:** `0xF8AD2EED7C47E8FE` | **Build:** `1734`

**C++ 原型:**
```cpp
void BLOCK_ALL_SPEECH_FROM_PED(Ped ped, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_ALL_SPEECH_FROM_PED, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STOP_PED_SPEAKING_SYNCED

**Hash:** `0xAB6781A5F3101470` | **Build:** `1868`

**C++ 原型:**
```cpp
void STOP_PED_SPEAKING_SYNCED(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PED_SPEAKING_SYNCED, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_PED_PAIN_AUDIO

**Hash:** `0xA9A41C1E940FB0E8` | **JHash:** `0x3B8E2D5F` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PED_PAIN_AUDIO(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PED_PAIN_AUDIO, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_AMBIENT_SPEECH_DISABLED

**Hash:** `0x932C2D096A2C3FFF` | **JHash:** `0x109D1F89` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AMBIENT_SPEECH_DISABLED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AMBIENT_SPEECH_DISABLED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Common in the scripts:
AUDIO::IS_AMBIENT_SPEECH_DISABLED(PLAYER::PLAYER_PED_ID());

---

### BLOCK_SPEECH_CONTEXT_GROUP

**Hash:** `0xA8A7D434AFB4B97B` | **Build:** `1493`

**C++ 原型:**
```cpp
void BLOCK_SPEECH_CONTEXT_GROUP(const char* p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_SPEECH_CONTEXT_GROUP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### UNBLOCK_SPEECH_CONTEXT_GROUP

**Hash:** `0x2ACABED337622DF2` | **Build:** `1493`

**C++ 原型:**
```cpp
void UNBLOCK_SPEECH_CONTEXT_GROUP(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNBLOCK_SPEECH_CONTEXT_GROUP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_SIREN_WITH_NO_DRIVER

**Hash:** `0x1FEF0683B96EBCF2` | **JHash:** `0x77182D58` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SIREN_WITH_NO_DRIVER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SIREN_WITH_NO_DRIVER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SIREN_BYPASS_MP_DRIVER_CHECK

**Hash:** `0xF584CF8529B51434` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_SIREN_BYPASS_MP_DRIVER_CHECK(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SIREN_BYPASS_MP_DRIVER_CHECK, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TRIGGER_SIREN_AUDIO

**Hash:** `0x66C3FB05206041BA` | **Build:** `1290`

**C++ 原型:**
```cpp
void TRIGGER_SIREN_AUDIO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_SIREN_AUDIO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_HORN_PERMANENTLY_ON

**Hash:** `0x9C11908013EA4715` | **JHash:** `0xDE8BA3CD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HORN_PERMANENTLY_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HORN_PERMANENTLY_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_HORN_ENABLED

**Hash:** `0x76D683C108594D0E` | **JHash:** `0x6EB92D05` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HORN_ENABLED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HORN_ENABLED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AUDIO_VEHICLE_PRIORITY

**Hash:** `0xE5564483E407F914` | **JHash:** `0x271A9766` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUDIO_VEHICLE_PRIORITY(Vehicle vehicle, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUDIO_VEHICLE_PRIORITY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_HORN_PERMANENTLY_ON_TIME

**Hash:** `0x9D3AF56E94C9AE98` | **JHash:** `0x2F0A16D1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HORN_PERMANENTLY_ON_TIME(Vehicle vehicle, float time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HORN_PERMANENTLY_ON_TIME, vehicle, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `time` | `float` | `float` | |

**返回值:** `void` → `void`

---

### USE_SIREN_AS_HORN

**Hash:** `0xFA932DE350266EF8` | **JHash:** `0xC6BC16F3` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_SIREN_AS_HORN(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_SIREN_AS_HORN, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_USE_AUDIO_GAME_OBJECT

**Hash:** `0x4F0C413926060B38` | **JHash:** `0x33B0B007` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_USE_AUDIO_GAME_OBJECT(Vehicle vehicle, const char* audioName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_USE_AUDIO_GAME_OBJECT, vehicle, audioName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `audioName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** This native sets the audio of the specified vehicle to the audioName (p1).

Use the audioNameHash found in vehicles.meta

Example:
_SET_VEHICLE_AUDIO(veh, "ADDER");
The selected vehicle will now have the audio of the Adder.

---

### PRELOAD_VEHICLE_AUDIO_BANK

**Hash:** `0xCA4CEA6AE0000A7E` | **Build:** `1180`

**C++ 原型:**
```cpp
void PRELOAD_VEHICLE_AUDIO_BANK(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_VEHICLE_AUDIO_BANK, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_STARTUP_REV_SOUND

**Hash:** `0xF1F8157B8C3F171C` | **JHash:** `0x1C0C5E4C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STARTUP_REV_SOUND(Vehicle vehicle, const char* p1, const char* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STARTUP_REV_SOUND, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### RESET_VEHICLE_STARTUP_REV_SOUND

**Hash:** `0xD2DCCD8E16E20997` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_VEHICLE_STARTUP_REV_SOUND(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_VEHICLE_STARTUP_REV_SOUND, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_FORCE_REVERSE_WARNING

**Hash:** `0x97FFB4ADEED08066` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_VEHICLE_FORCE_REVERSE_WARNING(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FORCE_REVERSE_WARNING, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_AUDIBLY_DAMAGED

**Hash:** `0x5DB8010EE71FDEF2` | **JHash:** `0x6E660D3F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_AUDIBLY_DAMAGED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_AUDIBLY_DAMAGED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_AUDIO_ENGINE_DAMAGE_FACTOR

**Hash:** `0x59E7B488451F4D3A` | **JHash:** `0x23BE6432` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_AUDIO_ENGINE_DAMAGE_FACTOR(Vehicle vehicle, float damageFactor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_AUDIO_ENGINE_DAMAGE_FACTOR, vehicle, damageFactor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `damageFactor` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_AUDIO_BODY_DAMAGE_FACTOR

**Hash:** `0x01BB4D577D38BD9E` | **JHash:** `0xE81FAC68` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_AUDIO_BODY_DAMAGE_FACTOR(Vehicle vehicle, float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_AUDIO_BODY_DAMAGE_FACTOR, vehicle, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** intensity: 0.0f - 1.0f, only used once with 1.0f in R* Scripts (nigel2)
Makes an engine rattling noise when you decelerate, you need to be going faster to hear lower values

---

### _FORCE_VEHICLE_ENGINE_SYNTH

**Hash:** `0xEB7D0E1FCC8FE17A` | **Build:** `3258`

**C++ 原型:**
```cpp
void _FORCE_VEHICLE_ENGINE_SYNTH(Vehicle vehicle, BOOL force)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._FORCE_VEHICLE_ENGINE_SYNTH, vehicle, force);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `force` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Called together with SET_VEHICLE_TYRES_CAN_BURST

---

### ENABLE_VEHICLE_FANBELT_DAMAGE

**Hash:** `0x1C073274E065C6D2` | **JHash:** `0x9365E042` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_VEHICLE_FANBELT_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_VEHICLE_FANBELT_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ENABLE_VEHICLE_EXHAUST_POPS

**Hash:** `0x2BE4BC731D039D5A` | **JHash:** `0x2A60A90E` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_VEHICLE_EXHAUST_POPS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_VEHICLE_EXHAUST_POPS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_BOOST_ACTIVE

**Hash:** `0x4A04DE7CAB2739A1` | **JHash:** `0x072F15F2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BOOST_ACTIVE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BOOST_ACTIVE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** SET_VEHICLE_BOOST_ACTIVE(vehicle, 1, 0);
SET_VEHICLE_BOOST_ACTIVE(vehicle, 0, 0); 

Will give a boost-soundeffect.

---

### SET_PLAYER_VEHICLE_ALARM_AUDIO_ACTIVE

**Hash:** `0x6FDDAD856E36988A` | **JHash:** `0x934BE749` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_VEHICLE_ALARM_AUDIO_ACTIVE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_VEHICLE_ALARM_AUDIO_ACTIVE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SCRIPT_UPDATE_DOOR_AUDIO

**Hash:** `0x06C0023BED16DD6B` | **JHash:** `0xE61110A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_UPDATE_DOOR_AUDIO(Hash doorHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_UPDATE_DOOR_AUDIO, doorHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAY_VEHICLE_DOOR_OPEN_SOUND

**Hash:** `0x3A539D52857EA82D` | **JHash:** `0x84930330` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_VEHICLE_DOOR_OPEN_SOUND(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_VEHICLE_DOOR_OPEN_SOUND, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### PLAY_VEHICLE_DOOR_CLOSE_SOUND

**Hash:** `0x62A456AA4769EF34` | **JHash:** `0xBA2CF407` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_VEHICLE_DOOR_CLOSE_SOUND(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_VEHICLE_DOOR_CLOSE_SOUND, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### ENABLE_STALL_WARNING_SOUNDS

**Hash:** `0xC15907D667F7CFB2` | **JHash:** `0x563B635D` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_STALL_WARNING_SOUNDS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_STALL_WARNING_SOUNDS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Works for planes only.

---

### _ENABLE_DRAG_RACE_STATIONARY_WARNING_SOUNDS

**Hash:** `0xBEFB80290414FD4F` | **Build:** `3095`

**C++ 原型:**
```cpp
void _ENABLE_DRAG_RACE_STATIONARY_WARNING_SOUNDS(Vehicle vehicle, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._ENABLE_DRAG_RACE_STATIONARY_WARNING_SOUNDS, vehicle, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_GAME_IN_CONTROL_OF_MUSIC

**Hash:** `0x6D28DC1671E334FD` | **JHash:** `0x7643170D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GAME_IN_CONTROL_OF_MUSIC(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GAME_IN_CONTROL_OF_MUSIC);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return 1

---

### SET_GPS_ACTIVE

**Hash:** `0x3BD3F52BA9B1E4E8` | **JHash:** `0x0FC3379A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_ACTIVE(BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_ACTIVE, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAY_MISSION_COMPLETE_AUDIO

**Hash:** `0xB138AAB8A70D3C69` | **JHash:** `0x3033EA1D` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_MISSION_COMPLETE_AUDIO(const char* audioName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_MISSION_COMPLETE_AUDIO, audioName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Called 38 times in the scripts. There are 5 different audioNames used.
 One unknown removed below.

AUDIO::PLAY_MISSION_COMPLETE_AUDIO("DEAD");
AUDIO::PLAY_MISSION_COMPLETE_AUDIO("FRANKLIN_BIG_01");
AUDIO::PLAY_MISSION_COMPLETE_AUDIO("GENERIC_FAILED");
AUDIO::PLAY_MISSION_COMPLETE_AUDIO("TREVOR_SMALL_01");

---

### IS_MISSION_COMPLETE_PLAYING

**Hash:** `0x19A30C23F5827F8A` | **JHash:** `0x939982A1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MISSION_COMPLETE_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MISSION_COMPLETE_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### IS_MISSION_COMPLETE_READY_FOR_UI

**Hash:** `0x6F259F82D873B8B8` | **JHash:** `0xCBE09AEC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MISSION_COMPLETE_READY_FOR_UI(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MISSION_COMPLETE_READY_FOR_UI);
```

**返回值:** `BOOL` → `bool`

---

### BLOCK_DEATH_JINGLE

**Hash:** `0xF154B8D1775B2DEC` | **JHash:** `0xD2858D8A` | **Build:** `323`

**C++ 原型:**
```cpp
void BLOCK_DEATH_JINGLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_DEATH_JINGLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_AUDIO_SCENE

**Hash:** `0x013A80FC08F6E4F2` | **JHash:** `0xE48D757B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_AUDIO_SCENE(const char* scene)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_AUDIO_SCENE, scene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scene` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Used to prepare a scene where the surrounding sound is muted or a bit changed. This does not play any sound.

List of all usable scene names found in b617d. Sorted alphabetically and identical names removed: https://pastebin.com/MtM9N9CC
Full list of audio scene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/audioSceneNames.json

---

### STOP_AUDIO_SCENE

**Hash:** `0xDFE8422B3B94E688` | **JHash:** `0xA08D8C58` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_AUDIO_SCENE(const char* scene)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_AUDIO_SCENE, scene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scene` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of audio scene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/audioSceneNames.json

---

### STOP_AUDIO_SCENES

**Hash:** `0xBAC7FC81A75EC1A1` | **JHash:** `0xF6C7342A` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_AUDIO_SCENES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_AUDIO_SCENES);
```

**返回值:** `void` → `void`

---

### IS_AUDIO_SCENE_ACTIVE

**Hash:** `0xB65B60556E2A9225` | **JHash:** `0xACBED05C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AUDIO_SCENE_ACTIVE(const char* scene)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AUDIO_SCENE_ACTIVE, scene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scene` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of audio scene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/audioSceneNames.json

---

### SET_AUDIO_SCENE_VARIABLE

**Hash:** `0xEF21A9EF089A2668` | **JHash:** `0x19BB3CE8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUDIO_SCENE_VARIABLE(const char* scene, const char* variable, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUDIO_SCENE_VARIABLE, scene, variable, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scene` | `const char*` | `string` | |
| `variable` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Full list of audio scene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/audioSceneNames.json

---

### SET_AUDIO_SCRIPT_CLEANUP_TIME

**Hash:** `0xA5F377B175A699C5` | **JHash:** `0xE812925D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUDIO_SCRIPT_CLEANUP_TIME(int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUDIO_SCRIPT_CLEANUP_TIME, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_ENTITY_TO_AUDIO_MIX_GROUP

**Hash:** `0x153973AB99FE8980` | **JHash:** `0x2BC93264` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_ENTITY_TO_AUDIO_MIX_GROUP(Entity entity, const char* groupName, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_ENTITY_TO_AUDIO_MIX_GROUP, entity, groupName, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `groupName` | `const char*` | `string` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** All found occurrences in b678d:
https://pastebin.com/ceu67jz8

---

### REMOVE_ENTITY_FROM_AUDIO_MIX_GROUP

**Hash:** `0x18EB48CFC41F2EA0` | **JHash:** `0x308ED0EC` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ENTITY_FROM_AUDIO_MIX_GROUP(Entity entity, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ENTITY_FROM_AUDIO_MIX_GROUP, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### AUDIO_IS_MUSIC_PLAYING

**Hash:** `0x845FFC3A4FEEFA3E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL AUDIO_IS_MUSIC_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.AUDIO_IS_MUSIC_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### AUDIO_IS_SCRIPTED_MUSIC_PLAYING

**Hash:** `0x2DD39BF3E2F9C47F` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL AUDIO_IS_SCRIPTED_MUSIC_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.AUDIO_IS_SCRIPTED_MUSIC_PLAYING);
```

**返回值:** `BOOL` → `bool`

**说明:** This is an alias of AUDIO_IS_MUSIC_PLAYING.

---

### PREPARE_MUSIC_EVENT

**Hash:** `0x1E5185B72EF5158A` | **JHash:** `0x534A5C1C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PREPARE_MUSIC_EVENT(const char* eventName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PREPARE_MUSIC_EVENT, eventName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** All music event names found in the b617d scripts: https://pastebin.com/GnYt0R3P
Full list of music event names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/musicEventNames.json

---

### CANCEL_MUSIC_EVENT

**Hash:** `0x5B17A90291133DA5` | **JHash:** `0x89FF942D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CANCEL_MUSIC_EVENT(const char* eventName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CANCEL_MUSIC_EVENT, eventName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** All music event names found in the b617d scripts: https://pastebin.com/GnYt0R3P
Full list of music event names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/musicEventNames.json

---

### TRIGGER_MUSIC_EVENT

**Hash:** `0x706D57B0F50DA710` | **JHash:** `0xB6094948` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TRIGGER_MUSIC_EVENT(const char* eventName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TRIGGER_MUSIC_EVENT, eventName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** List of all usable event names found in b617d used with this native. Sorted alphabetically and identical names removed: https://pastebin.com/RzDFmB1W

All music event names found in the b617d scripts: https://pastebin.com/GnYt0R3P
Full list of music event names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/musicEventNames.json

---

### IS_MUSIC_ONESHOT_PLAYING

**Hash:** `0xA097AB275061FB21` | **JHash:** `0x2705C4D5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MUSIC_ONESHOT_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MUSIC_ONESHOT_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### GET_MUSIC_PLAYTIME

**Hash:** `0xE7A0D23DC414507B` | **JHash:** `0xD633C809` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MUSIC_PLAYTIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MUSIC_PLAYTIME);
```

**返回值:** `int` → `int`

---

### SET_GLOBAL_RADIO_SIGNAL_LEVEL

**Hash:** `0x159B7318403A1CD8` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_GLOBAL_RADIO_SIGNAL_LEVEL(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GLOBAL_RADIO_SIGNAL_LEVEL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### RECORD_BROKEN_GLASS

**Hash:** `0xFBE20329593DEC9D` | **JHash:** `0x53FC3FEC` | **Build:** `323`

**C++ 原型:**
```cpp
void RECORD_BROKEN_GLASS(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RECORD_BROKEN_GLASS, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_ALL_BROKEN_GLASS

**Hash:** `0xB32209EFFDC04913` | **JHash:** `0xE6B033BF` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_BROKEN_GLASS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_BROKEN_GLASS);
```

**返回值:** `void` → `void`

---

### SCRIPT_OVERRIDES_WIND_ELEVATION

**Hash:** `0x70B8EC8FC108A634` | **JHash:** `0x95050CAD` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_OVERRIDES_WIND_ELEVATION(BOOL p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_OVERRIDES_WIND_ELEVATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_WALLA_DENSITY

**Hash:** `0x149AEE66F0CB3A99` | **JHash:** `0xE64F97A0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_WALLA_DENSITY(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WALLA_DENSITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_INTERIOR_WALLA_DENSITY

**Hash:** `0x8BF907833BE275DE` | **JHash:** `0x2CD26D69` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INTERIOR_WALLA_DENSITY(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INTERIOR_WALLA_DENSITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_PED_PANIC_WALLA

**Hash:** `0x062D5EAD4DA2FA6A` | **JHash:** `0xD87AF337` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_PED_PANIC_WALLA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PED_PANIC_WALLA);
```

**返回值:** `void` → `void`

---

### PREPARE_ALARM

**Hash:** `0x9D74AE343DB65533` | **JHash:** `0x084932E8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PREPARE_ALARM(const char* alarmName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PREPARE_ALARM, alarmName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alarmName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:

bool prepareAlarm = AUDIO::PREPARE_ALARM("PORT_OF_LS_HEIST_FORT_ZANCUDO_ALARMS");
Full list of alarm names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/alarmSounds.json

---

### START_ALARM

**Hash:** `0x0355EF116C4C97B2` | **JHash:** `0x703F524B` | **Build:** `323`

**C++ 原型:**
```cpp
void START_ALARM(const char* alarmName, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_ALARM, alarmName, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alarmName` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example:

This will start the alarm at Fort Zancudo.

AUDIO::START_ALARM("PORT_OF_LS_HEIST_FORT_ZANCUDO_ALARMS", 1);

First parameter (char) is the name of the alarm.
Second parameter (bool) is unknown, it does not seem to make a difference if this one is 0 or 1.

----------

It DOES make a difference but it has to do with the duration or something I dunno yet

----------

 Found in the b617d scripts:

 AUDIO::START_ALARM("AGENCY_HEIST_FIB_TOWER_ALARMS", 0);
 AUDIO::START_ALARM("AGENCY_HEIST_FIB_TOWER_ALARMS_UPPER", 1);
 AUDIO::START_ALARM("AGENCY_HEIST_FIB_TOWER_ALARMS_UPPER_B", 0);
 AUDIO::START_ALARM("BIG_SCORE_HEIST_VAULT_ALARMS", a_0);
 AUDIO::START_ALARM("FBI_01_MORGUE_ALARMS", 1);
 AUDIO::START_ALARM("FIB_05_BIOTECH_LAB_ALARMS", 0);
 AUDIO::START_ALARM("JEWEL_STORE_HEIST_ALARMS", 0);
 AUDIO::START_ALARM("PALETO_BAY_SCORE_ALARM", 1);
 AUDIO::START_ALARM("PALETO_BAY_SCORE_CHICKEN_FACTORY_ALARM", 0);
 AUDIO::START_ALARM("PORT_OF_LS_HEIST_FORT_ZANCUDO_ALARMS", 1);
 AUDIO::START_ALARM("PORT_OF_LS_HEIST_SHIP_ALARMS", 0);
 AUDIO::START_ALARM("PRISON_ALARMS", 0);
 AUDIO::START_ALARM("PROLOGUE_VAULT_ALARMS", 0);
Full list of alarm names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/alarmSounds.json

---

### STOP_ALARM

**Hash:** `0xA1CADDCD98415A41` | **JHash:** `0xF987BE8C` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ALARM(const char* alarmName, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ALARM, alarmName, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alarmName` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example:

This will stop the alarm at Fort Zancudo.

AUDIO::STOP_ALARM("PORT_OF_LS_HEIST_FORT_ZANCUDO_ALARMS", 1);

First parameter (char) is the name of the alarm.
Second parameter (bool) has to be true (1) to have any effect.
Full list of alarm names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/alarmSounds.json

---

### STOP_ALL_ALARMS

**Hash:** `0x2F794A877ADD4C92` | **JHash:** `0xC3CB9DC6` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ALL_ALARMS(BOOL stop)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ALL_ALARMS, stop);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `stop` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ALARM_PLAYING

**Hash:** `0x226435CB96CCFC8C` | **JHash:** `0x9D8E1D23` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ALARM_PLAYING(const char* alarmName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ALARM_PLAYING, alarmName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alarmName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:

bool playing = AUDIO::IS_ALARM_PLAYING("PORT_OF_LS_HEIST_FORT_ZANCUDO_ALARMS");
Full list of alarm names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/alarmSounds.json

---

### GET_VEHICLE_DEFAULT_HORN

**Hash:** `0x02165D55000219AC` | **JHash:** `0xE84ABC19` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_VEHICLE_DEFAULT_HORN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_VEHICLE_DEFAULT_HORN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns hash of default vehicle horn

Hash is stored in audVehicleAudioEntity

---

### GET_VEHICLE_DEFAULT_HORN_IGNORE_MODS

**Hash:** `0xACB5DCCA1EC76840` | **JHash:** `0xFD4B5B3B` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_VEHICLE_DEFAULT_HORN_IGNORE_MODS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_VEHICLE_DEFAULT_HORN_IGNORE_MODS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `Hash` → `uint`

---

### RESET_PED_AUDIO_FLAGS

**Hash:** `0xF54BB7B61036F335` | **JHash:** `0xDF720C86` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_AUDIO_FLAGS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_AUDIO_FLAGS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_FOOTSTEPS_EVENTS_ENABLED

**Hash:** `0x0653B735BFBDFE87` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_PED_FOOTSTEPS_EVENTS_ENABLED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_FOOTSTEPS_EVENTS_ENABLED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables/disables ped's "loud" footstep sound.

---

### SET_PED_CLOTH_EVENTS_ENABLED

**Hash:** `0x29DA3CA8D8B2692D` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_PED_CLOTH_EVENTS_ENABLED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CLOTH_EVENTS_ENABLED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables/disables ped's "quiet" footstep sound.

---

### OVERRIDE_PLAYER_GROUND_MATERIAL

**Hash:** `0xD2CC78CD3D0B50F9` | **JHash:** `0xC307D531` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_PLAYER_GROUND_MATERIAL(Hash hash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_PLAYER_GROUND_MATERIAL, hash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets audio flag "OverridePlayerGroundMaterial"

---

### USE_FOOTSTEP_SCRIPT_SWEETENERS

**Hash:** `0xBF4DC1784BE94DFA` | **JHash:** `0xD99E275B` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_FOOTSTEP_SCRIPT_SWEETENERS(Ped ped, BOOL p1, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_FOOTSTEP_SCRIPT_SWEETENERS, ped, p1, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### OVERRIDE_MICROPHONE_SETTINGS

**Hash:** `0x75773E11BA459E90` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_MICROPHONE_SETTINGS(Hash hash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_MICROPHONE_SETTINGS, hash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets audio flag "OverrideMicrophoneSettings"

---

### FREEZE_MICROPHONE

**Hash:** `0xD57AAAE0E2214D11` | **JHash:** `0x2B9D91E8` | **Build:** `323`

**C++ 原型:**
```cpp
void FREEZE_MICROPHONE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREEZE_MICROPHONE);
```

**返回值:** `void` → `void`

---

### DISTANT_COP_CAR_SIRENS

**Hash:** `0x552369F549563AD5` | **JHash:** `0x13EB5861` | **Build:** `323`

**C++ 原型:**
```cpp
void DISTANT_COP_CAR_SIRENS(BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISTANT_COP_CAR_SIRENS, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If value is set to true, and ambient siren sound will be played.
Appears to enable/disable an audio flag.

---

### SET_SIREN_CAN_BE_CONTROLLED_BY_AUDIO

**Hash:** `0x43FA0DFC5DF87815` | **JHash:** `0x7BED1872` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SIREN_CAN_BE_CONTROLLED_BY_AUDIO(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SIREN_CAN_BE_CONTROLLED_BY_AUDIO, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ENABLE_STUNT_JUMP_AUDIO

**Hash:** `0xB81CF134AEB56FFB` | **Build:** `791`

**C++ 原型:**
```cpp
void ENABLE_STUNT_JUMP_AUDIO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_STUNT_JUMP_AUDIO);
```

**返回值:** `void` → `void`

---

### SET_AUDIO_FLAG

**Hash:** `0xB9EFD5C25018725A` | **JHash:** `0x1C09C9E0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUDIO_FLAG(const char* flagName, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUDIO_FLAG, flagName, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flagName` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Possible flag names:
"ActivateSwitchWheelAudio"
"AllowAmbientSpeechInSlowMo"
"AllowCutsceneOverScreenFade"
"AllowForceRadioAfterRetune"
"AllowPainAndAmbientSpeechToPlayDuringCutscene"
"AllowPlayerAIOnMission"
"AllowPoliceScannerWhenPlayerHasNoControl"
"AllowRadioDuringSwitch"
"AllowRadioOverScreenFade"
"AllowScoreAndRadio"
"AllowScriptedSpeechInSlowMo"
"AvoidMissionCompleteDelay"
"DisableAbortConversationForDeathAndInjury"
"DisableAbortConversationForRagdoll"
"DisableBarks"
"DisableFlightMusic"
"DisableReplayScriptStreamRecording"
"EnableHeadsetBeep"
"ForceConversationInterrupt"
"ForceSeamlessRadioSwitch"
"ForceSniperAudio"
"FrontendRadioDisabled"
"HoldMissionCompleteWhenPrepared"
"IsDirectorModeActive"
"IsPlayerOnMissionForSpeech"
"ListenerReverbDisabled"
"LoadMPData"
"MobileRadioInGame"
"OnlyAllowScriptTriggerPoliceScanner"
"PlayMenuMusic"
"PoliceScannerDisabled"
"ScriptedConvListenerMaySpeak"
"SpeechDucksScore"
"SuppressPlayerScubaBreathing"
"WantedMusicDisabled"
"WantedMusicOnMission"

-------------------------------
No added flag names between b393d and b573d, including b573d.

#######################################################################

"IsDirectorModeActive" is an audio flag which will allow you to play speech infinitely without any pauses like in Director Mode.

-----------------------------------------------------------------------

All flag IDs and hashes:

ID: 00 | Hash: 0x0FED7A7F
ID: 01 | Hash: 0x20A7858F
ID: 02 | Hash: 0xA11C2259
ID: 03 | Hash: 0x08DE4700
ID: 04 | Hash: 0x989F652F
ID: 05 | Hash: 0x3C9E76BA
ID: 06 | Hash: 0xA805FEB0
ID: 07 | Hash: 0x4B94EA26
ID: 08 | Hash: 0x803ACD34
ID: 09 | Hash: 0x7C741226
ID: 10 | Hash: 0x31DB9EBD
ID: 11 | Hash: 0xDF386F18
ID: 12 | Hash: 0x669CED42
ID: 13 | Hash: 0x51F22743
ID: 14 | Hash: 0x2052B35C
ID: 15 | Hash: 0x071472DC
ID: 16 | Hash: 0xF9928BCC
ID: 17 | Hash: 0x7ADBDD48
ID: 18 | Hash: 0xA959BA1A
ID: 19 | Hash: 0xBBE89B60
ID: 20 | Hash: 0x87A08871
ID: 21 | Hash: 0xED1057CE
ID: 22 | Hash: 0x1584AD7A
ID: 23 | Hash: 0x8582CFCB
ID: 24 | Hash: 0x7E5E2FB0
ID: 25 | Hash: 0xAE4F72DB
ID: 26 | Hash: 0x5D16D1FA
ID: 27 | Hash: 0x06B2F4B8
ID: 28 | Hash: 0x5D4CDC96
ID: 29 | Hash: 0x8B5A48BA
ID: 30 | Hash: 0x98FBD539
ID: 31 | Hash: 0xD8CB0473
ID: 32 | Hash: 0x5CBB4874
ID: 33 | Hash: 0x2E9F93A9
ID: 34 | Hash: 0xD93BEA86
ID: 35 | Hash: 0x92109B7D
ID: 36 | Hash: 0xB7EC9E4D
ID: 37 | Hash: 0xCABDBB1D
ID: 38 | Hash: 0xB3FD4A52
ID: 39 | Hash: 0x370D94E5
ID: 40 | Hash: 0xA0F7938F
ID: 41 | Hash: 0xCBE1CE81
ID: 42 | Hash: 0xC27F1271
ID: 43 | Hash: 0x9E3258EB
ID: 44 | Hash: 0x551CDA5B
ID: 45 | Hash: 0xCB6D663C
ID: 46 | Hash: 0x7DACE87F
ID: 47 | Hash: 0xF9DE416F
ID: 48 | Hash: 0x882E6E9E
ID: 49 | Hash: 0x16B447E7
ID: 50 | Hash: 0xBD867739
ID: 51 | Hash: 0xA3A58604
ID: 52 | Hash: 0x7E046BBC
ID: 53 | Hash: 0xD95FDB98
ID: 54 | Hash: 0x5842C0ED
ID: 55 | Hash: 0x285FECC6
ID: 56 | Hash: 0x9351AC43
ID: 57 | Hash: 0x50032E75
ID: 58 | Hash: 0xAE6D0D59
ID: 59 | Hash: 0xD6351785
ID: 60 | Hash: 0xD25D71BC
ID: 61 | Hash: 0x1F7F6423
ID: 62 | Hash: 0xE24C3AA6
ID: 63 | Hash: 0xBFFDD2B7

---

### PREPARE_SYNCHRONIZED_AUDIO_EVENT

**Hash:** `0xC7ABCACA4985A766` | **JHash:** `0xE1D91FD0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PREPARE_SYNCHRONIZED_AUDIO_EVENT(const char* audioEvent, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PREPARE_SYNCHRONIZED_AUDIO_EVENT, audioEvent, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioEvent` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 is always 0 in the scripts

---

### PREPARE_SYNCHRONIZED_AUDIO_EVENT_FOR_SCENE

**Hash:** `0x029FE7CD1B7E2E75` | **JHash:** `0x7652DD49` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PREPARE_SYNCHRONIZED_AUDIO_EVENT_FOR_SCENE(int sceneID, const char* audioEvent)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PREPARE_SYNCHRONIZED_AUDIO_EVENT_FOR_SCENE, sceneID, audioEvent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `audioEvent` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### PLAY_SYNCHRONIZED_AUDIO_EVENT

**Hash:** `0x8B2FD4560E55DD2D` | **JHash:** `0x507F3241` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_SYNCHRONIZED_AUDIO_EVENT(int sceneID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_SYNCHRONIZED_AUDIO_EVENT, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STOP_SYNCHRONIZED_AUDIO_EVENT

**Hash:** `0x92D6A88E64A94430` | **JHash:** `0xADEED2B4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STOP_SYNCHRONIZED_AUDIO_EVENT(int sceneID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STOP_SYNCHRONIZED_AUDIO_EVENT, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### INIT_SYNCH_SCENE_AUDIO_WITH_POSITION

**Hash:** `0xC8EDE9BDBCCBA6D4` | **JHash:** `0x55A21772` | **Build:** `323`

**C++ 原型:**
```cpp
void INIT_SYNCH_SCENE_AUDIO_WITH_POSITION(const char* audioEvent, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INIT_SYNCH_SCENE_AUDIO_WITH_POSITION, audioEvent, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioEvent` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### INIT_SYNCH_SCENE_AUDIO_WITH_ENTITY

**Hash:** `0x950A154B8DAB6185` | **JHash:** `0xA17F9AB0` | **Build:** `323`

**C++ 原型:**
```cpp
void INIT_SYNCH_SCENE_AUDIO_WITH_ENTITY(const char* audioEvent, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INIT_SYNCH_SCENE_AUDIO_WITH_ENTITY, audioEvent, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `audioEvent` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_AUDIO_SPECIAL_EFFECT_MODE

**Hash:** `0x12561FCBB62D5B9C` | **JHash:** `0x62B43677` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUDIO_SPECIAL_EFFECT_MODE(int mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUDIO_SPECIAL_EFFECT_MODE, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Needs to be called every frame.
Audio mode to apply this frame: https://alloc8or.re/gta5/doc/enums/audSpecialEffectMode.txt

---

### SET_PORTAL_SETTINGS_OVERRIDE

**Hash:** `0x044DBAD7A7FA2BE5` | **JHash:** `0x8AD670EC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PORTAL_SETTINGS_OVERRIDE(const char* p0, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PORTAL_SETTINGS_OVERRIDE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Found in the b617d scripts, duplicates removed:  

AUDIO::SET_PORTAL_SETTINGS_OVERRIDE("V_CARSHOWROOM_PS_WINDOW_UNBROKEN", "V_CARSHOWROOM_PS_WINDOW_BROKEN");

 AUDIO::SET_PORTAL_SETTINGS_OVERRIDE("V_CIA_PS_WINDOW_UNBROKEN", "V_CIA_PS_WINDOW_BROKEN");

 AUDIO::SET_PORTAL_SETTINGS_OVERRIDE("V_DLC_HEIST_APARTMENT_DOOR_CLOSED", "V_DLC_HEIST_APARTMENT_DOOR_OPEN");

 AUDIO::SET_PORTAL_SETTINGS_OVERRIDE("V_FINALEBANK_PS_VAULT_INTACT", "V_FINALEBANK_PS_VAULT_BLOWN");

 AUDIO::SET_PORTAL_SETTINGS_OVERRIDE("V_MICHAEL_PS_BATHROOM_WITH_WINDOW", "V_MICHAEL_PS_BATHROOM_WITHOUT_WINDOW");

---

### SET_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE

**Hash:** `0xC9D623C5A3D8FD5D` | **Build:** `3570`

**C++ 原型:**
```cpp
void SET_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE(Hash interiorNameHash, int roomIndex, int doorIndex, const char* newPortalSettingsName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE, interiorNameHash, roomIndex, doorIndex, newPortalSettingsName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interiorNameHash` | `Hash` | `uint` | |
| `roomIndex` | `int` | `int` | |
| `doorIndex` | `int` | `int` | |
| `newPortalSettingsName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REMOVE_PORTAL_SETTINGS_OVERRIDE

**Hash:** `0xB4BBFD9CD8B3922B` | **JHash:** `0xD24B4D0C` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PORTAL_SETTINGS_OVERRIDE(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PORTAL_SETTINGS_OVERRIDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:**  Found in the b617d scripts, duplicates removed: 

 AUDIO::REMOVE_PORTAL_SETTINGS_OVERRIDE("V_CARSHOWROOM_PS_WINDOW_UNBROKEN");
 AUDIO::REMOVE_PORTAL_SETTINGS_OVERRIDE("V_CIA_PS_WINDOW_UNBROKEN");
 AUDIO::REMOVE_PORTAL_SETTINGS_OVERRIDE("V_DLC_HEIST_APARTMENT_DOOR_CLOSED");
 AUDIO::REMOVE_PORTAL_SETTINGS_OVERRIDE("V_FINALEBANK_PS_VAULT_INTACT");
 AUDIO::REMOVE_PORTAL_SETTINGS_OVERRIDE("V_MICHAEL_PS_BATHROOM_WITH_WINDOW");

---

### REMOVE_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE

**Hash:** `0x8EF105736194F80C` | **Build:** `3570`

**C++ 原型:**
```cpp
void REMOVE_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE(Hash interiorNameHash, int roomIndex, int doorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_INDIVIDUAL_PORTAL_SETTINGS_OVERRIDE, interiorNameHash, roomIndex, doorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interiorNameHash` | `Hash` | `uint` | |
| `roomIndex` | `int` | `int` | |
| `doorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### STOP_SMOKE_GRENADE_EXPLOSION_SOUNDS

**Hash:** `0xE4E6DD5566D28C82` | **JHash:** `0x7262B5BA` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SMOKE_GRENADE_EXPLOSION_SOUNDS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SMOKE_GRENADE_EXPLOSION_SOUNDS);
```

**返回值:** `void` → `void`

---

### GET_MUSIC_VOL_SLIDER

**Hash:** `0x3A48AB4445D499BE` | **JHash:** `0x93A44A1F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MUSIC_VOL_SLIDER(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MUSIC_VOL_SLIDER);
```

**返回值:** `int` → `int`

---

### REQUEST_TENNIS_BANKS

**Hash:** `0x4ADA3F19BE4A6047` | **JHash:** `0x13777A0B` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_TENNIS_BANKS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_TENNIS_BANKS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### UNREQUEST_TENNIS_BANKS

**Hash:** `0x0150B6FF25A9E2E5` | **JHash:** `0x1134F68B` | **Build:** `323`

**C++ 原型:**
```cpp
void UNREQUEST_TENNIS_BANKS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNREQUEST_TENNIS_BANKS);
```

**返回值:** `void` → `void`

---

### SET_SKIP_MINIGUN_SPIN_UP_AUDIO

**Hash:** `0xBEF34B1D9624D5DD` | **JHash:** `0xE0047BFD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SKIP_MINIGUN_SPIN_UP_AUDIO(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SKIP_MINIGUN_SPIN_UP_AUDIO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STOP_CUTSCENE_AUDIO

**Hash:** `0x806058BBDC136E06` | **JHash:** `0x55461BE6` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CUTSCENE_AUDIO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CUTSCENE_AUDIO);
```

**返回值:** `void` → `void`

---

### HAS_LOADED_MP_DATA_SET

**Hash:** `0x544810ED9DB6BBE6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_LOADED_MP_DATA_SET(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_LOADED_MP_DATA_SET);
```

**返回值:** `BOOL` → `bool`

---

### HAS_LOADED_SP_DATA_SET

**Hash:** `0x5B50ABB1FE3746F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_LOADED_SP_DATA_SET(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_LOADED_SP_DATA_SET);
```

**返回值:** `BOOL` → `bool`

---

### GET_VEHICLE_HORN_SOUND_INDEX

**Hash:** `0xD53F3A29BCE2580E` | **Build:** `1365`

**C++ 原型:**
```cpp
int GET_VEHICLE_HORN_SOUND_INDEX(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_HORN_SOUND_INDEX, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SET_VEHICLE_HORN_SOUND_INDEX

**Hash:** `0x0350E7E17BA767D0` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_VEHICLE_HORN_SOUND_INDEX(Vehicle vehicle, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HORN_SOUND_INDEX, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---


---

## BRAIN

**包含 11 个 Native 函数**

### ADD_SCRIPT_TO_RANDOM_PED

**Hash:** `0x4EE5367468A65CCC` | **JHash:** `0xECC76C3D` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_SCRIPT_TO_RANDOM_PED(const char* name, Hash model, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_SCRIPT_TO_RANDOM_PED, name, model, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `model` | `Hash` | `uint` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** BRAIN::ADD_SCRIPT_TO_RANDOM_PED("pb_prostitute", ${s_f_y_hooker_01}, 100, 0);

- Nacorpio

-----

Hardcoded to not work in Multiplayer.

---

### REGISTER_OBJECT_SCRIPT_BRAIN

**Hash:** `0x0BE84C318BA6EC22` | **JHash:** `0xB6BCC608` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_OBJECT_SCRIPT_BRAIN(const char* scriptName, Hash modelHash, int p2, float activationRange, int p4, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_OBJECT_SCRIPT_BRAIN, scriptName, modelHash, p2, activationRange, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |
| `p2` | `int` | `int` | |
| `activationRange` | `float` | `float` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Registers a script for any object with a specific model hash.

BRAIN::REGISTER_OBJECT_SCRIPT_BRAIN("ob_telescope", ${prop_telescope_01}, 100, 4.0, -1, 9);

- Nacorpio

---

### IS_OBJECT_WITHIN_BRAIN_ACTIVATION_RANGE

**Hash:** `0xCCBA154209823057` | **JHash:** `0xBA4CAA56` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_WITHIN_BRAIN_ACTIVATION_RANGE(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_WITHIN_BRAIN_ACTIVATION_RANGE, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REGISTER_WORLD_POINT_SCRIPT_BRAIN

**Hash:** `0x3CDC7136613284BD` | **JHash:** `0x725D91F7` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_WORLD_POINT_SCRIPT_BRAIN(const char* scriptName, float activationRange, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_WORLD_POINT_SCRIPT_BRAIN, scriptName, activationRange, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `activationRange` | `float` | `float` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_WORLD_POINT_WITHIN_BRAIN_ACTIVATION_RANGE

**Hash:** `0xC5042CC6F5E3D450` | **JHash:** `0x2CF305A0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WORLD_POINT_WITHIN_BRAIN_ACTIVATION_RANGE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WORLD_POINT_WITHIN_BRAIN_ACTIVATION_RANGE);
```

**返回值:** `BOOL` → `bool`

**说明:** Gets whether the world point the calling script is registered to is within desired range of the player.

---

### ENABLE_SCRIPT_BRAIN_SET

**Hash:** `0x67AA4D73F0CFA86B` | **JHash:** `0x2765919F` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_SCRIPT_BRAIN_SET(int brainSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_SCRIPT_BRAIN_SET, brainSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `brainSet` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DISABLE_SCRIPT_BRAIN_SET

**Hash:** `0x14D8518E9760F08F` | **JHash:** `0xFBD13FAD` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_SCRIPT_BRAIN_SET(int brainSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_SCRIPT_BRAIN_SET, brainSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `brainSet` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REACTIVATE_ALL_WORLD_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE

**Hash:** `0x0B40ED49D7D6FF84` | **JHash:** `0x19B27825` | **Build:** `323`

**C++ 原型:**
```cpp
void REACTIVATE_ALL_WORLD_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REACTIVATE_ALL_WORLD_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE);
```

**返回值:** `void` → `void`

---

### REACTIVATE_ALL_OBJECT_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE

**Hash:** `0x4D953DF78EBF8158` | **JHash:** `0xF3A3AB08` | **Build:** `323`

**C++ 原型:**
```cpp
void REACTIVATE_ALL_OBJECT_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REACTIVATE_ALL_OBJECT_BRAINS_THAT_ARE_WAITING_TILL_OUT_OF_RANGE);
```

**返回值:** `void` → `void`

---

### REACTIVATE_NAMED_WORLD_BRAINS_WAITING_TILL_OUT_OF_RANGE

**Hash:** `0x6D6840CEE8845831` | **JHash:** `0x949FE53E` | **Build:** `323`

**C++ 原型:**
```cpp
void REACTIVATE_NAMED_WORLD_BRAINS_WAITING_TILL_OUT_OF_RANGE(const char* scriptName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REACTIVATE_NAMED_WORLD_BRAINS_WAITING_TILL_OUT_OF_RANGE, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Possible values:

act_cinema
am_mp_carwash_launch
am_mp_carwash_control
am_mp_property_ext
chop
fairgroundHub
launcher_BasejumpHeli
launcher_BasejumpPack
launcher_CarWash
launcher_golf
launcher_Hunting_Ambient
launcher_MrsPhilips
launcher_OffroadRacing
launcher_pilotschool
launcher_Racing
launcher_rampage
launcher_rampage
launcher_range
launcher_stunts
launcher_stunts
launcher_tennis
launcher_Tonya
launcher_Triathlon
launcher_Yoga
ob_mp_bed_low
ob_mp_bed_med

---

### REACTIVATE_NAMED_OBJECT_BRAINS_WAITING_TILL_OUT_OF_RANGE

**Hash:** `0x6E91B04E08773030` | **JHash:** `0x29CE8BAA` | **Build:** `323`

**C++ 原型:**
```cpp
void REACTIVATE_NAMED_OBJECT_BRAINS_WAITING_TILL_OUT_OF_RANGE(const char* scriptName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REACTIVATE_NAMED_OBJECT_BRAINS_WAITING_TILL_OUT_OF_RANGE, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Looks like a cousin of above function _6D6840CEE8845831 as it was found among them. Must be similar

Here are possible values of argument - 

"ob_tv"
"launcher_Darts"

---


---

## CAM

**包含 248 个 Native 函数**

### RENDER_SCRIPT_CAMS

**Hash:** `0x07E5B515DB0636FC` | **JHash:** `0x74337969` | **Build:** `323`

**C++ 原型:**
```cpp
void RENDER_SCRIPT_CAMS(BOOL render, BOOL ease, int easeTime, BOOL p3, BOOL p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RENDER_SCRIPT_CAMS, render, ease, easeTime, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `render` | `BOOL` | `bool` | |
| `ease` | `BOOL` | `bool` | |
| `easeTime` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** ease - smooth transition between the camera's positions
easeTime - Time in milliseconds for the transition to happen

If you have created a script (rendering) camera, and want to go back to the 
character (gameplay) camera, call this native with render set to 0.
Setting ease to 1 will smooth the transition.

---

### STOP_RENDERING_SCRIPT_CAMS_USING_CATCH_UP

**Hash:** `0xC819F3CBB62BF692` | **JHash:** `0xD3C08183` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_RENDERING_SCRIPT_CAMS_USING_CATCH_UP(BOOL render, float p1, int p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_RENDERING_SCRIPT_CAMS_USING_CATCH_UP, render, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `render` | `BOOL` | `bool` | |
| `p1` | `float` | `float` | |
| `p2` | `int` | `int` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native makes the gameplay camera zoom into first person/third person with a special effect.

---

### CREATE_CAM

**Hash:** `0xC3981DCE61D9E13F` | **JHash:** `0xE9BF2A7D` | **Build:** `323`

**C++ 原型:**
```cpp
Cam CREATE_CAM(const char* camName, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_CAM, camName, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camName` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Cam` → `int`

**说明:** "DEFAULT_SCRIPTED_CAMERA"
"DEFAULT_ANIMATED_CAMERA"
"DEFAULT_SPLINE_CAMERA"
"DEFAULT_SCRIPTED_FLY_CAMERA"
"TIMED_SPLINE_CAMERA"

---

### CREATE_CAM_WITH_PARAMS

**Hash:** `0xB51194800B257161` | **JHash:** `0x23B02F15` | **Build:** `323`

**C++ 原型:**
```cpp
Cam CREATE_CAM_WITH_PARAMS(const char* camName, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float fov, BOOL p8, int p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_CAM_WITH_PARAMS, camName, posX, posY, posZ, rotX, rotY, rotZ, fov, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camName` | `const char*` | `string` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `fov` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `int` | `int` | |

**返回值:** `Cam` → `int`

**说明:** camName is always set to "DEFAULT_SCRIPTED_CAMERA" in Rockstar's scripts.
------------
Camera names found in the b617d scripts:
"DEFAULT_ANIMATED_CAMERA"
"DEFAULT_SCRIPTED_CAMERA"
"DEFAULT_SCRIPTED_FLY_CAMERA"
"DEFAULT_SPLINE_CAMERA"
------------
Side Note: It seems p8 is basically to represent what would be the bool p1 within CREATE_CAM native. As well as the p9 since it's always 2 in scripts seems to represent what would be the last param within SET_CAM_ROT native which normally would be 2.

---

### CREATE_CAMERA

**Hash:** `0x5E3CF89C6BCCA67D` | **JHash:** `0x5D6739AE` | **Build:** `323`

**C++ 原型:**
```cpp
Cam CREATE_CAMERA(Hash camHash, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_CAMERA, camHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camHash` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Cam` → `int`

---

### CREATE_CAMERA_WITH_PARAMS

**Hash:** `0x6ABFA3E16460F22D` | **JHash:** `0x0688BE9A` | **Build:** `323`

**C++ 原型:**
```cpp
Cam CREATE_CAMERA_WITH_PARAMS(Hash camHash, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float fov, BOOL p8, Any p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_CAMERA_WITH_PARAMS, camHash, posX, posY, posZ, rotX, rotY, rotZ, fov, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camHash` | `Hash` | `uint` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `fov` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `Any` | `object` | |

**返回值:** `Cam` → `int`

**说明:** p9 uses 2 by default

---

### DESTROY_CAM

**Hash:** `0x865908C81A2C22E9` | **JHash:** `0xC39302BD` | **Build:** `323`

**C++ 原型:**
```cpp
void DESTROY_CAM(Cam cam, BOOL bScriptHostCam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DESTROY_CAM, cam, bScriptHostCam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `bScriptHostCam` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** BOOL param indicates whether the cam should be destroyed if it belongs to the calling script.

---

### DESTROY_ALL_CAMS

**Hash:** `0x8E5FB15663F79120` | **JHash:** `0x10C151CE` | **Build:** `323`

**C++ 原型:**
```cpp
void DESTROY_ALL_CAMS(BOOL bScriptHostCam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DESTROY_ALL_CAMS, bScriptHostCam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `bScriptHostCam` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** BOOL param indicates whether the cam should be destroyed if it belongs to the calling script.

---

### DOES_CAM_EXIST

**Hash:** `0xA7A932170592B50E` | **JHash:** `0x1EF89DC0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_CAM_EXIST(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CAM_EXIST, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether or not the passed camera handle exists.

---

### SET_CAM_ACTIVE

**Hash:** `0x026FB97D0A425F84` | **JHash:** `0x064659C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_ACTIVE(Cam cam, BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_ACTIVE, cam, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Set camera as active/inactive.

---

### IS_CAM_ACTIVE

**Hash:** `0xDFB2B516207D3534` | **JHash:** `0x4B58F177` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_ACTIVE(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_ACTIVE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether or not the passed camera handle is active.

---

### IS_CAM_RENDERING

**Hash:** `0x02EC0AF5C5A49B7A` | **JHash:** `0x6EC6B5B2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_RENDERING(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_RENDERING, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_RENDERING_CAM

**Hash:** `0x5234F9F10919EABA` | **JHash:** `0x0FCF4DF1` | **Build:** `323`

**C++ 原型:**
```cpp
Cam GET_RENDERING_CAM(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RENDERING_CAM);
```

**返回值:** `Cam` → `int`

---

### GET_CAM_COORD

**Hash:** `0xBAC038F7459AE5AE` | **JHash:** `0x7C40F09C` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_CAM_COORD(Cam cam)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_CAM_COORD, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_CAM_ROT

**Hash:** `0x7D304C1C955E3E12` | **JHash:** `0xDAC84C9F` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_CAM_ROT(Cam cam, int rotationOrder)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_CAM_ROT, cam, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** The last parameter, as in other "ROT" methods, is usually 2.

---

### GET_CAM_FOV

**Hash:** `0xC3330A45CCCDB26A` | **JHash:** `0xD6E9FCF5` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_FOV(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_FOV, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### GET_CAM_NEAR_CLIP

**Hash:** `0xC520A34DAFBF24B1` | **JHash:** `0xCFCD35EE` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_NEAR_CLIP(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_NEAR_CLIP, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### GET_CAM_FAR_CLIP

**Hash:** `0xB60A9CFEB21CA6AA` | **JHash:** `0x09F119B8` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_FAR_CLIP(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_FAR_CLIP, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### GET_CAM_NEAR_DOF

**Hash:** `0xC2612D223D915A1C` | **Build:** `2699`

**C++ 原型:**
```cpp
float GET_CAM_NEAR_DOF(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_NEAR_DOF, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### GET_CAM_FAR_DOF

**Hash:** `0x255F8DAFD540D397` | **JHash:** `0x98C5CCE9` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_FAR_DOF(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_FAR_DOF, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### GET_CAM_DOF_STRENGTH

**Hash:** `0x06D153C0B99B6128` | **Build:** `2699`

**C++ 原型:**
```cpp
float GET_CAM_DOF_STRENGTH(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_DOF_STRENGTH, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### SET_CAM_PARAMS

**Hash:** `0xBFD8727AEA3CCEBA` | **JHash:** `0x2167CEBF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_PARAMS(Cam cam, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float fieldOfView, Any p8, int p9, int p10, int p11)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_PARAMS, cam, posX, posY, posZ, rotX, rotY, rotZ, fieldOfView, p8, p9, p10, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `fieldOfView` | `float` | `float` | |
| `p8` | `Any` | `object` | |
| `p9` | `int` | `int` | |
| `p10` | `int` | `int` | |
| `p11` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_CAM_COORD

**Hash:** `0x4D41783FB745E42E` | **JHash:** `0x7A8053AF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_COORD(Cam cam, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_COORD, cam, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the position of the cam.

---

### SET_CAM_ROT

**Hash:** `0x85973643155D0B07` | **JHash:** `0xEE38B3C1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_ROT(Cam cam, float rotX, float rotY, float rotZ, int rotationOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_ROT, cam, rotX, rotY, rotZ, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the rotation of the cam.
Last parameter unknown.

Last parameter seems to always be set to 2.

---

### SET_CAM_FOV

**Hash:** `0xB13C14F66A00D047` | **JHash:** `0xD3D5D74F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_FOV(Cam cam, float fieldOfView)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_FOV, cam, fieldOfView);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `fieldOfView` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the field of view of the cam.
---------------------------------------------
Min: 1.0f
Max: 130.0f

---

### SET_CAM_NEAR_CLIP

**Hash:** `0xC7848EFCCC545182` | **JHash:** `0x46DB13B1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_NEAR_CLIP(Cam cam, float nearClip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_NEAR_CLIP, cam, nearClip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `nearClip` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_FAR_CLIP

**Hash:** `0xAE306F2A904BF86E` | **JHash:** `0x0D23E381` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_FAR_CLIP(Cam cam, float farClip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_FAR_CLIP, cam, farClip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `farClip` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_CAM_FAR_CLIP

**Hash:** `0xAABD62873FFB1A33` | **Build:** `2189`

**C++ 原型:**
```cpp
void FORCE_CAM_FAR_CLIP(Cam cam, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CAM_FAR_CLIP, cam, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_MOTION_BLUR_STRENGTH

**Hash:** `0x6F0F77FBA9A8F2E6` | **JHash:** `0xFD6E0D67` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_MOTION_BLUR_STRENGTH(Cam cam, float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_MOTION_BLUR_STRENGTH, cam, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_NEAR_DOF

**Hash:** `0x3FA4BF0A7AB7DE2C` | **JHash:** `0xF28254DF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_NEAR_DOF(Cam cam, float nearDOF)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_NEAR_DOF, cam, nearDOF);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `nearDOF` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_FAR_DOF

**Hash:** `0xEDD91296CD01AEE0` | **JHash:** `0x58515E8E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_FAR_DOF(Cam cam, float farDOF)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_FAR_DOF, cam, farDOF);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `farDOF` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_DOF_STRENGTH

**Hash:** `0x5EE29B4D7D5DF897` | **JHash:** `0x3CC4EB3F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_STRENGTH(Cam cam, float dofStrength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_STRENGTH, cam, dofStrength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `dofStrength` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_DOF_PLANES

**Hash:** `0x3CF48F6F96E749DC` | **JHash:** `0xAD6C2B8F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_PLANES(Cam cam, float p1, float p2, float p3, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_PLANES, cam, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_USE_SHALLOW_DOF_MODE

**Hash:** `0x16A96863A17552BB` | **JHash:** `0x8306C256` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_USE_SHALLOW_DOF_MODE(Cam cam, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_USE_SHALLOW_DOF_MODE, cam, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_USE_HI_DOF

**Hash:** `0xA13B0222F3D94A94` | **JHash:** `0x8BBF2950` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_USE_HI_DOF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_HI_DOF);
```

**返回值:** `void` → `void`

---

### SET_USE_HI_DOF_ON_SYNCED_SCENE_THIS_UPDATE

**Hash:** `0x731A880555DA3647` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_USE_HI_DOF_ON_SYNCED_SCENE_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_HI_DOF_ON_SYNCED_SCENE_THIS_UPDATE);
```

**返回值:** `void` → `void`

**说明:** Only used in R* Script fm_mission_controller_2020

---

### SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE

**Hash:** `0xF55E4046F6F831DC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE(Cam camera, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE, camera, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE_BLEND_LEVEL

**Hash:** `0xE111A7C0D200CBC5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE_BLEND_LEVEL(Any p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_OVERRIDDEN_FOCUS_DISTANCE_BLEND_LEVEL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_DOF_FNUMBER_OF_LENS

**Hash:** `0x7DD234D6F3914C5B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_FNUMBER_OF_LENS(Cam camera, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_FNUMBER_OF_LENS, camera, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native has its name defined inside its codE


---

### SET_CAM_DOF_FOCAL_LENGTH_MULTIPLIER

**Hash:** `0x47B595D60664CFFA` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_CAM_DOF_FOCAL_LENGTH_MULTIPLIER(Cam camera, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_FOCAL_LENGTH_MULTIPLIER, camera, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Native name labeled within its code

---

### SET_CAM_DOF_FOCUS_DISTANCE_BIAS

**Hash:** `0xC669EEA5D031B7DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_FOCUS_DISTANCE_BIAS(Cam camera, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_FOCUS_DISTANCE_BIAS, camera, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native has a name defined inside its code

---

### SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE

**Hash:** `0xC3654A441402562D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE(Cam camera, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE, camera, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native has a name defined inside its code

---

### SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE_BLEND_LEVEL

**Hash:** `0x2C654B4943BDDF7C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE_BLEND_LEVEL(Cam camera, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_MAX_NEAR_IN_FOCUS_DISTANCE_BLEND_LEVEL, camera, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native has a name defined inside its code

---

### SET_CAM_DOF_SHOULD_KEEP_LOOK_AT_TARGET_IN_FOCUS

**Hash:** `0x7CF3AF51DCFE4108` | **Build:** `2944`

**C++ 原型:**
```cpp
void SET_CAM_DOF_SHOULD_KEEP_LOOK_AT_TARGET_IN_FOCUS(Cam camera, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DOF_SHOULD_KEEP_LOOK_AT_TARGET_IN_FOCUS, camera, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native has a name defined inside its code

---

### ATTACH_CAM_TO_ENTITY

**Hash:** `0xFEDB7D269E8C60E3` | **JHash:** `0xAD7C45F6` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_CAM_TO_ENTITY(Cam cam, Entity entity, float xOffset, float yOffset, float zOffset, BOOL isRelative)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_CAM_TO_ENTITY, cam, entity, xOffset, yOffset, zOffset, isRelative);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `isRelative` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Last param determines if its relative to the Entity

---

### ATTACH_CAM_TO_PED_BONE

**Hash:** `0x61A3DBA14AB7F411` | **JHash:** `0x506BB35C` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_CAM_TO_PED_BONE(Cam cam, Ped ped, int boneIndex, float x, float y, float z, BOOL heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_CAM_TO_PED_BONE, cam, ped, boneIndex, x, y, z, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `ped` | `Ped` | `int` | |
| `boneIndex` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HARD_ATTACH_CAM_TO_PED_BONE

**Hash:** `0x149916F50C34A40D` | **Build:** `1180`

**C++ 原型:**
```cpp
void HARD_ATTACH_CAM_TO_PED_BONE(Cam cam, Ped ped, int boneIndex, float p3, float p4, float p5, float p6, float p7, float p8, BOOL p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HARD_ATTACH_CAM_TO_PED_BONE, cam, ped, boneIndex, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `ped` | `Ped` | `int` | |
| `boneIndex` | `int` | `int` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HARD_ATTACH_CAM_TO_ENTITY

**Hash:** `0x202A5ED9CE01D6E7` | **Build:** `2189`

**C++ 原型:**
```cpp
void HARD_ATTACH_CAM_TO_ENTITY(Cam cam, Entity entity, float xRot, float yRot, float zRot, float xOffset, float yOffset, float zOffset, BOOL isRelative)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HARD_ATTACH_CAM_TO_ENTITY, cam, entity, xRot, yRot, zRot, xOffset, yOffset, zOffset, isRelative);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `entity` | `Entity` | `int` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `isRelative` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example from am_mp_drone script: 

CAM::HARD_ATTACH_CAM_TO_ENTITY(Local_190.f_169, NETWORK::NET_TO_OBJ(Local_190.f_159), 0f, 0f, 180f, Var0, 1);

---

### ATTACH_CAM_TO_VEHICLE_BONE

**Hash:** `0x8DB3F12A02CAEF72` | **Build:** `1290`

**C++ 原型:**
```cpp
void ATTACH_CAM_TO_VEHICLE_BONE(Cam cam, Vehicle vehicle, int boneIndex, BOOL relativeRotation, float rotX, float rotY, float rotZ, float offsetX, float offsetY, float offsetZ, BOOL fixedDirection)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_CAM_TO_VEHICLE_BONE, cam, vehicle, boneIndex, relativeRotation, rotX, rotY, rotZ, offsetX, offsetY, offsetZ, fixedDirection);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `boneIndex` | `int` | `int` | |
| `relativeRotation` | `BOOL` | `bool` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `fixedDirection` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native works with vehicles only. Bone indexes are usually given by this native GET_ENTITY_BONE_INDEX_BY_NAME.

---

### DETACH_CAM

**Hash:** `0xA2FABBE87F4BAD82` | **JHash:** `0xF4FBF14A` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_CAM(Cam cam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_CAM, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `void` → `void`

---

### SET_CAM_INHERIT_ROLL_VEHICLE

**Hash:** `0x45F1DE9C34B93AE6` | **JHash:** `0xE4BD5342` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_INHERIT_ROLL_VEHICLE(Cam cam, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_INHERIT_ROLL_VEHICLE, cam, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The native seems to only be called once.

The native is used as so,
CAM::SET_CAM_INHERIT_ROLL_VEHICLE(l_544, getElem(2, &l_525, 4));
In the exile1 script.

---

### POINT_CAM_AT_COORD

**Hash:** `0xF75497BB865F0803` | **JHash:** `0x914BC21A` | **Build:** `323`

**C++ 原型:**
```cpp
void POINT_CAM_AT_COORD(Cam cam, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POINT_CAM_AT_COORD, cam, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### POINT_CAM_AT_ENTITY

**Hash:** `0x5640BFF86B16E8DC` | **JHash:** `0x7597A0F7` | **Build:** `323`

**C++ 原型:**
```cpp
void POINT_CAM_AT_ENTITY(Cam cam, Entity entity, float p2, float p3, float p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POINT_CAM_AT_ENTITY, cam, entity, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `entity` | `Entity` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p5 always seems to be 1 i.e TRUE

---

### POINT_CAM_AT_PED_BONE

**Hash:** `0x68B2B5F33BA63C41` | **JHash:** `0x09F47049` | **Build:** `323`

**C++ 原型:**
```cpp
void POINT_CAM_AT_PED_BONE(Cam cam, Ped ped, int boneIndex, float x, float y, float z, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POINT_CAM_AT_PED_BONE, cam, ped, boneIndex, x, y, z, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `ped` | `Ped` | `int` | |
| `boneIndex` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Parameters p0-p5 seems correct. The bool p6 is unknown, but through every X360 script it's always 1. Please correct p0-p5 if any prove to be wrong. 

---

### STOP_CAM_POINTING

**Hash:** `0xF33AB75780BA57DE` | **JHash:** `0x5435F6A5` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CAM_POINTING(Cam cam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CAM_POINTING, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `void` → `void`

---

### SET_CAM_AFFECTS_AIMING

**Hash:** `0x8C1DC7770C51DC8D` | **JHash:** `0x0C74F9AF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_AFFECTS_AIMING(Cam cam, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_AFFECTS_AIMING, cam, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Allows you to aim and shoot at the direction the camera is facing.

---

### SET_CAM_CONTROLS_MINI_MAP_HEADING

**Hash:** `0x661B5C8654ADD825` | **JHash:** `0xE1A0B2F1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_CONTROLS_MINI_MAP_HEADING(Cam cam, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_CONTROLS_MINI_MAP_HEADING, cam, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Rotates the radar to match the camera's Z rotation

---

### SET_CAM_IS_INSIDE_VEHICLE

**Hash:** `0xA2767257A320FC82` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_IS_INSIDE_VEHICLE(Cam cam, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_IS_INSIDE_VEHICLE, cam, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When set to true shadows appear more smooth but less detailed.
Set to false by default.

---

### ALLOW_MOTION_BLUR_DECAY

**Hash:** `0x271017B9BA825366` | **JHash:** `0x43220969` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_MOTION_BLUR_DECAY(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_MOTION_BLUR_DECAY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CAM_DEBUG_NAME

**Hash:** `0x1B93E0107865DD40` | **JHash:** `0x9B00DF3F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DEBUG_NAME(Cam camera, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DEBUG_NAME, camera, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### GET_DEBUG_CAM

**Hash:** `0x77C3CEC46BE286F6` | **Build:** `2372`

**C++ 原型:**
```cpp
Cam GET_DEBUG_CAM(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DEBUG_CAM);
```

**返回值:** `Cam` → `int`

---

### ADD_CAM_SPLINE_NODE

**Hash:** `0x8609C75EC438FB3B` | **JHash:** `0xAD3C7EAA` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_CAM_SPLINE_NODE(Cam camera, float x, float y, float z, float xRot, float yRot, float zRot, int length, int smoothingStyle, int rotationOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_CAM_SPLINE_NODE, camera, x, y, z, xRot, yRot, zRot, length, smoothingStyle, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `length` | `int` | `int` | |
| `smoothingStyle` | `int` | `int` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** I filled p1-p6 (the floats) as they are as other natives with 6 floats in a row are similar and I see no other method. So if a test from anyone proves them wrong please correct.

p7 (length) determines the length of the spline, affects camera path and duration of transition between previous node and this one

p8 big values ~100 will slow down the camera movement before reaching this node

p9 != 0 seems to override the rotation/pitch (bool?)

---

### ADD_CAM_SPLINE_NODE_USING_CAMERA_FRAME

**Hash:** `0x0A9F2A468B328E74` | **JHash:** `0x30510511` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_CAM_SPLINE_NODE_USING_CAMERA_FRAME(Cam cam, Cam cam2, int length, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_CAM_SPLINE_NODE_USING_CAMERA_FRAME, cam, cam2, length, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `cam2` | `Cam` | `int` | |
| `length` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 is the spline camera to which the node is being added.
p1 is the camera used to create the node.
p3 is always 3 in scripts. It might be smoothing style or rotation order.

---

### ADD_CAM_SPLINE_NODE_USING_CAMERA

**Hash:** `0x0FB82563989CF4FB` | **JHash:** `0xBA6C085B` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_CAM_SPLINE_NODE_USING_CAMERA(Cam cam, Cam cam2, int length, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_CAM_SPLINE_NODE_USING_CAMERA, cam, cam2, length, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `cam2` | `Cam` | `int` | |
| `length` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 is the spline camera to which the node is being added.
p1 is the camera used to create the node.
p3 is always 3 in scripts. It might be smoothing style or rotation order.

---

### ADD_CAM_SPLINE_NODE_USING_GAMEPLAY_FRAME

**Hash:** `0x609278246A29CA34` | **JHash:** `0xB4737F03` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_CAM_SPLINE_NODE_USING_GAMEPLAY_FRAME(Cam cam, int length, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_CAM_SPLINE_NODE_USING_GAMEPLAY_FRAME, cam, length, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `length` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p2 is always 2 in scripts. It might be smoothing style or rotation order.

---

### SET_CAM_SPLINE_PHASE

**Hash:** `0x242B5874F0A4E052` | **JHash:** `0xF0AED233` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_PHASE(Cam cam, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_PHASE, cam, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CAM_SPLINE_PHASE

**Hash:** `0xB5349E36C546509A` | **JHash:** `0x39784DD9` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_SPLINE_PHASE(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_SPLINE_PHASE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

**说明:** Can use this with SET_CAM_SPLINE_PHASE to set the float it this native returns.

(returns 1.0f when no nodes has been added, reached end of non existing spline)

---

### GET_CAM_SPLINE_NODE_PHASE

**Hash:** `0xD9D0E694C8282C96` | **JHash:** `0x7B9522F6` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_SPLINE_NODE_PHASE(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_SPLINE_NODE_PHASE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

**说明:** I'm pretty sure the parameter is the camera as usual, but I am not certain so I'm going to leave it as is.

---

### SET_CAM_SPLINE_DURATION

**Hash:** `0x1381539FEE034CDA` | **JHash:** `0x3E91FC8A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_DURATION(Cam cam, int timeDuration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_DURATION, cam, timeDuration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `timeDuration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** I named p1 as timeDuration as it is obvious. I'm assuming tho it is ran in ms(Milliseconds) as usual.

---

### SET_CAM_SPLINE_SMOOTHING_STYLE

**Hash:** `0xD1B0F412F109EA5D` | **JHash:** `0x15E141CE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_SMOOTHING_STYLE(Cam cam, int smoothingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_SMOOTHING_STYLE, cam, smoothingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `smoothingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CAM_SPLINE_NODE_INDEX

**Hash:** `0xB22B17DF858716A6` | **JHash:** `0xF8AEB6BD` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CAM_SPLINE_NODE_INDEX(Cam cam)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CAM_SPLINE_NODE_INDEX, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `int` → `int`

---

### SET_CAM_SPLINE_NODE_EASE

**Hash:** `0x83B8201ED82A9A2D` | **JHash:** `0x21D275DA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_NODE_EASE(Cam cam, int easingFunction, int p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_NODE_EASE, cam, easingFunction, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `easingFunction` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAM_SPLINE_NODE_VELOCITY_SCALE

**Hash:** `0xA6385DEB180F319F` | **JHash:** `0xA3BD9E94` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_NODE_VELOCITY_SCALE(Cam cam, int p1, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_NODE_VELOCITY_SCALE, cam, p1, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `int` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### OVERRIDE_CAM_SPLINE_VELOCITY

**Hash:** `0x40B62FA033EB0346` | **JHash:** `0x326A17E2` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_CAM_SPLINE_VELOCITY(Cam cam, int p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_CAM_SPLINE_VELOCITY, cam, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### OVERRIDE_CAM_SPLINE_MOTION_BLUR

**Hash:** `0x7DCF7C708D292D55` | **JHash:** `0x633179E6` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_CAM_SPLINE_MOTION_BLUR(Cam cam, int p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_CAM_SPLINE_MOTION_BLUR, cam, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Max value for p1 is 15.

---

### SET_CAM_SPLINE_NODE_EXTRA_FLAGS

**Hash:** `0x7BF1A54AE67AC070` | **JHash:** `0xC90B2DDC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SPLINE_NODE_EXTRA_FLAGS(Cam cam, int p1, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SPLINE_NODE_EXTRA_FLAGS, cam, p1, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_CAM_SPLINE_PAUSED

**Hash:** `0x0290F35C0AD97864` | **JHash:** `0x60B34FF5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_SPLINE_PAUSED(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_SPLINE_PAUSED, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _INTERPOLATE_CAM_WITH_PARAMS

**Hash:** `0xDDA77EE33C005AAF` | **Build:** `3258`

**C++ 原型:**
```cpp
void _INTERPOLATE_CAM_WITH_PARAMS(Cam camera, float camPosX, float camPosY, float camPosZ, float camRotX, float camRotY, float camRotZ, float fov, int duration, int posCurveType, int rotCurveType, int rotOrder, int fovCurveType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._INTERPOLATE_CAM_WITH_PARAMS, camera, camPosX, camPosY, camPosZ, camRotX, camRotY, camRotZ, fov, duration, posCurveType, rotCurveType, rotOrder, fovCurveType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camera` | `Cam` | `int` | |
| `camPosX` | `float` | `float` | |
| `camPosY` | `float` | `float` | |
| `camPosZ` | `float` | `float` | |
| `camRotX` | `float` | `float` | |
| `camRotY` | `float` | `float` | |
| `camRotZ` | `float` | `float` | |
| `fov` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `posCurveType` | `int` | `int` | |
| `rotCurveType` | `int` | `int` | |
| `rotOrder` | `int` | `int` | |
| `fovCurveType` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _ACTIVATE_CAM_WITH_INTERP_AND_FOV_CURVE

**Hash:** `0x34CFC4C2A38E83E3` | **Build:** `3258`

**C++ 原型:**
```cpp
void _ACTIVATE_CAM_WITH_INTERP_AND_FOV_CURVE(Cam camTo, Cam camFrom, int duration, int easeLocation, int easeRotation, int easeFov)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._ACTIVATE_CAM_WITH_INTERP_AND_FOV_CURVE, camTo, camFrom, duration, easeLocation, easeRotation, easeFov);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camTo` | `Cam` | `int` | |
| `camFrom` | `Cam` | `int` | |
| `duration` | `int` | `int` | |
| `easeLocation` | `int` | `int` | |
| `easeRotation` | `int` | `int` | |
| `easeFov` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_CAM_ACTIVE_WITH_INTERP

**Hash:** `0x9FBDA379383A52A4` | **JHash:** `0x7983E7F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_ACTIVE_WITH_INTERP(Cam camTo, Cam camFrom, int duration, int easeLocation, int easeRotation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_ACTIVE_WITH_INTERP, camTo, camFrom, duration, easeLocation, easeRotation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camTo` | `Cam` | `int` | |
| `camFrom` | `Cam` | `int` | |
| `duration` | `int` | `int` | |
| `easeLocation` | `int` | `int` | |
| `easeRotation` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Previous declaration void SET_CAM_ACTIVE_WITH_INTERP(Cam camTo, Cam camFrom, int duration, BOOL easeLocation, BOOL easeRotation) is completely wrong. The last two params are integers not BOOLs...


---

### IS_CAM_INTERPOLATING

**Hash:** `0x036F97C908C2B52C` | **JHash:** `0x7159CB5D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_INTERPOLATING(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_INTERPOLATING, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SHAKE_CAM

**Hash:** `0x6A25241C340D3822` | **JHash:** `0x1D4211B0` | **Build:** `323`

**C++ 原型:**
```cpp
void SHAKE_CAM(Cam cam, const char* type, float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHAKE_CAM, cam, type, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `type` | `const char*` | `string` | |
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Possible shake types (updated b617d):

DEATH_FAIL_IN_EFFECT_SHAKE
DRUNK_SHAKE
FAMILY5_DRUG_TRIP_SHAKE
HAND_SHAKE
JOLT_SHAKE
LARGE_EXPLOSION_SHAKE
MEDIUM_EXPLOSION_SHAKE
SMALL_EXPLOSION_SHAKE
ROAD_VIBRATION_SHAKE
SKY_DIVING_SHAKE
VIBRATE_SHAKE

Full list of cam shake types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/camShakeTypesCompact.json

---

### ANIMATED_SHAKE_CAM

**Hash:** `0xA2746EEAE3E577CD` | **JHash:** `0xE1168767` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMATED_SHAKE_CAM(Cam cam, const char* p1, const char* p2, const char* p3, float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMATED_SHAKE_CAM, cam, p1, p2, p3, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `const char*` | `string` | |
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Example from michael2 script.

CAM::ANIMATED_SHAKE_CAM(l_5069, "shake_cam_all@", "light", "", 1f);

---

### IS_CAM_SHAKING

**Hash:** `0x6B24BFE83A2BE47B` | **JHash:** `0x0961FD9B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_SHAKING(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_SHAKING, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_CAM_SHAKE_AMPLITUDE

**Hash:** `0xD93DB43B82BC0D00` | **JHash:** `0x60FF6382` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_SHAKE_AMPLITUDE(Cam cam, float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_SHAKE_AMPLITUDE, cam, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_CAM_SHAKING

**Hash:** `0xBDECF64367884AC3` | **JHash:** `0x40D0EB87` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CAM_SHAKING(Cam cam, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CAM_SHAKING, cam, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHAKE_SCRIPT_GLOBAL

**Hash:** `0xF4C8CF9E353AFECA` | **JHash:** `0x2B0F05CD` | **Build:** `323`

**C++ 原型:**
```cpp
void SHAKE_SCRIPT_GLOBAL(const char* p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHAKE_SCRIPT_GLOBAL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** CAM::SHAKE_SCRIPT_GLOBAL("HAND_SHAKE", 0.2);

Full list of cam shake types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/camShakeTypesCompact.json

---

### ANIMATED_SHAKE_SCRIPT_GLOBAL

**Hash:** `0xC2EAE3FB8CDBED31` | **JHash:** `0xCB75BD9C` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMATED_SHAKE_SCRIPT_GLOBAL(const char* p0, const char* p1, const char* p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMATED_SHAKE_SCRIPT_GLOBAL, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** CAM::ANIMATED_SHAKE_SCRIPT_GLOBAL("SHAKE_CAM_medium", "medium", "", 0.5f);

Full list of cam shake types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/camShakeTypesCompact.json

---

### IS_SCRIPT_GLOBAL_SHAKING

**Hash:** `0xC912AF078AF19212` | **JHash:** `0x6AEFE6A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPT_GLOBAL_SHAKING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPT_GLOBAL_SHAKING);
```

**返回值:** `BOOL` → `bool`

**说明:** In drunk_controller.c4, sub_309
if (CAM::IS_SCRIPT_GLOBAL_SHAKING()) {
    CAM::STOP_SCRIPT_GLOBAL_SHAKING(0);
}

---

### STOP_SCRIPT_GLOBAL_SHAKING

**Hash:** `0x1C9D7949FA533490` | **JHash:** `0x26FCFB96` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SCRIPT_GLOBAL_SHAKING(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SCRIPT_GLOBAL_SHAKING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** In drunk_controller.c4, sub_309
if (CAM::IS_SCRIPT_GLOBAL_SHAKING()) {
    CAM::STOP_SCRIPT_GLOBAL_SHAKING(0);
}

---

### TRIGGER_VEHICLE_PART_BROKEN_CAMERA_SHAKE

**Hash:** `0x5D96CFB59DA076A0` | **Build:** `2060`

**C++ 原型:**
```cpp
void TRIGGER_VEHICLE_PART_BROKEN_CAMERA_SHAKE(Vehicle vehicle, int p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_VEHICLE_PART_BROKEN_CAMERA_SHAKE, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p1: 0..16

---

### PLAY_CAM_ANIM

**Hash:** `0x9A2D0FB2E7852392` | **JHash:** `0xBCEFB87E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_CAM_ANIM(Cam cam, const char* animName, const char* animDictionary, float x, float y, float z, float xRot, float yRot, float zRot, BOOL p9, int p10)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_CAM_ANIM, cam, animName, animDictionary, x, y, z, xRot, yRot, zRot, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `animName` | `const char*` | `string` | |
| `animDictionary` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Atleast one time in a script for the zRot Rockstar uses GET_ENTITY_HEADING to help fill the parameter.

p9 is unknown at this time.
p10 throughout all the X360 Scripts is always 2.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### IS_CAM_PLAYING_ANIM

**Hash:** `0xC90621D8A0CEECF2` | **JHash:** `0xB998CB49` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CAM_PLAYING_ANIM(Cam cam, const char* animName, const char* animDictionary)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CAM_PLAYING_ANIM, cam, animName, animDictionary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `animName` | `const char*` | `string` | |
| `animDictionary` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SET_CAM_ANIM_CURRENT_PHASE

**Hash:** `0x4145A4C44FF3B5A6` | **JHash:** `0x3CB1D17F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_ANIM_CURRENT_PHASE(Cam cam, float phase)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_ANIM_CURRENT_PHASE, cam, phase);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `phase` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CAM_ANIM_CURRENT_PHASE

**Hash:** `0xA10B2DB49E92A6B0` | **JHash:** `0x345F72D0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CAM_ANIM_CURRENT_PHASE(Cam cam)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CAM_ANIM_CURRENT_PHASE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `float` → `float`

---

### PLAY_SYNCHRONIZED_CAM_ANIM

**Hash:** `0xE32EFE9AB4A9AA0C` | **JHash:** `0x9458459E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_SYNCHRONIZED_CAM_ANIM(Any p0, Any p1, const char* animName, const char* animDictionary)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_SYNCHRONIZED_CAM_ANIM, p0, p1, animName, animDictionary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `animName` | `const char*` | `string` | |
| `animDictionary` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Examples:

CAM::PLAY_SYNCHRONIZED_CAM_ANIM(l_2734, NETWORK::NETWORK_GET_LOCAL_SCENE_FROM_NETWORK_ID(l_2739), "PLAYER_EXIT_L_CAM", "mp_doorbell");

CAM::PLAY_SYNCHRONIZED_CAM_ANIM(l_F0D[7/*1*/], l_F4D[15/*1*/], "ah3b_attackheli_cam2", "missheistfbi3b_helicrash");

---

### SET_FLY_CAM_HORIZONTAL_RESPONSE

**Hash:** `0x503F5920162365B2` | **JHash:** `0x56F9ED27` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLY_CAM_HORIZONTAL_RESPONSE(Cam cam, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLY_CAM_HORIZONTAL_RESPONSE, cam, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLY_CAM_VERTICAL_RESPONSE

**Hash:** `0xE827B9382CFB41BA` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_FLY_CAM_VERTICAL_RESPONSE(Cam cam, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLY_CAM_VERTICAL_RESPONSE, cam, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLY_CAM_MAX_HEIGHT

**Hash:** `0xF9D02130ECDD1D77` | **JHash:** `0x71570DBA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLY_CAM_MAX_HEIGHT(Cam cam, float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLY_CAM_MAX_HEIGHT, cam, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLY_CAM_COORD_AND_CONSTRAIN

**Hash:** `0xC91C6C55199308CA` | **JHash:** `0x60B345DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLY_CAM_COORD_AND_CONSTRAIN(Cam cam, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLY_CAM_COORD_AND_CONSTRAIN, cam, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLY_CAM_VERTICAL_CONTROLS_THIS_UPDATE

**Hash:** `0xC8B5C4A79CC18B94` | **JHash:** `0x44473EFC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLY_CAM_VERTICAL_CONTROLS_THIS_UPDATE(Cam cam)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLY_CAM_VERTICAL_CONTROLS_THIS_UPDATE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `void` → `void`

---

### WAS_FLY_CAM_CONSTRAINED_ON_PREVIOUS_UDPATE

**Hash:** `0x5C48A1D6E3B33179` | **JHash:** `0xDA931D65` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_FLY_CAM_CONSTRAINED_ON_PREVIOUS_UDPATE(Cam cam)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_FLY_CAM_CONSTRAINED_ON_PREVIOUS_UDPATE, cam);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cam` | `Cam` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_SCREEN_FADED_OUT

**Hash:** `0xB16FCE9DDC7BA182` | **JHash:** `0x9CAA05FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCREEN_FADED_OUT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCREEN_FADED_OUT);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCREEN_FADED_IN

**Hash:** `0x5A859503B0C08678` | **JHash:** `0x4F37276D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCREEN_FADED_IN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCREEN_FADED_IN);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCREEN_FADING_OUT

**Hash:** `0x797AC7CB535BA28F` | **JHash:** `0x79275A57` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCREEN_FADING_OUT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCREEN_FADING_OUT);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCREEN_FADING_IN

**Hash:** `0x5C544BC6C57AC575` | **JHash:** `0xC7C82800` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCREEN_FADING_IN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCREEN_FADING_IN);
```

**返回值:** `BOOL` → `bool`

---

### DO_SCREEN_FADE_IN

**Hash:** `0xD4E8E24955024033` | **JHash:** `0x66C1BDEE` | **Build:** `323`

**C++ 原型:**
```cpp
void DO_SCREEN_FADE_IN(int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DO_SCREEN_FADE_IN, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Fades the screen in.

duration: The time the fade should take, in milliseconds.

---

### DO_SCREEN_FADE_OUT

**Hash:** `0x891B5B39AC6302AF` | **JHash:** `0x89D01805` | **Build:** `323`

**C++ 原型:**
```cpp
void DO_SCREEN_FADE_OUT(int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DO_SCREEN_FADE_OUT, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Fades the screen out.

duration: The time the fade should take, in milliseconds.

---

### SET_WIDESCREEN_BORDERS

**Hash:** `0xDCD4EA924F42D01A` | **JHash:** `0x1A75DC9A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WIDESCREEN_BORDERS(BOOL p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WIDESCREEN_BORDERS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ARE_WIDESCREEN_BORDERS_ACTIVE

**Hash:** `0x4879E4FE39074CDF` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL ARE_WIDESCREEN_BORDERS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_WIDESCREEN_BORDERS_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### GET_GAMEPLAY_CAM_COORD

**Hash:** `0x14D6F5678D8F1B37` | **JHash:** `0x9388CF79` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_GAMEPLAY_CAM_COORD(void)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_GAMEPLAY_CAM_COORD);
```

**返回值:** `Vector3` → `Vector3`

---

### GET_GAMEPLAY_CAM_ROT

**Hash:** `0x837765A25378F0BB` | **JHash:** `0x13A010B5` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_GAMEPLAY_CAM_ROT(int rotationOrder)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_GAMEPLAY_CAM_ROT, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rotationOrder` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** p0 dosen't seem to change much, I tried it with 0, 1, 2:
0-Pitch(X): -70.000092
0-Roll(Y): -0.000001
0-Yaw(Z): -43.886459
1-Pitch(X): -70.000092
1-Roll(Y): -0.000001
1-Yaw(Z): -43.886463
2-Pitch(X): -70.000092
2-Roll(Y): -0.000002
2-Yaw(Z): -43.886467

---

### GET_GAMEPLAY_CAM_FOV

**Hash:** `0x65019750A0324133` | **JHash:** `0x4D6B3BFA` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_GAMEPLAY_CAM_FOV(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_GAMEPLAY_CAM_FOV);
```

**返回值:** `float` → `float`

---

### SET_GAMEPLAY_CAM_MOTION_BLUR_SCALING_THIS_UPDATE

**Hash:** `0x487A82C650EB7799` | **JHash:** `0xA6E73135` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_MOTION_BLUR_SCALING_THIS_UPDATE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_MOTION_BLUR_SCALING_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** some camera effect that is used in the drunk-cheat, and turned off (by setting it to 0.0) along with the shaking effects once the drunk cheat is disabled.

---

### SET_GAMEPLAY_CAM_MAX_MOTION_BLUR_STRENGTH_THIS_UPDATE

**Hash:** `0x0225778816FDC28C` | **JHash:** `0x1126E37C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_MAX_MOTION_BLUR_STRENGTH_THIS_UPDATE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_MAX_MOTION_BLUR_STRENGTH_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** some camera effect that is (also) used in the drunk-cheat, and turned off (by setting it to 0.0) along with the shaking effects once the drunk cheat is disabled.

---

### GET_GAMEPLAY_CAM_RELATIVE_HEADING

**Hash:** `0x743607648ADD4587` | **JHash:** `0xCAF839C2` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_GAMEPLAY_CAM_RELATIVE_HEADING(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_GAMEPLAY_CAM_RELATIVE_HEADING);
```

**返回值:** `float` → `float`

---

### SET_GAMEPLAY_CAM_RELATIVE_HEADING

**Hash:** `0xB4EC2312F4E5B1F1` | **JHash:** `0x20C6217C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_RELATIVE_HEADING(float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_RELATIVE_HEADING, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the camera position relative to heading in float from -360 to +360.

Heading is alwyas 0 in aiming camera.

---

### GET_GAMEPLAY_CAM_RELATIVE_PITCH

**Hash:** `0x3A6867B4845BEDA2` | **JHash:** `0xFC5A4946` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_GAMEPLAY_CAM_RELATIVE_PITCH(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_GAMEPLAY_CAM_RELATIVE_PITCH);
```

**返回值:** `float` → `float`

---

### SET_GAMEPLAY_CAM_RELATIVE_PITCH

**Hash:** `0x6D0858B8EDFD2B7D` | **JHash:** `0x6381B963` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_RELATIVE_PITCH(float angle, float scalingFactor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_RELATIVE_PITCH, angle, scalingFactor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `angle` | `float` | `float` | |
| `scalingFactor` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native sets the camera's pitch (rotation on the x-axis).

---

### RESET_GAMEPLAY_CAM_FULL_ATTACH_PARENT_TRANSFORM_TIMER

**Hash:** `0x7295C203DD659DFE` | **Build:** `2699`

**C++ 原型:**
```cpp
void RESET_GAMEPLAY_CAM_FULL_ATTACH_PARENT_TRANSFORM_TIMER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_GAMEPLAY_CAM_FULL_ATTACH_PARENT_TRANSFORM_TIMER);
```

**返回值:** `void` → `void`

---

### FORCE_CAMERA_RELATIVE_HEADING_AND_PITCH

**Hash:** `0x48608C3464F58AB4` | **Build:** `505`

**C++ 原型:**
```cpp
void FORCE_CAMERA_RELATIVE_HEADING_AND_PITCH(float roll, float pitch, float yaw)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CAMERA_RELATIVE_HEADING_AND_PITCH, roll, pitch, yaw);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `roll` | `float` | `float` | |
| `pitch` | `float` | `float` | |
| `yaw` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_BONNET_CAMERA_RELATIVE_HEADING_AND_PITCH

**Hash:** `0x28B022A17B068A3A` | **Build:** `1734`

**C++ 原型:**
```cpp
void FORCE_BONNET_CAMERA_RELATIVE_HEADING_AND_PITCH(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_BONNET_CAMERA_RELATIVE_HEADING_AND_PITCH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_SHOOTER_CAMERA_HEADING

**Hash:** `0x103991D4A307D472` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_SHOOTER_CAMERA_HEADING(float yaw)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_SHOOTER_CAMERA_HEADING, yaw);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `yaw` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Does nothing

---

### SET_FIRST_PERSON_SHOOTER_CAMERA_PITCH

**Hash:** `0x759E13EBC1C15C5A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_SHOOTER_CAMERA_PITCH(float pitch)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_SHOOTER_CAMERA_PITCH, pitch);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pitch` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_SCRIPTED_CAMERA_IS_FIRST_PERSON_THIS_FRAME

**Hash:** `0x469F2ECDEC046337` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPTED_CAMERA_IS_FIRST_PERSON_THIS_FRAME(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPTED_CAMERA_IS_FIRST_PERSON_THIS_FRAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHAKE_GAMEPLAY_CAM

**Hash:** `0xFD55E49555E017CF` | **JHash:** `0xF2EFE660` | **Build:** `323`

**C++ 原型:**
```cpp
void SHAKE_GAMEPLAY_CAM(const char* shakeName, float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHAKE_GAMEPLAY_CAM, shakeName, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shakeName` | `const char*` | `string` | |
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Possible shake types (updated b617d):

DEATH_FAIL_IN_EFFECT_SHAKE
DRUNK_SHAKE
FAMILY5_DRUG_TRIP_SHAKE
HAND_SHAKE
JOLT_SHAKE
LARGE_EXPLOSION_SHAKE
MEDIUM_EXPLOSION_SHAKE
SMALL_EXPLOSION_SHAKE
ROAD_VIBRATION_SHAKE
SKY_DIVING_SHAKE
VIBRATE_SHAKE

Full list of cam shake types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/camShakeTypesCompact.json

---

### IS_GAMEPLAY_CAM_SHAKING

**Hash:** `0x016C090630DF1F89` | **JHash:** `0x3457D681` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GAMEPLAY_CAM_SHAKING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GAMEPLAY_CAM_SHAKING);
```

**返回值:** `BOOL` → `bool`

---

### SET_GAMEPLAY_CAM_SHAKE_AMPLITUDE

**Hash:** `0xA87E00932DB4D85D` | **JHash:** `0x9219D44A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_SHAKE_AMPLITUDE(float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_SHAKE_AMPLITUDE, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the amplitude for the gameplay (i.e. 3rd or 1st) camera to shake. Used in script "drunk_controller.ysc.c4" to simulate making the player drunk.

---

### STOP_GAMEPLAY_CAM_SHAKING

**Hash:** `0x0EF93E9F3D08C178` | **JHash:** `0xFD569E4E` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_GAMEPLAY_CAM_SHAKING(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_GAMEPLAY_CAM_SHAKING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_CAM_FOLLOW_PED_THIS_UPDATE

**Hash:** `0x8BBACBF51DA047A8` | **JHash:** `0x7D3007A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_FOLLOW_PED_THIS_UPDATE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_FOLLOW_PED_THIS_UPDATE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Forces gameplay cam to specified ped as if you were the ped or spectating it

---

### IS_GAMEPLAY_CAM_RENDERING

**Hash:** `0x39B5D1B10383F0C8` | **JHash:** `0x0EF276DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GAMEPLAY_CAM_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GAMEPLAY_CAM_RENDERING);
```

**返回值:** `BOOL` → `bool`

**说明:** Examples when this function will return 0 are:
- During busted screen.
- When player is coming out from a hospital.
- When player is coming out from a police station.
- When player is buying gun from AmmuNation.

---

### IS_INTERPOLATING_FROM_SCRIPT_CAMS

**Hash:** `0x3044240D2E0FA842` | **JHash:** `0xC0B00C20` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERPOLATING_FROM_SCRIPT_CAMS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERPOLATING_FROM_SCRIPT_CAMS);
```

**返回值:** `BOOL` → `bool`

---

### IS_INTERPOLATING_TO_SCRIPT_CAMS

**Hash:** `0x705A276EBFF3133D` | **JHash:** `0x60C23785` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERPOLATING_TO_SCRIPT_CAMS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERPOLATING_TO_SCRIPT_CAMS);
```

**返回值:** `BOOL` → `bool`

---

### SET_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_STATE

**Hash:** `0xDB90C6CCA48940F1` | **JHash:** `0x20BFF6E5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_STATE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_THIS_UPDATE

**Hash:** `0xEA7F0AD7E9BA676F` | **JHash:** `0xA61FF9AC` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_GAMEPLAY_CAM_ALTITUDE_FOV_SCALING_THIS_UPDATE);
```

**返回值:** `void` → `void`

**说明:** Shows the crosshair even if it wouldn't show normally. Only works for one frame, so make sure to call it repeatedly.

---

### IS_GAMEPLAY_CAM_LOOKING_BEHIND

**Hash:** `0x70FDA869F3317EA9` | **JHash:** `0x33C83F17` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GAMEPLAY_CAM_LOOKING_BEHIND(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GAMEPLAY_CAM_LOOKING_BEHIND);
```

**返回值:** `BOOL` → `bool`

---

### SET_GAMEPLAY_CAM_IGNORE_ENTITY_COLLISION_THIS_UPDATE

**Hash:** `0x2AED6301F67007D5` | **JHash:** `0x2701A9AD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_IGNORE_ENTITY_COLLISION_THIS_UPDATE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_IGNORE_ENTITY_COLLISION_THIS_UPDATE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### DISABLE_CAM_COLLISION_FOR_OBJECT

**Hash:** `0x49482F9FCD825AAA` | **JHash:** `0xC4736ED3` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_CAM_COLLISION_FOR_OBJECT(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_CAM_COLLISION_FOR_OBJECT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### BYPASS_CAMERA_COLLISION_BUOYANCY_TEST_THIS_UPDATE

**Hash:** `0xA7092AFE81944852` | **Build:** `2189`

**C++ 原型:**
```cpp
void BYPASS_CAMERA_COLLISION_BUOYANCY_TEST_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BYPASS_CAMERA_COLLISION_BUOYANCY_TEST_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_CAM_ENTITY_TO_LIMIT_FOCUS_OVER_BOUNDING_SPHERE_THIS_UPDATE

**Hash:** `0xFD3151CD37EA2245` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_CAM_ENTITY_TO_LIMIT_FOCUS_OVER_BOUNDING_SPHERE_THIS_UPDATE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_CAM_ENTITY_TO_LIMIT_FOCUS_OVER_BOUNDING_SPHERE_THIS_UPDATE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### DISABLE_FIRST_PERSON_CAMERA_WATER_CLIPPING_TEST_THIS_UPDATE

**Hash:** `0xB1381B97F70C7B30` | **Build:** `1180`

**C++ 原型:**
```cpp
void DISABLE_FIRST_PERSON_CAMERA_WATER_CLIPPING_TEST_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_FIRST_PERSON_CAMERA_WATER_CLIPPING_TEST_THIS_UPDATE);
```

**返回值:** `void` → `void`

**说明:** Sets some flag on cinematic camera

---

### SET_FOLLOW_CAM_IGNORE_ATTACH_PARENT_MOVEMENT_THIS_UPDATE

**Hash:** `0xDD79DF9F4D26E1C9` | **JHash:** `0x6B0E9D57` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_CAM_IGNORE_ATTACH_PARENT_MOVEMENT_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_CAM_IGNORE_ATTACH_PARENT_MOVEMENT_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### IS_SPHERE_VISIBLE

**Hash:** `0xE33D59DA70B58FDF` | **JHash:** `0xDD1329E2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPHERE_VISIBLE(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPHERE_VISIBLE, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### IS_FOLLOW_PED_CAM_ACTIVE

**Hash:** `0xC6D3D26810C8E0F9` | **JHash:** `0x9F9E856C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FOLLOW_PED_CAM_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FOLLOW_PED_CAM_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_FOLLOW_PED_CAM_THIS_UPDATE

**Hash:** `0x44A113DD6FFC48D1` | **JHash:** `0x1425F6AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_FOLLOW_PED_CAM_THIS_UPDATE(const char* camName, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_FOLLOW_PED_CAM_THIS_UPDATE, camName, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camName` | `const char*` | `string` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** From the scripts:

CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_ATTACHED_TO_ROPE_CAMERA", 0);
CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_ON_EXILE1_LADDER_CAMERA", 1500);
CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_SKY_DIVING_CAMERA", 0);
CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_SKY_DIVING_CAMERA", 3000);
CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_SKY_DIVING_FAMILY5_CAMERA", 0);
CAM::SET_FOLLOW_PED_CAM_THIS_UPDATE("FOLLOW_PED_SKY_DIVING_CAMERA", 0);

---

### USE_SCRIPT_CAM_FOR_AMBIENT_POPULATION_ORIGIN_THIS_FRAME

**Hash:** `0x271401846BD26E92` | **JHash:** `0x8DC53629` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_SCRIPT_CAM_FOR_AMBIENT_POPULATION_ORIGIN_THIS_FRAME(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_SCRIPT_CAM_FOR_AMBIENT_POPULATION_ORIGIN_THIS_FRAME, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_FOLLOW_PED_CAM_LADDER_ALIGN_THIS_UPDATE

**Hash:** `0xC8391C309684595A` | **JHash:** `0x1F9DE6E4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_PED_CAM_LADDER_ALIGN_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_PED_CAM_LADDER_ALIGN_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_THIRD_PERSON_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE

**Hash:** `0x8F993D26E0CA5E8E` | **JHash:** `0x749909AC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIRD_PERSON_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE(float minimum, float maximum)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIRD_PERSON_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE, minimum, maximum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minimum` | `float` | `float` | |
| `maximum` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** minimum: Degrees between -180f and 180f.
maximum: Degrees between -180f and 180f.

Clamps the gameplay camera's current yaw.

Eg. SET_THIRD_PERSON_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE(0.0f, 0.0f) will set the horizontal angle directly behind the player.

---

### SET_THIRD_PERSON_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE

**Hash:** `0xA516C198B7DCA1E1` | **JHash:** `0xFA3A16E7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIRD_PERSON_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE(float minimum, float maximum)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIRD_PERSON_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE, minimum, maximum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minimum` | `float` | `float` | |
| `maximum` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** minimum: Degrees between -90f and 90f.
maximum: Degrees between -90f and 90f.

Clamps the gameplay camera's current pitch.

Eg. SET_THIRD_PERSON_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE(0.0f, 0.0f) will set the vertical angle directly behind the player.

---

### SET_THIRD_PERSON_CAM_ORBIT_DISTANCE_LIMITS_THIS_UPDATE

**Hash:** `0xDF2E1F7742402E81` | **JHash:** `0x77340650` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIRD_PERSON_CAM_ORBIT_DISTANCE_LIMITS_THIS_UPDATE(float p0, float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIRD_PERSON_CAM_ORBIT_DISTANCE_LIMITS_THIS_UPDATE, p0, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Seems to animate the gameplay camera zoom.

Eg. SET_THIRD_PERSON_CAM_ORBIT_DISTANCE_LIMITS_THIS_UPDATE(1f, 1000f);
will animate the camera zooming in from 1000 meters away.

Game scripts use it like this:

// Setting this to 1 prevents V key from changing zoom
PLAYER::SET_PLAYER_FORCED_ZOOM(PLAYER::PLAYER_ID(), 1);

// These restrict how far you can move cam up/down left/right
CAM::SET_THIRD_PERSON_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE(-20f, 50f);
CAM::SET_THIRD_PERSON_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE(-60f, 0f);

CAM::SET_THIRD_PERSON_CAM_ORBIT_DISTANCE_LIMITS_THIS_UPDATE(1f, 1f);

---

### _GET_THIRD_PERSON_CAM_MIN_ORBIT_DISTANCE_SPRING

**Hash:** `0xBC456FB703431785` | **Build:** `3095`

**C++ 原型:**
```cpp
float _GET_THIRD_PERSON_CAM_MIN_ORBIT_DISTANCE_SPRING(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_THIRD_PERSON_CAM_MIN_ORBIT_DISTANCE_SPRING);
```

**返回值:** `float` → `float`

---

### _GET_THIRD_PERSON_CAM_MAX_ORBIT_DISTANCE_SPRING

**Hash:** `0xD4592A16D36673ED` | **Build:** `3095`

**C++ 原型:**
```cpp
float _GET_THIRD_PERSON_CAM_MAX_ORBIT_DISTANCE_SPRING(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_THIRD_PERSON_CAM_MAX_ORBIT_DISTANCE_SPRING);
```

**返回值:** `float` → `float`

---

### SET_IN_VEHICLE_CAM_STATE_THIS_UPDATE

**Hash:** `0xE9EA16D6E54CDCA4` | **JHash:** `0x4B22C5CB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IN_VEHICLE_CAM_STATE_THIS_UPDATE(Vehicle p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IN_VEHICLE_CAM_STATE_THIS_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Forces gameplay cam to specified vehicle as if you were in it

---

### DISABLE_ON_FOOT_FIRST_PERSON_VIEW_THIS_UPDATE

**Hash:** `0xDE2EF5DA284CC8DF` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_ON_FOOT_FIRST_PERSON_VIEW_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_ON_FOOT_FIRST_PERSON_VIEW_THIS_UPDATE);
```

**返回值:** `void` → `void`

**说明:** Disables first person camera for the current frame.

Found in decompiled scripts:
GRAPHICS::DRAW_DEBUG_TEXT_2D("Disabling First Person Cam", 0.5, 0.8, 0.0, 0, 0, 255, 255);
CAM::DISABLE_ON_FOOT_FIRST_PERSON_VIEW_THIS_UPDATE();

---

### DISABLE_FIRST_PERSON_FLASH_EFFECT_THIS_UPDATE

**Hash:** `0x59424BD75174C9B1` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_FIRST_PERSON_FLASH_EFFECT_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_FIRST_PERSON_FLASH_EFFECT_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### BLOCK_FIRST_PERSON_ORIENTATION_RESET_THIS_UPDATE

**Hash:** `0x9F97DA93681F87EA` | **Build:** `1734`

**C++ 原型:**
```cpp
void BLOCK_FIRST_PERSON_ORIENTATION_RESET_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_FIRST_PERSON_ORIENTATION_RESET_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### GET_FOLLOW_PED_CAM_ZOOM_LEVEL

**Hash:** `0x33E6C8EFD0CD93E9` | **JHash:** `0x57583DF1` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FOLLOW_PED_CAM_ZOOM_LEVEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FOLLOW_PED_CAM_ZOOM_LEVEL);
```

**返回值:** `int` → `int`

---

### GET_FOLLOW_PED_CAM_VIEW_MODE

**Hash:** `0x8D4D46230B2C353A` | **JHash:** `0xA65FF946` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FOLLOW_PED_CAM_VIEW_MODE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FOLLOW_PED_CAM_VIEW_MODE);
```

**返回值:** `int` → `int`

**说明:** See viewmode enum in CAM.GET_FOLLOW_VEHICLE_CAM_VIEW_MODE for return value

---

### SET_FOLLOW_PED_CAM_VIEW_MODE

**Hash:** `0x5A4F9EDF1673F704` | **JHash:** `0x495DBE8D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_PED_CAM_VIEW_MODE(int viewMode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_PED_CAM_VIEW_MODE, viewMode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `viewMode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the type of Player camera:

0 - Third Person Close
1 - Third Person Mid
2 - Third Person Far
4 - First Person

---

### IS_FOLLOW_VEHICLE_CAM_ACTIVE

**Hash:** `0xCBBDE6D335D6D496` | **JHash:** `0x8DD49B77` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FOLLOW_VEHICLE_CAM_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FOLLOW_VEHICLE_CAM_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_THIS_UPDATE

**Hash:** `0x91EF6EE6419E5B97` | **JHash:** `0x9DB5D391` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_THIS_UPDATE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_EVERY_UPDATE

**Hash:** `0x9DFE13ECDC1EC196` | **JHash:** `0x92302899` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_EVERY_UPDATE(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_VEHICLE_CAM_HIGH_ANGLE_MODE_EVERY_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TABLE_GAMES_CAMERA_THIS_UPDATE

**Hash:** `0x79C0E43EB9B944E2` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL SET_TABLE_GAMES_CAMERA_THIS_UPDATE(Hash hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_TABLE_GAMES_CAMERA_THIS_UPDATE, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### GET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL

**Hash:** `0xEE82280AB767B690` | **JHash:** `0x8CD67DE3` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL);
```

**返回值:** `int` → `int`

---

### SET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL

**Hash:** `0x19464CB6E4078C8A` | **JHash:** `0x8F55EBBE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL(int zoomLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_VEHICLE_CAM_ZOOM_LEVEL, zoomLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoomLevel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_FOLLOW_VEHICLE_CAM_VIEW_MODE

**Hash:** `0xA4FF579AC0E3AAAE` | **JHash:** `0xA4B4DB03` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FOLLOW_VEHICLE_CAM_VIEW_MODE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FOLLOW_VEHICLE_CAM_VIEW_MODE);
```

**返回值:** `int` → `int`

**说明:** Returns the type of camera:

enum _viewmode //0xA11D7CA8
{
	THIRD_PERSON_NEAR = 0,
	THIRD_PERSON_MEDIUM = 1,
	THIRD_PERSON_FAR = 2,
	CINEMATIC = 3,
	FIRST_PERSON = 4
};

---

### SET_FOLLOW_VEHICLE_CAM_VIEW_MODE

**Hash:** `0xAC253D7842768F48` | **JHash:** `0xC4FBBBD3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOLLOW_VEHICLE_CAM_VIEW_MODE(int viewMode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_VEHICLE_CAM_VIEW_MODE, viewMode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `viewMode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the type of Player camera in vehicles:
viewmode: see CAM.GET_FOLLOW_VEHICLE_CAM_VIEW_MODE

---

### GET_CAM_VIEW_MODE_FOR_CONTEXT

**Hash:** `0xEE778F8C7E1142E2` | **JHash:** `0xF3B148A6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CAM_VIEW_MODE_FOR_CONTEXT(int context)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CAM_VIEW_MODE_FOR_CONTEXT, context);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `context` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** context: see _GET_CAM_ACTIVE_VIEW_MODE_CONTEXT

---

### SET_CAM_VIEW_MODE_FOR_CONTEXT

**Hash:** `0x2A2173E46DAECD12` | **JHash:** `0x1DEBCB45` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_VIEW_MODE_FOR_CONTEXT(int context, int viewMode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_VIEW_MODE_FOR_CONTEXT, context, viewMode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `context` | `int` | `int` | |
| `viewMode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** context: see _GET_CAM_ACTIVE_VIEW_MODE_CONTEXT, viewmode: see CAM.GET_FOLLOW_VEHICLE_CAM_VIEW_MODE

---

### GET_CAM_ACTIVE_VIEW_MODE_CONTEXT

**Hash:** `0x19CAFA3C87F7C2FF` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CAM_ACTIVE_VIEW_MODE_CONTEXT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CAM_ACTIVE_VIEW_MODE_CONTEXT);
```

**返回值:** `int` → `int`

**说明:** enum Context
{
	ON_FOOT,
	IN_VEHICLE,
	ON_BIKE,
	IN_BOAT,
	IN_AIRCRAFT,
	IN_SUBMARINE,
	IN_HELI,
	IN_TURRET
};

---

### USE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE

**Hash:** `0x6493CF69859B116A` | **Build:** `791`

**C++ 原型:**
```cpp
void USE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### USE_DEDICATED_STUNT_CAMERA_THIS_UPDATE

**Hash:** `0x425A920FDB9A0DDA` | **Build:** `1180`

**C++ 原型:**
```cpp
void USE_DEDICATED_STUNT_CAMERA_THIS_UPDATE(const char* camName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_DEDICATED_STUNT_CAMERA_THIS_UPDATE, camName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `camName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Sets gameplay camera to hash

---

### FORCE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE

**Hash:** `0x0AA27680A0BD43FA` | **Build:** `1103`

**C++ 原型:**
```cpp
void FORCE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_VEHICLE_CAM_STUNT_SETTINGS_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_FOLLOW_VEHICLE_CAM_SEAT_THIS_UPDATE

**Hash:** `0x5C90CAB09951A12F` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_FOLLOW_VEHICLE_CAM_SEAT_THIS_UPDATE(int seatIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOLLOW_VEHICLE_CAM_SEAT_THIS_UPDATE, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `seatIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_AIM_CAM_ACTIVE

**Hash:** `0x68EDDA28A5976D07` | **JHash:** `0xC24B4F6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AIM_CAM_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AIM_CAM_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### IS_AIM_CAM_ACTIVE_IN_ACCURATE_MODE

**Hash:** `0x74BD83EA840F6BC9` | **JHash:** `0x8F320DE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AIM_CAM_ACTIVE_IN_ACCURATE_MODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AIM_CAM_ACTIVE_IN_ACCURATE_MODE);
```

**返回值:** `BOOL` → `bool`

---

### IS_FIRST_PERSON_AIM_CAM_ACTIVE

**Hash:** `0x5E346D934122613F` | **JHash:** `0xD6280468` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FIRST_PERSON_AIM_CAM_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FIRST_PERSON_AIM_CAM_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### DISABLE_AIM_CAM_THIS_UPDATE

**Hash:** `0x1A31FE0049E542F6` | **JHash:** `0x1BAA7182` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_AIM_CAM_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_AIM_CAM_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### GET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR

**Hash:** `0x7EC52CC40597D170` | **JHash:** `0x33951005` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR);
```

**返回值:** `float` → `float`

---

### SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR

**Hash:** `0x70894BD0915C5BCA` | **JHash:** `0x9F4AF763` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR(float zoomFactor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR, zoomFactor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoomFactor` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR_LIMITS_THIS_UPDATE

**Hash:** `0xCED08CBE8EBB97C7` | **JHash:** `0x68BA0730` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR_LIMITS_THIS_UPDATE(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_AIM_CAM_ZOOM_FACTOR_LIMITS_THIS_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_AIM_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE

**Hash:** `0x2F7F2B26DD3F18EE` | **JHash:** `0x2F29F0D5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_AIM_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_AIM_CAM_RELATIVE_HEADING_LIMITS_THIS_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_AIM_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE

**Hash:** `0xBCFC632DB7673BF0` | **JHash:** `0x76DAC96C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_AIM_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_AIM_CAM_RELATIVE_PITCH_LIMITS_THIS_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE

**Hash:** `0x0AF7B437918103B3` | **JHash:** `0x0E21069D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_THIRD_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE

**Hash:** `0x42156508606DE65E` | **JHash:** `0x71E9C63E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIRD_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIRD_PERSON_AIM_CAM_NEAR_CLIP_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_CUSTOM_VEHICLE_DRIVE_BY_CAM_THIS_UPDATE

**Hash:** `0x4008EDF7D6E48175` | **JHash:** `0xD1EEBC45` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALLOW_CUSTOM_VEHICLE_DRIVE_BY_CAM_THIS_UPDATE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_CUSTOM_VEHICLE_DRIVE_BY_CAM_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_TIGHTSPACE_CUSTOM_FRAMING_THIS_UPDATE

**Hash:** `0x380B4968D1E09E55` | **Build:** `1290`

**C++ 原型:**
```cpp
void FORCE_TIGHTSPACE_CUSTOM_FRAMING_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_TIGHTSPACE_CUSTOM_FRAMING_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### GET_FINAL_RENDERED_CAM_COORD

**Hash:** `0xA200EB1EE790F448` | **JHash:** `0x9C84BDA0` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_FINAL_RENDERED_CAM_COORD(void)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_FINAL_RENDERED_CAM_COORD);
```

**返回值:** `Vector3` → `Vector3`

---

### GET_FINAL_RENDERED_CAM_ROT

**Hash:** `0x5B4E4C817FCC2DFB` | **JHash:** `0x1FFBEFC5` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_FINAL_RENDERED_CAM_ROT(int rotationOrder)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_FINAL_RENDERED_CAM_ROT, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rotationOrder` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** p0 seems to consistently be 2 across scripts

Function is called faily often by CAM::CREATE_CAM_WITH_PARAMS

---

### GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_ROT

**Hash:** `0x26903D9CD1175F2C` | **JHash:** `0xACADF916` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_ROT(Player player, int rotationOrder)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_ROT, player, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_FINAL_RENDERED_CAM_FOV

**Hash:** `0x80EC114669DAEFF4` | **JHash:** `0x721B763B` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_FOV(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_FOV);
```

**返回值:** `float` → `float`

**说明:** Gets some camera fov

---

### GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_FOV

**Hash:** `0x5F35F6732C3FBBA0` | **JHash:** `0x23E3F106` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_FOV(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_REMOTE_PLAYER_CAM_FOV, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### GET_FINAL_RENDERED_CAM_NEAR_CLIP

**Hash:** `0xD0082607100D7193` | **JHash:** `0x457AE195` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_NEAR_CLIP(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_NEAR_CLIP);
```

**返回值:** `float` → `float`

---

### GET_FINAL_RENDERED_CAM_FAR_CLIP

**Hash:** `0xDFC8CBC606FDB0FC` | **JHash:** `0x46CB3A49` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_FAR_CLIP(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_FAR_CLIP);
```

**返回值:** `float` → `float`

---

### GET_FINAL_RENDERED_CAM_NEAR_DOF

**Hash:** `0xA03502FC581F7D9B` | **JHash:** `0x19297A7A` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_NEAR_DOF(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_NEAR_DOF);
```

**返回值:** `float` → `float`

---

### GET_FINAL_RENDERED_CAM_FAR_DOF

**Hash:** `0x9780F32BCAF72431` | **JHash:** `0xF24777CA` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_FAR_DOF(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_FAR_DOF);
```

**返回值:** `float` → `float`

---

### GET_FINAL_RENDERED_CAM_MOTION_BLUR_STRENGTH

**Hash:** `0x162F9D995753DC19` | **JHash:** `0x38992E83` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FINAL_RENDERED_CAM_MOTION_BLUR_STRENGTH(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FINAL_RENDERED_CAM_MOTION_BLUR_STRENGTH);
```

**返回值:** `float` → `float`

---

### SET_GAMEPLAY_COORD_HINT

**Hash:** `0xD51ADCD2D8BC0FB3` | **JHash:** `0xF27483C9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_COORD_HINT(float x, float y, float z, int duration, int blendOutDuration, int blendInDuration, int p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_COORD_HINT, x, y, z, duration, blendOutDuration, blendInDuration, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `blendOutDuration` | `int` | `int` | |
| `blendInDuration` | `int` | `int` | |
| `p6` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_PED_HINT

**Hash:** `0x2B486269ACD548D3` | **JHash:** `0x7C27343E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_PED_HINT(Ped ped, float x1, float y1, float z1, BOOL p4, int duration, int blendOutDuration, int blendInDuration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_PED_HINT, ped, x1, y1, z1, p4, duration, blendOutDuration, blendInDuration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `duration` | `int` | `int` | |
| `blendOutDuration` | `int` | `int` | |
| `blendInDuration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_VEHICLE_HINT

**Hash:** `0xA2297E18F3E71C2E` | **JHash:** `0x2C9A11D8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_VEHICLE_HINT(Vehicle vehicle, float offsetX, float offsetY, float offsetZ, BOOL p4, int time, int easeInTime, int easeOutTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_VEHICLE_HINT, vehicle, offsetX, offsetY, offsetZ, p4, time, easeInTime, easeOutTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `time` | `int` | `int` | |
| `easeInTime` | `int` | `int` | |
| `easeOutTime` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Focuses the camera on the specified vehicle.

---

### SET_GAMEPLAY_OBJECT_HINT

**Hash:** `0x83E87508A2CA2AC6` | **JHash:** `0x2ED5E2F8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_OBJECT_HINT(Object object, float xOffset, float yOffset, float zOffset, BOOL p4, int time, int easeInTime, int easeOutTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_OBJECT_HINT, object, xOffset, yOffset, zOffset, p4, time, easeInTime, easeOutTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `time` | `int` | `int` | |
| `easeInTime` | `int` | `int` | |
| `easeOutTime` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_ENTITY_HINT

**Hash:** `0x189E955A8313E298` | **JHash:** `0x66C32306` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_ENTITY_HINT(Entity entity, float xOffset, float yOffset, float zOffset, BOOL p4, int time, int easeInTime, int easeOutTime, int p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_ENTITY_HINT, entity, xOffset, yOffset, zOffset, p4, time, easeInTime, easeOutTime, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `time` | `int` | `int` | |
| `easeInTime` | `int` | `int` | |
| `easeOutTime` | `int` | `int` | |
| `p8` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p8 could be some sort of flag. Scripts use:
-244429742
0
1726668277
1844968929

---

### IS_GAMEPLAY_HINT_ACTIVE

**Hash:** `0xE520FF1AD2785B40` | **JHash:** `0xAD8DA205` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GAMEPLAY_HINT_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GAMEPLAY_HINT_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### STOP_GAMEPLAY_HINT

**Hash:** `0xF46C581C61718916` | **JHash:** `0x1BC28B7B` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_GAMEPLAY_HINT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_GAMEPLAY_HINT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STOP_GAMEPLAY_HINT_BEING_CANCELLED_THIS_UPDATE

**Hash:** `0xCCD078C2665D2973` | **JHash:** `0xCAFEE798` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_GAMEPLAY_HINT_BEING_CANCELLED_THIS_UPDATE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_GAMEPLAY_HINT_BEING_CANCELLED_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### STOP_CODE_GAMEPLAY_HINT

**Hash:** `0x247ACBC4ABBC9D1C` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CODE_GAMEPLAY_HINT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CODE_GAMEPLAY_HINT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_CODE_GAMEPLAY_HINT_ACTIVE

**Hash:** `0xBF72910D0F26F025` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CODE_GAMEPLAY_HINT_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CODE_GAMEPLAY_HINT_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_GAMEPLAY_HINT_FOV

**Hash:** `0x513403FB9C56211F` | **JHash:** `0x96FD173B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_FOV(float FOV)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_FOV, FOV);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `FOV` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_HINT_FOLLOW_DISTANCE_SCALAR

**Hash:** `0xF8BDBF3D573049A1` | **JHash:** `0x72E8CD3A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_FOLLOW_DISTANCE_SCALAR(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_FOLLOW_DISTANCE_SCALAR, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_HINT_BASE_ORBIT_PITCH_OFFSET

**Hash:** `0xD1F8363DFAD03848` | **JHash:** `0x79472AE3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_BASE_ORBIT_PITCH_OFFSET(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_BASE_ORBIT_PITCH_OFFSET, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_HINT_CAMERA_RELATIVE_SIDE_OFFSET

**Hash:** `0x5D7B620DAE436138` | **JHash:** `0xFC7464A0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_CAMERA_RELATIVE_SIDE_OFFSET(float xOffset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_CAMERA_RELATIVE_SIDE_OFFSET, xOffset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xOffset` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_HINT_CAMERA_RELATIVE_VERTICAL_OFFSET

**Hash:** `0xC92717EF615B6704` | **JHash:** `0x3554AA0E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_CAMERA_RELATIVE_VERTICAL_OFFSET(float yOffset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_CAMERA_RELATIVE_VERTICAL_OFFSET, yOffset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `yOffset` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GAMEPLAY_HINT_CAMERA_BLEND_TO_FOLLOW_PED_MEDIUM_VIEW_MODE

**Hash:** `0xE3433EADAAF7EE40` | **JHash:** `0x2F0CE859` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAMEPLAY_HINT_CAMERA_BLEND_TO_FOLLOW_PED_MEDIUM_VIEW_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAMEPLAY_HINT_CAMERA_BLEND_TO_FOLLOW_PED_MEDIUM_VIEW_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CINEMATIC_BUTTON_ACTIVE

**Hash:** `0x51669F7D1FB53D9F` | **JHash:** `0x3FBC5D00` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CINEMATIC_BUTTON_ACTIVE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CINEMATIC_BUTTON_ACTIVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_CINEMATIC_CAM_RENDERING

**Hash:** `0xB15162CB5826E9E8` | **JHash:** `0x80471AD9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_CAM_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_CAM_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### SHAKE_CINEMATIC_CAM

**Hash:** `0xDCE214D9ED58F3CF` | **JHash:** `0x61815F31` | **Build:** `323`

**C++ 原型:**
```cpp
void SHAKE_CINEMATIC_CAM(const char* shakeType, float amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHAKE_CINEMATIC_CAM, shakeType, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shakeType` | `const char*` | `string` | |
| `amount` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p0 argument found in the b617d scripts: "DRUNK_SHAKE"

Full list of cam shake types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/camShakeTypesCompact.json

---

### IS_CINEMATIC_CAM_SHAKING

**Hash:** `0xBBC08F6B4CB8FF0A` | **JHash:** `0x8376D939` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_CAM_SHAKING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_CAM_SHAKING);
```

**返回值:** `BOOL` → `bool`

---

### SET_CINEMATIC_CAM_SHAKE_AMPLITUDE

**Hash:** `0xC724C701C30B2FE7` | **JHash:** `0x67510C4B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CINEMATIC_CAM_SHAKE_AMPLITUDE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CINEMATIC_CAM_SHAKE_AMPLITUDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_CINEMATIC_CAM_SHAKING

**Hash:** `0x2238E588E588A6D7` | **JHash:** `0x71C12904` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CINEMATIC_CAM_SHAKING(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CINEMATIC_CAM_SHAKING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_CINEMATIC_BONNET_CAMERA_THIS_UPDATE

**Hash:** `0xADFF1B2A555F5FBA` | **JHash:** `0x5AC6DAC9` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_CINEMATIC_BONNET_CAMERA_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_CINEMATIC_BONNET_CAMERA_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### DISABLE_CINEMATIC_VEHICLE_IDLE_MODE_THIS_UPDATE

**Hash:** `0x62ECFCFDEE7885D6` | **JHash:** `0x837F8581` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_CINEMATIC_VEHICLE_IDLE_MODE_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_CINEMATIC_VEHICLE_IDLE_MODE_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### INVALIDATE_CINEMATIC_VEHICLE_IDLE_MODE

**Hash:** `0x9E4CFFF989258472` | **JHash:** `0x65DDE8AF` | **Build:** `323`

**C++ 原型:**
```cpp
void INVALIDATE_CINEMATIC_VEHICLE_IDLE_MODE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INVALIDATE_CINEMATIC_VEHICLE_IDLE_MODE);
```

**返回值:** `void` → `void`

**说明:** Resets the vehicle idle camera timer. Calling this in a loop will disable the idle camera.

---

### INVALIDATE_IDLE_CAM

**Hash:** `0xF4F2C0D4EE209E20` | **JHash:** `0xD75CDD75` | **Build:** `323`

**C++ 原型:**
```cpp
void INVALIDATE_IDLE_CAM(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INVALIDATE_IDLE_CAM);
```

**返回值:** `void` → `void`

**说明:** Resets the idle camera timer. Calling that in a loop once every few seconds is enough to disable the idle cinematic camera.

---

### IS_CINEMATIC_IDLE_CAM_RENDERING

**Hash:** `0xCA9D2AA3E326D720` | **JHash:** `0x96A07066` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_IDLE_CAM_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_IDLE_CAM_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### IS_CINEMATIC_FIRST_PERSON_VEHICLE_INTERIOR_CAM_RENDERING

**Hash:** `0x4F32C0D5A90A9B40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_FIRST_PERSON_VEHICLE_INTERIOR_CAM_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_FIRST_PERSON_VEHICLE_INTERIOR_CAM_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### CREATE_CINEMATIC_SHOT

**Hash:** `0x741B0129D4560F31` | **JHash:** `0xAC494E35` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_CINEMATIC_SHOT(Hash p0, int time, BOOL p2, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_CINEMATIC_SHOT, p0, time, p2, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |
| `time` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** hash is always JOAAT("CAMERA_MAN_SHOT") in decompiled scripts

---

### IS_CINEMATIC_SHOT_ACTIVE

**Hash:** `0xCC9F3371A7C28BC9` | **JHash:** `0xA4049042` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_SHOT_ACTIVE(Hash p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_SHOT_ACTIVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Hash is always JOAAT("CAMERA_MAN_SHOT") in decompiled scripts

---

### STOP_CINEMATIC_SHOT

**Hash:** `0x7660C6E75D3A078E` | **JHash:** `0xD78358C5` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CINEMATIC_SHOT(Hash p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CINEMATIC_SHOT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Only used once in carsteal3 with p0 set to -1096069633 (CAMERA_MAN_SHOT)

---

### FORCE_CINEMATIC_RENDERING_THIS_UPDATE

**Hash:** `0xA41BCD7213805AAC` | **JHash:** `0xFBB85E02` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CINEMATIC_RENDERING_THIS_UPDATE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CINEMATIC_RENDERING_THIS_UPDATE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CINEMATIC_NEWS_CHANNEL_ACTIVE_THIS_UPDATE

**Hash:** `0xDC9DA9E8789F5246` | **JHash:** `0x4938C82F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CINEMATIC_NEWS_CHANNEL_ACTIVE_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CINEMATIC_NEWS_CHANNEL_ACTIVE_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_CINEMATIC_MODE_ACTIVE

**Hash:** `0xDCF0754AC3D6FD4E` | **JHash:** `0x2009E747` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CINEMATIC_MODE_ACTIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CINEMATIC_MODE_ACTIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the vehicle cinematic cam; requires the player ped to be in a vehicle to work.

---

### IS_IN_VEHICLE_MOBILE_PHONE_CAMERA_RENDERING

**Hash:** `0x1F2300CB7FA7B7F6` | **JHash:** `0x6739AD55` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_IN_VEHICLE_MOBILE_PHONE_CAMERA_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_IN_VEHICLE_MOBILE_PHONE_CAMERA_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### DISABLE_CINEMATIC_SLOW_MO_THIS_UPDATE

**Hash:** `0x17FCA7199A530203` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DISABLE_CINEMATIC_SLOW_MO_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DISABLE_CINEMATIC_SLOW_MO_THIS_UPDATE);
```

**返回值:** `BOOL` → `bool`

---

### IS_BONNET_CINEMATIC_CAM_RENDERING

**Hash:** `0xD7360051C885628B` | **JHash:** `0x1DD55F13` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL IS_BONNET_CINEMATIC_CAM_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BONNET_CINEMATIC_CAM_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### IS_CINEMATIC_CAM_INPUT_ACTIVE

**Hash:** `0xF5F1E89A970B7796` | **JHash:** `0x1A900C84` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL IS_CINEMATIC_CAM_INPUT_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CINEMATIC_CAM_INPUT_ACTIVE);
```

**返回值:** `BOOL` → `bool`

**说明:** Tests some cinematic camera flags

---

### IGNORE_MENU_PREFERENCE_FOR_BONNET_CAMERA_THIS_UPDATE

**Hash:** `0x7B8A361C1813FBEF` | **Build:** `573`

**C++ 原型:**
```cpp
void IGNORE_MENU_PREFERENCE_FOR_BONNET_CAMERA_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.IGNORE_MENU_PREFERENCE_FOR_BONNET_CAMERA_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### BYPASS_CUTSCENE_CAM_RENDERING_THIS_UPDATE

**Hash:** `0xDB629FFD9285FA06` | **Build:** `323`

**C++ 原型:**
```cpp
void BYPASS_CUTSCENE_CAM_RENDERING_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BYPASS_CUTSCENE_CAM_RENDERING_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### STOP_CUTSCENE_CAM_SHAKING

**Hash:** `0x324C5AA411DA7737` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CUTSCENE_CAM_SHAKING(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CUTSCENE_CAM_SHAKING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CUTSCENE_CAM_FAR_CLIP_THIS_UPDATE

**Hash:** `0x12DED8CA53D47EA5` | **JHash:** `0x067BA6F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_CAM_FAR_CLIP_THIS_UPDATE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_CAM_FAR_CLIP_THIS_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Hardcoded to only work in multiplayer.

---

### GET_FOCUS_PED_ON_SCREEN

**Hash:** `0x89215EC747DF244A` | **JHash:** `0xFD99BE2B` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_FOCUS_PED_ON_SCREEN(float p0, int p1, float p2, float p3, float p4, float p5, float p6, int p7, int p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FOCUS_PED_ON_SCREEN, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `int` | `int` | |
| `p8` | `int` | `int` | |

**返回值:** `Ped` → `int`

---

### DISABLE_NEAR_CLIP_SCAN_THIS_UPDATE

**Hash:** `0x5A43C76F7FC7BA5F` | **JHash:** `0xE206C450` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_NEAR_CLIP_SCAN_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_NEAR_CLIP_SCAN_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_CAM_DEATH_FAIL_EFFECT_STATE

**Hash:** `0x80C8B1846639BB19` | **JHash:** `0xB06CCD38` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAM_DEATH_FAIL_EFFECT_STATE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAM_DEATH_FAIL_EFFECT_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** if p0 is 0, effect is cancelled

if p0 is 1, effect zooms in, gradually tilts cam clockwise apx 30 degrees, wobbles slowly. Motion blur is active until cancelled.

if p0 is 2, effect immediately tilts cam clockwise apx 30 degrees, begins to wobble slowly, then gradually tilts cam back to normal. The wobbling will continue until the effect is cancelled.

---

### SET_FIRST_PERSON_FLASH_EFFECT_TYPE

**Hash:** `0x5C41E6BABC9E2112` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_FLASH_EFFECT_TYPE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_FLASH_EFFECT_TYPE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME

**Hash:** `0x21E253A7F8DA5DFB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME(const char* vehicleName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME, vehicleName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** From b617 scripts:

CAM::SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME("DINGHY");
CAM::SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME("ISSI2");
CAM::SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_NAME("SPEEDO");

---

### SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_HASH

**Hash:** `0x11FA5D3479C7DD47` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_HASH(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRST_PERSON_FLASH_EFFECT_VEHICLE_MODEL_HASH, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### IS_ALLOWED_INDEPENDENT_CAMERA_MODES

**Hash:** `0xEAF0FA793D05C592` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ALLOWED_INDEPENDENT_CAMERA_MODES(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ALLOWED_INDEPENDENT_CAMERA_MODES);
```

**返回值:** `BOOL` → `bool`

---

### CAMERA_PREVENT_COLLISION_SETTINGS_FOR_TRIPLEHEAD_IN_INTERIORS_THIS_UPDATE

**Hash:** `0x62374889A4D59F72` | **Build:** `877`

**C++ 原型:**
```cpp
void CAMERA_PREVENT_COLLISION_SETTINGS_FOR_TRIPLEHEAD_IN_INTERIORS_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CAMERA_PREVENT_COLLISION_SETTINGS_FOR_TRIPLEHEAD_IN_INTERIORS_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### REPLAY_GET_MAX_DISTANCE_ALLOWED_FROM_PLAYER

**Hash:** `0x8BFCEB5EA1B161B6` | **Build:** `323`

**C++ 原型:**
```cpp
float REPLAY_GET_MAX_DISTANCE_ALLOWED_FROM_PLAYER(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.REPLAY_GET_MAX_DISTANCE_ALLOWED_FROM_PLAYER);
```

**返回值:** `float` → `float`

---


---

## CLOCK

**包含 16 个 Native 函数**

### SET_CLOCK_TIME

**Hash:** `0x47C3B5848C3E45D8` | **JHash:** `0x26F6AF14` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CLOCK_TIME(int hour, int minute, int second)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CLOCK_TIME, hour, minute, second);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hour` | `int` | `int` | |
| `minute` | `int` | `int` | |
| `second` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** SET_CLOCK_TIME(12, 34, 56);

---

### PAUSE_CLOCK

**Hash:** `0x4055E40BD2DBEC1D` | **JHash:** `0xB02D6124` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_CLOCK(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_CLOCK, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADVANCE_CLOCK_TIME_TO

**Hash:** `0xC8CA9670B9D83B3B` | **JHash:** `0x57B8DA7C` | **Build:** `323`

**C++ 原型:**
```cpp
void ADVANCE_CLOCK_TIME_TO(int hour, int minute, int second)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADVANCE_CLOCK_TIME_TO, hour, minute, second);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hour` | `int` | `int` | |
| `minute` | `int` | `int` | |
| `second` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TO_CLOCK_TIME

**Hash:** `0xD716F30D8C8980E2` | **JHash:** `0xCC40D20D` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TO_CLOCK_TIME(int hours, int minutes, int seconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TO_CLOCK_TIME, hours, minutes, seconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hours` | `int` | `int` | |
| `minutes` | `int` | `int` | |
| `seconds` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CLOCK_HOURS

**Hash:** `0x25223CA6B4D20B7F` | **JHash:** `0x7EF8316F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_HOURS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_HOURS);
```

**返回值:** `int` → `int`

**说明:** Gets the current ingame hour, expressed without zeros. (09:34 will be represented as 9)

---

### GET_CLOCK_MINUTES

**Hash:** `0x13D2B8ADD79640F2` | **JHash:** `0x94AAC486` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_MINUTES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_MINUTES);
```

**返回值:** `int` → `int`

**说明:** Gets the current ingame clock minute.

---

### GET_CLOCK_SECONDS

**Hash:** `0x494E97C2EF27C470` | **JHash:** `0x099C927E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_SECONDS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_SECONDS);
```

**返回值:** `int` → `int`

**说明:** Gets the current ingame clock second. Note that ingame clock seconds change really fast since a day in GTA is only 48 minutes in real life.

---

### SET_CLOCK_DATE

**Hash:** `0xB096419DF0D06CE7` | **JHash:** `0x96891C94` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CLOCK_DATE(int day, int month, int year)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CLOCK_DATE, day, month, year);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `day` | `int` | `int` | |
| `month` | `int` | `int` | |
| `year` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CLOCK_DAY_OF_WEEK

**Hash:** `0xD972E4BD7AEB235F` | **JHash:** `0x84E4A289` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_DAY_OF_WEEK(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_DAY_OF_WEEK);
```

**返回值:** `int` → `int`

**说明:** Gets the current day of the week.

0: Sunday
1: Monday
2: Tuesday
3: Wednesday
4: Thursday
5: Friday
6: Saturday

---

### GET_CLOCK_DAY_OF_MONTH

**Hash:** `0x3D10BC92A4DB1D35` | **JHash:** `0xC7A5ACB7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_DAY_OF_MONTH(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_DAY_OF_MONTH);
```

**返回值:** `int` → `int`

---

### GET_CLOCK_MONTH

**Hash:** `0xBBC72712E80257A1` | **JHash:** `0x3C48A3D5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_MONTH(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_MONTH);
```

**返回值:** `int` → `int`

---

### GET_CLOCK_YEAR

**Hash:** `0x961777E64BDAF717` | **JHash:** `0xB8BECF15` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOCK_YEAR(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOCK_YEAR);
```

**返回值:** `int` → `int`

---

### GET_MILLISECONDS_PER_GAME_MINUTE

**Hash:** `0x2F8B4D1C595B11DB` | **JHash:** `0x3B74095C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MILLISECONDS_PER_GAME_MINUTE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MILLISECONDS_PER_GAME_MINUTE);
```

**返回值:** `int` → `int`

---

### GET_POSIX_TIME

**Hash:** `0xDA488F299A5B164E` | **JHash:** `0xE15A5281` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_POSIX_TIME(int* year, int* month, int* day, int* hour, int* minute, int* second)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_POSIX_TIME, year, month, day, hour, minute, second);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `year` | `int*` | `int*` | |
| `month` | `int*` | `int*` | |
| `day` | `int*` | `int*` | |
| `hour` | `int*` | `int*` | |
| `minute` | `int*` | `int*` | |
| `second` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Gets system time as year, month, day, hour, minute and second.

Example usage:

    int year;
 int month;
    int day;
  int hour;
 int minute;
   int second;

 TIME::GET_POSIX_TIME(&year, &month, &day, &hour, &minute, &second);


---

### GET_UTC_TIME

**Hash:** `0x8117E09A19EEF4D3` | **JHash:** `0xC589CD7D` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_UTC_TIME(int* year, int* month, int* day, int* hour, int* minute, int* second)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_UTC_TIME, year, month, day, hour, minute, second);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `year` | `int*` | `int*` | |
| `month` | `int*` | `int*` | |
| `day` | `int*` | `int*` | |
| `hour` | `int*` | `int*` | |
| `minute` | `int*` | `int*` | |
| `second` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Gets current UTC time

---

### GET_LOCAL_TIME

**Hash:** `0x50C7A99057A69748` | **JHash:** `0x124BCFA2` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_LOCAL_TIME(int* year, int* month, int* day, int* hour, int* minute, int* second)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_LOCAL_TIME, year, month, day, hour, minute, second);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `year` | `int*` | `int*` | |
| `month` | `int*` | `int*` | |
| `day` | `int*` | `int*` | |
| `hour` | `int*` | `int*` | |
| `minute` | `int*` | `int*` | |
| `second` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Gets local system time as year, month, day, hour, minute and second.

Example usage:

int year;
int month;
int day;
int hour;
int minute;
int second;
or use std::tm struct

TIME::GET_LOCAL_TIME(&year, &month, &day, &hour, &minute, &second);


---


---

## CUTSCENE

**包含 56 个 Native 函数**

### REQUEST_CUTSCENE

**Hash:** `0x7A86743F475D9E09` | **JHash:** `0xB5977853` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_CUTSCENE(const char* cutsceneName, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_CUTSCENE, cutsceneName, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags: Usually 8
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### REQUEST_CUTSCENE_WITH_PLAYBACK_LIST

**Hash:** `0xC23DE0E91C30B58C` | **JHash:** `0xD98F656A` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_CUTSCENE_WITH_PLAYBACK_LIST(const char* cutsceneName, int playbackFlags, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_CUTSCENE_WITH_PLAYBACK_LIST, cutsceneName, playbackFlags, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |
| `playbackFlags` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags: Usually 8

playbackFlags: Which scenes should be played.
Example: 0x105 (bit 0, 2 and 8 set) will enable scene 1, 3 and 9.
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### REMOVE_CUTSCENE

**Hash:** `0x440AF51A3462B86F` | **JHash:** `0x8052F533` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_CUTSCENE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_CUTSCENE);
```

**返回值:** `void` → `void`

---

### HAS_CUTSCENE_LOADED

**Hash:** `0xC59F528E9AB9F339` | **JHash:** `0xF9998582` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CUTSCENE_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CUTSCENE_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### HAS_THIS_CUTSCENE_LOADED

**Hash:** `0x228D3D94F8A11C3C` | **JHash:** `0x3C5619F2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_THIS_CUTSCENE_LOADED(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_THIS_CUTSCENE_LOADED, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### SET_SCRIPT_CAN_START_CUTSCENE

**Hash:** `0x8D9DF6ECA8768583` | **JHash:** `0x25A2CABC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_CAN_START_CUTSCENE(int threadId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_CAN_START_CUTSCENE, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the cutscene's owning thread ID.

---

### CAN_REQUEST_ASSETS_FOR_CUTSCENE_ENTITY

**Hash:** `0xB56BBBCC2955D9CB` | **JHash:** `0xDD8878E9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_REQUEST_ASSETS_FOR_CUTSCENE_ENTITY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REQUEST_ASSETS_FOR_CUTSCENE_ENTITY);
```

**返回值:** `BOOL` → `bool`

---

### IS_CUTSCENE_PLAYBACK_FLAG_SET

**Hash:** `0x71B74D2AE19338D0` | **JHash:** `0x7B93CDAA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CUTSCENE_PLAYBACK_FLAG_SET(int flag)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CUTSCENE_PLAYBACK_FLAG_SET, flag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flag` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_CUTSCENE_ENTITY_STREAMING_FLAGS

**Hash:** `0x4C61C75BEE8184C2` | **JHash:** `0x47DB08A9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_ENTITY_STREAMING_FLAGS(const char* cutsceneEntName, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_ENTITY_STREAMING_FLAGS, cutsceneEntName, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REQUEST_CUT_FILE

**Hash:** `0x06A3524161C502BA` | **JHash:** `0x0D732CD6` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_CUT_FILE(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_CUT_FILE, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Simply loads the cutscene and doesn't do extra stuff that REQUEST_CUTSCENE does.
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### HAS_CUT_FILE_LOADED

**Hash:** `0xA1C996C2A744262E` | **JHash:** `0x56D5B144` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CUT_FILE_LOADED(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CUT_FILE_LOADED, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Simply checks if the cutscene has loaded and doesn't check via CutSceneManager as opposed to HAS_[THIS]_CUTSCENE_LOADED.
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### REMOVE_CUT_FILE

**Hash:** `0xD00D76A7DFC9D852` | **JHash:** `0xB70D7C6D` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_CUT_FILE(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_CUT_FILE, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Simply unloads the cutscene and doesn't do extra stuff that REMOVE_CUTSCENE does.
Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### GET_CUT_FILE_OFFSET

**Hash:** `0x1FA904B60E492336` | **Build:** `3570`

**C++ 原型:**
```cpp
Vector3 GET_CUT_FILE_OFFSET(const char* cutsceneName, int index)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_CUT_FILE_OFFSET, cutsceneName, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |
| `index` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_CUT_FILE_CONCAT_COUNT

**Hash:** `0x0ABC54DE641DC0FC` | **JHash:** `0xFD8B1AC2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CUT_FILE_CONCAT_COUNT(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUT_FILE_CONCAT_COUNT, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Full list of cutscene names by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/cutsceneNames.json

---

### START_CUTSCENE

**Hash:** `0x186D5CB5E7B0FF7B` | **JHash:** `0x210106F6` | **Build:** `323`

**C++ 原型:**
```cpp
void START_CUTSCENE(int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_CUTSCENE, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags: Usually 0.

---

### START_CUTSCENE_AT_COORDS

**Hash:** `0x1C9ADDA3244A1FBF` | **JHash:** `0x58BEA436` | **Build:** `323`

**C++ 原型:**
```cpp
void START_CUTSCENE_AT_COORDS(float x, float y, float z, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_CUTSCENE_AT_COORDS, x, y, z, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags: Usually 0.

---

### STOP_CUTSCENE

**Hash:** `0xC7272775B4DC786E` | **JHash:** `0x5EE84DC7` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CUTSCENE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CUTSCENE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STOP_CUTSCENE_IMMEDIATELY

**Hash:** `0xD220BDD222AC4A1E` | **JHash:** `0xF528A2AD` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CUTSCENE_IMMEDIATELY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CUTSCENE_IMMEDIATELY);
```

**返回值:** `void` → `void`

---

### SET_CUTSCENE_ORIGIN

**Hash:** `0xB812B3FD1C01CF27` | **JHash:** `0xB0AD7792` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_ORIGIN(float x, float y, float z, float p3, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_ORIGIN, x, y, z, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p3 could be heading. Needs more research.

---

### SET_CUTSCENE_ORIGIN_AND_ORIENTATION

**Hash:** `0x011883F41211432A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_ORIGIN_AND_ORIENTATION(float x1, float y1, float z1, float x2, float y2, float z2, int p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_ORIGIN_AND_ORIENTATION, x1, y1, z1, x2, y2, z2, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CUTSCENE_TIME

**Hash:** `0xE625BEABBAFFDAB9` | **JHash:** `0x53F5B5AB` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CUTSCENE_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_TIME);
```

**返回值:** `int` → `int`

---

### GET_CUTSCENE_PLAY_TIME

**Hash:** `0x710286BC5EF4D6E1` | **Build:** `3258`

**C++ 原型:**
```cpp
int GET_CUTSCENE_PLAY_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_PLAY_TIME);
```

**返回值:** `int` → `int`

---

### GET_CUTSCENE_TOTAL_DURATION

**Hash:** `0xEE53B14A19E480D4` | **JHash:** `0x0824EBE8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CUTSCENE_TOTAL_DURATION(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_TOTAL_DURATION);
```

**返回值:** `int` → `int`

---

### GET_CUTSCENE_END_TIME

**Hash:** `0x971D7B15BCDBEF99` | **Build:** `1734`

**C++ 原型:**
```cpp
int GET_CUTSCENE_END_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_END_TIME);
```

**返回值:** `int` → `int`

---

### GET_CUTSCENE_PLAY_DURATION

**Hash:** `0x5D583F71C901F2A3` | **Build:** `2802`

**C++ 原型:**
```cpp
int GET_CUTSCENE_PLAY_DURATION(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_PLAY_DURATION);
```

**返回值:** `int` → `int`

---

### WAS_CUTSCENE_SKIPPED

**Hash:** `0x40C8656EDAEDD569` | **JHash:** `0xC9B6949D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_CUTSCENE_SKIPPED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_CUTSCENE_SKIPPED);
```

**返回值:** `BOOL` → `bool`

---

### HAS_CUTSCENE_FINISHED

**Hash:** `0x7C0A893088881D57` | **JHash:** `0x5DED14B4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CUTSCENE_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CUTSCENE_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### IS_CUTSCENE_ACTIVE

**Hash:** `0x991251AFC3981F84` | **JHash:** `0xCCE2FE9D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CUTSCENE_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CUTSCENE_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### IS_CUTSCENE_PLAYING

**Hash:** `0xD3C2E180A40F031E` | **JHash:** `0xA3A78392` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CUTSCENE_PLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CUTSCENE_PLAYING);
```

**返回值:** `BOOL` → `bool`

---

### GET_CUTSCENE_SECTION_PLAYING

**Hash:** `0x49010A6A396553D8` | **JHash:** `0x1026F0D6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CUTSCENE_SECTION_PLAYING(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_SECTION_PLAYING);
```

**返回值:** `int` → `int`

---

### GET_ENTITY_INDEX_OF_CUTSCENE_ENTITY

**Hash:** `0x0A2E9FDB9A8C62F6` | **JHash:** `0x1D09ABC7` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_ENTITY_INDEX_OF_CUTSCENE_ENTITY(const char* cutsceneEntName, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_INDEX_OF_CUTSCENE_ENTITY, cutsceneEntName, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Entity` → `int`

---

### GET_CUTSCENE_CONCAT_SECTION_PLAYING

**Hash:** `0x583DF8E3D4AFBD98` | **JHash:** `0x5AE68AE6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CUTSCENE_CONCAT_SECTION_PLAYING(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CUTSCENE_CONCAT_SECTION_PLAYING);
```

**返回值:** `int` → `int`

---

### IS_CUTSCENE_AUTHORIZED

**Hash:** `0x4CEBC1ED31E8925E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CUTSCENE_AUTHORIZED(const char* cutsceneName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CUTSCENE_AUTHORIZED, cutsceneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### DOES_CUTSCENE_HANDLE_EXIST

**Hash:** `0x4FCD976DA686580C` | **Build:** `1290`

**C++ 原型:**
```cpp
int DOES_CUTSCENE_HANDLE_EXIST(int cutsceneHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DOES_CUTSCENE_HANDLE_EXIST, cutsceneHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneHandle` | `int` | `int` | |

**返回值:** `int` → `int`

---

### REGISTER_ENTITY_FOR_CUTSCENE

**Hash:** `0xE40C1C56DF95C2E8` | **JHash:** `0x7CBC3EC7` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_ENTITY_FOR_CUTSCENE(Ped cutscenePed, const char* cutsceneEntName, int p2, Hash modelHash, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_ENTITY_FOR_CUTSCENE, cutscenePed, cutsceneEntName, p2, modelHash, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutscenePed` | `Ped` | `int` | |
| `cutsceneEntName` | `const char*` | `string` | |
| `p2` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_ENTITY_INDEX_OF_REGISTERED_ENTITY

**Hash:** `0xC0741A26499654CD` | **JHash:** `0x46D18755` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_ENTITY_INDEX_OF_REGISTERED_ENTITY(const char* cutsceneEntName, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_INDEX_OF_REGISTERED_ENTITY, cutsceneEntName, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Entity` → `int`

---

### SET_VEHICLE_MODEL_PLAYER_WILL_EXIT_SCENE

**Hash:** `0x7F96F23FA9B73327` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MODEL_PLAYER_WILL_EXIT_SCENE(Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MODEL_PLAYER_WILL_EXIT_SCENE, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### SET_CUTSCENE_TRIGGER_AREA

**Hash:** `0x9896CE4721BE84BA` | **JHash:** `0x9D76D9DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_TRIGGER_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_TRIGGER_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only used twice in R* scripts

---

### CAN_SET_ENTER_STATE_FOR_REGISTERED_ENTITY

**Hash:** `0x645D0B458D8E17B5` | **JHash:** `0x55C30B26` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_SET_ENTER_STATE_FOR_REGISTERED_ENTITY(const char* cutsceneEntName, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_SET_ENTER_STATE_FOR_REGISTERED_ENTITY, cutsceneEntName, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** modelHash (p1) was always 0 in R* scripts

---

### CAN_SET_EXIT_STATE_FOR_REGISTERED_ENTITY

**Hash:** `0x4C6A6451C79E4662` | **JHash:** `0x8FF5D3C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_SET_EXIT_STATE_FOR_REGISTERED_ENTITY(const char* cutsceneEntName, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_SET_EXIT_STATE_FOR_REGISTERED_ENTITY, cutsceneEntName, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### CAN_SET_EXIT_STATE_FOR_CAMERA

**Hash:** `0xB2CBCD0930DFB420` | **JHash:** `0xEDAE6C02` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_SET_EXIT_STATE_FOR_CAMERA(BOOL p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_SET_EXIT_STATE_FOR_CAMERA, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SET_PAD_CAN_SHAKE_DURING_CUTSCENE

**Hash:** `0xC61B86C9F61EB404` | **JHash:** `0x35721A08` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PAD_CAN_SHAKE_DURING_CUTSCENE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PAD_CAN_SHAKE_DURING_CUTSCENE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles a value (bool) for cutscenes.

---

### SET_CUTSCENE_FADE_VALUES

**Hash:** `0x8093F23ABACCC7D4` | **JHash:** `0xD19EF0DD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_FADE_VALUES(BOOL p0, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_FADE_VALUES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CUTSCENE_MULTIHEAD_FADE

**Hash:** `0x20746F7B1032A3C7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_MULTIHEAD_FADE(BOOL p0, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_MULTIHEAD_FADE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CUTSCENE_MULTIHEAD_FADE_MANUAL

**Hash:** `0x06EE9048FD080382` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_MULTIHEAD_FADE_MANUAL(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_MULTIHEAD_FADE_MANUAL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_MULTIHEAD_FADE_UP

**Hash:** `0xA0FE76168A189DDB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MULTIHEAD_FADE_UP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MULTIHEAD_FADE_UP);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_MOCAP_CUTSCENE_CAN_BE_SKIPPED

**Hash:** `0x2F137B508DE238F2` | **JHash:** `0x8338DA1D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_MOCAP_CUTSCENE_CAN_BE_SKIPPED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_MOCAP_CUTSCENE_CAN_BE_SKIPPED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CAR_GENERATORS_CAN_UPDATE_DURING_CUTSCENE

**Hash:** `0xE36A98D8AB3D3C66` | **JHash:** `0x04377C10` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAR_GENERATORS_CAN_UPDATE_DURING_CUTSCENE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAR_GENERATORS_CAN_UPDATE_DURING_CUTSCENE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CAN_USE_MOBILE_PHONE_DURING_CUTSCENE

**Hash:** `0x5EDEF0CF8C1DAB3C` | **JHash:** `0xDBD88708` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_USE_MOBILE_PHONE_DURING_CUTSCENE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_USE_MOBILE_PHONE_DURING_CUTSCENE);
```

**返回值:** `BOOL` → `bool`

---

### SET_CUTSCENE_CAN_BE_SKIPPED

**Hash:** `0x41FAA8FB2ECE8720` | **JHash:** `0x28D54A7F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_CAN_BE_SKIPPED(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_CAN_BE_SKIPPED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CAN_DISPLAY_MINIMAP_DURING_CUTSCENE_THIS_UPDATE

**Hash:** `0x2131046957F31B04` | **JHash:** `0xB60CFBB9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_DISPLAY_MINIMAP_DURING_CUTSCENE_THIS_UPDATE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_DISPLAY_MINIMAP_DURING_CUTSCENE_THIS_UPDATE);
```

**返回值:** `void` → `void`

---

### SET_CUTSCENE_PED_COMPONENT_VARIATION

**Hash:** `0xBA01E7B6DEEFBBC9` | **JHash:** `0x6AF994A1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_PED_COMPONENT_VARIATION(const char* cutsceneEntName, int componentId, int drawableId, int textureId, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_PED_COMPONENT_VARIATION, cutsceneEntName, componentId, drawableId, textureId, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of ped components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedComponentVariations.json

---

### SET_CUTSCENE_PED_COMPONENT_VARIATION_FROM_PED

**Hash:** `0x2A56C06EBEF2B0D9` | **JHash:** `0x1E7DA95E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_PED_COMPONENT_VARIATION_FROM_PED(const char* cutsceneEntName, Ped ped, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_PED_COMPONENT_VARIATION_FROM_PED, cutsceneEntName, ped, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### DOES_CUTSCENE_ENTITY_EXIST

**Hash:** `0x499EF20C5DB25C59` | **JHash:** `0x58E67409` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_CUTSCENE_ENTITY_EXIST(const char* cutsceneEntName, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CUTSCENE_ENTITY_EXIST, cutsceneEntName, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### SET_CUTSCENE_PED_PROP_VARIATION

**Hash:** `0x0546524ADE2E9723` | **JHash:** `0x22E9A9DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENE_PED_PROP_VARIATION(const char* cutsceneEntName, int componentId, int drawableId, int textureId, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENE_PED_PROP_VARIATION, cutsceneEntName, componentId, drawableId, textureId, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cutsceneEntName` | `const char*` | `string` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Thanks R*! ;)

if ((l_161 == 0) || (l_161 == 2)) {
    sub_2ea27("Trying to set Jimmy prop variation");
    CUTSCENE::SET_CUTSCENE_PED_PROP_VARIATION("Jimmy_Boston", 1, 0, 0, 0);
}

Full list of ped components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedComponentVariations.json

---

### HAS_CUTSCENE_CUT_THIS_FRAME

**Hash:** `0x708BDD8CD795B043` | **JHash:** `0x4315A7C5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CUTSCENE_CUT_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CUTSCENE_CUT_THIS_FRAME);
```

**返回值:** `BOOL` → `bool`

**说明:** Possibly HAS_CUTSCENE_CUT_THIS_FRAME, needs more research.

---


---

## DATAFILE

**包含 57 个 Native 函数**

### DATAFILE_WATCH_REQUEST_ID

**Hash:** `0xAD6875BBC0FC899C` | **JHash:** `0x621388FF` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_WATCH_REQUEST_ID(int requestId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_WATCH_REQUEST_ID, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Adds the given requestID to the watch list.

---

### DATAFILE_CLEAR_WATCH_LIST

**Hash:** `0x6CC86E78358D5119` | **JHash:** `0x5B39D0AC` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_CLEAR_WATCH_LIST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_CLEAR_WATCH_LIST);
```

**返回值:** `void` → `void`

---

### DATAFILE_IS_VALID_REQUEST_ID

**Hash:** `0xFCCAE5B92A830878` | **JHash:** `0x01393D16` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_IS_VALID_REQUEST_ID(int index)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_IS_VALID_REQUEST_ID, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_HAS_LOADED_FILE_DATA

**Hash:** `0x15FF52B809DB2353` | **JHash:** `0x36FB8B3F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_HAS_LOADED_FILE_DATA(int requestId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_HAS_LOADED_FILE_DATA, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_HAS_VALID_FILE_DATA

**Hash:** `0xF8CC1EBE0B62E29F` | **JHash:** `0x2A9411DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_HAS_VALID_FILE_DATA(int requestId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_HAS_VALID_FILE_DATA, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_SELECT_ACTIVE_FILE

**Hash:** `0x22DA66936E0FFF37` | **JHash:** `0xB41064A4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_SELECT_ACTIVE_FILE(int requestId, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_SELECT_ACTIVE_FILE, requestId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_DELETE_REQUESTED_FILE

**Hash:** `0x8F5EA1C01D65A100` | **JHash:** `0x9DB63CFF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_DELETE_REQUESTED_FILE(int requestId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_DELETE_REQUESTED_FILE, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### UGC_CREATE_CONTENT

**Hash:** `0xC84527E235FCA219` | **JHash:** `0xF09157B0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_CREATE_CONTENT(Any* data, int dataCount, const char* contentName, const char* description, const char* tagsCsv, const char* contentTypeName, BOOL publish, Any p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_CREATE_CONTENT, data, dataCount, contentName, description, tagsCsv, contentTypeName, publish, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `dataCount` | `int` | `int` | |
| `contentName` | `const char*` | `string` | |
| `description` | `const char*` | `string` | |
| `tagsCsv` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |
| `publish` | `BOOL` | `bool` | |
| `p7` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_CREATE_MISSION

**Hash:** `0xA5EFC3E847D60507` | **JHash:** `0xD96860FC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_CREATE_MISSION(const char* contentName, const char* description, const char* tagsCsv, const char* contentTypeName, BOOL publish, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_CREATE_MISSION, contentName, description, tagsCsv, contentTypeName, publish, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentName` | `const char*` | `string` | |
| `description` | `const char*` | `string` | |
| `tagsCsv` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |
| `publish` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_UPDATE_CONTENT

**Hash:** `0x648E7A5434AF7969` | **JHash:** `0x459F2683` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_UPDATE_CONTENT(const char* contentId, Any* data, int dataCount, const char* contentName, const char* description, const char* tagsCsv, const char* contentTypeName, Any p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_UPDATE_CONTENT, contentId, data, dataCount, contentName, description, tagsCsv, contentTypeName, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `data` | `Any*` | `object*` | |
| `dataCount` | `int` | `int` | |
| `contentName` | `const char*` | `string` | |
| `description` | `const char*` | `string` | |
| `tagsCsv` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |
| `p7` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_UPDATE_MISSION

**Hash:** `0x4645DE9980999E93` | **JHash:** `0xDBB83E2B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_UPDATE_MISSION(const char* contentId, const char* contentName, const char* description, const char* tagsCsv, const char* contentTypeName, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_UPDATE_MISSION, contentId, contentName, description, tagsCsv, contentTypeName, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `contentName` | `const char*` | `string` | |
| `description` | `const char*` | `string` | |
| `tagsCsv` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_SET_PLAYER_DATA

**Hash:** `0x692D808C34A82143` | **JHash:** `0xBB6321BD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_SET_PLAYER_DATA(const char* contentId, float rating, const char* contentTypeName, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_SET_PLAYER_DATA, contentId, rating, contentTypeName, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `rating` | `float` | `float` | |
| `contentTypeName` | `const char*` | `string` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_SELECT_UGC_DATA

**Hash:** `0xA69AC4ADE82B57A4` | **JHash:** `0xE8D56DA2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_SELECT_UGC_DATA(int p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_SELECT_UGC_DATA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_SELECT_UGC_STATS

**Hash:** `0x9CB0BFA7A9342C3D` | **JHash:** `0xCB6A351E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_SELECT_UGC_STATS(int p0, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_SELECT_UGC_STATS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_SELECT_UGC_PLAYER_DATA

**Hash:** `0x52818819057F2B40` | **JHash:** `0xA4D1B30E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_SELECT_UGC_PLAYER_DATA(int p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_SELECT_UGC_PLAYER_DATA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_SELECT_CREATOR_STATS

**Hash:** `0x01095C95CD46B624` | **JHash:** `0xB8515B2F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_SELECT_CREATOR_STATS(int p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_SELECT_CREATOR_STATS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_LOAD_OFFLINE_UGC

**Hash:** `0xC5238C011AF405E4` | **JHash:** `0x660C468E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_LOAD_OFFLINE_UGC(const char* filename, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_LOAD_OFFLINE_UGC, filename, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `filename` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** Loads a User-Generated Content (UGC) file. These files can be found in "[GTA5]\data\ugc" and "[GTA5]\common\patch\ugc". They seem to follow a naming convention, most likely of "[name]_[part].ugc". See example below for usage.

Returns whether or not the file was successfully loaded.

Example:
DATAFILE::DATAFILE_LOAD_OFFLINE_UGC("RockstarPlaylists") // loads "rockstarplaylists_00.ugc"

---

### DATAFILE_CREATE

**Hash:** `0xD27058A1CA2B13EE` | **JHash:** `0x95F8A221` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_CREATE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_CREATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DATAFILE_DELETE

**Hash:** `0x9AB9C1CFC8862DFB` | **JHash:** `0xDEF31B0A` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_DELETE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_DELETE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DATAFILE_STORE_MISSION_HEADER

**Hash:** `0x2ED61456317B8178` | **JHash:** `0x4E03F632` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_STORE_MISSION_HEADER(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_STORE_MISSION_HEADER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DATAFILE_FLUSH_MISSION_HEADER

**Hash:** `0xC55854C7D7274882` | **JHash:** `0xF11F956F` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAFILE_FLUSH_MISSION_HEADER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_FLUSH_MISSION_HEADER);
```

**返回值:** `void` → `void`

---

### DATAFILE_GET_FILE_DICT

**Hash:** `0x906B778CA1DC72B6` | **JHash:** `0x86DDF9C2` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATAFILE_GET_FILE_DICT(int p0)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATAFILE_GET_FILE_DICT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `Any*` → `object*`

---

### DATAFILE_START_SAVE_TO_CLOUD

**Hash:** `0x83BCCE3224735F05` | **JHash:** `0x768CBB35` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_START_SAVE_TO_CLOUD(const char* filename, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_START_SAVE_TO_CLOUD, filename, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `filename` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_UPDATE_SAVE_TO_CLOUD

**Hash:** `0x4DFDD9EB705F8140` | **JHash:** `0x0B4087F7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_UPDATE_SAVE_TO_CLOUD(BOOL* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_UPDATE_SAVE_TO_CLOUD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_IS_SAVE_PENDING

**Hash:** `0xBEDB96A7584AA8CF` | **JHash:** `0x5DCD0796` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAFILE_IS_SAVE_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_IS_SAVE_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### DATAFILE_LOAD_OFFLINE_UGC_FOR_ADDITIONAL_DATA_FILE

**Hash:** `0xA6EEF01087181EDD` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL DATAFILE_LOAD_OFFLINE_UGC_FOR_ADDITIONAL_DATA_FILE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAFILE_LOAD_OFFLINE_UGC_FOR_ADDITIONAL_DATA_FILE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DATAFILE_DELETE_FOR_ADDITIONAL_DATA_FILE

**Hash:** `0x6AD0BD5E087866CB` | **Build:** `2189`

**C++ 原型:**
```cpp
void DATAFILE_DELETE_FOR_ADDITIONAL_DATA_FILE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAFILE_DELETE_FOR_ADDITIONAL_DATA_FILE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DATAFILE_GET_FILE_DICT_FOR_ADDITIONAL_DATA_FILE

**Hash:** `0xDBF860CF1DB8E599` | **Build:** `2189`

**C++ 原型:**
```cpp
Any* DATAFILE_GET_FILE_DICT_FOR_ADDITIONAL_DATA_FILE(Any p0)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATAFILE_GET_FILE_DICT_FOR_ADDITIONAL_DATA_FILE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `Any*` → `object*`

---

### DATADICT_SET_BOOL

**Hash:** `0x35124302A556A325` | **JHash:** `0x9B29D99B` | **Build:** `323`

**C++ 原型:**
```cpp
void DATADICT_SET_BOOL(Any* objectData, const char* key, BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATADICT_SET_BOOL, objectData, key, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DATADICT_SET_INT

**Hash:** `0xE7E035450A7948D5` | **JHash:** `0xEFCF554A` | **Build:** `323`

**C++ 原型:**
```cpp
void DATADICT_SET_INT(Any* objectData, const char* key, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATADICT_SET_INT, objectData, key, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DATADICT_SET_FLOAT

**Hash:** `0xC27E1CC2D795105E` | **JHash:** `0xE972CACF` | **Build:** `323`

**C++ 原型:**
```cpp
void DATADICT_SET_FLOAT(Any* objectData, const char* key, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATADICT_SET_FLOAT, objectData, key, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DATADICT_SET_STRING

**Hash:** `0x8FF3847DADD8E30C` | **JHash:** `0xD437615C` | **Build:** `323`

**C++ 原型:**
```cpp
void DATADICT_SET_STRING(Any* objectData, const char* key, const char* value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATADICT_SET_STRING, objectData, key, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |
| `value` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### DATADICT_SET_VECTOR

**Hash:** `0x4CD49B76338C7DEE` | **JHash:** `0x75FC6C3C` | **Build:** `323`

**C++ 原型:**
```cpp
void DATADICT_SET_VECTOR(Any* objectData, const char* key, float valueX, float valueY, float valueZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATADICT_SET_VECTOR, objectData, key, valueX, valueY, valueZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |
| `valueX` | `float` | `float` | |
| `valueY` | `float` | `float` | |
| `valueZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DATADICT_CREATE_DICT

**Hash:** `0xA358F56F10732EE1` | **JHash:** `0x96A8E05F` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATADICT_CREATE_DICT(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATADICT_CREATE_DICT, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `Any*` → `object*`

---

### DATADICT_CREATE_ARRAY

**Hash:** `0x5B11728527CA6E5F` | **JHash:** `0x03939B8D` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATADICT_CREATE_ARRAY(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATADICT_CREATE_ARRAY, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `Any*` → `object*`

---

### DATADICT_GET_BOOL

**Hash:** `0x1186940ED72FFEEC` | **JHash:** `0x8876C872` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATADICT_GET_BOOL(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATADICT_GET_BOOL, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### DATADICT_GET_INT

**Hash:** `0x78F06F6B1FB5A80C` | **JHash:** `0xA6C68693` | **Build:** `323`

**C++ 原型:**
```cpp
int DATADICT_GET_INT(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DATADICT_GET_INT, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### DATADICT_GET_FLOAT

**Hash:** `0x06610343E73B9727` | **JHash:** `0xA92C1AF4` | **Build:** `323`

**C++ 原型:**
```cpp
float DATADICT_GET_FLOAT(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.DATADICT_GET_FLOAT, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `float` → `float`

---

### DATADICT_GET_STRING

**Hash:** `0x3D2FD9E763B24472` | **JHash:** `0x942160EC` | **Build:** `323`

**C++ 原型:**
```cpp
const char* DATADICT_GET_STRING(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.DATADICT_GET_STRING, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `const char*` → `string`

---

### DATADICT_GET_VECTOR

**Hash:** `0x46CD3CB66E0825CC` | **JHash:** `0xE84A127A` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 DATADICT_GET_VECTOR(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.DATADICT_GET_VECTOR, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `Vector3` → `Vector3`

---

### DATADICT_GET_DICT

**Hash:** `0xB6B9DDC412FCEEE2` | **JHash:** `0xC9C13D8D` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATADICT_GET_DICT(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATADICT_GET_DICT, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `Any*` → `object*`

---

### DATADICT_GET_ARRAY

**Hash:** `0x7A983AA9DA2659ED` | **JHash:** `0x1F2F7D00` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATADICT_GET_ARRAY(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATADICT_GET_ARRAY, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `Any*` → `object*`

---

### DATADICT_GET_TYPE

**Hash:** `0x031C55ED33227371` | **JHash:** `0x2678342A` | **Build:** `323`

**C++ 原型:**
```cpp
int DATADICT_GET_TYPE(Any* objectData, const char* key)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DATADICT_GET_TYPE, objectData, key);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectData` | `Any*` | `object*` | |
| `key` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Types:
1 = Boolean
2 = Integer
3 = Float
4 = String
5 = Vector3
6 = Object
7 = Array

---

### DATAARRAY_ADD_BOOL

**Hash:** `0xF8B0F5A43E928C76` | **JHash:** `0x08174B90` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAARRAY_ADD_BOOL(Any* arrayData, BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAARRAY_ADD_BOOL, arrayData, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DATAARRAY_ADD_INT

**Hash:** `0xCABDB751D86FE93B` | **JHash:** `0xF29C0B36` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAARRAY_ADD_INT(Any* arrayData, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAARRAY_ADD_INT, arrayData, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DATAARRAY_ADD_FLOAT

**Hash:** `0x57A995FD75D37F56` | **JHash:** `0xE4302123` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAARRAY_ADD_FLOAT(Any* arrayData, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAARRAY_ADD_FLOAT, arrayData, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DATAARRAY_ADD_STRING

**Hash:** `0x2F0661C155AEEEAA` | **JHash:** `0xF3C01350` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAARRAY_ADD_STRING(Any* arrayData, const char* value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAARRAY_ADD_STRING, arrayData, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `value` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### DATAARRAY_ADD_VECTOR

**Hash:** `0x407F8D034F70F0C2` | **JHash:** `0x16F464B6` | **Build:** `323`

**C++ 原型:**
```cpp
void DATAARRAY_ADD_VECTOR(Any* arrayData, float valueX, float valueY, float valueZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DATAARRAY_ADD_VECTOR, arrayData, valueX, valueY, valueZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `valueX` | `float` | `float` | |
| `valueY` | `float` | `float` | |
| `valueZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DATAARRAY_ADD_DICT

**Hash:** `0x6889498B3E19C797` | **JHash:** `0xC174C71B` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATAARRAY_ADD_DICT(Any* arrayData)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATAARRAY_ADD_DICT, arrayData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |

**返回值:** `Any*` → `object*`

---

### DATAARRAY_GET_BOOL

**Hash:** `0x50C1B2874E50C114` | **JHash:** `0xA2E5F921` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DATAARRAY_GET_BOOL(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DATAARRAY_GET_BOOL, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DATAARRAY_GET_INT

**Hash:** `0x3E5AE19425CD74BE` | **JHash:** `0xBB120CFC` | **Build:** `323`

**C++ 原型:**
```cpp
int DATAARRAY_GET_INT(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DATAARRAY_GET_INT, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `int` → `int`

---

### DATAARRAY_GET_FLOAT

**Hash:** `0xC0C527B525D7CFB5` | **JHash:** `0x08AD2CC2` | **Build:** `323`

**C++ 原型:**
```cpp
float DATAARRAY_GET_FLOAT(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.DATAARRAY_GET_FLOAT, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `float` → `float`

---

### DATAARRAY_GET_STRING

**Hash:** `0xD3F2FFEB8D836F52` | **JHash:** `0x93F985A6` | **Build:** `323`

**C++ 原型:**
```cpp
const char* DATAARRAY_GET_STRING(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.DATAARRAY_GET_STRING, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### DATAARRAY_GET_VECTOR

**Hash:** `0x8D2064E5B64A628A` | **JHash:** `0x80E3DA55` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 DATAARRAY_GET_VECTOR(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.DATAARRAY_GET_VECTOR, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### DATAARRAY_GET_DICT

**Hash:** `0x8B5FADCC4E3A145F` | **JHash:** `0xECE81278` | **Build:** `323`

**C++ 原型:**
```cpp
Any* DATAARRAY_GET_DICT(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
object* result = Function.Call<object*>(Hash.DATAARRAY_GET_DICT, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `Any*` → `object*`

---

### DATAARRAY_GET_COUNT

**Hash:** `0x065DB281590CEA2D` | **JHash:** `0xA8A21766` | **Build:** `323`

**C++ 原型:**
```cpp
int DATAARRAY_GET_COUNT(Any* arrayData)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DATAARRAY_GET_COUNT, arrayData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### DATAARRAY_GET_TYPE

**Hash:** `0x3A0014ADB172A3C5` | **JHash:** `0xFA2402C8` | **Build:** `323`

**C++ 原型:**
```cpp
int DATAARRAY_GET_TYPE(Any* arrayData, int arrayIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DATAARRAY_GET_TYPE, arrayData, arrayIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `arrayData` | `Any*` | `object*` | |
| `arrayIndex` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Types:
1 = Boolean
2 = Integer
3 = Float
4 = String
5 = Vector3
6 = Object
7 = Array

---


---

## DECORATOR

**包含 12 个 Native 函数**

### DECOR_SET_TIME

**Hash:** `0x95AED7B8E39ECAA4` | **JHash:** `0xBBAEEF94` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_SET_TIME(Entity entity, const char* propertyName, int timestamp)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_SET_TIME, entity, propertyName, timestamp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |
| `timestamp` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DECOR_SET_BOOL

**Hash:** `0x6B1E8E2ED1335B71` | **JHash:** `0x8E101F5C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_SET_BOOL(Entity entity, const char* propertyName, BOOL value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_SET_BOOL, entity, propertyName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |
| `value` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** This function sets metadata of type bool to specified entity.


---

### DECOR_SET_FLOAT

**Hash:** `0x211AB1DD8D0F363A` | **JHash:** `0xBC7BD5CB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_SET_FLOAT(Entity entity, const char* propertyName, float value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_SET_FLOAT, entity, propertyName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### DECOR_SET_INT

**Hash:** `0x0CE3AA5E1CA19E10` | **JHash:** `0xDB718B21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_SET_INT(Entity entity, const char* propertyName, int value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_SET_INT, entity, propertyName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |
| `value` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Sets property to int.

---

### DECOR_GET_BOOL

**Hash:** `0xDACE671663F2F5DB` | **JHash:** `0xDBCE51E0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_GET_BOOL(Entity entity, const char* propertyName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_GET_BOOL, entity, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### DECOR_GET_FLOAT

**Hash:** `0x6524A2F114706F43` | **JHash:** `0x8DE5382F` | **Build:** `323`

**C++ 原型:**
```cpp
float DECOR_GET_FLOAT(Entity entity, const char* propertyName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.DECOR_GET_FLOAT, entity, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |

**返回值:** `float` → `float`

---

### DECOR_GET_INT

**Hash:** `0xA06C969B02A97298` | **JHash:** `0xDDDE59B5` | **Build:** `323`

**C++ 原型:**
```cpp
int DECOR_GET_INT(Entity entity, const char* propertyName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DECOR_GET_INT, entity, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### DECOR_EXIST_ON

**Hash:** `0x05661B80A8C9165F` | **JHash:** `0x74EF9C40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_EXIST_ON(Entity entity, const char* propertyName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_EXIST_ON, entity, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether or not the specified property is set for the entity.

---

### DECOR_REMOVE

**Hash:** `0x00EE9F297C738720` | **JHash:** `0xE0E2640B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_REMOVE(Entity entity, const char* propertyName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_REMOVE, entity, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `propertyName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### DECOR_REGISTER

**Hash:** `0x9FD90732F56403CE` | **JHash:** `0x68BD42A9` | **Build:** `323`

**C++ 原型:**
```cpp
void DECOR_REGISTER(const char* propertyName, int type)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DECOR_REGISTER, propertyName, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `propertyName` | `const char*` | `string` | |
| `type` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** https://alloc8or.re/gta5/doc/enums/eDecorType.txt

---

### DECOR_IS_REGISTERED_AS_TYPE

**Hash:** `0x4F14F9F870D6FBC8` | **JHash:** `0x7CF0971D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DECOR_IS_REGISTERED_AS_TYPE(const char* propertyName, int type)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DECOR_IS_REGISTERED_AS_TYPE, propertyName, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `propertyName` | `const char*` | `string` | |
| `type` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** type: see DECOR_REGISTER

---

### DECOR_REGISTER_LOCK

**Hash:** `0xA9D14EEA259F9248` | **JHash:** `0x7F3F1C02` | **Build:** `323`

**C++ 原型:**
```cpp
void DECOR_REGISTER_LOCK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DECOR_REGISTER_LOCK);
```

**返回值:** `void` → `void`

**说明:** Called after all decorator type initializations.

---


---

## DLC

**包含 11 个 Native 函数**

### ARE_ANY_CCS_PENDING

**Hash:** `0x241FCA5B1AA14F75` | **JHash:** `0x0AF83036` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ANY_CCS_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ANY_CCS_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### IS_DLC_PRESENT

**Hash:** `0x812595A0644CE1DE` | **JHash:** `0x1F321943` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DLC_PRESENT(Hash dlcHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DLC_PRESENT, dlcHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the given DLC pack is present.

---

### DLC_CHECK_CLOUD_DATA_CORRECT

**Hash:** `0xF2E07819EF1A5289` | **JHash:** `0x881B1FDB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DLC_CHECK_CLOUD_DATA_CORRECT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DLC_CHECK_CLOUD_DATA_CORRECT);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### GET_EXTRACONTENT_CLOUD_RESULT

**Hash:** `0x9489659372A81585` | **JHash:** `0xC2169164` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_EXTRACONTENT_CLOUD_RESULT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_EXTRACONTENT_CLOUD_RESULT);
```

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 0.

---

### DLC_CHECK_COMPAT_PACK_CONFIGURATION

**Hash:** `0xA213B11DFF526300` | **JHash:** `0xF79A97F5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DLC_CHECK_COMPAT_PACK_CONFIGURATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DLC_CHECK_COMPAT_PACK_CONFIGURATION);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### GET_EVER_HAD_BAD_PACK_ORDER

**Hash:** `0x8D30F648014A92B5` | **JHash:** `0xF69B729C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_EVER_HAD_BAD_PACK_ORDER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_EVER_HAD_BAD_PACK_ORDER);
```

**返回值:** `BOOL` → `bool`

---

### GET_IS_LOADING_SCREEN_ACTIVE

**Hash:** `0x10D0A8F259E93EC9` | **JHash:** `0x517B601B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_LOADING_SCREEN_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_LOADING_SCREEN_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### GET_IS_INITIAL_LOADING_SCREEN_ACTIVE

**Hash:** `0xC4637A6D03C24CC3` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL GET_IS_INITIAL_LOADING_SCREEN_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_INITIAL_LOADING_SCREEN_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### HAS_CLOUD_REQUESTS_FINISHED

**Hash:** `0x46E2B844905BC5F0` | **JHash:** `0x6087C10C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CLOUD_REQUESTS_FINISHED(BOOL* p0, int unused)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CLOUD_REQUESTS_FINISHED, p0, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL*` | `bool*` | |
| `unused` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Sets the value of the specified variable to 0.
Always returns true.

---

### ON_ENTER_SP

**Hash:** `0xD7C10C4A637992C9` | **JHash:** `0x8BF60FC3` | **Build:** `323`

**C++ 原型:**
```cpp
void ON_ENTER_SP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ON_ENTER_SP);
```

**返回值:** `void` → `void`

**说明:** Unloads GROUP_MAP (GTAO/MP) DLC data and loads GROUP_MAP_SP DLC. Neither are loaded by default, ON_ENTER_MP is a cognate to this function and loads MP DLC (and unloads SP DLC by extension).
Works in singleplayer.

---

### ON_ENTER_MP

**Hash:** `0x0888C3502DBBEEF5` | **JHash:** `0xC65586A9` | **Build:** `323`

**C++ 原型:**
```cpp
void ON_ENTER_MP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ON_ENTER_MP);
```

**返回值:** `void` → `void`

**说明:** This loads the GTA:O dlc map parts (high end garages, apartments).
Works in singleplayer.
In order to use GTA:O heist IPL's you have to call this native with the following params: SET_INSTANCE_PRIORITY_MODE(1);

---


---

## ENTITY

**包含 189 个 Native 函数**

### DOES_ENTITY_EXIST

**Hash:** `0x7239B21A38F536BA` | **JHash:** `0x3AC90869` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_EXIST(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_EXIST, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks whether an entity exists in the game world.

---

### DOES_ENTITY_BELONG_TO_THIS_SCRIPT

**Hash:** `0xDDE6DF5AE89981D2` | **JHash:** `0xACFEB3F9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_BELONG_TO_THIS_SCRIPT(Entity entity, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_BELONG_TO_THIS_SCRIPT, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### DOES_ENTITY_HAVE_DRAWABLE

**Hash:** `0x060D6E96F8B8E48D` | **JHash:** `0xA5B33300` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_HAVE_DRAWABLE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_HAVE_DRAWABLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_ENTITY_HAVE_PHYSICS

**Hash:** `0xDA95EA3317CC5064` | **JHash:** `0x9BCD2979` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_HAVE_PHYSICS(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_HAVE_PHYSICS, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_ENTITY_HAVE_SKELETON

**Hash:** `0x764EB96874EFFDC1` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_HAVE_SKELETON(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_HAVE_SKELETON, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_ENTITY_HAVE_ANIM_DIRECTOR

**Hash:** `0x2158E81A6AF65EA9` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL DOES_ENTITY_HAVE_ANIM_DIRECTOR(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ENTITY_HAVE_ANIM_DIRECTOR, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ENTITY_ANIM_FINISHED

**Hash:** `0x20B711662962B472` | **JHash:** `0x1D9CAB92` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_ANIM_FINISHED(Entity entity, const char* animDict, const char* animName, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_ANIM_FINISHED, entity, animDict, animName, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** P3 is always 3 as far as i cant tell

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### HAS_ENTITY_BEEN_DAMAGED_BY_ANY_OBJECT

**Hash:** `0x95EB9964FF5C5C65` | **JHash:** `0x6B74582E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_BEEN_DAMAGED_BY_ANY_OBJECT(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_BEEN_DAMAGED_BY_ANY_OBJECT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ENTITY_BEEN_DAMAGED_BY_ANY_PED

**Hash:** `0x605F5A140F202491` | **JHash:** `0x53FD4A25` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_BEEN_DAMAGED_BY_ANY_PED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_BEEN_DAMAGED_BY_ANY_PED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ENTITY_BEEN_DAMAGED_BY_ANY_VEHICLE

**Hash:** `0xDFD5033FDBA0A9C8` | **JHash:** `0x878C2CE0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_BEEN_DAMAGED_BY_ANY_VEHICLE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_BEEN_DAMAGED_BY_ANY_VEHICLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ENTITY_BEEN_DAMAGED_BY_ENTITY

**Hash:** `0xC86D67D52A707CF8` | **JHash:** `0x07FC77E0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_BEEN_DAMAGED_BY_ENTITY(Entity entity1, Entity entity2, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_BEEN_DAMAGED_BY_ENTITY, entity1, entity2, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Entity 1 = Victim
Entity 2 = Attacker

p2 seems to always be 1

---

### HAS_ENTITY_CLEAR_LOS_TO_ENTITY

**Hash:** `0xFCDFF7B72D23A1AC` | **JHash:** `0x53576FA7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_CLEAR_LOS_TO_ENTITY(Entity entity1, Entity entity2, int traceType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_CLEAR_LOS_TO_ENTITY, entity1, entity2, traceType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `traceType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** traceType is always 17 in the scripts.

There is other codes used for traceType:
19 - in jewelry_prep1a
126 - in am_hunt_the_beast
256 & 287 - in fm_mission_controller

---

### HAS_ENTITY_CLEAR_LOS_TO_ENTITY_ADJUST_FOR_COVER

**Hash:** `0x394BDE2A7BBA031E` | **Build:** `1868`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_CLEAR_LOS_TO_ENTITY_ADJUST_FOR_COVER(Entity entity1, Entity entity2, int traceType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_CLEAR_LOS_TO_ENTITY_ADJUST_FOR_COVER, entity1, entity2, traceType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `traceType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ENTITY_CLEAR_LOS_TO_ENTITY_IN_FRONT

**Hash:** `0x0267D00AF114F17A` | **JHash:** `0x210D87C8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_CLEAR_LOS_TO_ENTITY_IN_FRONT(Entity entity1, Entity entity2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_CLEAR_LOS_TO_ENTITY_IN_FRONT, entity1, entity2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Has the entity1 got a clear line of sight to the other entity2 from the direction entity1 is facing.
This is one of the most CPU demanding BOOL natives in the game; avoid calling this in things like nested for-loops

---

### HAS_ENTITY_COLLIDED_WITH_ANYTHING

**Hash:** `0x8BAD02F0368D9E14` | **JHash:** `0x662A2F41` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_COLLIDED_WITH_ANYTHING(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_COLLIDED_WITH_ANYTHING, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Called on tick.
Tested with vehicles, returns true whenever the vehicle is touching any entity.

Note: for vehicles, the wheels can touch the ground and it will still return false, but if the body of the vehicle touches the ground, it will return true.

---

### _GET_LAST_ENTITY_HIT_BY_ENTITY

**Hash:** `0xA75EE4F689B85391` | **Build:** `2802`

**C++ 原型:**
```cpp
Entity _GET_LAST_ENTITY_HIT_BY_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_LAST_ENTITY_HIT_BY_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Entity` → `int`

---

### GET_LAST_MATERIAL_HIT_BY_ENTITY

**Hash:** `0x5C3D0A935F535C4C` | **JHash:** `0xC0E3AA47` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_LAST_MATERIAL_HIT_BY_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_LAST_MATERIAL_HIT_BY_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_COLLISION_NORMAL_OF_LAST_HIT_FOR_ENTITY

**Hash:** `0xE465D4AB7CA6AE72` | **JHash:** `0xAB415C07` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_COLLISION_NORMAL_OF_LAST_HIT_FOR_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_COLLISION_NORMAL_OF_LAST_HIT_FOR_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### FORCE_ENTITY_AI_AND_ANIMATION_UPDATE

**Hash:** `0x40FDEDB72F8293B2` | **JHash:** `0x58D9775F` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ENTITY_AI_AND_ANIMATION_UPDATE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ENTITY_AI_AND_ANIMATION_UPDATE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Based on carmod_shop script decompile this takes a vehicle parameter. It is called when repair is done on initial enter.

---

### GET_ENTITY_ANIM_CURRENT_TIME

**Hash:** `0x346D81500D088F42` | **JHash:** `0x83943F41` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_ANIM_CURRENT_TIME(Entity entity, const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_ANIM_CURRENT_TIME, entity, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `float` → `float`

**说明:** Returns a float value representing animation's current playtime with respect to its total playtime. This value increasing in a range from [0 to 1] and wrap back to 0 when it reach 1.

Example:
0.000000 - mark the starting of animation.
0.500000 - mark the midpoint of the animation.
1.000000 - mark the end of animation.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_ENTITY_ANIM_TOTAL_TIME

**Hash:** `0x50BD2730B191E360` | **JHash:** `0x433A9D18` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_ANIM_TOTAL_TIME(Entity entity, const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_ANIM_TOTAL_TIME, entity, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `float` → `float`

**说明:** Returns a float value representing animation's total playtime in milliseconds.

Example:
GET_ENTITY_ANIM_TOTAL_TIME(PLAYER_ID(),"amb@world_human_yoga@female@base","base_b") 
return 20800.000000

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_ANIM_DURATION

**Hash:** `0xFEDDF04D62B8D790` | **JHash:** `0x8B5E3E3D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ANIM_DURATION(const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ANIM_DURATION, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `float` → `float`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_ENTITY_ATTACHED_TO

**Hash:** `0x48C2BED9180FE123` | **JHash:** `0xFE1589F9` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_ENTITY_ATTACHED_TO(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_ATTACHED_TO, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Entity` → `int`

---

### GET_ENTITY_COORDS

**Hash:** `0x3FEF770D40960D5A` | **JHash:** `0x1647F1CB` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_COORDS(Entity entity, BOOL alive)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_COORDS, entity, alive);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `alive` | `BOOL` | `bool` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the current coordinates for a specified entity.
`entity` = The entity to get the coordinates from.
`alive` = Unused by the game, potentially used by debug builds of GTA in order to assert whether or not an entity was alive.

---

### GET_ENTITY_FORWARD_VECTOR

**Hash:** `0x0A794A5A57F8DF91` | **JHash:** `0x84DCECBF` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_FORWARD_VECTOR(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_FORWARD_VECTOR, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the entity's forward vector.

---

### GET_ENTITY_FORWARD_X

**Hash:** `0x8BB4EF4214E0E6D5` | **JHash:** `0x49FAE914` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_FORWARD_X(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_FORWARD_X, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Gets the X-component of the entity's forward vector.

---

### GET_ENTITY_FORWARD_Y

**Hash:** `0x866A4A5FAE349510` | **JHash:** `0x9E2F917C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_FORWARD_Y(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_FORWARD_Y, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Gets the Y-component of the entity's forward vector.

---

### GET_ENTITY_HEADING

**Hash:** `0xE83D4F9BA2A38914` | **JHash:** `0x972CC383` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_HEADING(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_HEADING, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Returns the heading of the entity in degrees. Also know as the "Yaw" of an entity.

---

### GET_ENTITY_HEADING_FROM_EULERS

**Hash:** `0x846BF6291198A71E` | **JHash:** `0x9320E642` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_HEADING_FROM_EULERS(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_HEADING_FROM_EULERS, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Gets the heading of the entity physics in degrees, which tends to be more accurate than just "GET_ENTITY_HEADING". This can be clearly seen while, for example, ragdolling a ped/player.

NOTE: The name and description of this native are based on independent research. If you find this native to be more suitable under a different name and/or described differently, please feel free to do so.

---

### GET_ENTITY_HEALTH

**Hash:** `0xEEF059FAD016D209` | **JHash:** `0x8E3222B7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_HEALTH(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_HEALTH, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns an integer value of entity's current health.

Example of range for ped:
- Player [0 to 200]
- Ped [100 to 200]
- Vehicle [0 to 1000]
- Object [0 to 1000]

Health is actually a float value but this native casts it to int.
In order to get the actual value, do:
float health = *(float *)(entityAddress + 0x280);

---

### GET_ENTITY_MAX_HEALTH

**Hash:** `0x15D757606D170C3C` | **JHash:** `0xC7AE6AA1` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_MAX_HEALTH(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_MAX_HEALTH, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

**说明:** Return an integer value of entity's maximum health.

Example:
- Player = 200
- Ped = 150

---

### SET_ENTITY_MAX_HEALTH

**Hash:** `0x166E7CF68597D8B5` | **JHash:** `0x96F84DF8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_MAX_HEALTH(Entity entity, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_MAX_HEALTH, entity, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** For instance: ENTITY::SET_ENTITY_MAX_HEALTH(PLAYER::PLAYER_PED_ID(), 200); // director_mode.c4: 67849

---

### GET_ENTITY_HEIGHT

**Hash:** `0x5A504562485944DD` | **JHash:** `0xEE443481` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_HEIGHT(Entity entity, float X, float Y, float Z, BOOL atTop, BOOL inWorldCoords)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_HEIGHT, entity, X, Y, Z, atTop, inWorldCoords);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `X` | `float` | `float` | |
| `Y` | `float` | `float` | |
| `Z` | `float` | `float` | |
| `atTop` | `BOOL` | `bool` | |
| `inWorldCoords` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

---

### GET_ENTITY_HEIGHT_ABOVE_GROUND

**Hash:** `0x1DD55701034110E5` | **JHash:** `0x57F56A4D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_HEIGHT_ABOVE_GROUND(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_HEIGHT_ABOVE_GROUND, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Return height (z-dimension) above ground. 
Example: The pilot in a titan plane is 1.844176 above ground.

How can i convert it to meters?
Everything seems to be in meters, probably this too.

---

### GET_ENTITY_MATRIX

**Hash:** `0xECB2FC7235A7D137` | **JHash:** `0xEB9EB001` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_ENTITY_MATRIX(Entity entity, Vector3* forwardVector, Vector3* rightVector, Vector3* upVector, Vector3* position)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_ENTITY_MATRIX, entity, forwardVector, rightVector, upVector, position);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `forwardVector` | `Vector3*` | `Vector3*` | |
| `rightVector` | `Vector3*` | `Vector3*` | |
| `upVector` | `Vector3*` | `Vector3*` | |
| `position` | `Vector3*` | `Vector3*` | |

**返回值:** `void` → `void`

---

### GET_ENTITY_MODEL

**Hash:** `0x9F47B058362C84B5` | **JHash:** `0xDAFCB3EC` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_ENTITY_MODEL(Entity entity)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_ENTITY_MODEL, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the model hash from the entity

---

### GET_OFFSET_FROM_ENTITY_GIVEN_WORLD_COORDS

**Hash:** `0x2274BC1C4885E333` | **JHash:** `0x6477EC9E` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_OFFSET_FROM_ENTITY_GIVEN_WORLD_COORDS(Entity entity, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_OFFSET_FROM_ENTITY_GIVEN_WORLD_COORDS, entity, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Converts world coords (posX - Z) to coords relative to the entity

Example:
posX is given as 50
entity's x coord is 40
the returned x coord will then be 10 or -10, not sure haven't used this in a while (think it is 10 though).

---

### GET_OFFSET_FROM_ENTITY_IN_WORLD_COORDS

**Hash:** `0x1899F328B0E12848` | **JHash:** `0xABCF043A` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_OFFSET_FROM_ENTITY_IN_WORLD_COORDS(Entity entity, float offsetX, float offsetY, float offsetZ)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_OFFSET_FROM_ENTITY_IN_WORLD_COORDS, entity, offsetX, offsetY, offsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Offset values are relative to the entity.

x = left/right
y = forward/backward
z = up/down

---

### GET_ENTITY_PITCH

**Hash:** `0xD45DC2893621E1FE` | **JHash:** `0xFCE6ECE5` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_PITCH(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_PITCH, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

---

### GET_ENTITY_QUATERNION

**Hash:** `0x7B3703D2D32DFA18` | **JHash:** `0x5154EC90` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_ENTITY_QUATERNION(Entity entity, float* x, float* y, float* z, float* w)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_ENTITY_QUATERNION, entity, x, y, z, w);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float*` | `float*` | |
| `y` | `float*` | `float*` | |
| `z` | `float*` | `float*` | |
| `w` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** w is the correct parameter name!

---

### GET_ENTITY_ROLL

**Hash:** `0x831E0242595560DF` | **JHash:** `0x36610842` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_ROLL(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_ROLL, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Displays the current ROLL axis of the entity [-180.0000/180.0000+]
(Sideways Roll) such as a vehicle tipped on its side

---

### GET_ENTITY_ROTATION

**Hash:** `0xAFBD61CC738D9EB9` | **JHash:** `0x8FF45B04` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_ROTATION(Entity entity, int rotationOrder)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_ROTATION, entity, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** rotationOrder is the order yaw, pitch and roll is applied. Usually 2. Returns a vector where the Z coordinate is the yaw.

rotationOrder refers to the order yaw pitch roll is applied; value ranges from 0 to 5 and is usually *2* in scripts.
What you use for rotationOrder when getting must be the same as rotationOrder when setting the rotation.

What it returns is the yaw on the z part of the vector, which makes sense considering R* considers z as vertical. Here's a picture for those of you who don't understand pitch, yaw, and roll: www.allstar.fiu.edu/aero/images/pic5-1.gif

Rotation Orders:
0: ZYX - Rotate around the z-axis, then the y-axis and finally the x-axis.
1: YZX - Rotate around the y-axis, then the z-axis and finally the x-axis.
2: ZXY - Rotate around the z-axis, then the x-axis and finally the y-axis.
3: XZY - Rotate around the x-axis, then the z-axis and finally the y-axis.
4: YXZ - Rotate around the y-axis, then the x-axis and finally the z-axis.
5: XYZ - Rotate around the x-axis, then the y-axis and finally the z-axis.

---

### GET_ENTITY_ROTATION_VELOCITY

**Hash:** `0x213B91045D09B983` | **JHash:** `0x9BF8A73F` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_ROTATION_VELOCITY(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_ROTATION_VELOCITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_ENTITY_SCRIPT

**Hash:** `0xA6E9C38DB51D7748` | **JHash:** `0xB7F70784` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_ENTITY_SCRIPT(Entity entity, ScrHandle* script)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_ENTITY_SCRIPT, entity, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `script` | `ScrHandle*` | `int*` | |

**返回值:** `const char*` → `string`

**说明:** Returns the name of the script that owns/created the entity or nullptr. Second parameter is unused, can just be a nullptr.

---

### GET_ENTITY_SPEED

**Hash:** `0xD5037BA82E12416F` | **JHash:** `0x9E1E4798` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_SPEED(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_SPEED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** result is in meters per second

------------------------------------------------------------
So would the conversion to mph and km/h, be along the lines of this.

float speed = GET_ENTITY_SPEED(veh);
float kmh = (speed * 3.6);
float mph = (speed * 2.236936);
------------------------------------------------------------

---

### GET_ENTITY_SPEED_VECTOR

**Hash:** `0x9A8D700A51CB7B0D` | **JHash:** `0x3ED2B997` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_SPEED_VECTOR(Entity entity, BOOL relative)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_SPEED_VECTOR, entity, relative);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `relative` | `BOOL` | `bool` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Relative can be used for getting speed relative to the frame of the vehicle, to determine for example, if you are going in reverse (-y speed) or not (+y speed). 

---

### GET_ENTITY_UPRIGHT_VALUE

**Hash:** `0x95EED5A694951F9F` | **JHash:** `0xF4268190` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_UPRIGHT_VALUE(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_UPRIGHT_VALUE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

---

### GET_ENTITY_VELOCITY

**Hash:** `0x4805D2B1D8CF94A9` | **JHash:** `0xC14C9B6B` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_VELOCITY(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_VELOCITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_OBJECT_INDEX_FROM_ENTITY_INDEX

**Hash:** `0xD7E3B9735C0F89D6` | **JHash:** `0xBC5A9C58` | **Build:** `323`

**C++ 原型:**
```cpp
Object GET_OBJECT_INDEX_FROM_ENTITY_INDEX(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_OBJECT_INDEX_FROM_ENTITY_INDEX, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Object` → `int`

**说明:** Simply returns whatever is passed to it (Regardless of whether the handle is valid or not).

---

### GET_PED_INDEX_FROM_ENTITY_INDEX

**Hash:** `0x04A2A40C73395041` | **JHash:** `0xC46F74AC` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PED_INDEX_FROM_ENTITY_INDEX(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_INDEX_FROM_ENTITY_INDEX, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Ped` → `int`

**说明:** Simply returns whatever is passed to it (Regardless of whether the handle is valid or not).

---

### GET_VEHICLE_INDEX_FROM_ENTITY_INDEX

**Hash:** `0x4B53F92932ADFAC0` | **JHash:** `0xC69CF43D` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_INDEX_FROM_ENTITY_INDEX(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_INDEX_FROM_ENTITY_INDEX, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Simply returns whatever is passed to it (Regardless of whether the handle is valid or not).

---

### GET_WORLD_POSITION_OF_ENTITY_BONE

**Hash:** `0x44A8FCB8ED227738` | **JHash:** `0x7C6339DF` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_WORLD_POSITION_OF_ENTITY_BONE(Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_WORLD_POSITION_OF_ENTITY_BONE, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Returns the coordinates of an entity-bone.

---

### GET_NEAREST_PLAYER_TO_ENTITY

**Hash:** `0x7196842CB375CDB3` | **JHash:** `0xCE17FDEC` | **Build:** `323`

**C++ 原型:**
```cpp
Player GET_NEAREST_PLAYER_TO_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEAREST_PLAYER_TO_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Player` → `int`

---

### GET_NEAREST_PLAYER_TO_ENTITY_ON_TEAM

**Hash:** `0x4DC9A62F844D9337` | **JHash:** `0xB1808F56` | **Build:** `323`

**C++ 原型:**
```cpp
Player GET_NEAREST_PLAYER_TO_ENTITY_ON_TEAM(Entity entity, int team)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEAREST_PLAYER_TO_ENTITY_ON_TEAM, entity, team);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `team` | `int` | `int` | |

**返回值:** `Player` → `int`

---

### GET_NEAREST_PARTICIPANT_TO_ENTITY

**Hash:** `0xFFBD7052D65BE0FF` | **Build:** `2944`

**C++ 原型:**
```cpp
int GET_NEAREST_PARTICIPANT_TO_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEAREST_PARTICIPANT_TO_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

---

### GET_ENTITY_TYPE

**Hash:** `0x8ACD366038D14505` | **JHash:** `0x0B1BD08D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_TYPE(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_TYPE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns:
0 = no entity
1 = ped
2 = vehicle
3 = object

---

### GET_ENTITY_POPULATION_TYPE

**Hash:** `0xF6F5161F4534EDFF` | **JHash:** `0xFC30DDFF` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_POPULATION_TYPE(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_POPULATION_TYPE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

**说明:** A population type, from the following enum: https://alloc8or.re/gta5/doc/enums/ePopulationType.txt

---

### IS_AN_ENTITY

**Hash:** `0x731EC8A916BD11A1` | **JHash:** `0xD4B9715A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AN_ENTITY(ScrHandle handle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AN_ENTITY, handle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `handle` | `ScrHandle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_A_PED

**Hash:** `0x524AC5ECEA15343E` | **JHash:** `0x55D33EAB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_A_PED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_A_PED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_A_MISSION_ENTITY

**Hash:** `0x0A7B270912999B3C` | **JHash:** `0x2632E124` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_A_MISSION_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_A_MISSION_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_A_VEHICLE

**Hash:** `0x6AC7003FA6E5575E` | **JHash:** `0xBE800B01` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_A_VEHICLE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_A_VEHICLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_AN_OBJECT

**Hash:** `0x8D68C8FD0FACA94E` | **JHash:** `0x3F52E561` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_AN_OBJECT(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_AN_OBJECT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_AT_COORD

**Hash:** `0x20B60995556D004F` | **JHash:** `0xD749B606` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_AT_COORD(Entity entity, float xPos, float yPos, float zPos, float xSize, float ySize, float zSize, BOOL p7, BOOL p8, int p9)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_AT_COORD, entity, xPos, yPos, zPos, xSize, ySize, zSize, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xSize` | `float` | `float` | |
| `ySize` | `float` | `float` | |
| `zSize` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if entity is within x/y/zSize distance of x/y/z. 

Last three are unknown ints, almost always p7 = 0, p8 = 1, p9 = 0

---

### IS_ENTITY_AT_ENTITY

**Hash:** `0x751B70C3D034E187` | **JHash:** `0xDABDCB52` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_AT_ENTITY(Entity entity1, Entity entity2, float xSize, float ySize, float zSize, BOOL p5, BOOL p6, int p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_AT_ENTITY, entity1, entity2, xSize, ySize, zSize, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `xSize` | `float` | `float` | |
| `ySize` | `float` | `float` | |
| `zSize` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if entity1 is within the box defined by x/y/zSize of entity2.

Last three parameters are almost alwasy p5 = 0, p6 = 1, p7 = 0

---

### IS_ENTITY_ATTACHED

**Hash:** `0xB346476EF1A64897` | **JHash:** `0xEC1479D5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Whether the entity is attached to any other entity.

---

### IS_ENTITY_ATTACHED_TO_ANY_OBJECT

**Hash:** `0xCF511840CEEDE0CC` | **JHash:** `0x0B5DE340` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED_TO_ANY_OBJECT(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED_TO_ANY_OBJECT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_ATTACHED_TO_ANY_PED

**Hash:** `0xB1632E9A5F988D11` | **JHash:** `0x9D7A609C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED_TO_ANY_PED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED_TO_ANY_PED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_ATTACHED_TO_ANY_VEHICLE

**Hash:** `0x26AA915AD89BFB4B` | **JHash:** `0xDE5C995E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED_TO_ANY_VEHICLE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED_TO_ANY_VEHICLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_ATTACHED_TO_ENTITY

**Hash:** `0xEFBE71898A993728` | **JHash:** `0xB0ABFEA8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED_TO_ENTITY(Entity from, Entity to)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED_TO_ENTITY, from, to);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `from` | `Entity` | `int` | |
| `to` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_DEAD

**Hash:** `0x5F9532F3B5CC2551` | **JHash:** `0xB6F7CBAC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_DEAD(Entity entity, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_DEAD, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_IN_AIR

**Hash:** `0x886E37EC497200B6` | **JHash:** `0xA4157987` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_AIR(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_AIR, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_IN_ANGLED_AREA

**Hash:** `0x51210CED3DA1C78A` | **JHash:** `0x883622FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_ANGLED_AREA(Entity entity, float x1, float y1, float z1, float x2, float y2, float z2, float width, BOOL debug, BOOL includeZ, Any p10)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_ANGLED_AREA, entity, x1, y1, z1, x2, y2, z2, width, debug, includeZ, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `debug` | `BOOL` | `bool` | |
| `includeZ` | `BOOL` | `bool` | |
| `p10` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** `p8` is a debug flag invoking functions in the same path as ``DRAW_MARKER``
`p10` is some entity flag check, also used in `IS_ENTITY_AT_ENTITY`, `IS_ENTITY_IN_AREA`, and `IS_ENTITY_AT_COORD`.
See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.

---

### IS_ENTITY_IN_AREA

**Hash:** `0x54736AA40E271165` | **JHash:** `0x8C2DFA9D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_AREA(Entity entity, float x1, float y1, float z1, float x2, float y2, float z2, BOOL p7, BOOL p8, Any p9)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_AREA, entity, x1, y1, z1, x2, y2, z2, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_IN_ZONE

**Hash:** `0xB6463CF6AF527071` | **JHash:** `0x45C82B21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_ZONE(Entity entity, const char* zone)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_ZONE, entity, zone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `zone` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/zones.json

---

### IS_ENTITY_IN_WATER

**Hash:** `0xCFB0A0D8EDD145A3` | **JHash:** `0x4C3C2508` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_WATER(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_WATER, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_ENTITY_SUBMERGED_LEVEL

**Hash:** `0xE81AFC1BC4CC41CE` | **JHash:** `0x0170F68C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ENTITY_SUBMERGED_LEVEL(Entity entity)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ENTITY_SUBMERGED_LEVEL, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `float` → `float`

**说明:** Get how much of the entity is submerged.  1.0f is whole entity.

---

### SET_ENTITY_REQUIRES_MORE_EXPENSIVE_RIVER_CHECK

**Hash:** `0x694E00132F2823ED` | **JHash:** `0x40C84A74` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_REQUIRES_MORE_EXPENSIVE_RIVER_CHECK(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_REQUIRES_MORE_EXPENSIVE_RIVER_CHECK, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ENTITY_ON_SCREEN

**Hash:** `0xE659E47AF827484B` | **JHash:** `0xC1FEC5ED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ON_SCREEN(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ON_SCREEN, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the entity is in between the minimum and maximum values for the 2d screen coords. 
This means that it will return true even if the entity is behind a wall for example, as long as you're looking at their location. 
Chipping

---

### IS_ENTITY_PLAYING_ANIM

**Hash:** `0x1F0B79228E461EC9` | **JHash:** `0x0D130D34` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_PLAYING_ANIM(Entity entity, const char* animDict, const char* animName, int taskFlag)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_PLAYING_ANIM, entity, animDict, animName, taskFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `taskFlag` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** See also PED::IS_SCRIPTED_SCENARIO_PED_USING_CONDITIONAL_ANIM 0x6EC47A344923E1ED 0x3C30B447

Taken from ENTITY::IS_ENTITY_PLAYING_ANIM(PLAYER::PLAYER_PED_ID(), "creatures@shark@move", "attack_player", 3)

p4 is always 3 in the scripts.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### IS_ENTITY_STATIC

**Hash:** `0x1218E6886D3D8327` | **JHash:** `0x928E12E9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_STATIC(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_STATIC, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** a static ped will not react to natives like "APPLY_FORCE_TO_ENTITY" or "SET_ENTITY_VELOCITY" and oftentimes will not react to task-natives like "TASK::TASK_COMBAT_PED". The only way I know of to make one of these peds react is to ragdoll them (or sometimes to use CLEAR_PED_TASKS_IMMEDIATELY(). Static peds include almost all far-away peds, beach-combers, peds in certain scenarios, peds crossing a crosswalk, peds walking to get back into their cars, and others. If anyone knows how to make a ped non-static without ragdolling them, please edit this with the solution.

---

### IS_ENTITY_TOUCHING_ENTITY

**Hash:** `0x17FFC1B2BA35A494` | **JHash:** `0x6B931477` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_TOUCHING_ENTITY(Entity entity, Entity targetEntity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_TOUCHING_ENTITY, entity, targetEntity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `targetEntity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_TOUCHING_MODEL

**Hash:** `0x0F42323798A58C8C` | **JHash:** `0x307E7611` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_TOUCHING_MODEL(Entity entity, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_TOUCHING_MODEL, entity, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_UPRIGHT

**Hash:** `0x5333F526F6AB19AA` | **JHash:** `0x3BCDF4E1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_UPRIGHT(Entity entity, float angle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_UPRIGHT, entity, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_UPSIDEDOWN

**Hash:** `0x1DBD58820FA61D71` | **JHash:** `0x5ACAA48F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_UPSIDEDOWN(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_UPSIDEDOWN, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_VISIBLE

**Hash:** `0x47D6F43D77935C75` | **JHash:** `0x120B4ED5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_VISIBLE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_VISIBLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_VISIBLE_TO_SCRIPT

**Hash:** `0xD796CB5BA8F20E32` | **JHash:** `0x5D240E9D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_VISIBLE_TO_SCRIPT(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_VISIBLE_TO_SCRIPT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_OCCLUDED

**Hash:** `0xE31C2C72B8692B64` | **JHash:** `0x46BC5B40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_OCCLUDED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_OCCLUDED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### WOULD_ENTITY_BE_OCCLUDED

**Hash:** `0xEE5D2A122E09EC42` | **JHash:** `0xEA127CBC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WOULD_ENTITY_BE_OCCLUDED(Hash entityModelHash, float x, float y, float z, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WOULD_ENTITY_BE_OCCLUDED, entityModelHash, x, y, z, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entityModelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTITY_WAITING_FOR_WORLD_COLLISION

**Hash:** `0xD05BFF0C0A12C68F` | **JHash:** `0x00AB7A4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_WAITING_FOR_WORLD_COLLISION(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_WAITING_FOR_WORLD_COLLISION, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### APPLY_FORCE_TO_ENTITY_CENTER_OF_MASS

**Hash:** `0x18FF00FC7EFF559E` | **JHash:** `0x28924E98` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_FORCE_TO_ENTITY_CENTER_OF_MASS(Entity entity, int forceType, float x, float y, float z, BOOL p5, BOOL isDirectionRel, BOOL isForceRel, BOOL p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_FORCE_TO_ENTITY_CENTER_OF_MASS, entity, forceType, x, y, z, p5, isDirectionRel, isForceRel, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `forceType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `isDirectionRel` | `BOOL` | `bool` | |
| `isForceRel` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Applies a force to the specified entity.

**List of force types (p1)**:
public enum ForceType
{
    MinForce = 0,
    MaxForceRot = 1,
    MinForce2 = 2,
    MaxForceRot2 = 3,
    ForceNoRot = 4,
    ForceRotPlusForce = 5
}
Research/documentation on the gtaforums can be found here https://gtaforums.com/topic/885669-precisely-define-object-physics/) and here https://gtaforums.com/topic/887362-apply-forces-and-momentums-to-entityobject/.

p6/relative - makes the xyz force not relative to world coords, but to something else
p7/highForce - setting false will make the force really low

---

### APPLY_FORCE_TO_ENTITY

**Hash:** `0xC5F68BE9613E2D18` | **JHash:** `0xC1C0855A` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_FORCE_TO_ENTITY(Entity entity, int forceFlags, float x, float y, float z, float offX, float offY, float offZ, int boneIndex, BOOL isDirectionRel, BOOL ignoreUpVec, BOOL isForceRel, BOOL p12, BOOL p13)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_FORCE_TO_ENTITY, entity, forceFlags, x, y, z, offX, offY, offZ, boneIndex, isDirectionRel, ignoreUpVec, isForceRel, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `forceFlags` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `offX` | `float` | `float` | |
| `offY` | `float` | `float` | |
| `offZ` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `isDirectionRel` | `BOOL` | `bool` | |
| `ignoreUpVec` | `BOOL` | `bool` | |
| `isForceRel` | `BOOL` | `bool` | |
| `p12` | `BOOL` | `bool` | |
| `p13` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Documented here:
gtaforums.com/topic/885669-precisely-define-object-physics/
gtaforums.com/topic/887362-apply-forces-and-momentums-to-entityobject/

forceFlags:
First bit (lowest): Strong force flag, factor 100
Second bit: Unkown flag
Third bit: Momentum flag=1 (vector (x,y,z) is a momentum, more research needed)
If higher bits are unequal 0 the function doesn't applay any forces at all.
(As integer possible values are 0-7)

0: weak force
1: strong force
2: same as 0 (2nd bit?)
3: same as 1
4: weak momentum
5: strong momentum
6: same as 4
7: same as 5

isLocal: vector defined in local (body-fixed) coordinate frame
isMassRel: if true the force gets multiplied with the objects mass (this is why it was known as highForce) and different objects will have the same acceleration.

p8 !!! Whenever I set this !=0, my script stopped.

---

### ATTACH_ENTITY_TO_ENTITY

**Hash:** `0x6B9BBD38AB0796DF` | **JHash:** `0xEC024237` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_TO_ENTITY(Entity entity1, Entity entity2, int boneIndex, float xPos, float yPos, float zPos, float xRot, float yRot, float zRot, BOOL p9, BOOL useSoftPinning, BOOL collision, BOOL isPed, int vertexIndex, BOOL fixedRot, Any p15)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_TO_ENTITY, entity1, entity2, boneIndex, xPos, yPos, zPos, xRot, yRot, zRot, p9, useSoftPinning, collision, isPed, vertexIndex, fixedRot, p15);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `p9` | `BOOL` | `bool` | |
| `useSoftPinning` | `BOOL` | `bool` | |
| `collision` | `BOOL` | `bool` | |
| `isPed` | `BOOL` | `bool` | |
| `vertexIndex` | `int` | `int` | |
| `fixedRot` | `BOOL` | `bool` | |
| `p15` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Attaches entity1 to bone (boneIndex) of entity2.

boneIndex - this is different to boneID, use GET_PED_BONE_INDEX to get the index from the ID. use the index for attaching to specific bones. entity1 will be attached to entity2's centre if bone index given doesn't correspond to bone indexes for that entity type.

useSoftPinning - if set to false attached entity will not detach when fixed
collision - controls collision between the two entities (FALSE disables collision).
isPed - pitch doesnt work when false and roll will only work on negative numbers (only peds)
vertexIndex - position of vertex
fixedRot - if false it ignores entity vector 


---

### ATTACH_ENTITY_BONE_TO_ENTITY_BONE

**Hash:** `0x5C48B75732C8456C` | **Build:** `791`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_BONE_TO_ENTITY_BONE(Entity entity1, Entity entity2, int boneIndex1, int boneIndex2, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_BONE_TO_ENTITY_BONE, entity1, entity2, boneIndex1, boneIndex2, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `boneIndex1` | `int` | `int` | |
| `boneIndex2` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ATTACH_ENTITY_BONE_TO_ENTITY_BONE_Y_FORWARD

**Hash:** `0xFD1695C5D3B05439` | **Build:** `791`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_BONE_TO_ENTITY_BONE_Y_FORWARD(Entity entity1, Entity entity2, int boneIndex1, int boneIndex2, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_BONE_TO_ENTITY_BONE_Y_FORWARD, entity1, entity2, boneIndex1, boneIndex2, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `boneIndex1` | `int` | `int` | |
| `boneIndex2` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ATTACH_ENTITY_TO_ENTITY_PHYSICALLY

**Hash:** `0xC3675780C92F90F9` | **JHash:** `0x0547417F` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_TO_ENTITY_PHYSICALLY(Entity entity1, Entity entity2, int boneIndex1, int boneIndex2, float xPos1, float yPos1, float zPos1, float xPos2, float yPos2, float zPos2, float xRot, float yRot, float zRot, float breakForce, BOOL fixedRot, BOOL p15, BOOL collision, BOOL p17, int p18)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_TO_ENTITY_PHYSICALLY, entity1, entity2, boneIndex1, boneIndex2, xPos1, yPos1, zPos1, xPos2, yPos2, zPos2, xRot, yRot, zRot, breakForce, fixedRot, p15, collision, p17, p18);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `boneIndex1` | `int` | `int` | |
| `boneIndex2` | `int` | `int` | |
| `xPos1` | `float` | `float` | |
| `yPos1` | `float` | `float` | |
| `zPos1` | `float` | `float` | |
| `xPos2` | `float` | `float` | |
| `yPos2` | `float` | `float` | |
| `zPos2` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `breakForce` | `float` | `float` | |
| `fixedRot` | `BOOL` | `bool` | |
| `p15` | `BOOL` | `bool` | |
| `collision` | `BOOL` | `bool` | |
| `p17` | `BOOL` | `bool` | |
| `p18` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** breakForce is the amount of force required to break the bond.
p14 - is always 1 in scripts
p15 - is 1 or 0 in scripts - unknoun what it does
p16 - controls collision between the two entities (FALSE disables collision).
p17 - do not teleport entity to be attached to the position of the bone Index of the target entity (if 1, entity will not be teleported to target bone)
p18 - is always 2 in scripts.



---

### ATTACH_ENTITY_TO_ENTITY_PHYSICALLY_OVERRIDE_INVERSE_MASS

**Hash:** `0x168A09D1B25B0BA4` | **Build:** `2944`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_TO_ENTITY_PHYSICALLY_OVERRIDE_INVERSE_MASS(Entity firstEntityIndex, Entity secondEntityIndex, int firstEntityBoneIndex, int secondEntityBoneIndex, float secondEntityOffsetX, float secondEntityOffsetY, float secondEntityOffsetZ, float firstEntityOffsetX, float firstEntityOffsetY, float firstEntityOffsetZ, float vecRotationX, float vecRotationY, float vecRotationZ, float physicalStrength, BOOL constrainRotation, BOOL doInitialWarp, BOOL collideWithEntity, BOOL addInitialSeperation, int rotOrder, float invMassScaleA, float invMassScaleB)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_TO_ENTITY_PHYSICALLY_OVERRIDE_INVERSE_MASS, firstEntityIndex, secondEntityIndex, firstEntityBoneIndex, secondEntityBoneIndex, secondEntityOffsetX, secondEntityOffsetY, secondEntityOffsetZ, firstEntityOffsetX, firstEntityOffsetY, firstEntityOffsetZ, vecRotationX, vecRotationY, vecRotationZ, physicalStrength, constrainRotation, doInitialWarp, collideWithEntity, addInitialSeperation, rotOrder, invMassScaleA, invMassScaleB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `firstEntityIndex` | `Entity` | `int` | |
| `secondEntityIndex` | `Entity` | `int` | |
| `firstEntityBoneIndex` | `int` | `int` | |
| `secondEntityBoneIndex` | `int` | `int` | |
| `secondEntityOffsetX` | `float` | `float` | |
| `secondEntityOffsetY` | `float` | `float` | |
| `secondEntityOffsetZ` | `float` | `float` | |
| `firstEntityOffsetX` | `float` | `float` | |
| `firstEntityOffsetY` | `float` | `float` | |
| `firstEntityOffsetZ` | `float` | `float` | |
| `vecRotationX` | `float` | `float` | |
| `vecRotationY` | `float` | `float` | |
| `vecRotationZ` | `float` | `float` | |
| `physicalStrength` | `float` | `float` | |
| `constrainRotation` | `BOOL` | `bool` | |
| `doInitialWarp` | `BOOL` | `bool` | |
| `collideWithEntity` | `BOOL` | `bool` | |
| `addInitialSeperation` | `BOOL` | `bool` | |
| `rotOrder` | `int` | `int` | |
| `invMassScaleA` | `float` | `float` | |
| `invMassScaleB` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PROCESS_ENTITY_ATTACHMENTS

**Hash:** `0xF4080490ADC51C6F` | **JHash:** `0x6909BA59` | **Build:** `323`

**C++ 原型:**
```cpp
void PROCESS_ENTITY_ATTACHMENTS(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PROCESS_ENTITY_ATTACHMENTS, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Called to update entity attachments.

---

### GET_ENTITY_BONE_INDEX_BY_NAME

**Hash:** `0xFB71170B7E76ACBA` | **JHash:** `0xE4ECAC22` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_BONE_INDEX_BY_NAME(Entity entity, const char* boneName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_BONE_INDEX_BY_NAME, entity, boneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Returns the index of the bone. If the bone was not found, -1 will be returned. 

list:
https://pastebin.com/D7JMnX1g

BoneNames:
  chassis,
  windscreen,
   seat_pside_r,
 seat_dside_r,
 bodyshell,
    suspension_lm,
    suspension_lr,
    platelight,
   attach_female,
    attach_male,
  bonnet,
   boot,
 chassis_dummy,  //Center of the dummy
 chassis_Control,    //Not found yet
   door_dside_f,   //Door left, front
    door_dside_r,   //Door left, back
 door_pside_f,   //Door right, front
   door_pside_r,   //Door right, back
    Gun_GripR,
    windscreen_f,
 platelight, //Position where the light above the numberplate is located
   VFX_Emitter,
  window_lf,  //Window left, front
  window_lr,  //Window left, back
   window_rf,  //Window right, front
 window_rr,  //Window right, back
  engine, //Position of the engine
  gun_ammo,
 ROPE_ATTATCH,   //Not misspelled. In script "finale_heist2b.c4".
    wheel_lf,   //Wheel left, front
   wheel_lr,   //Wheel left, back
    wheel_rf,   //Wheel right, front
  wheel_rr,   //Wheel right, back
   exhaust,    //Exhaust. shows only the position of the stock-exhaust
   overheat,   //A position on the engine(not exactly sure, how to name it)
  misc_e, //Not a car-bone.
 seat_dside_f,   //Driver-seat
 seat_pside_f,   //Seat next to driver
 Gun_Nuzzle,
   seat_r

I doubt that the function is case-sensitive, since I found a "Chassis" and a "chassis". - Just tested: Definitely not case-sensitive.



---

### CLEAR_ENTITY_LAST_DAMAGE_ENTITY

**Hash:** `0xA72CD9CA74A5ECBA` | **JHash:** `0x2B83F43B` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ENTITY_LAST_DAMAGE_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ENTITY_LAST_DAMAGE_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### DELETE_ENTITY

**Hash:** `0xAE3CBE5BF394C9C9` | **JHash:** `0xFAA3D236` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_ENTITY(Entity* entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Deletes the specified entity, then sets the handle pointed to by the pointer to NULL.

---

### DETACH_ENTITY

**Hash:** `0x961AC54BF0613F5D` | **JHash:** `0xC8EFCB41` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_ENTITY(Entity entity, BOOL dynamic, BOOL collision)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_ENTITY, entity, dynamic, collision);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `dynamic` | `BOOL` | `bool` | |
| `collision` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If `collision` is set to true, both entities won't collide with the other until the distance between them is above 4 meters.
Set `dynamic` to true to keep velocity after dettaching

---

### FREEZE_ENTITY_POSITION

**Hash:** `0x428CA6DBD1094446` | **JHash:** `0x65C16D57` | **Build:** `323`

**C++ 原型:**
```cpp
void FREEZE_ENTITY_POSITION(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREEZE_ENTITY_POSITION, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Freezes or unfreezes an entity preventing its coordinates to change by the player if set to `true`. You can still change the entity position using SET_ENTITY_COORDS.

---

### SET_ENTITY_SHOULD_FREEZE_WAITING_ON_COLLISION

**Hash:** `0x3910051CCECDB00C` | **JHash:** `0xD3850671` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_SHOULD_FREEZE_WAITING_ON_COLLISION(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_SHOULD_FREEZE_WAITING_ON_COLLISION, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** True means it can be deleted by the engine when switching lobbies/missions/etc, false means the script is expected to clean it up.

"Allow Freeze If No Collision"

---

### PLAY_ENTITY_ANIM

**Hash:** `0x7FB218262B810701` | **JHash:** `0x878753D5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_ENTITY_ANIM(Entity entity, const char* animName, const char* animDict, float p3, BOOL loop, BOOL stayInAnim, BOOL p6, float delta, Any bitset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_ENTITY_ANIM, entity, animName, animDict, p3, loop, stayInAnim, p6, delta, bitset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animName` | `const char*` | `string` | |
| `animDict` | `const char*` | `string` | |
| `p3` | `float` | `float` | |
| `loop` | `BOOL` | `bool` | |
| `stayInAnim` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `delta` | `float` | `float` | |
| `bitset` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** delta and bitset are guessed fields. They are based on the fact that most of the calls have 0 or nil field types passed in.

The only time bitset has a value is 0x4000 and the only time delta has a value is during stealth with usually <1.0f values.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### PLAY_SYNCHRONIZED_ENTITY_ANIM

**Hash:** `0xC77720A12FE14A86` | **JHash:** `0x012760AA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_SYNCHRONIZED_ENTITY_ANIM(Entity entity, int syncedScene, const char* animation, const char* propName, float p4, float p5, Any p6, float p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_SYNCHRONIZED_ENTITY_ANIM, entity, syncedScene, animation, propName, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `syncedScene` | `int` | `int` | |
| `animation` | `const char*` | `string` | |
| `propName` | `const char*` | `string` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `Any` | `object` | |
| `p7` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** p4 and p7 are usually 1000.0f.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### PLAY_SYNCHRONIZED_MAP_ENTITY_ANIM

**Hash:** `0xB9C54555ED30FBC4` | **JHash:** `0xEB4CBA74` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAY_SYNCHRONIZED_MAP_ENTITY_ANIM(float x1, float y1, float z1, float x2, Any y2, float z2, const char* p6, const char* p7, float p8, float p9, Any p10, float p11)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAY_SYNCHRONIZED_MAP_ENTITY_ANIM, x1, y1, z1, x2, y2, z2, p6, p7, p8, p9, p10, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `Any` | `object` | |
| `z2` | `float` | `float` | |
| `p6` | `const char*` | `string` | |
| `p7` | `const char*` | `string` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `Any` | `object` | |
| `p11` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** p6,p7 probably animname and animdict

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### STOP_SYNCHRONIZED_MAP_ENTITY_ANIM

**Hash:** `0x11E79CAB7183B6F5` | **JHash:** `0x7253D5B2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STOP_SYNCHRONIZED_MAP_ENTITY_ANIM(float x1, float y1, float z1, float x2, Any y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STOP_SYNCHRONIZED_MAP_ENTITY_ANIM, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `Any` | `object` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### STOP_ENTITY_ANIM

**Hash:** `0x28004F88151E03E0` | **JHash:** `0xC4769830` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STOP_ENTITY_ANIM(Entity entity, const char* animation, const char* animGroup, float p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STOP_ENTITY_ANIM, entity, animation, animGroup, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animation` | `const char*` | `string` | |
| `animGroup` | `const char*` | `string` | |
| `p3` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

RAGEPluginHook list: docs.ragepluginhook.net/html/62951c37-a440-478c-b389-c471230ddfc5.htm

---

### STOP_SYNCHRONIZED_ENTITY_ANIM

**Hash:** `0x43D3807C077261E3` | **JHash:** `0xE27D2FC1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STOP_SYNCHRONIZED_ENTITY_ANIM(Entity entity, float p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STOP_SYNCHRONIZED_ENTITY_ANIM, entity, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### HAS_ANIM_EVENT_FIRED

**Hash:** `0xEAF4CD9EA3E7E922` | **JHash:** `0x66571CA0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ANIM_EVENT_FIRED(Entity entity, Hash actionHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ANIM_EVENT_FIRED, entity, actionHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `actionHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** if (ENTITY::HAS_ANIM_EVENT_FIRED(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("CreateObject")))

---

### FIND_ANIM_EVENT_PHASE

**Hash:** `0x07F1BE2BCCAA27A7` | **JHash:** `0xC41DDA62` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FIND_ANIM_EVENT_PHASE(const char* animDictionary, const char* animName, const char* p2, Any* p3, Any* p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FIND_ANIM_EVENT_PHASE, animDictionary, animName, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDictionary` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `Any*` | `object*` | |
| `p4` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** In the script "player_scene_t_bbfight.c4":
"if (ENTITY::FIND_ANIM_EVENT_PHASE(&l_16E, &l_19F[v_4/*16*/], v_9, &v_A, &v_B))"
-- &l_16E (p0) is requested as an anim dictionary earlier in the script.
-- &l_19F[v_4/*16*/] (p1) is used in other natives in the script as the "animation" param.
-- v_9 (p2) is instantiated as "victim_fall"; I'm guessing that's another anim
--v_A and v_B (p3 & p4) are both set as -1.0, but v_A is used immediately after this native for: 
"if (v_A < ENTITY::GET_ENTITY_ANIM_CURRENT_TIME(...))"
Both v_A and v_B are seemingly used to contain both Vector3's and floats, so I can't say what either really is other than that they are both output parameters. p4 looks more like a *Vector3 though

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### SET_ENTITY_ANIM_CURRENT_TIME

**Hash:** `0x4487C259F0F70977` | **JHash:** `0x99D90735` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ANIM_CURRENT_TIME(Entity entity, const char* animDictionary, const char* animName, float time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ANIM_CURRENT_TIME, entity, animDictionary, animName, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `time` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### SET_ENTITY_ANIM_SPEED

**Hash:** `0x28D1A16553C51776` | **JHash:** `0x3990C90A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ANIM_SPEED(Entity entity, const char* animDictionary, const char* animName, float speedMultiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ANIM_SPEED, entity, animDictionary, animName, speedMultiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `speedMultiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### SET_ENTITY_AS_MISSION_ENTITY

**Hash:** `0xAD738C3085FE7E11` | **JHash:** `0x5D1F9E0F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_AS_MISSION_ENTITY(Entity entity, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_AS_MISSION_ENTITY, entity, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the specified entity (ped, vehicle or object) persistent. Persistent entities will not automatically be removed by the engine.

p1 has no effect when either its on or off 
maybe a quick disassembly will tell us what it does

p2 has no effect when either its on or off 
maybe a quick disassembly will tell us what it does

---

### SET_ENTITY_AS_NO_LONGER_NEEDED

**Hash:** `0xB736A491E64A32CF` | **JHash:** `0xADF2267C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_AS_NO_LONGER_NEEDED(Entity* entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_AS_NO_LONGER_NEEDED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Marks the specified entity (ped, vehicle or object) as no longer needed if its population type is set to the mission type.
If the entity is ped, it will also clear their tasks immediately just like when CLEAR_PED_TASKS_IMMEDIATELY is called.
Entities marked as no longer needed, will be deleted as the engine sees fit.
Use this if you just want to just let the game delete the ped:
void MarkPedAsAmbientPed(Ped ped) {
  auto addr = getScriptHandleBaseAddress(ped);

  if (!addr) {
    return;
  }

  //the game uses only lower 4 bits as entity population type 
  BYTE origValue = *(BYTE *)(addr + 0xDA);
  *(BYTE *)(addr + 0xDA) = ((origValue & 0xF0) | ePopulationType::POPTYPE_RANDOM_AMBIENT);
}

---

### SET_PED_AS_NO_LONGER_NEEDED

**Hash:** `0x2595DD4236549CE3` | **JHash:** `0x9A388380` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AS_NO_LONGER_NEEDED(Ped* ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AS_NO_LONGER_NEEDED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped*` | `int*` | |

**返回值:** `void` → `void`

**说明:** This is an alias of SET_ENTITY_AS_NO_LONGER_NEEDED.

---

### SET_VEHICLE_AS_NO_LONGER_NEEDED

**Hash:** `0x629BFA74418D6239` | **JHash:** `0x9B0E10BE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_AS_NO_LONGER_NEEDED(Vehicle* vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_AS_NO_LONGER_NEEDED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle*` | `int*` | |

**返回值:** `void` → `void`

**说明:** This is an alias of SET_ENTITY_AS_NO_LONGER_NEEDED.

---

### SET_OBJECT_AS_NO_LONGER_NEEDED

**Hash:** `0x3AE22DEB5BA5A3E6` | **JHash:** `0x3F6B949F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_AS_NO_LONGER_NEEDED(Object* object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_AS_NO_LONGER_NEEDED, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object*` | `int*` | |

**返回值:** `void` → `void`

**说明:** This is an alias of SET_ENTITY_AS_NO_LONGER_NEEDED.

---

### SET_ENTITY_CAN_BE_DAMAGED

**Hash:** `0x1760FFA8AB074D66` | **JHash:** `0x60B6E744` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_CAN_BE_DAMAGED(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CAN_BE_DAMAGED, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_ENTITY_CAN_BE_DAMAGED

**Hash:** `0xD95CC5D2AB15A09F` | **JHash:** `0xE4938B5D` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL GET_ENTITY_CAN_BE_DAMAGED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ENTITY_CAN_BE_DAMAGED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENTITY_CAN_BE_DAMAGED_BY_RELATIONSHIP_GROUP

**Hash:** `0xE22D8FDE858B8119` | **JHash:** `0x34165B5D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_CAN_BE_DAMAGED_BY_RELATIONSHIP_GROUP(Entity entity, BOOL bCanBeDamaged, int relGroup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CAN_BE_DAMAGED_BY_RELATIONSHIP_GROUP, entity, bCanBeDamaged, relGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `bCanBeDamaged` | `BOOL` | `bool` | |
| `relGroup` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_SCRIPT_PARTICIPANTS

**Hash:** `0x352E2B5CF420BF3B` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_SCRIPT_PARTICIPANTS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_SCRIPT_PARTICIPANTS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_CAN_BE_TARGETED_WITHOUT_LOS

**Hash:** `0xD3997889736FD899` | **JHash:** `0x3B13797C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_CAN_BE_TARGETED_WITHOUT_LOS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CAN_BE_TARGETED_WITHOUT_LOS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether the entity can be targeted without being in line-of-sight.

---

### SET_ENTITY_COLLISION

**Hash:** `0x1A9205C1B9EE827F` | **JHash:** `0x139FD37D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_COLLISION(Entity entity, BOOL toggle, BOOL keepPhysics)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_COLLISION, entity, toggle, keepPhysics);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `keepPhysics` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_ENTITY_COLLISION_DISABLED

**Hash:** `0xCCF1E97BEFDAE480` | **JHash:** `0xE8C0C629` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_ENTITY_COLLISION_DISABLED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ENTITY_COLLISION_DISABLED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENTITY_COMPLETELY_DISABLE_COLLISION

**Hash:** `0x9EBC85ED0FFFE51C` | **JHash:** `0xBD0D4831` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_COMPLETELY_DISABLE_COLLISION(Entity entity, BOOL toggle, BOOL keepPhysics)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_COMPLETELY_DISABLE_COLLISION, entity, toggle, keepPhysics);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `keepPhysics` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_COORDS

**Hash:** `0x06843DA7060A026B` | **JHash:** `0xDF70B41B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_COORDS(Entity entity, float xPos, float yPos, float zPos, BOOL xAxis, BOOL yAxis, BOOL zAxis, BOOL clearArea)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_COORDS, entity, xPos, yPos, zPos, xAxis, yAxis, zAxis, clearArea);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |
| `clearArea` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p7 is always 1 in the scripts. Set to 1, an area around the destination coords for the moved entity is cleared from other entities. 
 
Often ends with 1, 0, 0, 1); in the scripts. It works. 

Axis - Invert Axis Flags

---

### SET_ENTITY_COORDS_WITHOUT_PLANTS_RESET

**Hash:** `0x621873ECE1178967` | **JHash:** `0x749B282E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_COORDS_WITHOUT_PLANTS_RESET(Entity entity, float xPos, float yPos, float zPos, BOOL alive, BOOL deadFlag, BOOL ragdollFlag, BOOL clearArea)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_COORDS_WITHOUT_PLANTS_RESET, entity, xPos, yPos, zPos, alive, deadFlag, ragdollFlag, clearArea);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `alive` | `BOOL` | `bool` | |
| `deadFlag` | `BOOL` | `bool` | |
| `ragdollFlag` | `BOOL` | `bool` | |
| `clearArea` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_COORDS_NO_OFFSET

**Hash:** `0x239A3351AC1DA385` | **JHash:** `0x4C83DE8D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_COORDS_NO_OFFSET(Entity entity, float xPos, float yPos, float zPos, BOOL xAxis, BOOL yAxis, BOOL zAxis)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_COORDS_NO_OFFSET, entity, xPos, yPos, zPos, xAxis, yAxis, zAxis);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Axis - Invert Axis Flags

---

### SET_ENTITY_DYNAMIC

**Hash:** `0x1718DE8E3F2823CA` | **JHash:** `0x236F525B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_DYNAMIC(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_DYNAMIC, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_HEADING

**Hash:** `0x8E2530AA8ADA980E` | **JHash:** `0xE0FF064D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_HEADING(Entity entity, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_HEADING, entity, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Set the heading of an entity in degrees also known as "Yaw".

---

### SET_ENTITY_HEALTH

**Hash:** `0x6B76DC1F3AE6E6A3` | **JHash:** `0xFBCD1831` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_HEALTH(Entity entity, int health, Entity instigator, Hash weaponType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_HEALTH, entity, health, instigator, weaponType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `health` | `int` | `int` | |
| `instigator` | `Entity` | `int` | |
| `weaponType` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** health >= 0
male ped ~= 100 - 200
female ped ~= 0 - 100

---

### SET_ENTITY_INVINCIBLE

**Hash:** `0x3882114BDE571AD4` | **JHash:** `0xC1213A21` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_INVINCIBLE(Entity entity, BOOL toggle, BOOL dontResetOnCleanup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_INVINCIBLE, entity, toggle, dontResetOnCleanup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `dontResetOnCleanup` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a ped or an object totally invincible. It doesn't take any kind of damage. Peds will not ragdoll on explosions and the tazer animation won't apply either.

If you use this for a ped and you want Ragdoll to stay enabled, then do:
*(DWORD *)(pedAddress + 0x188) |= (1 << 9);

Use this if you want to get the invincibility status:
  bool IsPedInvincible(Ped ped)
 {
     auto addr = getScriptHandleBaseAddress(ped);    

        if (addr)
     {
         DWORD flag = *(DWORD *)(addr + 0x188);
            return ((flag & (1 << 8)) != 0) || ((flag & (1 << 9)) != 0);
      }

       return false;
 }

---

### SET_ENTITY_IS_TARGET_PRIORITY

**Hash:** `0xEA02E132F5C68722` | **JHash:** `0x9729EE32` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_IS_TARGET_PRIORITY(Entity entity, BOOL p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_IS_TARGET_PRIORITY, entity, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_LIGHTS

**Hash:** `0x7CFBA6A80BDF3874` | **JHash:** `0xE8FC85AF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_LIGHTS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_LIGHTS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_LOAD_COLLISION_FLAG

**Hash:** `0x0DC7CABAB1E9B67E` | **JHash:** `0xC52F295B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_LOAD_COLLISION_FLAG(Entity entity, BOOL toggle, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_LOAD_COLLISION_FLAG, entity, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Loads collision grid for an entity spawned outside of a player's loaded area. This allows peds to execute tasks rather than sit dormant because of a lack of a physics grid.
Certainly not the main usage of this native but when set to true for a Vehicle, it will prevent the vehicle to explode if it is spawned far away from the player.

---

### HAS_COLLISION_LOADED_AROUND_ENTITY

**Hash:** `0xE9676F61BC0B3321` | **JHash:** `0x851687F9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_COLLISION_LOADED_AROUND_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_COLLISION_LOADED_AROUND_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENTITY_MAX_SPEED

**Hash:** `0x0E46A3FCBDE2A1B1` | **JHash:** `0x46AFFED3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_MAX_SPEED(Entity entity, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_MAX_SPEED, entity, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_ONLY_DAMAGED_BY_PLAYER

**Hash:** `0x79F020FF9EDC0748` | **JHash:** `0x4B707F50` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ONLY_DAMAGED_BY_PLAYER(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ONLY_DAMAGED_BY_PLAYER, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_ONLY_DAMAGED_BY_RELATIONSHIP_GROUP

**Hash:** `0x7022BD828FA0B082` | **JHash:** `0x202237E2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ONLY_DAMAGED_BY_RELATIONSHIP_GROUP(Entity entity, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ONLY_DAMAGED_BY_RELATIONSHIP_GROUP, entity, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_PROOFS

**Hash:** `0xFAEE099C6F890BB8` | **JHash:** `0x7E9EAB66` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_PROOFS(Entity entity, BOOL bulletProof, BOOL fireProof, BOOL explosionProof, BOOL collisionProof, BOOL meleeProof, BOOL steamProof, BOOL dontResetOnCleanup, BOOL waterProof)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_PROOFS, entity, bulletProof, fireProof, explosionProof, collisionProof, meleeProof, steamProof, dontResetOnCleanup, waterProof);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `bulletProof` | `BOOL` | `bool` | |
| `fireProof` | `BOOL` | `bool` | |
| `explosionProof` | `BOOL` | `bool` | |
| `collisionProof` | `BOOL` | `bool` | |
| `meleeProof` | `BOOL` | `bool` | |
| `steamProof` | `BOOL` | `bool` | |
| `dontResetOnCleanup` | `BOOL` | `bool` | |
| `waterProof` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enable / disable each type of damage.

waterProof is damage related to water not drowning
--------------
p7 is to to '1' in am_mp_property_ext/int: ENTITY::SET_ENTITY_PROOFS(uParam0->f_19, true, true, true, true, true, true, 1, true);


---

### GET_ENTITY_PROOFS

**Hash:** `0xBE8CD9BE829BBEBF` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_ENTITY_PROOFS(Entity entity, BOOL* bulletProof, BOOL* fireProof, BOOL* explosionProof, BOOL* collisionProof, BOOL* meleeProof, BOOL* steamProof, BOOL* p7, BOOL* drownProof)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ENTITY_PROOFS, entity, bulletProof, fireProof, explosionProof, collisionProof, meleeProof, steamProof, p7, drownProof);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `bulletProof` | `BOOL*` | `bool*` | |
| `fireProof` | `BOOL*` | `bool*` | |
| `explosionProof` | `BOOL*` | `bool*` | |
| `collisionProof` | `BOOL*` | `bool*` | |
| `meleeProof` | `BOOL*` | `bool*` | |
| `steamProof` | `BOOL*` | `bool*` | |
| `p7` | `BOOL*` | `bool*` | |
| `drownProof` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENTITY_QUATERNION

**Hash:** `0x77B21BE7AC540F07` | **JHash:** `0x83B6046F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_QUATERNION(Entity entity, float x, float y, float z, float w)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_QUATERNION, entity, x, y, z, w);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `w` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** w is the correct parameter name!

---

### SET_ENTITY_RECORDS_COLLISIONS

**Hash:** `0x0A50A1EEDAD01E65` | **JHash:** `0x6B189A1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_RECORDS_COLLISIONS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_RECORDS_COLLISIONS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_ROTATION

**Hash:** `0x8524A8B0171D5E07` | **JHash:** `0x0A345EFE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ROTATION(Entity entity, float pitch, float roll, float yaw, int rotationOrder, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ROTATION, entity, pitch, roll, yaw, rotationOrder, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `pitch` | `float` | `float` | |
| `roll` | `float` | `float` | |
| `yaw` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** rotationOrder refers to the order yaw pitch roll is applied
value ranges from 0 to 5. What you use for rotationOrder when setting must be the same as rotationOrder when getting the rotation. 
Unsure what value corresponds to what rotation order, more testing will be needed for that.
For the most part R* uses 1 or 2 as the order.
p5 is usually set as true


---

### SET_ENTITY_VISIBLE

**Hash:** `0xEA1C610A04DB6BBB` | **JHash:** `0xD043E8E1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_VISIBLE(Entity entity, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_VISIBLE, entity, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p2 is always 0.

---

### SET_ENTITY_WATER_REFLECTION_FLAG

**Hash:** `0xC34BC448DA29F5E9` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_ENTITY_WATER_REFLECTION_FLAG(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_WATER_REFLECTION_FLAG, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_MIRROR_REFLECTION_FLAG

**Hash:** `0xE66377CDDADA4810` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_ENTITY_MIRROR_REFLECTION_FLAG(Entity entity, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_MIRROR_REFLECTION_FLAG, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_VELOCITY

**Hash:** `0x1C99BB7B6E96D16F` | **JHash:** `0xFF5A1988` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_VELOCITY(Entity entity, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_VELOCITY, entity, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Note that the third parameter(denoted as z) is "up and down" with positive numbers encouraging upwards movement.

---

### SET_ENTITY_ANGULAR_VELOCITY

**Hash:** `0x8339643499D1222E` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_ENTITY_ANGULAR_VELOCITY(Entity entity, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ANGULAR_VELOCITY, entity, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_HAS_GRAVITY

**Hash:** `0x4A4722448F18EEF5` | **JHash:** `0xE2F262BF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_HAS_GRAVITY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_HAS_GRAVITY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_LOD_DIST

**Hash:** `0x5927F96A78577363` | **JHash:** `0xD7ACC7AD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_LOD_DIST(Entity entity, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_LOD_DIST, entity, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** LOD distance can be 0 to 0xFFFF (higher values will result in 0xFFFF) as it is actually stored as a 16-bit value (aka uint16_t).

---

### GET_ENTITY_LOD_DIST

**Hash:** `0x4159C2762B5791D6` | **JHash:** `0x4DA3D51F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_LOD_DIST(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_LOD_DIST, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the LOD distance of an entity.

---

### SET_ENTITY_ALPHA

**Hash:** `0x44A0870B7E92D7C0` | **JHash:** `0xAE667CB0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ALPHA(Entity entity, int alphaLevel, BOOL skin)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ALPHA, entity, alphaLevel, skin);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `alphaLevel` | `int` | `int` | |
| `skin` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** skin - everything alpha except skin
Set entity alpha level. Ranging from 0 to 255 but chnages occur after every 20 percent (after every 51).

---

### GET_ENTITY_ALPHA

**Hash:** `0x5A47B3B5E63E94C6` | **JHash:** `0x1560B017` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ENTITY_ALPHA(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_ALPHA, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

---

### RESET_ENTITY_ALPHA

**Hash:** `0x9B1E824FFBB7027A` | **JHash:** `0x8A30761C` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_ENTITY_ALPHA(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_ENTITY_ALPHA, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### RESET_PICKUP_ENTITY_GLOW

**Hash:** `0x490861B88F4FD846` | **Build:** `944`

**C++ 原型:**
```cpp
void RESET_PICKUP_ENTITY_GLOW(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PICKUP_ENTITY_GLOW, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Similar to RESET_ENTITY_ALPHA

---

### SET_PICKUP_COLLIDES_WITH_PROJECTILES

**Hash:** `0xCEA7C8E1B48FF68C` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_PICKUP_COLLIDES_WITH_PROJECTILES(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_COLLIDES_WITH_PROJECTILES, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_SORT_BIAS

**Hash:** `0x5C3B791D580E0BC2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_SORT_BIAS(Entity entity, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_SORT_BIAS, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only called once in the scripts.

Related to weapon objects.


---

### SET_ENTITY_ALWAYS_PRERENDER

**Hash:** `0xACAD101E1FB66689` | **JHash:** `0xD8FF798A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ALWAYS_PRERENDER(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ALWAYS_PRERENDER, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_RENDER_SCORCHED

**Hash:** `0x730F5F8D3F0F2050` | **JHash:** `0xAAC9317B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_RENDER_SCORCHED(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_RENDER_SCORCHED, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_TRAFFICLIGHT_OVERRIDE

**Hash:** `0x57C5DB656185EAC4` | **JHash:** `0xC47F5B91` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_TRAFFICLIGHT_OVERRIDE(Entity entity, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_TRAFFICLIGHT_OVERRIDE, entity, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example here: www.gtaforums.com/topic/830463-help-with-turning-lights-green-and-causing-peds-to-crash-into-each-other/#entry1068211340

0 = green
1 = red
2 = yellow
3 = reset changes
changing lights may not change the behavior of vehicles

---

### SET_ENTITY_IS_IN_VEHICLE

**Hash:** `0x78E8E3A640178255` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_IS_IN_VEHICLE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_IS_IN_VEHICLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### CREATE_MODEL_SWAP

**Hash:** `0x92C47782FDA8B2A3` | **JHash:** `0x0BC12F9E` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MODEL_SWAP(float x, float y, float z, float radius, Hash originalModel, Hash newModel, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MODEL_SWAP, x, y, z, radius, originalModel, newModel, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `originalModel` | `Hash` | `uint` | |
| `newModel` | `Hash` | `uint` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only works with objects!

---

### REMOVE_MODEL_SWAP

**Hash:** `0x033C0F9A64E229AE` | **JHash:** `0xCE0AA8BC` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MODEL_SWAP(float x, float y, float z, float radius, Hash originalModel, Hash newModel, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MODEL_SWAP, x, y, z, radius, originalModel, newModel, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `originalModel` | `Hash` | `uint` | |
| `newModel` | `Hash` | `uint` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CREATE_MODEL_HIDE

**Hash:** `0x8A97BCA30A0CE478` | **JHash:** `0x7BD5CF2F` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MODEL_HIDE(float x, float y, float z, float radius, Hash modelHash, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MODEL_HIDE, x, y, z, radius, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p5 = sets as true in scripts
Same as the comment for CREATE_MODEL_SWAP unless for some reason p5 affects it this only works with objects as well.

Network players do not see changes done with this.

---

### CREATE_MODEL_HIDE_EXCLUDING_SCRIPT_OBJECTS

**Hash:** `0x3A52AE588830BF7F` | **JHash:** `0x07AAF22C` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MODEL_HIDE_EXCLUDING_SCRIPT_OBJECTS(float x, float y, float z, float radius, Hash modelHash, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MODEL_HIDE_EXCLUDING_SCRIPT_OBJECTS, x, y, z, radius, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_MODEL_HIDE

**Hash:** `0xD9E3006FB3CBD765` | **JHash:** `0x993DBC10` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MODEL_HIDE(float x, float y, float z, float radius, Hash modelHash, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MODEL_HIDE, x, y, z, radius, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native makes entities visible that are hidden by the native CREATE_MODEL_HIDE.
p5 should be false, true does nothing

---

### CREATE_FORCED_OBJECT

**Hash:** `0x150E808B375A385A` | **JHash:** `0x335190A2` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_FORCED_OBJECT(float x, float y, float z, Any p3, Hash modelHash, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_FORCED_OBJECT, x, y, z, p3, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `Any` | `object` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_FORCED_OBJECT

**Hash:** `0x61B6775E83C0DB6F` | **JHash:** `0xAED73ADD` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_FORCED_OBJECT(float x, float y, float z, float p3, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_FORCED_OBJECT, x, y, z, p3, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_NO_COLLISION_ENTITY

**Hash:** `0xA53ED5520C07654A` | **JHash:** `0x1E11BFE9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_NO_COLLISION_ENTITY(Entity entity1, Entity entity2, BOOL thisFrameOnly)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_NO_COLLISION_ENTITY, entity1, entity2, thisFrameOnly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |
| `thisFrameOnly` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Calling this function disables collision between two entities.
The importance of the order for entity1 and entity2 is unclear.
The third parameter, `thisFrame`, decides whether the collision is to be disabled until it is turned back on, or if it's just this frame.

---

### _SET_ENTITY_NO_COLLISION_WITH_NETWORKED_ENTITY

**Hash:** `0x0A27A7827347B3B1` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_ENTITY_NO_COLLISION_WITH_NETWORKED_ENTITY(Entity entity1, Entity entity2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_ENTITY_NO_COLLISION_WITH_NETWORKED_ENTITY, entity1, entity2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_MOTION_BLUR

**Hash:** `0x295D82A8559F9150` | **JHash:** `0xE90005B8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_MOTION_BLUR(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_MOTION_BLUR, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CAN_AUTO_VAULT_ON_ENTITY

**Hash:** `0xE12ABE5E3A389A6C` | **JHash:** `0x44767B31` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_AUTO_VAULT_ON_ENTITY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_AUTO_VAULT_ON_ENTITY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 always false.

---

### SET_CAN_CLIMB_ON_ENTITY

**Hash:** `0xA80AE305E0A3044F` | **JHash:** `0xE224A6A5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_CLIMB_ON_ENTITY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_CLIMB_ON_ENTITY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 always false.

---

### SET_WAIT_FOR_COLLISIONS_BEFORE_PROBE

**Hash:** `0xDC6F8601FAF2E893` | **JHash:** `0xA0466A69` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WAIT_FOR_COLLISIONS_BEFORE_PROBE(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WAIT_FOR_COLLISIONS_BEFORE_PROBE, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only called within 1 script for x360. 'fm_mission_controller' and it used on an object. 

Ran after these 2 natives,
set_object_targettable(uParam0, 0);
set_entity_invincible(uParam0, 1);

---

### SET_ENTITY_NOWEAPONDECALS

**Hash:** `0x2C2E3DC128F44309` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_NOWEAPONDECALS(Entity entity, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_NOWEAPONDECALS, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_USE_MAX_DISTANCE_FOR_WATER_REFLECTION

**Hash:** `0x1A092BB0C3808B96` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_USE_MAX_DISTANCE_FOR_WATER_REFLECTION(Entity entity, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_USE_MAX_DISTANCE_FOR_WATER_REFLECTION, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_ENTITY_BONE_ROTATION

**Hash:** `0xCE6294A232D03786` | **Build:** `791`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_BONE_ROTATION(Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_BONE_ROTATION, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the world rotation of the specified bone of the specified entity.

---

### GET_ENTITY_BONE_POSTION

**Hash:** `0x46F8696933A63C9B` | **Build:** `877`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_BONE_POSTION(Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_BONE_POSTION, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the world position of the specified bone of the specified entity.

---

### GET_ENTITY_BONE_OBJECT_ROTATION

**Hash:** `0xBD8D32550E5CEBFE` | **Build:** `1734`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_BONE_OBJECT_ROTATION(Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_BONE_OBJECT_ROTATION, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the local rotation of the specified bone of the specified entity.

---

### GET_ENTITY_BONE_OBJECT_POSTION

**Hash:** `0xCF1247CC86961FD6` | **Build:** `2802`

**C++ 原型:**
```cpp
Vector3 GET_ENTITY_BONE_OBJECT_POSTION(Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTITY_BONE_OBJECT_POSTION, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_ENTITY_BONE_COUNT

**Hash:** `0xB328DCC3A3AA401B` | **Build:** `791`

**C++ 原型:**
```cpp
int GET_ENTITY_BONE_COUNT(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_BONE_COUNT, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

---

### ENABLE_ENTITY_BULLET_COLLISION

**Hash:** `0x6CE177D014502E8A` | **Build:** `877`

**C++ 原型:**
```cpp
void ENABLE_ENTITY_BULLET_COLLISION(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_ENTITY_BULLET_COLLISION, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_ENTITY

**Hash:** `0xB17BC6453F6CF5AC` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_ENTITY(Entity entity1, Entity entity2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CAN_ONLY_BE_DAMAGED_BY_ENTITY, entity1, entity2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_CANT_CAUSE_COLLISION_DAMAGED_ENTITY

**Hash:** `0x68B562E124CC0AEF` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_ENTITY_CANT_CAUSE_COLLISION_DAMAGED_ENTITY(Entity entity1, Entity entity2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_CANT_CAUSE_COLLISION_DAMAGED_ENTITY, entity1, entity2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity1` | `Entity` | `int` | |
| `entity2` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_MIGRATE_TO_SPECTATOR

**Hash:** `0x36F32DE87082343E` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_ALLOW_MIGRATE_TO_SPECTATOR(Entity entity, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_MIGRATE_TO_SPECTATOR, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 is always set to 1

---

### GET_ENTITY_OF_TYPE_ATTACHED_TO_ENTITY

**Hash:** `0x1F922734E259BD26` | **Build:** `1180`

**C++ 原型:**
```cpp
Entity GET_ENTITY_OF_TYPE_ATTACHED_TO_ENTITY(Entity entity, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_OF_TYPE_ATTACHED_TO_ENTITY, entity, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Entity` → `int`

**说明:** Gets the handle of an entity with a specific model hash attached to another entity, such as an object attached to a ped.
 This native does not appear to have anything to do with pickups as in scripts it is used with objects.

Example from fm_mission_controller_2020.c:

iVar8 = ENTITY::GET_ENTITY_OF_TYPE_ATTACHED_TO_ENTITY(bParam0->f_9, joaat("p_cs_clipboard"));

---

### SET_PICK_UP_BY_CARGOBOB_DISABLED

**Hash:** `0xD7B80E7C3BEFC396` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_PICK_UP_BY_CARGOBOB_DISABLED(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICK_UP_BY_CARGOBOB_DISABLED, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---


---

## EVENT

**包含 13 个 Native 函数**

### SET_DECISION_MAKER

**Hash:** `0xB604A2942ADED0EE` | **JHash:** `0x19CEAC9E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DECISION_MAKER(Ped ped, Hash name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DECISION_MAKER, ped, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### CLEAR_DECISION_MAKER_EVENT_RESPONSE

**Hash:** `0x4FC9381A7AEE8968` | **JHash:** `0x07ABD94D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_DECISION_MAKER_EVENT_RESPONSE(Hash name, int eventType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_DECISION_MAKER_EVENT_RESPONSE, name, eventType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `Hash` | `uint` | |
| `eventType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### BLOCK_DECISION_MAKER_EVENT

**Hash:** `0xE42FCDFD0E4196F7` | **JHash:** `0x57506EA6` | **Build:** `323`

**C++ 原型:**
```cpp
void BLOCK_DECISION_MAKER_EVENT(Hash name, int eventType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_DECISION_MAKER_EVENT, name, eventType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `Hash` | `uint` | |
| `eventType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

This is limited to 4 blocked events at a time.

---

### UNBLOCK_DECISION_MAKER_EVENT

**Hash:** `0xD7CD9CF34F2C99E8` | **JHash:** `0x62A3161D` | **Build:** `323`

**C++ 原型:**
```cpp
void UNBLOCK_DECISION_MAKER_EVENT(Hash name, int eventType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNBLOCK_DECISION_MAKER_EVENT, name, eventType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `Hash` | `uint` | |
| `eventType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### ADD_SHOCKING_EVENT_AT_POSITION

**Hash:** `0xD9F8455409B525E9` | **JHash:** `0x0B30F779` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_SHOCKING_EVENT_AT_POSITION(int eventType, float x, float y, float z, float duration)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_SHOCKING_EVENT_AT_POSITION, eventType, x, y, z, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### ADD_SHOCKING_EVENT_FOR_ENTITY

**Hash:** `0x7FD8F3BE76F89422` | **JHash:** `0xA81B5B71` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_SHOCKING_EVENT_FOR_ENTITY(int eventType, Entity entity, float duration)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_SHOCKING_EVENT_FOR_ENTITY, eventType, entity, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventType` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `duration` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### IS_SHOCKING_EVENT_IN_SPHERE

**Hash:** `0x1374ABB7C15BAB92` | **JHash:** `0x2F98823E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SHOCKING_EVENT_IN_SPHERE(int eventType, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SHOCKING_EVENT_IN_SPHERE, eventType, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### REMOVE_SHOCKING_EVENT

**Hash:** `0x2CDA538C44C6CCE5` | **JHash:** `0xF82D5A87` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REMOVE_SHOCKING_EVENT(ScrHandle event)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REMOVE_SHOCKING_EVENT, event);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `event` | `ScrHandle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_ALL_SHOCKING_EVENTS

**Hash:** `0xEAABE8FDFA21274C` | **JHash:** `0x64DF6282` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_SHOCKING_EVENTS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_SHOCKING_EVENTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_SHOCKING_EVENT_SPAWN_BLOCKING_AREAS

**Hash:** `0x340F1415B68AEADE` | **JHash:** `0xA0CE89C8` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_SHOCKING_EVENT_SPAWN_BLOCKING_AREAS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SHOCKING_EVENT_SPAWN_BLOCKING_AREAS);
```

**返回值:** `void` → `void`

---

### SUPPRESS_SHOCKING_EVENTS_NEXT_FRAME

**Hash:** `0x2F9A292AD0A3BD89` | **JHash:** `0x4CC674B5` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_SHOCKING_EVENTS_NEXT_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_SHOCKING_EVENTS_NEXT_FRAME);
```

**返回值:** `void` → `void`

---

### SUPPRESS_SHOCKING_EVENT_TYPE_NEXT_FRAME

**Hash:** `0x3FD2EC8BF1F1CF30` | **JHash:** `0xA0FDCB82` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_SHOCKING_EVENT_TYPE_NEXT_FRAME(int eventType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_SHOCKING_EVENT_TYPE_NEXT_FRAME, eventType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** eventType: https://alloc8or.re/gta5/doc/enums/eEventType.txt

---

### SUPPRESS_AGITATION_EVENTS_NEXT_FRAME

**Hash:** `0x5F3B7749C112D552` | **JHash:** `0x80340396` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_AGITATION_EVENTS_NEXT_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_AGITATION_EVENTS_NEXT_FRAME);
```

**返回值:** `void` → `void`

---


---

## EXTRAMETADATA

**包含 49 个 Native 函数**

### GET_NUM_TATTOO_SHOP_DLC_ITEMS

**Hash:** `0x278F76C3B0A8F109` | **JHash:** `0x71D0CF3E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_TATTOO_SHOP_DLC_ITEMS(int character)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_TATTOO_SHOP_DLC_ITEMS, character);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `character` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Character types:
0 = Michael, 
1 = Franklin, 
2 = Trevor, 
3 = MPMale, 
4 = MPFemale

---

### GET_TATTOO_SHOP_DLC_ITEM_DATA

**Hash:** `0xFF56381874F82086` | **JHash:** `0x2E9D628C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TATTOO_SHOP_DLC_ITEM_DATA(int characterType, int decorationIndex, Any* outComponent)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TATTOO_SHOP_DLC_ITEM_DATA, characterType, decorationIndex, outComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterType` | `int` | `int` | |
| `decorationIndex` | `int` | `int` | |
| `outComponent` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** Character types:
0 = Michael, 
1 = Franklin, 
2 = Trevor, 
3 = MPMale, 
4 = MPFemale


enum TattooZoneData
{  
    ZONE_TORSO = 0,  
    ZONE_HEAD = 1,  
    ZONE_LEFT_ARM = 2,  
    ZONE_RIGHT_ARM = 3,  
    ZONE_LEFT_LEG = 4,  
    ZONE_RIGHT_LEG = 5,  
    ZONE_UNKNOWN = 6,
    ZONE_NONE = 7,  
};
struct outComponent
{
    // these vars are suffixed with 4 bytes of padding each.
    uint unk;
    int unk2;
    uint tattooCollectionHash;
    uint tattooNameHash;
    int unk3;
    TattooZoneData zoneId;
    uint unk4;
    uint unk5;
    // maybe more, not sure exactly, decompiled scripts are very vague around this part.
}

---

### GET_TATTOO_SHOP_DLC_ITEM_INDEX

**Hash:** `0x10144267DD22866C` | **Build:** `2189`

**C++ 原型:**
```cpp
int GET_TATTOO_SHOP_DLC_ITEM_INDEX(Hash overlayHash, Any p1, int character)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TATTOO_SHOP_DLC_ITEM_INDEX, overlayHash, p1, character);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `overlayHash` | `Hash` | `uint` | |
| `p1` | `Any` | `object` | |
| `character` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns some sort of index/offset for overlays/decorations.

Character types:
0 = Michael, 
1 = Franklin, 
2 = Trevor, 
3 = MPMale, 
4 = MPFemale

---

### INIT_SHOP_PED_COMPONENT

**Hash:** `0x1E8C308FD312C036` | **JHash:** `0xB818C7FC` | **Build:** `323`

**C++ 原型:**
```cpp
void INIT_SHOP_PED_COMPONENT(Any* outComponent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INIT_SHOP_PED_COMPONENT, outComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outComponent` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### INIT_SHOP_PED_PROP

**Hash:** `0xEB0A2B758F7B850F` | **JHash:** `0xF5659E50` | **Build:** `323`

**C++ 原型:**
```cpp
void INIT_SHOP_PED_PROP(Any* outProp)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INIT_SHOP_PED_PROP, outProp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outProp` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### SETUP_SHOP_PED_APPAREL_QUERY

**Hash:** `0x50F457823CE6EB5F` | **JHash:** `0xC937FF3D` | **Build:** `323`

**C++ 原型:**
```cpp
int SETUP_SHOP_PED_APPAREL_QUERY(int p0, int p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SETUP_SHOP_PED_APPAREL_QUERY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SETUP_SHOP_PED_APPAREL_QUERY_TU

**Hash:** `0x9BDF59818B1E38C1` | **JHash:** `0x594E862C` | **Build:** `323`

**C++ 原型:**
```cpp
int SETUP_SHOP_PED_APPAREL_QUERY_TU(int character, int p1, int p2, BOOL p3, int p4, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SETUP_SHOP_PED_APPAREL_QUERY_TU, character, p1, p2, p3, p4, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `character` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `int` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** character is 0 for Michael, 1 for Franklin, 2 for Trevor, 3 for freemode male, and 4 for freemode female.

componentId is between 0 and 11 and corresponds to the usual component slots.

p1 could be the outfit number; unsure.

p2 is usually -1; unknown function.

p3 appears to be for selecting between clothes and props; false is used with components/clothes, true is used with props.

p4 is usually -1; unknown function.

componentId is -1 when p3 is true in decompiled scripts.

---

### GET_SHOP_PED_QUERY_COMPONENT

**Hash:** `0x249E310B2D920699` | **JHash:** `0xC0718904` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_QUERY_COMPONENT(int componentId, Any* outComponent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_QUERY_COMPONENT, componentId, outComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentId` | `int` | `int` | |
| `outComponent` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** See https://git.io/JtcRf for example and structs.

---

### GET_SHOP_PED_QUERY_COMPONENT_INDEX

**Hash:** `0x96E2929292A4DB77` | **Build:** `2189`

**C++ 原型:**
```cpp
int GET_SHOP_PED_QUERY_COMPONENT_INDEX(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_QUERY_COMPONENT_INDEX, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns some sort of index/offset for components.
Needs _GET_NUM_PROPS_FROM_OUTFIT to be called with p3 = false and componentId with the drawable's component slot first, returns -1 otherwise.

---

### GET_SHOP_PED_COMPONENT

**Hash:** `0x74C0E2A57EC66760` | **JHash:** `0xB39677C5` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_COMPONENT(Hash componentHash, Any* outComponent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_COMPONENT, componentHash, outComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `outComponent` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** More info here: https://gist.github.com/root-cause/3b80234367b0c856d60bf5cb4b826f86

---

### GET_SHOP_PED_QUERY_PROP

**Hash:** `0xDE44A00999B2837D` | **JHash:** `0x1D3C1466` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_QUERY_PROP(int componentId, Any* outProp)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_QUERY_PROP, componentId, outProp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentId` | `int` | `int` | |
| `outProp` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** See https://git.io/JtcRf for example and structs.

---

### GET_SHOP_PED_QUERY_PROP_INDEX

**Hash:** `0x6CEBE002E58DEE97` | **Build:** `2189`

**C++ 原型:**
```cpp
int GET_SHOP_PED_QUERY_PROP_INDEX(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_QUERY_PROP_INDEX, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns some sort of index/offset for props.
Needs _GET_NUM_PROPS_FROM_OUTFIT to be called with p3 = true and componentId = -1 first, returns -1 otherwise.

---

### GET_SHOP_PED_PROP

**Hash:** `0x5D5CAFF661DDF6FC` | **JHash:** `0xDB0A7A58` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_PROP(Hash componentHash, Any* outProp)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_PROP, componentHash, outProp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `outProp` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** More info here: https://gist.github.com/root-cause/3b80234367b0c856d60bf5cb4b826f86

---

### GET_HASH_NAME_FOR_COMPONENT

**Hash:** `0x0368B3A838070348` | **JHash:** `0xC8A4BF12` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_HASH_NAME_FOR_COMPONENT(Entity entity, int componentId, int drawableVariant, int textureVariant)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_HASH_NAME_FOR_COMPONENT, entity, componentId, drawableVariant, textureVariant);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableVariant` | `int` | `int` | |
| `textureVariant` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_HASH_NAME_FOR_PROP

**Hash:** `0x5D6160275CAEC8DD` | **JHash:** `0x7D876DC0` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_HASH_NAME_FOR_PROP(Entity entity, int componentId, int propIndex, int propTextureIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_HASH_NAME_FOR_PROP, entity, componentId, propIndex, propTextureIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `componentId` | `int` | `int` | |
| `propIndex` | `int` | `int` | |
| `propTextureIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_SHOP_PED_APPAREL_VARIANT_COMPONENT_COUNT

**Hash:** `0xC17AD0E5752BECDA` | **JHash:** `0x159751B4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHOP_PED_APPAREL_VARIANT_COMPONENT_COUNT(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_APPAREL_VARIANT_COMPONENT_COUNT, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### GET_SHOP_PED_APPAREL_VARIANT_PROP_COUNT

**Hash:** `0xD40AAC51E8E4C663` | **Build:** `791`

**C++ 原型:**
```cpp
int GET_SHOP_PED_APPAREL_VARIANT_PROP_COUNT(Hash propHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_APPAREL_VARIANT_PROP_COUNT, propHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `propHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** `propHash`: Ped helmet prop hash?
This native returns 1 when the player helmet has a visor (there is another prop index for the same helmet with closed/opened visor variant) that can be toggled. 0 if there's no alternative version with a visor for this helmet prop.

---

### GET_VARIANT_COMPONENT

**Hash:** `0x6E11F282F11863B6` | **JHash:** `0xE4FF7103` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VARIANT_COMPONENT(Hash componentHash, int variantComponentIndex, Hash* nameHash, int* enumValue, int* componentType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VARIANT_COMPONENT, componentHash, variantComponentIndex, nameHash, enumValue, componentType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `variantComponentIndex` | `int` | `int` | |
| `nameHash` | `Hash*` | `uint*` | |
| `enumValue` | `int*` | `int*` | |
| `componentType` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_VARIANT_PROP

**Hash:** `0xD81B7F27BC773E66` | **Build:** `791`

**C++ 原型:**
```cpp
void GET_VARIANT_PROP(Hash componentHash, int variantPropIndex, Hash* nameHash, int* enumValue, int* anchorPoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VARIANT_PROP, componentHash, variantPropIndex, nameHash, enumValue, anchorPoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `variantPropIndex` | `int` | `int` | |
| `nameHash` | `Hash*` | `uint*` | |
| `enumValue` | `int*` | `int*` | |
| `anchorPoint` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_SHOP_PED_APPAREL_FORCED_COMPONENT_COUNT

**Hash:** `0xC6B9DB42C04DD8C3` | **JHash:** `0xCE70F183` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHOP_PED_APPAREL_FORCED_COMPONENT_COUNT(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_APPAREL_FORCED_COMPONENT_COUNT, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns number of possible values of the forcedComponentIndex argument of GET_FORCED_COMPONENT.

---

### GET_SHOP_PED_APPAREL_FORCED_PROP_COUNT

**Hash:** `0x017568A8182D98A6` | **JHash:** `0xC560D7C0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHOP_PED_APPAREL_FORCED_PROP_COUNT(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_APPAREL_FORCED_PROP_COUNT, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns number of possible values of the forcedPropIndex argument of GET_FORCED_PROP.

---

### GET_FORCED_COMPONENT

**Hash:** `0x6C93ED8C2F74859B` | **JHash:** `0x382C70BE` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_FORCED_COMPONENT(Hash componentHash, int forcedComponentIndex, Hash* nameHash, int* enumValue, int* componentType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_FORCED_COMPONENT, componentHash, forcedComponentIndex, nameHash, enumValue, componentType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `forcedComponentIndex` | `int` | `int` | |
| `nameHash` | `Hash*` | `uint*` | |
| `enumValue` | `int*` | `int*` | |
| `componentType` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_FORCED_PROP

**Hash:** `0xE1CA84EBF72E691D` | **JHash:** `0x22DAE257` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_FORCED_PROP(Hash componentHash, int forcedPropIndex, Hash* nameHash, int* enumValue, int* anchorPoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_FORCED_PROP, componentHash, forcedPropIndex, nameHash, enumValue, anchorPoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `forcedPropIndex` | `int` | `int` | |
| `nameHash` | `Hash*` | `uint*` | |
| `enumValue` | `int*` | `int*` | |
| `anchorPoint` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### DOES_SHOP_PED_APPAREL_HAVE_RESTRICTION_TAG

**Hash:** `0x341DE7ED1D2A1BFD` | **JHash:** `0x8E2C7FD5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SHOP_PED_APPAREL_HAVE_RESTRICTION_TAG(Hash componentHash, Hash restrictionTagHash, int componentId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SHOP_PED_APPAREL_HAVE_RESTRICTION_TAG, componentHash, restrictionTagHash, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `restrictionTagHash` | `Hash` | `uint` | |
| `componentId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of restriction tags by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedApparelRestrictionTags.json

componentId/last parameter seems to be unused.

---

### DOES_CURRENT_PED_COMPONENT_HAVE_RESTRICTION_TAG

**Hash:** `0x7796B21B76221BC5` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL DOES_CURRENT_PED_COMPONENT_HAVE_RESTRICTION_TAG(Ped ped, int componentId, Hash restrictionTagHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CURRENT_PED_COMPONENT_HAVE_RESTRICTION_TAG, ped, componentId, restrictionTagHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `restrictionTagHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### DOES_CURRENT_PED_PROP_HAVE_RESTRICTION_TAG

**Hash:** `0xD726BAB4554DA580` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL DOES_CURRENT_PED_PROP_HAVE_RESTRICTION_TAG(Ped ped, int componentId, Hash restrictionTagHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CURRENT_PED_PROP_HAVE_RESTRICTION_TAG, ped, componentId, restrictionTagHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `restrictionTagHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### SETUP_SHOP_PED_OUTFIT_QUERY

**Hash:** `0xF3FBE2D50A6A8C28` | **JHash:** `0x1ECD23E7` | **Build:** `323`

**C++ 原型:**
```cpp
int SETUP_SHOP_PED_OUTFIT_QUERY(int character, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SETUP_SHOP_PED_OUTFIT_QUERY, character, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `character` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** characters

0: Michael
1: Franklin
2: Trevor
3: MPMale
4: MPFemale

---

### GET_SHOP_PED_QUERY_OUTFIT

**Hash:** `0x6D793F03A631FE56` | **JHash:** `0x2F8013A1` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_QUERY_OUTFIT(int outfitIndex, Any* outfit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_QUERY_OUTFIT, outfitIndex, outfit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outfitIndex` | `int` | `int` | |
| `outfit` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** outfitIndex: from 0 to SETUP_SHOP_PED_OUTFIT_QUERY(characterIndex, false) - 1.
See https://git.io/JtcB8 for example and outfit struct.

---

### GET_SHOP_PED_OUTFIT

**Hash:** `0xB7952076E444979D` | **JHash:** `0xCAFE9209` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SHOP_PED_OUTFIT(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SHOP_PED_OUTFIT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### GET_SHOP_PED_OUTFIT_LOCATE

**Hash:** `0x073CA26B079F956E` | **JHash:** `0x2798F56F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHOP_PED_OUTFIT_LOCATE(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHOP_PED_OUTFIT_LOCATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### GET_SHOP_PED_OUTFIT_PROP_VARIANT

**Hash:** `0xA9F9C2E0FDE11CBB` | **JHash:** `0x6641A864` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_SHOP_PED_OUTFIT_PROP_VARIANT(Hash outfitHash, int variantIndex, Any* outPropVariant)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SHOP_PED_OUTFIT_PROP_VARIANT, outfitHash, variantIndex, outPropVariant);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outfitHash` | `Hash` | `uint` | |
| `variantIndex` | `int` | `int` | |
| `outPropVariant` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** See https://git.io/JtcBH for example and structs.

---

### GET_SHOP_PED_OUTFIT_COMPONENT_VARIANT

**Hash:** `0x19F2A026EDF0013F` | **JHash:** `0x818534AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_SHOP_PED_OUTFIT_COMPONENT_VARIANT(Hash outfitHash, int variantIndex, Any* outComponentVariant)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SHOP_PED_OUTFIT_COMPONENT_VARIANT, outfitHash, variantIndex, outComponentVariant);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outfitHash` | `Hash` | `uint` | |
| `variantIndex` | `int` | `int` | |
| `outComponentVariant` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** See https://git.io/JtcBH for example and structs.

---

### GET_NUM_DLC_VEHICLES

**Hash:** `0xA7A866D21CD2329B` | **JHash:** `0x8EAF9CF6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_DLC_VEHICLES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_DLC_VEHICLES);
```

**返回值:** `int` → `int`

---

### GET_DLC_VEHICLE_MODEL

**Hash:** `0xECC01B7C5763333C` | **JHash:** `0xA2201E09` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_DLC_VEHICLE_MODEL(int dlcVehicleIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_DLC_VEHICLE_MODEL, dlcVehicleIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcVehicleIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** dlcVehicleIndex is 0 to GET_NUM_DLC_VEHICLS() - 1

---

### GET_DLC_VEHICLE_DATA

**Hash:** `0x33468EDC08E371F6` | **JHash:** `0xCF428FA4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_DLC_VEHICLE_DATA(int dlcVehicleIndex, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DLC_VEHICLE_DATA, dlcVehicleIndex, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcVehicleIndex` | `int` | `int` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** dlcVehicleIndex takes a number from 0 - GET_NUM_DLC_VEHICLES() - 1.
outData is a struct of 3 8-byte items.
The Second item in the struct *(Hash *)(outData + 1) is the vehicle hash.

---

### GET_DLC_VEHICLE_FLAGS

**Hash:** `0x5549EE11FA22FCF2` | **JHash:** `0xAB12738C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_DLC_VEHICLE_FLAGS(int dlcVehicleIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DLC_VEHICLE_FLAGS, dlcVehicleIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcVehicleIndex` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_NUM_DLC_WEAPONS

**Hash:** `0xEE47635F352DA367` | **JHash:** `0x2B757E6C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_DLC_WEAPONS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_DLC_WEAPONS);
```

**返回值:** `int` → `int`

**说明:** Returns the total number of DLC weapons.

---

### GET_NUM_DLC_WEAPONS_SP

**Hash:** `0x4160B65AE085B5A9` | **Build:** `2060`

**C++ 原型:**
```cpp
int GET_NUM_DLC_WEAPONS_SP(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_DLC_WEAPONS_SP);
```

**返回值:** `int` → `int`

**说明:** Returns the total number of DLC weapons that are available in SP (availableInSP field in shop_weapon.meta).

---

### GET_DLC_WEAPON_DATA

**Hash:** `0x79923CD21BECE14E` | **JHash:** `0xD88EC8EA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_DLC_WEAPON_DATA(int dlcWeaponIndex, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DLC_WEAPON_DATA, dlcWeaponIndex, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** 
dlcWeaponIndex takes a number from 0 - GET_NUM_DLC_WEAPONS() - 1.
struct DlcWeaponData
{
int emptyCheck; //use DLC1::IS_CONTENT_ITEM_LOCKED on this
int padding1;
int weaponHash;
int padding2;
int unk;
int padding3;
int weaponCost;
int padding4;
int ammoCost;
int padding5;
int ammoType;
int padding6;
int defaultClipSize;
int padding7;
char nameLabel[64];
char descLabel[64];
char desc2Label[64]; // usually "the" + name
char upperCaseNameLabel[64];
};

---

### GET_DLC_WEAPON_DATA_SP

**Hash:** `0x310836EE7129BA33` | **Build:** `2060`

**C++ 原型:**
```cpp
BOOL GET_DLC_WEAPON_DATA_SP(int dlcWeaponIndex, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DLC_WEAPON_DATA_SP, dlcWeaponIndex, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** Same as GET_DLC_WEAPON_DATA but only works for DLC weapons that are available in SP.

---

### GET_NUM_DLC_WEAPON_COMPONENTS

**Hash:** `0x405425358A7D61FE` | **JHash:** `0x476B23A9` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_DLC_WEAPON_COMPONENTS(int dlcWeaponIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_DLC_WEAPON_COMPONENTS, dlcWeaponIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the total number of DLC weapon components.

---

### GET_NUM_DLC_WEAPON_COMPONENTS_SP

**Hash:** `0xAD2A7A6DFF55841B` | **Build:** `2060`

**C++ 原型:**
```cpp
int GET_NUM_DLC_WEAPON_COMPONENTS_SP(int dlcWeaponIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_DLC_WEAPON_COMPONENTS_SP, dlcWeaponIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the total number of DLC weapon components that are available in SP.

---

### GET_DLC_WEAPON_COMPONENT_DATA

**Hash:** `0x6CF598A2957C2BF8` | **JHash:** `0x4B83FCAF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_DLC_WEAPON_COMPONENT_DATA(int dlcWeaponIndex, int dlcWeapCompIndex, Any* ComponentDataPtr)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DLC_WEAPON_COMPONENT_DATA, dlcWeaponIndex, dlcWeapCompIndex, ComponentDataPtr);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |
| `dlcWeapCompIndex` | `int` | `int` | |
| `ComponentDataPtr` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** p0 seems to be the weapon index
p1 seems to be the weapon component index
struct DlcComponentData{
int attachBone;
int padding1;
int bActiveByDefault;
int padding2;
int unk;
int padding3;
int componentHash;
int padding4;
int unk2;
int padding5;
int componentCost;
int padding6;
char nameLabel[64];
char descLabel[64];
};


---

### GET_DLC_WEAPON_COMPONENT_DATA_SP

**Hash:** `0x31D5E073B6F93CDC` | **Build:** `2060`

**C++ 原型:**
```cpp
BOOL GET_DLC_WEAPON_COMPONENT_DATA_SP(int dlcWeaponIndex, int dlcWeapCompIndex, Any* ComponentDataPtr)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DLC_WEAPON_COMPONENT_DATA_SP, dlcWeaponIndex, dlcWeapCompIndex, ComponentDataPtr);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dlcWeaponIndex` | `int` | `int` | |
| `dlcWeapCompIndex` | `int` | `int` | |
| `ComponentDataPtr` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** Same as GET_DLC_WEAPON_COMPONENT_DATA but only works for DLC components that are available in SP.

---

### IS_CONTENT_ITEM_LOCKED

**Hash:** `0xD4D7B033C3AA243C` | **JHash:** `0x06396058` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTENT_ITEM_LOCKED(Hash itemHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTENT_ITEM_LOCKED, itemHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_DLC_VEHICLE_MOD

**Hash:** `0x0564B9FF9631B82C` | **JHash:** `0x35BCA844` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DLC_VEHICLE_MOD(Hash hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DLC_VEHICLE_MOD, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### GET_DLC_VEHICLE_MOD_LOCK_HASH

**Hash:** `0xC098810437312FFF` | **JHash:** `0x59352658` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_DLC_VEHICLE_MOD_LOCK_HASH(Hash hash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_DLC_VEHICLE_MOD_LOCK_HASH, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

---

### EXECUTE_CONTENT_CHANGESET_GROUP_FOR_ALL

**Hash:** `0x6BEDF5769AC2DC07` | **Build:** `1604`

**C++ 原型:**
```cpp
void EXECUTE_CONTENT_CHANGESET_GROUP_FOR_ALL(Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXECUTE_CONTENT_CHANGESET_GROUP_FOR_ALL, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** From fm_deathmatch_creator and fm_race_creator:

FILES::REVERT_CONTENT_CHANGESET_GROUP_FOR_ALL(joaat("GROUP_MAP_SP"));
FILES::EXECUTE_CONTENT_CHANGESET_GROUP_FOR_ALL(joaat("GROUP_MAP"));

---

### REVERT_CONTENT_CHANGESET_GROUP_FOR_ALL

**Hash:** `0x3C1978285B036B25` | **Build:** `1604`

**C++ 原型:**
```cpp
void REVERT_CONTENT_CHANGESET_GROUP_FOR_ALL(Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REVERT_CONTENT_CHANGESET_GROUP_FOR_ALL, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** From fm_deathmatch_creator and fm_race_creator:

FILES::REVERT_CONTENT_CHANGESET_GROUP_FOR_ALL(joaat("GROUP_MAP_SP"));
FILES::EXECUTE_CONTENT_CHANGESET_GROUP_FOR_ALL(joaat("GROUP_MAP"));

---


---

## FIRE

**包含 21 个 Native 函数**

### START_SCRIPT_FIRE

**Hash:** `0x6B83617E04503888` | **JHash:** `0xE7529357` | **Build:** `323`

**C++ 原型:**
```cpp
FireId START_SCRIPT_FIRE(float X, float Y, float Z, int maxChildren, BOOL isGasFire)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SCRIPT_FIRE, X, Y, Z, maxChildren, isGasFire);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `X` | `float` | `float` | |
| `Y` | `float` | `float` | |
| `Z` | `float` | `float` | |
| `maxChildren` | `int` | `int` | |
| `isGasFire` | `BOOL` | `bool` | |

**返回值:** `FireId` → `int`

**说明:** Starts a fire:

xyz: Location of fire
maxChildren: The max amount of times a fire can spread to other objects. Must be 25 or less, or the function will do nothing.
isGasFire: Whether or not the fire is powered by gasoline.

---

### REMOVE_SCRIPT_FIRE

**Hash:** `0x7FF548385680673F` | **JHash:** `0x6B21FE26` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_SCRIPT_FIRE(FireId fireHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SCRIPT_FIRE, fireHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fireHandle` | `FireId` | `int` | |

**返回值:** `void` → `void`

---

### START_ENTITY_FIRE

**Hash:** `0xF6A9D9708F6F23DF` | **JHash:** `0x8928428E` | **Build:** `323`

**C++ 原型:**
```cpp
FireId START_ENTITY_FIRE(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_ENTITY_FIRE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `FireId` → `int`

---

### STOP_ENTITY_FIRE

**Hash:** `0x7F0DD2EBBB651AFF` | **JHash:** `0xCE8C9066` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ENTITY_FIRE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ENTITY_FIRE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### IS_ENTITY_ON_FIRE

**Hash:** `0x28D3FED7190D3A0B` | **JHash:** `0x8C73E64F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ON_FIRE(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ON_FIRE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_NUMBER_OF_FIRES_IN_RANGE

**Hash:** `0x50CAD495A460B305` | **JHash:** `0x654D93B7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_FIRES_IN_RANGE(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_FIRES_IN_RANGE, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `int` → `int`

---

### SET_FLAMMABILITY_MULTIPLIER

**Hash:** `0x8F390AC4155099BA` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_FLAMMABILITY_MULTIPLIER(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLAMMABILITY_MULTIPLIER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_FIRE_IN_RANGE

**Hash:** `0x056A8A219B8E829F` | **JHash:** `0x725C7205` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_FIRE_IN_RANGE(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_FIRE_IN_RANGE, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CLOSEST_FIRE_POS

**Hash:** `0x352A9F6BCF90081F` | **JHash:** `0xC4977B47` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_FIRE_POS(Vector3* outPosition, float x, float y, float z)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_FIRE_POS, outPosition, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outPosition` | `Vector3*` | `Vector3*` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns TRUE if it found something. FALSE if not.

---

### ADD_EXPLOSION

**Hash:** `0xE3AD2BDBAEE269AC` | **JHash:** `0x10AF5258` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_EXPLOSION(float x, float y, float z, int explosionType, float damageScale, BOOL isAudible, BOOL isInvisible, float cameraShake, BOOL noDamage)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_EXPLOSION, x, y, z, explosionType, damageScale, isAudible, isInvisible, cameraShake, noDamage);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `explosionType` | `int` | `int` | |
| `damageScale` | `float` | `float` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `cameraShake` | `float` | `float` | |
| `noDamage` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** BOOL isAudible = If explosion makes a sound.
BOOL isInvisible = If the explosion is invisible or not.

explosionType: https://alloc8or.re/gta5/doc/enums/eExplosionTag.txt

---

### ADD_OWNED_EXPLOSION

**Hash:** `0x172AA1B624FA1013` | **JHash:** `0x27EE0D67` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_OWNED_EXPLOSION(Ped ped, float x, float y, float z, int explosionType, float damageScale, BOOL isAudible, BOOL isInvisible, float cameraShake)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_OWNED_EXPLOSION, ped, x, y, z, explosionType, damageScale, isAudible, isInvisible, cameraShake);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `explosionType` | `int` | `int` | |
| `damageScale` | `float` | `float` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `cameraShake` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** isAudible: If explosion makes a sound.
isInvisible: If the explosion is invisible or not.
explosionType: See ADD_EXPLOSION.

---

### ADD_EXPLOSION_WITH_USER_VFX

**Hash:** `0x36DD3FE58B5E5212` | **JHash:** `0xCF358946` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_EXPLOSION_WITH_USER_VFX(float x, float y, float z, int explosionType, Hash explosionFx, float damageScale, BOOL isAudible, BOOL isInvisible, float cameraShake)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_EXPLOSION_WITH_USER_VFX, x, y, z, explosionType, explosionFx, damageScale, isAudible, isInvisible, cameraShake);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `explosionType` | `int` | `int` | |
| `explosionFx` | `Hash` | `uint` | |
| `damageScale` | `float` | `float` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `cameraShake` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** isAudible: If explosion makes a sound.
isInvisible: If the explosion is invisible or not.
explosionType: See ADD_EXPLOSION.

---

### IS_EXPLOSION_IN_AREA

**Hash:** `0x2E2EBA0EE7CED0E0` | **JHash:** `0xFB40075B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_EXPLOSION_IN_AREA(int explosionType, float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_EXPLOSION_IN_AREA, explosionType, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** explosionType: See ADD_EXPLOSION.

---

### IS_EXPLOSION_ACTIVE_IN_AREA

**Hash:** `0x6070104B699B2EF4` | **JHash:** `0x37C388DB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_EXPLOSION_ACTIVE_IN_AREA(int explosionType, float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_EXPLOSION_ACTIVE_IN_AREA, explosionType, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** explosionType: See ADD_EXPLOSION.

---

### IS_EXPLOSION_IN_SPHERE

**Hash:** `0xAB0F816885B0E483` | **JHash:** `0xD455A7F3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_EXPLOSION_IN_SPHERE(int explosionType, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_EXPLOSION_IN_SPHERE, explosionType, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** explosionType: See ADD_EXPLOSION.

---

### GET_OWNER_OF_EXPLOSION_IN_SPHERE

**Hash:** `0xB3CD51E3DB86F176` | **Build:** `1290`

**C++ 原型:**
```cpp
Entity GET_OWNER_OF_EXPLOSION_IN_SPHERE(int explosionType, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_OWNER_OF_EXPLOSION_IN_SPHERE, explosionType, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `Entity` → `int`

**说明:** explosionType: See ADD_EXPLOSION.

---

### IS_EXPLOSION_IN_ANGLED_AREA

**Hash:** `0xA079A6C51525DC4B` | **JHash:** `0x0128FED9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_EXPLOSION_IN_ANGLED_AREA(int explosionType, float x1, float y1, float z1, float x2, float y2, float z2, float width)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_EXPLOSION_IN_ANGLED_AREA, explosionType, x1, y1, z1, x2, y2, z2, width);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** explosionType: See ADD_EXPLOSION, -1 for any explosion type


---

### GET_OWNER_OF_EXPLOSION_IN_ANGLED_AREA

**Hash:** `0x14BA4BA137AF6CEC` | **JHash:** `0xAEC0D176` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_OWNER_OF_EXPLOSION_IN_ANGLED_AREA(int explosionType, float x1, float y1, float z1, float x2, float y2, float z2, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_OWNER_OF_EXPLOSION_IN_ANGLED_AREA, explosionType, x1, y1, z1, x2, y2, z2, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `explosionType` | `int` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `Entity` → `int`

**说明:** Returns a handle to the first entity within the a circle spawned inside the 2 points from a radius.

explosionType: See ADD_EXPLOSION.

---

### _NETWORK_EXPECT_EXPLOSION_EVENTS_FOR_PLAYER

**Hash:** `0x5241DB47A8B8AD54` | **Build:** `3570`

**C++ 原型:**
```cpp
void _NETWORK_EXPECT_EXPLOSION_EVENTS_FOR_PLAYER(BOOL expect, Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EXPECT_EXPLOSION_EVENTS_FOR_PLAYER, expect, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `expect` | `BOOL` | `bool` | |
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### _GET_MAXIMUM_NUMBER_OF_WATER_CANNONS

**Hash:** `0x56581E7E219D6263` | **Build:** `3717`

**C++ 原型:**
```cpp
int _GET_MAXIMUM_NUMBER_OF_WATER_CANNONS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_MAXIMUM_NUMBER_OF_WATER_CANNONS);
```

**返回值:** `int` → `int`

---

### _GET_WATER_CANNON_COORDS

**Hash:** `0xE61CBD3ED80E7327` | **Build:** `3717`

**C++ 原型:**
```cpp
Vector3 _GET_WATER_CANNON_COORDS(int index)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash._GET_WATER_CANNON_COORDS, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---


---

## GRAPHICS

**包含 401 个 Native 函数**

### SET_DEBUG_LINES_AND_SPHERES_DRAWING_ACTIVE

**Hash:** `0x175B6BFC15CDD0C5` | **JHash:** `0x1418CA37` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DEBUG_LINES_AND_SPHERES_DRAWING_ACTIVE(BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DEBUG_LINES_AND_SPHERES_DRAWING_ACTIVE, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_DEBUG_LINE

**Hash:** `0x7FDFADE676AA3CB0` | **JHash:** `0xABF783AB` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_LINE(float x1, float y1, float z1, float x2, float y2, float z2, int r, int g, int b, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_LINE, x1, y1, z1, x2, y2, z2, r, g, b, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_DEBUG_LINE_WITH_TWO_COLOURS

**Hash:** `0xD8B9A8AC5608FF94` | **JHash:** `0xE8BFF632` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_LINE_WITH_TWO_COLOURS(float x1, float y1, float z1, float x2, float y2, float z2, int r1, int g1, int b1, int r2, int g2, int b2, int alpha1, int alpha2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_LINE_WITH_TWO_COLOURS, x1, y1, z1, x2, y2, z2, r1, g1, b1, r2, g2, b2, alpha1, alpha2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `r1` | `int` | `int` | |
| `g1` | `int` | `int` | |
| `b1` | `int` | `int` | |
| `r2` | `int` | `int` | |
| `g2` | `int` | `int` | |
| `b2` | `int` | `int` | |
| `alpha1` | `int` | `int` | |
| `alpha2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_DEBUG_SPHERE

**Hash:** `0xAAD68E1AB39DA632` | **JHash:** `0x304D0EEF` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_SPHERE(float x, float y, float z, float radius, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_SPHERE, x, y, z, radius, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_DEBUG_BOX

**Hash:** `0x083A2CA4F2E573BD` | **JHash:** `0x8524A848` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_BOX(float x1, float y1, float z1, float x2, float y2, float z2, int r, int g, int b, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_BOX, x1, y1, z1, x2, y2, z2, r, g, b, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_DEBUG_CROSS

**Hash:** `0x73B1189623049839` | **JHash:** `0xB6DF3709` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_CROSS(float x, float y, float z, float size, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_CROSS, x, y, z, size, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `size` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_DEBUG_TEXT

**Hash:** `0x3903E216620488E8` | **JHash:** `0x269B006F` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_TEXT(const char* text, float x, float y, float z, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_TEXT, text, x, y, z, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_DEBUG_TEXT_2D

**Hash:** `0xA3BB2E9555C05A8F` | **JHash:** `0x528B973B` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_DEBUG_TEXT_2D(const char* text, float x, float y, float z, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_DEBUG_TEXT_2D, text, x, y, z, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### DRAW_LINE

**Hash:** `0x6B7256074AE34680` | **JHash:** `0xB3426BCC` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_LINE(float x1, float y1, float z1, float x2, float y2, float z2, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_LINE, x1, y1, z1, x2, y2, z2, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Draws a depth-tested line from one point to another.
----------------
x1, y1, z1 : Coordinates for the first point
x2, y2, z2 : Coordinates for the second point
r, g, b, alpha : Color with RGBA-Values
I recommend using a predefined function to call this.
[VB.NET]
Public Sub DrawLine(from As Vector3, [to] As Vector3, col As Color)
    [Function].Call(Hash.DRAW_LINE, from.X, from.Y, from.Z, [to].X, [to].Y, [to].Z, col.R, col.G, col.B, col.A)
End Sub

[C#]
public void DrawLine(Vector3 from, Vector3 to, Color col)
{
    Function.Call(Hash.DRAW_LINE, from.X, from.Y, from.Z, to.X, to.Y, to.Z, col.R, col.G, col.B, col.A);
}

---

### DRAW_POLY

**Hash:** `0xAC26716048436851` | **JHash:** `0xABD19253` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_POLY(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_POLY, x1, y1, z1, x2, y2, z2, x3, y3, z3, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** x/y/z - Location of a vertex (in world coords), presumably.
----------------
x1, y1, z1     : Coordinates for the first point
x2, y2, z2     : Coordinates for the second point
x3, y3, z3     : Coordinates for the third point
r, g, b, alpha : Color with RGBA-Values

Keep in mind that only one side of the drawn triangle is visible: It's the side, in which the vector-product of the vectors heads to: (b-a)x(c-a) Or (b-a)x(c-b).
But be aware: The function seems to work somehow differently. I have trouble having them drawn in rotated orientation. Try it yourself and if you somehow succeed, please edit this and post your solution.
I recommend using a predefined function to call this.
[VB.NET]
Public Sub DrawPoly(a As Vector3, b As Vector3, c As Vector3, col As Color)
    [Function].Call(Hash.DRAW_POLY, a.X, a.Y, a.Z, b.X, b.Y, b.Z, c.X, c.Y, c.Z, col.R, col.G, col.B, col.A)
End Sub

[C#]
public void DrawPoly(Vector3 a, Vector3 b, Vector3 c, Color col)
{
    Function.Call(Hash.DRAW_POLY, a.X, a.Y, a.Z, b.X, b.Y, b.Z, c.X, c.Y, c.Z, col.R, col.G, col.B, col.A);
}
BTW: Intersecting triangles are not supported: They overlap in the order they were called.

---

### DRAW_TEXTURED_POLY

**Hash:** `0x29280002282F1928` | **Build:** `877`

**C++ 原型:**
```cpp
void DRAW_TEXTURED_POLY(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, int red, int green, int blue, int alpha, const char* textureDict, const char* textureName, float u1, float v1, float w1, float u2, float v2, float w2, float u3, float v3, float w3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_TEXTURED_POLY, x1, y1, z1, x2, y2, z2, x3, y3, z3, red, green, blue, alpha, textureDict, textureName, u1, v1, w1, u2, v2, w2, u3, v3, w3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `u1` | `float` | `float` | |
| `v1` | `float` | `float` | |
| `w1` | `float` | `float` | |
| `u2` | `float` | `float` | |
| `v2` | `float` | `float` | |
| `w2` | `float` | `float` | |
| `u3` | `float` | `float` | |
| `v3` | `float` | `float` | |
| `w3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Used for drawling Deadline trailing lights, see deadline.ytd

p15 through p23 are values that appear to be related to illiumation, scaling, and rotation; more testing required.
For UVW mapping (u,v,w parameters), reference your favourite internet resource for more details.

---

### DRAW_TEXTURED_POLY_WITH_THREE_COLOURS

**Hash:** `0x736D7AA1B750856B` | **Build:** `877`

**C++ 原型:**
```cpp
void DRAW_TEXTURED_POLY_WITH_THREE_COLOURS(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, float red1, float green1, float blue1, int alpha1, float red2, float green2, float blue2, int alpha2, float red3, float green3, float blue3, int alpha3, const char* textureDict, const char* textureName, float u1, float v1, float w1, float u2, float v2, float w2, float u3, float v3, float w3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_TEXTURED_POLY_WITH_THREE_COLOURS, x1, y1, z1, x2, y2, z2, x3, y3, z3, red1, green1, blue1, alpha1, red2, green2, blue2, alpha2, red3, green3, blue3, alpha3, textureDict, textureName, u1, v1, w1, u2, v2, w2, u3, v3, w3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `red1` | `float` | `float` | |
| `green1` | `float` | `float` | |
| `blue1` | `float` | `float` | |
| `alpha1` | `int` | `int` | |
| `red2` | `float` | `float` | |
| `green2` | `float` | `float` | |
| `blue2` | `float` | `float` | |
| `alpha2` | `int` | `int` | |
| `red3` | `float` | `float` | |
| `green3` | `float` | `float` | |
| `blue3` | `float` | `float` | |
| `alpha3` | `int` | `int` | |
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `u1` | `float` | `float` | |
| `v1` | `float` | `float` | |
| `w1` | `float` | `float` | |
| `u2` | `float` | `float` | |
| `v2` | `float` | `float` | |
| `w2` | `float` | `float` | |
| `u3` | `float` | `float` | |
| `v3` | `float` | `float` | |
| `w3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Used for drawling Deadline trailing lights, see deadline.ytd

Each vertex has its own colour that is blended/illuminated on the texture. Additionally, the R, G, and B components are floats that are int-casted internally.
For UVW mapping (u,v,w parameters), reference your favourite internet resource for more details.

---

### DRAW_BOX

**Hash:** `0xD3A9971CADAC7252` | **JHash:** `0xCD4D9DD5` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_BOX(float x1, float y1, float z1, float x2, float y2, float z2, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_BOX, x1, y1, z1, x2, y2, z2, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** x,y,z = start pos
x2,y2,z2 = end pos

Draw's a 3D Box between the two x,y,z coords.
--------------
Keep in mind that the edges of the box do only align to the worlds base-vectors. Therefore something like rotation cannot be applied. That means this function is pretty much useless, unless you want a static unicolor box somewhere.
I recommend using a predefined function to call this.
[VB.NET]
Public Sub DrawBox(a As Vector3, b As Vector3, col As Color)
    [Function].Call(Hash.DRAW_BOX,a.X, a.Y, a.Z,b.X, b.Y, b.Z,col.R, col.G, col.B, col.A)
End Sub

[C#]
public void DrawBox(Vector3 a, Vector3 b, Color col)
{
    Function.Call(Hash.DRAW_BOX,a.X, a.Y, a.Z,b.X, b.Y, b.Z,col.R, col.G, col.B, col.A);
}

---

### SET_BACKFACECULLING

**Hash:** `0x23BA6B0C2AD7B0D3` | **JHash:** `0xC44C2F44` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BACKFACECULLING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BACKFACECULLING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DEPTHWRITING

**Hash:** `0xC5C8F970D4EDFF71` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_DEPTHWRITING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DEPTHWRITING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### BEGIN_TAKE_MISSION_CREATOR_PHOTO

**Hash:** `0x1DD2139A9A20DCE8` | **JHash:** `0xBA9AD458` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_TAKE_MISSION_CREATOR_PHOTO(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_TAKE_MISSION_CREATOR_PHOTO);
```

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_TAKE_MISSION_CREATOR_PHOTO

**Hash:** `0x90A78ECAA4E78453` | **JHash:** `0xADBBA287` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_TAKE_MISSION_CREATOR_PHOTO(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_TAKE_MISSION_CREATOR_PHOTO);
```

**返回值:** `int` → `int`

---

### FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO

**Hash:** `0x0A46AF8A78DC5E0A` | **JHash:** `0x9E553002` | **Build:** `323`

**C++ 原型:**
```cpp
void FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO);
```

**返回值:** `void` → `void`

---

### LOAD_MISSION_CREATOR_PHOTO

**Hash:** `0x4862437A486F91B0` | **JHash:** `0x56C1E488` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOAD_MISSION_CREATOR_PHOTO(Any* p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOAD_MISSION_CREATOR_PHOTO, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_LOAD_MISSION_CREATOR_PHOTO

**Hash:** `0x1670F8D05056F257` | **JHash:** `0x226B08EA` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_LOAD_MISSION_CREATOR_PHOTO(Any* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_LOAD_MISSION_CREATOR_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### BEGIN_CREATE_MISSION_CREATOR_PHOTO_PREVIEW

**Hash:** `0x7FA5D82B8F58EC06` | **JHash:** `0x1F3CADB0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_CREATE_MISSION_CREATOR_PHOTO_PREVIEW(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_CREATE_MISSION_CREATOR_PHOTO_PREVIEW);
```

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_CREATE_MISSION_CREATOR_PHOTO_PREVIEW

**Hash:** `0x5B0316762AFD4A64` | **JHash:** `0xA9DC8558` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_CREATE_MISSION_CREATOR_PHOTO_PREVIEW(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_CREATE_MISSION_CREATOR_PHOTO_PREVIEW);
```

**返回值:** `int` → `int`

---

### FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO_PREVIEW

**Hash:** `0x346EF3ECAAAB149E` | **JHash:** `0x88EAF398` | **Build:** `323`

**C++ 原型:**
```cpp
void FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO_PREVIEW(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREE_MEMORY_FOR_MISSION_CREATOR_PHOTO_PREVIEW);
```

**返回值:** `void` → `void`

---

### BEGIN_TAKE_HIGH_QUALITY_PHOTO

**Hash:** `0xA67C35C56EB1BD9D` | **JHash:** `0x47B0C137` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_TAKE_HIGH_QUALITY_PHOTO(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_TAKE_HIGH_QUALITY_PHOTO);
```

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_TAKE_HIGH_QUALITY_PHOTO

**Hash:** `0x0D6CA79EEEBD8CA3` | **JHash:** `0x65376C9B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_TAKE_HIGH_QUALITY_PHOTO(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_TAKE_HIGH_QUALITY_PHOTO);
```

**返回值:** `int` → `int`

---

### FREE_MEMORY_FOR_HIGH_QUALITY_PHOTO

**Hash:** `0xD801CC02177FA3F1` | **JHash:** `0x9CBA682A` | **Build:** `323`

**C++ 原型:**
```cpp
void FREE_MEMORY_FOR_HIGH_QUALITY_PHOTO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREE_MEMORY_FOR_HIGH_QUALITY_PHOTO);
```

**返回值:** `void` → `void`

---

### SET_TAKEN_PHOTO_IS_MUGSHOT

**Hash:** `0x1BBC135A4D25EDDE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TAKEN_PHOTO_IS_MUGSHOT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TAKEN_PHOTO_IS_MUGSHOT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ARENA_THEME_AND_VARIATION_FOR_TAKEN_PHOTO

**Hash:** `0xF3F776ADA161E47D` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_ARENA_THEME_AND_VARIATION_FOR_TAKEN_PHOTO(Any p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ARENA_THEME_AND_VARIATION_FOR_TAKEN_PHOTO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_ON_ISLAND_X_FOR_TAKEN_PHOTO

**Hash:** `0xADD6627C4D325458` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_ON_ISLAND_X_FOR_TAKEN_PHOTO(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ON_ISLAND_X_FOR_TAKEN_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SAVE_HIGH_QUALITY_PHOTO

**Hash:** `0x3DEC726C25A11BAC` | **JHash:** `0x3B15D33C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SAVE_HIGH_QUALITY_PHOTO(int unused)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVE_HIGH_QUALITY_PHOTO, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `unused` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** 1 match in 1 script. cellphone_controller.
p0 is -1 in scripts.

---

### GET_STATUS_OF_SAVE_HIGH_QUALITY_PHOTO

**Hash:** `0x0C0C4E81E1AC60A0` | **JHash:** `0xEC5D0317` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_SAVE_HIGH_QUALITY_PHOTO(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_SAVE_HIGH_QUALITY_PHOTO);
```

**返回值:** `int` → `int`

---

### BEGIN_CREATE_LOW_QUALITY_COPY_OF_PHOTO

**Hash:** `0x759650634F07B6B4` | **JHash:** `0x25D569EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_CREATE_LOW_QUALITY_COPY_OF_PHOTO(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_CREATE_LOW_QUALITY_COPY_OF_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_CREATE_LOW_QUALITY_COPY_OF_PHOTO

**Hash:** `0xCB82A0BF0E3E3265` | **JHash:** `0xCFCDC518` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_CREATE_LOW_QUALITY_COPY_OF_PHOTO(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_CREATE_LOW_QUALITY_COPY_OF_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### FREE_MEMORY_FOR_LOW_QUALITY_PHOTO

**Hash:** `0x6A12D88881435DCA` | **JHash:** `0x108F36CC` | **Build:** `323`

**C++ 原型:**
```cpp
void FREE_MEMORY_FOR_LOW_QUALITY_PHOTO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FREE_MEMORY_FOR_LOW_QUALITY_PHOTO);
```

**返回值:** `void` → `void`

---

### DRAW_LOW_QUALITY_PHOTO_TO_PHONE

**Hash:** `0x1072F115DAB0717E` | **JHash:** `0xE9F2B68F` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_LOW_QUALITY_PHOTO_TO_PHONE(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_LOW_QUALITY_PHOTO_TO_PHONE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_MAXIMUM_NUMBER_OF_PHOTOS

**Hash:** `0x34D23450F028B0BF` | **JHash:** `0x727AA63F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAXIMUM_NUMBER_OF_PHOTOS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAXIMUM_NUMBER_OF_PHOTOS);
```

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 0.

---

### GET_MAXIMUM_NUMBER_OF_CLOUD_PHOTOS

**Hash:** `0xDC54A7AF8B3A14EF` | **JHash:** `0x239272BD` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAXIMUM_NUMBER_OF_CLOUD_PHOTOS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAXIMUM_NUMBER_OF_CLOUD_PHOTOS);
```

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 96.

---

### GET_CURRENT_NUMBER_OF_CLOUD_PHOTOS

**Hash:** `0x473151EBC762C6DA` | **JHash:** `0x21DBF0C9` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_NUMBER_OF_CLOUD_PHOTOS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_NUMBER_OF_CLOUD_PHOTOS);
```

**返回值:** `int` → `int`

---

### QUEUE_OPERATION_TO_CREATE_SORTED_LIST_OF_PHOTOS

**Hash:** `0x2A893980E96B659A` | **JHash:** `0x199FABF0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL QUEUE_OPERATION_TO_CREATE_SORTED_LIST_OF_PHOTOS(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.QUEUE_OPERATION_TO_CREATE_SORTED_LIST_OF_PHOTOS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** 2 matches across 2 scripts. Only showed in appcamera & appmedia. Both were 0.

---

### GET_STATUS_OF_SORTED_LIST_OPERATION

**Hash:** `0xF5BED327CEA362B1` | **JHash:** `0x596B900D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_SORTED_LIST_OPERATION(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_SORTED_LIST_OPERATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** 3 matches across 3 scripts. First 2 were 0, 3rd was 1. Possibly a bool.
appcamera, appmedia, and cellphone_controller.

---

### CLEAR_STATUS_OF_SORTED_LIST_OPERATION

**Hash:** `0x4AF92ACD3141D96C` | **JHash:** `0xC9EF81ED` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_STATUS_OF_SORTED_LIST_OPERATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_STATUS_OF_SORTED_LIST_OPERATION);
```

**返回值:** `void` → `void`

---

### DOES_THIS_PHOTO_SLOT_CONTAIN_A_VALID_PHOTO

**Hash:** `0xE791DF1F73ED2C8B` | **JHash:** `0x9D84554C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_THIS_PHOTO_SLOT_CONTAIN_A_VALID_PHOTO(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_THIS_PHOTO_SLOT_CONTAIN_A_VALID_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### LOAD_HIGH_QUALITY_PHOTO

**Hash:** `0xEC72C258667BE5EA` | **JHash:** `0x9C106AD9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOAD_HIGH_QUALITY_PHOTO(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOAD_HIGH_QUALITY_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### GET_LOAD_HIGH_QUALITY_PHOTO_STATUS

**Hash:** `0x40AFB081F8ADD4EE` | **JHash:** `0x762E5C5F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LOAD_HIGH_QUALITY_PHOTO_STATUS(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LOAD_HIGH_QUALITY_PHOTO_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Hardcoded to always return 2.

---

### DRAW_LIGHT_WITH_RANGEEX

**Hash:** `0xF49E9A9716A04595` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_LIGHT_WITH_RANGEEX(float x, float y, float z, int r, int g, int b, float range, float intensity, float shadow)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_LIGHT_WITH_RANGEEX, x, y, z, r, g, b, range, intensity, shadow);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `range` | `float` | `float` | |
| `intensity` | `float` | `float` | |
| `shadow` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DRAW_LIGHT_WITH_RANGE

**Hash:** `0xF2A1B2771A01DBD4` | **JHash:** `0x6A396E9A` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_LIGHT_WITH_RANGE(float posX, float posY, float posZ, int colorR, int colorG, int colorB, float range, float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_LIGHT_WITH_RANGE, posX, posY, posZ, colorR, colorG, colorB, range, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `colorR` | `int` | `int` | |
| `colorG` | `int` | `int` | |
| `colorB` | `int` | `int` | |
| `range` | `float` | `float` | |
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DRAW_SPOT_LIGHT

**Hash:** `0xD0F64B265C8C8B33` | **JHash:** `0xBDBC410C` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SPOT_LIGHT(float posX, float posY, float posZ, float dirX, float dirY, float dirZ, int colorR, int colorG, int colorB, float distance, float brightness, float hardness, float radius, float falloff)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SPOT_LIGHT, posX, posY, posZ, dirX, dirY, dirZ, colorR, colorG, colorB, distance, brightness, hardness, radius, falloff);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `dirX` | `float` | `float` | |
| `dirY` | `float` | `float` | |
| `dirZ` | `float` | `float` | |
| `colorR` | `int` | `int` | |
| `colorG` | `int` | `int` | |
| `colorB` | `int` | `int` | |
| `distance` | `float` | `float` | |
| `brightness` | `float` | `float` | |
| `hardness` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `falloff` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Parameters:
* pos - coordinate where the spotlight is located
* dir - the direction vector the spotlight should aim at from its current position
* r,g,b - color of the spotlight
* distance - the maximum distance the light can reach
* brightness - the brightness of the light
* roundness - "smoothness" of the circle edge
* radius - the radius size of the spotlight
* falloff - the falloff size of the light's edge (example: www.i.imgur.com/DemAWeO.jpg)

Example in C# (spotlight aims at the closest vehicle):
Vector3 myPos = Game.Player.Character.Position;
Vehicle nearest = World.GetClosestVehicle(myPos , 1000f);
Vector3 destinationCoords = nearest.Position;
Vector3 dirVector = destinationCoords - myPos;
dirVector.Normalize();
Function.Call(Hash.DRAW_SPOT_LIGHT, pos.X, pos.Y, pos.Z, dirVector.X, dirVector.Y, dirVector.Z, 255, 255, 255, 100.0f, 1f, 0.0f, 13.0f, 1f);

---

### DRAW_SHADOWED_SPOT_LIGHT

**Hash:** `0x5BCA583A583194DB` | **JHash:** `0x32BF9598` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SHADOWED_SPOT_LIGHT(float posX, float posY, float posZ, float dirX, float dirY, float dirZ, int colorR, int colorG, int colorB, float distance, float brightness, float roundness, float radius, float falloff, int shadowId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SHADOWED_SPOT_LIGHT, posX, posY, posZ, dirX, dirY, dirZ, colorR, colorG, colorB, distance, brightness, roundness, radius, falloff, shadowId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `dirX` | `float` | `float` | |
| `dirY` | `float` | `float` | |
| `dirZ` | `float` | `float` | |
| `colorR` | `int` | `int` | |
| `colorG` | `int` | `int` | |
| `colorB` | `int` | `int` | |
| `distance` | `float` | `float` | |
| `brightness` | `float` | `float` | |
| `roundness` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `falloff` | `float` | `float` | |
| `shadowId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FADE_UP_PED_LIGHT

**Hash:** `0xC9B18B4619F48F7B` | **JHash:** `0x93628786` | **Build:** `323`

**C++ 原型:**
```cpp
void FADE_UP_PED_LIGHT(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FADE_UP_PED_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UPDATE_LIGHTS_ON_ENTITY

**Hash:** `0xDEADC0DEDEADC0DE` | **JHash:** `0xC12AC47A` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_LIGHTS_ON_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_LIGHTS_ON_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE

**Hash:** `0x9641588DAB93B4B5` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE

**Hash:** `0x393BD2275CEB7793` | **Build:** `1103`

**C++ 原型:**
```cpp
float GET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_LIGHT_OVERRIDE_MAX_INTENSITY_SCALE);
```

**返回值:** `float` → `float`

---

### DRAW_MARKER

**Hash:** `0x28477EC23D892089` | **JHash:** `0x48D84A02` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_MARKER(int type, float posX, float posY, float posZ, float dirX, float dirY, float dirZ, float rotX, float rotY, float rotZ, float scaleX, float scaleY, float scaleZ, int red, int green, int blue, int alpha, BOOL bobUpAndDown, BOOL faceCamera, int p19, BOOL rotate, const char* textureDict, const char* textureName, BOOL drawOnEnts)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_MARKER, type, posX, posY, posZ, dirX, dirY, dirZ, rotX, rotY, rotZ, scaleX, scaleY, scaleZ, red, green, blue, alpha, bobUpAndDown, faceCamera, p19, rotate, textureDict, textureName, drawOnEnts);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `dirX` | `float` | `float` | |
| `dirY` | `float` | `float` | |
| `dirZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `scaleX` | `float` | `float` | |
| `scaleY` | `float` | `float` | |
| `scaleZ` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `bobUpAndDown` | `BOOL` | `bool` | |
| `faceCamera` | `BOOL` | `bool` | |
| `p19` | `int` | `int` | |
| `rotate` | `BOOL` | `bool` | |
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `drawOnEnts` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enum MarkerTypes
{
    MarkerTypeUpsideDownCone = 0,
 MarkerTypeVerticalCylinder = 1,
   MarkerTypeThickChevronUp = 2,
 MarkerTypeThinChevronUp = 3,
  MarkerTypeCheckeredFlagRect = 4,
  MarkerTypeCheckeredFlagCircle = 5,
    MarkerTypeVerticleCircle = 6,
 MarkerTypePlaneModel = 7,
 MarkerTypeLostMCDark = 8,
 MarkerTypeLostMCLight = 9,
    MarkerTypeNumber0 = 10,
   MarkerTypeNumber1 = 11,
   MarkerTypeNumber2 = 12,
   MarkerTypeNumber3 = 13,
   MarkerTypeNumber4 = 14,
   MarkerTypeNumber5 = 15,
   MarkerTypeNumber6 = 16,
   MarkerTypeNumber7 = 17,
   MarkerTypeNumber8 = 18,
   MarkerTypeNumber9 = 19,
   MarkerTypeChevronUpx1 = 20,
   MarkerTypeChevronUpx2 = 21,
   MarkerTypeChevronUpx3 = 22,
   MarkerTypeHorizontalCircleFat = 23,
   MarkerTypeReplayIcon = 24,
    MarkerTypeHorizontalCircleSkinny = 25,
    MarkerTypeHorizontalCircleSkinny_Arrow = 26,
  MarkerTypeHorizontalSplitArrowCircle = 27,
    MarkerTypeDebugSphere = 28,
   MarkerTypeDallorSign = 29,
    MarkerTypeHorizontalBars = 30,
    MarkerTypeWolfHead = 31
};

dirX/Y/Z represent a heading on each axis in which the marker should face, alternatively you can rotate each axis independently with rotX/Y/Z (and set dirX/Y/Z all to 0).

faceCamera - Rotates only the y-axis (the heading) towards the camera

p19 - no effect, default value in script is 2

rotate - Rotates only on the y-axis (the heading)

textureDict - Name of texture dictionary to load texture from (e.g. "GolfPutting")

textureName - Name of texture inside dictionary to load (e.g. "PuttingMarker")

drawOnEnts - Draws the marker onto any entities that intersect it

basically what he said, except textureDict and textureName are totally not const char*, or if so, then they are always set to 0/NULL/nullptr in every script I checked, eg:

bj.c: graphics::draw_marker(6, vParam0, 0f, 0f, 1f, 0f, 0f, 0f, 4f, 4f, 4f, 240, 200, 80, iVar1, 0, 0, 2, 0, 0, 0, false);

his is what I used to draw an amber downward pointing chevron "V", has to be redrawn every frame.  The 180 is for 180 degrees rotation around the Y axis, the 50 is alpha, assuming max is 100, but it will accept 255.

GRAPHICS::DRAW_MARKER(2, v.x, v.y, v.z + 2, 0, 0, 0, 0, 180, 0, 2, 2, 2, 255, 128, 0, 50, 0, 1, 1, 0, 0, 0, 0);



---

### DRAW_MARKER_EX

**Hash:** `0xE82728F0DE75D13A` | **Build:** `573`

**C++ 原型:**
```cpp
void DRAW_MARKER_EX(int type, float posX, float posY, float posZ, float dirX, float dirY, float dirZ, float rotX, float rotY, float rotZ, float scaleX, float scaleY, float scaleZ, int red, int green, int blue, int alpha, BOOL bobUpAndDown, BOOL faceCamera, Any p19, BOOL rotate, const char* textureDict, const char* textureName, BOOL drawOnEnts, BOOL p24, BOOL p25)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_MARKER_EX, type, posX, posY, posZ, dirX, dirY, dirZ, rotX, rotY, rotZ, scaleX, scaleY, scaleZ, red, green, blue, alpha, bobUpAndDown, faceCamera, p19, rotate, textureDict, textureName, drawOnEnts, p24, p25);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `dirX` | `float` | `float` | |
| `dirY` | `float` | `float` | |
| `dirZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `scaleX` | `float` | `float` | |
| `scaleY` | `float` | `float` | |
| `scaleZ` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `bobUpAndDown` | `BOOL` | `bool` | |
| `faceCamera` | `BOOL` | `bool` | |
| `p19` | `Any` | `object` | |
| `rotate` | `BOOL` | `bool` | |
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `drawOnEnts` | `BOOL` | `bool` | |
| `p24` | `BOOL` | `bool` | |
| `p25` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DRAW_MARKER_SPHERE

**Hash:** `0x799017F9E3B10112` | **Build:** `463`

**C++ 原型:**
```cpp
void DRAW_MARKER_SPHERE(float x, float y, float z, float radius, int red, int green, int blue, float alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_MARKER_SPHERE, x, y, z, radius, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Draws a 3D sphere, typically seen in the GTA:O freemode event "Penned In".
Example https://imgur.com/nCbtS4H

alpha - The alpha for the sphere. Goes from 0.0 to 1.0.

---

### CREATE_CHECKPOINT

**Hash:** `0x0134F0835AB6BFCB` | **JHash:** `0xF541B690` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_CHECKPOINT(int type, float posX1, float posY1, float posZ1, float posX2, float posY2, float posZ2, float diameter, int red, int green, int blue, int alpha, int reserved)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_CHECKPOINT, type, posX1, posY1, posZ1, posX2, posY2, posZ2, diameter, red, green, blue, alpha, reserved);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `posX1` | `float` | `float` | |
| `posY1` | `float` | `float` | |
| `posZ1` | `float` | `float` | |
| `posX2` | `float` | `float` | |
| `posY2` | `float` | `float` | |
| `posZ2` | `float` | `float` | |
| `diameter` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `reserved` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Creates a checkpoint. Returns the handle of the checkpoint.

20/03/17 : Attention, checkpoints are already handled by the game itself, so you must not loop it like markers.

Parameters:
* type - The type of checkpoint to create. See below for a list of checkpoint types.
* pos1 - The position of the checkpoint.
* pos2 - The position of the next checkpoint to point to.
* radius - The radius of the checkpoint.
* color - The color of the checkpoint.
* reserved - Special parameter, see below for details. Usually set to 0 in the scripts.

Checkpoint types:
0-4---------Cylinder: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker
5-9---------Cylinder: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker
10-14-------Ring: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker
15-19-------1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker      
20-24-------Cylinder: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker 
25-29-------Cylinder: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker    
30-34-------Cylinder: 1 arrow, 2 arrow, 3 arrows, CycleArrow, Checker 
35-38-------Ring: Airplane Up, Left, Right, UpsideDown
39----------?
40----------Ring: just a ring
41----------?
42-44-------Cylinder w/ number (uses 'reserved' parameter)
45-47-------Cylinder no arrow or number

If using type 42-44, reserved sets number / number and shape to display

0-99------------Just numbers (0-99)
100-109-----------------Arrow (0-9)
110-119------------Two arrows (0-9)
120-129----------Three arrows (0-9)
130-139----------------Circle (0-9)
140-149------------CycleArrow (0-9)
150-159----------------Circle (0-9)
160-169----Circle  w/ pointer (0-9)
170-179-------Perforated ring (0-9)
180-189----------------Sphere (0-9)

---

### SET_CHECKPOINT_INSIDE_CYLINDER_HEIGHT_SCALE

**Hash:** `0x4B5B4DA5D79F1943` | **JHash:** `0x80151CCF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_INSIDE_CYLINDER_HEIGHT_SCALE(int checkpoint, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_INSIDE_CYLINDER_HEIGHT_SCALE, checkpoint, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CHECKPOINT_INSIDE_CYLINDER_SCALE

**Hash:** `0x44621483FF966526` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_INSIDE_CYLINDER_SCALE(int checkpoint, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_INSIDE_CYLINDER_SCALE, checkpoint, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CHECKPOINT_CYLINDER_HEIGHT

**Hash:** `0x2707AAE9D9297D89` | **JHash:** `0xFF0F9B22` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_CYLINDER_HEIGHT(int checkpoint, float nearHeight, float farHeight, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_CYLINDER_HEIGHT, checkpoint, nearHeight, farHeight, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `nearHeight` | `float` | `float` | |
| `farHeight` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the cylinder height of the checkpoint.

Parameters:
* nearHeight - The height of the checkpoint when inside of the radius.
* farHeight - The height of the checkpoint when outside of the radius.
* radius - The radius of the checkpoint.

---

### SET_CHECKPOINT_RGBA

**Hash:** `0x7167371E8AD747F7` | **JHash:** `0xEF9C8CB3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_RGBA(int checkpoint, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_RGBA, checkpoint, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the checkpoint color.

---

### SET_CHECKPOINT_RGBA2

**Hash:** `0xB9EA40907C680580` | **JHash:** `0xA5456DBB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_RGBA2(int checkpoint, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_RGBA2, checkpoint, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the checkpoint icon color.

---

### SET_CHECKPOINT_CLIPPLANE_WITH_POS_NORM

**Hash:** `0xF51D36185993515D` | **JHash:** `0x20EABD0F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_CLIPPLANE_WITH_POS_NORM(int checkpoint, float posX, float posY, float posZ, float unkX, float unkY, float unkZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_CLIPPLANE_WITH_POS_NORM, checkpoint, posX, posY, posZ, unkX, unkY, unkZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `unkX` | `float` | `float` | |
| `unkY` | `float` | `float` | |
| `unkZ` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This does not move an existing checkpoint... so wtf.

---

### SET_CHECKPOINT_FORCE_OLD_ARROW_POINTING

**Hash:** `0xFCF6788FC4860CD4` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_FORCE_OLD_ARROW_POINTING(int checkpoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_FORCE_OLD_ARROW_POINTING, checkpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_CHECKPOINT_DECAL_ROT_ALIGNED_TO_CAMERA_ROT

**Hash:** `0x615D3925E87A3B26` | **JHash:** `0x1E3A3126` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_DECAL_ROT_ALIGNED_TO_CAMERA_ROT(int checkpoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_DECAL_ROT_ALIGNED_TO_CAMERA_ROT, checkpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Unknown. Called after creating a checkpoint (type: 51) in the creators.

---

### SET_CHECKPOINT_FORCE_DIRECTION

**Hash:** `0xDB1EA9411C8911EC` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_FORCE_DIRECTION(int checkpoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_FORCE_DIRECTION, checkpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_CHECKPOINT_DIRECTION

**Hash:** `0x3C788E7F6438754D` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_CHECKPOINT_DIRECTION(int checkpoint, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECKPOINT_DIRECTION, checkpoint, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DELETE_CHECKPOINT

**Hash:** `0xF5ED37F54CD4D52E` | **JHash:** `0xB66CF3CA` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_CHECKPOINT(int checkpoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_CHECKPOINT, checkpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkpoint` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DONT_RENDER_IN_GAME_UI

**Hash:** `0x22A249A53034450A` | **JHash:** `0x932FDB81` | **Build:** `323`

**C++ 原型:**
```cpp
void DONT_RENDER_IN_GAME_UI(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DONT_RENDER_IN_GAME_UI, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_RENDER_IN_GAME_UI

**Hash:** `0xDC459CFA0CCE245B` | **JHash:** `0x7E946E87` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_RENDER_IN_GAME_UI(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_RENDER_IN_GAME_UI, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_STREAMED_TEXTURE_DICT

**Hash:** `0xDFA2EF8E04127DD5` | **JHash:** `0x4C9B035F` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_STREAMED_TEXTURE_DICT(const char* textureDict, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_STREAMED_TEXTURE_DICT, textureDict, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This function can requests texture dictonaries from following RPFs:
scaleform_generic.rpf
scaleform_minigames.rpf
scaleform_minimap.rpf
scaleform_web.rpf

last param isnt a toggle

---

### HAS_STREAMED_TEXTURE_DICT_LOADED

**Hash:** `0x0145F696AAAAD2E4` | **JHash:** `0x3F436EEF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_STREAMED_TEXTURE_DICT_LOADED(const char* textureDict)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_STREAMED_TEXTURE_DICT_LOADED, textureDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SET_STREAMED_TEXTURE_DICT_AS_NO_LONGER_NEEDED

**Hash:** `0xBE2CACCF5A8AA805` | **JHash:** `0xF07DDA38` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STREAMED_TEXTURE_DICT_AS_NO_LONGER_NEEDED(const char* textureDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STREAMED_TEXTURE_DICT_AS_NO_LONGER_NEEDED, textureDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### DRAW_RECT

**Hash:** `0x3A618A217E5154F0` | **JHash:** `0xDD2BFC77` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_RECT(float x, float y, float width, float height, int r, int g, int b, int a, BOOL p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_RECT, x, y, width, height, r, g, b, a, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Draws a rectangle on the screen.

-x: The relative X point of the center of the rectangle. (0.0-1.0, 0.0 is the left edge of the screen, 1.0 is the right edge of the screen)

-y: The relative Y point of the center of the rectangle. (0.0-1.0, 0.0 is the top edge of the screen, 1.0 is the bottom edge of the screen)

-width: The relative width of the rectangle. (0.0-1.0, 1.0 means the whole screen width)

-height: The relative height of the rectangle. (0.0-1.0, 1.0 means the whole screen height)

-R: Red part of the color. (0-255)

-G: Green part of the color. (0-255)

-B: Blue part of the color. (0-255)

-A: Alpha part of the color. (0-255, 0 means totally transparent, 255 means totally opaque)

The total number of rectangles to be drawn in one frame is apparently limited to 399.


---

### SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU

**Hash:** `0xC6372ECD45D73BCD` | **JHash:** `0xF8FBCC25` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a flag defining whether or not script draw commands should continue being drawn behind the pause menu. This is usually used for TV channels and other draw commands that are used with a world render target.

---

### SET_SCRIPT_GFX_DRAW_ORDER

**Hash:** `0x61BB1D9B3A95D802` | **JHash:** `0xADF81D24` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_GFX_DRAW_ORDER(int drawOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_GFX_DRAW_ORDER, drawOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `drawOrder` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the draw order for script draw commands.

Examples from decompiled scripts:
GRAPHICS::SET_SCRIPT_GFX_DRAW_ORDER(7);
GRAPHICS::DRAW_RECT(0.5, 0.5, 3.0, 3.0, v_4, v_5, v_6, a_0._f172, 0);

GRAPHICS::SET_SCRIPT_GFX_DRAW_ORDER(1);
GRAPHICS::DRAW_RECT(0.5, 0.5, 1.5, 1.5, 0, 0, 0, 255, 0);

---

### SET_SCRIPT_GFX_ALIGN

**Hash:** `0xB8A850F20A067EB6` | **JHash:** `0x228A2598` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_GFX_ALIGN(int horizontalAlign, int verticalAlign)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_GFX_ALIGN, horizontalAlign, verticalAlign);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `horizontalAlign` | `int` | `int` | |
| `verticalAlign` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** horizontalAlign: The horizontal alignment. This can be 67 ('C'), 76 ('L'), or 82 ('R').
verticalAlign: The vertical alignment. This can be 67 ('C'), 66 ('B'), or 84 ('T').

This function anchors script draws to a side of the safe zone. This needs to be called to make the interface independent of the player's safe zone configuration.

These values are equivalent to alignX and alignY in common:/data/ui/frontend.xml, which can be used as a baseline for default alignment.

Using any other value (including 0) will result in the safe zone not being taken into account for this draw. The canonical value for this is 'I' (73).

For example, you can use SET_SCRIPT_GFX_ALIGN(0, 84) to only scale on the Y axis (to the top), but not change the X axis.

To reset the value, use RESET_SCRIPT_GFX_ALIGN.

---

### RESET_SCRIPT_GFX_ALIGN

**Hash:** `0xE3A3DB414A373DAB` | **JHash:** `0x3FE33BD6` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_SCRIPT_GFX_ALIGN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_SCRIPT_GFX_ALIGN);
```

**返回值:** `void` → `void`

**说明:** This function resets the alignment set using SET_SCRIPT_GFX_ALIGN and SET_SCRIPT_GFX_ALIGN_PARAMS to the default values ('I', 'I'; 0, 0, 0, 0).
This should be used after having used the aforementioned functions in order to not affect any other scripts attempting to draw.

---

### SET_SCRIPT_GFX_ALIGN_PARAMS

**Hash:** `0xF5A2C681787E579D` | **JHash:** `0x76C641E4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_GFX_ALIGN_PARAMS(float x, float y, float w, float h)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_GFX_ALIGN_PARAMS, x, y, w, h);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `w` | `float` | `float` | |
| `h` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the draw offset/calculated size for SET_SCRIPT_GFX_ALIGN. If using any alignment other than left/top, the game expects the width/height to be configured using this native in order to get a proper starting position for the draw command.

---

### GET_SCRIPT_GFX_ALIGN_POSITION

**Hash:** `0x6DD8F5AA635EB4B2` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SCRIPT_GFX_ALIGN_POSITION(float x, float y, float* calculatedX, float* calculatedY)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SCRIPT_GFX_ALIGN_POSITION, x, y, calculatedX, calculatedY);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `calculatedX` | `float*` | `float*` | |
| `calculatedY` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** Calculates the effective X/Y fractions when applying the values set by SET_SCRIPT_GFX_ALIGN and SET_SCRIPT_GFX_ALIGN_PARAMS

---

### GET_SAFE_ZONE_SIZE

**Hash:** `0xBAF107B6BB2C97F0` | **JHash:** `0x3F0D1A6F` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SAFE_ZONE_SIZE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SAFE_ZONE_SIZE);
```

**返回值:** `float` → `float`

**说明:** Gets the scale of safe zone. if the safe zone size scale is max, it will return 1.0.

---

### DRAW_SPRITE

**Hash:** `0xE7FFAE5EBF23D890` | **JHash:** `0x1FEC16B0` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SPRITE(const char* textureDict, const char* textureName, float screenX, float screenY, float width, float height, float heading, int red, int green, int blue, int alpha, BOOL p11, Any p12)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SPRITE, textureDict, textureName, screenX, screenY, width, height, heading, red, green, blue, alpha, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `screenX` | `float` | `float` | |
| `screenY` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Draws a 2D sprite on the screen.

Parameters:
textureDict - Name of texture dictionary to load texture from (e.g. "CommonMenu", "MPWeaponsCommon", etc.)

textureName - Name of texture to load from texture dictionary (e.g. "last_team_standing_icon", "tennis_icon", etc.)

screenX/Y - Screen offset (0.5 = center)
scaleX/Y - Texture scaling. Negative values can be used to flip the texture on that axis. (0.5 = half)

heading - Texture rotation in degrees (default = 0.0) positive is clockwise, measured in degrees

red,green,blue - Sprite color (default = 255/255/255)

alpha - opacity level

---

### DRAW_SPRITE_ARX

**Hash:** `0x2D3B147AFAD49DE0` | **Build:** `1290`

**C++ 原型:**
```cpp
void DRAW_SPRITE_ARX(const char* textureDict, const char* textureName, float x, float y, float width, float height, float p6, int red, int green, int blue, int alpha, Any p11, Any p12)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SPRITE_ARX, textureDict, textureName, x, y, width, height, p6, red, green, blue, alpha, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p11` | `Any` | `object` | |
| `p12` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Used in arcade games and Beam hack minigame in Doomsday Heist. I will most certainly dive into this to try replicate arcade games.
x position must be between 0.0 and 1.0 (1.0 being the most right side of the screen)
y position must be between 0.0 and 1.0 (1.0 being the most bottom side of the screen)
width 0.0 - 1.0 is the reasonable amount generally
height 0.0 - 1.0 is the reasonable amount generally
p6 almost always 0.0
p11 seems to be unknown but almost always 0 int

---

### DRAW_SPRITE_NAMED_RENDERTARGET

**Hash:** `0x2BC54A8188768488` | **Build:** `877`

**C++ 原型:**
```cpp
void DRAW_SPRITE_NAMED_RENDERTARGET(const char* textureDict, const char* textureName, float screenX, float screenY, float width, float height, float heading, int red, int green, int blue, int alpha, Any p11)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SPRITE_NAMED_RENDERTARGET, textureDict, textureName, screenX, screenY, width, height, heading, red, green, blue, alpha, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `screenX` | `float` | `float` | |
| `screenY` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p11` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Similar to _DRAW_SPRITE, but seems to be some kind of "interactive" sprite, at least used by render targets.
These seem to be the only dicts ever requested by this native:

prop_screen_biker_laptop
Prop_Screen_GR_Disruption
Prop_Screen_TaleOfUs
prop_screen_nightclub
Prop_Screen_IE_Adhawk
prop_screen_sm_free_trade_shipping
prop_screen_hacker_truck
MPDesktop
Prop_Screen_Nightclub
And a few others


---

### DRAW_SPRITE_ARX_WITH_UV

**Hash:** `0x95812F9B26074726` | **Build:** `1868`

**C++ 原型:**
```cpp
void DRAW_SPRITE_ARX_WITH_UV(const char* textureDict, const char* textureName, float x, float y, float width, float height, float u1, float v1, float u2, float v2, float heading, int red, int green, int blue, int alpha, Any p15)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SPRITE_ARX_WITH_UV, textureDict, textureName, x, y, width, height, u1, v1, u2, v2, heading, red, green, blue, alpha, p15);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `u1` | `float` | `float` | |
| `v1` | `float` | `float` | |
| `u2` | `float` | `float` | |
| `v2` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p15` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Similar to DRAW_SPRITE, but allows to specify the texture coordinates used to draw the sprite.

u1, v1 - texture coordinates for the top-left corner
u2, v2 - texture coordinates for the bottom-right corner

---

### ADD_ENTITY_ICON

**Hash:** `0x9CD43EEE12BF4DD0` | **JHash:** `0xF3027D21` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_ENTITY_ICON(Entity entity, const char* icon)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_ENTITY_ICON, entity, icon);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `icon` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Example:
GRAPHICS::ADD_ENTITY_ICON(a_0, "MP_Arrow");

I tried this and nothing happened...

---

### SET_ENTITY_ICON_VISIBILITY

**Hash:** `0xE0E8BEECCA96BA31` | **JHash:** `0xD1D2FD52` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ICON_VISIBILITY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ICON_VISIBILITY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_ICON_COLOR

**Hash:** `0x1D5F595CCAE2E238` | **JHash:** `0x6EE1E946` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_ICON_COLOR(Entity entity, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_ICON_COLOR, entity, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_DRAW_ORIGIN

**Hash:** `0xAA0008F3BBB8F416` | **JHash:** `0xE10198D5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRAW_ORIGIN(float x, float y, float z, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRAW_ORIGIN, x, y, z, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the on-screen drawing origin for draw-functions (which is normally x=0,y=0 in the upper left corner of the screen) to a world coordinate.
From now on, the screen coordinate which displays the given world coordinate on the screen is seen as x=0,y=0.

Example in C#:
Vector3 boneCoord = somePed.GetBoneCoord(Bone.SKEL_Head);
Function.Call(Hash.SET_DRAW_ORIGIN, boneCoord.X, boneCoord.Y, boneCoord.Z, 0);
Function.Call(Hash.DRAW_SPRITE, "helicopterhud", "hud_corner", -0.01, -0.015, 0.013, 0.013, 0.0, 255, 0, 0, 200);
Function.Call(Hash.DRAW_SPRITE, "helicopterhud", "hud_corner", 0.01, -0.015, 0.013, 0.013, 90.0, 255, 0, 0, 200);
Function.Call(Hash.DRAW_SPRITE, "helicopterhud", "hud_corner", -0.01, 0.015, 0.013, 0.013, 270.0, 255, 0, 0, 200);
Function.Call(Hash.DRAW_SPRITE, "helicopterhud", "hud_corner", 0.01, 0.015, 0.013, 0.013, 180.0, 255, 0, 0, 200);
Function.Call(Hash.CLEAR_DRAW_ORIGIN);

Result: www11.pic-upload.de/19.06.15/bkqohvil2uao.jpg
If the pedestrian starts walking around now, the sprites are always around her head, no matter where the head is displayed on the screen.

This function also effects the drawing of texts and other UI-elements.
The effect can be reset by calling GRAPHICS::CLEAR_DRAW_ORIGIN().

---

### CLEAR_DRAW_ORIGIN

**Hash:** `0xFF0B610F6BE0D7AF` | **JHash:** `0xDD76B263` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_DRAW_ORIGIN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_DRAW_ORIGIN);
```

**返回值:** `void` → `void`

**说明:** Resets the screen's draw-origin which was changed by the function GRAPHICS::SET_DRAW_ORIGIN(...) back to x=0,y=0.

See GRAPHICS::SET_DRAW_ORIGIN(...) for further information.

---

### SET_BINK_MOVIE

**Hash:** `0x338D9F609FD632DB` | **Build:** `1290`

**C++ 原型:**
```cpp
int SET_BINK_MOVIE(const char* name)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_BINK_MOVIE, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### PLAY_BINK_MOVIE

**Hash:** `0x70D2CC8A542A973C` | **Build:** `1290`

**C++ 原型:**
```cpp
void PLAY_BINK_MOVIE(int binkMovie)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_BINK_MOVIE, binkMovie);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |

**返回值:** `void` → `void`

---

### STOP_BINK_MOVIE

**Hash:** `0x63606A61DE68898A` | **Build:** `1290`

**C++ 原型:**
```cpp
void STOP_BINK_MOVIE(int binkMovie)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_BINK_MOVIE, binkMovie);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RELEASE_BINK_MOVIE

**Hash:** `0x04D950EEFA4EED8C` | **Build:** `1290`

**C++ 原型:**
```cpp
void RELEASE_BINK_MOVIE(int binkMovie)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_BINK_MOVIE, binkMovie);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_BINK_MOVIE

**Hash:** `0x7118E83EEB9F7238` | **Build:** `1290`

**C++ 原型:**
```cpp
void DRAW_BINK_MOVIE(int binkMovie, float p1, float p2, float p3, float p4, float p5, int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_BINK_MOVIE, binkMovie, p1, p2, p3, p4, p5, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_BINK_MOVIE_TIME

**Hash:** `0x0CB6B3446855B57A` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_BINK_MOVIE_TIME(int binkMovie, float progress)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BINK_MOVIE_TIME, binkMovie, progress);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |
| `progress` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** In percentage: 0.0 - 100.0

---

### GET_BINK_MOVIE_TIME

**Hash:** `0x8E17DDD6B9D5BF29` | **Build:** `1734`

**C++ 原型:**
```cpp
float GET_BINK_MOVIE_TIME(int binkMovie)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_BINK_MOVIE_TIME, binkMovie);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** In percentage: 0.0 - 100.0

---

### SET_BINK_MOVIE_VOLUME

**Hash:** `0xAFF33B1178172223` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_BINK_MOVIE_VOLUME(int binkMovie, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BINK_MOVIE_VOLUME, binkMovie, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** binkMovie: Is return value from _SET_BINK_MOVIE. Has something to do with bink volume? (audRequestedSettings::SetVolumeCurveScale)

---

### ATTACH_TV_AUDIO_TO_ENTITY

**Hash:** `0x845BAD77CC770633` | **JHash:** `0x784944DB` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_TV_AUDIO_TO_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_TV_AUDIO_TO_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_BINK_MOVIE_AUDIO_FRONTEND

**Hash:** `0xF816F2933752322D` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_BINK_MOVIE_AUDIO_FRONTEND(int binkMovie, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BINK_MOVIE_AUDIO_FRONTEND, binkMovie, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TV_AUDIO_FRONTEND

**Hash:** `0x113D2C5DC57E1774` | **JHash:** `0x2E0DFA35` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_AUDIO_FRONTEND(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_AUDIO_FRONTEND, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Probably changes tvs from being a 3d audio to being "global" audio

---

### SET_BINK_SHOULD_SKIP

**Hash:** `0x6805D58CAA427B72` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_BINK_SHOULD_SKIP(int binkMovie, BOOL bShouldSkip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BINK_SHOULD_SKIP, binkMovie, bShouldSkip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `binkMovie` | `int` | `int` | |
| `bShouldSkip` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOAD_MOVIE_MESH_SET

**Hash:** `0xB66064452270E8F1` | **JHash:** `0x9627905C` | **Build:** `323`

**C++ 原型:**
```cpp
int LOAD_MOVIE_MESH_SET(const char* movieMeshSetName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LOAD_MOVIE_MESH_SET, movieMeshSetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `movieMeshSetName` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### RELEASE_MOVIE_MESH_SET

**Hash:** `0xEB119AA014E89183` | **JHash:** `0x4FA5501D` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_MOVIE_MESH_SET(int movieMeshSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_MOVIE_MESH_SET, movieMeshSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `movieMeshSet` | `int` | `int` | |

**返回值:** `void` → `void`

---

### QUERY_MOVIE_MESH_SET_STATE

**Hash:** `0x9B6E70C5CEEF4EEB` | **JHash:** `0x9D5D9B38` | **Build:** `323`

**C++ 原型:**
```cpp
int QUERY_MOVIE_MESH_SET_STATE(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.QUERY_MOVIE_MESH_SET_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### GET_SCREEN_RESOLUTION

**Hash:** `0x888D57E407E63624` | **JHash:** `0x29F3572F` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_SCREEN_RESOLUTION(int* x, int* y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SCREEN_RESOLUTION, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `int*` | `int*` | |
| `y` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** int screenresx,screenresy;
GET_SCREEN_RESOLUTION(&screenresx,&screenresy);

---

### GET_ACTUAL_SCREEN_RESOLUTION

**Hash:** `0x873C9F3104101DD3` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_ACTUAL_SCREEN_RESOLUTION(int* x, int* y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_ACTUAL_SCREEN_RESOLUTION, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `int*` | `int*` | |
| `y` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Returns current screen resolution.

---

### GET_ASPECT_RATIO

**Hash:** `0xF1307EF624A80D87` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ASPECT_RATIO(BOOL b)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ASPECT_RATIO, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `b` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

---

### GET_SCREEN_ASPECT_RATIO

**Hash:** `0xB2EBE8CBC58B90E9` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SCREEN_ASPECT_RATIO(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SCREEN_ASPECT_RATIO);
```

**返回值:** `float` → `float`

---

### GET_IS_WIDESCREEN

**Hash:** `0x30CF4BDA4FCB1905` | **JHash:** `0xEC717AEF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_WIDESCREEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_WIDESCREEN);
```

**返回值:** `BOOL` → `bool`

**说明:** Setting Aspect Ratio Manually in game will return:

false - for Narrow format Aspect Ratios (3:2, 4:3, 5:4, etc. )
true - for Wide format Aspect Ratios (5:3, 16:9, 16:10, etc. )

Setting Aspect Ratio to "Auto" in game will return "false" or "true" based on the actual set Resolution Ratio.

---

### GET_IS_HIDEF

**Hash:** `0x84ED31191CC5D2C9` | **JHash:** `0x1C340359` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_HIDEF(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_HIDEF);
```

**返回值:** `BOOL` → `bool`

**说明:** false = Any resolution < 1280x720
true = Any resolution >= 1280x720

---

### ADJUST_NEXT_POS_SIZE_AS_NORMALIZED_16_9

**Hash:** `0xEFABC7722293DA7C` | **Build:** `323`

**C++ 原型:**
```cpp
void ADJUST_NEXT_POS_SIZE_AS_NORMALIZED_16_9(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADJUST_NEXT_POS_SIZE_AS_NORMALIZED_16_9);
```

**返回值:** `void` → `void`

---

### SET_NIGHTVISION

**Hash:** `0x18F621F7A5B1F85D` | **JHash:** `0xD1E5565F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NIGHTVISION(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NIGHTVISION, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables Night Vision.

Example:
C#: Function.Call(Hash.SET_NIGHTVISION, true);
C++: GRAPHICS::SET_NIGHTVISION(true);

BOOL toggle:
true = turns night vision on for your player.
false = turns night vision off for your player.

---

### GET_REQUESTINGNIGHTVISION

**Hash:** `0x35FB78DC42B7BD21` | **JHash:** `0xF3A6309E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_REQUESTINGNIGHTVISION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_REQUESTINGNIGHTVISION);
```

**返回值:** `BOOL` → `bool`

---

### GET_USINGNIGHTVISION

**Hash:** `0x2202A3F42C8E5F79` | **JHash:** `0x62619061` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_USINGNIGHTVISION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_USINGNIGHTVISION);
```

**返回值:** `BOOL` → `bool`

---

### SET_EXPOSURETWEAK

**Hash:** `0xEF398BEEE4EF45F9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EXPOSURETWEAK(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EXPOSURETWEAK, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_EXPOSURE_READBACK

**Hash:** `0x814AF7DCAACC597B` | **Build:** `372`

**C++ 原型:**
```cpp
void FORCE_EXPOSURE_READBACK(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_EXPOSURE_READBACK, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### OVERRIDE_NIGHTVISION_LIGHT_RANGE

**Hash:** `0x43FA7CBE20DAB219` | **Build:** `1290`

**C++ 原型:**
```cpp
void OVERRIDE_NIGHTVISION_LIGHT_RANGE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_NIGHTVISION_LIGHT_RANGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_NOISEOVERIDE

**Hash:** `0xE787BF1C5CF823C9` | **JHash:** `0xD576F5DD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NOISEOVERIDE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NOISEOVERIDE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NOISINESSOVERIDE

**Hash:** `0xCB6A7C3BB17A0C67` | **JHash:** `0x046B62D9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NOISINESSOVERIDE(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NOISINESSOVERIDE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_SCREEN_COORD_FROM_WORLD_COORD

**Hash:** `0x34E82F05DF2974F5` | **JHash:** `0x1F950E4B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_SCREEN_COORD_FROM_WORLD_COORD(float worldX, float worldY, float worldZ, float* screenX, float* screenY)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SCREEN_COORD_FROM_WORLD_COORD, worldX, worldY, worldZ, screenX, screenY);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `worldX` | `float` | `float` | |
| `worldY` | `float` | `float` | |
| `worldZ` | `float` | `float` | |
| `screenX` | `float*` | `float*` | |
| `screenY` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

**说明:** Convert a world coordinate into its relative screen coordinate.  (WorldToScreen)

Returns a boolean; whether or not the operation was successful. It will return false if the coordinates given are not visible to the rendering camera.


For .NET users...

VB:
Public Shared Function World3DToScreen2d(pos as vector3) As Vector2

        Dim x2dp, y2dp As New Native.OutputArgument

        Native.Function.Call(Of Boolean)(Native.Hash.GET_SCREEN_COORD_FROM_WORLD_COORD , pos.x, pos.y, pos.z, x2dp, y2dp)
        Return New Vector2(x2dp.GetResult(Of Single), y2dp.GetResult(Of Single))
      
    End Function

C#:
Vector2 World3DToScreen2d(Vector3 pos)
    {
        var x2dp = new OutputArgument();
        var y2dp = new OutputArgument();

        Function.Call<bool>(Hash.GET_SCREEN_COORD_FROM_WORLD_COORD , pos.X, pos.Y, pos.Z, x2dp, y2dp);
        return new Vector2(x2dp.GetResult<float>(), y2dp.GetResult<float>());
    }
//USE VERY SMALL VALUES FOR THE SCALE OF RECTS/TEXT because it is dramatically larger on screen than in 3D, e.g '0.05' small.

Used to be called _WORLD3D_TO_SCREEN2D

I thought we lost you from the scene forever. It does seem however that calling SET_DRAW_ORIGIN then your natives, then ending it. Seems to work better for certain things such as keeping boxes around people for a predator missile e.g.

---

### GET_TEXTURE_RESOLUTION

**Hash:** `0x35736EE65BD00C11` | **JHash:** `0x096DAA4D` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_TEXTURE_RESOLUTION(const char* textureDict, const char* textureName)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_TEXTURE_RESOLUTION, textureDict, textureName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Returns the texture resolution of the passed texture dict+name.

Note: Most texture resolutions are doubled compared to the console version of the game.

---

### OVERRIDE_PED_CREW_LOGO_TEXTURE

**Hash:** `0x95EB5E34F821BABE` | **Build:** `877`

**C++ 原型:**
```cpp
BOOL OVERRIDE_PED_CREW_LOGO_TEXTURE(Ped ped, const char* txd, const char* txn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.OVERRIDE_PED_CREW_LOGO_TEXTURE, ped, txd, txn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `txd` | `const char*` | `string` | |
| `txn` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Overriding ped badge texture to a passed texture. It's synced between players (even custom textures!), don't forget to request used dict on *all* clients to make it sync properly. Can be removed by passing empty strings.

---

### SET_DISTANCE_BLUR_STRENGTH_OVERRIDE

**Hash:** `0xE2892E7E55D7073A` | **JHash:** `0x455F1084` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISTANCE_BLUR_STRENGTH_OVERRIDE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISTANCE_BLUR_STRENGTH_OVERRIDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLASH

**Hash:** `0x0AB84296FED9CFC6` | **JHash:** `0x7E55A1EE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLASH(float p0, float p1, float fadeIn, float duration, float fadeOut)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLASH, p0, p1, fadeIn, duration, fadeOut);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `fadeIn` | `float` | `float` | |
| `duration` | `float` | `float` | |
| `fadeOut` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Purpose of p0 and p1 unknown.

---

### DISABLE_OCCLUSION_THIS_FRAME

**Hash:** `0x3669F1B198DCAA4F` | **JHash:** `0x0DCC0B8B` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_OCCLUSION_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_OCCLUSION_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_ARTIFICIAL_LIGHTS_STATE

**Hash:** `0x1268615ACE24D504` | **JHash:** `0xAA2A0EAF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ARTIFICIAL_LIGHTS_STATE(BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ARTIFICIAL_LIGHTS_STATE, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does not affect weapons, particles, fire/explosions, flashlights or the sun.
When set to true, all emissive textures (including ped components that have light effects), street lights, building lights, vehicle lights, etc will all be turned off.

Used in Humane Labs Heist for EMP.

state: True turns off all artificial light sources in the map: buildings, street lights, car lights, etc. False turns them back on.

---

### SET_ARTIFICIAL_VEHICLE_LIGHTS_STATE

**Hash:** `0xE2B187C0939B3D32` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_ARTIFICIAL_VEHICLE_LIGHTS_STATE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ARTIFICIAL_VEHICLE_LIGHTS_STATE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If "blackout" is enabled, this native allows you to ignore "blackout" for vehicles.

---

### DISABLE_HDTEX_THIS_FRAME

**Hash:** `0xC35A6D07C93802B2` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_HDTEX_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_HDTEX_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### CREATE_TRACKED_POINT

**Hash:** `0xE2C9439ED45DEA60` | **JHash:** `0x3129C31A` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_TRACKED_POINT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_TRACKED_POINT);
```

**返回值:** `int` → `int`

**说明:** Creates a tracked point, useful for checking the visibility of a 3D point on screen.

---

### SET_TRACKED_POINT_INFO

**Hash:** `0x164ECBB3CF750CB0` | **JHash:** `0x28689AA4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRACKED_POINT_INFO(int point, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRACKED_POINT_INFO, point, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `point` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_TRACKED_POINT_VISIBLE

**Hash:** `0xC45CCDAAC9221CA8` | **JHash:** `0x0BFC4F64` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TRACKED_POINT_VISIBLE(int point)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TRACKED_POINT_VISIBLE, point);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `point` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DESTROY_TRACKED_POINT

**Hash:** `0xB25DC90BAD56CA42` | **JHash:** `0x14AC675F` | **Build:** `323`

**C++ 原型:**
```cpp
void DESTROY_TRACKED_POINT(int point)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DESTROY_TRACKED_POINT, point);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `point` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_GRASS_CULL_SPHERE

**Hash:** `0xBE197EAA669238F4` | **Build:** `323`

**C++ 原型:**
```cpp
int SET_GRASS_CULL_SPHERE(float p0, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_GRASS_CULL_SPHERE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 0.

---

### REMOVE_GRASS_CULL_SPHERE

**Hash:** `0x61F95E5BB3E0A8C6` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_GRASS_CULL_SPHERE(int handle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_GRASS_CULL_SPHERE, handle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `handle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### PROCGRASS_ENABLE_CULLSPHERE

**Hash:** `0xAE51BC858F32BA66` | **Build:** `323`

**C++ 原型:**
```cpp
void PROCGRASS_ENABLE_CULLSPHERE(int handle, float x, float y, float z, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PROCGRASS_ENABLE_CULLSPHERE, handle, x, y, z, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `handle` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PROCGRASS_DISABLE_CULLSPHERE

**Hash:** `0x649C97D52332341A` | **Build:** `323`

**C++ 原型:**
```cpp
void PROCGRASS_DISABLE_CULLSPHERE(int handle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PROCGRASS_DISABLE_CULLSPHERE, handle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `handle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PROCGRASS_IS_CULLSPHERE_ENABLED

**Hash:** `0x2C42340F916C5930` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PROCGRASS_IS_CULLSPHERE_ENABLED(int handle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PROCGRASS_IS_CULLSPHERE_ENABLED, handle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `handle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PROCGRASS_ENABLE_AMBSCALESCAN

**Hash:** `0x14FC5833464340A8` | **Build:** `323`

**C++ 原型:**
```cpp
void PROCGRASS_ENABLE_AMBSCALESCAN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PROCGRASS_ENABLE_AMBSCALESCAN);
```

**返回值:** `void` → `void`

---

### PROCGRASS_DISABLE_AMBSCALESCAN

**Hash:** `0x0218BA067D249DEA` | **Build:** `323`

**C++ 原型:**
```cpp
void PROCGRASS_DISABLE_AMBSCALESCAN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PROCGRASS_DISABLE_AMBSCALESCAN);
```

**返回值:** `void` → `void`

---

### DISABLE_PROCOBJ_CREATION

**Hash:** `0x1612C45F9E3E0D44` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PROCOBJ_CREATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PROCOBJ_CREATION);
```

**返回值:** `void` → `void`

---

### ENABLE_PROCOBJ_CREATION

**Hash:** `0x5DEBD9C4DC995692` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_PROCOBJ_CREATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_PROCOBJ_CREATION);
```

**返回值:** `void` → `void`

---

### GRASSBATCH_ENABLE_FLATTENING_EXT_IN_SPHERE

**Hash:** `0xAAE9BE70EC7C69AB` | **Build:** `1290`

**C++ 原型:**
```cpp
void GRASSBATCH_ENABLE_FLATTENING_EXT_IN_SPHERE(float x, float y, float z, Any p3, float p4, float p5, float p6, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GRASSBATCH_ENABLE_FLATTENING_EXT_IN_SPHERE, x, y, z, p3, p4, p5, p6, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `Any` | `object` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GRASSBATCH_ENABLE_FLATTENING_IN_SPHERE

**Hash:** `0x6D955F6A9E0295B1` | **Build:** `323`

**C++ 原型:**
```cpp
void GRASSBATCH_ENABLE_FLATTENING_IN_SPHERE(float x, float y, float z, float radius, float p4, float p5, float p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GRASSBATCH_ENABLE_FLATTENING_IN_SPHERE, x, y, z, radius, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Wraps 0xAAE9BE70EC7C69AB with FLT_MAX as p7

---

### GRASSBATCH_DISABLE_FLATTENING

**Hash:** `0x302C91AB2D477F7E` | **Build:** `323`

**C++ 原型:**
```cpp
void GRASSBATCH_DISABLE_FLATTENING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GRASSBATCH_DISABLE_FLATTENING);
```

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_INIT_SESSION

**Hash:** `0x03FC694AE06C5A20` | **JHash:** `0x48F16186` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_INIT_SESSION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_INIT_SESSION);
```

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_CASCADE_BOUNDS

**Hash:** `0xD2936CAB8B58FCBD` | **JHash:** `0x84F05943` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_CASCADE_BOUNDS(Any p0, BOOL p1, float p2, float p3, float p4, float p5, BOOL p6, float p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_CASCADE_BOUNDS, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_CASCADE_BOUNDS_SCALE

**Hash:** `0x5F0F3F56635809EF` | **JHash:** `0x13D4ABC0` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_CASCADE_BOUNDS_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_CASCADE_BOUNDS_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_ENTITY_TRACKER_SCALE

**Hash:** `0x5E9DAF5A20F15908` | **JHash:** `0xD2157428` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_ENTITY_TRACKER_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_ENTITY_TRACKER_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_SPLIT_Z_EXP_WEIGHT

**Hash:** `0x36F6626459D91457` | **JHash:** `0xC07C64C9` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_SPLIT_Z_EXP_WEIGHT(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_SPLIT_Z_EXP_WEIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_BOUND_POSITION

**Hash:** `0x259BA6D4E6F808F1` | **Build:** `1011`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_BOUND_POSITION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_BOUND_POSITION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_ENABLE_ENTITY_TRACKER

**Hash:** `0x80ECBC0C856D3B0B` | **JHash:** `0xFE903D0F` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_ENABLE_ENTITY_TRACKER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_ENABLE_ENTITY_TRACKER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When this is set to ON, shadows only draw as you get nearer.

When OFF, they draw from a further distance.

---

### CASCADE_SHADOWS_SET_SCREEN_SIZE_CHECK_ENABLED

**Hash:** `0x25FC3E33A31AD0C9` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_SCREEN_SIZE_CHECK_ENABLED(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_SCREEN_SIZE_CHECK_ENABLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_SHADOW_SAMPLE_TYPE

**Hash:** `0xB11D94BC55F41932` | **JHash:** `0xDE10BA1F` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_SHADOW_SAMPLE_TYPE(const char* type)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_SHADOW_SAMPLE_TYPE, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Possible values:
"CSM_ST_POINT"
"CSM_ST_LINEAR"
"CSM_ST_TWOTAP"
"CSM_ST_BOX3x3"
"CSM_ST_BOX4x4"
"CSM_ST_DITHER2_LINEAR"
"CSM_ST_CUBIC"
"CSM_ST_DITHER4"
"CSM_ST_DITHER16"
"CSM_ST_SOFT16"
"CSM_ST_DITHER16_RPDB"
"CSM_ST_POISSON16_RPDB_GNORM"
"CSM_ST_HIGHRES_BOX4x4"
"CSM_ST_CLOUDS_SIMPLE"
"CSM_ST_CLOUDS_LINEAR"
"CSM_ST_CLOUDS_TWOTAP"
"CSM_ST_CLOUDS_BOX3x3"
"CSM_ST_CLOUDS_BOX4x4"
"CSM_ST_CLOUDS_DITHER2_LINEAR"
"CSM_ST_CLOUDS_SOFT16"
"CSM_ST_CLOUDS_DITHER16_RPDB"
"CSM_ST_CLOUDS_POISSON16_RPDB_GNORM"

---

### CASCADE_SHADOWS_CLEAR_SHADOW_SAMPLE_TYPE

**Hash:** `0x27CB772218215325` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_CLEAR_SHADOW_SAMPLE_TYPE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_CLEAR_SHADOW_SAMPLE_TYPE);
```

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_AIRCRAFT_MODE

**Hash:** `0x6DDBF9DFFC4AC080` | **JHash:** `0x9F470BE3` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_AIRCRAFT_MODE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_AIRCRAFT_MODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_MODE

**Hash:** `0xD39D13C9FEBF0511` | **JHash:** `0x4A124267` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_MODE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_MODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_VALUE

**Hash:** `0x02AC28F3A01FA04A` | **JHash:** `0xB19B2764` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_VALUE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_SET_DYNAMIC_DEPTH_VALUE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CASCADE_SHADOWS_ENABLE_FREEZER

**Hash:** `0x0AE73D8DF3A762B2` | **JHash:** `0x342FA2B4` | **Build:** `323`

**C++ 原型:**
```cpp
void CASCADE_SHADOWS_ENABLE_FREEZER(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CASCADE_SHADOWS_ENABLE_FREEZER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WATER_REFLECTION_SET_SCRIPT_OBJECT_VISIBILITY

**Hash:** `0xCA465D9CC0D231BA` | **Build:** `1011`

**C++ 原型:**
```cpp
void WATER_REFLECTION_SET_SCRIPT_OBJECT_VISIBILITY(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_REFLECTION_SET_SCRIPT_OBJECT_VISIBILITY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_ENABLED

**Hash:** `0xA51C4B86B71652AE` | **JHash:** `0x5D3BFFC9` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_ENABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_ENABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_PATH

**Hash:** `0x312342E1A4874F3F` | **JHash:** `0xD9653728` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_PATH(float p0, float p1, float p2, float p3, float p4, float p5, float p6, float p7, BOOL p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_PATH, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p8 seems to always be false.

---

### GOLF_TRAIL_SET_RADIUS

**Hash:** `0x2485D34E50A22E84` | **JHash:** `0x72BA8A14` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_RADIUS(float p0, float p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_RADIUS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_COLOUR

**Hash:** `0x12995F2E53FFA601` | **JHash:** `0x804F444C` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_COLOUR(int p0, int p1, int p2, int p3, int p4, int p5, int p6, int p7, int p8, int p9, int p10, int p11)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_COLOUR, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `int` | `int` | |
| `p7` | `int` | `int` | |
| `p8` | `int` | `int` | |
| `p9` | `int` | `int` | |
| `p10` | `int` | `int` | |
| `p11` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_TESSELLATION

**Hash:** `0xDBAA5EC848BA2D46` | **JHash:** `0xBB1A1294` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_TESSELLATION(int p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_TESSELLATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_FIXED_CONTROL_POINT_ENABLE

**Hash:** `0xC0416B061F2B7E5E` | **JHash:** `0x1A1A72EF` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_FIXED_CONTROL_POINT_ENABLE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_FIXED_CONTROL_POINT_ENABLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_SET_FIXED_CONTROL_POINT

**Hash:** `0xB1BB03742917A5D6` | **JHash:** `0x3BB12B75` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_FIXED_CONTROL_POINT(int type, float xPos, float yPos, float zPos, float p4, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_FIXED_CONTROL_POINT, type, xPos, yPos, zPos, p4, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 12 matches across 4 scripts. All 4 scripts were job creators.

type ranged from 0 - 2.
p4 was always 0.2f. Likely scale.
assuming p5 - p8 is RGBA, the graphic is always yellow (255, 255, 0, 255).

Tested but noticed nothing.

---

### GOLF_TRAIL_SET_SHADER_PARAMS

**Hash:** `0x9CFDD90B2B844BF7` | **JHash:** `0x4EA70FB4` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_SHADER_PARAMS(float p0, float p1, float p2, float p3, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_SHADER_PARAMS, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only appeared in Golf & Golf_mp. Parameters were all ptrs

---

### GOLF_TRAIL_SET_FACING

**Hash:** `0x06F761EA47C1D3ED` | **JHash:** `0x0D830DC7` | **Build:** `323`

**C++ 原型:**
```cpp
void GOLF_TRAIL_SET_FACING(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GOLF_TRAIL_SET_FACING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GOLF_TRAIL_GET_MAX_HEIGHT

**Hash:** `0xA4819F5E23E2FFAD` | **JHash:** `0xA08B46AD` | **Build:** `323`

**C++ 原型:**
```cpp
float GOLF_TRAIL_GET_MAX_HEIGHT(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GOLF_TRAIL_GET_MAX_HEIGHT);
```

**返回值:** `float` → `float`

---

### GOLF_TRAIL_GET_VISUAL_CONTROL_POINT

**Hash:** `0xA4664972A9B8F8BA` | **JHash:** `0xECD470F0` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GOLF_TRAIL_GET_VISUAL_CONTROL_POINT(int p0)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GOLF_TRAIL_GET_VISUAL_CONTROL_POINT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### SET_SEETHROUGH

**Hash:** `0x7E08924259E08CE0` | **JHash:** `0x74D4995C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SEETHROUGH(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SEETHROUGH, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles Heatvision on/off.

---

### GET_USINGSEETHROUGH

**Hash:** `0x44B80ABAB9D80BD3` | **JHash:** `0x1FE547F2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_USINGSEETHROUGH(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_USINGSEETHROUGH);
```

**返回值:** `BOOL` → `bool`

---

### SEETHROUGH_RESET

**Hash:** `0x70A64C0234EF522C` | **JHash:** `0x310E9B67` | **Build:** `323`

**C++ 原型:**
```cpp
void SEETHROUGH_RESET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_RESET);
```

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_FADE_STARTDISTANCE

**Hash:** `0xA78DE25577300BA1` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_FADE_STARTDISTANCE(float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_FADE_STARTDISTANCE, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_FADE_ENDDISTANCE

**Hash:** `0x9D75795B9DC6EBBF` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_FADE_ENDDISTANCE(float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_FADE_ENDDISTANCE, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_GET_MAX_THICKNESS

**Hash:** `0x43DBAE39626CE83F` | **Build:** `1290`

**C++ 原型:**
```cpp
float SEETHROUGH_GET_MAX_THICKNESS(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.SEETHROUGH_GET_MAX_THICKNESS);
```

**返回值:** `float` → `float`

---

### SEETHROUGH_SET_MAX_THICKNESS

**Hash:** `0x0C8FAC83902A62DF` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_MAX_THICKNESS(float thickness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_MAX_THICKNESS, thickness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `thickness` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 0.0 = you will not be able to see people behind the walls. 50.0 and more = you will see everyone through the walls. More value is "better" view. See https://gfycat.com/FirmFlippantGourami
min: 1.0
max: 10000.0

---

### SEETHROUGH_SET_NOISE_MIN

**Hash:** `0xFF5992E1C9E65D05` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_NOISE_MIN(float amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_NOISE_MIN, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_NOISE_MAX

**Hash:** `0xFEBFBFDFB66039DE` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_NOISE_MAX(float amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_NOISE_MAX, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_HILIGHT_INTENSITY

**Hash:** `0x19E50EB6E33E1D28` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_HILIGHT_INTENSITY(float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_HILIGHT_INTENSITY, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_HIGHLIGHT_NOISE

**Hash:** `0x1636D7FC127B10D2` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_HIGHLIGHT_NOISE(float noise)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_HIGHLIGHT_NOISE, noise);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `noise` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SEETHROUGH_SET_HEATSCALE

**Hash:** `0xD7D0B00177485411` | **JHash:** `0x654F0287` | **Build:** `323`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_HEATSCALE(int index, float heatScale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_HEATSCALE, index, heatScale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `heatScale` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** min: 0.0
max: 0.75

---

### SEETHROUGH_SET_COLOR_NEAR

**Hash:** `0x1086127B3A63505E` | **JHash:** `0x5B2A67A8` | **Build:** `573`

**C++ 原型:**
```cpp
void SEETHROUGH_SET_COLOR_NEAR(int red, int green, int blue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEETHROUGH_SET_COLOR_NEAR, red, green, blue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_MOTIONBLUR_MAX_VEL_SCALER

**Hash:** `0xB3C641F3630BF6DA` | **JHash:** `0xF6B837F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOTIONBLUR_MAX_VEL_SCALER(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOTIONBLUR_MAX_VEL_SCALER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Setter for GET_MOTIONBLUR_MAX_VEL_SCALER

---

### GET_MOTIONBLUR_MAX_VEL_SCALER

**Hash:** `0xE59343E9E96529E7` | **JHash:** `0xD906A3A9` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_MOTIONBLUR_MAX_VEL_SCALER(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_MOTIONBLUR_MAX_VEL_SCALER);
```

**返回值:** `float` → `float`

**说明:** Getter for SET_MOTIONBLUR_MAX_VEL_SCALER

---

### SET_FORCE_MOTIONBLUR

**Hash:** `0x6A51F78772175A51` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_FORCE_MOTIONBLUR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_MOTIONBLUR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TOGGLE_PLAYER_DAMAGE_OVERLAY

**Hash:** `0xE63D7C6EECECB66B` | **JHash:** `0xD34A6CBA` | **Build:** `323`

**C++ 原型:**
```cpp
void TOGGLE_PLAYER_DAMAGE_OVERLAY(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_PLAYER_DAMAGE_OVERLAY, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RESET_ADAPTATION

**Hash:** `0xE3E2C1B4C59DBC77` | **JHash:** `0xD8CC7221` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_ADAPTATION(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_ADAPTATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets an value related to timecycles.

---

### TRIGGER_SCREENBLUR_FADE_IN

**Hash:** `0xA328A24AAA6B7FDC` | **JHash:** `0x5604B890` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TRIGGER_SCREENBLUR_FADE_IN(float transitionTime)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TRIGGER_SCREENBLUR_FADE_IN, transitionTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transitionTime` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** time in ms to transition to fully blurred screen

---

### TRIGGER_SCREENBLUR_FADE_OUT

**Hash:** `0xEFACC8AEF94430D5` | **JHash:** `0x46617502` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TRIGGER_SCREENBLUR_FADE_OUT(float transitionTime)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TRIGGER_SCREENBLUR_FADE_OUT, transitionTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transitionTime` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** time in ms to transition from fully blurred to normal

---

### DISABLE_SCREENBLUR_FADE

**Hash:** `0xDE81239437E8C5A8` | **JHash:** `0xDB7AECDA` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_SCREENBLUR_FADE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_SCREENBLUR_FADE);
```

**返回值:** `void` → `void`

---

### GET_SCREENBLUR_FADE_CURRENT_TIME

**Hash:** `0x5CCABFFCA31DDE33` | **JHash:** `0xEA432A94` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SCREENBLUR_FADE_CURRENT_TIME(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SCREENBLUR_FADE_CURRENT_TIME);
```

**返回值:** `float` → `float`

---

### IS_SCREENBLUR_FADE_RUNNING

**Hash:** `0x7B226C785A52A0A9` | **JHash:** `0x926B8734` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCREENBLUR_FADE_RUNNING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCREENBLUR_FADE_RUNNING);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns whether screen transition to blur/from blur is running.

---

### TOGGLE_PAUSED_RENDERPHASES

**Hash:** `0xDFC252D8A3E15AB7` | **JHash:** `0x30ADE541` | **Build:** `323`

**C++ 原型:**
```cpp
void TOGGLE_PAUSED_RENDERPHASES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_PAUSED_RENDERPHASES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_TOGGLE_PAUSED_RENDERPHASES_STATUS

**Hash:** `0xEB3DAC2C86001E5E` | **JHash:** `0xD4F5D07D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TOGGLE_PAUSED_RENDERPHASES_STATUS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TOGGLE_PAUSED_RENDERPHASES_STATUS);
```

**返回值:** `BOOL` → `bool`

---

### RESET_PAUSED_RENDERPHASES

**Hash:** `0xE1C8709406F2C41C` | **JHash:** `0x0113EAE4` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PAUSED_RENDERPHASES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PAUSED_RENDERPHASES);
```

**返回值:** `void` → `void`

---

### GRAB_PAUSEMENU_OWNERSHIP

**Hash:** `0x851CD923176EBA7C` | **JHash:** `0xDCBA251B` | **Build:** `323`

**C++ 原型:**
```cpp
void GRAB_PAUSEMENU_OWNERSHIP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GRAB_PAUSEMENU_OWNERSHIP);
```

**返回值:** `void` → `void`

---

### SET_HIDOF_OVERRIDE

**Hash:** `0xBA3D65906822BED5` | **JHash:** `0x513D444B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HIDOF_OVERRIDE(BOOL p0, BOOL p1, float nearplaneOut, float nearplaneIn, float farplaneOut, float farplaneIn)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HIDOF_OVERRIDE, p0, p1, nearplaneOut, nearplaneIn, farplaneOut, farplaneIn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `nearplaneOut` | `float` | `float` | |
| `nearplaneIn` | `float` | `float` | |
| `farplaneOut` | `float` | `float` | |
| `farplaneIn` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_LOCK_ADAPTIVE_DOF_DISTANCE

**Hash:** `0xB569F41F3E7E83A4` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_LOCK_ADAPTIVE_DOF_DISTANCE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCK_ADAPTIVE_DOF_DISTANCE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PHONEPHOTOEDITOR_TOGGLE

**Hash:** `0x7AC24EAB6D74118D` | **JHash:** `0xB2410EAB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PHONEPHOTOEDITOR_TOGGLE(BOOL p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PHONEPHOTOEDITOR_TOGGLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### PHONEPHOTOEDITOR_IS_ACTIVE

**Hash:** `0xBCEDB009461DA156` | **JHash:** `0x5AB94128` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PHONEPHOTOEDITOR_IS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PHONEPHOTOEDITOR_IS_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### PHONEPHOTOEDITOR_SET_FRAME_TXD

**Hash:** `0x27FEB5254759CDE3` | **JHash:** `0xD63FCB3E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PHONEPHOTOEDITOR_SET_FRAME_TXD(const char* textureDict, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PHONEPHOTOEDITOR_SET_FRAME_TXD, textureDict, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### START_PARTICLE_FX_NON_LOOPED_AT_COORD

**Hash:** `0x25129531F77B9ED3` | **JHash:** `0xDD79D679` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_PARTICLE_FX_NON_LOOPED_AT_COORD(const char* effectName, float xPos, float yPos, float zPos, float xRot, float yRot, float zRot, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_PARTICLE_FX_NON_LOOPED_AT_COORD, effectName, xPos, yPos, zPos, xRot, yRot, zRot, scale, xAxis, yAxis, zAxis);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** GRAPHICS::START_PARTICLE_FX_NON_LOOPED_AT_COORD("scr_paleto_roof_impact", -140.8576f, 6420.789f, 41.1391f, 0f, 0f, 267.3957f, 0x3F800000, 0, 0, 0);

Axis - Invert Axis Flags

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json


-------------------------------------------------------------------
C#

Function.Call<int>(Hash.START_PARTICLE_FX_NON_LOOPED_AT_COORD, = you are calling this function.

char *effectname = This is an in-game effect name, for e.g. "scr_fbi4_trucks_crash" is used to give the effects when truck crashes etc

float x, y, z pos = this one is Simple, you just have to declare, where do you want this effect to take place at, so declare the ordinates

float xrot, yrot, zrot = Again simple? just mention the value in case if you want the effect to rotate.

float scale = is declare the scale of the effect, this may vary as per the effects for e.g 1.0f

bool xaxis, yaxis, zaxis = To bool the axis values.

example:
Function.Call<int>(Hash.START_PARTICLE_FX_NON_LOOPED_AT_COORD, "scr_fbi4_trucks_crash", GTA.Game.Player.Character.Position.X, GTA.Game.Player.Character.Position.Y, GTA.Game.Player.Character.Position.Z + 4f, 0, 0, 0, 5.5f, 0, 0, 0);

---

### START_NETWORKED_PARTICLE_FX_NON_LOOPED_AT_COORD

**Hash:** `0xF56B8137DF10135D` | **JHash:** `0x633F8C48` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_NETWORKED_PARTICLE_FX_NON_LOOPED_AT_COORD(const char* effectName, float xPos, float yPos, float zPos, float xRot, float yRot, float zRot, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis, BOOL p11)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_NETWORKED_PARTICLE_FX_NON_LOOPED_AT_COORD, effectName, xPos, yPos, zPos, xRot, yRot, zRot, scale, xAxis, yAxis, zAxis, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |
| `p11` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE

**Hash:** `0x0E7E72961BA18619` | **JHash:** `0x53DAEF4E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE(const char* effectName, Ped ped, float offsetX, float offsetY, float offsetZ, float rotX, float rotY, float rotZ, int boneIndex, float scale, BOOL axisX, BOOL axisY, BOOL axisZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE, effectName, ped, offsetX, offsetY, offsetZ, rotX, rotY, rotZ, boneIndex, scale, axisX, axisY, axisZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `axisX` | `BOOL` | `bool` | |
| `axisY` | `BOOL` | `bool` | |
| `axisZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** GRAPHICS::START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE("scr_sh_bong_smoke", PLAYER::PLAYER_PED_ID(), -0.025f, 0.13f, 0f, 0f, 0f, 0f, 31086, 0x3F800000, 0, 0, 0);

Axis - Invert Axis Flags

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_PED_BONE

**Hash:** `0xA41B6A43642AC2CF` | **JHash:** `0x161780C1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_PED_BONE(const char* effectName, Ped ped, float offsetX, float offsetY, float offsetZ, float rotX, float rotY, float rotZ, int boneIndex, float scale, BOOL axisX, BOOL axisY, BOOL axisZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_PED_BONE, effectName, ped, offsetX, offsetY, offsetZ, rotX, rotY, rotZ, boneIndex, scale, axisX, axisY, axisZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `axisX` | `BOOL` | `bool` | |
| `axisY` | `BOOL` | `bool` | |
| `axisZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_NON_LOOPED_ON_ENTITY

**Hash:** `0x0D53A3B8DA0809D2` | **JHash:** `0x9604DAD4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_PARTICLE_FX_NON_LOOPED_ON_ENTITY(const char* effectName, Entity entity, float offsetX, float offsetY, float offsetZ, float rotX, float rotY, float rotZ, float scale, BOOL axisX, BOOL axisY, BOOL axisZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_PARTICLE_FX_NON_LOOPED_ON_ENTITY, effectName, entity, offsetX, offsetY, offsetZ, rotX, rotY, rotZ, scale, axisX, axisY, axisZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `axisX` | `BOOL` | `bool` | |
| `axisY` | `BOOL` | `bool` | |
| `axisZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Starts a particle effect on an entity for example your player.

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

Example:
C#:
Function.Call(Hash.REQUEST_NAMED_PTFX_ASSET, "scr_rcbarry2");                     Function.Call(Hash.USE_PARTICLE_FX_ASSET, "scr_rcbarry2");                             Function.Call(Hash.START_PARTICLE_FX_NON_LOOPED_ON_ENTITY, "scr_clown_appears", Game.Player.Character, 0.0, 0.0, -0.5, 0.0, 0.0, 0.0, 1.0, false, false, false);

Internally this calls the same function as GRAPHICS::START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE
however it uses -1 for the specified bone index, so it should be possible to start a non looped fx on an entity bone using that native

-can confirm START_PARTICLE_FX_NON_LOOPED_ON_PED_BONE does NOT work on vehicle bones.

---

### START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_ENTITY

**Hash:** `0xC95EB1DB6E92113D` | **JHash:** `0x469A2B4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_ENTITY(const char* effectName, Entity entity, float offsetX, float offsetY, float offsetZ, float rotX, float rotY, float rotZ, float scale, BOOL axisX, BOOL axisY, BOOL axisZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_NETWORKED_PARTICLE_FX_NON_LOOPED_ON_ENTITY, effectName, entity, offsetX, offsetY, offsetZ, rotX, rotY, rotZ, scale, axisX, axisY, axisZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `axisX` | `BOOL` | `bool` | |
| `axisY` | `BOOL` | `bool` | |
| `axisZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_NON_LOOPED_ON_ENTITY_BONE

**Hash:** `0x02B1F2A72E0F5325` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL START_PARTICLE_FX_NON_LOOPED_ON_ENTITY_BONE(const char* effectName, Entity entity, float offsetX, float offsetY, float offsetZ, float rotX, float rotY, float rotZ, int boneIndex, float scale, BOOL axisX, BOOL axisY, BOOL axisZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.START_PARTICLE_FX_NON_LOOPED_ON_ENTITY_BONE, effectName, entity, offsetX, offsetY, offsetZ, rotX, rotY, rotZ, boneIndex, scale, axisX, axisY, axisZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `axisX` | `BOOL` | `bool` | |
| `axisY` | `BOOL` | `bool` | |
| `axisZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### SET_PARTICLE_FX_NON_LOOPED_COLOUR

**Hash:** `0x26143A59EF48B262` | **JHash:** `0x7B689E20` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_NON_LOOPED_COLOUR(float r, float g, float b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_NON_LOOPED_COLOUR, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `r` | `float` | `float` | |
| `g` | `float` | `float` | |
| `b` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** only works on some fx's, not networked

---

### SET_PARTICLE_FX_NON_LOOPED_ALPHA

**Hash:** `0x77168D722C58B2FC` | **JHash:** `0x497EAFF2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_NON_LOOPED_ALPHA(float alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_NON_LOOPED_ALPHA, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alpha` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Usage example for C#:

Function.Call(Hash.SET_PARTICLE_FX_NON_LOOPED_ALPHA, new InputArgument[] { 0.1f });

Note: the argument alpha ranges from 0.0f-1.0f !

---

### SET_PARTICLE_FX_NON_LOOPED_SCALE

**Hash:** `0xB7EF5850C39FABCA` | **Build:** `2802`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_NON_LOOPED_SCALE(float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_NON_LOOPED_SCALE, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_NON_LOOPED_EMITTER_SIZE

**Hash:** `0x1E2E01C00837D26E` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_NON_LOOPED_EMITTER_SIZE(float p0, float p1, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_NON_LOOPED_EMITTER_SIZE, p0, p1, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_FORCE_VEHICLE_INTERIOR

**Hash:** `0x8CDE909A0370BB3A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_FORCE_VEHICLE_INTERIOR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_FORCE_VEHICLE_INTERIOR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used only once in the scripts (taxi_clowncar)

---

### START_PARTICLE_FX_LOOPED_AT_COORD

**Hash:** `0xE184F4F0DC5910E7` | **JHash:** `0xD348E3E6` | **Build:** `323`

**C++ 原型:**
```cpp
int START_PARTICLE_FX_LOOPED_AT_COORD(const char* effectName, float x, float y, float z, float xRot, float yRot, float zRot, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis, BOOL p11)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_PARTICLE_FX_LOOPED_AT_COORD, effectName, x, y, z, xRot, yRot, zRot, scale, xAxis, yAxis, zAxis, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |
| `p11` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** GRAPHICS::START_PARTICLE_FX_LOOPED_AT_COORD("scr_fbi_falling_debris", 93.7743f, -749.4572f, 70.86904f, 0f, 0f, 0f, 0x3F800000, 0, 0, 0, 0)


p11 seems to be always 0

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_LOOPED_ON_PED_BONE

**Hash:** `0xF28DA9F38CD1787C` | **JHash:** `0xF8FC196F` | **Build:** `323`

**C++ 原型:**
```cpp
int START_PARTICLE_FX_LOOPED_ON_PED_BONE(const char* effectName, Ped ped, float xOffset, float yOffset, float zOffset, float xRot, float yRot, float zRot, int boneIndex, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_PARTICLE_FX_LOOPED_ON_PED_BONE, effectName, ped, xOffset, yOffset, zOffset, xRot, yRot, zRot, boneIndex, scale, xAxis, yAxis, zAxis);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_LOOPED_ON_ENTITY

**Hash:** `0x1AE42C1660FD6517` | **JHash:** `0x0D06FF62` | **Build:** `323`

**C++ 原型:**
```cpp
int START_PARTICLE_FX_LOOPED_ON_ENTITY(const char* effectName, Entity entity, float xOffset, float yOffset, float zOffset, float xRot, float yRot, float zRot, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_PARTICLE_FX_LOOPED_ON_ENTITY, effectName, entity, xOffset, yOffset, zOffset, xRot, yRot, zRot, scale, xAxis, yAxis, zAxis);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_PARTICLE_FX_LOOPED_ON_ENTITY_BONE

**Hash:** `0xC6EB449E33977F0B` | **JHash:** `0x23BF0F9B` | **Build:** `323`

**C++ 原型:**
```cpp
int START_PARTICLE_FX_LOOPED_ON_ENTITY_BONE(const char* effectName, Entity entity, float xOffset, float yOffset, float zOffset, float xRot, float yRot, float zRot, int boneIndex, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_PARTICLE_FX_LOOPED_ON_ENTITY_BONE, effectName, entity, xOffset, yOffset, zOffset, xRot, yRot, zRot, boneIndex, scale, xAxis, yAxis, zAxis);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY

**Hash:** `0x6F60E89A7B64EE1D` | **JHash:** `0x110752B2` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY(const char* effectName, Entity entity, float xOffset, float yOffset, float zOffset, float xRot, float yRot, float zRot, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis, float r, float g, float b, float a)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY, effectName, entity, xOffset, yOffset, zOffset, xRot, yRot, zRot, scale, xAxis, yAxis, zAxis, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |
| `r` | `float` | `float` | |
| `g` | `float` | `float` | |
| `b` | `float` | `float` | |
| `a` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY_BONE

**Hash:** `0xDDE23F30CC5A0F03` | **JHash:** `0xF478EFCF` | **Build:** `323`

**C++ 原型:**
```cpp
int START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY_BONE(const char* effectName, Entity entity, float xOffset, float yOffset, float zOffset, float xRot, float yRot, float zRot, int boneIndex, float scale, BOOL xAxis, BOOL yAxis, BOOL zAxis, float r, float g, float b, float a)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_NETWORKED_PARTICLE_FX_LOOPED_ON_ENTITY_BONE, effectName, entity, xOffset, yOffset, zOffset, xRot, yRot, zRot, boneIndex, scale, xAxis, yAxis, zAxis, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `entity` | `Entity` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `boneIndex` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `xAxis` | `BOOL` | `bool` | |
| `yAxis` | `BOOL` | `bool` | |
| `zAxis` | `BOOL` | `bool` | |
| `r` | `float` | `float` | |
| `g` | `float` | `float` | |
| `b` | `float` | `float` | |
| `a` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### STOP_PARTICLE_FX_LOOPED

**Hash:** `0x8F75998877616996` | **JHash:** `0xD245455B` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PARTICLE_FX_LOOPED(int ptfxHandle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PARTICLE_FX_LOOPED, ptfxHandle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0 in the native scripts

---

### REMOVE_PARTICLE_FX

**Hash:** `0xC401503DFE8D53CF` | **JHash:** `0x6BA48C7E` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PARTICLE_FX(int ptfxHandle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PARTICLE_FX, ptfxHandle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_PARTICLE_FX_FROM_ENTITY

**Hash:** `0xB8FEAEEBCC127425` | **JHash:** `0xCEDE52E9` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PARTICLE_FX_FROM_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PARTICLE_FX_FROM_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_PARTICLE_FX_IN_RANGE

**Hash:** `0xDD19FA1C6D657305` | **JHash:** `0x7EB8F275` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PARTICLE_FX_IN_RANGE(float X, float Y, float Z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PARTICLE_FX_IN_RANGE, X, Y, Z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `X` | `float` | `float` | |
| `Y` | `float` | `float` | |
| `Z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_PARTICLE_FX_IN_VEHICLE_INTERIOR

**Hash:** `0xBA0127DA25FD54C9` | **Build:** `372`

**C++ 原型:**
```cpp
void FORCE_PARTICLE_FX_IN_VEHICLE_INTERIOR(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PARTICLE_FX_IN_VEHICLE_INTERIOR, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DOES_PARTICLE_FX_LOOPED_EXIST

**Hash:** `0x74AFEF0D2E1E409B` | **JHash:** `0xCBF91D2A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PARTICLE_FX_LOOPED_EXIST(int ptfxHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PARTICLE_FX_LOOPED_EXIST, ptfxHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PARTICLE_FX_LOOPED_OFFSETS

**Hash:** `0xF7DDEBEC43483C43` | **JHash:** `0x641F7790` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_OFFSETS(int ptfxHandle, float x, float y, float z, float rotX, float rotY, float rotZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_OFFSETS, ptfxHandle, x, y, z, rotX, rotY, rotZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_LOOPED_EVOLUTION

**Hash:** `0x5F0C4B5B1C393BE2` | **JHash:** `0x1CBC1373` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_EVOLUTION(int ptfxHandle, const char* propertyName, float amount, BOOL noNetwork)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_EVOLUTION, ptfxHandle, propertyName, amount, noNetwork);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `propertyName` | `const char*` | `string` | |
| `amount` | `float` | `float` | |
| `noNetwork` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_LOOPED_COLOUR

**Hash:** `0x7F8F65877F88783B` | **JHash:** `0x5219D530` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_COLOUR(int ptfxHandle, float r, float g, float b, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_COLOUR, ptfxHandle, r, g, b, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `r` | `float` | `float` | |
| `g` | `float` | `float` | |
| `b` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** only works on some fx's

p4 = 0

---

### SET_PARTICLE_FX_LOOPED_ALPHA

**Hash:** `0x726845132380142E` | **JHash:** `0x5ED49BE1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_ALPHA(int ptfxHandle, float alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_ALPHA, ptfxHandle, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `alpha` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_LOOPED_SCALE

**Hash:** `0xB44250AAA456492D` | **JHash:** `0x099B8B49` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_SCALE(int ptfxHandle, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_SCALE, ptfxHandle, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_LOOPED_FAR_CLIP_DIST

**Hash:** `0xDCB194B85EF7B541` | **JHash:** `0x233DE879` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_LOOPED_FAR_CLIP_DIST(int ptfxHandle, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_LOOPED_FAR_CLIP_DIST, ptfxHandle, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

---

### _SET_PARTICLE_FX_LOOPED_CAMERA_BIAS

**Hash:** `0x4100BF0346A8D2C3` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_PARTICLE_FX_LOOPED_CAMERA_BIAS(int ptfxHandle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_PARTICLE_FX_LOOPED_CAMERA_BIAS, ptfxHandle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ptfxHandle` | `int` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_CAM_INSIDE_VEHICLE

**Hash:** `0xEEC4047028426510` | **JHash:** `0x19EC0001` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_CAM_INSIDE_VEHICLE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_CAM_INSIDE_VEHICLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_CAM_INSIDE_NONPLAYER_VEHICLE

**Hash:** `0xACEE6F360FC1F6B6` | **JHash:** `0x6B125A02` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_CAM_INSIDE_NONPLAYER_VEHICLE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_CAM_INSIDE_NONPLAYER_VEHICLE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_SHOOTOUT_BOAT

**Hash:** `0x96EF97DAEB89BEF5` | **JHash:** `0xD938DEE0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_SHOOTOUT_BOAT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_SHOOTOUT_BOAT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CLEAR_PARTICLE_FX_SHOOTOUT_BOAT

**Hash:** `0x2A251AA48B2B46DB` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PARTICLE_FX_SHOOTOUT_BOAT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PARTICLE_FX_SHOOTOUT_BOAT);
```

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_BLOOD_SCALE

**Hash:** `0x908311265D42A820` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_BLOOD_SCALE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_BLOOD_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DISABLE_IN_WATER_PTFX

**Hash:** `0xCFD16F0DB5A3535C` | **Build:** `2060`

**C++ 原型:**
```cpp
void DISABLE_IN_WATER_PTFX(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_IN_WATER_PTFX, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_DOWNWASH_PTFX

**Hash:** `0x5F6DF3D92271E8A1` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_DOWNWASH_PTFX(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_DOWNWASH_PTFX, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_SLIPSTREAM_LODRANGE_SCALE

**Hash:** `0x2B40A97646381508` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_SLIPSTREAM_LODRANGE_SCALE(float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_SLIPSTREAM_LODRANGE_SCALE, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ENABLE_CLOWN_BLOOD_VFX

**Hash:** `0xD821490579791273` | **JHash:** `0xC61C75E9` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_CLOWN_BLOOD_VFX(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_CLOWN_BLOOD_VFX, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Creates cartoon effect when Michel smokes the weed

---

### ENABLE_ALIEN_BLOOD_VFX

**Hash:** `0x9DCE1F0F78260875` | **JHash:** `0xCE8B8748` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_ALIEN_BLOOD_VFX(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_ALIEN_BLOOD_VFX, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Creates a motion-blur sort of effect, this native does not seem to work, however by using the `START_SCREEN_EFFECT` native with `DrugsMichaelAliensFight` as the effect parameter, you should be able to get the effect.

---

### SET_PARTICLE_FX_BULLET_IMPACT_SCALE

**Hash:** `0x27E32866E9A5C416` | **JHash:** `0xC1AD5DDF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_BULLET_IMPACT_SCALE(float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_BULLET_IMPACT_SCALE, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_BULLET_IMPACT_LODRANGE_SCALE

**Hash:** `0xBB90E12CAC1DAB25` | **JHash:** `0x3968E915` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_BULLET_IMPACT_LODRANGE_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_BULLET_IMPACT_LODRANGE_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_BULLET_TRACE_NO_ANGLE_REJECT

**Hash:** `0xCA4AE345A153D573` | **JHash:** `0x64BA4648` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_BULLET_TRACE_NO_ANGLE_REJECT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_BULLET_TRACE_NO_ANGLE_REJECT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_BANG_SCRAPE_LODRANGE_SCALE

**Hash:** `0x54E22EA2C1956A8D` | **JHash:** `0x8BE3D47F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_BANG_SCRAPE_LODRANGE_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_BANG_SCRAPE_LODRANGE_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_FOOT_LODRANGE_SCALE

**Hash:** `0x949F397A288B28B3` | **JHash:** `0xE3880F5A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_FOOT_LODRANGE_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_FOOT_LODRANGE_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARTICLE_FX_FOOT_OVERRIDE_NAME

**Hash:** `0xBA3D194057C79A7B` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_FOOT_OVERRIDE_NAME(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_FOOT_OVERRIDE_NAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### SET_SKIDMARK_RANGE_SCALE

**Hash:** `0x5DBF05DB5926D089` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_SKIDMARK_RANGE_SCALE(float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SKIDMARK_RANGE_SCALE, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PTFX_FORCE_VEHICLE_INTERIOR_FLAG

**Hash:** `0xC6730E0D14E50703` | **Build:** `2545`

**C++ 原型:**
```cpp
void SET_PTFX_FORCE_VEHICLE_INTERIOR_FLAG(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PTFX_FORCE_VEHICLE_INTERIOR_FLAG, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REGISTER_POSTFX_BULLET_IMPACT

**Hash:** `0x170911F37F646F29` | **Build:** `2802`

**C++ 原型:**
```cpp
void REGISTER_POSTFX_BULLET_IMPACT(float weaponWorldPosX, float weaponWorldPosY, float weaponWorldPosZ, float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_POSTFX_BULLET_IMPACT, weaponWorldPosX, weaponWorldPosY, weaponWorldPosZ, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponWorldPosX` | `float` | `float` | |
| `weaponWorldPosY` | `float` | `float` | |
| `weaponWorldPosZ` | `float` | `float` | |
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_POSTFX_BULLET_IMPACTS_AFTER_HUD

**Hash:** `0x9B079E5221D984D3` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_POSTFX_BULLET_IMPACTS_AFTER_HUD(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_POSTFX_BULLET_IMPACTS_AFTER_HUD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### USE_PARTICLE_FX_ASSET

**Hash:** `0x6C38AF3693A69A91` | **JHash:** `0x9C720B61` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_PARTICLE_FX_ASSET(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_PARTICLE_FX_ASSET, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** From the b678d decompiled scripts:

 GRAPHICS::USE_PARTICLE_FX_ASSET("FM_Mission_Controler");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_apartment_mp");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_indep_fireworks");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_mp_cig_plane");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_mp_creator");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_ornate_heist");
 GRAPHICS::USE_PARTICLE_FX_ASSET("scr_prison_break_heist_station");

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### SET_PARTICLE_FX_OVERRIDE

**Hash:** `0xEA1E2D93F6F75ED9` | **JHash:** `0xC92719A7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARTICLE_FX_OVERRIDE(const char* oldAsset, const char* newAsset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARTICLE_FX_OVERRIDE, oldAsset, newAsset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `oldAsset` | `const char*` | `string` | |
| `newAsset` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### RESET_PARTICLE_FX_OVERRIDE

**Hash:** `0x89C8553DD3274AAE` | **JHash:** `0x9E8D8B72` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PARTICLE_FX_OVERRIDE(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PARTICLE_FX_OVERRIDE, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Resets the effect of SET_PARTICLE_FX_OVERRIDE

Full list of particle effect dictionaries and effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/particleEffectsCompact.json

---

### _START_VEHICLE_PARTICLE_FX_LOOPED

**Hash:** `0xDF269BE2909E181A` | **Build:** `3095`

**C++ 原型:**
```cpp
int _START_VEHICLE_PARTICLE_FX_LOOPED(Vehicle vehicle, const char* effectName, BOOL frontBack, BOOL leftRight, BOOL localOnly)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._START_VEHICLE_PARTICLE_FX_LOOPED, vehicle, effectName, frontBack, leftRight, localOnly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `effectName` | `const char*` | `string` | |
| `frontBack` | `BOOL` | `bool` | |
| `leftRight` | `BOOL` | `bool` | |
| `localOnly` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** Returns ptfxHandle
effectName: scr_sv_drag_burnout

---

### SET_WEATHER_PTFX_USE_OVERRIDE_SETTINGS

**Hash:** `0xA46B73FAA3460AE1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_PTFX_USE_OVERRIDE_SETTINGS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_PTFX_USE_OVERRIDE_SETTINGS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_WEATHER_PTFX_OVERRIDE_CURR_LEVEL

**Hash:** `0xF78B803082D4386F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_PTFX_OVERRIDE_CURR_LEVEL(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_PTFX_OVERRIDE_CURR_LEVEL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WASH_DECALS_IN_RANGE

**Hash:** `0x9C30613D50A6ADEF` | **JHash:** `0xDEECBC57` | **Build:** `323`

**C++ 原型:**
```cpp
void WASH_DECALS_IN_RANGE(float x, float y, float z, float range, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WASH_DECALS_IN_RANGE, x, y, z, range, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WASH_DECALS_FROM_VEHICLE

**Hash:** `0x5B712761429DBC14` | **JHash:** `0x2929F11A` | **Build:** `323`

**C++ 原型:**
```cpp
void WASH_DECALS_FROM_VEHICLE(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WASH_DECALS_FROM_VEHICLE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FADE_DECALS_IN_RANGE

**Hash:** `0xD77EDADB0420E6E0` | **JHash:** `0xF81E884A` | **Build:** `323`

**C++ 原型:**
```cpp
void FADE_DECALS_IN_RANGE(float x, float y, float z, float p3, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FADE_DECALS_IN_RANGE, x, y, z, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Fades nearby decals within the range specified

---

### REMOVE_DECALS_IN_RANGE

**Hash:** `0x5D6B2D4830A67C62` | **JHash:** `0x06A619A0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DECALS_IN_RANGE(float x, float y, float z, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DECALS_IN_RANGE, x, y, z, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Removes all decals in range from a position, it includes the bullet holes, blood pools, petrol...

---

### REMOVE_DECALS_FROM_OBJECT

**Hash:** `0xCCF71CBDDF5B6CB9` | **JHash:** `0x8B67DCA7` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DECALS_FROM_OBJECT(Object obj)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DECALS_FROM_OBJECT, obj);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `obj` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_DECALS_FROM_OBJECT_FACING

**Hash:** `0xA6F6F70FDC6D144C` | **JHash:** `0xF4999A55` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DECALS_FROM_OBJECT_FACING(Object obj, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DECALS_FROM_OBJECT_FACING, obj, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `obj` | `Object` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### REMOVE_DECALS_FROM_VEHICLE

**Hash:** `0xE91F1B65F2B48D57` | **JHash:** `0x831D06CA` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DECALS_FROM_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DECALS_FROM_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### ADD_DECAL

**Hash:** `0xB302244A1839BDAD` | **JHash:** `0xEAD0C412` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_DECAL(int decalType, float posX, float posY, float posZ, float p4, float p5, float p6, float p7, float p8, float p9, float width, float height, float rCoef, float gCoef, float bCoef, float opacity, float timeout, BOOL p17, BOOL p18, BOOL p19)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_DECAL, decalType, posX, posY, posZ, p4, p5, p6, p7, p8, p9, width, height, rCoef, gCoef, bCoef, opacity, timeout, p17, p18, p19);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decalType` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `rCoef` | `float` | `float` | |
| `gCoef` | `float` | `float` | |
| `bCoef` | `float` | `float` | |
| `opacity` | `float` | `float` | |
| `timeout` | `float` | `float` | |
| `p17` | `BOOL` | `bool` | |
| `p18` | `BOOL` | `bool` | |
| `p19` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** decal types:

public enum DecalTypes
{
    splatters_blood = 1010,
    splatters_blood_dir = 1015,
    splatters_blood_mist = 1017,
    splatters_mud = 1020,
    splatters_paint = 1030,
    splatters_water = 1040,
    splatters_water_hydrant = 1050,
    splatters_blood2 = 1110,
    weapImpact_metal = 4010,
    weapImpact_concrete = 4020,
    weapImpact_mattress = 4030,
    weapImpact_mud = 4032,
    weapImpact_wood = 4050,
    weapImpact_sand = 4053,
    weapImpact_cardboard = 4040,
    weapImpact_melee_glass = 4100,
    weapImpact_glass_blood = 4102,
    weapImpact_glass_blood2 = 4104,
    weapImpact_shotgun_paper = 4200,
    weapImpact_shotgun_mattress,
    weapImpact_shotgun_metal,
    weapImpact_shotgun_wood,
    weapImpact_shotgun_dirt,
    weapImpact_shotgun_tvscreen,
    weapImpact_shotgun_tvscreen2,
    weapImpact_shotgun_tvscreen3,
    weapImpact_melee_concrete = 4310,
    weapImpact_melee_wood = 4312,
    weapImpact_melee_metal = 4314,
    burn1 = 4421,
    burn2,
    burn3,
    burn4,
    burn5,
    bang_concrete_bang = 5000,
    bang_concrete_bang2,
    bang_bullet_bang,
    bang_bullet_bang2 = 5004,
    bang_glass = 5031,
    bang_glass2,
    solidPool_water = 9000,
    solidPool_blood,
    solidPool_oil,
    solidPool_petrol,
    solidPool_mud,
    porousPool_water,
    porousPool_blood,
    porousPool_oil,
    porousPool_petrol,
    porousPool_mud,
    porousPool_water_ped_drip,
    liquidTrail_water = 9050
}

---

### ADD_PETROL_DECAL

**Hash:** `0x4F5212C7AD880DF8` | **JHash:** `0x1259DF42` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_PETROL_DECAL(float x, float y, float z, float groundLvl, float width, float transparency)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_PETROL_DECAL, x, y, z, groundLvl, width, transparency);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `groundLvl` | `float` | `float` | |
| `width` | `float` | `float` | |
| `transparency` | `float` | `float` | |

**返回值:** `int` → `int`

---

### ADD_OIL_DECAL

**Hash:** `0x126D7F89FE859A5E` | **Build:** `2699`

**C++ 原型:**
```cpp
int ADD_OIL_DECAL(float x, float y, float z, float groundLvl, float width, float transparency)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_OIL_DECAL, x, y, z, groundLvl, width, transparency);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `groundLvl` | `float` | `float` | |
| `width` | `float` | `float` | |
| `transparency` | `float` | `float` | |

**返回值:** `int` → `int`

---

### START_PETROL_TRAIL_DECALS

**Hash:** `0x99AC7F0D8B9C893D` | **JHash:** `0xE3938B0B` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PETROL_TRAIL_DECALS(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PETROL_TRAIL_DECALS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ADD_PETROL_TRAIL_DECAL_INFO

**Hash:** `0x967278682CB6967A` | **JHash:** `0xBAEC6ADD` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PETROL_TRAIL_DECAL_INFO(float x, float y, float z, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PETROL_TRAIL_DECAL_INFO, x, y, z, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### END_PETROL_TRAIL_DECALS

**Hash:** `0x0A123435A26C36CD` | **JHash:** `0xCCCA6855` | **Build:** `323`

**C++ 原型:**
```cpp
void END_PETROL_TRAIL_DECALS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_PETROL_TRAIL_DECALS);
```

**返回值:** `void` → `void`

---

### REMOVE_DECAL

**Hash:** `0xED3F346429CCD659` | **JHash:** `0xA4363188` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DECAL(int decal)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DECAL, decal);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decal` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_DECAL_ALIVE

**Hash:** `0xC694D74949CAFD0C` | **JHash:** `0xCDD4A61A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DECAL_ALIVE(int decal)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DECAL_ALIVE, decal);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decal` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_DECAL_WASH_LEVEL

**Hash:** `0x323F647679A09103` | **JHash:** `0x054448EF` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_DECAL_WASH_LEVEL(int decal)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_DECAL_WASH_LEVEL, decal);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decal` | `int` | `int` | |

**返回值:** `float` → `float`

---

### SET_DISABLE_PETROL_DECALS_IGNITING_THIS_FRAME

**Hash:** `0xD9454B5752C857DC` | **JHash:** `0xEAB6417C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_PETROL_DECALS_IGNITING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_PETROL_DECALS_IGNITING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_DISABLE_PETROL_DECALS_RECYCLING_THIS_FRAME

**Hash:** `0x27CFB1B1E078CB2D` | **JHash:** `0xC2703B88` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_PETROL_DECALS_RECYCLING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_PETROL_DECALS_RECYCLING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_DISABLE_DECAL_RENDERING_THIS_FRAME

**Hash:** `0x4B5CFC83122DF602` | **JHash:** `0xA706E84D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_DECAL_RENDERING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_DECAL_RENDERING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### GET_IS_PETROL_DECAL_IN_RANGE

**Hash:** `0x2F09F7976C512404` | **JHash:** `0x242C6A04` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PETROL_DECAL_IN_RANGE(float xCoord, float yCoord, float zCoord, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PETROL_DECAL_IN_RANGE, xCoord, yCoord, zCoord, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xCoord` | `float` | `float` | |
| `yCoord` | `float` | `float` | |
| `zCoord` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### PATCH_DECAL_DIFFUSE_MAP

**Hash:** `0x8A35C742130C6080` | **JHash:** `0x335695CF` | **Build:** `323`

**C++ 原型:**
```cpp
void PATCH_DECAL_DIFFUSE_MAP(int decalType, const char* textureDict, const char* textureName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PATCH_DECAL_DIFFUSE_MAP, decalType, textureDict, textureName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decalType` | `int` | `int` | |
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### UNPATCH_DECAL_DIFFUSE_MAP

**Hash:** `0xB7ED70C49521A61D` | **JHash:** `0x7B786555` | **Build:** `323`

**C++ 原型:**
```cpp
void UNPATCH_DECAL_DIFFUSE_MAP(int decalType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNPATCH_DECAL_DIFFUSE_MAP, decalType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decalType` | `int` | `int` | |

**返回值:** `void` → `void`

---

### MOVE_VEHICLE_DECALS

**Hash:** `0x84C8D7C2D30D3280` | **JHash:** `0xCE9E6CF2` | **Build:** `323`

**C++ 原型:**
```cpp
void MOVE_VEHICLE_DECALS(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MOVE_VEHICLE_DECALS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ADD_VEHICLE_CREW_EMBLEM

**Hash:** `0x428BDCB9DA58DA53` | **JHash:** `0x12077738` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ADD_VEHICLE_CREW_EMBLEM(Vehicle vehicle, Ped ped, int boneIndex, float x1, float x2, float x3, float y1, float y2, float y3, float z1, float z2, float z3, float scale, Any p13, int alpha)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ADD_VEHICLE_CREW_EMBLEM, vehicle, ped, boneIndex, x1, x2, x3, y1, y2, y3, z1, z2, z3, scale, p13, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ped` | `Ped` | `int` | |
| `boneIndex` | `int` | `int` | |
| `x1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `p13` | `Any` | `object` | |
| `alpha` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** boneIndex is always chassis_dummy in the scripts. The x/y/z params are location relative to the chassis bone.

---

### ABORT_VEHICLE_CREW_EMBLEM_REQUEST

**Hash:** `0x82ACC484FFA3B05F` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL ABORT_VEHICLE_CREW_EMBLEM_REQUEST(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ABORT_VEHICLE_CREW_EMBLEM_REQUEST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_VEHICLE_CREW_EMBLEM

**Hash:** `0xD2300034310557E4` | **JHash:** `0x667046A8` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_CREW_EMBLEM(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_CREW_EMBLEM, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_CREW_EMBLEM_REQUEST_STATE

**Hash:** `0xFE26117A5841B2FF` | **JHash:** `0x4F4D76E8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_CREW_EMBLEM_REQUEST_STATE(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_CREW_EMBLEM_REQUEST_STATE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `int` → `int`

---

### DOES_VEHICLE_HAVE_CREW_EMBLEM

**Hash:** `0x060D935D3981A275` | **JHash:** `0x6D58F73B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_HAVE_CREW_EMBLEM(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_HAVE_CREW_EMBLEM, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DISABLE_COMPOSITE_SHOTGUN_DECALS

**Hash:** `0x0E4299C549F0D1F1` | **JHash:** `0x9BABCBA4` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_COMPOSITE_SHOTGUN_DECALS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_COMPOSITE_SHOTGUN_DECALS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_SCUFF_DECALS

**Hash:** `0x02369D5C8A51FDCF` | **JHash:** `0xFDF6D8DA` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_SCUFF_DECALS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_SCUFF_DECALS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DECAL_BULLET_IMPACT_RANGE_SCALE

**Hash:** `0x46D1A61A21F566FC` | **JHash:** `0x2056A015` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DECAL_BULLET_IMPACT_RANGE_SCALE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DECAL_BULLET_IMPACT_RANGE_SCALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### OVERRIDE_INTERIOR_SMOKE_NAME

**Hash:** `0x2A2A52824DB96700` | **JHash:** `0x0F486429` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_INTERIOR_SMOKE_NAME(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_INTERIOR_SMOKE_NAME, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### OVERRIDE_INTERIOR_SMOKE_LEVEL

**Hash:** `0x1600FD8CF72EBC12` | **JHash:** `0xD87CC710` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_INTERIOR_SMOKE_LEVEL(float level)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_INTERIOR_SMOKE_LEVEL, level);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `level` | `float` | `float` | |

**返回值:** `void` → `void`

---

### OVERRIDE_INTERIOR_SMOKE_END

**Hash:** `0xEFB55E7C25D3B3BE` | **JHash:** `0xE29EE145` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_INTERIOR_SMOKE_END(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_INTERIOR_SMOKE_END);
```

**返回值:** `void` → `void`

---

### REGISTER_NOIR_LENS_EFFECT

**Hash:** `0xA44FF770DFBC5DAE` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_NOIR_LENS_EFFECT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_NOIR_LENS_EFFECT);
```

**返回值:** `void` → `void`

**说明:** Used with 'NG_filmnoir_BW{01,02}' timecycles and the "NOIR_FILTER_SOUNDS" audioref.

---

### DISABLE_VEHICLE_DISTANTLIGHTS

**Hash:** `0xC9F98AC1884E73A2` | **JHash:** `0x7CFAE36F` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_VEHICLE_DISTANTLIGHTS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_VEHICLE_DISTANTLIGHTS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RENDER_SHADOWED_LIGHTS_WITH_NO_SHADOWS

**Hash:** `0x03300B57FCAC6DDB` | **JHash:** `0x60F72371` | **Build:** `323`

**C++ 原型:**
```cpp
void RENDER_SHADOWED_LIGHTS_WITH_NO_SHADOWS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RENDER_SHADOWED_LIGHTS_WITH_NO_SHADOWS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_EARLY_LIGHT_CHECK

**Hash:** `0x98EDF76A7271E4F2` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_EARLY_LIGHT_CHECK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_EARLY_LIGHT_CHECK);
```

**返回值:** `void` → `void`

---

### USE_SNOW_FOOT_VFX_WHEN_UNSHELTERED

**Hash:** `0xAEEDAD1420C65CC0` | **JHash:** `0xC53576CA` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_SNOW_FOOT_VFX_WHEN_UNSHELTERED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_SNOW_FOOT_VFX_WHEN_UNSHELTERED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Forces footstep tracks on all surfaces.

---

### _FORCE_ALLOW_SNOW_FOOT_VFX_ON_ICE

**Hash:** `0xA342A3763B3AFB6C` | **Build:** `3095`

**C++ 原型:**
```cpp
void _FORCE_ALLOW_SNOW_FOOT_VFX_ON_ICE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._FORCE_ALLOW_SNOW_FOOT_VFX_ON_ICE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### USE_SNOW_WHEEL_VFX_WHEN_UNSHELTERED

**Hash:** `0x4CC7F0FEA5283FE0` | **JHash:** `0x7158B1EA` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_SNOW_WHEEL_VFX_WHEN_UNSHELTERED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_SNOW_WHEEL_VFX_WHEN_UNSHELTERED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Forces vehicle trails on all surfaces.

---

### DISABLE_REGION_VFX

**Hash:** `0xEFD97FF47B745B8D` | **Build:** `791`

**C++ 原型:**
```cpp
void DISABLE_REGION_VFX(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_REGION_VFX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _FORCE_GROUND_SNOW_PASS

**Hash:** `0x6E9EF3A33C8899F8` | **Build:** `3095`

**C++ 原型:**
```cpp
void _FORCE_GROUND_SNOW_PASS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._FORCE_GROUND_SNOW_PASS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PRESET_INTERIOR_AMBIENT_CACHE

**Hash:** `0xD7021272EB0A451E` | **JHash:** `0x137E3E24` | **Build:** `323`

**C++ 原型:**
```cpp
void PRESET_INTERIOR_AMBIENT_CACHE(const char* timecycleModifierName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRESET_INTERIOR_AMBIENT_CACHE, timecycleModifierName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timecycleModifierName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Only one match in the scripts:

GRAPHICS::PRESET_INTERIOR_AMBIENT_CACHE("int_carrier_hanger");

---

### SET_TIMECYCLE_MODIFIER

**Hash:** `0x2C933ABF17A1DF41` | **JHash:** `0xA81F3638` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TIMECYCLE_MODIFIER(const char* modifierName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TIMECYCLE_MODIFIER, modifierName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Loads the specified timecycle modifier. Modifiers are defined separately in another file (e.g. "timecycle_mods_1.xml")

Parameters:
modifierName - The modifier to load (e.g. "V_FIB_IT3", "scanline_cam", etc.)

Full list of timecycle modifiers by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/timecycleModifiers.json

---

### SET_TIMECYCLE_MODIFIER_STRENGTH

**Hash:** `0x82E7FFCD5B2326B3` | **JHash:** `0x458F4F45` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TIMECYCLE_MODIFIER_STRENGTH(float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TIMECYCLE_MODIFIER_STRENGTH, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TRANSITION_TIMECYCLE_MODIFIER

**Hash:** `0x3BCF567485E1971C` | **JHash:** `0xBB2BA72A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRANSITION_TIMECYCLE_MODIFIER(const char* modifierName, float transition)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRANSITION_TIMECYCLE_MODIFIER, modifierName, transition);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName` | `const char*` | `string` | |
| `transition` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Full list of timecycle modifiers by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/timecycleModifiers.json

---

### SET_TRANSITION_OUT_OF_TIMECYCLE_MODIFIER

**Hash:** `0x1CBA05AE7BD7EE05` | **JHash:** `0x56345F6B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRANSITION_OUT_OF_TIMECYCLE_MODIFIER(float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRANSITION_OUT_OF_TIMECYCLE_MODIFIER, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_TIMECYCLE_MODIFIER

**Hash:** `0x0F07E7745A236711` | **JHash:** `0x8D8DF8EE` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_TIMECYCLE_MODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_TIMECYCLE_MODIFIER);
```

**返回值:** `void` → `void`

---

### GET_TIMECYCLE_MODIFIER_INDEX

**Hash:** `0xFDF3D97C674AFB66` | **JHash:** `0x594FEEC4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIMECYCLE_MODIFIER_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIMECYCLE_MODIFIER_INDEX);
```

**返回值:** `int` → `int`

**说明:** Only use for this in the PC scripts is:

if (GRAPHICS::GET_TIMECYCLE_MODIFIER_INDEX() != -1)

---

### GET_TIMECYCLE_TRANSITION_MODIFIER_INDEX

**Hash:** `0x459FD2C8D0AB78BC` | **JHash:** `0x03C44E4B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIMECYCLE_TRANSITION_MODIFIER_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIMECYCLE_TRANSITION_MODIFIER_INDEX);
```

**返回值:** `int` → `int`

---

### GET_IS_TIMECYCLE_TRANSITIONING_OUT

**Hash:** `0x98D18905BF723B99` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL GET_IS_TIMECYCLE_TRANSITIONING_OUT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_TIMECYCLE_TRANSITIONING_OUT);
```

**返回值:** `BOOL` → `bool`

---

### PUSH_TIMECYCLE_MODIFIER

**Hash:** `0x58F735290861E6B4` | **JHash:** `0x7E082045` | **Build:** `323`

**C++ 原型:**
```cpp
void PUSH_TIMECYCLE_MODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PUSH_TIMECYCLE_MODIFIER);
```

**返回值:** `void` → `void`

---

### POP_TIMECYCLE_MODIFIER

**Hash:** `0x3C8938D7D872211E` | **JHash:** `0x79D7D235` | **Build:** `323`

**C++ 原型:**
```cpp
void POP_TIMECYCLE_MODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POP_TIMECYCLE_MODIFIER);
```

**返回值:** `void` → `void`

---

### SET_CURRENT_PLAYER_TCMODIFIER

**Hash:** `0xBBF327DED94E4DEB` | **JHash:** `0x85BA15A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CURRENT_PLAYER_TCMODIFIER(const char* modifierName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CURRENT_PLAYER_TCMODIFIER, modifierName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_TCMODIFIER_TRANSITION

**Hash:** `0xBDEB86F4D5809204` | **JHash:** `0x9559BB38` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_TCMODIFIER_TRANSITION(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_TCMODIFIER_TRANSITION, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_NEXT_PLAYER_TCMODIFIER

**Hash:** `0xBF59707B3E5ED531` | **JHash:** `0x554BA16E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NEXT_PLAYER_TCMODIFIER(const char* modifierName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NEXT_PLAYER_TCMODIFIER, modifierName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### ADD_TCMODIFIER_OVERRIDE

**Hash:** `0x1A8E2C8B9CF4549C` | **JHash:** `0xE8F538B5` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TCMODIFIER_OVERRIDE(const char* modifierName1, const char* modifierName2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TCMODIFIER_OVERRIDE, modifierName1, modifierName2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName1` | `const char*` | `string` | |
| `modifierName2` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### CLEAR_ALL_TCMODIFIER_OVERRIDES

**Hash:** `0x15E33297C3E8DC60` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_TCMODIFIER_OVERRIDES(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_TCMODIFIER_OVERRIDES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_EXTRA_TCMODIFIER

**Hash:** `0x5096FD9CCB49056D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EXTRA_TCMODIFIER(const char* modifierName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EXTRA_TCMODIFIER, modifierName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifierName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of timecycle modifiers by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/timecycleModifiers.json

---

### CLEAR_EXTRA_TCMODIFIER

**Hash:** `0x92CCC17A7A2285DA` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_EXTRA_TCMODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_EXTRA_TCMODIFIER);
```

**返回值:** `void` → `void`

**说明:** Clears the secondary timecycle modifier usually set with _SET_EXTRA_TIMECYCLE_MODIFIER

---

### GET_EXTRA_TCMODIFIER

**Hash:** `0xBB0527EC6341496D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_EXTRA_TCMODIFIER(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_EXTRA_TCMODIFIER);
```

**返回值:** `int` → `int`

**说明:** See GET_TIMECYCLE_MODIFIER_INDEX for use, works the same just for the secondary timecycle modifier.
Returns an integer representing the Timecycle modifier

---

### ENABLE_MOON_CYCLE_OVERRIDE

**Hash:** `0x2C328AF17210F009` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_MOON_CYCLE_OVERRIDE(float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_MOON_CYCLE_OVERRIDE, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The same as SET_TIMECYCLE_MODIFIER_STRENGTH but for the secondary timecycle modifier.

---

### DISABLE_MOON_CYCLE_OVERRIDE

**Hash:** `0x2BF72AD5B41AA739` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_MOON_CYCLE_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_MOON_CYCLE_OVERRIDE);
```

**返回值:** `void` → `void`

**说明:** Resets the extra timecycle modifier strength normally set with 0x2C328AF17210F009

---

### REQUEST_SCALEFORM_MOVIE

**Hash:** `0x11FE353CF9733E6F` | **JHash:** `0xC67E3DCB` | **Build:** `323`

**C++ 原型:**
```cpp
int REQUEST_SCALEFORM_MOVIE(const char* scaleformName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REQUEST_SCALEFORM_MOVIE, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformName` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### REQUEST_SCALEFORM_MOVIE_WITH_IGNORE_SUPER_WIDESCREEN

**Hash:** `0x65E7E78842E74CDB` | **Build:** `372`

**C++ 原型:**
```cpp
int REQUEST_SCALEFORM_MOVIE_WITH_IGNORE_SUPER_WIDESCREEN(const char* scaleformName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REQUEST_SCALEFORM_MOVIE_WITH_IGNORE_SUPER_WIDESCREEN, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Another REQUEST_SCALEFORM_MOVIE equivalent.

---

### REQUEST_SCALEFORM_MOVIE_INSTANCE

**Hash:** `0xC514489CFB8AF806` | **JHash:** `0x7CC8057D` | **Build:** `323`

**C++ 原型:**
```cpp
int REQUEST_SCALEFORM_MOVIE_INSTANCE(const char* scaleformName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REQUEST_SCALEFORM_MOVIE_INSTANCE, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformName` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### REQUEST_SCALEFORM_MOVIE_SKIP_RENDER_WHILE_PAUSED

**Hash:** `0xBD06C611BB9048C2` | **Build:** `323`

**C++ 原型:**
```cpp
int REQUEST_SCALEFORM_MOVIE_SKIP_RENDER_WHILE_PAUSED(const char* scaleformName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REQUEST_SCALEFORM_MOVIE_SKIP_RENDER_WHILE_PAUSED, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Similar to REQUEST_SCALEFORM_MOVIE, but seems to be some kind of "interactive" scaleform movie?

These seem to be the only scaleforms ever requested by this native:
"breaking_news"
"desktop_pc"
"ECG_MONITOR"
"Hacking_PC"
"TEETH_PULLING"

Note: Unless this hash is out-of-order, this native is next-gen only.


---

### HAS_SCALEFORM_MOVIE_LOADED

**Hash:** `0x85F01B8D5B90570E` | **JHash:** `0xDDFB6448` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCALEFORM_MOVIE_LOADED(int scaleformHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCALEFORM_MOVIE_LOADED, scaleformHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _HAS_SCALEFORM_MOVIE_NAMED_LOADED

**Hash:** `0x9743BCCF7CD6E1F6` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _HAS_SCALEFORM_MOVIE_NAMED_LOADED(int* scaleformHandle, const char* scaleformName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._HAS_SCALEFORM_MOVIE_NAMED_LOADED, scaleformHandle, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int*` | `int*` | |
| `scaleformName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### IS_ACTIVE_SCALEFORM_MOVIE_DELETING

**Hash:** `0x2FCB133CA50A49EB` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL IS_ACTIVE_SCALEFORM_MOVIE_DELETING(int val)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ACTIVE_SCALEFORM_MOVIE_DELETING, val);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `val` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** val is 1-20 (0 will return false)

---

### IS_SCALEFORM_MOVIE_DELETING

**Hash:** `0x86255B1FC929E33E` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL IS_SCALEFORM_MOVIE_DELETING(int val)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCALEFORM_MOVIE_DELETING, val);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `val` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** val is 1-20. Return is related to INSTRUCTIONAL_BUTTONS, COLOUR_SWITCHER_02, etc?

---

### HAS_SCALEFORM_MOVIE_FILENAME_LOADED

**Hash:** `0x0C1C5D756FB5F337` | **JHash:** `0x494A9E50` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCALEFORM_MOVIE_FILENAME_LOADED(const char* scaleformName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCALEFORM_MOVIE_FILENAME_LOADED, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Only values used in the scripts are:

"heist_mp"
"heistmap_mp"
"instructional_buttons"
"heist_pre"

---

### HAS_SCALEFORM_CONTAINER_MOVIE_LOADED_INTO_PARENT

**Hash:** `0x8217150E1217EBFD` | **JHash:** `0x1DFE8D8A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCALEFORM_CONTAINER_MOVIE_LOADED_INTO_PARENT(int scaleformHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCALEFORM_CONTAINER_MOVIE_LOADED_INTO_PARENT, scaleformHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_SCALEFORM_MOVIE_AS_NO_LONGER_NEEDED

**Hash:** `0x1D132D614DD86811` | **JHash:** `0x5FED3BA1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCALEFORM_MOVIE_AS_NO_LONGER_NEEDED(int* scaleformHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCALEFORM_MOVIE_AS_NO_LONGER_NEEDED, scaleformHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### _SET_SCALEFORM_MOVIE_NAMED_AS_NO_LONGER_NEEDED

**Hash:** `0x2FDFB1B04C76E9C3` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_SCALEFORM_MOVIE_NAMED_AS_NO_LONGER_NEEDED(int scaleformHandle, const char* scaleformName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_SCALEFORM_MOVIE_NAMED_AS_NO_LONGER_NEEDED, scaleformHandle, scaleformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |
| `scaleformName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_SCALEFORM_MOVIE_TO_USE_SYSTEM_TIME

**Hash:** `0x6D8EB211944DCE08` | **JHash:** `0x18C9DE8D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCALEFORM_MOVIE_TO_USE_SYSTEM_TIME(int scaleform, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCALEFORM_MOVIE_TO_USE_SYSTEM_TIME, scaleform, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SCALEFORM_MOVIE_TO_USE_LARGE_RT

**Hash:** `0x32F34FF7F617643B` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_SCALEFORM_MOVIE_TO_USE_LARGE_RT(int scaleformHandle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCALEFORM_MOVIE_TO_USE_LARGE_RT, scaleformHandle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SCALEFORM_MOVIE_TO_USE_SUPER_LARGE_RT

**Hash:** `0xE6A9F00D4240B519` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_SCALEFORM_MOVIE_TO_USE_SUPER_LARGE_RT(int scaleformHandle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCALEFORM_MOVIE_TO_USE_SUPER_LARGE_RT, scaleformHandle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native is used in some casino scripts to fit the scaleform in the rendertarget.

---

### DRAW_SCALEFORM_MOVIE

**Hash:** `0x54972ADAF0294A93` | **JHash:** `0x48DA6A58` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SCALEFORM_MOVIE(int scaleformHandle, float x, float y, float width, float height, int red, int green, int blue, int alpha, int p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SCALEFORM_MOVIE, scaleformHandle, x, y, width, height, red, green, blue, alpha, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p9` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_SCALEFORM_MOVIE_FULLSCREEN

**Hash:** `0x0DF606929C105BE1` | **JHash:** `0x7B48E696` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SCALEFORM_MOVIE_FULLSCREEN(int scaleform, int red, int green, int blue, int alpha, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SCALEFORM_MOVIE_FULLSCREEN, scaleform, red, green, blue, alpha, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** unk is not used so no need

---

### DRAW_SCALEFORM_MOVIE_FULLSCREEN_MASKED

**Hash:** `0xCF537FDE4FBD4CE5` | **JHash:** `0x9C59FC06` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SCALEFORM_MOVIE_FULLSCREEN_MASKED(int scaleform1, int scaleform2, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SCALEFORM_MOVIE_FULLSCREEN_MASKED, scaleform1, scaleform2, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform1` | `int` | `int` | |
| `scaleform2` | `int` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_SCALEFORM_MOVIE_3D

**Hash:** `0x87D51D72255D4E78` | **JHash:** `0xC4F63A89` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SCALEFORM_MOVIE_3D(int scaleform, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float p7, float p8, float p9, float scaleX, float scaleY, float scaleZ, int rotationOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SCALEFORM_MOVIE_3D, scaleform, posX, posY, posZ, rotX, rotY, rotZ, p7, p8, p9, scaleX, scaleY, scaleZ, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `scaleX` | `float` | `float` | |
| `scaleY` | `float` | `float` | |
| `scaleZ` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DRAW_SCALEFORM_MOVIE_3D_SOLID

**Hash:** `0x1CE592FDC749D6F5` | **JHash:** `0x899933C8` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_SCALEFORM_MOVIE_3D_SOLID(int scaleform, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float p7, float p8, float p9, float scaleX, float scaleY, float scaleZ, int rotationOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_SCALEFORM_MOVIE_3D_SOLID, scaleform, posX, posY, posZ, rotX, rotY, rotZ, p7, p8, p9, scaleX, scaleY, scaleZ, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `scaleX` | `float` | `float` | |
| `scaleY` | `float` | `float` | |
| `scaleZ` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `void` → `void`

---

### CALL_SCALEFORM_MOVIE_METHOD

**Hash:** `0xFBD96D87AC96D533` | **JHash:** `0x7AB77B57` | **Build:** `323`

**C++ 原型:**
```cpp
void CALL_SCALEFORM_MOVIE_METHOD(int scaleform, const char* method)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CALL_SCALEFORM_MOVIE_METHOD, scaleform, method);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `method` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Calls the Scaleform function.

---

### CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER

**Hash:** `0xD0837058AE2E4BEE` | **JHash:** `0x557EDA1D` | **Build:** `323`

**C++ 原型:**
```cpp
void CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER(int scaleform, const char* methodName, float param1, float param2, float param3, float param4, float param5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER, scaleform, methodName, param1, param2, param3, param4, param5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `methodName` | `const char*` | `string` | |
| `param1` | `float` | `float` | |
| `param2` | `float` | `float` | |
| `param3` | `float` | `float` | |
| `param4` | `float` | `float` | |
| `param5` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Calls the Scaleform function and passes the parameters as floats.

The number of parameters passed to the function varies, so the end of the parameter list is represented by -1.0.

---

### CALL_SCALEFORM_MOVIE_METHOD_WITH_STRING

**Hash:** `0x51BC1ED3CC44E8F7` | **JHash:** `0x91A7FCEB` | **Build:** `323`

**C++ 原型:**
```cpp
void CALL_SCALEFORM_MOVIE_METHOD_WITH_STRING(int scaleform, const char* methodName, const char* param1, const char* param2, const char* param3, const char* param4, const char* param5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CALL_SCALEFORM_MOVIE_METHOD_WITH_STRING, scaleform, methodName, param1, param2, param3, param4, param5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `methodName` | `const char*` | `string` | |
| `param1` | `const char*` | `string` | |
| `param2` | `const char*` | `string` | |
| `param3` | `const char*` | `string` | |
| `param4` | `const char*` | `string` | |
| `param5` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Calls the Scaleform function and passes the parameters as strings.

The number of parameters passed to the function varies, so the end of the parameter list is represented by 0 (NULL).

---

### CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER_AND_STRING

**Hash:** `0xEF662D8D57E290B1` | **JHash:** `0x6EAF56DE` | **Build:** `323`

**C++ 原型:**
```cpp
void CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER_AND_STRING(int scaleform, const char* methodName, float floatParam1, float floatParam2, float floatParam3, float floatParam4, float floatParam5, const char* stringParam1, const char* stringParam2, const char* stringParam3, const char* stringParam4, const char* stringParam5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER_AND_STRING, scaleform, methodName, floatParam1, floatParam2, floatParam3, floatParam4, floatParam5, stringParam1, stringParam2, stringParam3, stringParam4, stringParam5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `methodName` | `const char*` | `string` | |
| `floatParam1` | `float` | `float` | |
| `floatParam2` | `float` | `float` | |
| `floatParam3` | `float` | `float` | |
| `floatParam4` | `float` | `float` | |
| `floatParam5` | `float` | `float` | |
| `stringParam1` | `const char*` | `string` | |
| `stringParam2` | `const char*` | `string` | |
| `stringParam3` | `const char*` | `string` | |
| `stringParam4` | `const char*` | `string` | |
| `stringParam5` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Calls the Scaleform function and passes both float and string parameters (in their respective order).

The number of parameters passed to the function varies, so the end of the float parameters is represented by -1.0, and the end of the string parameters is represented by 0 (NULL).

NOTE: The order of parameters in the function prototype is important! All float parameters must come first, followed by the string parameters.

Examples:
// function MY_FUNCTION(floatParam1, floatParam2, stringParam)
GRAPHICS::CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER_AND_STRING(scaleform, "MY_FUNCTION", 10.0, 20.0, -1.0, -1.0, -1.0, "String param", 0, 0, 0, 0);

// function MY_FUNCTION_2(floatParam, stringParam1, stringParam2)
GRAPHICS::CALL_SCALEFORM_MOVIE_METHOD_WITH_NUMBER_AND_STRING(scaleform, "MY_FUNCTION_2", 10.0, -1.0, -1.0, -1.0, -1.0, "String param #1", "String param #2", 0, 0, 0);

---

### BEGIN_SCALEFORM_SCRIPT_HUD_MOVIE_METHOD

**Hash:** `0x98C494FD5BDFBFD5` | **JHash:** `0x5D66CE1E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_SCALEFORM_SCRIPT_HUD_MOVIE_METHOD(int hudComponent, const char* methodName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_SCALEFORM_SCRIPT_HUD_MOVIE_METHOD, hudComponent, methodName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudComponent` | `int` | `int` | |
| `methodName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Pushes a function from the Hud component Scaleform onto the stack. Same behavior as GRAPHICS::BEGIN_SCALEFORM_MOVIE_METHOD, just a hud component id instead of a Scaleform.

Known components:
19 - MP_RANK_BAR
20 - HUD_DIRECTOR_MODE

This native requires more research - all information can be found inside of 'hud.gfx'. Using a decompiler, the different components are located under "scripts\__Packages\com\rockstargames\gtav\hud\hudComponents" and "scripts\__Packages\com\rockstargames\gtav\Multiplayer".

---

### BEGIN_SCALEFORM_MOVIE_METHOD

**Hash:** `0xF6E48914C7A8694E` | **JHash:** `0x215ABBE8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_SCALEFORM_MOVIE_METHOD(int scaleform, const char* methodName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_SCALEFORM_MOVIE_METHOD, scaleform, methodName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleform` | `int` | `int` | |
| `methodName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Push a function from the Scaleform onto the stack


---

### BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND

**Hash:** `0xAB58C27C2E6123C6` | **JHash:** `0xF6015178` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND(const char* methodName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND, methodName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Starts frontend (pause menu) scaleform movie methods.
This can be used when you want to make custom frontend menus, and customize things like images or text in the menus etc.
Use `BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND_HEADER` for header scaleform functions.

---

### BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND_HEADER

**Hash:** `0xB9449845F73F5E9C` | **JHash:** `0x5E219B67` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND_HEADER(const char* methodName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND_HEADER, methodName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Starts frontend (pause menu) scaleform movie methods for header options.
Use `BEGIN_SCALEFORM_MOVIE_METHOD_ON_FRONTEND` to customize the content inside the frontend menus.

---

### END_SCALEFORM_MOVIE_METHOD

**Hash:** `0xC6796A8FFA375E53` | **JHash:** `0x02DBF2D7` | **Build:** `323`

**C++ 原型:**
```cpp
void END_SCALEFORM_MOVIE_METHOD(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_SCALEFORM_MOVIE_METHOD);
```

**返回值:** `void` → `void`

**说明:** Pops and calls the Scaleform function on the stack

---

### END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE

**Hash:** `0xC50AA39A577AF886` | **JHash:** `0x2F38B526` | **Build:** `323`

**C++ 原型:**
```cpp
int END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE);
```

**返回值:** `int` → `int`

---

### IS_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_READY

**Hash:** `0x768FF8961BA904D6` | **JHash:** `0x5CD7C3C0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_READY(int methodReturn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_READY, methodReturn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodReturn` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** methodReturn: The return value of this native: END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE
Returns true if the return value of a scaleform function is ready to be collected (using GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_STRING or GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_INT).

---

### GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_INT

**Hash:** `0x2DE7EFA66B906036` | **JHash:** `0x2CFB0E6D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_INT(int methodReturn)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_INT, methodReturn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodReturn` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** methodReturn: The return value of this native: END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE
Used to get a return value from a scaleform function. Returns an int in the same way GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_STRING returns a string.

---

### GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_BOOL

**Hash:** `0xD80A80346A45D761` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_BOOL(int methodReturn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_BOOL, methodReturn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodReturn` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** methodReturn: The return value of this native: END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE

---

### GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_STRING

**Hash:** `0xE1E258829A885245` | **JHash:** `0x516862EB` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_STRING(int methodReturn)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_STRING, methodReturn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `methodReturn` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** methodReturn: The return value of this native: END_SCALEFORM_MOVIE_METHOD_RETURN_VALUE
Used to get a return value from a scaleform function. Returns a string in the same way GET_SCALEFORM_MOVIE_METHOD_RETURN_VALUE_INT returns an int.

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_INT

**Hash:** `0xC3D0841A0CC546A6` | **JHash:** `0x716777CB` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_INT(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_INT, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Pushes an integer for the Scaleform function onto the stack.

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_FLOAT

**Hash:** `0xD69736AAE04DB51A` | **JHash:** `0x9A01FFDA` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_FLOAT(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_FLOAT, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Pushes a float for the Scaleform function onto the stack.

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_BOOL

**Hash:** `0xC58424BA936EB458` | **JHash:** `0x0D4AE8CB` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_BOOL(BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_BOOL, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Pushes a boolean for the Scaleform function onto the stack.

---

### BEGIN_TEXT_COMMAND_SCALEFORM_STRING

**Hash:** `0x80338406F3475E55` | **JHash:** `0x3AC9CB55` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_SCALEFORM_STRING(const char* componentType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_SCALEFORM_STRING, componentType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Called prior to adding a text component to the UI. After doing so, GRAPHICS::END_TEXT_COMMAND_SCALEFORM_STRING is called.

Examples:
GRAPHICS::BEGIN_TEXT_COMMAND_SCALEFORM_STRING("NUMBER");
HUD::ADD_TEXT_COMPONENT_INTEGER(MISC::ABSI(a_1));
GRAPHICS::END_TEXT_COMMAND_SCALEFORM_STRING();

GRAPHICS::BEGIN_TEXT_COMMAND_SCALEFORM_STRING("STRING");
HUD::ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(a_2);
GRAPHICS::END_TEXT_COMMAND_SCALEFORM_STRING();

GRAPHICS::BEGIN_TEXT_COMMAND_SCALEFORM_STRING("STRTNM2");
HUD::ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY(v_3);
HUD::ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY(v_4);
GRAPHICS::END_TEXT_COMMAND_SCALEFORM_STRING();

GRAPHICS::BEGIN_TEXT_COMMAND_SCALEFORM_STRING("STRTNM1");
HUD::ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY(v_3);
GRAPHICS::END_TEXT_COMMAND_SCALEFORM_STRING();

---

### END_TEXT_COMMAND_SCALEFORM_STRING

**Hash:** `0x362E2D3FE93A9959` | **JHash:** `0x386CE0B8` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_SCALEFORM_STRING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_SCALEFORM_STRING);
```

**返回值:** `void` → `void`

---

### END_TEXT_COMMAND_UNPARSED_SCALEFORM_STRING

**Hash:** `0xAE4E8157D9ECF087` | **JHash:** `0x2E80DB52` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_UNPARSED_SCALEFORM_STRING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_UNPARSED_SCALEFORM_STRING);
```

**返回值:** `void` → `void`

**说明:** Same as END_TEXT_COMMAND_SCALEFORM_STRING but does not perform HTML conversion for text tokens.

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_LITERAL_STRING

**Hash:** `0x77FE3402004CD1B0` | **Build:** `573`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_LITERAL_STRING(const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_LITERAL_STRING, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Same as SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING
Both SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING / _SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING_2 works, but _SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING_2 is usually used for "name" (organisation, players..).

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING

**Hash:** `0xBA7148484BD90365` | **JHash:** `0x4DAAD55B` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING(const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_TEXTURE_NAME_STRING, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_PLAYER_NAME_STRING

**Hash:** `0xE83A3E3557A56640` | **JHash:** `0xCCBF0334` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_PLAYER_NAME_STRING(const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_PLAYER_NAME_STRING, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### DOES_LATEST_BRIEF_STRING_EXIST

**Hash:** `0x5E657EF1099EDD65` | **JHash:** `0x91A081A1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_LATEST_BRIEF_STRING_EXIST(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_LATEST_BRIEF_STRING_EXIST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SCALEFORM_MOVIE_METHOD_ADD_PARAM_LATEST_BRIEF_STRING

**Hash:** `0xEC52C631A1831C03` | **JHash:** `0x83A9811D` | **Build:** `323`

**C++ 原型:**
```cpp
void SCALEFORM_MOVIE_METHOD_ADD_PARAM_LATEST_BRIEF_STRING(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCALEFORM_MOVIE_METHOD_ADD_PARAM_LATEST_BRIEF_STRING, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REQUEST_SCALEFORM_SCRIPT_HUD_MOVIE

**Hash:** `0x9304881D6F6537EA` | **JHash:** `0x7AF85862` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_SCALEFORM_SCRIPT_HUD_MOVIE(int hudComponent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_SCALEFORM_SCRIPT_HUD_MOVIE, hudComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudComponent` | `int` | `int` | |

**返回值:** `void` → `void`

---

### HAS_SCALEFORM_SCRIPT_HUD_MOVIE_LOADED

**Hash:** `0xDF6E5987D2B4D140` | **JHash:** `0x79B43255` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCALEFORM_SCRIPT_HUD_MOVIE_LOADED(int hudComponent)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCALEFORM_SCRIPT_HUD_MOVIE_LOADED, hudComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudComponent` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_SCALEFORM_SCRIPT_HUD_MOVIE

**Hash:** `0xF44A5456AC3F4F97` | **JHash:** `0x03D87600` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_SCALEFORM_SCRIPT_HUD_MOVIE(int hudComponent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SCALEFORM_SCRIPT_HUD_MOVIE, hudComponent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudComponent` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PASS_KEYBOARD_INPUT_TO_SCALEFORM

**Hash:** `0xD1C7CB175E012964` | **JHash:** `0xE9183D3A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PASS_KEYBOARD_INPUT_TO_SCALEFORM(int scaleformHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PASS_KEYBOARD_INPUT_TO_SCALEFORM, scaleformHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_TV_CHANNEL

**Hash:** `0xBAABBB23EB6E484E` | **JHash:** `0x41A8A627` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_CHANNEL(int channel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_CHANNEL, channel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `channel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_TV_CHANNEL

**Hash:** `0xFC1E275A90D39995` | **JHash:** `0x6B96145A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TV_CHANNEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TV_CHANNEL);
```

**返回值:** `int` → `int`

---

### SET_TV_VOLUME

**Hash:** `0x2982BF73F66E9DDC` | **JHash:** `0xF3504F4D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_VOLUME(float volume)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_VOLUME, volume);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `volume` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_TV_VOLUME

**Hash:** `0x2170813D3DD8661B` | **JHash:** `0x39555CF0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_TV_VOLUME(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TV_VOLUME);
```

**返回值:** `float` → `float`

---

### DRAW_TV_CHANNEL

**Hash:** `0xFDDC2B4ED3C69DF0` | **JHash:** `0x8129EF89` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_TV_CHANNEL(float xPos, float yPos, float xScale, float yScale, float rotation, int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_TV_CHANNEL, xPos, yPos, xScale, yScale, rotation, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `xScale` | `float` | `float` | |
| `yScale` | `float` | `float` | |
| `rotation` | `float` | `float` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** All calls to this native are preceded by calls to GRAPHICS::SET_SCRIPT_GFX_DRAW_ORDER and GRAPHICS::SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU, respectively.

"act_cinema.ysc", line 1483:
HUD::SET_HUD_COMPONENT_POSITION(15, 0.0, -0.0375);
HUD::SET_TEXT_RENDER_ID(l_AE);
GRAPHICS::SET_SCRIPT_GFX_DRAW_ORDER(4);
GRAPHICS::SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU(1);
if (GRAPHICS::IS_TVSHOW_CURRENTLY_PLAYING(${movie_arthouse})) {
    GRAPHICS::DRAW_TV_CHANNEL(0.5, 0.5, 0.7375, 1.0, 0.0, 255, 255, 255, 255);
} else { 
    GRAPHICS::DRAW_TV_CHANNEL(0.5, 0.5, 1.0, 1.0, 0.0, 255, 255, 255, 255);
}

"am_mp_property_int.ysc", line 102545:
if (ENTITY::DOES_ENTITY_EXIST(a_2._f3)) {
    if (HUD::IS_NAMED_RENDERTARGET_LINKED(ENTITY::GET_ENTITY_MODEL(a_2._f3))) {
        HUD::SET_TEXT_RENDER_ID(a_2._f1);
        GRAPHICS::SET_SCRIPT_GFX_DRAW_ORDER(4);
        GRAPHICS::SET_SCRIPT_GFX_DRAW_BEHIND_PAUSEMENU(1);
        GRAPHICS::DRAW_TV_CHANNEL(0.5, 0.5, 1.0, 1.0, 0.0, 255, 255, 255, 255);
        if (GRAPHICS::GET_TV_CHANNEL() == -1) {
            sub_a8fa5(a_2, 1);
        } else { 
            sub_a8fa5(a_2, 1);
            GRAPHICS::ATTACH_TV_AUDIO_TO_ENTITY(a_2._f3);
        }
        HUD::SET_TEXT_RENDER_ID(HUD::GET_DEFAULT_SCRIPT_RENDERTARGET_RENDER_ID());
    }
}


---

### SET_TV_CHANNEL_PLAYLIST

**Hash:** `0xF7B38B8305F1FE8B` | **JHash:** `0xB262DE67` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_CHANNEL_PLAYLIST(int tvChannel, const char* playlistName, BOOL restart)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_CHANNEL_PLAYLIST, tvChannel, playlistName, restart);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tvChannel` | `int` | `int` | |
| `playlistName` | `const char*` | `string` | |
| `restart` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Loads specified video sequence into the TV Channel
TV_Channel ranges from 0-2
VideoSequence can be any of the following:
"PL_STD_CNT" CNT Standard Channel
"PL_STD_WZL" Weazel Standard Channel
"PL_LO_CNT"
"PL_LO_WZL"
"PL_SP_WORKOUT"
"PL_SP_INV" - Jay Norris Assassination Mission Fail
"PL_SP_INV_EXP" - Jay Norris Assassination Mission Success
"PL_LO_RS" - Righteous Slaughter Ad
"PL_LO_RS_CUTSCENE" - Righteous Slaughter Cut-scene
"PL_SP_PLSH1_INTRO"
"PL_LES1_FAME_OR_SHAME"
"PL_STD_WZL_FOS_EP2"
"PL_MP_WEAZEL" - Weazel Logo on loop
"PL_MP_CCTV" - Generic CCTV loop

Restart:
0=video sequence continues as normal
1=sequence restarts from beginning every time that channel is selected


The above playlists work as intended, and are commonly used, but there are many more playlists, as seen in `tvplaylists.xml`. A pastebin below outlines all playlists, they will be surronded by the name tag I.E. (<Name>PL_STD_CNT</Name> = PL_STD_CNT).
https://pastebin.com/zUzGB6h7

---

### SET_TV_CHANNEL_PLAYLIST_AT_HOUR

**Hash:** `0x2201C576FACAEBE8` | **JHash:** `0x78C4DCBE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_CHANNEL_PLAYLIST_AT_HOUR(int tvChannel, const char* playlistName, int hour)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_CHANNEL_PLAYLIST_AT_HOUR, tvChannel, playlistName, hour);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tvChannel` | `int` | `int` | |
| `playlistName` | `const char*` | `string` | |
| `hour` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _SET_TV_CHANNEL_PLAYLIST_DIRTY

**Hash:** `0xEE831F15A8D0D94A` | **Build:** `3095`

**C++ 原型:**
```cpp
Any _SET_TV_CHANNEL_PLAYLIST_DIRTY(int tvChannel, BOOL p1)
```

**C# 调用格式:**
```csharp
object result = Function.Call<object>(Hash._SET_TV_CHANNEL_PLAYLIST_DIRTY, tvChannel, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tvChannel` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Any` → `object`

**说明:** Does not actually return anything.

---

### CLEAR_TV_CHANNEL_PLAYLIST

**Hash:** `0xBEB3D46BB7F043C0` | **JHash:** `0xCBE7068F` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_TV_CHANNEL_PLAYLIST(int tvChannel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_TV_CHANNEL_PLAYLIST, tvChannel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tvChannel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PLAYLIST_ON_CHANNEL

**Hash:** `0x1F710BFF7DAE6261` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL IS_PLAYLIST_ON_CHANNEL(int tvChannel, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYLIST_ON_CHANNEL, tvChannel, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tvChannel` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_TVSHOW_CURRENTLY_PLAYING

**Hash:** `0x0AD973CA1E077B60` | **JHash:** `0x4D1EB0FB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TVSHOW_CURRENTLY_PLAYING(Hash videoCliphash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TVSHOW_CURRENTLY_PLAYING, videoCliphash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `videoCliphash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### ENABLE_MOVIE_KEYFRAME_WAIT

**Hash:** `0x74C180030FDE4B69` | **JHash:** `0x796DE696` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_MOVIE_KEYFRAME_WAIT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_MOVIE_KEYFRAME_WAIT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TV_PLAYER_WATCHING_THIS_FRAME

**Hash:** `0xD1C55B110E4DF534` | **JHash:** `0xD99EC000` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TV_PLAYER_WATCHING_THIS_FRAME(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TV_PLAYER_WATCHING_THIS_FRAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_CURRENT_TV_CLIP_NAMEHASH

**Hash:** `0x30432A0118736E00` | **Build:** `1493`

**C++ 原型:**
```cpp
Hash GET_CURRENT_TV_CLIP_NAMEHASH(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_CURRENT_TV_CLIP_NAMEHASH);
```

**返回值:** `Hash` → `uint`

---

### ENABLE_MOVIE_SUBTITLES

**Hash:** `0x873FA65C778AD970` | **JHash:** `0xC2DEBA3D` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_MOVIE_SUBTITLES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_MOVIE_SUBTITLES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### UI3DSCENE_IS_AVAILABLE

**Hash:** `0xD3A10FC7FD8D98CD` | **JHash:** `0xE40A0F1A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UI3DSCENE_IS_AVAILABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UI3DSCENE_IS_AVAILABLE);
```

**返回值:** `BOOL` → `bool`

---

### UI3DSCENE_PUSH_PRESET

**Hash:** `0xF1CEA8A4198D8E9A` | **JHash:** `0x2E7D9B98` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UI3DSCENE_PUSH_PRESET(const char* presetName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UI3DSCENE_PUSH_PRESET, presetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `presetName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** All presets can be found in common\data\ui\uiscenes.meta

---

### UI3DSCENE_ASSIGN_PED_TO_SLOT

**Hash:** `0x98C4FE6EC34154CA` | **JHash:** `0x9A0E3BFE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UI3DSCENE_ASSIGN_PED_TO_SLOT(const char* presetName, Ped ped, int slot, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UI3DSCENE_ASSIGN_PED_TO_SLOT, presetName, ped, slot, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `presetName` | `const char*` | `string` | |
| `ped` | `Ped` | `int` | |
| `slot` | `int` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** It's called after UI3DSCENE_IS_AVAILABLE and UI3DSCENE_PUSH_PRESET

presetName was always "CELEBRATION_WINNER"
All presets can be found in common\data\ui\uiscenes.meta

---

### UI3DSCENE_CLEAR_PATCHED_DATA

**Hash:** `0x7A42B2E236E71415` | **JHash:** `0x431AA036` | **Build:** `323`

**C++ 原型:**
```cpp
void UI3DSCENE_CLEAR_PATCHED_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UI3DSCENE_CLEAR_PATCHED_DATA);
```

**返回值:** `void` → `void`

---

### UI3DSCENE_MAKE_PUSHED_PRESET_PERSISTENT

**Hash:** `0x108BE26959A9D9BB` | **JHash:** `0x24A7A7F6` | **Build:** `323`

**C++ 原型:**
```cpp
void UI3DSCENE_MAKE_PUSHED_PRESET_PERSISTENT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UI3DSCENE_MAKE_PUSHED_PRESET_PERSISTENT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TERRAINGRID_ACTIVATE

**Hash:** `0xA356990E161C9E65` | **JHash:** `0xA1CB6C94` | **Build:** `323`

**C++ 原型:**
```cpp
void TERRAINGRID_ACTIVATE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERRAINGRID_ACTIVATE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native enables/disables the gold putting grid display (https://i.imgur.com/TC6cku6.png).
This requires these two natives to be called as well to configure the grid: `TERRAINGRID_SET_PARAMS` and `TERRAINGRID_SET_COLOURS`.

---

### TERRAINGRID_SET_PARAMS

**Hash:** `0x1C4FC5752BCD8E48` | **JHash:** `0x3B637AA7` | **Build:** `323`

**C++ 原型:**
```cpp
void TERRAINGRID_SET_PARAMS(float x, float y, float z, float forwardX, float forwardY, float forwardZ, float sizeX, float sizeY, float sizeZ, float gridScale, float glowIntensity, float normalHeight, float heightDiff)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERRAINGRID_SET_PARAMS, x, y, z, forwardX, forwardY, forwardZ, sizeX, sizeY, sizeZ, gridScale, glowIntensity, normalHeight, heightDiff);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `forwardX` | `float` | `float` | |
| `forwardY` | `float` | `float` | |
| `forwardZ` | `float` | `float` | |
| `sizeX` | `float` | `float` | |
| `sizeY` | `float` | `float` | |
| `sizeZ` | `float` | `float` | |
| `gridScale` | `float` | `float` | |
| `glowIntensity` | `float` | `float` | |
| `normalHeight` | `float` | `float` | |
| `heightDiff` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native is used along with these two natives: `TERRAINGRID_ACTIVATE` and `TERRAINGRID_SET_COLOURS`.
This native configures the location, size, rotation, normal height, and the difference ratio between min, normal and max.

All those natives combined they will output something like this: https://i.imgur.com/TC6cku6.png

This native renders a box at the given position, with a special shader that renders a grid on world geometry behind it. This box does not have backface culling.
The forward args here are a direction vector, something similar to what's returned by GET_ENTITY_FORWARD_VECTOR.
normalHeight and heightDiff are used for positioning the color gradient of the grid, colors specified via TERRAINGRID_SET_COLOURS.

Example with box superimposed on the image to demonstrate: https://i.imgur.com/wdqskxd.jpg

---

### TERRAINGRID_SET_COLOURS

**Hash:** `0x5CE62918F8D703C7` | **JHash:** `0xDF552973` | **Build:** `323`

**C++ 原型:**
```cpp
void TERRAINGRID_SET_COLOURS(int lowR, int lowG, int lowB, int lowAlpha, int r, int g, int b, int alpha, int highR, int highG, int highB, int highAlpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERRAINGRID_SET_COLOURS, lowR, lowG, lowB, lowAlpha, r, g, b, alpha, highR, highG, highB, highAlpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `lowR` | `int` | `int` | |
| `lowG` | `int` | `int` | |
| `lowB` | `int` | `int` | |
| `lowAlpha` | `int` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `highR` | `int` | `int` | |
| `highG` | `int` | `int` | |
| `highB` | `int` | `int` | |
| `highAlpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This native is used along with these two natives: `TERRAINGRID_ACTIVATE` and `TERRAINGRID_SET_PARAMS`.
This native sets the colors for the golf putting grid. the 'min...' values are for the lower areas that the grid covers, the 'max...' values are for the higher areas that the grid covers, all remaining values are for the 'normal' ground height.
All those natives combined they will output something like this: https://i.imgur.com/TC6cku6.png

---

### ANIMPOSTFX_PLAY

**Hash:** `0x2206BF9A37B7F724` | **JHash:** `0x1D980479` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMPOSTFX_PLAY(const char* effectName, int duration, BOOL looped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMPOSTFX_PLAY, effectName, duration, looped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |
| `duration` | `int` | `int` | |
| `looped` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** duration - is how long to play the effect for in milliseconds. If 0, it plays the default length
if loop is true, the effect won't stop until you call ANIMPOSTFX_STOP on it. (only loopable effects)

Full list of animpostFX / screen effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animPostFxNamesCompact.json

---

### ANIMPOSTFX_STOP

**Hash:** `0x068E835A1D0DC0E3` | **JHash:** `0x06BB5CDA` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMPOSTFX_STOP(const char* effectName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMPOSTFX_STOP, effectName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** See ANIMPOSTFX_PLAY

Full list of animpostFX / screen effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animPostFxNamesCompact.json

---

### ANIMPOSTFX_GET_CURRENT_TIME

**Hash:** `0xE35B38A27E8E7179` | **Build:** `877`

**C++ 原型:**
```cpp
float ANIMPOSTFX_GET_CURRENT_TIME(const char* effectName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ANIMPOSTFX_GET_CURRENT_TIME, effectName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |

**返回值:** `float` → `float`

**说明:** See ANIMPOSTFX_PLAY

Full list of animpostFX / screen effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animPostFxNamesCompact.json

---

### ANIMPOSTFX_IS_RUNNING

**Hash:** `0x36AD3E690DA5ACEB` | **JHash:** `0x089D5921` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ANIMPOSTFX_IS_RUNNING(const char* effectName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ANIMPOSTFX_IS_RUNNING, effectName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified effect is active.
See ANIMPOSTFX_PLAY

Full list of animpostFX / screen effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animPostFxNamesCompact.json

---

### ANIMPOSTFX_STOP_ALL

**Hash:** `0xB4EDDC19532BFB85` | **JHash:** `0x4E6D875B` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMPOSTFX_STOP_ALL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMPOSTFX_STOP_ALL);
```

**返回值:** `void` → `void`

**说明:** Stops ALL currently playing effects.

---

### ANIMPOSTFX_STOP_AND_FLUSH_REQUESTS

**Hash:** `0xD2209BE128B5418C` | **Build:** `323`

**C++ 原型:**
```cpp
void ANIMPOSTFX_STOP_AND_FLUSH_REQUESTS(const char* effectName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ANIMPOSTFX_STOP_AND_FLUSH_REQUESTS, effectName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `effectName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Stops the effect and sets a value (bool) in its data (+0x199) to false.
See ANIMPOSTFX_PLAY

Full list of animpostFX / screen effects by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animPostFxNamesCompact.json

---


---

## HUD

**包含 519 个 Native 函数**

### BEGIN_TEXT_COMMAND_BUSYSPINNER_ON

**Hash:** `0xABA17D7CE615ADBF` | **JHash:** `0xCB7C8994` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_BUSYSPINNER_ON(const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_BUSYSPINNER_ON, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Initializes the text entry for the the text next to a loading prompt. All natives for building UI texts can be used here


e.g
void StartLoadingMessage(char *text, int spinnerType = 3)
  {
     BEGIN_TEXT_COMMAND_BUSYSPINNER_ON("STRING");
       ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
       END_TEXT_COMMAND_BUSYSPINNER_ON(spinnerType);
    }
/*OR*/
 void ShowLoadingMessage(char *text, int spinnerType = 3, int timeMs = 10000)
  {
     BEGIN_TEXT_COMMAND_BUSYSPINNER_ON("STRING");
       ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
       END_TEXT_COMMAND_BUSYSPINNER_ON(spinnerType);
        WAIT(timeMs);
     BUSYSPINNER_OFF();
 }


These are some localized strings used in the loading spinner.
"PM_WAIT"                   = Please Wait
"CELEB_WPLYRS"              = Waiting For Players.
"CELL_SPINNER2"             = Scanning storage.
"ERROR_CHECKYACHTNAME" = Registering your yacht's name. Please wait.
"ERROR_CHECKPROFANITY"   = Checking your text for profanity. Please wait.
"FM_COR_AUTOD"                        = Just spinner no text
"FM_IHELP_WAT2"                        = Waiting for other players
"FM_JIP_WAITO"                            = Game options are being set
"FMMC_DOWNLOAD"                    = Downloading
"FMMC_PLYLOAD"                         = Loading
"FMMC_STARTTRAN"                    = Launching session
"HUD_QUITTING"                           =  Quiting session
"KILL_STRIP_IDM"                         = Waiting for to accept
"MP_SPINLOADING"                      = Loading

---

### END_TEXT_COMMAND_BUSYSPINNER_ON

**Hash:** `0xBD12F8228410D9B4` | **JHash:** `0x903F5EE4` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_BUSYSPINNER_ON(int busySpinnerType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_BUSYSPINNER_ON, busySpinnerType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `busySpinnerType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eBusySpinnerType
{
	BUSY_SPINNER_LEFT,
	BUSY_SPINNER_LEFT_2,
	BUSY_SPINNER_LEFT_3,
	BUSY_SPINNER_SAVE,
	BUSY_SPINNER_RIGHT,
};

---

### BUSYSPINNER_OFF

**Hash:** `0x10D373323E5B9C0D` | **JHash:** `0x94119534` | **Build:** `323`

**C++ 原型:**
```cpp
void BUSYSPINNER_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BUSYSPINNER_OFF);
```

**返回值:** `void` → `void`

**说明:** Removes the loading prompt at the bottom right of the screen.

---

### PRELOAD_BUSYSPINNER

**Hash:** `0xC65AB383CD91DF98` | **JHash:** `0x71077FBD` | **Build:** `323`

**C++ 原型:**
```cpp
void PRELOAD_BUSYSPINNER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_BUSYSPINNER);
```

**返回值:** `void` → `void`

---

### BUSYSPINNER_IS_ON

**Hash:** `0xD422FCC5F239A915` | **JHash:** `0xB8B3A5D0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BUSYSPINNER_IS_ON(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BUSYSPINNER_IS_ON);
```

**返回值:** `BOOL` → `bool`

---

### BUSYSPINNER_IS_DISPLAYING

**Hash:** `0xB2A592B04648A9CB` | **JHash:** `0x3AF34DEF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BUSYSPINNER_IS_DISPLAYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BUSYSPINNER_IS_DISPLAYING);
```

**返回值:** `BOOL` → `bool`

---

### DISABLE_PAUSEMENU_SPINNER

**Hash:** `0x9245E81072704B8A` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PAUSEMENU_SPINNER(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PAUSEMENU_SPINNER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MOUSE_CURSOR_THIS_FRAME

**Hash:** `0xAAE7CE1D63167423` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOUSE_CURSOR_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOUSE_CURSOR_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Shows the cursor on screen for one frame.

---

### SET_MOUSE_CURSOR_STYLE

**Hash:** `0x8DB8CFFD58B62552` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOUSE_CURSOR_STYLE(int spriteId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOUSE_CURSOR_STYLE, spriteId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `spriteId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Changes the mouse cursor's sprite. 
1 = Normal
6 = Left Arrow
7 = Right Arrow

---

### SET_MOUSE_CURSOR_VISIBLE

**Hash:** `0x98215325A695E78A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOUSE_CURSOR_VISIBLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOUSE_CURSOR_VISIBLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Shows/hides the frontend cursor on the pause menu or similar menus.
Clicking off and then on the game window will show it again.

---

### IS_MOUSE_ROLLED_OVER_INSTRUCTIONAL_BUTTONS

**Hash:** `0x3D9ACB1EB139E702` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOUSE_ROLLED_OVER_INSTRUCTIONAL_BUTTONS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOUSE_ROLLED_OVER_INSTRUCTIONAL_BUTTONS);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns TRUE if mouse is hovering above instructional buttons. Works with all buttons gfx, such as popup_warning, pause_menu_instructional_buttons, instructional_buttons, etc. Note: You have to call TOGGLE_MOUSE_BUTTONS on the scaleform if you want this native to work.

---

### GET_MOUSE_EVENT

**Hash:** `0x632B2940C67F4EA9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MOUSE_EVENT(int scaleformHandle, Any* p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MOUSE_EVENT, scaleformHandle, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHandle` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### THEFEED_ONLY_SHOW_TOOLTIPS

**Hash:** `0x6F1554B0CC2089FA` | **JHash:** `0xA7C8594B` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_ONLY_SHOW_TOOLTIPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_ONLY_SHOW_TOOLTIPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### THEFEED_SET_SCRIPTED_MENU_HEIGHT

**Hash:** `0x55598D21339CB998` | **JHash:** `0x1DA7E41A` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_SCRIPTED_MENU_HEIGHT(float pos)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_SCRIPTED_MENU_HEIGHT, pos);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pos` | `float` | `float` | |

**返回值:** `void` → `void`

---

### THEFEED_HIDE

**Hash:** `0x32888337579A5970` | **Build:** `463`

**C++ 原型:**
```cpp
void THEFEED_HIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_HIDE);
```

**返回值:** `void` → `void`

**说明:** Stops loading screen tips shown by invoking `THEFEED_SHOW`

---

### THEFEED_HIDE_THIS_FRAME

**Hash:** `0x25F87B30C382FCA7` | **JHash:** `0x1E63088A` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_HIDE_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_HIDE_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Once called each frame hides all above radar notifications.

---

### THEFEED_SHOW

**Hash:** `0x15CFA549788D35EF` | **Build:** `463`

**C++ 原型:**
```cpp
void THEFEED_SHOW(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SHOW);
```

**返回值:** `void` → `void`

**说明:** Displays loading screen tips, requires `THEFEED_AUTO_POST_GAMETIPS_ON` to be called beforehand.

---

### THEFEED_FLUSH_QUEUE

**Hash:** `0xA8FDB297A8D25FBA` | **JHash:** `0x5205C6F5` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_FLUSH_QUEUE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_FLUSH_QUEUE);
```

**返回值:** `void` → `void`

---

### THEFEED_REMOVE_ITEM

**Hash:** `0xBE4390CB40B3E627` | **JHash:** `0xECA8ACB9` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_REMOVE_ITEM(int notificationId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_REMOVE_ITEM, notificationId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `notificationId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Removes a notification instantly instead of waiting for it to disappear

---

### THEFEED_FORCE_RENDER_ON

**Hash:** `0xA13C11E1B5C06BFC` | **JHash:** `0x520FCB6D` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_FORCE_RENDER_ON(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_FORCE_RENDER_ON);
```

**返回值:** `void` → `void`

---

### THEFEED_FORCE_RENDER_OFF

**Hash:** `0x583049884A2EEE3C` | **JHash:** `0xC8BAB2F2` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_FORCE_RENDER_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_FORCE_RENDER_OFF);
```

**返回值:** `void` → `void`

**说明:** Enables loading screen tips to be be shown (`THEFEED_SHOW`), blocks other kinds of notifications from being displayed (at least from current script). Call `0xADED7F5748ACAFE6` to display those again.

---

### THEFEED_PAUSE

**Hash:** `0xFDB423997FA30340` | **JHash:** `0x4D0449C6` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_PAUSE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_PAUSE);
```

**返回值:** `void` → `void`

---

### THEFEED_RESUME

**Hash:** `0xE1CD1E48E025E661` | **JHash:** `0xD3F40140` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_RESUME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_RESUME);
```

**返回值:** `void` → `void`

---

### THEFEED_IS_PAUSED

**Hash:** `0xA9CBFD40B3FA3010` | **JHash:** `0xC5223796` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL THEFEED_IS_PAUSED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.THEFEED_IS_PAUSED);
```

**返回值:** `BOOL` → `bool`

---

### THEFEED_REPORT_LOGO_ON

**Hash:** `0xD4438C0564490E63` | **JHash:** `0x709B4BCB` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_REPORT_LOGO_ON(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_REPORT_LOGO_ON);
```

**返回值:** `void` → `void`

---

### THEFEED_REPORT_LOGO_OFF

**Hash:** `0xB695E2CD0A2DA9EE` | **JHash:** `0x4A4A40A4` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_REPORT_LOGO_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_REPORT_LOGO_OFF);
```

**返回值:** `void` → `void`

---

### THEFEED_GET_LAST_SHOWN_PHONE_ACTIVATABLE_FEED_ID

**Hash:** `0x82352748437638CA` | **JHash:** `0x294405D4` | **Build:** `323`

**C++ 原型:**
```cpp
int THEFEED_GET_LAST_SHOWN_PHONE_ACTIVATABLE_FEED_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.THEFEED_GET_LAST_SHOWN_PHONE_ACTIVATABLE_FEED_ID);
```

**返回值:** `int` → `int`

**说明:** Returns the handle for the notification currently displayed on the screen. Name may be a hash collision, but describes the function accurately.

---

### THEFEED_AUTO_POST_GAMETIPS_ON

**Hash:** `0x56C8B608CFD49854` | **JHash:** `0xF881AB87` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_AUTO_POST_GAMETIPS_ON(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_AUTO_POST_GAMETIPS_ON);
```

**返回值:** `void` → `void`

**说明:** Enables loading screen tips to be be shown (`THEFEED_SHOW`), blocks other kinds of notifications from being displayed (at least from current script). Call `THEFEED_AUTO_POST_GAMETIPS_OFF` to display those again.

---

### THEFEED_AUTO_POST_GAMETIPS_OFF

**Hash:** `0xADED7F5748ACAFE6` | **JHash:** `0x1D6859CA` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_AUTO_POST_GAMETIPS_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_AUTO_POST_GAMETIPS_OFF);
```

**返回值:** `void` → `void`

**说明:** Displays "normal" notifications again after calling `THEFEED_AUTO_POST_GAMETIPS_ON` (those that were drawn before calling this native too), though those will have a weird offset and stay on screen forever (tested with notifications created from same script).

---

### THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST

**Hash:** `0x92F0DA1E27DB96DC` | **JHash:** `0x07CE2EA4` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST(int hudColorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST, hudColorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** From the decompiled scripts:
HUD::THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST(6);
HUD::THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST(184);
HUD::THEFEED_SET_BACKGROUND_COLOR_FOR_NEXT_POST(190);

sets background color for the next notification
6 = red
184 = green
190 = yellow

Here is a list of some colors that can be used: https://gyazo.com/68bd384455fceb0a85a8729e48216e15

---

### THEFEED_SET_RGBA_PARAMETER_FOR_NEXT_MESSAGE

**Hash:** `0x17430B918701C342` | **JHash:** `0xCF14D7F2` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_RGBA_PARAMETER_FOR_NEXT_MESSAGE(int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_RGBA_PARAMETER_FOR_NEXT_MESSAGE, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

---

### THEFEED_SET_FLASH_DURATION_PARAMETER_FOR_NEXT_MESSAGE

**Hash:** `0x17AD8C9706BDD88A` | **JHash:** `0x24A97AF8` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_FLASH_DURATION_PARAMETER_FOR_NEXT_MESSAGE(int count)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_FLASH_DURATION_PARAMETER_FOR_NEXT_MESSAGE, count);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `count` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Related to notification color flashing, setting count to 0 invalidates a `THEFEED_SET_RGBA_PARAMETER_FOR_NEXT_MESSAGE` call for the target notification.

---

### THEFEED_SET_VIBRATE_PARAMETER_FOR_NEXT_MESSAGE

**Hash:** `0x4A0C7C9BB10ABB36` | **JHash:** `0x44018EDB` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_VIBRATE_PARAMETER_FOR_NEXT_MESSAGE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_VIBRATE_PARAMETER_FOR_NEXT_MESSAGE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### THEFEED_RESET_ALL_PARAMETERS

**Hash:** `0xFDD85225B2DEA55E` | **JHash:** `0xA4524B23` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_RESET_ALL_PARAMETERS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_RESET_ALL_PARAMETERS);
```

**返回值:** `void` → `void`

---

### THEFEED_FREEZE_NEXT_POST

**Hash:** `0xFDEC055AB549E328` | **JHash:** `0xAFA1148B` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_FREEZE_NEXT_POST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_FREEZE_NEXT_POST);
```

**返回值:** `void` → `void`

**说明:** Requires manual management of game stream handles (i.e., 0xBE4390CB40B3E627).

---

### THEFEED_CLEAR_FROZEN_POST

**Hash:** `0x80FE4F3AB4E1B62A` | **JHash:** `0x3CD4307C` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_CLEAR_FROZEN_POST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_CLEAR_FROZEN_POST);
```

**返回值:** `void` → `void`

---

### THEFEED_SET_SNAP_FEED_ITEM_POSITIONS

**Hash:** `0xBAE4F9B97CD43B30` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_SET_SNAP_FEED_ITEM_POSITIONS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_SET_SNAP_FEED_ITEM_POSITIONS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### THEFEED_UPDATE_ITEM_TEXTURE

**Hash:** `0x317EBA71D7543F52` | **JHash:** `0x6A3F747D` | **Build:** `323`

**C++ 原型:**
```cpp
void THEFEED_UPDATE_ITEM_TEXTURE(const char* txdString1, const char* txnString1, const char* txdString2, const char* txnString2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.THEFEED_UPDATE_ITEM_TEXTURE, txdString1, txnString1, txdString2, txnString2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdString1` | `const char*` | `string` | |
| `txnString1` | `const char*` | `string` | |
| `txdString2` | `const char*` | `string` | |
| `txnString2` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Used in the native scripts to reference "GET_PEDHEADSHOT_TXD_STRING" and "CHAR_DEFAULT".

---

### BEGIN_TEXT_COMMAND_THEFEED_POST

**Hash:** `0x202709F4C58A0424` | **JHash:** `0x574EE85C` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_THEFEED_POST(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_THEFEED_POST, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Declares the entry type of a notification, for example "STRING".

int ShowNotification(char *text)
{
	BEGIN_TEXT_COMMAND_THEFEED_POST("STRING");
	ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
	return END_TEXT_COMMAND_THEFEED_POST_TICKER(1, 1);
}

---

### END_TEXT_COMMAND_THEFEED_POST_STATS

**Hash:** `0x2B7E9A4EAAA93C89` | **JHash:** `0xED130FA1` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_STATS(const char* statTitle, int iconEnum, BOOL stepVal, int barValue, BOOL isImportant, const char* pictureTextureDict, const char* pictureTextureName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_STATS, statTitle, iconEnum, stepVal, barValue, isImportant, pictureTextureDict, pictureTextureName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statTitle` | `const char*` | `string` | |
| `iconEnum` | `int` | `int` | |
| `stepVal` | `BOOL` | `bool` | |
| `barValue` | `int` | `int` | |
| `isImportant` | `BOOL` | `bool` | |
| `pictureTextureDict` | `const char*` | `string` | |
| `pictureTextureName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** List of picture names: https://pastebin.com/XdpJVbHz
Example result: https://i.imgur.com/SdEZ22m.png

---

### END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT

**Hash:** `0x1CCD9A37359072CF` | **JHash:** `0xE7E3C98B` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT(const char* txdName, const char* textureName, BOOL flash, int iconType, const char* sender, const char* subject)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT, txdName, textureName, flash, iconType, sender, subject);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `flash` | `BOOL` | `bool` | |
| `iconType` | `int` | `int` | |
| `sender` | `const char*` | `string` | |
| `subject` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

List of picNames: https://pastebin.com/XdpJVbHz


flash is a bool for fading in.
iconTypes:
1 : Chat Box
2 : Email
3 : Add Friend Request
4 : Nothing
5 : Nothing
6 : Nothing
7 : Right Jumping Arrow
8 : RP Icon
9 : $ Icon

"sender" is the very top header. This can be any old string.
"subject" is the header under the sender.

---

### END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_SUBTITLE_LABEL

**Hash:** `0xC6F580E4C94926AC` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_SUBTITLE_LABEL(const char* txdName, const char* textureName, BOOL flash, int iconType, const char* sender, const char* subject)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_SUBTITLE_LABEL, txdName, textureName, flash, iconType, sender, subject);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `flash` | `BOOL` | `bool` | |
| `iconType` | `int` | `int` | |
| `sender` | `const char*` | `string` | |
| `subject` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

Needs more research.

Only one type of usage in the scripts:

HUD::END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_SUBTITLE_LABEL("CHAR_ACTING_UP", "CHAR_ACTING_UP", 0, 0, "DI_FEED_CHAR", a_0);

---

### END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_TU

**Hash:** `0x1E6611149DB3DB6B` | **JHash:** `0x0EB382B7` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_TU(const char* txdName, const char* textureName, BOOL flash, int iconType, const char* sender, const char* subject, float duration)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_TU, txdName, textureName, flash, iconType, sender, subject, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `flash` | `BOOL` | `bool` | |
| `iconType` | `int` | `int` | |
| `sender` | `const char*` | `string` | |
| `subject` | `const char*` | `string` | |
| `duration` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

NOTE: 'duration' is a multiplier, so 1.0 is normal, 2.0 is twice as long (very slow), and 0.5 is half as long.

Example, only occurrence in the scripts:
v_8 = HUD::END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_TU("CHAR_SOCIAL_CLUB", "CHAR_SOCIAL_CLUB", 0, 0, &v_9, "", a_5);

---

### END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG

**Hash:** `0x5CBF7BADE20DB93E` | **JHash:** `0x3E807FE3` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG(const char* txdName, const char* textureName, BOOL flash, int iconType, const char* sender, const char* subject, float duration, const char* clanTag)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG, txdName, textureName, flash, iconType, sender, subject, duration, clanTag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `flash` | `BOOL` | `bool` | |
| `iconType` | `int` | `int` | |
| `sender` | `const char*` | `string` | |
| `subject` | `const char*` | `string` | |
| `duration` | `float` | `float` | |
| `clanTag` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

List of picNames https://pastebin.com/XdpJVbHz

flash is a bool for fading in.
iconTypes:
1 : Chat Box
2 : Email
3 : Add Friend Request
4 : Nothing
5 : Nothing
6 : Nothing
7 : Right Jumping Arrow
8 : RP Icon
9 : $ Icon

"sender" is the very top header. This can be any old string.
"subject" is the header under the sender.
"duration" is a multiplier, so 1.0 is normal, 2.0 is twice as long (very slow), and 0.5 is half as long.
"clanTag" shows a crew tag in the "sender" header, after the text. You need to use 3 underscores as padding. Maximum length of this field seems to be 7. (e.g. "MK" becomes "___MK", "ACE" becomes "___ACE", etc.)

---

### END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG_AND_ADDITIONAL_ICON

**Hash:** `0x531B84E7DA981FB6` | **JHash:** `0xDEB491C8` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG_AND_ADDITIONAL_ICON(const char* txdName, const char* textureName, BOOL flash, int iconType1, const char* sender, const char* subject, float duration, const char* clanTag, int iconType2, int p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MESSAGETEXT_WITH_CREW_TAG_AND_ADDITIONAL_ICON, txdName, textureName, flash, iconType1, sender, subject, duration, clanTag, iconType2, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `flash` | `BOOL` | `bool` | |
| `iconType1` | `int` | `int` | |
| `sender` | `const char*` | `string` | |
| `subject` | `const char*` | `string` | |
| `duration` | `float` | `float` | |
| `clanTag` | `const char*` | `string` | |
| `iconType2` | `int` | `int` | |
| `p9` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

List of picNames:  https://pastebin.com/XdpJVbHz

flash is a bool for fading in.
iconTypes:
1 : Chat Box
2 : Email
3 : Add Friend Request
4 : Nothing
5 : Nothing
6 : Nothing
7 : Right Jumping Arrow
8 : RP Icon
9 : $ Icon

"sender" is the very top header. This can be any old string.
"subject" is the header under the sender.
"duration" is a multiplier, so 1.0 is normal, 2.0 is twice as long (very slow), and 0.5 is half as long.
"clanTag" shows a crew tag in the "sender" header, after the text. You need to use 3 underscores as padding. Maximum length of this field seems to be 7. (e.g. "MK" becomes "___MK", "ACE" becomes "___ACE", etc.)
iconType2 is a mirror of iconType. It shows in the "subject" line, right under the original iconType.


int IconNotification(char *text, char *text2, char *Subject)
{
    BEGIN_TEXT_COMMAND_THEFEED_POST("STRING");
 ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
   _SET_NOTIFICATION_MESSAGE_CLAN_TAG_2("CHAR_SOCIAL_CLUB", "CHAR_SOCIAL_CLUB", 1, 7, text2, Subject, 1.0f, "__EXAMPLE", 7);
   return END_TEXT_COMMAND_THEFEED_POST_TICKER(1, 1);
}

---

### END_TEXT_COMMAND_THEFEED_POST_TICKER

**Hash:** `0x2ED7843F8F801023` | **JHash:** `0x08F7AF78` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_TICKER(BOOL blink, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_TICKER, blink, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blink` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_TICKER_FORCED

**Hash:** `0x44FA03975424A0EE` | **JHash:** `0x57B8D0D4` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_TICKER_FORCED(BOOL blink, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_TICKER_FORCED, blink, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blink` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_TICKER_WITH_TOKENS

**Hash:** `0x378E809BF61EC840` | **JHash:** `0x02BCAF9B` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_TICKER_WITH_TOKENS(BOOL blink, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_TICKER_WITH_TOKENS, blink, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blink` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_AWARD

**Hash:** `0xAA295B6F28BD587D` | **JHash:** `0x02DED2B8` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_AWARD(const char* textureDict, const char* textureName, int rpBonus, int colorOverlay, const char* titleLabel)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_AWARD, textureDict, textureName, rpBonus, colorOverlay, titleLabel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textureDict` | `const char*` | `string` | |
| `textureName` | `const char*` | `string` | |
| `rpBonus` | `int` | `int` | |
| `colorOverlay` | `int` | `int` | |
| `titleLabel` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Shows an "award" notification above the minimap, example: https://i.imgur.com/e2DNaKX.png
Example:

HUD::BEGIN_TEXT_COMMAND_THEFEED_POST("HUNT");
HUD::END_TEXT_COMMAND_THEFEED_POST_AWARD("Hunting", "Hunting_Gold_128", 0, 109, "HUD_MED_UNLKED");

---

### END_TEXT_COMMAND_THEFEED_POST_CREWTAG

**Hash:** `0x97C9E4E7024A8F2C` | **JHash:** `0xA9CCEF66` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_CREWTAG(BOOL p0, BOOL p1, int* p2, int p3, BOOL isLeader, BOOL unk0, int clanDesc, int R, int G, int B)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_CREWTAG, p0, p1, p2, p3, isLeader, unk0, clanDesc, R, G, B);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int*` | `int*` | |
| `p3` | `int` | `int` | |
| `isLeader` | `BOOL` | `bool` | |
| `unk0` | `BOOL` | `bool` | |
| `clanDesc` | `int` | `int` | |
| `R` | `int` | `int` | |
| `G` | `int` | `int` | |
| `B` | `int` | `int` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_CREWTAG_WITH_GAME_NAME

**Hash:** `0x137BC35589E34E1E` | **JHash:** `0x88B9B909` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_CREWTAG_WITH_GAME_NAME(BOOL p0, BOOL p1, int* p2, int p3, BOOL isLeader, BOOL unk0, int clanDesc, const char* playerName, int R, int G, int B)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_CREWTAG_WITH_GAME_NAME, p0, p1, p2, p3, isLeader, unk0, clanDesc, playerName, R, G, B);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int*` | `int*` | |
| `p3` | `int` | `int` | |
| `isLeader` | `BOOL` | `bool` | |
| `unk0` | `BOOL` | `bool` | |
| `clanDesc` | `int` | `int` | |
| `playerName` | `const char*` | `string` | |
| `R` | `int` | `int` | |
| `G` | `int` | `int` | |
| `B` | `int` | `int` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_UNLOCK

**Hash:** `0x33EE12743CCD6343` | **JHash:** `0xE05E7052` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_UNLOCK(const char* gxtLabel1, int p1, const char* gxtLabel2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_UNLOCK, gxtLabel1, p1, gxtLabel2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxtLabel1` | `const char*` | `string` | |
| `p1` | `int` | `int` | |
| `gxtLabel2` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU

**Hash:** `0xC8F3AAF93D0600BF` | **JHash:** `0x4FA43BA4` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU(const char* gxtLabel1, int p1, const char* gxtLabel2, int p3)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU, gxtLabel1, p1, gxtLabel2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxtLabel1` | `const char*` | `string` | |
| `p1` | `int` | `int` | |
| `gxtLabel2` | `const char*` | `string` | |
| `p3` | `int` | `int` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU_WITH_COLOR

**Hash:** `0x7AE0589093A2E088` | **JHash:** `0x8C90D22F` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU_WITH_COLOR(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_UNLOCK_TU_WITH_COLOR, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_MPTICKER

**Hash:** `0xF020C96915705B3A` | **JHash:** `0x8E319AB8` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_MPTICKER(BOOL blink, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_MPTICKER, blink, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blink` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_CREW_RANKUP_WITH_LITERAL_FLAG

**Hash:** `0x8EFCCF6EC66D85E4` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_CREW_RANKUP_WITH_LITERAL_FLAG(const char* p0, const char* p1, const char* p2, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_CREW_RANKUP_WITH_LITERAL_FLAG, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### END_TEXT_COMMAND_THEFEED_POST_VERSUS_TU

**Hash:** `0xB6871B0555B02996` | **JHash:** `0x5E93FBFA` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_VERSUS_TU(const char* txdName1, const char* textureName1, int count1, const char* txdName2, const char* textureName2, int count2, int hudColor1, int hudColor2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_VERSUS_TU, txdName1, textureName1, count1, txdName2, textureName2, count2, hudColor1, hudColor2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `txdName1` | `const char*` | `string` | |
| `textureName1` | `const char*` | `string` | |
| `count1` | `int` | `int` | |
| `txdName2` | `const char*` | `string` | |
| `textureName2` | `const char*` | `string` | |
| `count2` | `int` | `int` | |
| `hudColor1` | `int` | `int` | |
| `hudColor2` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** This function can show pictures of every texture that can be requested by REQUEST_STREAMED_TEXTURE_DICT.

List of picNames: https://pastebin.com/XdpJVbHz
HUD colors and their values: https://pastebin.com/d9aHPbXN

Shows a deathmatch score above the minimap, example: https://i.imgur.com/YmoMklG.png

---

### END_TEXT_COMMAND_THEFEED_POST_REPLAY

**Hash:** `0xD202B92CBF1D816F` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_REPLAY(int type, int image, const char* text)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_REPLAY, type, image, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `image` | `int` | `int` | |
| `text` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** returns a notification handle, prints out a notification like below:
type range: 0 - 2
if you set type to 1, image goes from 0 - 39 - Xbox you can add text to

example: 
HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1, 20, "Who you trynna get crazy with, ese? Don't you know I'm LOCO?!");
- https://imgur.com/lGBPCz3

---

### END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT

**Hash:** `0xDD6CB2CCE7C2735C` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(int type, const char* button, const char* text)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT, type, button, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `button` | `const char*` | `string` | |
| `text` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** returns a notification handle, prints out a notification like below:
type range: 0 - 2
if you set type to 1, button accepts "~INPUT_SOMETHING~"

example:
HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1, "~INPUT_TALK~", "Who you trynna get crazy with, ese? Don't you know I'm LOCO?!");
- https://imgur.com/UPy0Ial


Examples from the scripts:
l_D1[1/*1*/]=HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1,"~INPUT_REPLAY_START_STOP_RECORDING~","");
l_D1[2/*1*/]=HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1,"~INPUT_SAVE_REPLAY_CLIP~","");
l_D1[1/*1*/]=HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1,"~INPUT_REPLAY_START_STOP_RECORDING~","");
l_D1[2/*1*/]=HUD::END_TEXT_COMMAND_THEFEED_POST_REPLAY_INPUT(1,"~INPUT_REPLAY_START_STOP_RECORDING_SECONDARY~","");


---

### BEGIN_TEXT_COMMAND_PRINT

**Hash:** `0xB87A37EEB7FAA67D` | **JHash:** `0xF42C43C7` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_PRINT(const char* GxtEntry)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_PRINT, GxtEntry);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `GxtEntry` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** void ShowSubtitle(const char *text)
{
  BEGIN_TEXT_COMMAND_PRINT("STRING");
 ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
   END_TEXT_COMMAND_PRINT(2000, true);
}

---

### END_TEXT_COMMAND_PRINT

**Hash:** `0x9D77056A530643F6` | **JHash:** `0x38F82261` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_PRINT(int duration, BOOL drawImmediately)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_PRINT, duration, drawImmediately);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `duration` | `int` | `int` | |
| `drawImmediately` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Draws the subtitle at middle center of the screen.

int duration = time in milliseconds to show text on screen before disappearing

drawImmediately = If true, the text will be drawn immediately, if false, the text will be drawn after the previous subtitle has finished

Used to be known as _DRAW_SUBTITLE_TIMED

---

### BEGIN_TEXT_COMMAND_IS_MESSAGE_DISPLAYED

**Hash:** `0x853648FD1063A213` | **JHash:** `0xDD524A11` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_IS_MESSAGE_DISPLAYED(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_IS_MESSAGE_DISPLAYED, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** nothin doin. 

BOOL Message(const char* text)
   {
     BEGIN_TEXT_COMMAND_IS_MESSAGE_DISPLAYED("STRING");
      ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
       return END_TEXT_COMMAND_IS_MESSAGE_DISPLAYED();
   }

---

### END_TEXT_COMMAND_IS_MESSAGE_DISPLAYED

**Hash:** `0x8A9BA1AB3E237613` | **JHash:** `0x672EFB45` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL END_TEXT_COMMAND_IS_MESSAGE_DISPLAYED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.END_TEXT_COMMAND_IS_MESSAGE_DISPLAYED);
```

**返回值:** `BOOL` → `bool`

---

### BEGIN_TEXT_COMMAND_DISPLAY_TEXT

**Hash:** `0x25FBB336DF1804CB` | **JHash:** `0x3E35563E` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_DISPLAY_TEXT(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_DISPLAY_TEXT, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** The following were found in the decompiled script files:
STRING, TWOSTRINGS, NUMBER, PERCENTAGE, FO_TWO_NUM, ESMINDOLLA, ESDOLLA, MTPHPER_XPNO, AHD_DIST, CMOD_STAT_0, CMOD_STAT_1, CMOD_STAT_2, CMOD_STAT_3, DFLT_MNU_OPT, F3A_TRAFDEST, ES_HELP_SOC3

ESDOLLA - cash
ESMINDOLLA - cash (negative)

Used to be known as _SET_TEXT_ENTRY

---

### END_TEXT_COMMAND_DISPLAY_TEXT

**Hash:** `0xCD015E5BB0D96A57` | **JHash:** `0x6F8350CE` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_DISPLAY_TEXT(float x, float y, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_DISPLAY_TEXT, x, y, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** After applying the properties to the text (See HUD::SET_TEXT_), this will draw the text in the applied position. Also 0.0f < x, y < 1.0f, percentage of the axis.

Used to be known as _DRAW_TEXT

---

### BEGIN_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT

**Hash:** `0x54CE8AC98E120CAB` | **JHash:** `0x51E7A037` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### END_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT

**Hash:** `0x85F061DA64ED2F67` | **JHash:** `0xD12A643A` | **Build:** `323`

**C++ 原型:**
```cpp
float END_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT(BOOL p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.END_TEXT_COMMAND_GET_SCREEN_WIDTH_OF_DISPLAY_TEXT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

---

### BEGIN_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING

**Hash:** `0x521FB041D93DD0E4` | **JHash:** `0x94B82066` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING(const char* entry)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING, entry);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entry` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** int GetLineCount(char *text, float x, float y)
    {
     BEGIN_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING("STRING");
                ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
      return BEGIN_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING(x, y);
    }

---

### END_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING

**Hash:** `0x9040DFB09BE75706` | **JHash:** `0xAA318785` | **Build:** `323`

**C++ 原型:**
```cpp
int END_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING(float x, float y)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.END_TEXT_COMMAND_GET_NUMBER_OF_LINES_FOR_STRING, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Determines how many lines the text string will use when drawn on screen. 
Must use 0x521FB041D93DD0E4 for setting up

---

### BEGIN_TEXT_COMMAND_DISPLAY_HELP

**Hash:** `0x8509B634FBE7DA11` | **JHash:** `0xB245FC10` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_DISPLAY_HELP(const char* inputType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_DISPLAY_HELP, inputType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `inputType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Used to be known as _SET_TEXT_COMPONENT_FORMAT

---

### END_TEXT_COMMAND_DISPLAY_HELP

**Hash:** `0x238FFE5C7B0498A6` | **JHash:** `0xB59B530D` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_DISPLAY_HELP(int p0, BOOL loop, BOOL beep, int shape)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_DISPLAY_HELP, p0, loop, beep, shape);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `loop` | `BOOL` | `bool` | |
| `beep` | `BOOL` | `bool` | |
| `shape` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** shape goes from -1 to 50 (may be more).
p0 is always 0.

Example:
void FloatingHelpText(const char* text)
{
    BEGIN_TEXT_COMMAND_DISPLAY_HELP("STRING");
  ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(text);
   END_TEXT_COMMAND_DISPLAY_HELP (0, 0, 1, -1);
}

Image:
- imgbin.org/images/26209.jpg

more inputs/icons:
- https://pastebin.com/nqNYWMSB

Used to be known as _DISPLAY_HELP_TEXT_FROM_STRING_LABEL

---

### BEGIN_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED

**Hash:** `0x0A24DA3A41B718F5` | **JHash:** `0x00E20F2D` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED(const char* labelName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED, labelName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `labelName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** BOOL IsContextActive(char *ctx)
    {
     BEGIN_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED(ctx);
     return END_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED(0);
  }

---

### END_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED

**Hash:** `0x10BDDBFC529428DD` | **JHash:** `0xF63A13EC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL END_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.END_TEXT_COMMAND_IS_THIS_HELP_MESSAGE_BEING_DISPLAYED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### BEGIN_TEXT_COMMAND_SET_BLIP_NAME

**Hash:** `0xF9113A30DE5C6670` | **JHash:** `0xF4C211F6` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_SET_BLIP_NAME(const char* textLabel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_SET_BLIP_NAME, textLabel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `textLabel` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Starts a text command to change the name of a blip displayed in the pause menu.
This should be paired with `END_TEXT_COMMAND_SET_BLIP_NAME`, once adding all required text components.
Example:

HUD::BEGIN_TEXT_COMMAND_SET_BLIP_NAME("STRING");
HUD::ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME("Name");
HUD::END_TEXT_COMMAND_SET_BLIP_NAME(blip);

---

### END_TEXT_COMMAND_SET_BLIP_NAME

**Hash:** `0xBC38B49BCB83BC9B` | **JHash:** `0xE8E59820` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_SET_BLIP_NAME(Blip blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_SET_BLIP_NAME, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `void` → `void`

**说明:** Finalizes a text command started with BEGIN_TEXT_COMMAND_SET_BLIP_NAME, setting the name of the specified blip.

---

### BEGIN_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS

**Hash:** `0x23D69E0465570028` | **JHash:** `0x0E103475` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### END_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS

**Hash:** `0xCFDBDF5AE59BA0F4` | **JHash:** `0x2944A6C5` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_ADD_DIRECTLY_TO_PREVIOUS_BRIEFS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### BEGIN_TEXT_COMMAND_CLEAR_PRINT

**Hash:** `0xE124FA80A759019C` | **JHash:** `0x550665AE` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_CLEAR_PRINT(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_CLEAR_PRINT, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** clears a print text command with this text

---

### END_TEXT_COMMAND_CLEAR_PRINT

**Hash:** `0xFCC75460ABA29378` | **JHash:** `0x67785AF2` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_CLEAR_PRINT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_CLEAR_PRINT);
```

**返回值:** `void` → `void`

---

### BEGIN_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT

**Hash:** `0x8F9EE5687F8EECCD` | **JHash:** `0xBF855650` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT(const char* gxtEntry)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT, gxtEntry);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxtEntry` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### END_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT

**Hash:** `0xA86911979638106F` | **JHash:** `0x6E7FDA1C` | **Build:** `323`

**C++ 原型:**
```cpp
void END_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_TEXT_COMMAND_OVERRIDE_BUTTON_TEXT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_INTEGER

**Hash:** `0x03B504CF259931BC` | **JHash:** `0xFE272A57` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_INTEGER(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_INTEGER, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_FLOAT

**Hash:** `0xE7DCB5B874BCD96E` | **JHash:** `0x24D78013` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_FLOAT(float value, int decimalPlaces)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_FLOAT, value, decimalPlaces);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |
| `decimalPlaces` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL

**Hash:** `0xC63CD5D2920ACBE7` | **JHash:** `0xDCE05406` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL(const char* labelName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL, labelName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `labelName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY

**Hash:** `0x17299B63C7683A2B` | **JHash:** `0x150E03B6` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY(Hash gxtEntryHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_TEXT_LABEL_HASH_KEY, gxtEntryHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxtEntryHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** It adds the localized text of the specified GXT entry name. Eg. if the argument is GET_HASH_KEY("ES_HELP"), adds "Continue". Just uses a text labels hash key

---

### ADD_TEXT_COMPONENT_SUBSTRING_BLIP_NAME

**Hash:** `0x80EAD8E2E1D5D52E` | **JHash:** `0x5DE98F0A` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_BLIP_NAME(Blip blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_BLIP_NAME, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME

**Hash:** `0x6C188BE134E074AA` | **JHash:** `0x27A244D8` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME(const char* text)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME, text);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_SUBSTRING_TIME

**Hash:** `0x1115F16B8AB9E8BF` | **JHash:** `0x135B3CD0` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_TIME(int timestamp, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_TIME, timestamp, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timestamp` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Adds a timer (e.g. "00:00:00:000"). The appearance of the timer depends on the flags, which needs more research.

---

### ADD_TEXT_COMPONENT_FORMATTED_INTEGER

**Hash:** `0x0E4C749FF9DE9CC4` | **JHash:** `0x12929BDF` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_FORMATTED_INTEGER(int value, BOOL commaSeparated)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_FORMATTED_INTEGER, value, commaSeparated);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |
| `commaSeparated` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADD_TEXT_COMPONENT_SUBSTRING_PHONE_NUMBER

**Hash:** `0x761B77454205A61D` | **JHash:** `0x65E1D404` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_PHONE_NUMBER(const char* p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_PHONE_NUMBER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 was always -1

---

### ADD_TEXT_COMPONENT_SUBSTRING_WEBSITE

**Hash:** `0x94CF4AC034C9C986` | **JHash:** `0xC736999E` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_WEBSITE(const char* website)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_WEBSITE, website);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `website` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** This native (along with ADD_TEXT_COMPONENT_SUBSTRING_KEYBOARD_DISPLAY and ADD_TEXT_COMPONENT_SUBSTRING_PLAYER_NAME) do not actually filter anything. They simply add the provided text (as of 944)

---

### ADD_TEXT_COMPONENT_SUBSTRING_KEYBOARD_DISPLAY

**Hash:** `0x5F68520888E69014` | **JHash:** `0x0829A799` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TEXT_COMPONENT_SUBSTRING_KEYBOARD_DISPLAY(const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TEXT_COMPONENT_SUBSTRING_KEYBOARD_DISPLAY, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_COLOUR_OF_NEXT_TEXT_COMPONENT

**Hash:** `0x39BBF623FC803EAC` | **JHash:** `0x6F1A1901` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_COLOUR_OF_NEXT_TEXT_COMPONENT(int hudColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_COLOUR_OF_NEXT_TEXT_COMPONENT, hudColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColor` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME

**Hash:** `0x169BD9382084C8C0` | **JHash:** `0x34A396EE` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME(const char* text, int position, int length)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME, text, position, length);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |
| `position` | `int` | `int` | |
| `length` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns a substring of a specified length starting at a specified position.

Example:
// Get "STRING" text from "MY_STRING"
subStr = HUD::GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME("MY_STRING", 3, 6);

---

### GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_WITH_BYTE_LIMIT

**Hash:** `0xB2798643312205C5` | **JHash:** `0x0183A66C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_WITH_BYTE_LIMIT(const char* text, int position, int length, int maxLength)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_WITH_BYTE_LIMIT, text, position, length, maxLength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |
| `position` | `int` | `int` | |
| `length` | `int` | `int` | |
| `maxLength` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns a substring of a specified length starting at a specified position. The result is guaranteed not to exceed the specified max length.

NOTE: The 'maxLength' parameter might actually be the size of the buffer that is returned. More research is needed. -CL69

Example:
// Condensed example of how Rockstar uses this function
strLen = HUD::GET_LENGTH_OF_LITERAL_STRING(MISC::GET_ONSCREEN_KEYBOARD_RESULT());
subStr = HUD::GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_WITH_BYTE_LIMIT(MISC::GET_ONSCREEN_KEYBOARD_RESULT(), 0, strLen, 63);

--

"fm_race_creator.ysc", line 85115:
// parameters modified for clarity
BOOL sub_8e5aa(char *text, int length) {
    for (i = 0; i <= (length - 2); i += 1) {
        if (!MISC::ARE_STRINGS_EQUAL(HUD::GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_WITH_BYTE_LIMIT(text, i, i + 1, 1), " ")) {
            return FALSE;
        }
    }
    return TRUE;
}

---

### GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_BYTES

**Hash:** `0xCE94AEBA5D82908A` | **JHash:** `0xFA6373BB` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_BYTES(const char* text, int startPosition, int endPosition)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_BYTES, text, startPosition, endPosition);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `text` | `const char*` | `string` | |
| `startPosition` | `int` | `int` | |
| `endPosition` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns a substring that is between two specified positions. The length of the string will be calculated using (endPosition - startPosition).

Example:
// Get "STRING" text from "MY_STRING"
subStr = HUD::GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_BYTES("MY_STRING", 3, 9);
// Overflows are possibly replaced with underscores (needs verification)
subStr = HUD::GET_CHARACTER_FROM_AUDIO_CONVERSATION_FILENAME_BYTES("MY_STRING", 3, 10); // "STRING_"?

---

### GET_FILENAME_FOR_AUDIO_CONVERSATION

**Hash:** `0x7B5280EBA9840C72` | **JHash:** `0x95C4B5AD` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_FILENAME_FOR_AUDIO_CONVERSATION(const char* labelName)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_FILENAME_FOR_AUDIO_CONVERSATION, labelName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `labelName` | `const char*` | `string` | |

**返回值:** `const char*` → `string`

**说明:** Gets a localized string literal from a label name. Can be used for output of e.g. VEHICLE::GET_LIVERY_NAME. To check if a GXT label can be localized with this, HUD::DOES_TEXT_LABEL_EXIST can be used.

---

### CLEAR_PRINTS

**Hash:** `0xCC33FA791322B9D9` | **JHash:** `0x216CB1C5` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PRINTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PRINTS);
```

**返回值:** `void` → `void`

---

### CLEAR_BRIEF

**Hash:** `0x9D292F73ADBD9313` | **JHash:** `0x9F75A929` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_BRIEF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_BRIEF);
```

**返回值:** `void` → `void`

---

### CLEAR_ALL_HELP_MESSAGES

**Hash:** `0x6178F68A87A4D3A0` | **JHash:** `0x9E5D9198` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_HELP_MESSAGES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_HELP_MESSAGES);
```

**返回值:** `void` → `void`

---

### CLEAR_THIS_PRINT

**Hash:** `0xCF708001E1E536DD` | **JHash:** `0x06878327` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_THIS_PRINT(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_THIS_PRINT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** p0: found arguments in the b617d scripts: https://pastebin.com/X5akCN7z

---

### CLEAR_SMALL_PRINTS

**Hash:** `0x2CEA2839313C09AC` | **JHash:** `0xA869A238` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_SMALL_PRINTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_SMALL_PRINTS);
```

**返回值:** `void` → `void`

---

### DOES_TEXT_BLOCK_EXIST

**Hash:** `0x1C7302E725259789` | **JHash:** `0x96F74838` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_TEXT_BLOCK_EXIST(const char* gxt)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_TEXT_BLOCK_EXIST, gxt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_ADDITIONAL_TEXT

**Hash:** `0x71A78003C8E71424` | **JHash:** `0x9FA9175B` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ADDITIONAL_TEXT(const char* gxt, int slot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ADDITIONAL_TEXT, gxt, slot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |
| `slot` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Request a gxt into the passed slot.

---

### REQUEST_ADDITIONAL_TEXT_FOR_DLC

**Hash:** `0x6009F9F1AE90D8A6` | **JHash:** `0xF4D27EBE` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ADDITIONAL_TEXT_FOR_DLC(const char* gxt, int slot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ADDITIONAL_TEXT_FOR_DLC, gxt, slot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |
| `slot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### HAS_ADDITIONAL_TEXT_LOADED

**Hash:** `0x02245FE4BED318B8` | **JHash:** `0xB0E56045` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ADDITIONAL_TEXT_LOADED(int slot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ADDITIONAL_TEXT_LOADED, slot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `slot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_ADDITIONAL_TEXT

**Hash:** `0x2A179DF17CCF04CD` | **JHash:** `0x518141E0` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ADDITIONAL_TEXT(int p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ADDITIONAL_TEXT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_STREAMING_ADDITIONAL_TEXT

**Hash:** `0x8B6817B71B85EBF0` | **JHash:** `0xF079E4EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STREAMING_ADDITIONAL_TEXT(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STREAMING_ADDITIONAL_TEXT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_THIS_ADDITIONAL_TEXT_LOADED

**Hash:** `0xADBF060E2B30C5BC` | **JHash:** `0x80A52040` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_THIS_ADDITIONAL_TEXT_LOADED(const char* gxt, int slot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_THIS_ADDITIONAL_TEXT_LOADED, gxt, slot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |
| `slot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the specified gxt has loaded into the passed slot.

---

### IS_MESSAGE_BEING_DISPLAYED

**Hash:** `0x7984C03AA5CC2F41` | **JHash:** `0x6A77FE8D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MESSAGE_BEING_DISPLAYED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MESSAGE_BEING_DISPLAYED);
```

**返回值:** `BOOL` → `bool`

---

### DOES_TEXT_LABEL_EXIST

**Hash:** `0xAC09CA973C564252` | **JHash:** `0x6ECAE560` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_TEXT_LABEL_EXIST(const char* gxt)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_TEXT_LABEL_EXIST, gxt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the passed gxt name exists in the game files.

---

### GET_FIRST_N_CHARACTERS_OF_LITERAL_STRING

**Hash:** `0x98C3CF913D895111` | **Build:** `505`

**C++ 原型:**
```cpp
const char* GET_FIRST_N_CHARACTERS_OF_LITERAL_STRING(const char* string, int length)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_FIRST_N_CHARACTERS_OF_LITERAL_STRING, string, length);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |
| `length` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_LENGTH_OF_STRING_WITH_THIS_TEXT_LABEL

**Hash:** `0x801BD273D3A23F74` | **JHash:** `0xA4CA7BE5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LENGTH_OF_STRING_WITH_THIS_TEXT_LABEL(const char* gxt)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LENGTH_OF_STRING_WITH_THIS_TEXT_LABEL, gxt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxt` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Returns the string length of the string from the gxt string .

---

### GET_LENGTH_OF_LITERAL_STRING

**Hash:** `0xF030907CCBB8A9FD` | **JHash:** `0x99379D55` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LENGTH_OF_LITERAL_STRING(const char* string)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LENGTH_OF_LITERAL_STRING, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** Returns the length of the string passed (much like strlen).

---

### GET_LENGTH_OF_LITERAL_STRING_IN_BYTES

**Hash:** `0x43E4111189E54F0E` | **JHash:** `0x7DBC0764` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LENGTH_OF_LITERAL_STRING_IN_BYTES(const char* string)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LENGTH_OF_LITERAL_STRING_IN_BYTES, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### GET_STREET_NAME_FROM_HASH_KEY

**Hash:** `0xD0EF8A959B8A4CB9` | **JHash:** `0x1E8E310C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_STREET_NAME_FROM_HASH_KEY(Hash hash)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_STREET_NAME_FROM_HASH_KEY, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `const char*` → `string`

**说明:** This functions converts the hash of a street name into a readable string.

For how to get the hashes, see PATHFIND::GET_STREET_NAME_AT_COORD.

---

### IS_HUD_PREFERENCE_SWITCHED_ON

**Hash:** `0x1930DFA731813EC4` | **JHash:** `0xC3BC1B4F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HUD_PREFERENCE_SWITCHED_ON(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HUD_PREFERENCE_SWITCHED_ON);
```

**返回值:** `BOOL` → `bool`

---

### IS_RADAR_PREFERENCE_SWITCHED_ON

**Hash:** `0x9EB6522EA68F22FE` | **JHash:** `0x14AEAA28` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_RADAR_PREFERENCE_SWITCHED_ON(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_RADAR_PREFERENCE_SWITCHED_ON);
```

**返回值:** `BOOL` → `bool`

---

### IS_SUBTITLE_PREFERENCE_SWITCHED_ON

**Hash:** `0xAD6DACA4BA53E0A4` | **JHash:** `0x63BA19F5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SUBTITLE_PREFERENCE_SWITCHED_ON(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SUBTITLE_PREFERENCE_SWITCHED_ON);
```

**返回值:** `BOOL` → `bool`

---

### DISPLAY_HUD

**Hash:** `0xA6294919E56FF02A` | **JHash:** `0xD10E4E31` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_HUD(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_HUD, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If Hud should be displayed

---

### DISPLAY_HUD_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME

**Hash:** `0x7669F9E39DC17063` | **JHash:** `0xC380AC85` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_HUD_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_HUD_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Enables drawing some hud components, such as help labels, this frame, when the player is dead.

---

### DISPLAY_HUD_WHEN_PAUSED_THIS_FRAME

**Hash:** `0x402F9ED62087E898` | **JHash:** `0xC47AB1B0` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_HUD_WHEN_PAUSED_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_HUD_WHEN_PAUSED_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### DISPLAY_RADAR

**Hash:** `0xA0EBB943C300E693` | **JHash:** `0x52816BD4` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_RADAR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_RADAR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If Minimap / Radar should be displayed.

---

### SET_FAKE_SPECTATOR_MODE

**Hash:** `0xCD74233600C4EA6B` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_FAKE_SPECTATOR_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAKE_SPECTATOR_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Setter for GET_FAKE_SPECTATOR_MODE

---

### GET_FAKE_SPECTATOR_MODE

**Hash:** `0xC2D2AD9EAAE265B8` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_FAKE_SPECTATOR_MODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_FAKE_SPECTATOR_MODE);
```

**返回值:** `BOOL` → `bool`

**说明:** Getter for SET_FAKE_SPECTATOR_MODE

---

### IS_HUD_HIDDEN

**Hash:** `0xA86478C6958735C5` | **JHash:** `0x40BADA1D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HUD_HIDDEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HUD_HIDDEN);
```

**返回值:** `BOOL` → `bool`

---

### IS_RADAR_HIDDEN

**Hash:** `0x157F93B036700462` | **JHash:** `0x1AB3B954` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_RADAR_HIDDEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_RADAR_HIDDEN);
```

**返回值:** `BOOL` → `bool`

---

### IS_MINIMAP_RENDERING

**Hash:** `0xAF754F20EB5CD51A` | **JHash:** `0x9CD18314` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MINIMAP_RENDERING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MINIMAP_RENDERING);
```

**返回值:** `BOOL` → `bool`

---

### USE_VEHICLE_TARGETING_RETICULE

**Hash:** `0x0C698D8F099174C7` | **Build:** `1180`

**C++ 原型:**
```cpp
void USE_VEHICLE_TARGETING_RETICULE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_VEHICLE_TARGETING_RETICULE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _USE_VEHICLE_TARGETING_RETICULE_ON_VEHICLES

**Hash:** `0x1BC0EA2912708625` | **Build:** `3095`

**C++ 原型:**
```cpp
void _USE_VEHICLE_TARGETING_RETICULE_ON_VEHICLES(BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._USE_VEHICLE_TARGETING_RETICULE_ON_VEHICLES, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADD_VALID_VEHICLE_HIT_HASH

**Hash:** `0xE4C3B169876D33D7` | **Build:** `1290`

**C++ 原型:**
```cpp
void ADD_VALID_VEHICLE_HIT_HASH(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VALID_VEHICLE_HIT_HASH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CLEAR_VALID_VEHICLE_HIT_HASHES

**Hash:** `0xEB81A3DADD503187` | **Build:** `1290`

**C++ 原型:**
```cpp
void CLEAR_VALID_VEHICLE_HIT_HASHES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VALID_VEHICLE_HIT_HASHES);
```

**返回值:** `void` → `void`

---

### SET_BLIP_ROUTE

**Hash:** `0x4F7D8A9BFB0B43E9` | **JHash:** `0x3E160C90` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_ROUTE(Blip blip, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_ROUTE, blip, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enable / disable showing route for the Blip-object.

---

### CLEAR_ALL_BLIP_ROUTES

**Hash:** `0xD12882D3FF82BF11` | **Build:** `877`

**C++ 原型:**
```cpp
void CLEAR_ALL_BLIP_ROUTES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_BLIP_ROUTES);
```

**返回值:** `void` → `void`

---

### SET_BLIP_ROUTE_COLOUR

**Hash:** `0x837155CD2F63DA09` | **JHash:** `0xDDE7C65C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_ROUTE_COLOUR(Blip blip, int colour)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_ROUTE_COLOUR, blip, colour);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `colour` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_FORCE_SHOW_GPS

**Hash:** `0x2790F4B17D098E26` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_FORCE_SHOW_GPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_SHOW_GPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_USE_SET_DESTINATION_IN_PAUSE_MAP

**Hash:** `0x6CDD58146A436083` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_USE_SET_DESTINATION_IN_PAUSE_MAP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_SET_DESTINATION_IN_PAUSE_MAP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLOCK_WANTED_FLASH

**Hash:** `0xD1942374085C8469` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_BLOCK_WANTED_FLASH(BOOL disabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLOCK_WANTED_FLASH, disabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `disabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADD_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS

**Hash:** `0x60296AF4BA14ABC5` | **JHash:** `0xB58B25BD` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS_LIST

**Hash:** `0x57D760D55F54E071` | **JHash:** `0x9854485F` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS_LIST(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_NEXT_MESSAGE_TO_PREVIOUS_BRIEFS_LIST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_RADAR_ZOOM_PRECISE

**Hash:** `0xBD12C5EEE184C337` | **JHash:** `0xDCA3F423` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_ZOOM_PRECISE(float zoom)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_ZOOM_PRECISE, zoom);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoom` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** zoom ranges from 0 to 90f in R* Scripts

---

### SET_RADAR_ZOOM

**Hash:** `0x096EF57A0C999BBA` | **JHash:** `0x2A50D1A6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_ZOOM(int zoomLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_ZOOM, zoomLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoomLevel` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** zoomLevel ranges from 0 to 1400 in R* Scripts

---

### SET_RADAR_ZOOM_TO_BLIP

**Hash:** `0xF98E4B3E56AFC7B1` | **JHash:** `0x25EC28C0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_ZOOM_TO_BLIP(Blip blip, float zoom)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_ZOOM_TO_BLIP, blip, zoom);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `zoom` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_RADAR_ZOOM_TO_DISTANCE

**Hash:** `0xCB7CC0D58405AD41` | **JHash:** `0x09CF1CE5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_ZOOM_TO_DISTANCE(float zoom)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_ZOOM_TO_DISTANCE, zoom);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoom` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UPDATE_RADAR_ZOOM_TO_BLIP

**Hash:** `0xD2049635DEB9C375` | **JHash:** `0xE8D3A910` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_RADAR_ZOOM_TO_BLIP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_RADAR_ZOOM_TO_BLIP);
```

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### GET_HUD_COLOUR

**Hash:** `0x7C9C91AB74A0360F` | **JHash:** `0x63F66A0B` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_HUD_COLOUR(int hudColorIndex, int* r, int* g, int* b, int* a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_HUD_COLOUR, hudColorIndex, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorIndex` | `int` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |
| `a` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_SCRIPT_VARIABLE_HUD_COLOUR

**Hash:** `0xD68A5FF8A3A89874` | **JHash:** `0x0E41E45C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_VARIABLE_HUD_COLOUR(int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_VARIABLE_HUD_COLOUR, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the color of HUD_COLOUR_SCRIPT_VARIABLE

---

### SET_SECOND_SCRIPT_VARIABLE_HUD_COLOUR

**Hash:** `0x16A304E6CB2BFAB9` | **JHash:** `0x6BE3ACA8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SECOND_SCRIPT_VARIABLE_HUD_COLOUR(int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SECOND_SCRIPT_VARIABLE_HUD_COLOUR, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the color of HUD_COLOUR_SCRIPT_VARIABLE_2

---

### REPLACE_HUD_COLOUR

**Hash:** `0x1CCC708F0F850613` | **JHash:** `0x3B216749` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLACE_HUD_COLOUR(int hudColorIndex, int hudColorIndex2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLACE_HUD_COLOUR, hudColorIndex, hudColorIndex2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorIndex` | `int` | `int` | |
| `hudColorIndex2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** makes hudColorIndex2 color into hudColorIndex color

---

### REPLACE_HUD_COLOUR_WITH_RGBA

**Hash:** `0xF314CF4F0211894E` | **JHash:** `0xF6E7E92B` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLACE_HUD_COLOUR_WITH_RGBA(int hudColorIndex, int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLACE_HUD_COLOUR_WITH_RGBA, hudColorIndex, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorIndex` | `int` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_ABILITY_BAR_VISIBILITY

**Hash:** `0x1DFEDD15019315A9` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_ABILITY_BAR_VISIBILITY(BOOL visible)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ABILITY_BAR_VISIBILITY, visible);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `visible` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_ABILITY_BAR

**Hash:** `0x889329C80FE5963C` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_ALLOW_ABILITY_BAR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_ABILITY_BAR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FLASH_ABILITY_BAR

**Hash:** `0x02CFBA0C9E9275CE` | **JHash:** `0x3648960D` | **Build:** `323`

**C++ 原型:**
```cpp
void FLASH_ABILITY_BAR(int millisecondsToFlash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FLASH_ABILITY_BAR, millisecondsToFlash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `millisecondsToFlash` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_ABILITY_BAR_VALUE

**Hash:** `0x9969599CCFF5D85E` | **JHash:** `0x24E53FD8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ABILITY_BAR_VALUE(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ABILITY_BAR_VALUE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FLASH_WANTED_DISPLAY

**Hash:** `0xA18AFB39081B6A1F` | **JHash:** `0x629F866B` | **Build:** `323`

**C++ 原型:**
```cpp
void FLASH_WANTED_DISPLAY(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FLASH_WANTED_DISPLAY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_OFF_WANTED_STAR_FLASH

**Hash:** `0xBA8D65C1C65702E5` | **JHash:** `0x58612465` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_OFF_WANTED_STAR_FLASH(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_OFF_WANTED_STAR_FLASH, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CUSTOM_MP_HUD_COLOR

**Hash:** `0x2ACCB195F3CCD9DE` | **Build:** `2545`

**C++ 原型:**
```cpp
void SET_CUSTOM_MP_HUD_COLOR(int hudColorId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUSTOM_MP_HUD_COLOR, hudColorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_RENDERED_CHARACTER_HEIGHT

**Hash:** `0xDB88A37483346780` | **JHash:** `0x3330175B` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_RENDERED_CHARACTER_HEIGHT(float size, int font)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_RENDERED_CHARACTER_HEIGHT, size, font);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `size` | `float` | `float` | |
| `font` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** This gets the height of the FONT and not the total text. You need to get the number of lines your text uses, and get the height of a newline (I'm using a smaller value) to get the total text height.

---

### SET_TEXT_SCALE

**Hash:** `0x07C837F9A01C34C9` | **JHash:** `0xB6E15B23` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_SCALE(float scale, float size)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_SCALE, scale, size);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |
| `size` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Size range : 0F to 1.0F
p0 is unknown and doesn't seem to have an effect, yet in the game scripts it changes to 1.0F sometimes.

---

### SET_TEXT_COLOUR

**Hash:** `0xBE6B23FFA53FB442` | **JHash:** `0xE54DD2C8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_COLOUR(int red, int green, int blue, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_COLOUR, red, green, blue, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** colors you input not same as you think?
A: for some reason its R B G A

---

### SET_TEXT_CENTRE

**Hash:** `0xC02F4DBFB51D988B` | **JHash:** `0xE26D39A1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_CENTRE(BOOL align)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_CENTRE, align);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `align` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TEXT_RIGHT_JUSTIFY

**Hash:** `0x6B3C4650BC8BEE47` | **JHash:** `0x45B60520` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_RIGHT_JUSTIFY(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_RIGHT_JUSTIFY, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TEXT_JUSTIFICATION

**Hash:** `0x4E096588B13FFECA` | **JHash:** `0x68CDFA60` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_JUSTIFICATION(int justifyType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_JUSTIFICATION, justifyType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `justifyType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Types -
0: Center-Justify
1: Left-Justify
2: Right-Justify

Right-Justify requires SET_TEXT_WRAP, otherwise it will draw to the far right of the screen

---

### SET_TEXT_LINE_HEIGHT_MULT

**Hash:** `0x9F4624F76E6953D1` | **Build:** `3095`

**C++ 原型:**
```cpp
void SET_TEXT_LINE_HEIGHT_MULT(float lineHeightMult)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_LINE_HEIGHT_MULT, lineHeightMult);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `lineHeightMult` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TEXT_WRAP

**Hash:** `0x63145D9C883A1A70` | **JHash:** `0x6F60AB54` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_WRAP(float start, float end)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_WRAP, start, end);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `start` | `float` | `float` | |
| `end` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** It sets the text in a specified box and wraps the text if it exceeds the boundries. Both values are for X axis. Useful when positioning text set to center or aligned to the right.

start - left boundry on screen position (0.0 - 1.0)
end - right boundry on screen position (0.0 - 1.0)

---

### SET_TEXT_LEADING

**Hash:** `0xA50ABC31E3CDFAFF` | **JHash:** `0x98CE21D4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_LEADING(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_LEADING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_TEXT_PROPORTIONAL

**Hash:** `0x038C1F517D7FDCF8` | **JHash:** `0xF49D8A08` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_PROPORTIONAL(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_PROPORTIONAL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### SET_TEXT_FONT

**Hash:** `0x66E0276CC5F6B9DA` | **JHash:** `0x80BC530D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_FONT(int fontType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_FONT, fontType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fontType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** fonts that mess up your text where made for number values/misc stuff

---

### SET_TEXT_DROP_SHADOW

**Hash:** `0x1CA3E9EAC9D93E5E` | **JHash:** `0xE2A11511` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_DROP_SHADOW(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_DROP_SHADOW);
```

**返回值:** `void` → `void`

---

### SET_TEXT_DROPSHADOW

**Hash:** `0x465C84BC39F1C351` | **JHash:** `0xE6587517` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_DROPSHADOW(int distance, int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_DROPSHADOW, distance, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `int` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** distance - shadow distance in pixels, both horizontal and vertical
r, g, b, a - color

---

### SET_TEXT_OUTLINE

**Hash:** `0x2513DFB0FB8400FE` | **JHash:** `0xC753412F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_OUTLINE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_OUTLINE);
```

**返回值:** `void` → `void`

---

### SET_TEXT_EDGE

**Hash:** `0x441603240D202FA6` | **JHash:** `0x3F1A5DAB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_EDGE(int p0, int r, int g, int b, int a)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_EDGE, p0, r, g, b, a);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `a` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### SET_TEXT_RENDER_ID

**Hash:** `0x5F15302936E07111` | **JHash:** `0xC5C3B7F3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_RENDER_ID(int renderId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_RENDER_ID, renderId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `renderId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_DEFAULT_SCRIPT_RENDERTARGET_RENDER_ID

**Hash:** `0x52F0982D7FD156B6` | **JHash:** `0x8188935F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_DEFAULT_SCRIPT_RENDERTARGET_RENDER_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DEFAULT_SCRIPT_RENDERTARGET_RENDER_ID);
```

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 1.

---

### REGISTER_NAMED_RENDERTARGET

**Hash:** `0x57D9C12635E25CE3` | **JHash:** `0xFAE5D6F0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REGISTER_NAMED_RENDERTARGET(const char* name, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REGISTER_NAMED_RENDERTARGET, name, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_NAMED_RENDERTARGET_REGISTERED

**Hash:** `0x78DCDC15C9F116B4` | **JHash:** `0x284057F5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NAMED_RENDERTARGET_REGISTERED(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NAMED_RENDERTARGET_REGISTERED, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_NAMED_RENDERTARGET

**Hash:** `0xE9F6FFE837354DD4` | **JHash:** `0xD3F6C892` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL RELEASE_NAMED_RENDERTARGET(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.RELEASE_NAMED_RENDERTARGET, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### LINK_NAMED_RENDERTARGET

**Hash:** `0xF6C09E276AEB3F2D` | **JHash:** `0x6844C4B9` | **Build:** `323`

**C++ 原型:**
```cpp
void LINK_NAMED_RENDERTARGET(Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LINK_NAMED_RENDERTARGET, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_NAMED_RENDERTARGET_RENDER_ID

**Hash:** `0x1A6478B61C6BDC3B` | **JHash:** `0xF9D7A401` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NAMED_RENDERTARGET_RENDER_ID(const char* name)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NAMED_RENDERTARGET_RENDER_ID, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### IS_NAMED_RENDERTARGET_LINKED

**Hash:** `0x113750538FA31298` | **JHash:** `0x8B52601F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NAMED_RENDERTARGET_LINKED(Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NAMED_RENDERTARGET_LINKED, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_HELP

**Hash:** `0x8DFCED7A656F8802` | **JHash:** `0xE6D85741` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_HELP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_HELP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_HELP_MESSAGE_ON_SCREEN

**Hash:** `0xDAD37F45428801AE` | **JHash:** `0x4B3C9CA9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HELP_MESSAGE_ON_SCREEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HELP_MESSAGE_ON_SCREEN);
```

**返回值:** `BOOL` → `bool`

---

### HAS_SCRIPT_HIDDEN_HELP_THIS_FRAME

**Hash:** `0x214CD562A939246A` | **JHash:** `0x812CBE0E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCRIPT_HIDDEN_HELP_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCRIPT_HIDDEN_HELP_THIS_FRAME);
```

**返回值:** `BOOL` → `bool`

---

### IS_HELP_MESSAGE_BEING_DISPLAYED

**Hash:** `0x4D79439A6B55AC67` | **JHash:** `0xA65F262A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HELP_MESSAGE_BEING_DISPLAYED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HELP_MESSAGE_BEING_DISPLAYED);
```

**返回值:** `BOOL` → `bool`

---

### IS_HELP_MESSAGE_FADING_OUT

**Hash:** `0x327EDEEEAC55C369` | **JHash:** `0x3E50AE92` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HELP_MESSAGE_FADING_OUT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HELP_MESSAGE_FADING_OUT);
```

**返回值:** `BOOL` → `bool`

---

### SET_HELP_MESSAGE_STYLE

**Hash:** `0xB9C362BABECDDC7A` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_HELP_MESSAGE_STYLE(int style, int hudColor, int alpha, int p3, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELP_MESSAGE_STYLE, style, hudColor, alpha, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `style` | `int` | `int` | |
| `hudColor` | `int` | `int` | |
| `alpha` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_STANDARD_BLIP_ENUM_ID

**Hash:** `0x4A9923385BDB9DAD` | **JHash:** `0x87871CE0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STANDARD_BLIP_ENUM_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STANDARD_BLIP_ENUM_ID);
```

**返回值:** `int` → `int`

---

### GET_WAYPOINT_BLIP_ENUM_ID

**Hash:** `0x186E5D252FA50E7D` | **JHash:** `0xB9827942` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WAYPOINT_BLIP_ENUM_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WAYPOINT_BLIP_ENUM_ID);
```

**返回值:** `int` → `int`

---

### GET_NUMBER_OF_ACTIVE_BLIPS

**Hash:** `0x9A3FF3DE163034E8` | **JHash:** `0x144020FA` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_ACTIVE_BLIPS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_ACTIVE_BLIPS);
```

**返回值:** `int` → `int`

---

### GET_NEXT_BLIP_INFO_ID

**Hash:** `0x14F96AA50D6FBEA7` | **JHash:** `0x9356E92F` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_NEXT_BLIP_INFO_ID(int blipSprite)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEXT_BLIP_INFO_ID, blipSprite);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blipSprite` | `int` | `int` | |

**返回值:** `Blip` → `int`

---

### GET_FIRST_BLIP_INFO_ID

**Hash:** `0x1BEDE233E6CD2A1F` | **JHash:** `0x64C0273D` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_FIRST_BLIP_INFO_ID(int blipSprite)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FIRST_BLIP_INFO_ID, blipSprite);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blipSprite` | `int` | `int` | |

**返回值:** `Blip` → `int`

---

### GET_CLOSEST_BLIP_INFO_ID

**Hash:** `0xD484BF71050CA1EE` | **Build:** `1180`

**C++ 原型:**
```cpp
Blip GET_CLOSEST_BLIP_INFO_ID(int blipSprite)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOSEST_BLIP_INFO_ID, blipSprite);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blipSprite` | `int` | `int` | |

**返回值:** `Blip` → `int`

---

### GET_BLIP_INFO_ID_COORD

**Hash:** `0xFA7C7F0AADF25D09` | **JHash:** `0xB7374A66` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_BLIP_INFO_ID_COORD(Blip blip)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_BLIP_INFO_ID_COORD, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_BLIP_INFO_ID_DISPLAY

**Hash:** `0x1E314167F701DC3B` | **JHash:** `0xD0FC19F4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_INFO_ID_DISPLAY(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_INFO_ID_DISPLAY, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

---

### GET_BLIP_INFO_ID_TYPE

**Hash:** `0xBE9B0959FFD0779B` | **JHash:** `0x501D7B4E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_INFO_ID_TYPE(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_INFO_ID_TYPE, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns a value based on what the blip is attached to
1 - Vehicle
2 - Ped
3 - Object
4 - Coord
5 - unk
6 - Pickup
7 - Radius

---

### GET_BLIP_INFO_ID_ENTITY_INDEX

**Hash:** `0x4BA4E2553AFEDC2C` | **JHash:** `0xA068C40B` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_BLIP_INFO_ID_ENTITY_INDEX(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_INFO_ID_ENTITY_INDEX, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `Entity` → `int`

---

### GET_BLIP_INFO_ID_PICKUP_INDEX

**Hash:** `0x9B6786E4C03DD382` | **JHash:** `0x86913D37` | **Build:** `323`

**C++ 原型:**
```cpp
Pickup GET_BLIP_INFO_ID_PICKUP_INDEX(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_INFO_ID_PICKUP_INDEX, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `Pickup` → `int`

**说明:** This function is hard-coded to always return 0.

---

### GET_BLIP_FROM_ENTITY

**Hash:** `0xBC8DBDCA2436F7E8` | **JHash:** `0x005A2A47` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_BLIP_FROM_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_FROM_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Blip` → `int`

**说明:** Returns the Blip handle of given Entity.

---

### ADD_BLIP_FOR_RADIUS

**Hash:** `0x46818D79B1F7499A` | **JHash:** `0x4626756C` | **Build:** `323`

**C++ 原型:**
```cpp
Blip ADD_BLIP_FOR_RADIUS(float posX, float posY, float posZ, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_BLIP_FOR_RADIUS, posX, posY, posZ, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `Blip` → `int`

---

### ADD_BLIP_FOR_AREA

**Hash:** `0xCE5D0E5E315DB238` | **Build:** `463`

**C++ 原型:**
```cpp
Blip ADD_BLIP_FOR_AREA(float x, float y, float z, float width, float height)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_BLIP_FOR_AREA, x, y, z, width, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `width` | `float` | `float` | |
| `height` | `float` | `float` | |

**返回值:** `Blip` → `int`

**说明:** Adds a rectangular blip for the specified coordinates/area.

It is recommended to use SET_BLIP_ROTATION and SET_BLIP_COLOUR to make the blip not rotate along with the camera.

By default, the blip will show as a _regular_ blip with the specified color/sprite if it is outside of the minimap view.

Example image:
minimap https://w.wew.wtf/pdcjig.png
big map https://w.wew.wtf/zgcjcm.png

(Native name is _likely_ to actually be ADD_BLIP_FOR_AREA, but due to the usual reasons this can't be confirmed)

---

### ADD_BLIP_FOR_ENTITY

**Hash:** `0x5CDE92C702A8FCE7` | **JHash:** `0x30822554` | **Build:** `323`

**C++ 原型:**
```cpp
Blip ADD_BLIP_FOR_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_BLIP_FOR_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Blip` → `int`

**说明:** Returns red ( default ) blip attached to entity.

Example:
Blip blip; //Put this outside your case or option
blip = HUD::ADD_BLIP_FOR_ENTITY(YourPedOrBodyguardName);
HUD::SET_BLIP_AS_FRIENDLY(blip, true);

---

### ADD_BLIP_FOR_PICKUP

**Hash:** `0xBE339365C863BD36` | **JHash:** `0x16693C3A` | **Build:** `323`

**C++ 原型:**
```cpp
Blip ADD_BLIP_FOR_PICKUP(Pickup pickup)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_BLIP_FOR_PICKUP, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `Blip` → `int`

---

### ADD_BLIP_FOR_COORD

**Hash:** `0x5A039BB0BCA604B6` | **JHash:** `0xC6F43D0E` | **Build:** `323`

**C++ 原型:**
```cpp
Blip ADD_BLIP_FOR_COORD(float x, float y, float z)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_BLIP_FOR_COORD, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `Blip` → `int`

**说明:** Creates an orange ( default ) Blip-object. Returns a Blip-object which can then be modified.

---

### TRIGGER_SONAR_BLIP

**Hash:** `0x72DD432F3CDFC0EE` | **JHash:** `0xBF25E7B2` | **Build:** `323`

**C++ 原型:**
```cpp
void TRIGGER_SONAR_BLIP(float posX, float posY, float posZ, float radius, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_SONAR_BLIP, posX, posY, posZ, radius, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ALLOW_SONAR_BLIPS

**Hash:** `0x60734CC207C9833C` | **JHash:** `0xE7E1E32B` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_SONAR_BLIPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_SONAR_BLIPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLIP_COORDS

**Hash:** `0xAE2AF67E9D9AF65D` | **JHash:** `0x680A34D4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_COORDS(Blip blip, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_COORDS, blip, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_BLIP_COORDS

**Hash:** `0x586AFE3FF72D996E` | **JHash:** `0xEF6FF47B` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_BLIP_COORDS(Blip blip)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_BLIP_COORDS, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### SET_BLIP_SPRITE

**Hash:** `0xDF735600A4696DAF` | **JHash:** `0x8DBBB0B9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_SPRITE(Blip blip, int spriteId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SPRITE, blip, spriteId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `spriteId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the displayed sprite for a specific blip..

You may have your own list, but since dev-c didn't show it I was bored and started looking through scripts and functions to get a presumable almost positive list of a majority of blip IDs
https://pastebin.com/Bpj9Sfft

Blips Images + IDs:
https://gtaxscripting.blogspot.com/2016/05/gta-v-blips-id-and-image.html

---

### GET_BLIP_SPRITE

**Hash:** `0x1FC877464A04FC4F` | **JHash:** `0x72FF2E73` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_SPRITE(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_SPRITE, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

**说明:** Blips Images + IDs:
gtaxscripting.blogspot.com/2016/05/gta-v-blips-id-and-image.html

---

### SET_COP_BLIP_SPRITE

**Hash:** `0x9FCB3CBFB3EAD69A` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_COP_BLIP_SPRITE(int p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_COP_BLIP_SPRITE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_COP_BLIP_SPRITE_AS_STANDARD

**Hash:** `0xB7B873520C84C118` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_COP_BLIP_SPRITE_AS_STANDARD(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_COP_BLIP_SPRITE_AS_STANDARD);
```

**返回值:** `void` → `void`

---

### SET_BLIP_NAME_FROM_TEXT_FILE

**Hash:** `0xEAA0FFE120D92784` | **JHash:** `0xAC8A5461` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_NAME_FROM_TEXT_FILE(Blip blip, const char* gxtEntry)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_NAME_FROM_TEXT_FILE, blip, gxtEntry);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `gxtEntry` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Doesn't work if the label text of gxtEntry is >= 80.

---

### SET_BLIP_NAME_TO_PLAYER_NAME

**Hash:** `0x127DE7B20C60A6A3` | **JHash:** `0x03A0B8F9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_NAME_TO_PLAYER_NAME(Blip blip, Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_NAME_TO_PLAYER_NAME, blip, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_BLIP_ALPHA

**Hash:** `0x45FF974EEE1C8734` | **JHash:** `0xA791FCCD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_ALPHA(Blip blip, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_ALPHA, blip, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets alpha-channel for blip color.

Example:

Blip blip = HUD::ADD_BLIP_FOR_ENTITY(entity);
HUD::SET_BLIP_COLOUR(blip , 3);
HUD::SET_BLIP_ALPHA(blip , 64);


---

### GET_BLIP_ALPHA

**Hash:** `0x970F608F0EE6C885` | **JHash:** `0x297AF6C8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_ALPHA(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_ALPHA, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

---

### SET_BLIP_FADE

**Hash:** `0x2AEE8F8390D2298C` | **JHash:** `0xA5999031` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_FADE(Blip blip, int opacity, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_FADE, blip, opacity, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `opacity` | `int` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_BLIP_FADE_DIRECTION

**Hash:** `0x2C173AE2BDB9385E` | **Build:** `463`

**C++ 原型:**
```cpp
int GET_BLIP_FADE_DIRECTION(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_FADE_DIRECTION, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns -1, 0, +1, depending on if the blip is fading out, doing nothing, or fading in respectively.

---

### SET_BLIP_ROTATION

**Hash:** `0xF87683CDF73C3F6E` | **JHash:** `0x6B8F44FE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_ROTATION(Blip blip, int rotation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_ROTATION, blip, rotation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `rotation` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** After some testing, looks like you need to use CEIL() on the rotation (vehicle/ped heading) before using it there.

---

### SET_BLIP_ROTATION_WITH_FLOAT

**Hash:** `0xA8B6AFDAC320AC87` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_BLIP_ROTATION_WITH_FLOAT(Blip blip, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_ROTATION_WITH_FLOAT, blip, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Does not require whole number/integer rotations.

---

### GET_BLIP_ROTATION

**Hash:** `0x003E92BA477F9D7F` | **Build:** `2060`

**C++ 原型:**
```cpp
int GET_BLIP_ROTATION(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_ROTATION, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

---

### SET_BLIP_FLASH_TIMER

**Hash:** `0xD3CD6FD297AE87CC` | **JHash:** `0x8D5DF611` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_FLASH_TIMER(Blip blip, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_FLASH_TIMER, blip, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Adds up after viewing multiple R* scripts. I believe that the duration is in miliseconds.

---

### SET_BLIP_FLASH_INTERVAL

**Hash:** `0xAA51DB313C010A7E` | **JHash:** `0xEAF67377` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_FLASH_INTERVAL(Blip blip, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_FLASH_INTERVAL, blip, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_BLIP_COLOUR

**Hash:** `0x03D7FB09E75D6B7E` | **JHash:** `0xBB3C5A41` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_COLOUR(Blip blip, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_COLOUR, blip, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** https://gtaforums.com/topic/864881-all-blip-color-ids-pictured/

---

### SET_BLIP_SECONDARY_COLOUR

**Hash:** `0x14892474891E09EB` | **JHash:** `0xC6384D32` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_SECONDARY_COLOUR(Blip blip, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SECONDARY_COLOUR, blip, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Can be used to give blips any RGB colour with SET_BLIP_COLOUR(blip, 84).

---

### GET_BLIP_COLOUR

**Hash:** `0xDF729E8D20CF7327` | **JHash:** `0xDD6A1E54` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_COLOUR(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_COLOUR, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

---

### GET_BLIP_HUD_COLOUR

**Hash:** `0x729B5F1EFBC0AAEE` | **JHash:** `0xE88B4BC2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BLIP_HUD_COLOUR(Blip blip)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BLIP_HUD_COLOUR, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `int` → `int`

---

### IS_BLIP_SHORT_RANGE

**Hash:** `0xDA5F8727EB75B926` | **JHash:** `0x1226765A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BLIP_SHORT_RANGE(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BLIP_SHORT_RANGE, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_BLIP_ON_MINIMAP

**Hash:** `0xE41CA53051197A27` | **JHash:** `0x258CBA3A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BLIP_ON_MINIMAP(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BLIP_ON_MINIMAP, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_BLIP_HAVE_GPS_ROUTE

**Hash:** `0xDD2238F57B977751` | **JHash:** `0x3E47F357` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_BLIP_HAVE_GPS_ROUTE(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_BLIP_HAVE_GPS_ROUTE, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_BLIP_HIDDEN_ON_LEGEND

**Hash:** `0x54318C915D27E4CE` | **JHash:** `0x43996428` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_HIDDEN_ON_LEGEND(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_HIDDEN_ON_LEGEND, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLIP_HIGH_DETAIL

**Hash:** `0xE2590BC29220CEBB` | **JHash:** `0xD5842BFF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_HIGH_DETAIL(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_HIGH_DETAIL, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLIP_AS_MISSION_CREATOR_BLIP

**Hash:** `0x24AC0137444F9FD5` | **JHash:** `0x802FB686` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_AS_MISSION_CREATOR_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_AS_MISSION_CREATOR_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_MISSION_CREATOR_BLIP

**Hash:** `0x26F49BF3381D933D` | **JHash:** `0x24ACC4E9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MISSION_CREATOR_BLIP(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MISSION_CREATOR_BLIP, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_NEW_SELECTED_MISSION_CREATOR_BLIP

**Hash:** `0x5C90988E7C8E1AF4` | **JHash:** `0xFFD7476C` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_NEW_SELECTED_MISSION_CREATOR_BLIP(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEW_SELECTED_MISSION_CREATOR_BLIP);
```

**返回值:** `Blip` → `int`

---

### IS_HOVERING_OVER_MISSION_CREATOR_BLIP

**Hash:** `0x4167EFE0527D706E` | **JHash:** `0xC5EB849A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HOVERING_OVER_MISSION_CREATOR_BLIP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HOVERING_OVER_MISSION_CREATOR_BLIP);
```

**返回值:** `BOOL` → `bool`

---

### SHOW_START_MISSION_INSTRUCTIONAL_BUTTON

**Hash:** `0xF1A6C18B35BCADE6` | **JHash:** `0xA2CAAB4F` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_START_MISSION_INSTRUCTIONAL_BUTTON(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_START_MISSION_INSTRUCTIONAL_BUTTON, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHOW_CONTACT_INSTRUCTIONAL_BUTTON

**Hash:** `0xC772A904CDE1186F` | **Build:** `2545`

**C++ 原型:**
```cpp
void SHOW_CONTACT_INSTRUCTIONAL_BUTTON(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_CONTACT_INSTRUCTIONAL_BUTTON, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SHOW_PURCHASE_INSTRUCTIONAL_BUTTON

**Hash:** `0xF6865E26067B708C` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SHOW_PURCHASE_INSTRUCTIONAL_BUTTON(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SHOW_PURCHASE_INSTRUCTIONAL_BUTTON, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RELOAD_MAP_MENU

**Hash:** `0x2916A928514C9827` | **Build:** `573`

**C++ 原型:**
```cpp
void RELOAD_MAP_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELOAD_MAP_MENU);
```

**返回值:** `void` → `void`

---

### SET_BLIP_MARKER_LONG_DISTANCE

**Hash:** `0xB552929B85FC27EC` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_BLIP_MARKER_LONG_DISTANCE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_MARKER_LONG_DISTANCE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_BLIP_FLASHES

**Hash:** `0xB14552383D39CE3E` | **JHash:** `0xC0047F15` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_FLASHES(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_FLASHES, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLIP_FLASHES_ALTERNATE

**Hash:** `0x2E8D9498C56DD0D1` | **JHash:** `0x1A81202B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_FLASHES_ALTERNATE(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_FLASHES_ALTERNATE, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_BLIP_FLASHING

**Hash:** `0xA5E41FD83AD6CEF0` | **JHash:** `0x52E111D7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BLIP_FLASHING(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BLIP_FLASHING, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_BLIP_AS_SHORT_RANGE

**Hash:** `0xBE8BE4FE60E27B72` | **JHash:** `0x5C67725E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_AS_SHORT_RANGE(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_AS_SHORT_RANGE, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether or not the specified blip should only be displayed when nearby, or on the minimap.

---

### SET_BLIP_SCALE

**Hash:** `0xD38744167B2FA257` | **JHash:** `0x1E6EC434` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_SCALE(Blip blip, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SCALE, blip, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_BLIP_SCALE_2D

**Hash:** `0xCD6524439909C979` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_BLIP_SCALE_2D(Blip blip, float xScale, float yScale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SCALE_2D, blip, xScale, yScale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `xScale` | `float` | `float` | |
| `yScale` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** See https://imgur.com/a/lLkEsMN

---

### SET_BLIP_PRIORITY

**Hash:** `0xAE9FC9EF6A9FAC79` | **JHash:** `0xCE87DA6F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_PRIORITY(Blip blip, int priority)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_PRIORITY, blip, priority);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `priority` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** See this topic for more details : gtaforums.com/topic/717612-v-scriptnative-documentation-and-research/page-35?p=1069477935

---

### SET_BLIP_DISPLAY

**Hash:** `0x9029B2F3DA924928` | **JHash:** `0x2B521F91` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_DISPLAY(Blip blip, int displayId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_DISPLAY, blip, displayId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `displayId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Display Id behaviours:
0 = Doesn't show up, ever, anywhere.
1 = Doesn't show up, ever, anywhere.
2 = Shows on both main map and minimap. (Selectable on map)
3 = Shows on main map only. (Selectable on map)
4 = Shows on main map only. (Selectable on map)
5 = Shows on minimap only.
6 = Shows on both main map and minimap. (Selectable on map)
7 = Doesn't show up, ever, anywhere.
8 = Shows on both main map and minimap. (Not selectable on map)
9 = Shows on minimap only.
10 = Shows on both main map and minimap. (Not selectable on map)

Anything higher than 10 seems to be exactly the same as 10.

---

### SET_BLIP_CATEGORY

**Hash:** `0x234CDD44D996FD9A` | **JHash:** `0xEF72F533` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_CATEGORY(Blip blip, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_CATEGORY, blip, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example: https://i.imgur.com/skY6vAJ.png

Index:
1 = No distance shown in legend
2 = Distance shown in legend
7 = "Other Players" category, also shows distance in legend
10 = "Property" category
11 = "Owned Property" category

Any other value behaves like index = 1, index wraps around after 255
Blips with categories 7, 10 or 11 will all show under the specific categories listing in the map legend, regardless of sprite or name.
Legend entries:
7 = Other Players (BLIP_OTHPLYR)
10 = Property (BLIP_PROPCAT)
11 = Owned Property (BLIP_APARTCAT)

Category needs to be `7` in order for blip names to show on the expanded minimap when using DISPLAY_PLAYER_NAME_TAGS_ON_BLIPS.

---

### REMOVE_BLIP

**Hash:** `0x86A652570E5F25DD` | **JHash:** `0xD8C3C1CD` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_BLIP(Blip* blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_BLIP, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip*` | `int*` | |

**返回值:** `void` → `void`

**说明:** In the C++ SDK, this seems not to work-- the blip isn't removed immediately. I use it for saving cars.

E.g.:

Ped pped = PLAYER::PLAYER_PED_ID();
Vehicle v = PED::GET_VEHICLE_PED_IS_USING(pped);
Blip b = HUD::ADD_BLIP_FOR_ENTITY(v);

works fine.
But later attempting to delete it with:

Blip b = HUD::GET_BLIP_FROM_ENTITY(v);
if (HUD::DOES_BLIP_EXIST(b)) HUD::REMOVE_BLIP(&b);

doesn't work. And yes, doesn't work without the DOES_BLIP_EXIST check either. Also, if you attach multiple blips to the same thing (say, a vehicle), and that thing disappears, the blips randomly attach to other things (in my case, a vehicle).

Thus for me, HUD::REMOVE_BLIP(&b) only works if there's one blip, (in my case) the vehicle is marked as no longer needed, you drive away from it and it eventually despawns, AND there is only one blip attached to it. I never intentionally attach multiple blips but if the user saves the car, this adds a blip. Then if they delete it, it is supposed to remove the blip, but it doesn't. Then they can immediately save it again, causing another blip to re-appear.
-------------

Passing the address of the variable instead of the value works for me.
e.g.
int blip = HUD::ADD_BLIP_FOR_ENTITY(ped);
HUD::REMOVE_BLIP(&blip);


Remove blip will currently crash your game, just artificially remove the blip by setting the sprite to a id that is 'invisible'.

---

### SET_BLIP_AS_FRIENDLY

**Hash:** `0x6F6F290102C02AB4` | **JHash:** `0xF290CFD8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_AS_FRIENDLY(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_AS_FRIENDLY, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** false for enemy
true for friendly

---

### PULSE_BLIP

**Hash:** `0x742D6FD43115AF73` | **JHash:** `0x44253855` | **Build:** `323`

**C++ 原型:**
```cpp
void PULSE_BLIP(Blip blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PULSE_BLIP, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `void` → `void`

---

### SHOW_NUMBER_ON_BLIP

**Hash:** `0xA3C0B359DCB848B6` | **JHash:** `0x7BFC66C6` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_NUMBER_ON_BLIP(Blip blip, int number)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_NUMBER_ON_BLIP, blip, number);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `number` | `int` | `int` | |

**返回值:** `void` → `void`

---

### HIDE_NUMBER_ON_BLIP

**Hash:** `0x532CFF637EF80148` | **JHash:** `0x0B6D610D` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_NUMBER_ON_BLIP(Blip blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_NUMBER_ON_BLIP, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `void` → `void`

---

### SHOW_HEIGHT_ON_BLIP

**Hash:** `0x75A16C3DA34F1245` | **JHash:** `0x1D99F676` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_HEIGHT_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_HEIGHT_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHOW_TICK_ON_BLIP

**Hash:** `0x74513EA3E505181E` | **JHash:** `0x3DCF0092` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_TICK_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_TICK_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Adds a green checkmark on top of a blip.

---

### SHOW_GOLD_TICK_ON_BLIP

**Hash:** `0xCAC2031EBF79B1A8` | **Build:** `2699`

**C++ 原型:**
```cpp
void SHOW_GOLD_TICK_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_GOLD_TICK_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Adds a orange checkmark on top of a given blip handle: https://imgur.com/a/aw5OTMF
_SHOW_FRIEND_INDICATOR_ON_BLIP* - _SHOW_HEADING_INDICATOR_ON_BLIP*

---

### SHOW_FOR_SALE_ICON_ON_BLIP

**Hash:** `0x19BD6E3C0E16A8FA` | **Build:** `2802`

**C++ 原型:**
```cpp
void SHOW_FOR_SALE_ICON_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_FOR_SALE_ICON_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHOW_HEADING_INDICATOR_ON_BLIP

**Hash:** `0x5FBCA48327B914DF` | **JHash:** `0xD1C3D71B` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_HEADING_INDICATOR_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_HEADING_INDICATOR_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Adds the GTA: Online player heading indicator to a blip.

---

### SHOW_OUTLINE_INDICATOR_ON_BLIP

**Hash:** `0xB81656BC81FE24D1` | **JHash:** `0x8DE82C15` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_OUTLINE_INDICATOR_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_OUTLINE_INDICATOR_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Highlights a blip by a cyan color circle.

Color can be changed with SET_BLIP_SECONDARY_COLOUR

---

### SHOW_FRIEND_INDICATOR_ON_BLIP

**Hash:** `0x23C3EB807312F01A` | **JHash:** `0x4C8F02B4` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_FRIEND_INDICATOR_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_FRIEND_INDICATOR_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Highlights a blip by a half cyan circle on the right side of the blip. https://i.imgur.com/FrV9M4e.png
.Indicating that that player is a friend (in GTA:O). This color can not be changed.
To toggle the left side (crew member indicator) of the half circle around the blip, use: `SHOW_CREW_INDICATOR_ON_BLIP`

---

### SHOW_CREW_INDICATOR_ON_BLIP

**Hash:** `0xDCFB5D4DB8BF367E` | **JHash:** `0xABBE1E45` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_CREW_INDICATOR_ON_BLIP(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_CREW_INDICATOR_ON_BLIP, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables or disables the blue half circle https://i.imgur.com/iZes9Ec.png around the specified blip on the left side of the blip. This is used to indicate that the player is in your crew in GTA:O. Color is changeable by using `SET_BLIP_SECONDARY_COLOUR`.

---

### SET_BLIP_EXTENDED_HEIGHT_THRESHOLD

**Hash:** `0xC4278F70131BAA6D` | **JHash:** `0x6AA6A1CC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_EXTENDED_HEIGHT_THRESHOLD(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_EXTENDED_HEIGHT_THRESHOLD, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Must be toggled before being queued for animation

---

### SET_BLIP_SHORT_HEIGHT_THRESHOLD

**Hash:** `0x4B5B620C9B59ED34` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_BLIP_SHORT_HEIGHT_THRESHOLD(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SHORT_HEIGHT_THRESHOLD, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_BLIP_USE_HEIGHT_INDICATOR_ON_EDGE

**Hash:** `0x2C9F302398E13141` | **JHash:** `0xCA95C487` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_BLIP_USE_HEIGHT_INDICATOR_ON_EDGE(Blip blip, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_USE_HEIGHT_INDICATOR_ON_EDGE, blip, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_BLIP_AS_MINIMAL_ON_EDGE

**Hash:** `0x2B6D467DAB714E8D` | **JHash:** `0xC575F0BC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_AS_MINIMAL_ON_EDGE(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_AS_MINIMAL_ON_EDGE, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes a blip go small when off the minimap.

---

### SET_RADIUS_BLIP_EDGE

**Hash:** `0x25615540D894B814` | **JHash:** `0x40E25DB8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADIUS_BLIP_EDGE(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADIUS_BLIP_EDGE, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enabling this on a radius blip will make it outline only. See https://cdn.discordapp.com/attachments/553235301632573459/575132227935928330/unknown.png

---

### DOES_BLIP_EXIST

**Hash:** `0xA6DB27D19ECBB7DA` | **JHash:** `0xAE92DD96` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_BLIP_EXIST(Blip blip)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_BLIP_EXIST, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_WAYPOINT_OFF

**Hash:** `0xA7E4E2D361C2627F` | **JHash:** `0xB3496E1B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WAYPOINT_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WAYPOINT_OFF);
```

**返回值:** `void` → `void`

**说明:** This native removes the current waypoint from the map.

Example:
C#:
Function.Call(Hash.SET_WAYPOINT_OFF);

C++:
HUD::SET_WAYPOINT_OFF();

---

### DELETE_WAYPOINTS_FROM_THIS_PLAYER

**Hash:** `0xD8E694757BCEA8E9` | **JHash:** `0x62BABF2C` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_WAYPOINTS_FROM_THIS_PLAYER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_WAYPOINTS_FROM_THIS_PLAYER);
```

**返回值:** `void` → `void`

---

### REFRESH_WAYPOINT

**Hash:** `0x81FA173F170560D1` | **JHash:** `0xB395D753` | **Build:** `323`

**C++ 原型:**
```cpp
void REFRESH_WAYPOINT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REFRESH_WAYPOINT);
```

**返回值:** `void` → `void`

---

### IS_WAYPOINT_ACTIVE

**Hash:** `0x1DD1F58F493F1DA5` | **JHash:** `0x5E4DF47B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WAYPOINT_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WAYPOINT_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_NEW_WAYPOINT

**Hash:** `0xFE43368D2AA4F2FC` | **JHash:** `0x8444E1F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NEW_WAYPOINT(float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NEW_WAYPOINT, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE

**Hash:** `0xF46851AB8B02EF40` | **Build:** `3717`

**C++ 原型:**
```cpp
int GET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE);
```

**返回值:** `int` → `int`

---

### SET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE

**Hash:** `0x3FFC556B62146F75` | **Build:** `3717`

**C++ 原型:**
```cpp
void SET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE(int mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WAYPOINT_CLEAR_ON_ARRIVAL_MODE, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mode` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_BLIP_BRIGHT

**Hash:** `0xB203913733F27884` | **JHash:** `0x72BEE6DF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_BRIGHT(Blip blip, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_BRIGHT, blip, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BLIP_SHOW_CONE

**Hash:** `0x13127EC3665E8EE1` | **JHash:** `0xFF545AD8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLIP_SHOW_CONE(Blip blip, BOOL toggle, int hudColorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLIP_SHOW_CONE, blip, toggle, hudColorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `hudColorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** As of b2189, the third parameter sets the color of the cone (before b2189 it was ignored). Note that it uses HUD colors, not blip colors.

---

### REMOVE_COP_BLIP_FROM_PED

**Hash:** `0xC594B315EDF2D4AF` | **JHash:** `0x41B0D022` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_COP_BLIP_FROM_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_COP_BLIP_FROM_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Interesting fact: A hash collision for this is RESET_JETPACK_MODEL_SETTINGS

---

### SETUP_FAKE_CONE_DATA

**Hash:** `0xF83D0FEBE75E62C9` | **Build:** `1290`

**C++ 原型:**
```cpp
void SETUP_FAKE_CONE_DATA(Blip blip, float p1, float p2, float p3, float p4, float p5, float p6, Any p7, int p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SETUP_FAKE_CONE_DATA, blip, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `Any` | `object` | |
| `p8` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_FAKE_CONE_DATA

**Hash:** `0x35A3CD97B2C0A6D2` | **Build:** `1290`

**C++ 原型:**
```cpp
void REMOVE_FAKE_CONE_DATA(Blip blip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_FAKE_CONE_DATA, blip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_FAKE_CONE_ARRAY

**Hash:** `0x8410C5E0CD847B9D` | **Build:** `1290`

**C++ 原型:**
```cpp
void CLEAR_FAKE_CONE_ARRAY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_FAKE_CONE_ARRAY);
```

**返回值:** `void` → `void`

---

### _SET_BLIP_GPS_ROUTE_DISPLAY_DISTANCE

**Hash:** `0x25D984CFB64ED6DE` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_BLIP_GPS_ROUTE_DISPLAY_DISTANCE(Blip blip, int blipChangeParam46, BOOL blipChangeParam47)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_BLIP_GPS_ROUTE_DISPLAY_DISTANCE, blip, blipChangeParam46, blipChangeParam47);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `blip` | `Blip` | `int` | |
| `blipChangeParam46` | `int` | `int` | |
| `blipChangeParam47` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Applies to new eBlipParams _BLIP_CHANGE_46* and _BLIP_CHANGE_47*

---

### SET_MINIMAP_COMPONENT

**Hash:** `0x75A9A10948D1DEA6` | **JHash:** `0x419DCDC4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_MINIMAP_COMPONENT(int componentId, BOOL toggle, int overrideColor)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_MINIMAP_COMPONENT, componentId, toggle, overrideColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `overrideColor` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This native is used to colorize certain map components like the army base at the top of the map.
p2 appears to be always -1. If p2 is -1 then native wouldn't change the color. See https://gfycat.com/SkinnyPinkChupacabra

---

### SET_MINIMAP_SONAR_SWEEP

**Hash:** `0x6B50FC8749632EC1` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_MINIMAP_SONAR_SWEEP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_SONAR_SWEEP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHOW_ACCOUNT_PICKER

**Hash:** `0x60E892BA4F5BDCA4` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_ACCOUNT_PICKER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_ACCOUNT_PICKER);
```

**返回值:** `void` → `void`

---

### GET_MAIN_PLAYER_BLIP_ID

**Hash:** `0xDCD4EC3F419D02FA` | **JHash:** `0xAB93F020` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_MAIN_PLAYER_BLIP_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAIN_PLAYER_BLIP_ID);
```

**返回值:** `Blip` → `int`

---

### SET_PM_WARNINGSCREEN_ACTIVE

**Hash:** `0x41350B4FC28E3941` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PM_WARNINGSCREEN_ACTIVE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PM_WARNINGSCREEN_ACTIVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HIDE_LOADING_ON_FADE_THIS_FRAME

**Hash:** `0x4B0311D3CDC4648F` | **JHash:** `0x35087963` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_LOADING_ON_FADE_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_LOADING_ON_FADE_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_RADAR_AS_INTERIOR_THIS_FRAME

**Hash:** `0x59E727A1C9D3E31A` | **JHash:** `0x6F2626E1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_AS_INTERIOR_THIS_FRAME(Hash interior, float x, float y, int z, int zoom)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_AS_INTERIOR_THIS_FRAME, interior, x, y, z, zoom);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `int` | `int` | |
| `zoom` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** List of interior hashes: https://pastebin.com/1FUyXNqY
Not for every interior zoom > 0 available.

---

### SET_INSIDE_VERY_SMALL_INTERIOR

**Hash:** `0x504DFE62A1692296` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_INSIDE_VERY_SMALL_INTERIOR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INSIDE_VERY_SMALL_INTERIOR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INSIDE_VERY_LARGE_INTERIOR

**Hash:** `0x7EC8ABA5E74B3D7A` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_INSIDE_VERY_LARGE_INTERIOR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INSIDE_VERY_LARGE_INTERIOR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_RADAR_AS_EXTERIOR_THIS_FRAME

**Hash:** `0xE81B7D2A3DAB2D81` | **JHash:** `0x39ABB10E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RADAR_AS_EXTERIOR_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RADAR_AS_EXTERIOR_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_FAKE_PAUSEMAP_PLAYER_POSITION_THIS_FRAME

**Hash:** `0x77E2DD177910E1CF` | **JHash:** `0x54E75C7D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FAKE_PAUSEMAP_PLAYER_POSITION_THIS_FRAME(float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAKE_PAUSEMAP_PLAYER_POSITION_THIS_FRAME, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the position of the arrow icon representing the player on both the minimap and world map.

Too bad this wouldn't work over the network (obviously not). Could spoof where we would be.

---

### SET_FAKE_GPS_PLAYER_POSITION_THIS_FRAME

**Hash:** `0xA17784FCA9548D15` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_FAKE_GPS_PLAYER_POSITION_THIS_FRAME(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAKE_GPS_PLAYER_POSITION_THIS_FRAME, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_PAUSEMAP_IN_INTERIOR_MODE

**Hash:** `0x9049FE339D5F6F6F` | **JHash:** `0x199DED14` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PAUSEMAP_IN_INTERIOR_MODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PAUSEMAP_IN_INTERIOR_MODE);
```

**返回值:** `BOOL` → `bool`

---

### HIDE_MINIMAP_EXTERIOR_MAP_THIS_FRAME

**Hash:** `0x5FBAE526203990C9` | **JHash:** `0x1A4318F7` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_MINIMAP_EXTERIOR_MAP_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_MINIMAP_EXTERIOR_MAP_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### HIDE_MINIMAP_INTERIOR_MAP_THIS_FRAME

**Hash:** `0x20FE7FDFEEAD38C0` | **JHash:** `0xCE36E3FE` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_MINIMAP_INTERIOR_MAP_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_MINIMAP_INTERIOR_MAP_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_USE_ISLAND_MAP

**Hash:** `0x5E1460624D194A38` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_USE_ISLAND_MAP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_ISLAND_MAP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the Cayo Perico map.

---

### _SET_PAUSE_EXTERIOR_RENDERING_WHILE_IN_INTERIOR

**Hash:** `0x35CCE12EAECB4A51` | **Build:** `2944`

**C++ 原型:**
```cpp
void _SET_PAUSE_EXTERIOR_RENDERING_WHILE_IN_INTERIOR(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_PAUSE_EXTERIOR_RENDERING_WHILE_IN_INTERIOR);
```

**返回值:** `void` → `void`

---

### DONT_TILT_MINIMAP_THIS_FRAME

**Hash:** `0x6D14BFDC33B34F55` | **JHash:** `0x334EFD46` | **Build:** `323`

**C++ 原型:**
```cpp
void DONT_TILT_MINIMAP_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DONT_TILT_MINIMAP_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** When calling this, the current frame will have the players "arrow icon" be focused on the dead center of the radar.

---

### DONT_ZOOM_MINIMAP_WHEN_RUNNING_THIS_FRAME

**Hash:** `0x89DA85D949CE57A0` | **Build:** `2802`

**C++ 原型:**
```cpp
void DONT_ZOOM_MINIMAP_WHEN_RUNNING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DONT_ZOOM_MINIMAP_WHEN_RUNNING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### DONT_ZOOM_MINIMAP_WHEN_SNIPING_THIS_FRAME

**Hash:** `0x55F5A5F07134DE60` | **JHash:** `0x7A569F78` | **Build:** `1180`

**C++ 原型:**
```cpp
void DONT_ZOOM_MINIMAP_WHEN_SNIPING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DONT_ZOOM_MINIMAP_WHEN_SNIPING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_WIDESCREEN_FORMAT

**Hash:** `0xC3B07BA00A83B0F1` | **JHash:** `0xF016E08F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WIDESCREEN_FORMAT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WIDESCREEN_FORMAT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DISPLAY_AREA_NAME

**Hash:** `0x276B6CE369C33678` | **JHash:** `0x489FDD41` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_AREA_NAME(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_AREA_NAME, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISPLAY_CASH

**Hash:** `0x96DEC8D5430208B7` | **JHash:** `0x0049DF83` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_CASH(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_CASH, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** "DISPLAY_CASH(false);" makes the cash amount render on the screen when appropriate
"DISPLAY_CASH(true);" disables cash amount rendering

---

### USE_FAKE_MP_CASH

**Hash:** `0x170F541E1CADD1DE` | **JHash:** `0x6253B9D7` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_FAKE_MP_CASH(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_FAKE_MP_CASH, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Related to displaying cash on the HUD
Always called before HUD::CHANGE_FAKE_MP_CASH in decompiled scripts

---

### CHANGE_FAKE_MP_CASH

**Hash:** `0x0772DF77852C2E30` | **JHash:** `0xE319F90B` | **Build:** `323`

**C++ 原型:**
```cpp
void CHANGE_FAKE_MP_CASH(int cash, int bank)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CHANGE_FAKE_MP_CASH, cash, bank);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cash` | `int` | `int` | |
| `bank` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Displays cash change notifications on HUD.

---

### DISPLAY_AMMO_THIS_FRAME

**Hash:** `0xA5E78BA2B1331C55` | **JHash:** `0x60693CEE` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_AMMO_THIS_FRAME(BOOL display)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_AMMO_THIS_FRAME, display);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `display` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISPLAY_SNIPER_SCOPE_THIS_FRAME

**Hash:** `0x73115226F4814E62` | **JHash:** `0xBC6C73CB` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_SNIPER_SCOPE_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_SNIPER_SCOPE_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Displays the crosshair for this frame.

---

### HIDE_HUD_AND_RADAR_THIS_FRAME

**Hash:** `0x719FF505F097FD20` | **JHash:** `0xB75D4AD2` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_HUD_AND_RADAR_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_HUD_AND_RADAR_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Hides HUD and radar this frame and prohibits switching to other weapons (or accessing the weapon wheel)

---

### ALLOW_DISPLAY_OF_MULTIPLAYER_CASH_TEXT

**Hash:** `0xE67C6DFD386EA5E7` | **JHash:** `0x5476B9FD` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_DISPLAY_OF_MULTIPLAYER_CASH_TEXT(BOOL allow)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_DISPLAY_OF_MULTIPLAYER_CASH_TEXT, allow);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `allow` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Controls whether to display 'Cash'/'Bank' next to the money balance HUD in Multiplayer (https://i.imgur.com/MiYUtNl.png)

---

### SET_MULTIPLAYER_WALLET_CASH

**Hash:** `0xC2D15BEF167E27BC` | **JHash:** `0xF4F3C796` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MULTIPLAYER_WALLET_CASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MULTIPLAYER_WALLET_CASH);
```

**返回值:** `void` → `void`

---

### REMOVE_MULTIPLAYER_WALLET_CASH

**Hash:** `0x95CF81BD06EE1887` | **JHash:** `0x7BFFE82F` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MULTIPLAYER_WALLET_CASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MULTIPLAYER_WALLET_CASH);
```

**返回值:** `void` → `void`

---

### SET_MULTIPLAYER_BANK_CASH

**Hash:** `0xDD21B55DF695CD0A` | **JHash:** `0x2C842D03` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MULTIPLAYER_BANK_CASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MULTIPLAYER_BANK_CASH);
```

**返回值:** `void` → `void`

---

### REMOVE_MULTIPLAYER_BANK_CASH

**Hash:** `0xC7C6789AA1CFEDD0` | **JHash:** `0x728B4EF4` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MULTIPLAYER_BANK_CASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MULTIPLAYER_BANK_CASH);
```

**返回值:** `void` → `void`

---

### SET_MULTIPLAYER_HUD_CASH

**Hash:** `0xFD1D220394BCB824` | **JHash:** `0xA8DB435E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MULTIPLAYER_HUD_CASH(int p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MULTIPLAYER_HUD_CASH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### REMOVE_MULTIPLAYER_HUD_CASH

**Hash:** `0x968F270E39141ECA` | **JHash:** `0x07BF4A7D` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MULTIPLAYER_HUD_CASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MULTIPLAYER_HUD_CASH);
```

**返回值:** `void` → `void`

**说明:** Removes multiplayer cash hud each frame

---

### HIDE_HELP_TEXT_THIS_FRAME

**Hash:** `0xD46923FC481CA285` | **JHash:** `0xF3807BED` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_HELP_TEXT_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_HELP_TEXT_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### IS_IME_IN_PROGRESS

**Hash:** `0x801879A9B4F4B2FB` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL IS_IME_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_IME_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### DISPLAY_HELP_TEXT_THIS_FRAME

**Hash:** `0x960C9FF8F616E41C` | **JHash:** `0x18E3360A` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_HELP_TEXT_THIS_FRAME(const char* message, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_HELP_TEXT_THIS_FRAME, message, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `message` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The messages are localized strings.
Examples:
"No_bus_money"
"Enter_bus"
"Tour_help"
"LETTERS_HELP2"
"Dummy"

**The bool appears to always be false (if it even is a bool, as it's represented by a zero)**
--------
p1 doesn't seem to make a difference, regardless of the state it's in. 


picture of where on the screen this is displayed? 

---

### HUD_FORCE_WEAPON_WHEEL

**Hash:** `0xEB354E5376BC81A7` | **JHash:** `0x1EFFB02A` | **Build:** `323`

**C++ 原型:**
```cpp
void HUD_FORCE_WEAPON_WHEEL(BOOL show)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HUD_FORCE_WEAPON_WHEEL, show);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `show` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Forces the weapon wheel to show/hide.

---

### HUD_FORCE_SPECIAL_VEHICLE_WEAPON_WHEEL

**Hash:** `0x488043841BBE156F` | **Build:** `1011`

**C++ 原型:**
```cpp
void HUD_FORCE_SPECIAL_VEHICLE_WEAPON_WHEEL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HUD_FORCE_SPECIAL_VEHICLE_WEAPON_WHEEL);
```

**返回值:** `void` → `void`

**说明:** Displays "blazer_wheels_up" and "blazer_wheels_down" "weapon" icons when switching between jetski and quadbike modes. Works only on vehicles using "VEHICLE_TYPE_AMPHIBIOUS_QUADBIKE" vehicle type. Needs to be called every time prior to switching modes, otherwise the icon will only appear when switching modes once.

---

### HUD_SUPPRESS_WEAPON_WHEEL_RESULTS_THIS_FRAME

**Hash:** `0x0AFC4AF510774B47` | **JHash:** `0xB26FED2B` | **Build:** `323`

**C++ 原型:**
```cpp
void HUD_SUPPRESS_WEAPON_WHEEL_RESULTS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HUD_SUPPRESS_WEAPON_WHEEL_RESULTS_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Calling this each frame, stops the player from receiving a weapon via the weapon wheel.

---

### HUD_GET_WEAPON_WHEEL_CURRENTLY_HIGHLIGHTED

**Hash:** `0xA48931185F0536FE` | **JHash:** `0x22E9F555` | **Build:** `323`

**C++ 原型:**
```cpp
Hash HUD_GET_WEAPON_WHEEL_CURRENTLY_HIGHLIGHTED(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.HUD_GET_WEAPON_WHEEL_CURRENTLY_HIGHLIGHTED);
```

**返回值:** `Hash` → `uint`

**说明:** Returns the weapon hash to the selected/highlighted weapon in the wheel

---

### HUD_SET_WEAPON_WHEEL_TOP_SLOT

**Hash:** `0x72C1056D678BB7D8` | **JHash:** `0x83B608A0` | **Build:** `323`

**C++ 原型:**
```cpp
void HUD_SET_WEAPON_WHEEL_TOP_SLOT(Hash weaponHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HUD_SET_WEAPON_WHEEL_TOP_SLOT, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Set the active slotIndex in the wheel weapon to the slot associated with the provided Weapon hash

---

### HUD_GET_WEAPON_WHEEL_TOP_SLOT

**Hash:** `0xA13E93403F26C812` | **Build:** `323`

**C++ 原型:**
```cpp
Hash HUD_GET_WEAPON_WHEEL_TOP_SLOT(int weaponTypeIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.HUD_GET_WEAPON_WHEEL_TOP_SLOT, weaponTypeIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponTypeIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the weapon hash active in a specific weapon wheel slotList

---

### HUD_SHOWING_CHARACTER_SWITCH_SELECTION

**Hash:** `0x14C9FDCC41F81F63` | **JHash:** `0xE70D1F43` | **Build:** `323`

**C++ 原型:**
```cpp
void HUD_SHOWING_CHARACTER_SWITCH_SELECTION(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HUD_SHOWING_CHARACTER_SWITCH_SELECTION, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a global that disables many weapon input tasks (shooting, aiming, etc.). Does not work with vehicle weapons, only used in selector.ysc

---

### SET_GPS_FLAGS

**Hash:** `0x5B440763A4C8D15B` | **JHash:** `0x60539BAB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_FLAGS(int p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_FLAGS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only the script that originally called SET_GPS_FLAGS can set them again. Another script cannot set the flags, until the first script that called it has called CLEAR_GPS_FLAGS.

Doesn't seem like the flags are actually read by the game at all.

---

### CLEAR_GPS_FLAGS

**Hash:** `0x21986729D6A3A830` | **JHash:** `0x056AFCE6` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_FLAGS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_FLAGS);
```

**返回值:** `void` → `void`

**说明:** Clears the GPS flags. Only the script that originally called SET_GPS_FLAGS can clear them.

Doesn't seem like the flags are actually read by the game at all.

---

### SET_RACE_TRACK_RENDER

**Hash:** `0x1EAC5F91BCBC5073` | **JHash:** `0xFB9BABF5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RACE_TRACK_RENDER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RACE_TRACK_RENDER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_GPS_RACE_TRACK

**Hash:** `0x7AA5B4CE533C858B` | **JHash:** `0x40C59829` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_RACE_TRACK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_RACE_TRACK);
```

**返回值:** `void` → `void`

**说明:** Does the same as SET_RACE_TRACK_RENDER(false);

---

### START_GPS_CUSTOM_ROUTE

**Hash:** `0xDB34E8D56FC13B08` | **JHash:** `0x7F93799B` | **Build:** `323`

**C++ 原型:**
```cpp
void START_GPS_CUSTOM_ROUTE(int hudColor, BOOL displayOnFoot, BOOL followPlayer)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_GPS_CUSTOM_ROUTE, hudColor, displayOnFoot, followPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColor` | `int` | `int` | |
| `displayOnFoot` | `BOOL` | `bool` | |
| `followPlayer` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Starts a new GPS custom-route, allowing you to plot lines on the map.
Lines are drawn directly between points.
The GPS custom route works like the GPS multi route, except it does not follow roads.
Example result: https://i.imgur.com/BDm5pzt.png
hudColor: The HUD color of the GPS path.
displayOnFoot: Draws the path regardless if the player is in a vehicle or not.
followPlayer: Draw the path partially between the previous and next point based on the players position between them. When false, the GPS appears to not disappear after the last leg is completed.

---

### ADD_POINT_TO_GPS_CUSTOM_ROUTE

**Hash:** `0x311438A071DD9B1A` | **JHash:** `0xEEBDFE55` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_POINT_TO_GPS_CUSTOM_ROUTE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_POINT_TO_GPS_CUSTOM_ROUTE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GPS_CUSTOM_ROUTE_RENDER

**Hash:** `0x900086F371220B6F` | **JHash:** `0xDA0AF00E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_CUSTOM_ROUTE_RENDER(BOOL toggle, int radarThickness, int mapThickness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_CUSTOM_ROUTE_RENDER, toggle, radarThickness, mapThickness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `radarThickness` | `int` | `int` | |
| `mapThickness` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** radarThickness: The width of the GPS route on the radar
mapThickness: The width of the GPS route on the map

---

### CLEAR_GPS_CUSTOM_ROUTE

**Hash:** `0xE6DE0561D9232A64` | **JHash:** `0xCF2E3E24` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_CUSTOM_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_CUSTOM_ROUTE);
```

**返回值:** `void` → `void`

---

### START_GPS_MULTI_ROUTE

**Hash:** `0x3D3D15AF7BCAAF83` | **JHash:** `0xC3DCBEDB` | **Build:** `323`

**C++ 原型:**
```cpp
void START_GPS_MULTI_ROUTE(int hudColor, BOOL routeFromPlayer, BOOL displayOnFoot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_GPS_MULTI_ROUTE, hudColor, routeFromPlayer, displayOnFoot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColor` | `int` | `int` | |
| `routeFromPlayer` | `BOOL` | `bool` | |
| `displayOnFoot` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Starts a new GPS multi-route, allowing you to create custom GPS paths.
GPS functions like the waypoint, except it can contain multiple points it's forced to go through.
Once the player has passed a point, the GPS will no longer force its path through it.

Works independently from the player-placed waypoint and blip routes.
Example result: https://i.imgur.com/ZZHQatX.png
hudColor: The HUD color of the GPS path.
routeFromPlayer: Makes the GPS draw a path from the player to the next point, rather than the original path from the previous point.
displayOnFoot: Draws the GPS path regardless if the player is in a vehicle or not.

---

### ADD_POINT_TO_GPS_MULTI_ROUTE

**Hash:** `0xA905192A6781C41B` | **JHash:** `0xFE485135` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_POINT_TO_GPS_MULTI_ROUTE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_POINT_TO_GPS_MULTI_ROUTE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GPS_MULTI_ROUTE_RENDER

**Hash:** `0x3DDA37128DD1ACA8` | **JHash:** `0xE87CBE4C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_MULTI_ROUTE_RENDER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_MULTI_ROUTE_RENDER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_GPS_MULTI_ROUTE

**Hash:** `0x67EEDEA1B9BAFD94` | **JHash:** `0x0D9969E4` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_MULTI_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_MULTI_ROUTE);
```

**返回值:** `void` → `void`

**说明:** Does the same as SET_GPS_MULTI_ROUTE_RENDER(false);

---

### CLEAR_GPS_PLAYER_WAYPOINT

**Hash:** `0xFF4FB7C8CDFA3DA7` | **JHash:** `0x0B9C7FC2` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_PLAYER_WAYPOINT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_PLAYER_WAYPOINT);
```

**返回值:** `void` → `void`

---

### SET_GPS_FLASHES

**Hash:** `0x320D0E0D936A0E9B` | **JHash:** `0xE991F733` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_FLASHES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_FLASHES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_ICON_COLOUR

**Hash:** `0x7B21E0BB01E8224A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_ICON_COLOUR(int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_ICON_COLOUR, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FLASH_MINIMAP_DISPLAY

**Hash:** `0xF2DD778C22B15BDA` | **JHash:** `0xB8359952` | **Build:** `323`

**C++ 原型:**
```cpp
void FLASH_MINIMAP_DISPLAY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FLASH_MINIMAP_DISPLAY);
```

**返回值:** `void` → `void`

**说明:** adds a short flash to the Radar/Minimap
Usage: UI.FLASH_MINIMAP_DISPLAY

---

### FLASH_MINIMAP_DISPLAY_WITH_COLOR

**Hash:** `0x6B1DE27EE78E6A19` | **JHash:** `0x79A6CAF6` | **Build:** `323`

**C++ 原型:**
```cpp
void FLASH_MINIMAP_DISPLAY_WITH_COLOR(int hudColorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FLASH_MINIMAP_DISPLAY_WITH_COLOR, hudColorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudColorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TOGGLE_STEALTH_RADAR

**Hash:** `0x6AFDFB93754950C7` | **JHash:** `0xC68D47C4` | **Build:** `323`

**C++ 原型:**
```cpp
void TOGGLE_STEALTH_RADAR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_STEALTH_RADAR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MINIMAP_IN_SPECTATOR_MODE

**Hash:** `0x1A5CD7752DD28CD3` | **JHash:** `0xD5BFCADB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_IN_SPECTATOR_MODE(BOOL toggle, Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_IN_SPECTATOR_MODE, toggle, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_MISSION_NAME

**Hash:** `0x5F28ECF5FC84772F` | **JHash:** `0x68DCAE10` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MISSION_NAME(BOOL p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MISSION_NAME, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_MISSION_NAME_FOR_UGC_MISSION

**Hash:** `0xE45087D85F468BC2` | **JHash:** `0x8D9A1734` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MISSION_NAME_FOR_UGC_MISSION(BOOL p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MISSION_NAME_FOR_UGC_MISSION, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_DESCRIPTION_FOR_UGC_MISSION_EIGHT_STRINGS

**Hash:** `0x817B86108EB94E51` | **JHash:** `0xD2161E77` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DESCRIPTION_FOR_UGC_MISSION_EIGHT_STRINGS(BOOL p0, const char* p1, const char* p2, const char* p3, const char* p4, const char* p5, const char* p6, const char* p7, const char* p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DESCRIPTION_FOR_UGC_MISSION_EIGHT_STRINGS, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `const char*` | `string` | |
| `p4` | `const char*` | `string` | |
| `p5` | `const char*` | `string` | |
| `p6` | `const char*` | `string` | |
| `p7` | `const char*` | `string` | |
| `p8` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_MINIMAP_BLOCK_WAYPOINT

**Hash:** `0x58FADDED207897DC` | **JHash:** `0xA41C3B62` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_BLOCK_WAYPOINT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_BLOCK_WAYPOINT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MINIMAP_IN_PROLOGUE

**Hash:** `0x9133955F1A2DA957` | **JHash:** `0x02F5F1D1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_IN_PROLOGUE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_IN_PROLOGUE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the North Yankton map

---

### SET_MINIMAP_BACKGROUND_HIDDEN

**Hash:** `0xB09D42557C45EBA1` | **Build:** `3258`

**C++ 原型:**
```cpp
void SET_MINIMAP_BACKGROUND_HIDDEN(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_BACKGROUND_HIDDEN, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing on PC master builds, just a nullsub.

---

### SET_MINIMAP_HIDE_FOW

**Hash:** `0xF8DEE0A5600CBB93` | **JHash:** `0xD8D77733` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_HIDE_FOW(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_HIDE_FOW, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If true, the entire map will be revealed.

FOW = Fog of War

---

### GET_MINIMAP_FOW_DISCOVERY_RATIO

**Hash:** `0xE0130B41D3CF4574` | **JHash:** `0xA4098ACC` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_MINIMAP_FOW_DISCOVERY_RATIO(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_MINIMAP_FOW_DISCOVERY_RATIO);
```

**返回值:** `float` → `float`

---

### GET_MINIMAP_FOW_COORDINATE_IS_REVEALED

**Hash:** `0x6E31B91145873922` | **JHash:** `0x65B705F6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MINIMAP_FOW_COORDINATE_IS_REVEALED(float x, float y, float z)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MINIMAP_FOW_COORDINATE_IS_REVEALED, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_MINIMAP_FOW_DO_NOT_UPDATE

**Hash:** `0x62E849B7EB28E770` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_FOW_DO_NOT_UPDATE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_FOW_DO_NOT_UPDATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MINIMAP_FOW_REVEAL_COORDINATE

**Hash:** `0x0923DBF87DFF735E` | **JHash:** `0xE010F081` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_FOW_REVEAL_COORDINATE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_FOW_REVEAL_COORDINATE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Up to eight coordinates may be revealed per frame

---

### SET_MINIMAP_GOLF_COURSE

**Hash:** `0x71BDB63DBAF8DA59` | **JHash:** `0x5133A750` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_GOLF_COURSE(int hole)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_GOLF_COURSE, hole);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hole` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Not much is known so far on what it does _exactly_.
All I know for sure is that it draws the specified hole ID on the pause menu map as well as on the mini-map/radar. This native also seems to change some other things related to the pause menu map's behaviour, for example: you can no longer set waypoints, the pause menu map starts up in a 'zoomed in' state. This native does not need to be executed every tick.
You need to center the minimap manually as well as change/lock it's zoom and angle in order for it to appear correctly on the minimap.
You'll also need to use the `GOLF` scaleform in order to get the correct minmap border to show up.
Use `0x35edd5b2e3ff01c0` to reset the map when you no longer want to display any golf holes (you still need to unlock zoom, position and angle of the radar manually after calling this).

---

### SET_MINIMAP_GOLF_COURSE_OFF

**Hash:** `0x35EDD5B2E3FF01C0` | **JHash:** `0x20FD3E87` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIMAP_GOLF_COURSE_OFF(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIMAP_GOLF_COURSE_OFF);
```

**返回值:** `void` → `void`

---

### LOCK_MINIMAP_ANGLE

**Hash:** `0x299FAEBB108AE05B` | **JHash:** `0xDEC733E4` | **Build:** `323`

**C++ 原型:**
```cpp
void LOCK_MINIMAP_ANGLE(int angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOCK_MINIMAP_ANGLE, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `angle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Locks the minimap to the specified angle in integer degrees.

angle: The angle in whole degrees. If less than 0 or greater than 360, unlocks the angle.

---

### UNLOCK_MINIMAP_ANGLE

**Hash:** `0x8183455E16C42E3A` | **JHash:** `0x742043F9` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOCK_MINIMAP_ANGLE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOCK_MINIMAP_ANGLE);
```

**返回值:** `void` → `void`

---

### LOCK_MINIMAP_POSITION

**Hash:** `0x1279E861A329E73F` | **JHash:** `0xB9632A91` | **Build:** `323`

**C++ 原型:**
```cpp
void LOCK_MINIMAP_POSITION(float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOCK_MINIMAP_POSITION, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Locks the minimap to the specified world position.

---

### UNLOCK_MINIMAP_POSITION

**Hash:** `0x3E93E06DB8EF1F30` | **JHash:** `0x5E8E6F54` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOCK_MINIMAP_POSITION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOCK_MINIMAP_POSITION);
```

**返回值:** `void` → `void`

---

### SET_FAKE_MINIMAP_MAX_ALTIMETER_HEIGHT

**Hash:** `0xD201F3FF917A506D` | **JHash:** `0x0308EDF6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FAKE_MINIMAP_MAX_ALTIMETER_HEIGHT(float altitude, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAKE_MINIMAP_MAX_ALTIMETER_HEIGHT, altitude, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `altitude` | `float` | `float` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Argument must be 0.0f or above 38.0f, or it will be ignored.

---

### SET_HEALTH_HUD_DISPLAY_VALUES

**Hash:** `0x3F5CC444DCAAA8F2` | **JHash:** `0x7FB6FB2A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HEALTH_HUD_DISPLAY_VALUES(int health, int capacity, BOOL wasAdded)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HEALTH_HUD_DISPLAY_VALUES, health, capacity, wasAdded);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `health` | `int` | `int` | |
| `capacity` | `int` | `int` | |
| `wasAdded` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MAX_HEALTH_HUD_DISPLAY

**Hash:** `0x975D66A0BC17064C` | **JHash:** `0xF07D8CEF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MAX_HEALTH_HUD_DISPLAY(int maximumValue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MAX_HEALTH_HUD_DISPLAY, maximumValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maximumValue` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_MAX_ARMOUR_HUD_DISPLAY

**Hash:** `0x06A320535F5F0248` | **JHash:** `0x827F14DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MAX_ARMOUR_HUD_DISPLAY(int maximumValue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MAX_ARMOUR_HUD_DISPLAY, maximumValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maximumValue` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_BIGMAP_ACTIVE

**Hash:** `0x231C8F89D0539D8F` | **JHash:** `0x08EB83D2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BIGMAP_ACTIVE(BOOL toggleBigMap, BOOL showFullMap)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BIGMAP_ACTIVE, toggleBigMap, showFullMap);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggleBigMap` | `BOOL` | `bool` | |
| `showFullMap` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the big minimap state like in GTA:Online.

---

### IS_HUD_COMPONENT_ACTIVE

**Hash:** `0xBC4C9EA5391ECC0D` | **JHash:** `0x6214631F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HUD_COMPONENT_ACTIVE(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HUD_COMPONENT_ACTIVE, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of components below

HUD = 0;
HUD_WANTED_STARS = 1;
HUD_WEAPON_ICON = 2;
HUD_CASH = 3;
HUD_MP_CASH = 4;
HUD_MP_MESSAGE = 5;
HUD_VEHICLE_NAME = 6;
HUD_AREA_NAME = 7;
HUD_VEHICLE_CLASS = 8;
HUD_STREET_NAME = 9;
HUD_HELP_TEXT = 10;
HUD_FLOATING_HELP_TEXT_1 = 11;
HUD_FLOATING_HELP_TEXT_2 = 12;
HUD_CASH_CHANGE = 13;
HUD_RETICLE = 14;
HUD_SUBTITLE_TEXT = 15;
HUD_RADIO_STATIONS = 16;
HUD_SAVING_GAME = 17;
HUD_GAME_STREAM = 18;
HUD_WEAPON_WHEEL = 19;
HUD_WEAPON_WHEEL_STATS = 20;
MAX_HUD_COMPONENTS = 21;
MAX_HUD_WEAPONS = 22;
MAX_SCRIPTED_HUD_COMPONENTS = 141;

---

### IS_SCRIPTED_HUD_COMPONENT_ACTIVE

**Hash:** `0xDD100EB17A94FF65` | **JHash:** `0x2B86F382` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_HUD_COMPONENT_ACTIVE(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_HUD_COMPONENT_ACTIVE, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HIDE_SCRIPTED_HUD_COMPONENT_THIS_FRAME

**Hash:** `0xE374C498D8BADC14` | **JHash:** `0x31ABA127` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_SCRIPTED_HUD_COMPONENT_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_SCRIPTED_HUD_COMPONENT_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SHOW_SCRIPTED_HUD_COMPONENT_THIS_FRAME

**Hash:** `0x4F38DCA127DAAEA2` | **Build:** `1734`

**C++ 原型:**
```cpp
void SHOW_SCRIPTED_HUD_COMPONENT_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_SCRIPTED_HUD_COMPONENT_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_SCRIPTED_HUD_COMPONENT_HIDDEN_THIS_FRAME

**Hash:** `0x09C0403ED9A751C2` | **JHash:** `0xE8C8E535` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_HUD_COMPONENT_HIDDEN_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_HUD_COMPONENT_HIDDEN_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HIDE_HUD_COMPONENT_THIS_FRAME

**Hash:** `0x6806C51AD12B83B8` | **JHash:** `0xDB2D0762` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_HUD_COMPONENT_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_HUD_COMPONENT_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This function hides various HUD (Heads-up Display) components.
Listed below are the integers and the corresponding HUD component.
- 1 : WANTED_STARS
- 2 : WEAPON_ICON
- 3 : CASH
- 4 : MP_CASH
- 5 : MP_MESSAGE
- 6 : VEHICLE_NAME
- 7 : AREA_NAME
- 8 : VEHICLE_CLASS
- 9 : STREET_NAME
- 10 : HELP_TEXT
- 11 : FLOATING_HELP_TEXT_1
- 12 : FLOATING_HELP_TEXT_2
- 13 : CASH_CHANGE
- 14 : RETICLE
- 15 : SUBTITLE_TEXT
- 16 : RADIO_STATIONS
- 17 : SAVING_GAME
- 18 : GAME_STREAM
- 19 : WEAPON_WHEEL
- 20 : WEAPON_WHEEL_STATS
- 21 : HUD_COMPONENTS
- 22 : HUD_WEAPONS

These integers also work for the `SHOW_HUD_COMPONENT_THIS_FRAME` native, but instead shows the HUD Component.

---

### IS_HUD_COMPONENT_HIDDEN_THIS_FRAME

**Hash:** `0x8EDC335C943465C8` | **Build:** `3717`

**C++ 原型:**
```cpp
BOOL IS_HUD_COMPONENT_HIDDEN_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HUD_COMPONENT_HIDDEN_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SHOW_HUD_COMPONENT_THIS_FRAME

**Hash:** `0x0B4DF1FA60C0E664` | **JHash:** `0x95E1546E` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOW_HUD_COMPONENT_THIS_FRAME(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOW_HUD_COMPONENT_THIS_FRAME, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This function hides various HUD (Heads-up Display) components.
Listed below are the integers and the corresponding HUD component.
- 1 : WANTED_STARS
- 2 : WEAPON_ICON
- 3 : CASH
- 4 : MP_CASH
- 5 : MP_MESSAGE
- 6 : VEHICLE_NAME
- 7 : AREA_NAME
- 8 : VEHICLE_CLASS
- 9 : STREET_NAME
- 10 : HELP_TEXT
- 11 : FLOATING_HELP_TEXT_1
- 12 : FLOATING_HELP_TEXT_2
- 13 : CASH_CHANGE
- 14 : RETICLE
- 15 : SUBTITLE_TEXT
- 16 : RADIO_STATIONS
- 17 : SAVING_GAME
- 18 : GAME_STREAM
- 19 : WEAPON_WHEEL
- 20 : WEAPON_WHEEL_STATS
- 21 : HUD_COMPONENTS
- 22 : HUD_WEAPONS

These integers also work for the `HIDE_HUD_COMPONENT_THIS_FRAME` native, but instead hides the HUD Component.

---

### HIDE_STREET_AND_CAR_NAMES_THIS_FRAME

**Hash:** `0xA4DEDE28B1814289` | **JHash:** `0x52746FE1` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_STREET_AND_CAR_NAMES_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_STREET_AND_CAR_NAMES_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Hides area and vehicle name HUD components for one frame.

---

### RESET_RETICULE_VALUES

**Hash:** `0x12782CE0A636E9F0` | **JHash:** `0xBE27AA3F` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_RETICULE_VALUES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_RETICULE_VALUES);
```

**返回值:** `void` → `void`

---

### RESET_HUD_COMPONENT_VALUES

**Hash:** `0x450930E616475D0D` | **JHash:** `0xD15B46DA` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_HUD_COMPONENT_VALUES(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_HUD_COMPONENT_VALUES, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_HUD_COMPONENT_POSITION

**Hash:** `0xAABB1F56E2A17CED` | **JHash:** `0x2F3A0D15` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HUD_COMPONENT_POSITION(int id, float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HUD_COMPONENT_POSITION, id, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_HUD_COMPONENT_POSITION

**Hash:** `0x223CA69A8C4417FD` | **JHash:** `0x080DCED6` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_HUD_COMPONENT_POSITION(int id)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_HUD_COMPONENT_POSITION, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### CLEAR_REMINDER_MESSAGE

**Hash:** `0xB57D8DD645CFA2CF` | **JHash:** `0x5BBCC934` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_REMINDER_MESSAGE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_REMINDER_MESSAGE);
```

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### GET_HUD_SCREEN_POSITION_FROM_WORLD_POSITION

**Hash:** `0xF9904D11F1ACBEC3` | **JHash:** `0xFE9A39F8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_HUD_SCREEN_POSITION_FROM_WORLD_POSITION(float worldX, float worldY, float worldZ, float* screenX, float* screenY)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_HUD_SCREEN_POSITION_FROM_WORLD_POSITION, worldX, worldY, worldZ, screenX, screenY);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `worldX` | `float` | `float` | |
| `worldY` | `float` | `float` | |
| `worldZ` | `float` | `float` | |
| `screenX` | `float*` | `float*` | |
| `screenY` | `float*` | `float*` | |

**返回值:** `int` → `int`

**说明:** World to relative screen coords, this world to screen will keep the text on screen. Was named _GET_SCREEN_COORD_FROM_WORLD_COORD, but this conflicts with 0x34E82F05DF2974F5. As that hash actually matches GET_SCREEN_COORD_FROM_WORLD_COORD that one supercedes and this one was renamed to _GET_2D_COORD_FROM_3D_COORD

---

### OPEN_REPORTUGC_MENU

**Hash:** `0x523A590C1A3CC0D3` | **JHash:** `0x10DE5150` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_REPORTUGC_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_REPORTUGC_MENU);
```

**返回值:** `void` → `void`

**说明:** Shows a menu for reporting UGC content.

---

### FORCE_CLOSE_REPORTUGC_MENU

**Hash:** `0xEE4C0E6DBC6F2C6F` | **JHash:** `0x67649EE0` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLOSE_REPORTUGC_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLOSE_REPORTUGC_MENU);
```

**返回值:** `void` → `void`

---

### IS_REPORTUGC_MENU_OPEN

**Hash:** `0x9135584D09A3437E` | **JHash:** `0x9D2C94FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REPORTUGC_MENU_OPEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REPORTUGC_MENU_OPEN);
```

**返回值:** `BOOL` → `bool`

---

### IS_FLOATING_HELP_TEXT_ON_SCREEN

**Hash:** `0x2432784ACA090DA4` | **JHash:** `0x45472FD5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FLOATING_HELP_TEXT_ON_SCREEN(int hudIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FLOATING_HELP_TEXT_ON_SCREEN, hudIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_FLOATING_HELP_TEXT_SCREEN_POSITION

**Hash:** `0x7679CC1BCEBE3D4C` | **JHash:** `0x198F32D7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLOATING_HELP_TEXT_SCREEN_POSITION(int hudIndex, float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLOATING_HELP_TEXT_SCREEN_POSITION, hudIndex, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLOATING_HELP_TEXT_WORLD_POSITION

**Hash:** `0x784BA7E0ECEB4178` | **JHash:** `0x93045157` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLOATING_HELP_TEXT_WORLD_POSITION(int hudIndex, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLOATING_HELP_TEXT_WORLD_POSITION, hudIndex, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLOATING_HELP_TEXT_TO_ENTITY

**Hash:** `0xB094BC1DB4018240` | **JHash:** `0x18B012B7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLOATING_HELP_TEXT_TO_ENTITY(int hudIndex, Entity entity, float offsetX, float offsetY)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLOATING_HELP_TEXT_TO_ENTITY, hudIndex, entity, offsetX, offsetY);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FLOATING_HELP_TEXT_STYLE

**Hash:** `0x788E7FD431BD67F1` | **JHash:** `0x97852A82` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FLOATING_HELP_TEXT_STYLE(int hudIndex, int p1, int p2, int p3, int p4, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLOATING_HELP_TEXT_STYLE, hudIndex, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_FLOATING_HELP

**Hash:** `0x50085246ABD3FEFA` | **JHash:** `0xB181F88F` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_FLOATING_HELP(int hudIndex, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_FLOATING_HELP, hudIndex, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hudIndex` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CREATE_MP_GAMER_TAG_WITH_CREW_COLOR

**Hash:** `0x6DD05E9D83EFA4C9` | **JHash:** `0xC969F2D0` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MP_GAMER_TAG_WITH_CREW_COLOR(Player player, const char* username, BOOL pointedClanTag, BOOL isRockstarClan, const char* clanTag, int clanFlag, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MP_GAMER_TAG_WITH_CREW_COLOR, player, username, pointedClanTag, isRockstarClan, clanTag, clanFlag, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `username` | `const char*` | `string` | |
| `pointedClanTag` | `BOOL` | `bool` | |
| `isRockstarClan` | `BOOL` | `bool` | |
| `clanTag` | `const char*` | `string` | |
| `clanFlag` | `int` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** clanFlag: takes a number 0-5

---

### IS_MP_GAMER_TAG_MOVIE_ACTIVE

**Hash:** `0x6E0EB3EB47C8D7AA` | **JHash:** `0xEFD2564A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MP_GAMER_TAG_MOVIE_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MP_GAMER_TAG_MOVIE_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### CREATE_FAKE_MP_GAMER_TAG

**Hash:** `0xBFEFE3321A3F5015` | **JHash:** `0xF5CD2AA4` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_FAKE_MP_GAMER_TAG(Ped ped, const char* username, BOOL pointedClanTag, BOOL isRockstarClan, const char* clanTag, int clanFlag)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_FAKE_MP_GAMER_TAG, ped, username, pointedClanTag, isRockstarClan, clanTag, clanFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `username` | `const char*` | `string` | |
| `pointedClanTag` | `BOOL` | `bool` | |
| `isRockstarClan` | `BOOL` | `bool` | |
| `clanTag` | `const char*` | `string` | |
| `clanFlag` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** clanFlag: takes a number 0-5

---

### REMOVE_MP_GAMER_TAG

**Hash:** `0x31698AA80E0223F8` | **JHash:** `0x3D081FE4` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MP_GAMER_TAG(int gamerTagId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MP_GAMER_TAG, gamerTagId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_MP_GAMER_TAG_ACTIVE

**Hash:** `0x4E929E7A5796FD26` | **JHash:** `0x60118951` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MP_GAMER_TAG_ACTIVE(int gamerTagId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MP_GAMER_TAG_ACTIVE, gamerTagId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_MP_GAMER_TAG_FREE

**Hash:** `0x595B5178E412E199` | **JHash:** `0x63959059` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MP_GAMER_TAG_FREE(int gamerTagId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MP_GAMER_TAG_FREE, gamerTagId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_MP_GAMER_TAG_VISIBILITY

**Hash:** `0x63BB75ABEDC1F6A0` | **JHash:** `0xD41DF479` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_VISIBILITY(int gamerTagId, int component, BOOL toggle, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_VISIBILITY, gamerTagId, component, toggle, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `component` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** enum eMpGamerTagComponent
{
	MP_TAG_GAMER_NAME,
	MP_TAG_CREW_TAG,
	MP_TAG_HEALTH_ARMOUR,
	MP_TAG_BIG_TEXT,
	MP_TAG_AUDIO_ICON,
	MP_TAG_USING_MENU,
	MP_TAG_PASSIVE_MODE,
	MP_TAG_WANTED_STARS,
	MP_TAG_DRIVER,
	MP_TAG_CO_DRIVER,
	MP_TAG_TAGGED,
	MP_TAG_GAMER_NAME_NEARBY,
	MP_TAG_ARROW,
	MP_TAG_PACKAGES,
	MP_TAG_INV_IF_PED_FOLLOWING,
	MP_TAG_RANK_TEXT,
	MP_TAG_TYPING,
	MP_TAG_BAG_LARGE,
	MP_TAG_ARROW,
	MP_TAG_GANG_CEO,
	MP_TAG_GANG_BIKER,
	MP_TAG_BIKER_ARROW,
	MP_TAG_MC_ROLE_PRESIDENT,
	MP_TAG_MC_ROLE_VICE_PRESIDENT,
	MP_TAG_MC_ROLE_ROAD_CAPTAIN,
	MP_TAG_MC_ROLE_SARGEANT,
	MP_TAG_MC_ROLE_ENFORCER,
	MP_TAG_MC_ROLE_PROSPECT,
	MP_TAG_TRANSMITTER,
	MP_TAG_BOMB
};

---

### SET_ALL_MP_GAMER_TAGS_VISIBILITY

**Hash:** `0xEE76FF7E6A0166B0` | **JHash:** `0x767DED29` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_MP_GAMER_TAGS_VISIBILITY(int gamerTagId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_MP_GAMER_TAGS_VISIBILITY, gamerTagId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MP_GAMER_TAGS_SHOULD_USE_VEHICLE_HEALTH

**Hash:** `0xA67F9C46D612B6F1` | **JHash:** `0xB01A5434` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAGS_SHOULD_USE_VEHICLE_HEALTH(int gamerTagId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAGS_SHOULD_USE_VEHICLE_HEALTH, gamerTagId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Displays a bunch of icons above the players name, and level, and their name twice

---

### SET_MP_GAMER_TAGS_SHOULD_USE_POINTS_HEALTH

**Hash:** `0xD29EC58C2F6B5014` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAGS_SHOULD_USE_POINTS_HEALTH(int gamerTagId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAGS_SHOULD_USE_POINTS_HEALTH, gamerTagId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MP_GAMER_TAGS_POINT_HEALTH

**Hash:** `0x1563FE35E9928E67` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAGS_POINT_HEALTH(int gamerTagId, int value, int maximumValue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAGS_POINT_HEALTH, gamerTagId, value, maximumValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `value` | `int` | `int` | |
| `maximumValue` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_MP_GAMER_TAG_COLOUR

**Hash:** `0x613ED644950626AE` | **JHash:** `0x7E3AA40A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_COLOUR(int gamerTagId, int component, int hudColorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_COLOUR, gamerTagId, component, hudColorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `component` | `int` | `int` | |
| `hudColorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets a gamer tag's component colour

gamerTagId is obtained using for example CREATE_FAKE_MP_GAMER_TAG
Ranges from 0 to 255. 0 is grey health bar, ~50 yellow, 200 purple.

---

### SET_MP_GAMER_TAG_HEALTH_BAR_COLOUR

**Hash:** `0x3158C77A7E888AB4` | **JHash:** `0x5777EC77` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_HEALTH_BAR_COLOUR(int gamerTagId, int hudColorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_HEALTH_BAR_COLOUR, gamerTagId, hudColorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `hudColorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Ranges from 0 to 255. 0 is grey health bar, ~50 yellow, 200 purple.
Should be enabled as flag (2). Has 0 opacity by default.

- This was _SET_MP_GAMER_TAG_HEALTH_BAR_COLOR,
-> Rockstar use the EU spelling of 'color' so I hashed the same name with COLOUR and it came back as the correct hash, so it has been corrected above.

---

### SET_MP_GAMER_TAG_ALPHA

**Hash:** `0xD48FE545CD46F857` | **JHash:** `0xF4418611` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_ALPHA(int gamerTagId, int component, int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_ALPHA, gamerTagId, component, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `component` | `int` | `int` | |
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets flag's sprite transparency. 0-255.

---

### SET_MP_GAMER_TAG_WANTED_LEVEL

**Hash:** `0xCF228E2AA03099C3` | **JHash:** `0x0EBB003F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_WANTED_LEVEL(int gamerTagId, int wantedlvl)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_WANTED_LEVEL, gamerTagId, wantedlvl);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `wantedlvl` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** displays wanted star above head

---

### SET_MP_GAMER_TAG_NUM_PACKAGES

**Hash:** `0x9C16459B2324B2CF` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_NUM_PACKAGES(int gamerTagId, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_NUM_PACKAGES, gamerTagId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_MP_GAMER_TAG_NAME

**Hash:** `0xDEA2B8283BAA3944` | **JHash:** `0x627A559B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_NAME(int gamerTagId, const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_NAME, gamerTagId, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### IS_UPDATING_MP_GAMER_TAG_NAME_AND_CREW_DETAILS

**Hash:** `0xEB709A36958ABE0D` | **JHash:** `0xF11414C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_UPDATING_MP_GAMER_TAG_NAME_AND_CREW_DETAILS(int gamerTagId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_UPDATING_MP_GAMER_TAG_NAME_AND_CREW_DETAILS, gamerTagId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_MP_GAMER_TAG_BIG_TEXT

**Hash:** `0x7B7723747CCB55B6` | **JHash:** `0x939218AB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MP_GAMER_TAG_BIG_TEXT(int gamerTagId, const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MP_GAMER_TAG_BIG_TEXT, gamerTagId, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerTagId` | `int` | `int` | |
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### GET_CURRENT_WEBPAGE_ID

**Hash:** `0x01A358D9128B7A86` | **JHash:** `0xAB5B7C18` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_WEBPAGE_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_WEBPAGE_ID);
```

**返回值:** `int` → `int`

---

### GET_CURRENT_WEBSITE_ID

**Hash:** `0x97D47996FC48CBAD` | **JHash:** `0x42A55B14` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_WEBSITE_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_WEBSITE_ID);
```

**返回值:** `int` → `int`

---

### GET_GLOBAL_ACTIONSCRIPT_FLAG

**Hash:** `0xE3B05614DCE1D014` | **JHash:** `0xD217EE7E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_GLOBAL_ACTIONSCRIPT_FLAG(int flagIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_GLOBAL_ACTIONSCRIPT_FLAG, flagIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flagIndex` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the ActionScript flagValue.
ActionScript flags are global flags that scaleforms use
Flags found during testing
0: Returns 1 if the web_browser keyboard is open, otherwise 0
1: Returns 1 if the player has clicked back twice on the opening page, otherwise 0 (web_browser)
2: Returns how many links the player has clicked in the web_browser scaleform, returns 0 when the browser gets closed
9: Returns the current selection on the mobile phone scaleform

There are 20 flags in total.

---

### RESET_GLOBAL_ACTIONSCRIPT_FLAG

**Hash:** `0xB99C4E4D9499DF29` | **JHash:** `0x4C4C10CF` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_GLOBAL_ACTIONSCRIPT_FLAG(int flagIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_GLOBAL_ACTIONSCRIPT_FLAG, flagIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flagIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_WARNING_MESSAGE_READY_FOR_CONTROL

**Hash:** `0xAF42195A42C63BBA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WARNING_MESSAGE_READY_FOR_CONTROL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WARNING_MESSAGE_READY_FOR_CONTROL);
```

**返回值:** `BOOL` → `bool`

---

### SET_WARNING_MESSAGE

**Hash:** `0x7B1776B3B53F8D74` | **JHash:** `0xBE699BDE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WARNING_MESSAGE(const char* titleMsg, int flags, const char* promptMsg, BOOL p3, int p4, const char* p5, const char* p6, BOOL showBackground, int errorCode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WARNING_MESSAGE, titleMsg, flags, promptMsg, p3, p4, p5, p6, showBackground, errorCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `titleMsg` | `const char*` | `string` | |
| `flags` | `int` | `int` | |
| `promptMsg` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `int` | `int` | |
| `p5` | `const char*` | `string` | |
| `p6` | `const char*` | `string` | |
| `showBackground` | `BOOL` | `bool` | |
| `errorCode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** You can only use text entries. No custom text.

Example: SET_WARNING_MESSAGE("t20", 3, "adder", false, -1, 0, 0, true);
errorCode: shows an error code at the bottom left if nonzero

---

### SET_WARNING_MESSAGE_WITH_HEADER

**Hash:** `0xDC38CC1E35B6A5D7` | **JHash:** `0x2DB9EAB5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WARNING_MESSAGE_WITH_HEADER(const char* entryHeader, const char* entryLine1, int instructionalKey, const char* entryLine2, BOOL p4, Any p5, Any* showBackground, Any* p7, BOOL p8, Any p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WARNING_MESSAGE_WITH_HEADER, entryHeader, entryLine1, instructionalKey, entryLine2, p4, p5, showBackground, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entryHeader` | `const char*` | `string` | |
| `entryLine1` | `const char*` | `string` | |
| `instructionalKey` | `int` | `int` | |
| `entryLine2` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |
| `showBackground` | `Any*` | `object*` | |
| `p7` | `Any*` | `object*` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Shows a warning message on screen with a header.
Note: You can only use text entries. No custom text. You can recreate this easily with scaleforms.
Example: https://i.imgur.com/ITJt8bJ.png

---

### SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS

**Hash:** `0x701919482C74B5AB` | **JHash:** `0x749929D3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS(const char* entryHeader, const char* entryLine1, int instructionalKey, const char* entryLine2, BOOL p4, Any p5, Any additionalIntInfo, const char* additionalTextInfoLine1, const char* additionalTextInfoLine2, BOOL showBackground, int errorCode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS, entryHeader, entryLine1, instructionalKey, entryLine2, p4, p5, additionalIntInfo, additionalTextInfoLine1, additionalTextInfoLine2, showBackground, errorCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entryHeader` | `const char*` | `string` | |
| `entryLine1` | `const char*` | `string` | |
| `instructionalKey` | `int` | `int` | |
| `entryLine2` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |
| `additionalIntInfo` | `Any` | `object` | |
| `additionalTextInfoLine1` | `const char*` | `string` | |
| `additionalTextInfoLine2` | `const char*` | `string` | |
| `showBackground` | `BOOL` | `bool` | |
| `errorCode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** You can use this native for custom input, without having to use any scaleform-related natives.
The native must be called on tick.
The entryHeader must be a valid label.
For Single lines use JL_INVITE_N as entryLine1, JL_INVITE_ND for multiple.
Notes:
- additionalIntInfo: replaces first occurrence of ~1~ in provided label with an integer
- additionalTextInfoLine1: replaces first occurrence of ~a~ in provided label, with your custom text
- additionalTextInfoLine2: replaces second occurrence of ~a~ in provided label, with your custom text
- showBackground: shows black background of the warning screen
- errorCode: shows an error code at the bottom left if nonzero
Example of usage:
SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS("ALERT", "JL_INVITE_ND", 66, "", true, -1, -1, "Testing line 1", "Testing line 2", true, 0);
Screenshot:
https://imgur.com/a/IYA7vJ8

---

### SET_WARNING_MESSAGE_WITH_HEADER_EXTENDED

**Hash:** `0x38B55259C2E078ED` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_WARNING_MESSAGE_WITH_HEADER_EXTENDED(const char* entryHeader, const char* entryLine1, int flags, const char* entryLine2, BOOL p4, Any p5, Any* p6, Any* p7, BOOL showBg, Any p9, Any p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WARNING_MESSAGE_WITH_HEADER_EXTENDED, entryHeader, entryLine1, flags, entryLine2, p4, p5, p6, p7, showBg, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entryHeader` | `const char*` | `string` | |
| `entryLine1` | `const char*` | `string` | |
| `flags` | `int` | `int` | |
| `entryLine2` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any*` | `object*` | |
| `p7` | `Any*` | `object*` | |
| `showBg` | `BOOL` | `bool` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS_EXTENDED

**Hash:** `0x15803FEC3B9A872B` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS_EXTENDED(const char* labelTitle, const char* labelMessage, int p2, int p3, const char* labelMessage2, BOOL p5, int p6, int p7, const char* p8, const char* p9, BOOL background, int errorCode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WARNING_MESSAGE_WITH_HEADER_AND_SUBSTRING_FLAGS_EXTENDED, labelTitle, labelMessage, p2, p3, labelMessage2, p5, p6, p7, p8, p9, background, errorCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `labelTitle` | `const char*` | `string` | |
| `labelMessage` | `const char*` | `string` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `labelMessage2` | `const char*` | `string` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `int` | `int` | |
| `p7` | `int` | `int` | |
| `p8` | `const char*` | `string` | |
| `p9` | `const char*` | `string` | |
| `background` | `BOOL` | `bool` | |
| `errorCode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** labelTitle: Label of the alert's title.
labelMsg: Label of the alert's message.
p2: This is an enum, check the description for a list.
p3: This is an enum, check the description for a list.
labelMsg2: Label of another message line
p5: usually 0
p6: usually -1
p7: usually 0
p8: unknown label
p9: unknown label
background: Set to anything other than 0 or false (even any string) and it will draw a background. Setting it to 0 or false will draw no background.
errorCode: Error code, shown at the bottom left if set to value other than 0.

instructionalKey enum list:
Buttons = {
      Empty = 0,
      Select = 1, -- (RETURN)
      Ok = 2, -- (RETURN)
      Yes = 4, -- (RETURN)
      Back = 8, -- (ESC)
      Cancel = 16, -- (ESC)
      No = 32, -- (ESC)
      RetrySpace = 64, -- (SPACE)
      Restart = 128, -- (SPACE)
      Skip = 256, -- (SPACE)
      Quit = 512, -- (ESC)
      Adjust = 1024, -- (ARROWS)
      SpaceKey = 2048, -- (SPACE)
      Share = 4096, -- (SPACE)
      SignIn = 8192, -- (SPACE)
      Continue = 16384, -- (RETURN)
      AdjustLeftRight = 32768, -- (SCROLL L/R)
      AdjustUpDown = 65536, -- (SCROLL U/D)
      Overwrite = 131072, -- (SPACE)
      SocialClubSignup = 262144, -- (RETURN)
      Confirm = 524288, -- (RETURN)
      Queue = 1048576, -- (RETURN)
      RetryReturn = 2097152, -- (RETURN)
      BackEsc = 4194304, -- (ESC)
      SocialClub = 8388608, -- (RETURN)
      Spectate = 16777216, -- (SPACE)
      OkEsc = 33554432, -- (ESC)
      CancelTransfer = 67108864, -- (ESC)
      LoadingSpinner = 134217728,
      NoReturnToGTA = 268435456, -- (ESC)
      CancelEsc = 536870912, -- (ESC)
}

Alt = {
      Empty = 0,
      No = 1, -- (SPACE)
      Host = 2, -- (ESC)
      SearchForJob = 4, -- (RETURN)
      ReturnKey = 8, -- (TURN)
      Freemode = 16, -- (ESC)
}

Example: https://i.imgur.com/TvmNF4k.png

---

### GET_WARNING_SCREEN_MESSAGE_HASH

**Hash:** `0x81DF9ABA6C83DFF9` | **Build:** `1290`

**C++ 原型:**
```cpp
Hash GET_WARNING_SCREEN_MESSAGE_HASH(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WARNING_SCREEN_MESSAGE_HASH);
```

**返回值:** `Hash` → `uint`

**说明:** Has to do with the confirmation overlay (E.g. confirm exit)

---

### SET_WARNING_MESSAGE_OPTION_ITEMS

**Hash:** `0x0C5A80A9E096D529` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_WARNING_MESSAGE_OPTION_ITEMS(int index, const char* name, int cash, int rp, int lvl, int colour)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_WARNING_MESSAGE_OPTION_ITEMS, index, name, cash, rp, lvl, colour);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `name` | `const char*` | `string` | |
| `cash` | `int` | `int` | |
| `rp` | `int` | `int` | |
| `lvl` | `int` | `int` | |
| `colour` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Some sort of list displayed in a warning message. Yet unknown how to prevent repeating.
Param names copied from the corresponding scaleform function "SET_LIST_ROW".
Example: https://i.imgur.com/arKvOYx.png

---

### SET_WARNING_MESSAGE_OPTION_HIGHLIGHT

**Hash:** `0xDAF87174BE7454FF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_WARNING_MESSAGE_OPTION_HIGHLIGHT(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_WARNING_MESSAGE_OPTION_HIGHLIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_WARNING_MESSAGE_OPTION_ITEMS

**Hash:** `0x6EF54AB721DC6242` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WARNING_MESSAGE_OPTION_ITEMS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WARNING_MESSAGE_OPTION_ITEMS);
```

**返回值:** `void` → `void`

---

### IS_WARNING_MESSAGE_ACTIVE

**Hash:** `0xE18B138FABC53103` | **JHash:** `0x94C834AD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WARNING_MESSAGE_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WARNING_MESSAGE_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### CLEAR_DYNAMIC_PAUSE_MENU_ERROR_MESSAGE

**Hash:** `0x7792424AA0EAC32E` | **JHash:** `0x2F9A309C` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_DYNAMIC_PAUSE_MENU_ERROR_MESSAGE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_DYNAMIC_PAUSE_MENU_ERROR_MESSAGE);
```

**返回值:** `void` → `void`

---

### CUSTOM_MINIMAP_SET_ACTIVE

**Hash:** `0x5354C5BA2EA868A4` | **JHash:** `0xE4FD20D8` | **Build:** `323`

**C++ 原型:**
```cpp
void CUSTOM_MINIMAP_SET_ACTIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CUSTOM_MINIMAP_SET_ACTIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If toggle is true, the map is shown in full screen
If toggle is false, the map is shown in normal mode

---

### CUSTOM_MINIMAP_SET_BLIP_OBJECT

**Hash:** `0x1EAE6DD17B7A5EFA` | **JHash:** `0x13E7A5A9` | **Build:** `323`

**C++ 原型:**
```cpp
void CUSTOM_MINIMAP_SET_BLIP_OBJECT(int spriteId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CUSTOM_MINIMAP_SET_BLIP_OBJECT, spriteId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `spriteId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the sprite of the next BLIP_GALLERY blip, values used in the native scripts: 143 (ObjectiveBlue), 144 (ObjectiveGreen), 145 (ObjectiveRed), 146 (ObjectiveYellow).

---

### CUSTOM_MINIMAP_CREATE_BLIP

**Hash:** `0x551DF99658DB6EE8` | **JHash:** `0x786CA0A2` | **Build:** `323`

**C++ 原型:**
```cpp
int CUSTOM_MINIMAP_CREATE_BLIP(float x, float y, float z)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CUSTOM_MINIMAP_CREATE_BLIP, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Add a BLIP_GALLERY at the specific coordinate. Used in fm_maintain_transition_players to display race track points.

---

### CUSTOM_MINIMAP_CLEAR_BLIPS

**Hash:** `0x2708FC083123F9FF` | **JHash:** `0xCBEC9369` | **Build:** `323`

**C++ 原型:**
```cpp
void CUSTOM_MINIMAP_CLEAR_BLIPS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CUSTOM_MINIMAP_CLEAR_BLIPS);
```

**返回值:** `void` → `void`

---

### FORCE_SONAR_BLIPS_THIS_FRAME

**Hash:** `0x1121BFA1A1A522A8` | **JHash:** `0x3F4AFB13` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FORCE_SONAR_BLIPS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FORCE_SONAR_BLIPS_THIS_FRAME);
```

**返回值:** `BOOL` → `bool`

**说明:** Doesn't actually return anything.

---

### GET_NORTH_BLID_INDEX

**Hash:** `0x3F0CF9CB7E589B88` | **Build:** `463`

**C++ 原型:**
```cpp
Blip GET_NORTH_BLID_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NORTH_BLID_INDEX);
```

**返回值:** `Blip` → `int`

---

### DISPLAY_PLAYER_NAME_TAGS_ON_BLIPS

**Hash:** `0x82CEDC33687E1F50` | **JHash:** `0x2F28F0A6` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_PLAYER_NAME_TAGS_ON_BLIPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_PLAYER_NAME_TAGS_ON_BLIPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles whether or not name labels are shown on the expanded minimap next to player blips, like in GTA:O.
Doesn't need to be called every frame.
Preview: https://i.imgur.com/DfqKWfJ.png

Make sure to call SET_BLIP_CATEGORY with index 7 for this to work on the desired blip.

---

### DRAW_FRONTEND_BACKGROUND_THIS_FRAME

**Hash:** `0x211C4EF450086857` | **JHash:** `0x801D0D86` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_FRONTEND_BACKGROUND_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_FRONTEND_BACKGROUND_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### DRAW_HUD_OVER_FADE_THIS_FRAME

**Hash:** `0xBF4F34A85CA2970C` | **JHash:** `0x317775DF` | **Build:** `323`

**C++ 原型:**
```cpp
void DRAW_HUD_OVER_FADE_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DRAW_HUD_OVER_FADE_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### ACTIVATE_FRONTEND_MENU

**Hash:** `0xEF01D36B9C9D0C7B` | **JHash:** `0x01D83872` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_FRONTEND_MENU(Hash menuhash, BOOL togglePause, int component)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_FRONTEND_MENU, menuhash, togglePause, component);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `menuhash` | `Hash` | `uint` | |
| `togglePause` | `BOOL` | `bool` | |
| `component` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Does stuff like this:
gyazo.com/7fcb78ea3520e3dbc5b2c0c0f3712617

Example:
int GetHash = GET_HASH_KEY("fe_menu_version_corona_lobby");
ACTIVATE_FRONTEND_MENU(GetHash, 0, -1);

BOOL p1 is a toggle to define the game in pause.
int p2 is unknown but -1 always works, not sure why though.

[30/03/2017] ins1de :

the int p2 is actually a component variable. When the pause menu is visible, it opens the tab related to it.

Example : Function.Call(Hash.ACTIVATE_FRONTEND_MENU,-1171018317, 0, 42);
Result : Opens the "Online" tab without pausing the menu, with -1 it opens the map.Below is a list of all known Frontend Menu Hashes.
- FE_MENU_VERSION_SP_PAUSE
- FE_MENU_VERSION_MP_PAUSE
- FE_MENU_VERSION_CREATOR_PAUSE
- FE_MENU_VERSION_CUTSCENE_PAUSE
- FE_MENU_VERSION_SAVEGAME
- FE_MENU_VERSION_PRE_LOBBY
- FE_MENU_VERSION_LOBBY
- FE_MENU_VERSION_MP_CHARACTER_SELECT
- FE_MENU_VERSION_MP_CHARACTER_CREATION
- FE_MENU_VERSION_EMPTY
- FE_MENU_VERSION_EMPTY_NO_BACKGROUND
- FE_MENU_VERSION_TEXT_SELECTION
- FE_MENU_VERSION_CORONA
- FE_MENU_VERSION_CORONA_LOBBY
- FE_MENU_VERSION_CORONA_JOINED_PLAYERS
- FE_MENU_VERSION_CORONA_INVITE_PLAYERS
- FE_MENU_VERSION_CORONA_INVITE_FRIENDS
- FE_MENU_VERSION_CORONA_INVITE_CREWS
- FE_MENU_VERSION_CORONA_INVITE_MATCHED_PLAYERS
- FE_MENU_VERSION_CORONA_INVITE_LAST_JOB_PLAYERS
- FE_MENU_VERSION_CORONA_RACE
- FE_MENU_VERSION_CORONA_BETTING
- FE_MENU_VERSION_JOINING_SCREEN
- FE_MENU_VERSION_LANDING_MENU
- FE_MENU_VERSION_LANDING_KEYMAPPING_MENU

---

### RESTART_FRONTEND_MENU

**Hash:** `0x10706DC6AD2D49C0` | **JHash:** `0xB07DAF98` | **Build:** `323`

**C++ 原型:**
```cpp
void RESTART_FRONTEND_MENU(Hash menuHash, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESTART_FRONTEND_MENU, menuHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `menuHash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Before using this native click the native above and look at the decription.

Example:
int GetHash = Function.Call<int>(Hash.GET_HASH_KEY, "fe_menu_version_corona_lobby");
Function.Call(Hash.ACTIVATE_FRONTEND_MENU, GetHash, 0, -1);
Function.Call(Hash.RESTART_FRONTEND_MENU(GetHash, -1);

This native refreshes the frontend menu.

p1 = Hash of Menu
p2 = Unknown but always works with -1.

---

### GET_CURRENT_FRONTEND_MENU_VERSION

**Hash:** `0x2309595AD6145265` | **JHash:** `0x33D6868F` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_CURRENT_FRONTEND_MENU_VERSION(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_CURRENT_FRONTEND_MENU_VERSION);
```

**返回值:** `Hash` → `uint`

**说明:** if (HUD::GET_CURRENT_FRONTEND_MENU_VERSION() == joaat("fe_menu_version_empty_no_background"))

---

### SET_PAUSE_MENU_ACTIVE

**Hash:** `0xDF47FC56C71569CF` | **JHash:** `0x1DCD878E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PAUSE_MENU_ACTIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PAUSE_MENU_ACTIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_FRONTEND_THIS_FRAME

**Hash:** `0x6D3465A73092F0E6` | **JHash:** `0xD86A029E` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_FRONTEND_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_FRONTEND_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SUPPRESS_FRONTEND_RENDERING_THIS_FRAME

**Hash:** `0xBA751764F0821256` | **JHash:** `0x7F349900` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_FRONTEND_RENDERING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_FRONTEND_RENDERING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### ALLOW_PAUSE_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME

**Hash:** `0xCC3FDDED67BCFC63` | **JHash:** `0x630CD8EE` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_PAUSE_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PAUSE_WHEN_NOT_IN_STATE_OF_PLAY_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Allows opening the pause menu this frame, when the player is dead.

---

### SET_FRONTEND_ACTIVE

**Hash:** `0x745711A75AB09277` | **JHash:** `0x81E1AD32` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FRONTEND_ACTIVE(BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FRONTEND_ACTIVE, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PAUSE_MENU_ACTIVE

**Hash:** `0xB0034A223497FFCB` | **JHash:** `0xD3600591` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PAUSE_MENU_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PAUSE_MENU_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### IS_STORE_PENDING_NETWORK_SHUTDOWN_TO_OPEN

**Hash:** `0x2F057596F2BD0061` | **JHash:** `0xC85C4487` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STORE_PENDING_NETWORK_SHUTDOWN_TO_OPEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STORE_PENDING_NETWORK_SHUTDOWN_TO_OPEN);
```

**返回值:** `BOOL` → `bool`

---

### GET_PAUSE_MENU_STATE

**Hash:** `0x272ACD84970869C5` | **JHash:** `0x92F50134` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PAUSE_MENU_STATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PAUSE_MENU_STATE);
```

**返回值:** `int` → `int`

**说明:** Returns:

0
5
10
15
20
25
30
35


---

### GET_PAUSE_MENU_POSITION

**Hash:** `0x5BFF36D6ED83E0AE` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PAUSE_MENU_POSITION(void)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PAUSE_MENU_POSITION);
```

**返回值:** `Vector3` → `Vector3`

---

### IS_PAUSE_MENU_RESTARTING

**Hash:** `0x1C491717107431C7` | **JHash:** `0x3C4CF4D9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PAUSE_MENU_RESTARTING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PAUSE_MENU_RESTARTING);
```

**返回值:** `BOOL` → `bool`

---

### FORCE_SCRIPTED_GFX_WHEN_FRONTEND_ACTIVE

**Hash:** `0x2162C446DFDF38FD` | **JHash:** `0x2DFD35C7` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_SCRIPTED_GFX_WHEN_FRONTEND_ACTIVE(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_SCRIPTED_GFX_WHEN_FRONTEND_ACTIVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Not present in retail version of the game, actual definiton seems to be
_LOG_DEBUG_INFO(const char* category, const char* debugText);

---

### PAUSE_MENUCEPTION_GO_DEEPER

**Hash:** `0x77F16B447824DA6C` | **JHash:** `0x0A89336C` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENUCEPTION_GO_DEEPER(int page)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENUCEPTION_GO_DEEPER, page);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `page` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PAUSE_MENUCEPTION_THE_KICK

**Hash:** `0xCDCA26E80FAECB8F` | **JHash:** `0xC84BE309` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENUCEPTION_THE_KICK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENUCEPTION_THE_KICK);
```

**返回值:** `void` → `void`

---

### PAUSE_TOGGLE_FULLSCREEN_MAP

**Hash:** `0x2DE6C5E2E996F178` | **Build:** `372`

**C++ 原型:**
```cpp
void PAUSE_TOGGLE_FULLSCREEN_MAP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_TOGGLE_FULLSCREEN_MAP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PAUSE_MENU_ACTIVATE_CONTEXT

**Hash:** `0xDD564BDD0472C936` | **JHash:** `0x9FE8FD5E` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENU_ACTIVATE_CONTEXT(Hash contextHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENU_ACTIVATE_CONTEXT, contextHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Activates the specified frontend menu context.
pausemenu.xml defines some specific menu options using 'context'. Context is basically a 'condition'. 
The `*ALL*` part of the context means that whatever is being defined, will be active when any or all of those conditions after `*ALL*` are met.
The `*NONE*` part of the context section means that whatever is being defined, will NOT be active if any or all of the conditions after `*NONE*` are met.
This basically allows you to hide certain menu sections, or things like instructional buttons.

---

### PAUSE_MENU_DEACTIVATE_CONTEXT

**Hash:** `0x444D8CF241EC25C5` | **JHash:** `0x0029046E` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENU_DEACTIVATE_CONTEXT(Hash contextHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENU_DEACTIVATE_CONTEXT, contextHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### PAUSE_MENU_IS_CONTEXT_ACTIVE

**Hash:** `0x84698AB38D0C6636` | **JHash:** `0xC51BC42F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PAUSE_MENU_IS_CONTEXT_ACTIVE(Hash contextHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PAUSE_MENU_IS_CONTEXT_ACTIVE, contextHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### PAUSE_MENU_IS_CONTEXT_MENU_ACTIVE

**Hash:** `0x2A25ADC48F87841F` | **JHash:** `0x016D7AF9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PAUSE_MENU_IS_CONTEXT_MENU_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PAUSE_MENU_IS_CONTEXT_MENU_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### PAUSE_MENU_GET_HAIR_COLOUR_INDEX

**Hash:** `0xDE03620F8703A9DF` | **Build:** `323`

**C++ 原型:**
```cpp
int PAUSE_MENU_GET_HAIR_COLOUR_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PAUSE_MENU_GET_HAIR_COLOUR_INDEX);
```

**返回值:** `int` → `int`

---

### PAUSE_MENU_GET_MOUSE_HOVER_INDEX

**Hash:** `0x359AF31A4B52F5ED` | **Build:** `323`

**C++ 原型:**
```cpp
int PAUSE_MENU_GET_MOUSE_HOVER_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PAUSE_MENU_GET_MOUSE_HOVER_INDEX);
```

**返回值:** `int` → `int`

---

### PAUSE_MENU_GET_MOUSE_HOVER_UNIQUE_ID

**Hash:** `0x13C4B962653A5280` | **Build:** `323`

**C++ 原型:**
```cpp
int PAUSE_MENU_GET_MOUSE_HOVER_UNIQUE_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PAUSE_MENU_GET_MOUSE_HOVER_UNIQUE_ID);
```

**返回值:** `int` → `int`

---

### PAUSE_MENU_GET_MOUSE_CLICK_EVENT

**Hash:** `0xC8E1071177A23BE5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PAUSE_MENU_GET_MOUSE_CLICK_EVENT(Any* p0, Any* p1, Any* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PAUSE_MENU_GET_MOUSE_CLICK_EVENT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### PAUSE_MENU_REDRAW_INSTRUCTIONAL_BUTTONS

**Hash:** `0x4895BDEA16E7C080` | **JHash:** `0x15B24768` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENU_REDRAW_INSTRUCTIONAL_BUTTONS(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENU_REDRAW_INSTRUCTIONAL_BUTTONS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PAUSE_MENU_SET_BUSY_SPINNER

**Hash:** `0xC78E239AC5B2DDB9` | **JHash:** `0x6C67131A` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENU_SET_BUSY_SPINNER(BOOL p0, int position, int spinnerIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENU_SET_BUSY_SPINNER, p0, position, spinnerIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `position` | `int` | `int` | |
| `spinnerIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PAUSE_MENU_SET_WARN_ON_TAB_CHANGE

**Hash:** `0xF06EBB91A81E09E3` | **JHash:** `0x11D09737` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_MENU_SET_WARN_ON_TAB_CHANGE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_MENU_SET_WARN_ON_TAB_CHANGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_FRONTEND_READY_FOR_CONTROL

**Hash:** `0x3BAB9A4E4F2FF5C7` | **JHash:** `0xD3BF3ABD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FRONTEND_READY_FOR_CONTROL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FRONTEND_READY_FOR_CONTROL);
```

**返回值:** `BOOL` → `bool`

---

### TAKE_CONTROL_OF_FRONTEND

**Hash:** `0xEC9264727EEC0F28` | **JHash:** `0xC06B763D` | **Build:** `323`

**C++ 原型:**
```cpp
void TAKE_CONTROL_OF_FRONTEND(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TAKE_CONTROL_OF_FRONTEND);
```

**返回值:** `void` → `void`

**说明:** Disables frontend (works in custom frontends, not sure about regular pause menu) navigation keys on keyboard. Not sure about controller. Does not disable mouse controls. No need to call this every tick.

To enable the keys again, use `0x14621BB1DF14E2B2`.

---

### RELEASE_CONTROL_OF_FRONTEND

**Hash:** `0x14621BB1DF14E2B2` | **JHash:** `0xB9392CE7` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_CONTROL_OF_FRONTEND(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_CONTROL_OF_FRONTEND);
```

**返回值:** `void` → `void`

**说明:** Enables frontend (works in custom frontends, not sure about regular pause menu) navigation keys on keyboard if they were disabled using the native below.
To disable the keys, use `0xEC9264727EEC0F28`

---

### CODE_WANTS_SCRIPT_TO_TAKE_CONTROL

**Hash:** `0x66E7CB63C97B7D20` | **JHash:** `0x92DAFA78` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CODE_WANTS_SCRIPT_TO_TAKE_CONTROL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CODE_WANTS_SCRIPT_TO_TAKE_CONTROL);
```

**返回值:** `BOOL` → `bool`

---

### GET_SCREEN_CODE_WANTS_SCRIPT_TO_CONTROL

**Hash:** `0x593FEAE1F73392D4` | **JHash:** `0x22CA9F2A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SCREEN_CODE_WANTS_SCRIPT_TO_CONTROL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SCREEN_CODE_WANTS_SCRIPT_TO_CONTROL);
```

**返回值:** `int` → `int`

---

### IS_NAVIGATING_MENU_CONTENT

**Hash:** `0x4E3CD0EF8A489541` | **JHash:** `0xDA7951A2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NAVIGATING_MENU_CONTENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NAVIGATING_MENU_CONTENT);
```

**返回值:** `BOOL` → `bool`

---

### HAS_MENU_TRIGGER_EVENT_OCCURRED

**Hash:** `0xF284AC67940C6812` | **JHash:** `0x7D95AFFF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_MENU_TRIGGER_EVENT_OCCURRED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_MENU_TRIGGER_EVENT_OCCURRED);
```

**返回值:** `BOOL` → `bool`

---

### HAS_MENU_LAYOUT_CHANGED_EVENT_OCCURRED

**Hash:** `0x2E22FEFA0100275E` | **JHash:** `0x96863460` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_MENU_LAYOUT_CHANGED_EVENT_OCCURRED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_MENU_LAYOUT_CHANGED_EVENT_OCCURRED);
```

**返回值:** `BOOL` → `bool`

---

### SET_SAVEGAME_LIST_UNIQUE_ID

**Hash:** `0x0CF54F20DE43879C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SAVEGAME_LIST_UNIQUE_ID(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SAVEGAME_LIST_UNIQUE_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_MENU_TRIGGER_EVENT_DETAILS

**Hash:** `0x36C1451A88A09630` | **JHash:** `0x8543AAC8` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MENU_TRIGGER_EVENT_DETAILS(int* lastItemMenuId, int* selectedItemUniqueId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MENU_TRIGGER_EVENT_DETAILS, lastItemMenuId, selectedItemUniqueId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `lastItemMenuId` | `int*` | `int*` | |
| `selectedItemUniqueId` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_MENU_LAYOUT_CHANGED_EVENT_DETAILS

**Hash:** `0x7E17BE53E1AAABAF` | **JHash:** `0x6025AA2F` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MENU_LAYOUT_CHANGED_EVENT_DETAILS(int* lastItemMenuId, int* selectedItemMenuId, int* selectedItemUniqueId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MENU_LAYOUT_CHANGED_EVENT_DETAILS, lastItemMenuId, selectedItemMenuId, selectedItemUniqueId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `lastItemMenuId` | `int*` | `int*` | |
| `selectedItemMenuId` | `int*` | `int*` | |
| `selectedItemUniqueId` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** lastItemMenuId: this is the menuID of the last selected item minus 1000 (lastItem.menuID - 1000)
selectedItemMenuId: same as lastItemMenuId except for the currently selected menu item
selectedItemUniqueId: this is uniqueID of the currently selected menu item

when the pausemenu is closed:
lastItemMenuId = -1
selectedItemMenuId = -1
selectedItemUniqueId = 0

when the header gains focus:
lastItemMenuId updates as normal or 0 if the pausemenu was just opened
selectedItemMenuId becomes a unique id for the pausemenu page that focus was taken from (?) or 0 if the pausemenu was just opened
selectedItemUniqueId = -1

when focus is moved from the header to a pausemenu page:
lastItemMenuId becomes a unique id for the pausemenu page that focus was moved to (?)
selectedItemMenuId = -1
selectedItemUniqueId updates as normal

---

### GET_PM_PLAYER_CREW_COLOR

**Hash:** `0xA238192F33110615` | **JHash:** `0x46794EB2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PM_PLAYER_CREW_COLOR(int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PM_PLAYER_CREW_COLOR, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### GET_MENU_PED_INT_STAT

**Hash:** `0xEF4CED81CEBEDC6D` | **JHash:** `0x4370999E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MENU_PED_INT_STAT(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MENU_PED_INT_STAT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### GET_CHARACTER_MENU_PED_INT_STAT

**Hash:** `0xCA6B2F7CE32AB653` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CHARACTER_MENU_PED_INT_STAT(Any p0, Any* p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CHARACTER_MENU_PED_INT_STAT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_MENU_PED_MASKED_INT_STAT

**Hash:** `0x90A6526CF0381030` | **JHash:** `0xD6CC4766` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MENU_PED_MASKED_INT_STAT(Hash statHash, int* outValue, int mask, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MENU_PED_MASKED_INT_STAT, statHash, outValue, mask, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `int*` | `int*` | |
| `mask` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_CHARACTER_MENU_PED_MASKED_INT_STAT

**Hash:** `0x24A49BEAF468DC90` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CHARACTER_MENU_PED_MASKED_INT_STAT(Hash statHash, Any* outValue, int p2, int mask, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CHARACTER_MENU_PED_MASKED_INT_STAT, statHash, outValue, p2, mask, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `Any*` | `object*` | |
| `p2` | `int` | `int` | |
| `mask` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_MENU_PED_FLOAT_STAT

**Hash:** `0x5FBD7095FE7AE57F` | **JHash:** `0x51972B04` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MENU_PED_FLOAT_STAT(Hash statHash, float* outValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MENU_PED_FLOAT_STAT, statHash, outValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### GET_CHARACTER_MENU_PED_FLOAT_STAT

**Hash:** `0x8F08017F9D7C47BD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CHARACTER_MENU_PED_FLOAT_STAT(float statHash, float* outValue, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CHARACTER_MENU_PED_FLOAT_STAT, statHash, outValue, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `float` | `float` | |
| `outValue` | `float*` | `float*` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_MENU_PED_BOOL_STAT

**Hash:** `0x052991E59076E4E4` | **JHash:** `0xD43BB56D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MENU_PED_BOOL_STAT(Hash statHash, BOOL* outValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MENU_PED_BOOL_STAT, statHash, outValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

**说明:** p0 was always 0xAE2602A3.

---

### CLEAR_PED_IN_PAUSE_MENU

**Hash:** `0x5E62BE5DC58E9E06` | **JHash:** `0x28058ACF` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_IN_PAUSE_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_IN_PAUSE_MENU);
```

**返回值:** `void` → `void`

---

### GIVE_PED_TO_PAUSE_MENU

**Hash:** `0xAC0BFBDC3BE00E14` | **JHash:** `0x2AD2C9CE` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_PED_TO_PAUSE_MENU(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_PED_TO_PAUSE_MENU, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 is either 1 or 2 in the PC scripts.

---

### SET_PAUSE_MENU_PED_LIGHTING

**Hash:** `0x3CA6050692BC61B0` | **JHash:** `0x127310EB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PAUSE_MENU_PED_LIGHTING(BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PAUSE_MENU_PED_LIGHTING, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the light state for the pause menu ped in frontend menus.

This is used by R* in combination with `SET_PAUSE_MENU_PED_SLEEP_STATE` to toggle the "offline" or "online" state in the "friends" tab of the pause menu in GTA Online.


Example:
Lights On: https://vespura.com/hi/i/2019-04-01_16-09_540ee_1015.png
Lights Off: https://vespura.com/hi/i/2019-04-01_16-10_8b5e7_1016.png

---

### SET_PAUSE_MENU_PED_SLEEP_STATE

**Hash:** `0xECF128344E9FF9F1` | **JHash:** `0x8F45D327` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PAUSE_MENU_PED_SLEEP_STATE(BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PAUSE_MENU_PED_SLEEP_STATE, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles the pause menu ped sleep state for frontend menus.

Example: https://vespura.com/hi/i/2019-04-01_15-51_8ed38_1014.gif

`state` 0 will make the ped slowly fall asleep, 1 will slowly wake the ped up.

---

### OPEN_ONLINE_POLICIES_MENU

**Hash:** `0x805D7CBB36FD6C4C` | **JHash:** `0x19FCBBB2` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_ONLINE_POLICIES_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_ONLINE_POLICIES_MENU);
```

**返回值:** `void` → `void`

---

### ARE_ONLINE_POLICIES_UP_TO_DATE

**Hash:** `0xF13FE2A80C05C561` | **JHash:** `0x850690FF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ONLINE_POLICIES_UP_TO_DATE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ONLINE_POLICIES_UP_TO_DATE);
```

**返回值:** `BOOL` → `bool`

---

### IS_ONLINE_POLICIES_MENU_ACTIVE

**Hash:** `0x6F72CD94F7B5B68C` | **JHash:** `0x9D4934F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ONLINE_POLICIES_MENU_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ONLINE_POLICIES_MENU_ACTIVE);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns the same as IS_SOCIAL_CLUB_ACTIVE

---

### OPEN_SOCIAL_CLUB_MENU

**Hash:** `0x75D3691713C3B05A` | **JHash:** `0x57218529` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_SOCIAL_CLUB_MENU(Hash menu)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_SOCIAL_CLUB_MENU, menu);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `menu` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Uses the `SOCIAL_CLUB2` scaleform.
menu: GALLERY, MISSIONS, CREWS, MIGRATE, PLAYLISTS, JOBS

---

### CLOSE_SOCIAL_CLUB_MENU

**Hash:** `0xD2B32BE3FC1626C6` | **JHash:** `0x5F86AA39` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_SOCIAL_CLUB_MENU(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_SOCIAL_CLUB_MENU);
```

**返回值:** `void` → `void`

---

### SET_SOCIAL_CLUB_TOUR

**Hash:** `0x9E778248D6685FE0` | **JHash:** `0x7AD67C95` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SOCIAL_CLUB_TOUR(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SOCIAL_CLUB_TOUR, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** HUD::SET_SOCIAL_CLUB_TOUR("Gallery");
HUD::SET_SOCIAL_CLUB_TOUR("Missions");
HUD::SET_SOCIAL_CLUB_TOUR("General");
HUD::SET_SOCIAL_CLUB_TOUR("Playlists");

---

### IS_SOCIAL_CLUB_ACTIVE

**Hash:** `0xC406BE343FC4B9AF` | **JHash:** `0xD4DA14EF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SOCIAL_CLUB_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SOCIAL_CLUB_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SET_TEXT_INPUT_BOX_ENABLED

**Hash:** `0x1185A8087587322C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEXT_INPUT_BOX_ENABLED(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEXT_INPUT_BOX_ENABLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_CLOSE_TEXT_INPUT_BOX

**Hash:** `0x8817605C2BA76200` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLOSE_TEXT_INPUT_BOX(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLOSE_TEXT_INPUT_BOX);
```

**返回值:** `void` → `void`

---

### SET_ALLOW_COMMA_ON_TEXT_INPUT

**Hash:** `0x577599CCED639CA2` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_ALLOW_COMMA_ON_TEXT_INPUT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_COMMA_ON_TEXT_INPUT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### OVERRIDE_MP_TEXT_CHAT_TEAM_STRING

**Hash:** `0x6A1738B4323FE2D9` | **Build:** `573`

**C++ 原型:**
```cpp
void OVERRIDE_MP_TEXT_CHAT_TEAM_STRING(Hash gxtEntryHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_MP_TEXT_CHAT_TEAM_STRING, gxtEntryHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gxtEntryHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### IS_MP_TEXT_CHAT_TYPING

**Hash:** `0xB118AF58B5F332A1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MP_TEXT_CHAT_TYPING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MP_TEXT_CHAT_TYPING);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns whether or not the text chat (MULTIPLAYER_CHAT Scaleform component) is active.

---

### CLOSE_MP_TEXT_CHAT

**Hash:** `0x1AC8F4AD40E22127` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_MP_TEXT_CHAT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_MP_TEXT_CHAT);
```

**返回值:** `void` → `void`

---

### MP_TEXT_CHAT_IS_TEAM_JOB

**Hash:** `0x7C226D5346D4D10A` | **Build:** `372`

**C++ 原型:**
```cpp
void MP_TEXT_CHAT_IS_TEAM_JOB(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MP_TEXT_CHAT_IS_TEAM_JOB, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### OVERRIDE_MP_TEXT_CHAT_COLOR

**Hash:** `0xF47E567B3630DD12` | **Build:** `678`

**C++ 原型:**
```cpp
void OVERRIDE_MP_TEXT_CHAT_COLOR(int p0, int hudColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_MP_TEXT_CHAT_COLOR, p0, hudColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `hudColor` | `int` | `int` | |

**返回值:** `void` → `void`

---

### MP_TEXT_CHAT_DISABLE

**Hash:** `0x1DB21A44B09E8BA3` | **Build:** `323`

**C++ 原型:**
```cpp
void MP_TEXT_CHAT_DISABLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MP_TEXT_CHAT_DISABLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Hides the chat history, closes the input box and makes it unable to be opened unless called again with FALSE.

---

### FLAG_PLAYER_CONTEXT_IN_TOURNAMENT

**Hash:** `0xCEF214315D276FD1` | **JHash:** `0xFF06772A` | **Build:** `323`

**C++ 原型:**
```cpp
void FLAG_PLAYER_CONTEXT_IN_TOURNAMENT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FLAG_PLAYER_CONTEXT_IN_TOURNAMENT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_HAS_AI_BLIP

**Hash:** `0xD30C50DF888D58B5` | **JHash:** `0x96C4C4DD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HAS_AI_BLIP(Ped ped, BOOL hasCone)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HAS_AI_BLIP, ped, hasCone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `hasCone` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native turns on the AI blip on the specified ped. It also disappears automatically when the ped is too far or if the ped is dead. You don't need to control it with other natives.

See gtaforums.com/topic/884370-native-research-ai-blips for further information.

---

### SET_PED_HAS_AI_BLIP_WITH_COLOUR

**Hash:** `0xB13DCB4C6FAAD238` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_PED_HAS_AI_BLIP_WITH_COLOUR(Ped ped, BOOL hasCone, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HAS_AI_BLIP_WITH_COLOUR, ped, hasCone, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `hasCone` | `BOOL` | `bool` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** color: see SET_BLIP_COLOUR

---

### DOES_PED_HAVE_AI_BLIP

**Hash:** `0x15B8ECF844EE67ED` | **JHash:** `0x3BE1257F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PED_HAVE_AI_BLIP(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PED_HAVE_AI_BLIP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_AI_BLIP_GANG_ID

**Hash:** `0xE52B8E7F85D39A08` | **JHash:** `0xD8E31B1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AI_BLIP_GANG_ID(Ped ped, int gangId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AI_BLIP_GANG_ID, ped, gangId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `gangId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_AI_BLIP_HAS_CONE

**Hash:** `0x3EED80DFF7325CAA` | **JHash:** `0x872C2CFB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AI_BLIP_HAS_CONE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AI_BLIP_HAS_CONE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_AI_BLIP_FORCED_ON

**Hash:** `0x0C4BBF625CA98C4E` | **JHash:** `0xFFDF46F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AI_BLIP_FORCED_ON(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AI_BLIP_FORCED_ON, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_AI_BLIP_NOTICE_RANGE

**Hash:** `0x97C65887D4B37FA9` | **JHash:** `0xF9DC2AF7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AI_BLIP_NOTICE_RANGE(Ped ped, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AI_BLIP_NOTICE_RANGE, ped, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_AI_BLIP_SPRITE

**Hash:** `0xFCFACD0DB9D7A57D` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PED_AI_BLIP_SPRITE(Ped ped, int spriteId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AI_BLIP_SPRITE, ped, spriteId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `spriteId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_AI_PED_PED_BLIP_INDEX

**Hash:** `0x7CD934010E115C2C` | **JHash:** `0x06349065` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_AI_PED_PED_BLIP_INDEX(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_AI_PED_PED_BLIP_INDEX, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Blip` → `int`

---

### GET_AI_PED_VEHICLE_BLIP_INDEX

**Hash:** `0x56176892826A4FE8` | **JHash:** `0xCA52CF43` | **Build:** `323`

**C++ 原型:**
```cpp
Blip GET_AI_PED_VEHICLE_BLIP_INDEX(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_AI_PED_VEHICLE_BLIP_INDEX, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Blip` → `int`

**说明:** Returns the current AI BLIP for the specified ped

---

### HAS_DIRECTOR_MODE_BEEN_LAUNCHED_BY_CODE

**Hash:** `0xA277800A9EAE340E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_DIRECTOR_MODE_BEEN_LAUNCHED_BY_CODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_DIRECTOR_MODE_BEEN_LAUNCHED_BY_CODE);
```

**返回值:** `BOOL` → `bool`

---

### SET_DIRECTOR_MODE_LAUNCHED_BY_SCRIPT

**Hash:** `0x2632482FD6B9AB87` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DIRECTOR_MODE_LAUNCHED_BY_SCRIPT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DIRECTOR_MODE_LAUNCHED_BY_SCRIPT);
```

**返回值:** `void` → `void`

---

### SET_PLAYER_IS_IN_DIRECTOR_MODE

**Hash:** `0x808519373FD336A3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_IS_IN_DIRECTOR_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_IS_IN_DIRECTOR_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If toggle is true, hides special ability bar / character name in the pause menu
If toggle is false, shows special ability bar / character name in the pause menu

---

### SET_DIRECTOR_MODE_AVAILABLE

**Hash:** `0x04655F9D075D0AE5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DIRECTOR_MODE_AVAILABLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DIRECTOR_MODE_AVAILABLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HIDE_HUDMARKERS_THIS_FRAME

**Hash:** `0x243296A510B562B6` | **Build:** `2060`

**C++ 原型:**
```cpp
void HIDE_HUDMARKERS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_HUDMARKERS_THIS_FRAME);
```

**返回值:** `void` → `void`

---


---

## INTERIOR

**包含 45 个 Native 函数**

### GET_INTERIOR_HEADING

**Hash:** `0xF49B58631D9E22D9` | **Build:** `1493`

**C++ 原型:**
```cpp
float GET_INTERIOR_HEADING(Interior interior)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_INTERIOR_HEADING, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `float` → `float`

---

### GET_INTERIOR_LOCATION_AND_NAMEHASH

**Hash:** `0x252BDC06B73FA6EA` | **JHash:** `0x75885CB3` | **Build:** `1290`

**C++ 原型:**
```cpp
void GET_INTERIOR_LOCATION_AND_NAMEHASH(Interior interior, Vector3* position, Hash* nameHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_INTERIOR_LOCATION_AND_NAMEHASH, interior, position, nameHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `position` | `Vector3*` | `Vector3*` | |
| `nameHash` | `Hash*` | `uint*` | |

**返回值:** `void` → `void`

---

### GET_INTERIOR_GROUP_ID

**Hash:** `0xE4A84ABF135EF91A` | **JHash:** `0x09D6376F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_INTERIOR_GROUP_ID(Interior interior)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_GROUP_ID, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the group ID of the specified interior.
0 = default
1 = subway station, subway tracks, sewers
3 = train tunnel under mirror park
5 = tunnel near del perro
6 = train tunnel near chilliad
7 = train tunnel near josiah
8 = train tunnel in sandy shores
9 = braddock tunnel (near chilliad)
12 = tunnel under fort zancudo
14 = train tunnel under cypress flats
18 = rockford plaza parking garage
19 = arcadius parking garage
20 = union depository parking garage
21 = fib parking garage

---

### GET_OFFSET_FROM_INTERIOR_IN_WORLD_COORDS

**Hash:** `0x9E3B3E6D66F6E22F` | **JHash:** `0x7D8F26A1` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_OFFSET_FROM_INTERIOR_IN_WORLD_COORDS(Interior interior, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_OFFSET_FROM_INTERIOR_IN_WORLD_COORDS, interior, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### IS_INTERIOR_SCENE

**Hash:** `0xBC72B5D7A1CBD54D` | **JHash:** `0x55226C13` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERIOR_SCENE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERIOR_SCENE);
```

**返回值:** `BOOL` → `bool`

---

### IS_VALID_INTERIOR

**Hash:** `0x26B0E73D7EAAF4D3` | **JHash:** `0x39C0B635` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VALID_INTERIOR(Interior interior)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VALID_INTERIOR, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_ROOM_FOR_ENTITY

**Hash:** `0xB365FC0C4E27FFA7` | **JHash:** `0x7DDADB92` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ROOM_FOR_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ROOM_FOR_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### FORCE_ROOM_FOR_ENTITY

**Hash:** `0x52923C4710DD9907` | **JHash:** `0x10BD4435` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ROOM_FOR_ENTITY(Entity entity, Interior interior, Hash roomHashKey)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ROOM_FOR_ENTITY, entity, interior, roomHashKey);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `interior` | `Interior` | `int` | |
| `roomHashKey` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_ROOM_KEY_FROM_ENTITY

**Hash:** `0x47C2A06D4F5F424B` | **JHash:** `0xE4ACF8C3` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_ROOM_KEY_FROM_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_ROOM_KEY_FROM_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Gets the room hash key from the room that the specified entity is in. Each room in every interior has a unique key. Returns 0 if the entity is outside.

---

### GET_KEY_FOR_ENTITY_IN_ROOM

**Hash:** `0x399685DB942336BC` | **JHash:** `0x91EA80EF` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_KEY_FOR_ENTITY_IN_ROOM(Entity entity)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_KEY_FOR_ENTITY_IN_ROOM, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Seems to do the exact same as INTERIOR::GET_ROOM_KEY_FROM_ENTITY

---

### GET_INTERIOR_FROM_ENTITY

**Hash:** `0x2107BA504071A6BB` | **JHash:** `0x5C644614` | **Build:** `323`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_FROM_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_FROM_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Interior` → `int`

**说明:** Returns the handle of the interior that the entity is in. Returns 0 if outside.

---

### RETAIN_ENTITY_IN_INTERIOR

**Hash:** `0x82EBB79E258FA2B7` | **JHash:** `0xE645E162` | **Build:** `323`

**C++ 原型:**
```cpp
void RETAIN_ENTITY_IN_INTERIOR(Entity entity, Interior interior)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RETAIN_ENTITY_IN_INTERIOR, entity, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `interior` | `Interior` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_INTERIOR_STATE_OF_ENTITY

**Hash:** `0x85D5422B2039A70D` | **Build:** `2189`

**C++ 原型:**
```cpp
void CLEAR_INTERIOR_STATE_OF_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_INTERIOR_STATE_OF_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Immediately removes entity from an interior. Like sets entity to `limbo` room.

---

### FORCE_ACTIVATING_TRACKING_ON_ENTITY

**Hash:** `0x38C1CB1CB119A016` | **Build:** `1493`

**C++ 原型:**
```cpp
void FORCE_ACTIVATING_TRACKING_ON_ENTITY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ACTIVATING_TRACKING_ON_ENTITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### FORCE_ROOM_FOR_GAME_VIEWPORT

**Hash:** `0x920D853F3E17F1DA` | **JHash:** `0xD79803B5` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ROOM_FOR_GAME_VIEWPORT(int interiorID, Hash roomHashKey)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ROOM_FOR_GAME_VIEWPORT, interiorID, roomHashKey);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interiorID` | `int` | `int` | |
| `roomHashKey` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_ROOM_FOR_GAME_VIEWPORT_BY_NAME

**Hash:** `0xAF348AFCB575A441` | **JHash:** `0x1F6B4B13` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROOM_FOR_GAME_VIEWPORT_BY_NAME(const char* roomName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROOM_FOR_GAME_VIEWPORT_BY_NAME, roomName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `roomName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Example of use (carmod_shop)
INTERIOR::SET_ROOM_FOR_GAME_VIEWPORT_BY_NAME("V_CarModRoom");

---

### SET_ROOM_FOR_GAME_VIEWPORT_BY_KEY

**Hash:** `0x405DC2AEF6AF95B9` | **JHash:** `0x0E9529CC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROOM_FOR_GAME_VIEWPORT_BY_KEY(Hash roomHashKey)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROOM_FOR_GAME_VIEWPORT_BY_KEY, roomHashKey);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `roomHashKey` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Usage: INTERIOR::SET_ROOM_FOR_GAME_VIEWPORT_BY_KEY(INTERIOR::GET_KEY_FOR_ENTITY_IN_ROOM(PLAYER::PLAYER_PED_ID()));

---

### GET_ROOM_KEY_FOR_GAME_VIEWPORT

**Hash:** `0xA6575914D2A0B450` | **JHash:** `0x4FF3D3F5` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_ROOM_KEY_FOR_GAME_VIEWPORT(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_ROOM_KEY_FOR_GAME_VIEWPORT);
```

**返回值:** `Hash` → `uint`

---

### CLEAR_ROOM_FOR_GAME_VIEWPORT

**Hash:** `0x23B59D8912F94246` | **JHash:** `0x617DC75D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ROOM_FOR_GAME_VIEWPORT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ROOM_FOR_GAME_VIEWPORT);
```

**返回值:** `void` → `void`

---

### GET_INTERIOR_FROM_PRIMARY_VIEW

**Hash:** `0xE7D267EC6CA966C3` | **JHash:** `0xA83C3D15` | **Build:** `1604`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_FROM_PRIMARY_VIEW(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_FROM_PRIMARY_VIEW);
```

**返回值:** `Interior` → `int`

**说明:** Returns the current interior id from gameplay camera

---

### GET_INTERIOR_AT_COORDS

**Hash:** `0xB0F7F8663821D9C3` | **JHash:** `0xA17FBF37` | **Build:** `323`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_AT_COORDS(float x, float y, float z)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_AT_COORDS, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `Interior` → `int`

**说明:** Returns interior ID from specified coordinates. If coordinates are outside, then it returns 0.

Example for VB.NET
Dim interiorID As Integer = Native.Function.Call(Of Integer)(Hash.GET_INTERIOR_AT_COORDS, X, Y, Z)

---

### ADD_PICKUP_TO_INTERIOR_ROOM_BY_NAME

**Hash:** `0x3F6167F351168730` | **JHash:** `0xA2A73564` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PICKUP_TO_INTERIOR_ROOM_BY_NAME(Pickup pickup, const char* roomName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PICKUP_TO_INTERIOR_ROOM_BY_NAME, pickup, roomName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `roomName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### PIN_INTERIOR_IN_MEMORY

**Hash:** `0x2CA429C029CCF247` | **JHash:** `0x3ADA414E` | **Build:** `323`

**C++ 原型:**
```cpp
void PIN_INTERIOR_IN_MEMORY(Interior interior)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PIN_INTERIOR_IN_MEMORY, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `void` → `void`

---

### UNPIN_INTERIOR

**Hash:** `0x261CCE7EED010641` | **JHash:** `0xFCFF792A` | **Build:** `323`

**C++ 原型:**
```cpp
void UNPIN_INTERIOR(Interior interior)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNPIN_INTERIOR, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `void` → `void`

**说明:** Does something similar to INTERIOR::DISABLE_INTERIOR.

You don't fall through the floor but everything is invisible inside and looks the same as when INTERIOR::DISABLE_INTERIOR is used. Peds behaves normally inside. 

---

### IS_INTERIOR_READY

**Hash:** `0x6726BDCCC1932F0E` | **JHash:** `0xE1EF6450` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERIOR_READY(Interior interior)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERIOR_READY, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_INTERIOR_IN_USE

**Hash:** `0x4C2330E61D3DEB56` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_INTERIOR_IN_USE(Interior interior)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_INTERIOR_IN_USE, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Only used once in the entire game scripts.
Does not actually return anything.

---

### GET_INTERIOR_AT_COORDS_WITH_TYPE

**Hash:** `0x05B7A89BD78797FC` | **JHash:** `0x96525B06` | **Build:** `323`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_AT_COORDS_WITH_TYPE(float x, float y, float z, const char* interiorType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_AT_COORDS_WITH_TYPE, x, y, z, interiorType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `interiorType` | `const char*` | `string` | |

**返回值:** `Interior` → `int`

**说明:** Returns the interior ID representing the requested interior at that location (if found?). The supplied interior string is not the same as the one used to load the interior.

Use: INTERIOR::UNPIN_INTERIOR(INTERIOR::GET_INTERIOR_AT_COORDS_WITH_TYPE(x, y, z, interior))

Interior types include: "V_Michael", "V_Franklins", "V_Franklinshouse", etc.. you can find them in the scripts.

Not a very useful native as you could just use GET_INTERIOR_AT_COORDS instead and get the same result, without even having to specify the interior type.

---

### GET_INTERIOR_AT_COORDS_WITH_TYPEHASH

**Hash:** `0xF0F77ADB9F67E79D` | **JHash:** `0x11718507` | **Build:** `323`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_AT_COORDS_WITH_TYPEHASH(float x, float y, float z, Hash typeHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_AT_COORDS_WITH_TYPEHASH, x, y, z, typeHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `typeHash` | `Hash` | `uint` | |

**返回值:** `Interior` → `int`

**说明:** Hashed version of GET_INTERIOR_AT_COORDS_WITH_TYPE

---

### ACTIVATE_INTERIOR_GROUPS_USING_CAMERA

**Hash:** `0x483ACA1176CA93F1` | **Build:** `1103`

**C++ 原型:**
```cpp
void ACTIVATE_INTERIOR_GROUPS_USING_CAMERA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_INTERIOR_GROUPS_USING_CAMERA);
```

**返回值:** `void` → `void`

---

### IS_COLLISION_MARKED_OUTSIDE

**Hash:** `0xEEA5AC2EDA7C33E8` | **JHash:** `0x7762249C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COLLISION_MARKED_OUTSIDE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COLLISION_MARKED_OUTSIDE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the collision at the specified coords is marked as being outside (false if there's an interior)

---

### GET_INTERIOR_FROM_COLLISION

**Hash:** `0xEC4CF9FCB29A4424` | **JHash:** `0x7ED33DC1` | **Build:** `323`

**C++ 原型:**
```cpp
Interior GET_INTERIOR_FROM_COLLISION(float x, float y, float z)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INTERIOR_FROM_COLLISION, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `Interior` → `int`

---

### ENABLE_STADIUM_PROBES_THIS_FRAME

**Hash:** `0x7ECDF98587E92DEC` | **Build:** `1604`

**C++ 原型:**
```cpp
void ENABLE_STADIUM_PROBES_THIS_FRAME(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_STADIUM_PROBES_THIS_FRAME, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ACTIVATE_INTERIOR_ENTITY_SET

**Hash:** `0x55E86AF2712B36A1` | **JHash:** `0xC80A5DDF` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_INTERIOR_ENTITY_SET(Interior interior, const char* entitySetName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_INTERIOR_ENTITY_SET, interior, entitySetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `entitySetName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** More info: http://gtaforums.com/topic/836367-adding-props-to-interiors/

Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### DEACTIVATE_INTERIOR_ENTITY_SET

**Hash:** `0x420BD37289EEE162` | **JHash:** `0xDBA768A1` | **Build:** `323`

**C++ 原型:**
```cpp
void DEACTIVATE_INTERIOR_ENTITY_SET(Interior interior, const char* entitySetName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DEACTIVATE_INTERIOR_ENTITY_SET, interior, entitySetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `entitySetName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### IS_INTERIOR_ENTITY_SET_ACTIVE

**Hash:** `0x35F7DD45E8C0A16D` | **JHash:** `0x39A3CC6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERIOR_ENTITY_SET_ACTIVE(Interior interior, const char* entitySetName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERIOR_ENTITY_SET_ACTIVE, interior, entitySetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `entitySetName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### SET_INTERIOR_ENTITY_SET_TINT_INDEX

**Hash:** `0xC1F1920BAF281317` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_INTERIOR_ENTITY_SET_TINT_INDEX(Interior interior, const char* entitySetName, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INTERIOR_ENTITY_SET_TINT_INDEX, interior, entitySetName, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `entitySetName` | `const char*` | `string` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### REFRESH_INTERIOR

**Hash:** `0x41F37C3427C75AE0` | **JHash:** `0x9A29ACE6` | **Build:** `323`

**C++ 原型:**
```cpp
void REFRESH_INTERIOR(Interior interior)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REFRESH_INTERIOR, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `void` → `void`

---

### ENABLE_EXTERIOR_CULL_MODEL_THIS_FRAME

**Hash:** `0xA97F257D0151A6AB` | **JHash:** `0x1F375B4C` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_EXTERIOR_CULL_MODEL_THIS_FRAME(Hash mapObjectHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_EXTERIOR_CULL_MODEL_THIS_FRAME, mapObjectHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mapObjectHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** This is the native that is used to hide the exterior of GTA Online apartment buildings when you are inside an apartment.

More info: http://gtaforums.com/topic/836301-hiding-gta-online-apartment-exteriors/

---

### ENABLE_SHADOW_CULL_MODEL_THIS_FRAME

**Hash:** `0x50C375537449F369` | **Build:** `757`

**C++ 原型:**
```cpp
void ENABLE_SHADOW_CULL_MODEL_THIS_FRAME(Hash mapObjectHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_SHADOW_CULL_MODEL_THIS_FRAME, mapObjectHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mapObjectHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### DISABLE_INTERIOR

**Hash:** `0x6170941419D7D8EC` | **JHash:** `0x093ADEA5` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_INTERIOR(Interior interior, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_INTERIOR, interior, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example: 
This removes the interior from the strip club and when trying to walk inside the player just falls:

INTERIOR::DISABLE_INTERIOR(118018, true);

---

### IS_INTERIOR_DISABLED

**Hash:** `0xBC5115A5A939DD15` | **JHash:** `0x81F34C71` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERIOR_DISABLED(Interior interior)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERIOR_DISABLED, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAP_INTERIOR

**Hash:** `0xD9175F941610DB54` | **JHash:** `0x34E735A6` | **Build:** `323`

**C++ 原型:**
```cpp
void CAP_INTERIOR(Interior interior, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CAP_INTERIOR, interior, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does something similar to INTERIOR::DISABLE_INTERIOR

---

### IS_INTERIOR_CAPPED

**Hash:** `0x92BAC8ACF88CEC26` | **JHash:** `0x18B17C80` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INTERIOR_CAPPED(Interior interior)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INTERIOR_CAPPED, interior);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DISABLE_METRO_SYSTEM

**Hash:** `0x9E6542F0CE8E70A3` | **JHash:** `0x5EF9C5C2` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_METRO_SYSTEM(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_METRO_SYSTEM, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IS_EXTERIOR_ONLY

**Hash:** `0x7241CCB7D020DB69` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_IS_EXTERIOR_ONLY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IS_EXTERIOR_ONLY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Jenkins hash _might_ be 0xFC227584.

---


---

## ITEMSET

**包含 9 个 Native 函数**

### CREATE_ITEMSET

**Hash:** `0x35AD299F50D91B24` | **JHash:** `0x0A113B2C` | **Build:** `323`

**C++ 原型:**
```cpp
ScrHandle CREATE_ITEMSET(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_ITEMSET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `ScrHandle` → `int`

---

### DESTROY_ITEMSET

**Hash:** `0xDE18220B1C183EDA` | **JHash:** `0x83CE1A4C` | **Build:** `323`

**C++ 原型:**
```cpp
void DESTROY_ITEMSET(ScrHandle itemset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DESTROY_ITEMSET, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `void` → `void`

---

### IS_ITEMSET_VALID

**Hash:** `0xB1B1EA596344DFAB` | **JHash:** `0xD201FC29` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ITEMSET_VALID(ScrHandle itemset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ITEMSET_VALID, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ADD_TO_ITEMSET

**Hash:** `0xE3945201F14637DD` | **JHash:** `0x6B0FE61B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ADD_TO_ITEMSET(ScrHandle item, ScrHandle itemset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ADD_TO_ITEMSET, item, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `item` | `ScrHandle` | `int` | |
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_FROM_ITEMSET

**Hash:** `0x25E68244B0177686` | **JHash:** `0xA9565228` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_FROM_ITEMSET(ScrHandle item, ScrHandle itemset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_FROM_ITEMSET, item, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `item` | `ScrHandle` | `int` | |
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `void` → `void`

---

### GET_ITEMSET_SIZE

**Hash:** `0xD9127E83ABF7C631` | **JHash:** `0x2B31F41A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ITEMSET_SIZE(ScrHandle itemset)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ITEMSET_SIZE, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `int` → `int`

---

### GET_INDEXED_ITEM_IN_ITEMSET

**Hash:** `0x7A197E2521EE2BAB` | **JHash:** `0x3F712874` | **Build:** `323`

**C++ 原型:**
```cpp
ScrHandle GET_INDEXED_ITEM_IN_ITEMSET(int index, ScrHandle itemset)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INDEXED_ITEM_IN_ITEMSET, index, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `ScrHandle` → `int`

---

### IS_IN_ITEMSET

**Hash:** `0x2D0FC594D1E9C107` | **JHash:** `0x0D4B9730` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_IN_ITEMSET(ScrHandle item, ScrHandle itemset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_IN_ITEMSET, item, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `item` | `ScrHandle` | `int` | |
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAN_ITEMSET

**Hash:** `0x41BC0D722FC04221` | **JHash:** `0x919A4858` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAN_ITEMSET(ScrHandle itemset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAN_ITEMSET, itemset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemset` | `ScrHandle` | `int` | |

**返回值:** `void` → `void`

---


---

## LOBBY

**包含 8 个 Native 函数**

### LOBBY_AUTO_MULTIPLAYER_MENU

**Hash:** `0xF2CA003F167E21D2` | **JHash:** `0x106C8317` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOBBY_AUTO_MULTIPLAYER_MENU(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOBBY_AUTO_MULTIPLAYER_MENU);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### LOBBY_AUTO_MULTIPLAYER_FREEMODE

**Hash:** `0xEF7D17BC6C85264C` | **JHash:** `0xD87F3A9E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOBBY_AUTO_MULTIPLAYER_FREEMODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOBBY_AUTO_MULTIPLAYER_FREEMODE);
```

**返回值:** `BOOL` → `bool`

---

### LOBBY_SET_AUTO_MULTIPLAYER

**Hash:** `0xB0C56BD3D808D863` | **JHash:** `0xC0B971EA` | **Build:** `323`

**C++ 原型:**
```cpp
void LOBBY_SET_AUTO_MULTIPLAYER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOBBY_SET_AUTO_MULTIPLAYER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOBBY_AUTO_MULTIPLAYER_EVENT

**Hash:** `0x8AA464D4E0F6ACCD` | **JHash:** `0x94BCAC7C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOBBY_AUTO_MULTIPLAYER_EVENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOBBY_AUTO_MULTIPLAYER_EVENT);
```

**返回值:** `BOOL` → `bool`

---

### LOBBY_SET_AUTO_MULTIPLAYER_EVENT

**Hash:** `0xFC309E94546FCDB5` | **JHash:** `0x7D90EEE5` | **Build:** `323`

**C++ 原型:**
```cpp
void LOBBY_SET_AUTO_MULTIPLAYER_EVENT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOBBY_SET_AUTO_MULTIPLAYER_EVENT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOBBY_AUTO_MULTIPLAYER_RANDOM_JOB

**Hash:** `0xC6DC823253FBB366` | **JHash:** `0x734CFEDA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LOBBY_AUTO_MULTIPLAYER_RANDOM_JOB(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOBBY_AUTO_MULTIPLAYER_RANDOM_JOB);
```

**返回值:** `BOOL` → `bool`

---

### LOBBY_SET_AUTO_MP_RANDOM_JOB

**Hash:** `0xC7E7181C09F33B69` | **JHash:** `0x8C227332` | **Build:** `323`

**C++ 原型:**
```cpp
void LOBBY_SET_AUTO_MP_RANDOM_JOB(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOBBY_SET_AUTO_MP_RANDOM_JOB, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SHUTDOWN_SESSION_CLEARS_AUTO_MULTIPLAYER

**Hash:** `0xFA1E0E893D915215` | **JHash:** `0x5C350D78` | **Build:** `323`

**C++ 原型:**
```cpp
void SHUTDOWN_SESSION_CLEARS_AUTO_MULTIPLAYER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHUTDOWN_SESSION_CLEARS_AUTO_MULTIPLAYER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---


---

## LOCALIZATION

**包含 3 个 Native 函数**

### LOCALIZATION_GET_SYSTEM_LANGUAGE

**Hash:** `0x497420E022796B3F` | **Build:** `877`

**C++ 原型:**
```cpp
int LOCALIZATION_GET_SYSTEM_LANGUAGE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LOCALIZATION_GET_SYSTEM_LANGUAGE);
```

**返回值:** `int` → `int`

**说明:** Same return values as GET_CURRENT_LANGUAGE

---

### GET_CURRENT_LANGUAGE

**Hash:** `0x2BDD44CC428A7EAE` | **JHash:** `0x761BE00B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_LANGUAGE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_LANGUAGE);
```

**返回值:** `int` → `int`

**说明:** 0 = american (en-US)
1 = french (fr-FR)
2 = german (de-DE)
3 = italian (it-IT)
4 = spanish (es-ES)
5 = brazilian (pt-BR)
6 = polish (pl-PL)
7 = russian (ru-RU)
8 = korean (ko-KR)
9 = chinesetrad (zh-TW)
10 = japanese (ja-JP)
11 = mexican (es-MX)
12 = chinesesimp (zh-CN)

---

### LOCALIZATION_GET_SYSTEM_DATE_TYPE

**Hash:** `0xA8AE43AEC1A61314` | **Build:** `323`

**C++ 原型:**
```cpp
int LOCALIZATION_GET_SYSTEM_DATE_TYPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LOCALIZATION_GET_SYSTEM_DATE_TYPE);
```

**返回值:** `int` → `int`

**说明:** Possible return values: 0, 1, 2

---


---

## MISC

**包含 334 个 Native 函数**

### GET_ALLOCATED_STACK_SIZE

**Hash:** `0x8B3CA62B1EF19B62` | **JHash:** `0x4E9CA30A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ALLOCATED_STACK_SIZE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ALLOCATED_STACK_SIZE);
```

**返回值:** `int` → `int`

---

### GET_NUMBER_OF_FREE_STACKS_OF_THIS_SIZE

**Hash:** `0xFEAD16FC8F9DFC0F` | **JHash:** `0x11A178B8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_FREE_STACKS_OF_THIS_SIZE(int stackSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_FREE_STACKS_OF_THIS_SIZE, stackSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `stackSize` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_RANDOM_SEED

**Hash:** `0x444D98F98C11F3EC` | **JHash:** `0xDB3FEB5C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_SEED(int seed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_SEED, seed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `seed` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_TIME_SCALE

**Hash:** `0x1D408577D440E81E` | **JHash:** `0xA7F84694` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TIME_SCALE(float timeScale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TIME_SCALE, timeScale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeScale` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Maximum value is 1.
At a value of 0 the game will still run at a minimum time scale.

Slow Motion 1: 0.6
Slow Motion 2: 0.4
Slow Motion 3: 0.2

---

### SET_MISSION_FLAG

**Hash:** `0xC4301E5121A0ED73` | **JHash:** `0x57592D52` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MISSION_FLAG(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MISSION_FLAG, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If true, the player can't save the game. 


If the parameter is true, sets the mission flag to true, if the parameter is false, the function does nothing at all.

^ also, if the mission flag is already set, the function does nothing at all

---

### GET_MISSION_FLAG

**Hash:** `0xA33CDCCDA663159E` | **JHash:** `0x95115F97` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MISSION_FLAG(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MISSION_FLAG);
```

**返回值:** `BOOL` → `bool`

---

### SET_RANDOM_EVENT_FLAG

**Hash:** `0x971927086CFD2158` | **JHash:** `0xA77F31E8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_EVENT_FLAG(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_EVENT_FLAG, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If the parameter is true, sets the random event flag to true, if the parameter is false, the function does nothing at all.
Does nothing if the mission flag is set.

---

### GET_RANDOM_EVENT_FLAG

**Hash:** `0xD2D57F1D764117B1` | **JHash:** `0x794CC92C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_RANDOM_EVENT_FLAG(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_RANDOM_EVENT_FLAG);
```

**返回值:** `BOOL` → `bool`

---

### GET_CONTENT_TO_LOAD

**Hash:** `0x24DA7D7667FD7B09` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CONTENT_TO_LOAD(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CONTENT_TO_LOAD);
```

**返回值:** `const char*` → `string`

**说明:** Returns pointer to an empty string.

---

### ACTIVITY_FEED_CREATE

**Hash:** `0x4DCDF92BF64236CD` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_CREATE(const char* p0, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_CREATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ADD_SUBSTRING_TO_CAPTION

**Hash:** `0x31125FD509D9043F` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ADD_SUBSTRING_TO_CAPTION(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ADD_SUBSTRING_TO_CAPTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ADD_LITERAL_SUBSTRING_TO_CAPTION

**Hash:** `0xEBD3205A207939ED` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ADD_LITERAL_SUBSTRING_TO_CAPTION(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ADD_LITERAL_SUBSTRING_TO_CAPTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ADD_INT_TO_CAPTION

**Hash:** `0x97E7E2C04245115B` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ADD_INT_TO_CAPTION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ADD_INT_TO_CAPTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_LARGE_IMAGE_URL

**Hash:** `0x916CA67D26FD1E37` | **Build:** `2060`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_LARGE_IMAGE_URL(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_LARGE_IMAGE_URL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE

**Hash:** `0xEB078CA2B5E82ADD` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE(const char* p0, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE_ADD

**Hash:** `0x703CC7F60CBB2B57` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE_ADD(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ACTION_START_WITH_COMMAND_LINE_ADD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_POST

**Hash:** `0x8951EB9C6906D3C8` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_POST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_POST);
```

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

---

### ACTIVITY_FEED_ONLINE_PLAYED_WITH_POST

**Hash:** `0xBA4B8D83BDC75551` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVITY_FEED_ONLINE_PLAYED_WITH_POST(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVITY_FEED_ONLINE_PLAYED_WITH_POST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub). Seems to be PS4 specific.

Used only once in the scripts (ingamehud) with p0 = "AF_GAMEMODE"

---

### HAS_RESUMED_FROM_SUSPEND

**Hash:** `0xE8B9C0EC9E183F35` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_RESUMED_FROM_SUSPEND(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_RESUMED_FROM_SUSPEND);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return false.

---

### SET_SCRIPT_HIGH_PRIO

**Hash:** `0x65D2EBB47E1CEC21` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_HIGH_PRIO(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_HIGH_PRIO, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets GtaThread+0x14A

---

### SET_THIS_IS_A_TRIGGER_SCRIPT

**Hash:** `0x6F2135B6129620C1` | **JHash:** `0x8B2DE971` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIS_IS_A_TRIGGER_SCRIPT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIS_IS_A_TRIGGER_SCRIPT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets bit 3 in GtaThread+0x150

---

### INFORM_CODE_OF_CONTENT_ID_OF_CURRENT_UGC_MISSION

**Hash:** `0x8D74E26F54B4E5C3` | **JHash:** `0xE77199F7` | **Build:** `323`

**C++ 原型:**
```cpp
void INFORM_CODE_OF_CONTENT_ID_OF_CURRENT_UGC_MISSION(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INFORM_CODE_OF_CONTENT_ID_OF_CURRENT_UGC_MISSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### GET_BASE_ELEMENT_LOCATION_FROM_METADATA_BLOCK

**Hash:** `0xB335F761606DB47C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_BASE_ELEMENT_LOCATION_FROM_METADATA_BLOCK(Any* p0, Any* p1, Any p2, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_BASE_ELEMENT_LOCATION_FROM_METADATA_BLOCK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_PREV_WEATHER_TYPE_HASH_NAME

**Hash:** `0x564B884A05EC45A3` | **JHash:** `0xA8171E9E` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PREV_WEATHER_TYPE_HASH_NAME(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PREV_WEATHER_TYPE_HASH_NAME);
```

**返回值:** `Hash` → `uint`

**说明:** Returns current weather name hash

---

### GET_NEXT_WEATHER_TYPE_HASH_NAME

**Hash:** `0x711327CD09C8F162` | **JHash:** `0x353E8056` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_NEXT_WEATHER_TYPE_HASH_NAME(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_NEXT_WEATHER_TYPE_HASH_NAME);
```

**返回值:** `Hash` → `uint`

**说明:** Returns weather name hash

---

### IS_PREV_WEATHER_TYPE

**Hash:** `0x44F28F86433B10A9` | **JHash:** `0x250ADA61` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PREV_WEATHER_TYPE(const char* weatherType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PREV_WEATHER_TYPE, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### IS_NEXT_WEATHER_TYPE

**Hash:** `0x2FAA3A30BEC0F25D` | **JHash:** `0x99CB167F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NEXT_WEATHER_TYPE(const char* weatherType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NEXT_WEATHER_TYPE, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SET_WEATHER_TYPE_PERSIST

**Hash:** `0x704983DF373B198F` | **JHash:** `0xC6C04C75` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_TYPE_PERSIST(const char* weatherType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_TYPE_PERSIST, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** The following weatherTypes are used in the scripts:
"CLEAR"
"EXTRASUNNY"
"CLOUDS"
"OVERCAST"
"RAIN"
"CLEARING"
"THUNDER"
"SMOG"
"FOGGY"
"XMAS"
"SNOW"
"SNOWLIGHT"
"BLIZZARD"
"HALLOWEEN"
"NEUTRAL"

---

### SET_WEATHER_TYPE_NOW_PERSIST

**Hash:** `0xED712CA327900C8A` | **JHash:** `0xC869FE97` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_TYPE_NOW_PERSIST(const char* weatherType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_TYPE_NOW_PERSIST, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** The following weatherTypes are used in the scripts:
"CLEAR"
"EXTRASUNNY"
"CLOUDS"
"OVERCAST"
"RAIN"
"CLEARING"
"THUNDER"
"SMOG"
"FOGGY"
"XMAS"
"SNOW"
"SNOWLIGHT"
"BLIZZARD"
"HALLOWEEN"
"NEUTRAL"

---

### SET_WEATHER_TYPE_NOW

**Hash:** `0x29B487C359E19889` | **JHash:** `0x361E9EAC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_TYPE_NOW(const char* weatherType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_TYPE_NOW, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** The following weatherTypes are used in the scripts:
"CLEAR"
"EXTRASUNNY"
"CLOUDS"
"OVERCAST"
"RAIN"
"CLEARING"
"THUNDER"
"SMOG"
"FOGGY"
"XMAS"
"SNOW"
"SNOWLIGHT"
"BLIZZARD"
"HALLOWEEN"
"NEUTRAL"

---

### SET_WEATHER_TYPE_OVERTIME_PERSIST

**Hash:** `0xFB5045B7C42B75BF` | **JHash:** `0x386F0D25` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEATHER_TYPE_OVERTIME_PERSIST(const char* weatherType, float time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEATHER_TYPE_OVERTIME_PERSIST, weatherType, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |
| `time` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_RANDOM_WEATHER_TYPE

**Hash:** `0x8B05F884CF7E8020` | **JHash:** `0xE7AA1BC9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_WEATHER_TYPE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_WEATHER_TYPE);
```

**返回值:** `void` → `void`

---

### CLEAR_WEATHER_TYPE_PERSIST

**Hash:** `0xCCC39339BEF76CF5` | **JHash:** `0x6AB757D8` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_WEATHER_TYPE_PERSIST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_WEATHER_TYPE_PERSIST);
```

**返回值:** `void` → `void`

---

### CLEAR_WEATHER_TYPE_NOW_PERSIST_NETWORK

**Hash:** `0x0CF97F497FE7D048` | **Build:** `1103`

**C++ 原型:**
```cpp
void CLEAR_WEATHER_TYPE_NOW_PERSIST_NETWORK(int milliseconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_WEATHER_TYPE_NOW_PERSIST_NETWORK, milliseconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `milliseconds` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CURR_WEATHER_STATE

**Hash:** `0xF3BBE884A14BB413` | **JHash:** `0x9A5C1D56` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_CURR_WEATHER_STATE(Hash* weatherType1, Hash* weatherType2, float* percentWeather2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_CURR_WEATHER_STATE, weatherType1, weatherType2, percentWeather2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType1` | `Hash*` | `uint*` | |
| `weatherType2` | `Hash*` | `uint*` | |
| `percentWeather2` | `float*` | `float*` | |

**返回值:** `void` → `void`

---

### SET_CURR_WEATHER_STATE

**Hash:** `0x578C752848ECFA0C` | **JHash:** `0x5CA74040` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CURR_WEATHER_STATE(Hash weatherType1, Hash weatherType2, float percentWeather2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CURR_WEATHER_STATE, weatherType1, weatherType2, percentWeather2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType1` | `Hash` | `uint` | |
| `weatherType2` | `Hash` | `uint` | |
| `percentWeather2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Mixes two weather types. If percentWeather2 is set to 0.0f, then the weather will be entirely of weatherType1, if it is set to 1.0f it will be entirely of weatherType2. If it's set somewhere in between, there will be a mixture of weather behaviors. To test, try this in the RPH console, and change the float to different values between 0 and 1:

execute "NativeFunction.Natives.x578C752848ECFA0C(Game.GetHashKey(""RAIN""), Game.GetHashKey(""SMOG""), 0.50f);

Note that unlike most of the other weather natives, this native takes the hash of the weather name, not the plain string. These are the weather names and their hashes:

CLEAR  0x36A83D84
EXTRASUNNY  0x97AA0A79
CLOUDS  0x30FDAF5C
OVERCAST    0xBB898D2D
RAIN    0x54A69840
CLEARING    0x6DB1A50D
THUNDER 0xB677829F
SMOG    0x10DCF4B5
FOGGY   0xAE737644
XMAS    0xAAC9C895
SNOWLIGHT   0x23FB812B
BLIZZARD    0x27EA2814





/* OLD INVALID INFO BELOW */
Not tested. Based purely on disassembly. Instantly sets the weather to sourceWeather, then transitions to targetWeather over the specified transitionTime in seconds.

If an invalid hash is specified for sourceWeather, the current weather type will be used.
If an invalid hash is specified for targetWeather, the next weather type will be used.
If an invalid hash is specified for both sourceWeather and targetWeather, the function just changes the transition time of the current transition.

---

### SET_OVERRIDE_WEATHER

**Hash:** `0xA43D5C6FE51ADBEF` | **JHash:** `0xD9082BB5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OVERRIDE_WEATHER(const char* weatherType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OVERRIDE_WEATHER, weatherType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Appears to have an optional bool parameter that is unused in the scripts.

If you pass true, something will be set to zero.

---

### SET_OVERRIDE_WEATHEREX

**Hash:** `0x1178E104409FE58C` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_OVERRIDE_WEATHEREX(const char* weatherType, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OVERRIDE_WEATHEREX, weatherType, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weatherType` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Identical to SET_OVERRIDE_WEATHER but has an additional BOOL param that sets some weather var to 0 if true

---

### CLEAR_OVERRIDE_WEATHER

**Hash:** `0x338D2E3477711050` | **JHash:** `0x7740EA4E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_OVERRIDE_WEATHER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_OVERRIDE_WEATHER);
```

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_SHOREWAVEAMPLITUDE

**Hash:** `0xB8F87EAD7533B176` | **JHash:** `0x625181DC` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_SHOREWAVEAMPLITUDE(float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_SHOREWAVEAMPLITUDE, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_SHOREWAVEMINAMPLITUDE

**Hash:** `0xC3EAD29AB273ECE8` | **JHash:** `0xBEBBFDC8` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_SHOREWAVEMINAMPLITUDE(float minAmplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_SHOREWAVEMINAMPLITUDE, minAmplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minAmplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_SHOREWAVEMAXAMPLITUDE

**Hash:** `0xA7A1127490312C36` | **JHash:** `0x6926AB03` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_SHOREWAVEMAXAMPLITUDE(float maxAmplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_SHOREWAVEMAXAMPLITUDE, maxAmplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxAmplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_OCEANNOISEMINAMPLITUDE

**Hash:** `0x31727907B2C43C55` | **JHash:** `0xD447439D` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_OCEANNOISEMINAMPLITUDE(float minAmplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_OCEANNOISEMINAMPLITUDE, minAmplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minAmplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_OCEANWAVEAMPLITUDE

**Hash:** `0x405591EC8FD9096D` | **JHash:** `0x584E9C59` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_OCEANWAVEAMPLITUDE(float amplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_OCEANWAVEAMPLITUDE, amplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_OCEANWAVEMINAMPLITUDE

**Hash:** `0xF751B16FB32ABC1D` | **JHash:** `0x5656D578` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_OCEANWAVEMINAMPLITUDE(float minAmplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_OCEANWAVEMINAMPLITUDE, minAmplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minAmplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_OCEANWAVEMAXAMPLITUDE

**Hash:** `0xB3E6360DDE733E82` | **JHash:** `0x0DE40C28` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_OCEANWAVEMAXAMPLITUDE(float maxAmplitude)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_OCEANWAVEMAXAMPLITUDE, maxAmplitude);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxAmplitude` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_RIPPLEBUMPINESS

**Hash:** `0x7C9C0B1EEB1F9072` | **JHash:** `0x98C9138B` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_RIPPLEBUMPINESS(float bumpiness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_RIPPLEBUMPINESS, bumpiness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `bumpiness` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_RIPPLEMINBUMPINESS

**Hash:** `0x6216B116083A7CB4` | **JHash:** `0xFB1A9CDE` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_RIPPLEMINBUMPINESS(float minBumpiness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_RIPPLEMINBUMPINESS, minBumpiness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `minBumpiness` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_RIPPLEMAXBUMPINESS

**Hash:** `0x9F5E6BB6B34540DA` | **JHash:** `0x1C0CAE89` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_RIPPLEMAXBUMPINESS(float maxBumpiness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_RIPPLEMAXBUMPINESS, maxBumpiness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxBumpiness` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_RIPPLEDISTURB

**Hash:** `0xB9854DFDE0D833D6` | **JHash:** `0x4671AC2E` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_RIPPLEDISTURB(float disturb)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_RIPPLEDISTURB, disturb);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `disturb` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_SET_STRENGTH

**Hash:** `0xC54A08C85AE4D410` | **JHash:** `0xDA02F415` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_SET_STRENGTH(float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_SET_STRENGTH, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This seems to edit the water wave, intensity around your current location.

0.0f = Normal
1.0f = So Calm and Smooth, a boat will stay still.
3.0f = Really Intense.

---

### WATER_OVERRIDE_FADE_IN

**Hash:** `0xA8434F1DFF41D6E7` | **JHash:** `0x5F3DDEC0` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_FADE_IN(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_FADE_IN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WATER_OVERRIDE_FADE_OUT

**Hash:** `0xC3C221ADDDE31A11` | **JHash:** `0x63A89684` | **Build:** `323`

**C++ 原型:**
```cpp
void WATER_OVERRIDE_FADE_OUT(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WATER_OVERRIDE_FADE_OUT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_WIND

**Hash:** `0xAC3A74E8384A9919` | **JHash:** `0xC6294698` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WIND(float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WIND, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the the normalized wind speed value. The wind speed clamps always at 12.0, SET_WIND sets the wind in a percentage, 0.0 is 0 and 1.0 is 12.0. Setting this value to a negative number resumes the random wind speed changes provided by the game.

---

### SET_WIND_SPEED

**Hash:** `0xEE09ECEDBABE47FC` | **JHash:** `0x45705F94` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WIND_SPEED(float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WIND_SPEED, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Using this native will set the absolute wind speed value. The wind speed clamps to a range of 0.0- 12.0. Setting this value to a negative number resumes the random wind speed changes provided by the game.

---

### GET_WIND_SPEED

**Hash:** `0xA8CF1CC0AFCD3F12` | **JHash:** `0x098F0F3C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_WIND_SPEED(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_WIND_SPEED);
```

**返回值:** `float` → `float`

---

### SET_WIND_DIRECTION

**Hash:** `0xEB0F4468467B4528` | **JHash:** `0x381AEEE9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WIND_DIRECTION(float direction)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WIND_DIRECTION, direction);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `direction` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The wind direction in radians
180 degrees (PI), wind will blow from the south. Setting this value to a negative number resumes the random wind direction changes provided by the game.

---

### GET_WIND_DIRECTION

**Hash:** `0x1F400FEF721170DA` | **JHash:** `0x89499A0D` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_WIND_DIRECTION(void)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_WIND_DIRECTION);
```

**返回值:** `Vector3` → `Vector3`

---

### SET_RAIN

**Hash:** `0x643E26EA6E024D92` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RAIN(float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RAIN, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** With an `intensity` higher than `0.5f`, only the creation of puddles gets faster, rain and rain sound won't increase after that.
With an `intensity` of `0.0f` rain and rain sounds are disabled and there won't be any new puddles.
To use the rain intensity of the current weather, call this native with `-1f` as `intensity`.

---

### GET_RAIN_LEVEL

**Hash:** `0x96695E368AD855F3` | **JHash:** `0xC9F67F28` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_RAIN_LEVEL(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_RAIN_LEVEL);
```

**返回值:** `float` → `float`

---

### SET_SNOW

**Hash:** `0x7F06937B0CDCBC1A` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_SNOW(float level)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SNOW, level);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `level` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_SNOW_LEVEL

**Hash:** `0xC5868A966E5BE3AE` | **JHash:** `0x1B09184F` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SNOW_LEVEL(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SNOW_LEVEL);
```

**返回值:** `float` → `float`

---

### FORCE_LIGHTNING_FLASH

**Hash:** `0xF6062E089251C898` | **JHash:** `0xDF38165E` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_LIGHTNING_FLASH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_LIGHTNING_FLASH);
```

**返回值:** `void` → `void`

**说明:** creates single lightning+thunder at random position

---

### SET_CLOUD_SETTINGS_OVERRIDE

**Hash:** `0x02DEAAC8F8EA7FE7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CLOUD_SETTINGS_OVERRIDE(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CLOUD_SETTINGS_OVERRIDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### PRELOAD_CLOUD_HAT

**Hash:** `0x11B56FBBF7224868` | **JHash:** `0x8727A4C5` | **Build:** `323`

**C++ 原型:**
```cpp
void PRELOAD_CLOUD_HAT(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_CLOUD_HAT, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### LOAD_CLOUD_HAT

**Hash:** `0xFC4842A34657BFCB` | **JHash:** `0xED88FC61` | **Build:** `323`

**C++ 原型:**
```cpp
void LOAD_CLOUD_HAT(const char* name, float transitionTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOAD_CLOUD_HAT, name, transitionTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `transitionTime` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The following cloudhats are useable:
altostratus
Cirrus
cirrocumulus
Clear 01
Cloudy 01
Contrails
Horizon
horizonband1
horizonband2
horizonband3
horsey
Nimbus
Puffs
RAIN
Snowy 01
Stormy 01
stratoscumulus
Stripey
shower
Wispy


---

### UNLOAD_CLOUD_HAT

**Hash:** `0xA74802FB8D0B7814` | **JHash:** `0xC9FA6E07` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOAD_CLOUD_HAT(const char* name, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOAD_CLOUD_HAT, name, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UNLOAD_ALL_CLOUD_HATS

**Hash:** `0x957E790EA1727B64` | **JHash:** `0x2D7787BC` | **Build:** `323`

**C++ 原型:**
```cpp
void UNLOAD_ALL_CLOUD_HATS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNLOAD_ALL_CLOUD_HATS);
```

**返回值:** `void` → `void`

---

### SET_CLOUDS_ALPHA

**Hash:** `0xF36199225D6D8C86` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CLOUDS_ALPHA(float opacity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CLOUDS_ALPHA, opacity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `opacity` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CLOUDS_ALPHA

**Hash:** `0x20AC25E781AE4A84` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CLOUDS_ALPHA(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CLOUDS_ALPHA);
```

**返回值:** `float` → `float`

---

### GET_GAME_TIMER

**Hash:** `0x9CD27B0045628463` | **JHash:** `0xA4EA0691` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_GAME_TIMER(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_GAME_TIMER);
```

**返回值:** `int` → `int`

---

### GET_FRAME_TIME

**Hash:** `0x15C40837039FFAF7` | **JHash:** `0x96374262` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FRAME_TIME(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FRAME_TIME);
```

**返回值:** `float` → `float`

---

### GET_SYSTEM_TIME_STEP

**Hash:** `0xE599A503B3837E1B` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SYSTEM_TIME_STEP(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SYSTEM_TIME_STEP);
```

**返回值:** `float` → `float`

---

### GET_FRAME_COUNT

**Hash:** `0xFC8202EFC642E6F2` | **JHash:** `0xB477A015` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FRAME_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FRAME_COUNT);
```

**返回值:** `int` → `int`

---

### GET_RANDOM_FLOAT_IN_RANGE

**Hash:** `0x313CE5879CEB6FCD` | **JHash:** `0x0562C4D0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_RANDOM_FLOAT_IN_RANGE(float startRange, float endRange)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_RANDOM_FLOAT_IN_RANGE, startRange, endRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startRange` | `float` | `float` | |
| `endRange` | `float` | `float` | |

**返回值:** `float` → `float`

---

### GET_RANDOM_INT_IN_RANGE

**Hash:** `0xD53343AA4FB7DD28` | **JHash:** `0x4051115B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_RANDOM_INT_IN_RANGE(int startRange, int endRange)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_INT_IN_RANGE, startRange, endRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startRange` | `int` | `int` | |
| `endRange` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_RANDOM_MWC_INT_IN_RANGE

**Hash:** `0xF2D49816A804D134` | **Build:** `1734`

**C++ 原型:**
```cpp
int GET_RANDOM_MWC_INT_IN_RANGE(int startRange, int endRange)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_MWC_INT_IN_RANGE, startRange, endRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startRange` | `int` | `int` | |
| `endRange` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_GROUND_Z_FOR_3D_COORD

**Hash:** `0xC906A7DAB05C8D2B` | **JHash:** `0xA1BFD5E0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_GROUND_Z_FOR_3D_COORD(float x, float y, float z, float* groundZ, BOOL ignoreWater, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_GROUND_Z_FOR_3D_COORD, x, y, z, groundZ, ignoreWater, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `groundZ` | `float*` | `float*` | |
| `ignoreWater` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets the ground elevation at the specified position. Note that if the specified position is below ground level, the function will output zero!

x: Position on the X-axis to get ground elevation at.
y: Position on the Y-axis to get ground elevation at.
z: Position on the Z-axis to get ground elevation at.
groundZ: The ground elevation at the specified position.
ignoreWater: Nearly always 0, very rarely 1 in the scripts: https://gfycat.com/NiftyTatteredCricket

Bear in mind this native can only calculate the elevation when the coordinates are within the client's render distance.

---

### GET_GROUND_Z_AND_NORMAL_FOR_3D_COORD

**Hash:** `0x8BDC7BFC57A81E76` | **JHash:** `0x64D91CED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_GROUND_Z_AND_NORMAL_FOR_3D_COORD(float x, float y, float z, float* groundZ, Vector3* normal)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_GROUND_Z_AND_NORMAL_FOR_3D_COORD, x, y, z, groundZ, normal);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `groundZ` | `float*` | `float*` | |
| `normal` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

---

### GET_GROUND_Z_EXCLUDING_OBJECTS_FOR_3D_COORD

**Hash:** `0x9E82F0F362881B29` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_GROUND_Z_EXCLUDING_OBJECTS_FOR_3D_COORD(float x, float y, float z, float* groundZ, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_GROUND_Z_EXCLUDING_OBJECTS_FOR_3D_COORD, x, y, z, groundZ, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `groundZ` | `float*` | `float*` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### ASIN

**Hash:** `0xC843060B5765DCE7` | **JHash:** `0x998E5CAD` | **Build:** `323`

**C++ 原型:**
```cpp
float ASIN(float p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ASIN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `float` → `float`

---

### ACOS

**Hash:** `0x1D08B970013C34B6` | **JHash:** `0xF4038776` | **Build:** `323`

**C++ 原型:**
```cpp
float ACOS(float p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ACOS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `float` → `float`

---

### TAN

**Hash:** `0x632106CC96E82E91` | **JHash:** `0xD320CE5E` | **Build:** `323`

**C++ 原型:**
```cpp
float TAN(float p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.TAN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `float` → `float`

---

### ATAN

**Hash:** `0xA9D1795CD5043663` | **JHash:** `0x7A03CC8E` | **Build:** `323`

**C++ 原型:**
```cpp
float ATAN(float p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ATAN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `float` → `float`

---

### ATAN2

**Hash:** `0x8927CBF9D22261A4` | **JHash:** `0x2508AC81` | **Build:** `323`

**C++ 原型:**
```cpp
float ATAN2(float p0, float p1)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ATAN2, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `float` → `float`

---

### GET_DISTANCE_BETWEEN_COORDS

**Hash:** `0xF1B760881820C952` | **JHash:** `0xF698765E` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_DISTANCE_BETWEEN_COORDS(float x1, float y1, float z1, float x2, float y2, float z2, BOOL useZ)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_DISTANCE_BETWEEN_COORDS, x1, y1, z1, x2, y2, z2, useZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `useZ` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

**说明:** Returns the distance between two three-dimensional points, optionally ignoring the Z values.
If useZ is false, only the 2D plane (X-Y) will be considered for calculating the distance.

Consider using this faster native instead: SYSTEM::VDIST - DVIST always takes in consideration the 3D coordinates.

---

### GET_ANGLE_BETWEEN_2D_VECTORS

**Hash:** `0x186FC4BE848E1C92` | **JHash:** `0xDBF75E58` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_ANGLE_BETWEEN_2D_VECTORS(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_ANGLE_BETWEEN_2D_VECTORS, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `float` → `float`

---

### GET_HEADING_FROM_VECTOR_2D

**Hash:** `0x2FFB6B224F4B2926` | **JHash:** `0xD209D52B` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_HEADING_FROM_VECTOR_2D(float dx, float dy)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_HEADING_FROM_VECTOR_2D, dx, dy);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dx` | `float` | `float` | |
| `dy` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** dx = x1 - x2
dy = y1 - y2

---

### GET_RATIO_OF_CLOSEST_POINT_ON_LINE

**Hash:** `0x7F8F6405F4777AF6` | **JHash:** `0x89459F0A` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_RATIO_OF_CLOSEST_POINT_ON_LINE(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, BOOL clamp)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_RATIO_OF_CLOSEST_POINT_ON_LINE, x1, y1, z1, x2, y2, z2, x3, y3, z3, clamp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `clamp` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

**说明:** returns a float between 0.0 and 1.0, clamp: sets whether the product should be clamped between the given coordinates

---

### GET_CLOSEST_POINT_ON_LINE

**Hash:** `0x21C235BC64831E5A` | **JHash:** `0xCAECF37E` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_CLOSEST_POINT_ON_LINE(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, BOOL clamp)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_CLOSEST_POINT_ON_LINE, x1, y1, z1, x2, y2, z2, x3, y3, z3, clamp);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `clamp` | `BOOL` | `bool` | |

**返回值:** `Vector3` → `Vector3`

**说明:** clamp: sets whether the product should be clamped between the given coordinates

---

### GET_LINE_PLANE_INTERSECTION

**Hash:** `0xF56DFB7B61BE7276` | **JHash:** `0xC6CC812C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_LINE_PLANE_INTERSECTION(float p0, float p1, float p2, float p3, float p4, float p5, float p6, float p7, float p8, float p9, float p10, float p11, float* p12)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_LINE_PLANE_INTERSECTION, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `float` | `float` | |
| `p11` | `float` | `float` | |
| `p12` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### GET_POINT_AREA_OVERLAP

**Hash:** `0xA0AD167E4B39D9A2` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL GET_POINT_AREA_OVERLAP(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9, Any p10, Any p11, Any p12, Any p13)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_POINT_AREA_OVERLAP, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |
| `p11` | `Any` | `object` | |
| `p12` | `Any` | `object` | |
| `p13` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_BIT

**Hash:** `0x933D6A9EEC1BACD0` | **JHash:** `0x4EFE7E6B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BIT(int* address, int offset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BIT, address, offset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `address` | `int*` | `int*` | |
| `offset` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This sets bit [offset] of [address] to on.

The offsets used are different bits to be toggled on and off, typically there is only one address used in a script.

Example:
MISC::SET_BIT(&bitAddress, 1);

To check if this bit has been enabled:
MISC::IS_BIT_SET(bitAddress, 1); // will return 1 afterwards

Please note, this method may assign a value to [address] when used.

---

### CLEAR_BIT

**Hash:** `0xE80492A9AC099A93` | **JHash:** `0x8BC9E618` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_BIT(int* address, int offset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_BIT, address, offset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `address` | `int*` | `int*` | |
| `offset` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This sets bit [offset] of [address] to off.

Example:
MISC::CLEAR_BIT(&bitAddress, 1);

To check if this bit has been enabled:
MISC::IS_BIT_SET(bitAddress, 1); // will return 0 afterwards

---

### GET_HASH_KEY

**Hash:** `0xD24D37CC275948CC` | **JHash:** `0x98EFF6F1` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_HASH_KEY(const char* string)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_HASH_KEY, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `Hash` → `uint`

**说明:** This native converts its past string to hash. It is hashed using jenkins one at a time method.

---

### SLERP_NEAR_QUATERNION

**Hash:** `0xF2F6A2FA49278625` | **JHash:** `0x87B92190` | **Build:** `323`

**C++ 原型:**
```cpp
void SLERP_NEAR_QUATERNION(float t, float x, float y, float z, float w, float x1, float y1, float z1, float w1, float* outX, float* outY, float* outZ, float* outW)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SLERP_NEAR_QUATERNION, t, x, y, z, w, x1, y1, z1, w1, outX, outY, outZ, outW);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `t` | `float` | `float` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `w` | `float` | `float` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `w1` | `float` | `float` | |
| `outX` | `float*` | `float*` | |
| `outY` | `float*` | `float*` | |
| `outZ` | `float*` | `float*` | |
| `outW` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** This native always come right before SET_ENTITY_QUATERNION where its final 4 parameters are SLERP_NEAR_QUATERNION p9 to p12

---

### IS_AREA_OCCUPIED

**Hash:** `0xA61B4DF533DCB56E` | **JHash:** `0xC013972F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AREA_OCCUPIED(float p0, float p1, float p2, float p3, float p4, float p5, BOOL p6, BOOL p7, BOOL p8, BOOL p9, BOOL p10, Any p11, BOOL p12)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AREA_OCCUPIED, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `BOOL` | `bool` | |
| `p11` | `Any` | `object` | |
| `p12` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_AREA_OCCUPIED_SLOW

**Hash:** `0x39455BF4F4F55186` | **Build:** `1868`

**C++ 原型:**
```cpp
BOOL IS_AREA_OCCUPIED_SLOW(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9, Any p10, Any p11, Any p12)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AREA_OCCUPIED_SLOW, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |
| `p11` | `Any` | `object` | |
| `p12` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_POSITION_OCCUPIED

**Hash:** `0xADCDE75E1C60F32D` | **JHash:** `0x452E8D9E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_POSITION_OCCUPIED(float x, float y, float z, float range, BOOL p4, BOOL checkVehicles, BOOL checkPeds, BOOL p7, BOOL p8, Entity ignoreEntity, BOOL p10)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_POSITION_OCCUPIED, x, y, z, range, p4, checkVehicles, checkPeds, p7, p8, ignoreEntity, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `checkVehicles` | `BOOL` | `bool` | |
| `checkPeds` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `ignoreEntity` | `Entity` | `int` | |
| `p10` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** `range`: The range, seems to not be very accurate during testing.
`p4`: Unknown, when set to true it seems to always return true no matter what I try.
`checkVehicle`: Check for any vehicles in that area.
`checkPeds`: Check for any peds in that area.
`ignoreEntity`: This entity will be ignored if it's in the area. Set to 0 if you don't want to exclude any entities.
The BOOL parameters that are documented have not been confirmed. They are just documented from what I've found during testing. They may not work as expected in all cases.

Returns true if there is anything in that location matching the provided parameters.

---

### IS_POINT_OBSCURED_BY_A_MISSION_ENTITY

**Hash:** `0xE54E209C35FFA18D` | **JHash:** `0xC161558D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_POINT_OBSCURED_BY_A_MISSION_ENTITY(float p0, float p1, float p2, float p3, float p4, float p5, Any p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_POINT_OBSCURED_BY_A_MISSION_ENTITY, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_AREA

**Hash:** `0xA56F01F3765B93A0` | **JHash:** `0x854E9AB8` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA(float X, float Y, float Z, float radius, BOOL p4, BOOL ignoreCopCars, BOOL ignoreObjects, BOOL p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA, X, Y, Z, radius, p4, ignoreCopCars, ignoreObjects, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `X` | `float` | `float` | |
| `Y` | `float` | `float` | |
| `Z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `ignoreCopCars` | `BOOL` | `bool` | |
| `ignoreObjects` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example: CLEAR_AREA(0, 0, 0, 30, true, false, false, false);

---

### CLEAR_AREA_LEAVE_VEHICLE_HEALTH

**Hash:** `0x957838AAF91BD12D` | **JHash:** `0x20E4FFD9` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_LEAVE_VEHICLE_HEALTH(float x, float y, float z, float radius, BOOL p4, BOOL p5, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_LEAVE_VEHICLE_HEALTH, x, y, z, radius, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** MISC::CLEAR_AREA_LEAVE_VEHICLE_HEALTH(x, y, z, radius, false, false, false, false); seem to make all objects go away, peds, vehicles etc. All booleans set to true doesn't seem to change anything. 

---

### CLEAR_AREA_OF_VEHICLES

**Hash:** `0x01C7B9B38428AEB6` | **JHash:** `0x63320F3C` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_OF_VEHICLES(float x, float y, float z, float radius, BOOL p4, BOOL p5, BOOL p6, BOOL p7, BOOL p8, BOOL p9, Any p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_OF_VEHICLES, x, y, z, radius, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Example:
CLEAR_AREA_OF_VEHICLES(0.0f, 0.0f, 0.0f, 10000.0f, false, false, false, false, false, false);

---

### CLEAR_ANGLED_AREA_OF_VEHICLES

**Hash:** `0x11DB3500F042A8AA` | **JHash:** `0xF11A3018` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ANGLED_AREA_OF_VEHICLES(float x1, float y1, float z1, float x2, float y2, float z2, float width, BOOL p7, BOOL p8, BOOL p9, BOOL p10, BOOL p11, Any p12, Any p13)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ANGLED_AREA_OF_VEHICLES, x1, y1, z1, x2, y2, z2, width, p7, p8, p9, p10, p11, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `BOOL` | `bool` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `Any` | `object` | |
| `p13` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CLEAR_AREA_OF_OBJECTS

**Hash:** `0xDD9B9B385AAC7F5B` | **JHash:** `0xBB720FE7` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_OF_OBJECTS(float x, float y, float z, float radius, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_OF_OBJECTS, x, y, z, radius, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** I looked through the PC scripts that this site provides you with a link to find. It shows the last param mainly uses, (0, 2, 6, 16, and 17) so I am going to assume it is a type of flag. 

---

### CLEAR_AREA_OF_PEDS

**Hash:** `0xBE31FD6CE464AC59` | **JHash:** `0x25BE7FA8` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_OF_PEDS(float x, float y, float z, float radius, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_OF_PEDS, x, y, z, radius, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example:       CLEAR_AREA_OF_PEDS(0, 0, 0, 10000, 1);

---

### CLEAR_AREA_OF_COPS

**Hash:** `0x04F8FC8FCF58F88D` | **JHash:** `0x95C53824` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_OF_COPS(float x, float y, float z, float radius, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_OF_COPS, x, y, z, radius, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags appears to always be 0

---

### CLEAR_AREA_OF_PROJECTILES

**Hash:** `0x0A1CB9094635D1A6` | **JHash:** `0x18DB5434` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_AREA_OF_PROJECTILES(float x, float y, float z, float radius, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_AREA_OF_PROJECTILES, x, y, z, radius, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags is usually 0 in the scripts.

---

### CLEAR_SCENARIO_SPAWN_HISTORY

**Hash:** `0x7EC6F9A478A6A512` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_SCENARIO_SPAWN_HISTORY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_SCENARIO_SPAWN_HISTORY);
```

**返回值:** `void` → `void`

**说明:** Possibly used to clear scenario points.

---

### SET_SAVE_MENU_ACTIVE

**Hash:** `0xC9BF75D28165FF77` | **JHash:** `0xF5CCF164` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SAVE_MENU_ACTIVE(BOOL ignoreVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SAVE_MENU_ACTIVE, ignoreVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ignoreVehicle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** ignoreVehicle - bypasses vehicle check of the local player (it will not open if you are in a vehicle and this is set to false)

---

### GET_STATUS_OF_MANUAL_SAVE

**Hash:** `0x397BAA01068BAA96` | **JHash:** `0x39771F21` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_MANUAL_SAVE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_MANUAL_SAVE);
```

**返回值:** `int` → `int`

---

### SET_CREDITS_ACTIVE

**Hash:** `0xB938B7E6D3C0620C` | **JHash:** `0xEC2A0ECF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CREDITS_ACTIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CREDITS_ACTIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CREDITS_FADE_OUT_WITH_SCREEN

**Hash:** `0xB51B9AB9EF81868C` | **JHash:** `0x75B06B5A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CREDITS_FADE_OUT_WITH_SCREEN(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CREDITS_FADE_OUT_WITH_SCREEN, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HAVE_CREDITS_REACHED_END

**Hash:** `0x075F1D57402C93BA` | **JHash:** `0x2569C9A7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAVE_CREDITS_REACHED_END(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_CREDITS_REACHED_END);
```

**返回值:** `BOOL` → `bool`

---

### ARE_CREDITS_RUNNING

**Hash:** `0xD19C0826DC20CF1C` | **Build:** `2802`

**C++ 原型:**
```cpp
BOOL ARE_CREDITS_RUNNING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_CREDITS_RUNNING);
```

**返回值:** `BOOL` → `bool`

---

### TERMINATE_ALL_SCRIPTS_WITH_THIS_NAME

**Hash:** `0x9DC711BC69C548DF` | **JHash:** `0x9F861FD4` | **Build:** `323`

**C++ 原型:**
```cpp
void TERMINATE_ALL_SCRIPTS_WITH_THIS_NAME(const char* scriptName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERMINATE_ALL_SCRIPTS_WITH_THIS_NAME, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_SCRIPT_IS_SAFE_FOR_NETWORK_GAME

**Hash:** `0x9243BAC96D64C050` | **JHash:** `0x878486CE` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_SCRIPT_IS_SAFE_FOR_NETWORK_GAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_SCRIPT_IS_SAFE_FOR_NETWORK_GAME);
```

**返回值:** `void` → `void`

---

### ADD_HOSPITAL_RESTART

**Hash:** `0x1F464EF988465A81` | **JHash:** `0x4F3E3104` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_HOSPITAL_RESTART(float x, float y, float z, float p3, Any p4)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_HOSPITAL_RESTART, x, y, z, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** Returns the index of the newly created hospital spawn point.

p3 might be radius?

---

### DISABLE_HOSPITAL_RESTART

**Hash:** `0xC8535819C450EBA8` | **JHash:** `0x09F49C72` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_HOSPITAL_RESTART(int hospitalIndex, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_HOSPITAL_RESTART, hospitalIndex, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hospitalIndex` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The game by default has 5 hospital respawn points. Disabling them all will cause the player to respawn at the last position they were.

---

### ADD_POLICE_RESTART

**Hash:** `0x452736765B31FC4B` | **JHash:** `0xE96C29FE` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_POLICE_RESTART(float p0, float p1, float p2, float p3, Any p4)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_POLICE_RESTART, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### DISABLE_POLICE_RESTART

**Hash:** `0x23285DED6EBD7EA3` | **JHash:** `0x0A280324` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_POLICE_RESTART(int policeIndex, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_POLICE_RESTART, policeIndex, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `policeIndex` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables the spawn point at the police house on the specified index.

policeIndex: The police house index.
toggle: true to enable the spawn point, false to disable.

- Nacorpio

---

### SET_RESTART_COORD_OVERRIDE

**Hash:** `0x706B5EDCAA7FA663` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RESTART_COORD_OVERRIDE(float x, float y, float z, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RESTART_COORD_OVERRIDE, x, y, z, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_RESTART_COORD_OVERRIDE

**Hash:** `0xA2716D40842EAF79` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_RESTART_COORD_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_RESTART_COORD_OVERRIDE);
```

**返回值:** `void` → `void`

---

### PAUSE_DEATH_ARREST_RESTART

**Hash:** `0x2C2B3493FBF51C71` | **JHash:** `0x296574AE` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_DEATH_ARREST_RESTART(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_DEATH_ARREST_RESTART, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IGNORE_NEXT_RESTART

**Hash:** `0x21FFB63D8C615361` | **JHash:** `0xDA13A4B6` | **Build:** `323`

**C++ 原型:**
```cpp
void IGNORE_NEXT_RESTART(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.IGNORE_NEXT_RESTART, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_FADE_OUT_AFTER_DEATH

**Hash:** `0x4A18E01DF2C87B86` | **JHash:** `0xC9F6F0BC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FADE_OUT_AFTER_DEATH(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FADE_OUT_AFTER_DEATH, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether the game should fade out after the player dies.

---

### SET_FADE_OUT_AFTER_ARREST

**Hash:** `0x1E0B4DC0D990A4E7` | **JHash:** `0xCB074B9D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FADE_OUT_AFTER_ARREST(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FADE_OUT_AFTER_ARREST, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether the game should fade out after the player is arrested.

---

### SET_FADE_IN_AFTER_DEATH_ARREST

**Hash:** `0xDA66D2796BA33F12` | **JHash:** `0xACDE6985` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FADE_IN_AFTER_DEATH_ARREST(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FADE_IN_AFTER_DEATH_ARREST, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether the game should fade in after the player dies or is arrested.

---

### SET_FADE_IN_AFTER_LOAD

**Hash:** `0xF3D78F59DFE18D79` | **JHash:** `0x6E00EB0B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FADE_IN_AFTER_LOAD(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FADE_IN_AFTER_LOAD, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REGISTER_SAVE_HOUSE

**Hash:** `0xC0714D0A7EEECA54` | **JHash:** `0x39C1849A` | **Build:** `323`

**C++ 原型:**
```cpp
int REGISTER_SAVE_HOUSE(float x, float y, float z, float p3, const char* p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REGISTER_SAVE_HOUSE, x, y, z, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `const char*` | `string` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** returns savehouseHandle

---

### SET_SAVE_HOUSE

**Hash:** `0x4F548CABEAE553BC` | **JHash:** `0xC3240BB4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SAVE_HOUSE(int savehouseHandle, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SAVE_HOUSE, savehouseHandle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `savehouseHandle` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### OVERRIDE_SAVE_HOUSE

**Hash:** `0x1162EA8AE9D24EEA` | **JHash:** `0x47436C12` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL OVERRIDE_SAVE_HOUSE(BOOL p0, float p1, float p2, float p3, float p4, BOOL p5, float p6, float p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.OVERRIDE_SAVE_HOUSE, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### GET_SAVE_HOUSE_DETAILS_AFTER_SUCCESSFUL_LOAD

**Hash:** `0xA4A0065E39C9F25C` | **JHash:** `0xC4D71AB4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_SAVE_HOUSE_DETAILS_AFTER_SUCCESSFUL_LOAD(Vector3* p0, float* p1, BOOL* fadeInAfterLoad, BOOL* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SAVE_HOUSE_DETAILS_AFTER_SUCCESSFUL_LOAD, p0, p1, fadeInAfterLoad, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Vector3*` | `Vector3*` | |
| `p1` | `float*` | `float*` | |
| `fadeInAfterLoad` | `BOOL*` | `bool*` | |
| `p3` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### DO_AUTO_SAVE

**Hash:** `0x50EEAAD86232EE55` | **JHash:** `0x54C44B1A` | **Build:** `323`

**C++ 原型:**
```cpp
void DO_AUTO_SAVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DO_AUTO_SAVE);
```

**返回值:** `void` → `void`

---

### GET_IS_AUTO_SAVE_OFF

**Hash:** `0x6E04F06094C87047` | **JHash:** `0xA8546914` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_AUTO_SAVE_OFF(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_AUTO_SAVE_OFF);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if profile setting 208 is equal to 0.

---

### IS_AUTO_SAVE_IN_PROGRESS

**Hash:** `0x69240733738C19A0` | **JHash:** `0x36F75399` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AUTO_SAVE_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AUTO_SAVE_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### HAS_CODE_REQUESTED_AUTOSAVE

**Hash:** `0x2107A3773771186D` | **JHash:** `0x78350773` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CODE_REQUESTED_AUTOSAVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CODE_REQUESTED_AUTOSAVE);
```

**返回值:** `BOOL` → `bool`

---

### CLEAR_CODE_REQUESTED_AUTOSAVE

**Hash:** `0x06462A961E94B67C` | **JHash:** `0x5A45B11A` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_CODE_REQUESTED_AUTOSAVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_CODE_REQUESTED_AUTOSAVE);
```

**返回值:** `void` → `void`

---

### BEGIN_REPLAY_STATS

**Hash:** `0xE0E500246FF73D66` | **JHash:** `0x17F4F44D` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_REPLAY_STATS(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_REPLAY_STATS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ADD_REPLAY_STAT_VALUE

**Hash:** `0x69FE6DC87BD2A5E9` | **JHash:** `0x81216EE0` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_REPLAY_STAT_VALUE(Any value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_REPLAY_STAT_VALUE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### END_REPLAY_STATS

**Hash:** `0xA23E821FBDF8A5F2` | **JHash:** `0xCB570185` | **Build:** `323`

**C++ 原型:**
```cpp
void END_REPLAY_STATS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_REPLAY_STATS);
```

**返回值:** `void` → `void`

---

### HAVE_REPLAY_STATS_BEEN_STORED

**Hash:** `0xD642319C54AADEB6` | **JHash:** `0xC58250F1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAVE_REPLAY_STATS_BEEN_STORED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_REPLAY_STATS_BEEN_STORED);
```

**返回值:** `BOOL` → `bool`

---

### GET_REPLAY_STAT_MISSION_ID

**Hash:** `0x5B1F2E327B6B6FE1` | **JHash:** `0x50C39926` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_REPLAY_STAT_MISSION_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_REPLAY_STAT_MISSION_ID);
```

**返回值:** `int` → `int`

---

### GET_REPLAY_STAT_MISSION_TYPE

**Hash:** `0x2B626A0150E4D449` | **JHash:** `0x710E5D1E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_REPLAY_STAT_MISSION_TYPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_REPLAY_STAT_MISSION_TYPE);
```

**返回值:** `int` → `int`

---

### GET_REPLAY_STAT_COUNT

**Hash:** `0xDC9274A7EF6B2867` | **JHash:** `0xC7BD1AF0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_REPLAY_STAT_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_REPLAY_STAT_COUNT);
```

**返回值:** `int` → `int`

---

### GET_REPLAY_STAT_AT_INDEX

**Hash:** `0x8098C8D6597AAE18` | **JHash:** `0x22BE2423` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_REPLAY_STAT_AT_INDEX(int index)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_REPLAY_STAT_AT_INDEX, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `int` → `int`

---

### CLEAR_REPLAY_STATS

**Hash:** `0x1B1AB132A16FDA55` | **JHash:** `0xC47DFF02` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_REPLAY_STATS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_REPLAY_STATS);
```

**返回值:** `void` → `void`

---

### QUEUE_MISSION_REPEAT_LOAD

**Hash:** `0x72DE52178C291CB5` | **JHash:** `0xF62B3C48` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL QUEUE_MISSION_REPEAT_LOAD(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.QUEUE_MISSION_REPEAT_LOAD);
```

**返回值:** `BOOL` → `bool`

---

### QUEUE_MISSION_REPEAT_SAVE

**Hash:** `0x44A0BDC559B35F6E` | **JHash:** `0x3589452B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL QUEUE_MISSION_REPEAT_SAVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.QUEUE_MISSION_REPEAT_SAVE);
```

**返回值:** `BOOL` → `bool`

**说明:** Shows the screen which is visible before you redo a mission? The game will make a restoration point where you will cameback when the mission is over.
Returns 1 if the message isn't currently on screen

---

### QUEUE_MISSION_REPEAT_SAVE_FOR_BENCHMARK_TEST

**Hash:** `0xEB2104E905C6F2E9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL QUEUE_MISSION_REPEAT_SAVE_FOR_BENCHMARK_TEST(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.QUEUE_MISSION_REPEAT_SAVE_FOR_BENCHMARK_TEST);
```

**返回值:** `BOOL` → `bool`

---

### GET_STATUS_OF_MISSION_REPEAT_SAVE

**Hash:** `0x2B5E102E4A42F2BF` | **JHash:** `0x144AAF22` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_MISSION_REPEAT_SAVE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_MISSION_REPEAT_SAVE);
```

**返回值:** `int` → `int`

---

### IS_MEMORY_CARD_IN_USE

**Hash:** `0x8A75CE2956274ADD` | **JHash:** `0x40CE4DFD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MEMORY_CARD_IN_USE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MEMORY_CARD_IN_USE);
```

**返回值:** `BOOL` → `bool`

---

### SHOOT_SINGLE_BULLET_BETWEEN_COORDS

**Hash:** `0x867654CBC7606F2C` | **JHash:** `0xCB7415AC` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOOT_SINGLE_BULLET_BETWEEN_COORDS(float x1, float y1, float z1, float x2, float y2, float z2, int damage, BOOL p7, Hash weaponHash, Ped ownerPed, BOOL isAudible, BOOL isInvisible, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOOT_SINGLE_BULLET_BETWEEN_COORDS, x1, y1, z1, x2, y2, z2, damage, p7, weaponHash, ownerPed, isAudible, isInvisible, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `damage` | `int` | `int` | |
| `p7` | `BOOL` | `bool` | |
| `weaponHash` | `Hash` | `uint` | |
| `ownerPed` | `Ped` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY

**Hash:** `0xE3A7742E0B7A2F8B` | **JHash:** `0x52ACCB7B` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY(float x1, float y1, float z1, float x2, float y2, float z2, int damage, BOOL p7, Hash weaponHash, Ped ownerPed, BOOL isAudible, BOOL isInvisible, float speed, Entity entity, Any p14)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY, x1, y1, z1, x2, y2, z2, damage, p7, weaponHash, ownerPed, isAudible, isInvisible, speed, entity, p14);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `damage` | `int` | `int` | |
| `p7` | `BOOL` | `bool` | |
| `weaponHash` | `Hash` | `uint` | |
| `ownerPed` | `Ped` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `speed` | `float` | `float` | |
| `entity` | `Entity` | `int` | |
| `p14` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** entity - entity to ignore

---

### SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY_NEW

**Hash:** `0xBFE5756E7407064A` | **JHash:** `0xCCDC33CC` | **Build:** `323`

**C++ 原型:**
```cpp
void SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY_NEW(float x1, float y1, float z1, float x2, float y2, float z2, int damage, BOOL p7, Hash weaponHash, Ped ownerPed, BOOL isAudible, BOOL isInvisible, float speed, Entity entity, BOOL p14, BOOL p15, Entity targetEntity, BOOL p17, Any p18, Any p19, Any p20)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHOOT_SINGLE_BULLET_BETWEEN_COORDS_IGNORE_ENTITY_NEW, x1, y1, z1, x2, y2, z2, damage, p7, weaponHash, ownerPed, isAudible, isInvisible, speed, entity, p14, p15, targetEntity, p17, p18, p19, p20);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `damage` | `int` | `int` | |
| `p7` | `BOOL` | `bool` | |
| `weaponHash` | `Hash` | `uint` | |
| `ownerPed` | `Ped` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `speed` | `float` | `float` | |
| `entity` | `Entity` | `int` | |
| `p14` | `BOOL` | `bool` | |
| `p15` | `BOOL` | `bool` | |
| `targetEntity` | `Entity` | `int` | |
| `p17` | `BOOL` | `bool` | |
| `p18` | `Any` | `object` | |
| `p19` | `Any` | `object` | |
| `p20` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** entity - entity to ignore
targetEntity - entity to home in on, if the weapon hash provided supports homing

---

### GET_MODEL_DIMENSIONS

**Hash:** `0x03E8D3D5F549087A` | **JHash:** `0x91ABB8E0` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MODEL_DIMENSIONS(Hash modelHash, Vector3* minimum, Vector3* maximum)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MODEL_DIMENSIONS, modelHash, minimum, maximum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `minimum` | `Vector3*` | `Vector3*` | |
| `maximum` | `Vector3*` | `Vector3*` | |

**返回值:** `void` → `void`

**说明:** Gets the dimensions of a model.

Calculate (maximum - minimum) to get the size, in which case, Y will be how long the model is.

Example from the scripts: MISC::GET_MODEL_DIMENSIONS(ENTITY::GET_ENTITY_MODEL(PLAYER::PLAYER_PED_ID()), &v_1A, &v_17);

---

### SET_FAKE_WANTED_LEVEL

**Hash:** `0x1454F2448DE30163` | **JHash:** `0x85B1C9FA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FAKE_WANTED_LEVEL(int fakeWantedLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAKE_WANTED_LEVEL, fakeWantedLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fakeWantedLevel` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets a visually fake wanted level on the user interface. Used by Rockstar's scripts to "override" regular wanted levels and make custom ones while the real wanted level and multipliers are still in effect.

Max is 6, anything above this makes it just 6. Also the mini-map gets the red & blue flashing effect.

---

### GET_FAKE_WANTED_LEVEL

**Hash:** `0x4C9296CBCD1B971E` | **JHash:** `0x0022A430` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FAKE_WANTED_LEVEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FAKE_WANTED_LEVEL);
```

**返回值:** `int` → `int`

---

### USING_MISSION_CREATOR

**Hash:** `0xF14878FC50BEC6EE` | **JHash:** `0x20AB0B6B` | **Build:** `323`

**C++ 原型:**
```cpp
void USING_MISSION_CREATOR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USING_MISSION_CREATOR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ALLOW_MISSION_CREATOR_WARP

**Hash:** `0xDEA36202FC3382DF` | **JHash:** `0x082BA6F2` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_MISSION_CREATOR_WARP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_MISSION_CREATOR_WARP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MINIGAME_IN_PROGRESS

**Hash:** `0x19E00D7322C6F85B` | **JHash:** `0x348B9046` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MINIGAME_IN_PROGRESS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MINIGAME_IN_PROGRESS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_MINIGAME_IN_PROGRESS

**Hash:** `0x2B4A15E44DE0F478` | **JHash:** `0x53A95E13` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MINIGAME_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MINIGAME_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### IS_THIS_A_MINIGAME_SCRIPT

**Hash:** `0x7B30F65D7B710098` | **JHash:** `0x7605EF6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_A_MINIGAME_SCRIPT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_A_MINIGAME_SCRIPT);
```

**返回值:** `BOOL` → `bool`

---

### IS_SNIPER_INVERTED

**Hash:** `0x61A23B7EDA9BDA24` | **JHash:** `0x5C3BF51B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SNIPER_INVERTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SNIPER_INVERTED);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### SHOULD_USE_METRIC_MEASUREMENTS

**Hash:** `0xD3D15555431AB793` | **JHash:** `0xBAF17315` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SHOULD_USE_METRIC_MEASUREMENTS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SHOULD_USE_METRIC_MEASUREMENTS);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the game is using the metric measurement system (profile setting 227), false if imperial is used.

---

### GET_PROFILE_SETTING

**Hash:** `0xC488FF2356EA7791` | **JHash:** `0xD374BEBC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PROFILE_SETTING(int profileSetting)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PROFILE_SETTING, profileSetting);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `profileSetting` | `int` | `int` | |

**返回值:** `int` → `int`

---

### ARE_STRINGS_EQUAL

**Hash:** `0x0C515FAB3FF9EA92` | **JHash:** `0x877C0BC5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_STRINGS_EQUAL(const char* string1, const char* string2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_STRINGS_EQUAL, string1, string2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string1` | `const char*` | `string` | |
| `string2` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### COMPARE_STRINGS

**Hash:** `0x1E34710ECD4AB0EB` | **JHash:** `0xFE25A58F` | **Build:** `323`

**C++ 原型:**
```cpp
int COMPARE_STRINGS(const char* str1, const char* str2, BOOL matchCase, int maxLength)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.COMPARE_STRINGS, str1, str2, matchCase, maxLength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `str1` | `const char*` | `string` | |
| `str2` | `const char*` | `string` | |
| `matchCase` | `BOOL` | `bool` | |
| `maxLength` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Compares two strings up to a specified number of characters.

Parameters:
str1 - String to be compared.
str2 - String to be compared.
matchCase - Comparison will be case-sensitive.
maxLength - Maximum number of characters to compare. A value of -1 indicates an infinite length.

Returns:
A value indicating the relationship between the strings:
<0 - The first non-matching character in 'str1' is less than the one in 'str2'. (e.g. 'A' < 'B', so result = -1)
0 - The contents of both strings are equal.
>0 - The first non-matching character in 'str1' is less than the one in 'str2'. (e.g. 'B' > 'A', so result = 1)

Examples:
MISC::COMPARE_STRINGS("STRING", "string", false, -1); // 0; equal
MISC::COMPARE_STRINGS("TESTING", "test", false, 4); // 0; equal
MISC::COMPARE_STRINGS("R2D2", "R2xx", false, 2); // 0; equal
MISC::COMPARE_STRINGS("foo", "bar", false, -1); // 4; 'f' > 'b'
MISC::COMPARE_STRINGS("A", "A", true, 1); // 0; equal

When comparing case-sensitive strings, lower-case characters are greater than upper-case characters:
MISC::COMPARE_STRINGS("A", "a", true, 1); // -1; 'A' < 'a'
MISC::COMPARE_STRINGS("a", "A", true, 1); // 1; 'a' > 'A'

---

### ABSI

**Hash:** `0xF0D31AD191A74F87` | **JHash:** `0xB44677C5` | **Build:** `323`

**C++ 原型:**
```cpp
int ABSI(int value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ABSI, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `int` → `int`

---

### ABSF

**Hash:** `0x73D57CFFDD12C355` | **JHash:** `0xAF6F6E0B` | **Build:** `323`

**C++ 原型:**
```cpp
float ABSF(float value)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ABSF, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `float` → `float`

---

### IS_SNIPER_BULLET_IN_AREA

**Hash:** `0xFEFCF11B01287125` | **JHash:** `0x0483715C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SNIPER_BULLET_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SNIPER_BULLET_IN_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Determines whether there is a sniper bullet within the specified coordinates. The coordinates form an axis-aligned bounding box.

---

### IS_PROJECTILE_IN_AREA

**Hash:** `0x5270A8FBC098C3F8` | **JHash:** `0x78E1A557` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PROJECTILE_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROJECTILE_IN_AREA, x1, y1, z1, x2, y2, z2, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Determines whether there is a projectile within the specified coordinates. The coordinates form a rectangle.

- Nacorpio


ownedByPlayer = only projectiles fired by the player will be detected.

---

### IS_PROJECTILE_TYPE_IN_AREA

**Hash:** `0x2E0DC353342C4A6D` | **JHash:** `0x2B73BCF6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PROJECTILE_TYPE_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, int type, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROJECTILE_TYPE_IN_AREA, x1, y1, z1, x2, y2, z2, type, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `type` | `int` | `int` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Determines whether there is a projectile of a specific type within the specified coordinates. The coordinates form a axis-aligned bounding box.

---

### IS_PROJECTILE_TYPE_IN_ANGLED_AREA

**Hash:** `0xF0BC12401061DEA0` | **JHash:** `0xD1AE2681` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PROJECTILE_TYPE_IN_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width, Any p7, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROJECTILE_TYPE_IN_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width, p7, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `p7` | `Any` | `object` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.

---

### IS_PROJECTILE_TYPE_WITHIN_DISTANCE

**Hash:** `0x34318593248C8FB2` | **JHash:** `0xBE81F1E2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PROJECTILE_TYPE_WITHIN_DISTANCE(float x, float y, float z, Hash projectileHash, float radius, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROJECTILE_TYPE_WITHIN_DISTANCE, x, y, z, projectileHash, radius, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `projectileHash` | `Hash` | `uint` | |
| `radius` | `float` | `float` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_COORDS_OF_PROJECTILE_TYPE_IN_AREA

**Hash:** `0x8D7A43EC6A5FEA45` | **JHash:** `0x1A40454B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_COORDS_OF_PROJECTILE_TYPE_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, Hash projectileHash, Vector3* projectilePos, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_COORDS_OF_PROJECTILE_TYPE_IN_AREA, x1, y1, z1, x2, y2, z2, projectileHash, projectilePos, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `projectileHash` | `Hash` | `uint` | |
| `projectilePos` | `Vector3*` | `Vector3*` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_COORDS_OF_PROJECTILE_TYPE_IN_ANGLED_AREA

**Hash:** `0x3DA8C28346B62CED` | **Build:** `2802`

**C++ 原型:**
```cpp
BOOL GET_COORDS_OF_PROJECTILE_TYPE_IN_ANGLED_AREA(float vecAngledAreaPoint1X, float vecAngledAreaPoint1Y, float vecAngledAreaPoint1Z, float vecAngledAreaPoint2X, float vecAngledAreaPoint2Y, float vecAngledAreaPoint2Z, float distanceOfOppositeFace, Hash weaponType, Vector3* positionOut, BOOL bIsPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_COORDS_OF_PROJECTILE_TYPE_IN_ANGLED_AREA, vecAngledAreaPoint1X, vecAngledAreaPoint1Y, vecAngledAreaPoint1Z, vecAngledAreaPoint2X, vecAngledAreaPoint2Y, vecAngledAreaPoint2Z, distanceOfOppositeFace, weaponType, positionOut, bIsPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vecAngledAreaPoint1X` | `float` | `float` | |
| `vecAngledAreaPoint1Y` | `float` | `float` | |
| `vecAngledAreaPoint1Z` | `float` | `float` | |
| `vecAngledAreaPoint2X` | `float` | `float` | |
| `vecAngledAreaPoint2Y` | `float` | `float` | |
| `vecAngledAreaPoint2Z` | `float` | `float` | |
| `distanceOfOppositeFace` | `float` | `float` | |
| `weaponType` | `Hash` | `uint` | |
| `positionOut` | `Vector3*` | `Vector3*` | |
| `bIsPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_COORDS_OF_PROJECTILE_TYPE_WITHIN_DISTANCE

**Hash:** `0xDFB4138EEFED7B81` | **JHash:** `0x6BDE5CE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_COORDS_OF_PROJECTILE_TYPE_WITHIN_DISTANCE(Ped ped, Hash weaponHash, float distance, Vector3* outCoords, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_COORDS_OF_PROJECTILE_TYPE_WITHIN_DISTANCE, ped, weaponHash, distance, outCoords, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `distance` | `float` | `float` | |
| `outCoords` | `Vector3*` | `Vector3*` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_PROJECTILE_OF_PROJECTILE_TYPE_WITHIN_DISTANCE

**Hash:** `0x82FDE6A57EE4EE44` | **JHash:** `0x507BC6F7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PROJECTILE_OF_PROJECTILE_TYPE_WITHIN_DISTANCE(Ped ped, Hash weaponHash, float distance, Vector3* outCoords, Object* outProjectile, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PROJECTILE_OF_PROJECTILE_TYPE_WITHIN_DISTANCE, ped, weaponHash, distance, outCoords, outProjectile, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `distance` | `float` | `float` | |
| `outCoords` | `Vector3*` | `Vector3*` | |
| `outProjectile` | `Object*` | `int*` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_BULLET_IN_ANGLED_AREA

**Hash:** `0x1A8B5F3C01E2B477` | **JHash:** `0xE2DB58F7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BULLET_IN_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BULLET_IN_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** For projectiles, see: IS_PROJECTILE_TYPE_IN_ANGLED_AREA
See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.
Returns True if a bullet, as maintained by a pool within CWeaponManager, has been fired into the defined angled area.

---

### IS_BULLET_IN_AREA

**Hash:** `0x3F2023999AD51C1F` | **JHash:** `0xB54F46CA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BULLET_IN_AREA(float x, float y, float z, float radius, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BULLET_IN_AREA, x, y, z, radius, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_BULLET_IN_BOX

**Hash:** `0xDE0F6D7450D37351` | **JHash:** `0xAB73ED26` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BULLET_IN_BOX(float x1, float y1, float z1, float x2, float y2, float z2, BOOL ownedByPlayer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BULLET_IN_BOX, x1, y1, z1, x2, y2, z2, ownedByPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `ownedByPlayer` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### HAS_BULLET_IMPACTED_IN_AREA

**Hash:** `0x9870ACFB89A90995` | **JHash:** `0x902BC7D9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_BULLET_IMPACTED_IN_AREA(float x, float y, float z, float p3, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_BULLET_IMPACTED_IN_AREA, x, y, z, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p3 - possibly radius?

---

### HAS_BULLET_IMPACTED_IN_BOX

**Hash:** `0xDC8C5D7CFEAB8394` | **JHash:** `0x2C2618CC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_BULLET_IMPACTED_IN_BOX(float p0, float p1, float p2, float p3, float p4, float p5, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_BULLET_IMPACTED_IN_BOX, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_ORBIS_VERSION

**Hash:** `0xA72BC0B675B1519E` | **JHash:** `0x40282018` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ORBIS_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ORBIS_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** PS4

---

### IS_DURANGO_VERSION

**Hash:** `0x4D982ADB1978442D` | **JHash:** `0x46FB06A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DURANGO_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DURANGO_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** XBOX ONE

---

### IS_XBOX360_VERSION

**Hash:** `0xF6201B4DAF662A9D` | **JHash:** `0x24005CC8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_XBOX360_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_XBOX360_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_PS3_VERSION

**Hash:** `0xCCA1072C29D096C2` | **JHash:** `0x4C0D5303` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PS3_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PS3_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_PC_VERSION

**Hash:** `0x48AF36444B965238` | **JHash:** `0x4D5D9EE3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PC_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PC_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_STEAM_VERSION

**Hash:** `0x0A27B2B6282F7169` | **Build:** `2545`

**C++ 原型:**
```cpp
BOOL IS_STEAM_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STEAM_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### _IS_XBOXPC_VERSION

**Hash:** `0xE2BCD0EFAE90D1F4` | **Build:** `3504`

**C++ 原型:**
```cpp
BOOL _IS_XBOXPC_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._IS_XBOXPC_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_AUSSIE_VERSION

**Hash:** `0x9F1935CA1F724008` | **JHash:** `0x944BA1DC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_AUSSIE_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AUSSIE_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** Used to block some of the prostitute stuff due to laws in Australia.

---

### IS_JAPANESE_VERSION

**Hash:** `0xB8C0BB75D8A77DB3` | **Build:** `2545`

**C++ 原型:**
```cpp
BOOL IS_JAPANESE_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_JAPANESE_VERSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_XBOX_PLATFORM

**Hash:** `0x138679CA01E21F53` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL IS_XBOX_PLATFORM(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_XBOX_PLATFORM);
```

**返回值:** `BOOL` → `bool`

---

### IS_SCARLETT_VERSION

**Hash:** `0xC545AB1CF97ABB34` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL IS_SCARLETT_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCARLETT_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** Xbox Series (Scarlett) version...

---

### IS_SCE_PLATFORM

**Hash:** `0xF911E695C1EB8518` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL IS_SCE_PLATFORM(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCE_PLATFORM);
```

**返回值:** `BOOL` → `bool`

---

### IS_PROSPERO_VERSION

**Hash:** `0x807ABE1AB65C24D2` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL IS_PROSPERO_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROSPERO_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** PS5 (Prospero) version...

---

### IS_STRING_NULL

**Hash:** `0xF22B6C47C6EAB066` | **JHash:** `0x8E71E00F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STRING_NULL(const char* string)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STRING_NULL, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### IS_STRING_NULL_OR_EMPTY

**Hash:** `0xCA042B6957743895` | **JHash:** `0x42E9F2CA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STRING_NULL_OR_EMPTY(const char* string)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STRING_NULL_OR_EMPTY, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### STRING_TO_INT

**Hash:** `0x5A5F40FE637EB584` | **JHash:** `0x590A8160` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STRING_TO_INT(const char* string, int* outInteger)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STRING_TO_INT, string, outInteger);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |
| `outInteger` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns false if it's a null or empty string or if the string is too long. outInteger will be set to -999 in that case.

If all checks have passed successfully, the return value will be set to whatever strtol(string, 0i64, 10); returns.

---

### SET_BITS_IN_RANGE

**Hash:** `0x8EF07E15701D61ED` | **JHash:** `0x32094719` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BITS_IN_RANGE(int* var, int rangeStart, int rangeEnd, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BITS_IN_RANGE, var, rangeStart, rangeEnd, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `var` | `int*` | `int*` | |
| `rangeStart` | `int` | `int` | |
| `rangeEnd` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_BITS_IN_RANGE

**Hash:** `0x53158863FCC0893A` | **JHash:** `0xCA03A1E5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BITS_IN_RANGE(int var, int rangeStart, int rangeEnd)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BITS_IN_RANGE, var, rangeStart, rangeEnd);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `var` | `int` | `int` | |
| `rangeStart` | `int` | `int` | |
| `rangeEnd` | `int` | `int` | |

**返回值:** `int` → `int`

---

### ADD_STUNT_JUMP

**Hash:** `0x1A992DA297A4630C` | **JHash:** `0xB630E5FF` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_STUNT_JUMP(float x1, float y1, float z1, float x2, float y2, float z2, float x3, float y3, float z3, float x4, float y4, float z4, float camX, float camY, float camZ, int p15, int p16, int p17)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_STUNT_JUMP, x1, y1, z1, x2, y2, z2, x3, y3, z3, x4, y4, z4, camX, camY, camZ, p15, p16, p17);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `x4` | `float` | `float` | |
| `y4` | `float` | `float` | |
| `z4` | `float` | `float` | |
| `camX` | `float` | `float` | |
| `camY` | `float` | `float` | |
| `camZ` | `float` | `float` | |
| `p15` | `int` | `int` | |
| `p16` | `int` | `int` | |
| `p17` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** See description of `ADD_STUNT_JUMP_ANGLED` for detailed info. The only difference really is this one does not have the radius (or angle, not sure) floats parameters for entry and landing zones.

---

### ADD_STUNT_JUMP_ANGLED

**Hash:** `0xBBE5D803A5360CBF` | **JHash:** `0xB9B7E777` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_STUNT_JUMP_ANGLED(float x1, float y1, float z1, float x2, float y2, float z2, float radius1, float x3, float y3, float z3, float x4, float y4, float z4, float radius2, float camX, float camY, float camZ, int p17, int p18, int p19)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_STUNT_JUMP_ANGLED, x1, y1, z1, x2, y2, z2, radius1, x3, y3, z3, x4, y4, z4, radius2, camX, camY, camZ, p17, p18, p19);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `radius1` | `float` | `float` | |
| `x3` | `float` | `float` | |
| `y3` | `float` | `float` | |
| `z3` | `float` | `float` | |
| `x4` | `float` | `float` | |
| `y4` | `float` | `float` | |
| `z4` | `float` | `float` | |
| `radius2` | `float` | `float` | |
| `camX` | `float` | `float` | |
| `camY` | `float` | `float` | |
| `camZ` | `float` | `float` | |
| `p17` | `int` | `int` | |
| `p18` | `int` | `int` | |
| `p19` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Creates a new stunt jump.

The radius1 and radius2 might actually not be a radius at all, but that's what it seems to me testing them in-game. But they may be 'angle' floats instead, considering this native is named ADD_STUNT_JUMP_**ANGLED**.

Info about the specific 'parameter sections':


**x1, y1, z1, x2, y2, z2 and radius1:**

First coordinates are for the jump entry area, and the radius that will be checked around that area. So if you're not exactly within the coordinates, but you are within the outter radius limit then it will still register as entering the stunt jump. Note as mentioned above, the radius is just a guess, I'm not really sure about it's exact purpose.


**x3, y3, z3, x4, y4, z4 and radius2:**

Next part is the landing area, again starting with the left bottom (nearest to the stunt jump entry zone) coordinate, and the second one being the top right furthest away part of the landing area. Followed by another (most likely) radius float, this is usually slightly larger than the entry zone 'radius' float value, just because you have quite a lot of places where you can land (I'm guessing).


**camX, camY and camZ:**

The final coordinate in this native is the Camera position. Rotation and zoom/FOV is managed by the game itself, you just need to provide the camera location.


**unk1, unk2 and unk3:**

Not sure what these are for, but they're always `150, 0, 0` in decompiled scripts.

Here is a list of almost all of the stunt jumps from GTA V (taken from decompiled scripts): https://pastebin.com/EW1jBPkY

---

### TOGGLE_SHOW_OPTIONAL_STUNT_JUMP_CAMERA

**Hash:** `0xFB80AB299D2EE1BD` | **Build:** `757`

**C++ 原型:**
```cpp
void TOGGLE_SHOW_OPTIONAL_STUNT_JUMP_CAMERA(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_SHOW_OPTIONAL_STUNT_JUMP_CAMERA, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles some stunt jump stuff.

---

### DELETE_STUNT_JUMP

**Hash:** `0xDC518000E39DAE1F` | **JHash:** `0x840CB5DA` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_STUNT_JUMP(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_STUNT_JUMP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ENABLE_STUNT_JUMP_SET

**Hash:** `0xE369A5783B866016` | **JHash:** `0x9D1E7785` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_STUNT_JUMP_SET(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_STUNT_JUMP_SET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DISABLE_STUNT_JUMP_SET

**Hash:** `0xA5272EBEDD4747F6` | **JHash:** `0x644C9FA4` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_STUNT_JUMP_SET(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_STUNT_JUMP_SET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_STUNT_JUMPS_CAN_TRIGGER

**Hash:** `0xD79185689F8FD5DF` | **JHash:** `0x3C806A2D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STUNT_JUMPS_CAN_TRIGGER(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STUNT_JUMPS_CAN_TRIGGER, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_STUNT_JUMP_IN_PROGRESS

**Hash:** `0x7A3F19700A4D0525` | **JHash:** `0xF477D0B1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STUNT_JUMP_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STUNT_JUMP_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### IS_STUNT_JUMP_MESSAGE_SHOWING

**Hash:** `0x2272B0A1343129F4` | **JHash:** `0x021636EE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STUNT_JUMP_MESSAGE_SHOWING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STUNT_JUMP_MESSAGE_SHOWING);
```

**返回值:** `BOOL` → `bool`

---

### GET_NUM_SUCCESSFUL_STUNT_JUMPS

**Hash:** `0x996DD1E1E02F1008` | **JHash:** `0x006F9BA2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_SUCCESSFUL_STUNT_JUMPS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_SUCCESSFUL_STUNT_JUMPS);
```

**返回值:** `int` → `int`

---

### GET_TOTAL_SUCCESSFUL_STUNT_JUMPS

**Hash:** `0x6856EC3D35C81EA4` | **JHash:** `0x495BE324` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TOTAL_SUCCESSFUL_STUNT_JUMPS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TOTAL_SUCCESSFUL_STUNT_JUMPS);
```

**返回值:** `int` → `int`

---

### CANCEL_STUNT_JUMP

**Hash:** `0xE6B7B0ACD4E4B75E` | **JHash:** `0xF43D9821` | **Build:** `323`

**C++ 原型:**
```cpp
void CANCEL_STUNT_JUMP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CANCEL_STUNT_JUMP);
```

**返回值:** `void` → `void`

---

### SET_GAME_PAUSED

**Hash:** `0x577D1284D6873711` | **JHash:** `0x8230FF6C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAME_PAUSED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAME_PAUSED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Make sure to call this from the correct thread if you're using multiple threads because all other threads except the one which is calling SET_GAME_PAUSED will be paused which means you will lose control and the game remains in paused mode until you exit GTA5.exe

---

### SET_THIS_SCRIPT_CAN_BE_PAUSED

**Hash:** `0xAA391C728106F7AF` | **JHash:** `0xA0C3CE29` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIS_SCRIPT_CAN_BE_PAUSED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIS_SCRIPT_CAN_BE_PAUSED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_THIS_SCRIPT_CAN_REMOVE_BLIPS_CREATED_BY_ANY_SCRIPT

**Hash:** `0xB98236CAAECEF897` | **JHash:** `0xD06F1720` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_THIS_SCRIPT_CAN_REMOVE_BLIPS_CREATED_BY_ANY_SCRIPT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_THIS_SCRIPT_CAN_REMOVE_BLIPS_CREATED_BY_ANY_SCRIPT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HAS_CHEAT_WITH_HASH_BEEN_ACTIVATED

**Hash:** `0x071E2A839DE82D90` | **JHash:** `0xFF6191E1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CHEAT_WITH_HASH_BEEN_ACTIVATED(Hash hash, int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CHEAT_WITH_HASH_BEEN_ACTIVATED, hash, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This native appears on the cheat_controller script and tracks a combination of buttons, which may be used to toggle cheats in-game. Credits to ThreeSocks for the info. The hash contains the combination, while the "amount" represents the amount of buttons used in a combination. The following page can be used to make a button combination: gta5offset.com/ts/hash/

INT_SCORES_SCORTED was a hash collision

---

### HAS_PC_CHEAT_WITH_HASH_BEEN_ACTIVATED

**Hash:** `0x557E43C447E700A8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PC_CHEAT_WITH_HASH_BEEN_ACTIVATED(Hash hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PC_CHEAT_WITH_HASH_BEEN_ACTIVATED, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Get inputted "Cheat code", for example:

while (TRUE)
{
    if (MISC::HAS_PC_CHEAT_WITH_HASH_BEEN_ACTIVATED(${fugitive}))
    {
       // Do something.
    }
    SYSTEM::WAIT(0);
}

Calling this will also set the last saved string hash to zero.


---

### OVERRIDE_FREEZE_FLAGS

**Hash:** `0xFA3FFB0EEBC288A3` | **Build:** `2060`

**C++ 原型:**
```cpp
void OVERRIDE_FREEZE_FLAGS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_FREEZE_FLAGS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INSTANCE_PRIORITY_MODE

**Hash:** `0x9BAE5AD2508DF078` | **JHash:** `0x721B2492` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INSTANCE_PRIORITY_MODE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INSTANCE_PRIORITY_MODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Formerly known as _LOWER_MAP_PROP_DENSITY and wrongly due to idiots as _ENABLE_MP_DLC_MAPS.
Sets the maximum prop density and changes a loading screen flag from 'loading story mode' to 'loading GTA Online'. Does not touch DLC map data at all.

In fact, I doubt this changes the flag whatsoever, that's the OTHER native idiots use together with this that does so, this one only causes a loading screen to show as it reloads map data.

---

### SET_INSTANCE_PRIORITY_HINT

**Hash:** `0xC5F0A8EBD3F361CE` | **JHash:** `0xE202879D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INSTANCE_PRIORITY_HINT(int flag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INSTANCE_PRIORITY_HINT, flag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `flag` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets an unknown flag used by CScene in determining which entities from CMapData scene nodes to draw, similar to SET_INSTANCE_PRIORITY_MODE.

---

### IS_FRONTEND_FADING

**Hash:** `0x7EA2B6AF97ECA6ED` | **JHash:** `0x8FF6232C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FRONTEND_FADING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FRONTEND_FADING);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### POPULATE_NOW

**Hash:** `0x7472BB270D7B4F3E` | **JHash:** `0x72C20700` | **Build:** `323`

**C++ 原型:**
```cpp
void POPULATE_NOW(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POPULATE_NOW);
```

**返回值:** `void` → `void`

**说明:** spawns a few distant/out-of-sight peds, vehicles, animals etc each time it is called

---

### GET_INDEX_OF_CURRENT_LEVEL

**Hash:** `0xCBAD6729F7B1F4FC` | **JHash:** `0x6F203C6E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_INDEX_OF_CURRENT_LEVEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_INDEX_OF_CURRENT_LEVEL);
```

**返回值:** `int` → `int`

---

### SET_GRAVITY_LEVEL

**Hash:** `0x740E14FAD5842351` | **JHash:** `0x2D833F4A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GRAVITY_LEVEL(int level)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GRAVITY_LEVEL, level);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `level` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** level can be from 0 to 3
0: 9.8 - normal
1: 2.4 - low
2: 0.1 - very low
3: 0.0 - off

---

### START_SAVE_DATA

**Hash:** `0xA9575F812C6A7997` | **JHash:** `0x881A694D` | **Build:** `323`

**C++ 原型:**
```cpp
void START_SAVE_DATA(Any* p0, Any p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_SAVE_DATA, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STOP_SAVE_DATA

**Hash:** `0x74E20C9145FB66FD` | **JHash:** `0x3B1C07C8` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SAVE_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SAVE_DATA);
```

**返回值:** `void` → `void`

---

### GET_SIZE_OF_SAVE_DATA

**Hash:** `0xA09F896CE912481F` | **JHash:** `0x9EF0BC64` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SIZE_OF_SAVE_DATA(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SIZE_OF_SAVE_DATA, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### REGISTER_INT_TO_SAVE

**Hash:** `0x34C9EE5986258415` | **JHash:** `0xB930956F` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_INT_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_INT_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_INT64_TO_SAVE

**Hash:** `0xA735353C77334EA0` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_INT64_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_INT64_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_ENUM_TO_SAVE

**Hash:** `0x10C2FA78D0E128A1` | **JHash:** `0x9B38374A` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_ENUM_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_ENUM_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_FLOAT_TO_SAVE

**Hash:** `0x7CAEC29ECB5DFEBB` | **JHash:** `0xDB06F7AD` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_FLOAT_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_FLOAT_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_BOOL_TO_SAVE

**Hash:** `0xC8F4131414C835A1` | **JHash:** `0x5417E0E0` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_BOOL_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_BOOL_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_TEXT_LABEL_TO_SAVE

**Hash:** `0xEDB1232C5BEAE62F` | **JHash:** `0x284352C4` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TEXT_LABEL_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TEXT_LABEL_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REGISTER_TEXT_LABEL_15_TO_SAVE

**Hash:** `0x6F7794F28C6B2535` | **JHash:** `0xE2089749` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TEXT_LABEL_15_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TEXT_LABEL_15_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** MISC::REGISTER_TEXT_LABEL_15_TO_SAVE(&a_0._f1, "tlPlateText");
MISC::REGISTER_TEXT_LABEL_15_TO_SAVE(&a_0._f1C, "tlPlateText_pending");
MISC::REGISTER_TEXT_LABEL_15_TO_SAVE(&a_0._f10B, "tlCarAppPlateText");

---

### REGISTER_TEXT_LABEL_23_TO_SAVE

**Hash:** `0x48F069265A0E4BEC` | **JHash:** `0xF91B8C33` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TEXT_LABEL_23_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TEXT_LABEL_23_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Only found 3 times in decompiled scripts.

MISC::REGISTER_TEXT_LABEL_23_TO_SAVE(a_0, "Movie_Name_For_This_Player");
MISC::REGISTER_TEXT_LABEL_23_TO_SAVE(&a_0._fB, "Ringtone_For_This_Player");
MISC::REGISTER_TEXT_LABEL_23_TO_SAVE(&a_0._f1EC4._f12[v_A/*6*/], &v_13); // where v_13 is "MPATMLOGSCRS0" thru "MPATMLOGSCRS15"

---

### REGISTER_TEXT_LABEL_31_TO_SAVE

**Hash:** `0x8269816F6CFD40F8` | **JHash:** `0x74E8FAD9` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TEXT_LABEL_31_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TEXT_LABEL_31_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Only found 2 times in decompiled scripts.

MISC::REGISTER_TEXT_LABEL_31_TO_SAVE(&a_0._f1F5A._f6[0/*8*/], "TEMPSTAT_LABEL"); // gets saved in a struct called "g_SaveData_STRING_ScriptSaves"
MISC::REGISTER_TEXT_LABEL_31_TO_SAVE(&a_0._f4B4[v_1A/*8*/], &v_5); // where v_5 is "Name0" thru "Name9", gets saved in a struct called "OUTFIT_Name"

---

### REGISTER_TEXT_LABEL_63_TO_SAVE

**Hash:** `0xFAA457EF263E8763` | **JHash:** `0x6B4335DD` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TEXT_LABEL_63_TO_SAVE(Any* p0, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TEXT_LABEL_63_TO_SAVE, p0, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(a_0, "Thumb_label");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f10, "Photo_label");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(a_0, "GXTlabel");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f21, "StringComp");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f43, "SecondStringComp");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f53, "ThirdStringComp");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f32, "SenderStringComp");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f726[v_1A/*16*/], &v_20); // where v_20 is "LastJobTL_0_1" thru "LastJobTL_2_1", gets saved in a struct called "LAST_JobGamer_TL"
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f4B, "PAID_PLAYER");
MISC::REGISTER_TEXT_LABEL_63_TO_SAVE(&a_0._f5B, "RADIO_STATION");

---

### START_SAVE_STRUCT_WITH_SIZE

**Hash:** `0xBF737600CDDBEADD` | **JHash:** `0xFB45728E` | **Build:** `323`

**C++ 原型:**
```cpp
void START_SAVE_STRUCT_WITH_SIZE(Any* p0, int size, const char* structName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_SAVE_STRUCT_WITH_SIZE, p0, size, structName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `size` | `int` | `int` | |
| `structName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### STOP_SAVE_STRUCT

**Hash:** `0xEB1774DF12BB9F12` | **JHash:** `0xC2624A28` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SAVE_STRUCT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SAVE_STRUCT);
```

**返回值:** `void` → `void`

---

### START_SAVE_ARRAY_WITH_SIZE

**Hash:** `0x60FE567DF1B1AF9D` | **JHash:** `0x893A342C` | **Build:** `323`

**C++ 原型:**
```cpp
void START_SAVE_ARRAY_WITH_SIZE(Any* p0, int size, const char* arrayName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_SAVE_ARRAY_WITH_SIZE, p0, size, arrayName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `size` | `int` | `int` | |
| `arrayName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### STOP_SAVE_ARRAY

**Hash:** `0x04456F95153C6BE4` | **JHash:** `0x0CAD8217` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_SAVE_ARRAY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_SAVE_ARRAY);
```

**返回值:** `void` → `void`

---

### COPY_SCRIPT_STRUCT

**Hash:** `0x213AEB2B90CBA7AC` | **Build:** `877`

**C++ 原型:**
```cpp
void COPY_SCRIPT_STRUCT(Any* dst, Any* src, int size)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.COPY_SCRIPT_STRUCT, dst, src, size);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dst` | `Any*` | `object*` | |
| `src` | `Any*` | `object*` | |
| `size` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ENABLE_DISPATCH_SERVICE

**Hash:** `0xDC0F817884CDD856` | **JHash:** `0x0B710A51` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_DISPATCH_SERVICE(int dispatchService, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_DISPATCH_SERVICE, dispatchService, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dispatchService` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** https://alloc8or.re/gta5/doc/enums/DispatchType.txt

---

### BLOCK_DISPATCH_SERVICE_RESOURCE_CREATION

**Hash:** `0x9B2BD3773123EA2F` | **JHash:** `0xE0F0684F` | **Build:** `323`

**C++ 原型:**
```cpp
void BLOCK_DISPATCH_SERVICE_RESOURCE_CREATION(int dispatchService, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_DISPATCH_SERVICE_RESOURCE_CREATION, dispatchService, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dispatchService` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_NUMBER_RESOURCES_ALLOCATED_TO_WANTED_LEVEL

**Hash:** `0xEB4A0C2D56441717` | **JHash:** `0x3CE5BF6B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_RESOURCES_ALLOCATED_TO_WANTED_LEVEL(int dispatchService)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_RESOURCES_ALLOCATED_TO_WANTED_LEVEL, dispatchService);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dispatchService` | `int` | `int` | |

**返回值:** `int` → `int`

---

### CREATE_INCIDENT

**Hash:** `0x3F892CAF67444AE7` | **JHash:** `0xFC5FF7B3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CREATE_INCIDENT(int dispatchService, float x, float y, float z, int numUnits, float radius, int* outIncidentID, Any p7, Any p8)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CREATE_INCIDENT, dispatchService, x, y, z, numUnits, radius, outIncidentID, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dispatchService` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `numUnits` | `int` | `int` | |
| `radius` | `float` | `float` | |
| `outIncidentID` | `int*` | `int*` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** As for the 'police' incident, it will call police cars to you, but unlike PedsInCavalcades & Merryweather they won't start shooting at you unless you shoot first or shoot at them. The top 2 however seem to cancel theirselves if there is noone dead around you or a fire. I only figured them out as I found out the 3rd param is definately the amountOfPeople and they called incident 3 in scripts with 4 people (which the firetruck has) and incident 5 with 2 people (which the ambulence has). The 4 param I cant say is radius, but for the pedsInCavalcades and Merryweather R* uses 0.0f and for the top 3 (Emergency Services) they use 3.0f. 

Side Note: It seems calling the pedsInCavalcades or Merryweather then removing it seems to break you from calling the EmergencyEvents and I also believe pedsInCavalcades. (The V cavalcades of course not IV).

Side Note 2: I say it breaks as if you call this proper,
if(CREATE_INCIDENT) etc it will return false if you do as I said above.
=====================================================

---

### CREATE_INCIDENT_WITH_ENTITY

**Hash:** `0x05983472F0494E60` | **JHash:** `0xBBC35B03` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CREATE_INCIDENT_WITH_ENTITY(int dispatchService, Ped ped, int numUnits, float radius, int* outIncidentID, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CREATE_INCIDENT_WITH_ENTITY, dispatchService, ped, numUnits, radius, outIncidentID, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dispatchService` | `int` | `int` | |
| `ped` | `Ped` | `int` | |
| `numUnits` | `int` | `int` | |
| `radius` | `float` | `float` | |
| `outIncidentID` | `int*` | `int*` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** As for the 'police' incident, it will call police cars to you, but unlike PedsInCavalcades & Merryweather they won't start shooting at you unless you shoot first or shoot at them. The top 2 however seem to cancel theirselves if there is noone dead around you or a fire. I only figured them out as I found out the 3rd param is definately the amountOfPeople and they called incident 3 in scripts with 4 people (which the firetruck has) and incident 5 with 2 people (which the ambulence has). The 4 param I cant say is radius, but for the pedsInCavalcades and Merryweather R* uses 0.0f and for the top 3 (Emergency Services) they use 3.0f. 

Side Note: It seems calling the pedsInCavalcades or Merryweather then removing it seems to break you from calling the EmergencyEvents and I also believe pedsInCavalcades. (The V cavalcades of course not IV).

Side Note 2: I say it breaks as if you call this proper,
if(CREATE_INCIDENT) etc it will return false if you do as I said above.
=====================================================

---

### DELETE_INCIDENT

**Hash:** `0x556C1AA270D5A207` | **JHash:** `0x212BD0DC` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_INCIDENT(int incidentId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_INCIDENT, incidentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `incidentId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Delete an incident with a given id.

=======================================================
Correction, I have change this to int, instead of int*
as it doesn't use a pointer to the createdIncident.
If you try it you will crash (or) freeze.
=======================================================

---

### IS_INCIDENT_VALID

**Hash:** `0xC8BC6461E629BEAA` | **JHash:** `0x31FD0BA4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_INCIDENT_VALID(int incidentId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_INCIDENT_VALID, incidentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `incidentId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** =======================================================
Correction, I have change this to int, instead of int*
as it doesn't use a pointer to the createdIncident.
If you try it you will crash (or) freeze.
=======================================================

---

### SET_INCIDENT_REQUESTED_UNITS

**Hash:** `0xB08B85D860E7BA3C` | **JHash:** `0x0242D88E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INCIDENT_REQUESTED_UNITS(int incidentId, int dispatchService, int numUnits)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INCIDENT_REQUESTED_UNITS, incidentId, dispatchService, numUnits);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `incidentId` | `int` | `int` | |
| `dispatchService` | `int` | `int` | |
| `numUnits` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_IDEAL_SPAWN_DISTANCE_FOR_INCIDENT

**Hash:** `0xD261BA3E7E998072` | **JHash:** `0x1F38102E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IDEAL_SPAWN_DISTANCE_FOR_INCIDENT(int incidentId, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IDEAL_SPAWN_DISTANCE_FOR_INCIDENT, incidentId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `incidentId` | `int` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FIND_SPAWN_POINT_IN_DIRECTION

**Hash:** `0x6874E2190B0C1972` | **JHash:** `0x71AEFD77` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FIND_SPAWN_POINT_IN_DIRECTION(float posX, float posY, float posZ, float fwdVecX, float fwdVecY, float fwdVecZ, float distance, Vector3* spawnPoint)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FIND_SPAWN_POINT_IN_DIRECTION, posX, posY, posZ, fwdVecX, fwdVecY, fwdVecZ, distance, spawnPoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `fwdVecX` | `float` | `float` | |
| `fwdVecY` | `float` | `float` | |
| `fwdVecZ` | `float` | `float` | |
| `distance` | `float` | `float` | |
| `spawnPoint` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

**说明:** Finds a position ahead of the player by predicting the players next actions.
The positions match path finding node positions.
When roads diverge, the position may rapidly change between two or more positions. This is due to the engine not being certain of which path the player will take.

---

### ADD_POP_MULTIPLIER_AREA

**Hash:** `0x67F6413D3220E18D` | **JHash:** `0x42BF09B3` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_POP_MULTIPLIER_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float p6, float p7, BOOL p8, BOOL p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_POP_MULTIPLIER_AREA, x1, y1, z1, x2, y2, z2, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### DOES_POP_MULTIPLIER_AREA_EXIST

**Hash:** `0x1327E2FE9746BAEE` | **JHash:** `0xE933B34A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_POP_MULTIPLIER_AREA_EXIST(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_POP_MULTIPLIER_AREA_EXIST, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_POP_MULTIPLIER_AREA

**Hash:** `0xB129E447A2EDA4BF` | **JHash:** `0xFBDBE374` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_POP_MULTIPLIER_AREA(int id, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_POP_MULTIPLIER_AREA, id, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_POP_MULTIPLIER_AREA_NETWORKED

**Hash:** `0x1312F4B242609CE3` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL IS_POP_MULTIPLIER_AREA_NETWORKED(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_POP_MULTIPLIER_AREA_NETWORKED, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ADD_POP_MULTIPLIER_SPHERE

**Hash:** `0x32C7A7E8C43A1F80` | **JHash:** `0xCE4A3D20` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_POP_MULTIPLIER_SPHERE(float x, float y, float z, float radius, float pedMultiplier, float vehicleMultiplier, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_POP_MULTIPLIER_SPHERE, x, y, z, radius, pedMultiplier, vehicleMultiplier, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `pedMultiplier` | `float` | `float` | |
| `vehicleMultiplier` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** This native is adding a zone, where you can change density settings. For example, you can add a zone on 0.0, 0.0, 0.0 with radius 900.0 and vehicleMultiplier 0.0, and you will not see any new population vehicle spawned in a radius of 900.0 from 0.0, 0.0, 0.0. Returns the id. You can have only 15 zones at the same time. You can remove zone using REMOVE_POP_MULTIPLIER_SPHERE

---

### DOES_POP_MULTIPLIER_SPHERE_EXIST

**Hash:** `0x171BAFB3C60389F4` | **JHash:** `0xD2B7A5A0` | **Build:** `791`

**C++ 原型:**
```cpp
BOOL DOES_POP_MULTIPLIER_SPHERE_EXIST(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_POP_MULTIPLIER_SPHERE_EXIST, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_POP_MULTIPLIER_SPHERE

**Hash:** `0xE6869BECDD8F2403` | **JHash:** `0x92E7E4EB` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_POP_MULTIPLIER_SPHERE(int id, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_POP_MULTIPLIER_SPHERE, id, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Removes population multiplier sphere

---

### ENABLE_TENNIS_MODE

**Hash:** `0x28A04B411933F8A6` | **JHash:** `0x0BD3F9EC` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_TENNIS_MODE(Ped ped, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_TENNIS_MODE, ped, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the ped jump around like they're in a tennis match

---

### IS_TENNIS_MODE

**Hash:** `0x5D5479D115290C3F` | **JHash:** `0x04A947BA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TENNIS_MODE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TENNIS_MODE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLAY_TENNIS_SWING_ANIM

**Hash:** `0xE266ED23311F24D4` | **JHash:** `0xC20A7D2B` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_TENNIS_SWING_ANIM(Ped ped, const char* animDict, const char* animName, float p3, float p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_TENNIS_SWING_ANIM, ped, animDict, animName, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_TENNIS_SWING_ANIM_COMPLETE

**Hash:** `0x17DF68D720AA77F8` | **JHash:** `0x8501E727` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TENNIS_SWING_ANIM_COMPLETE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TENNIS_SWING_ANIM_COMPLETE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_TENNIS_SWING_ANIM_CAN_BE_INTERRUPTED

**Hash:** `0x19BFED045C647C49` | **JHash:** `0x1A332D2D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TENNIS_SWING_ANIM_CAN_BE_INTERRUPTED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TENNIS_SWING_ANIM_CAN_BE_INTERRUPTED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_TENNIS_SWING_ANIM_SWUNG

**Hash:** `0xE95B0C7D5BA3B96B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TENNIS_SWING_ANIM_SWUNG(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TENNIS_SWING_ANIM_SWUNG, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLAY_TENNIS_DIVE_ANIM

**Hash:** `0x8FA9C42FC5D7C64B` | **JHash:** `0x0C8865DF` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_TENNIS_DIVE_ANIM(Ped ped, int p1, float p2, float p3, float p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_TENNIS_DIVE_ANIM, ped, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TENNIS_MOVE_NETWORK_SIGNAL_FLOAT

**Hash:** `0x54F157E0336A3822` | **JHash:** `0x49F977A9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TENNIS_MOVE_NETWORK_SIGNAL_FLOAT(Ped ped, const char* p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TENNIS_MOVE_NETWORK_SIGNAL_FLOAT, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** From the scripts:

MISC::SET_TENNIS_MOVE_NETWORK_SIGNAL_FLOAT(sub_aa49(a_0), "ForcedStopDirection", v_E);

Related to tennis mode.

---

### RESET_DISPATCH_SPAWN_LOCATION

**Hash:** `0x5896F2BD5683A4E1` | **Build:** `1868`

**C++ 原型:**
```cpp
void RESET_DISPATCH_SPAWN_LOCATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DISPATCH_SPAWN_LOCATION);
```

**返回值:** `void` → `void`

---

### SET_DISPATCH_SPAWN_LOCATION

**Hash:** `0xD10F442036302D50` | **JHash:** `0x6F009E33` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISPATCH_SPAWN_LOCATION(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISPATCH_SPAWN_LOCATION, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_DISPATCH_IDEAL_SPAWN_DISTANCE

**Hash:** `0x77A84429DD9F0A15` | **JHash:** `0xDA65ECAA` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_DISPATCH_IDEAL_SPAWN_DISTANCE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DISPATCH_IDEAL_SPAWN_DISTANCE);
```

**返回值:** `void` → `void`

---

### SET_DISPATCH_IDEAL_SPAWN_DISTANCE

**Hash:** `0x6FE601A64180D423` | **JHash:** `0x6283BE32` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISPATCH_IDEAL_SPAWN_DISTANCE(float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISPATCH_IDEAL_SPAWN_DISTANCE, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS

**Hash:** `0xEB2DB0CAD13154B3` | **JHash:** `0x1479E85A` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS

**Hash:** `0x44F7CBC1BEB3327D` | **JHash:** `0xABADB709` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS(Any p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS_MULTIPLIER

**Hash:** `0x48838ED9937A15D1` | **JHash:** `0x1C996BCD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS_MULTIPLIER(Any p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISPATCH_TIME_BETWEEN_SPAWN_ATTEMPTS_MULTIPLIER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ADD_DISPATCH_SPAWN_ANGLED_BLOCKING_AREA

**Hash:** `0x918C7B2D2FF3928B` | **JHash:** `0xF557BAF9` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_DISPATCH_SPAWN_ANGLED_BLOCKING_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_DISPATCH_SPAWN_ANGLED_BLOCKING_AREA, x1, y1, z1, x2, y2, z2, width);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** To remove, see: REMOVE_DISPATCH_SPAWN_BLOCKING_AREA
See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.

---

### ADD_DISPATCH_SPAWN_SPHERE_BLOCKING_AREA

**Hash:** `0x2D4259F1FEB81DA9` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_DISPATCH_SPAWN_SPHERE_BLOCKING_AREA(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_DISPATCH_SPAWN_SPHERE_BLOCKING_AREA, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `int` → `int`

---

### REMOVE_DISPATCH_SPAWN_BLOCKING_AREA

**Hash:** `0x264AC28B01B353A5` | **JHash:** `0xA8D2FB92` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DISPATCH_SPAWN_BLOCKING_AREA(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DISPATCH_SPAWN_BLOCKING_AREA, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESET_DISPATCH_SPAWN_BLOCKING_AREAS

**Hash:** `0xAC7BFD5C1D83EA75` | **JHash:** `0x9A17F835` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_DISPATCH_SPAWN_BLOCKING_AREAS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DISPATCH_SPAWN_BLOCKING_AREAS);
```

**返回值:** `void` → `void`

---

### RESET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN

**Hash:** `0xD9F692D349249528` | **JHash:** `0xE0C9307E` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN);
```

**返回值:** `void` → `void`

---

### SET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN

**Hash:** `0xE532EC1A63231B4F` | **JHash:** `0xA0D8C749` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN(int p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WANTED_RESPONSE_NUM_PEDS_TO_SPAWN, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ADD_TACTICAL_NAV_MESH_POINT

**Hash:** `0xB8721407EE9C3FF6` | **JHash:** `0x24A4E0B2` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_TACTICAL_NAV_MESH_POINT(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_TACTICAL_NAV_MESH_POINT, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_TACTICAL_NAV_MESH_POINTS

**Hash:** `0xB3CD58CCA6CDA852` | **JHash:** `0x66C3C59C` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_TACTICAL_NAV_MESH_POINTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_TACTICAL_NAV_MESH_POINTS);
```

**返回值:** `void` → `void`

---

### SET_RIOT_MODE_ENABLED

**Hash:** `0x2587A48BC88DFADF` | **JHash:** `0xD9660339` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RIOT_MODE_ENABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RIOT_MODE_ENABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Activates (usused?) riot mode. All NPCs are being hostile to each other (including player). Also the game will give weapons (pistols, smgs) to random NPCs.

---

### DISPLAY_ONSCREEN_KEYBOARD_WITH_LONGER_INITIAL_STRING

**Hash:** `0xCA78CFA0366592FE` | **JHash:** `0xD2688412` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_ONSCREEN_KEYBOARD_WITH_LONGER_INITIAL_STRING(int p0, const char* windowTitle, Any* p2, const char* defaultText, const char* defaultConcat1, const char* defaultConcat2, const char* defaultConcat3, const char* defaultConcat4, const char* defaultConcat5, const char* defaultConcat6, const char* defaultConcat7, int maxInputLength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_ONSCREEN_KEYBOARD_WITH_LONGER_INITIAL_STRING, p0, windowTitle, p2, defaultText, defaultConcat1, defaultConcat2, defaultConcat3, defaultConcat4, defaultConcat5, defaultConcat6, defaultConcat7, maxInputLength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `windowTitle` | `const char*` | `string` | |
| `p2` | `Any*` | `object*` | |
| `defaultText` | `const char*` | `string` | |
| `defaultConcat1` | `const char*` | `string` | |
| `defaultConcat2` | `const char*` | `string` | |
| `defaultConcat3` | `const char*` | `string` | |
| `defaultConcat4` | `const char*` | `string` | |
| `defaultConcat5` | `const char*` | `string` | |
| `defaultConcat6` | `const char*` | `string` | |
| `defaultConcat7` | `const char*` | `string` | |
| `maxInputLength` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DISPLAY_ONSCREEN_KEYBOARD

**Hash:** `0x00DC833F2568DBF6` | **JHash:** `0xAD99F2CE` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_ONSCREEN_KEYBOARD(int p0, const char* windowTitle, const char* p2, const char* defaultText, const char* defaultConcat1, const char* defaultConcat2, const char* defaultConcat3, int maxInputLength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_ONSCREEN_KEYBOARD, p0, windowTitle, p2, defaultText, defaultConcat1, defaultConcat2, defaultConcat3, maxInputLength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `windowTitle` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `defaultText` | `const char*` | `string` | |
| `defaultConcat1` | `const char*` | `string` | |
| `defaultConcat2` | `const char*` | `string` | |
| `defaultConcat3` | `const char*` | `string` | |
| `maxInputLength` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** sfink: note, p0 is set to 6 for PC platform in at least 1 script, or to `unk::_get_ui_language_id() == 0` otherwise.

NOTE: windowTitle uses text labels, and an invalid value will display nothing.

www.gtaforums.com/topic/788343-vrel-script-hook-v/?p=1067380474

windowTitle's
-----------------
CELL_EMAIL_BOD  =   "Enter your Eyefind message"
CELL_EMAIL_BODE =   "Message too long. Try again"
CELL_EMAIL_BODF    =   "Forbidden message. Try again"
CELL_EMAIL_SOD    =   "Enter your Eyefind subject"
CELL_EMAIL_SODE =   "Subject too long. Try again"
CELL_EMAIL_SODF    =   "Forbidden text. Try again"
CELL_EMASH_BOD   =   "Enter your Eyefind message"
CELL_EMASH_BODE =   "Message too long. Try again"
CELL_EMASH_BODF    =   "Forbidden message. Try again"
CELL_EMASH_SOD    =   "Enter your Eyefind subject"
CELL_EMASH_SODE =   "Subject too long. Try again"
CELL_EMASH_SODF    =   "Forbidden Text. Try again"
FMMC_KEY_TIP10   =   "Enter Synopsis"
FMMC_KEY_TIP12  =   "Enter Custom Team Name"
FMMC_KEY_TIP12F =   "Forbidden Text. Try again"
FMMC_KEY_TIP12N  =   "Custom Team Name"
FMMC_KEY_TIP8 =   "Enter Message"
FMMC_KEY_TIP8F   =   "Forbidden Text. Try again"
FMMC_KEY_TIP8FS  =   "Invalid Message. Try again"
FMMC_KEY_TIP8S  =   "Enter Message"
FMMC_KEY_TIP9    =   "Enter Outfit Name"
FMMC_KEY_TIP9F   =   "Invalid Outfit Name. Try again"
FMMC_KEY_TIP9N  =   "Outfit Name"
PM_NAME_CHALL  =   "Enter Challenge Name"

---

### UPDATE_ONSCREEN_KEYBOARD

**Hash:** `0x0CF2B696BBF945AE` | **JHash:** `0x23D0A1CE` | **Build:** `323`

**C++ 原型:**
```cpp
int UPDATE_ONSCREEN_KEYBOARD(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UPDATE_ONSCREEN_KEYBOARD);
```

**返回值:** `int` → `int`

**说明:** Returns the current status of the onscreen keyboard, and updates the output.

Status Codes:

-1: Keyboard isn't active
0: User still editing
1: User has finished editing
2: User has canceled editing

---

### GET_ONSCREEN_KEYBOARD_RESULT

**Hash:** `0x8362B09B91893647` | **JHash:** `0x44828FB3` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_ONSCREEN_KEYBOARD_RESULT(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_ONSCREEN_KEYBOARD_RESULT);
```

**返回值:** `const char*` → `string`

**说明:** Returns NULL unless UPDATE_ONSCREEN_KEYBOARD() returns 1 in the same tick.

---

### CANCEL_ONSCREEN_KEYBOARD

**Hash:** `0x58A39BE597CE99CD` | **Build:** `757`

**C++ 原型:**
```cpp
void CANCEL_ONSCREEN_KEYBOARD(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CANCEL_ONSCREEN_KEYBOARD);
```

**返回值:** `void` → `void`

**说明:** DO NOT use this as it doesn't clean up the text input box properly and your script will get stuck in the UPDATE_ONSCREEN_KEYBOARD() loop.
Use FORCE_CLOSE_TEXT_INPUT_BOX instead.

---

### NEXT_ONSCREEN_KEYBOARD_RESULT_WILL_DISPLAY_USING_THESE_FONTS

**Hash:** `0x3ED1438C1F5C6612` | **JHash:** `0x3301EA47` | **Build:** `323`

**C++ 原型:**
```cpp
void NEXT_ONSCREEN_KEYBOARD_RESULT_WILL_DISPLAY_USING_THESE_FONTS(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NEXT_ONSCREEN_KEYBOARD_RESULT_WILL_DISPLAY_USING_THESE_FONTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 was always 2 in R* scripts.
Called before calling DISPLAY_ONSCREEN_KEYBOARD if the input needs to be saved.

---

### ACTION_MANAGER_ENABLE_ACTION

**Hash:** `0xA6A12939F16D85BE` | **JHash:** `0x42B484ED` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTION_MANAGER_ENABLE_ACTION(Hash hash, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTION_MANAGER_ENABLE_ACTION, hash, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Appears to remove stealth kill action from memory

---

### GET_REAL_WORLD_TIME

**Hash:** `0x3F60413F5DF65748` | **Build:** `2612`

**C++ 原型:**
```cpp
int GET_REAL_WORLD_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_REAL_WORLD_TIME);
```

**返回值:** `int` → `int`

**说明:** GET_GAME_TIMER() / 1000

---

### SUPRESS_RANDOM_EVENT_THIS_FRAME

**Hash:** `0x1EAE0A6E978894A2` | **JHash:** `0x8F60366E` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPRESS_RANDOM_EVENT_THIS_FRAME(int eventType, BOOL suppress)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPRESS_RANDOM_EVENT_THIS_FRAME, eventType, suppress);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventType` | `int` | `int` | |
| `suppress` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_EXPLOSIVE_AMMO_THIS_FRAME

**Hash:** `0xA66C71C98D5F2CFB` | **JHash:** `0x2EAFA1D1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EXPLOSIVE_AMMO_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EXPLOSIVE_AMMO_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_FIRE_AMMO_THIS_FRAME

**Hash:** `0x11879CDD803D30F4` | **JHash:** `0x7C18FC8A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FIRE_AMMO_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FIRE_AMMO_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_EXPLOSIVE_MELEE_THIS_FRAME

**Hash:** `0xFF1BED81BFDC0FE0` | **JHash:** `0x96663D56` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EXPLOSIVE_MELEE_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EXPLOSIVE_MELEE_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_SUPER_JUMP_THIS_FRAME

**Hash:** `0x57FFF03E423A4C0B` | **JHash:** `0x86745EF3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SUPER_JUMP_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SUPER_JUMP_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_BEAST_JUMP_THIS_FRAME

**Hash:** `0x438822C279B73B93` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_BEAST_JUMP_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BEAST_JUMP_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_FORCED_JUMP_THIS_FRAME

**Hash:** `0xA1183BCFEE0F93D1` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_FORCED_JUMP_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCED_JUMP_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### HAS_GAME_INSTALLED_THIS_SESSION

**Hash:** `0x6FDDF453C0C756EC` | **JHash:** `0xC3C10FCC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_GAME_INSTALLED_THIS_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_GAME_INSTALLED_THIS_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### SET_TICKER_JOHNMARSTON_IS_DONE

**Hash:** `0xFB00CA71DA386228` | **JHash:** `0x054EC103` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TICKER_JOHNMARSTON_IS_DONE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TICKER_JOHNMARSTON_IS_DONE);
```

**返回值:** `void` → `void`

---

### ARE_PROFILE_SETTINGS_VALID

**Hash:** `0x5AA3BEFA29F03AD4` | **JHash:** `0x46B5A15C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_PROFILE_SETTINGS_VALID(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_PROFILE_SETTINGS_VALID);
```

**返回值:** `BOOL` → `bool`

---

### PREVENT_ARREST_STATE_THIS_FRAME

**Hash:** `0xE3D969D2785FFB5E` | **Build:** `323`

**C++ 原型:**
```cpp
void PREVENT_ARREST_STATE_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PREVENT_ARREST_STATE_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### FORCE_GAME_STATE_PLAYING

**Hash:** `0xC0AA53F866B3134D` | **JHash:** `0x5D209F25` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_GAME_STATE_PLAYING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_GAME_STATE_PLAYING);
```

**返回值:** `void` → `void`

**说明:** Sets the localplayer playerinfo state back to playing (State 0)

States are:
-1: "Invalid"
0: "Playing"
1: "Died"
2: "Arrested"
3: "Failed Mission"
4: "Left Game"
5: "Respawn"
6: "In MP Cutscene"

---

### SCRIPT_RACE_INIT

**Hash:** `0x0A60017F841A54F2` | **JHash:** `0x2D33F15A` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_RACE_INIT(int p0, int p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_RACE_INIT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SCRIPT_RACE_SHUTDOWN

**Hash:** `0x1FF6BF9A63E5757F` | **JHash:** `0xDF99925C` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_RACE_SHUTDOWN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_RACE_SHUTDOWN);
```

**返回值:** `void` → `void`

---

### SCRIPT_RACE_PLAYER_HIT_CHECKPOINT

**Hash:** `0x1BB299305C3E8C13` | **JHash:** `0xA27F4472` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_RACE_PLAYER_HIT_CHECKPOINT(Player player, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_RACE_PLAYER_HIT_CHECKPOINT, player, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SCRIPT_RACE_GET_PLAYER_SPLIT_TIME

**Hash:** `0x8EF5573A1F801A5C` | **JHash:** `0x07FF553F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SCRIPT_RACE_GET_PLAYER_SPLIT_TIME(Player player, int* p1, int* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SCRIPT_RACE_GET_PLAYER_SPLIT_TIME, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int*` | `int*` | |
| `p2` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### START_END_USER_BENCHMARK

**Hash:** `0x92790862E36C2ADA` | **Build:** `323`

**C++ 原型:**
```cpp
void START_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_END_USER_BENCHMARK);
```

**返回值:** `void` → `void`

---

### STOP_END_USER_BENCHMARK

**Hash:** `0xC7DB36C24634F52B` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_END_USER_BENCHMARK);
```

**返回值:** `void` → `void`

---

### RESET_END_USER_BENCHMARK

**Hash:** `0x437138B6A830166A` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_END_USER_BENCHMARK);
```

**返回值:** `void` → `void`

---

### SAVE_END_USER_BENCHMARK

**Hash:** `0x37DEB0AA183FB6D8` | **Build:** `323`

**C++ 原型:**
```cpp
void SAVE_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SAVE_END_USER_BENCHMARK);
```

**返回值:** `void` → `void`

**说明:** Saves the benchmark recording to %USERPROFILE%\Documents\Rockstar Games\GTA V\Benchmarks and submits some metrics.

---

### UI_STARTED_END_USER_BENCHMARK

**Hash:** `0xEA2F2061875EED90` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UI_STARTED_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UI_STARTED_END_USER_BENCHMARK);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the current frontend menu is FE_MENU_VERSION_SP_PAUSE

---

### LANDING_SCREEN_STARTED_END_USER_BENCHMARK

**Hash:** `0x3BBBD13E5041A79E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LANDING_SCREEN_STARTED_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LANDING_SCREEN_STARTED_END_USER_BENCHMARK);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the current frontend menu is FE_MENU_VERSION_LANDING_MENU

---

### IS_COMMANDLINE_END_USER_BENCHMARK

**Hash:** `0xA049A5BE0F04F2F8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COMMANDLINE_END_USER_BENCHMARK(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COMMANDLINE_END_USER_BENCHMARK);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if command line option '-benchmark' is set.

---

### GET_BENCHMARK_ITERATIONS

**Hash:** `0x4750FC27570311EC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BENCHMARK_ITERATIONS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BENCHMARK_ITERATIONS);
```

**返回值:** `int` → `int`

**说明:** Returns value of the '-benchmarkIterations' command line option.

---

### GET_BENCHMARK_PASS

**Hash:** `0x1B2366C3F2A5C8DF` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_BENCHMARK_PASS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_BENCHMARK_PASS);
```

**返回值:** `int` → `int`

**说明:** Returns value of the '-benchmarkPass' command line option.

---

### RESTART_GAME

**Hash:** `0xE574A662ACAEFBB1` | **Build:** `372`

**C++ 原型:**
```cpp
void RESTART_GAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESTART_GAME);
```

**返回值:** `void` → `void`

**说明:** In singleplayer it does exactly what the name implies. In FiveM / GTA:Online it shows `Disconnecting from GTA Online` HUD and then quits the game.

---

### QUIT_GAME

**Hash:** `0xEB6891F03362FB12` | **Build:** `323`

**C++ 原型:**
```cpp
void QUIT_GAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.QUIT_GAME);
```

**返回值:** `void` → `void`

**说明:** Exits the game and downloads a fresh social club update on next restart.

---

### HAS_ASYNC_INSTALL_FINISHED

**Hash:** `0x14832BF2ABA53FC5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ASYNC_INSTALL_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ASYNC_INSTALL_FINISHED);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to always return true.

---

### CLEANUP_ASYNC_INSTALL

**Hash:** `0xC79AE21974B01FB2` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEANUP_ASYNC_INSTALL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEANUP_ASYNC_INSTALL);
```

**返回值:** `void` → `void`

---

### PLM_IS_IN_CONSTRAINED_MODE

**Hash:** `0x684A41975F077262` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLM_IS_IN_CONSTRAINED_MODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLM_IS_IN_CONSTRAINED_MODE);
```

**返回值:** `BOOL` → `bool`

**说明:** aka "constrained"

---

### PLM_GET_CONSTRAINED_DURATION_MS

**Hash:** `0xABB2FA71C83A1B72` | **Build:** `323`

**C++ 原型:**
```cpp
int PLM_GET_CONSTRAINED_DURATION_MS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PLM_GET_CONSTRAINED_DURATION_MS);
```

**返回值:** `int` → `int`

**说明:** Returns duration of how long the game has been in power-saving mode (aka "constrained") in milliseconds.

---

### SET_PLAYER_IS_IN_ANIMAL_FORM

**Hash:** `0x4EBB7E87AA0DBED4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_IS_IN_ANIMAL_FORM(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_IS_IN_ANIMAL_FORM, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If toggle is true, the ped's head is shown in the pause menu
If toggle is false, the ped's head is not shown in the pause menu

---

### GET_IS_PLAYER_IN_ANIMAL_FORM

**Hash:** `0x9689123E3F213AA5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PLAYER_IN_ANIMAL_FORM(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PLAYER_IN_ANIMAL_FORM);
```

**返回值:** `BOOL` → `bool`

**说明:** Although we don't have a jenkins hash for this one, the name is 100% confirmed.

---

### SET_PLAYER_IS_REPEATING_A_MISSION

**Hash:** `0x9D8D44ADBBA61EF2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_IS_REPEATING_A_MISSION(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_IS_REPEATING_A_MISSION, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_SCREEN_DIMMING_THIS_FRAME

**Hash:** `0x23227DF0B2115469` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_SCREEN_DIMMING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_SCREEN_DIMMING_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### GET_CITY_DENSITY

**Hash:** `0xD10282B6E3751BA0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CITY_DENSITY(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CITY_DENSITY);
```

**返回值:** `float` → `float`

---

### USE_ACTIVE_CAMERA_FOR_TIMESLICING_CENTRE

**Hash:** `0x693478ACBD7F18E7` | **Build:** `1103`

**C++ 原型:**
```cpp
void USE_ACTIVE_CAMERA_FOR_TIMESLICING_CENTRE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_ACTIVE_CAMERA_FOR_TIMESLICING_CENTRE);
```

**返回值:** `void` → `void`

---

### SET_CONTENT_ID_INDEX

**Hash:** `0x4B82FA6F2D624634` | **Build:** `2612`

**C++ 原型:**
```cpp
void SET_CONTENT_ID_INDEX(Hash contentId, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONTENT_ID_INDEX, contentId, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `Hash` | `uint` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CONTENT_ID_INDEX

**Hash:** `0xECF041186C5A94DC` | **Build:** `2612`

**C++ 原型:**
```cpp
int GET_CONTENT_ID_INDEX(Hash contentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CONTENT_ID_INDEX, contentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### _SET_CONTENT_PROP_TYPE

**Hash:** `0xBA4583AF4C678A9B` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_CONTENT_PROP_TYPE(Hash model, int type)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_CONTENT_PROP_TYPE, model, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |
| `type` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _GET_CONTENT_PROP_TYPE

**Hash:** `0x8BAF8AD59F47AAFC` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_CONTENT_PROP_TYPE(Hash model)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_CONTENT_PROP_TYPE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns prop type for given model hash

---


---

## MOBILE

**包含 25 个 Native 函数**

### CREATE_MOBILE_PHONE

**Hash:** `0xA4E8E696C532FBC7` | **JHash:** `0x5BBC5E23` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MOBILE_PHONE(int phoneType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MOBILE_PHONE, phoneType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `phoneType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Creates a mobile phone of the specified type.

Possible phone types:

0 - Default phone / Michael's phone
1 - Trevor's phone
2 - Franklin's phone
3 - Unused police phone
4 - Prologue phone

Higher values may crash your game.

---

### DESTROY_MOBILE_PHONE

**Hash:** `0x3BC861DF703E5097` | **JHash:** `0x1A65037B` | **Build:** `323`

**C++ 原型:**
```cpp
void DESTROY_MOBILE_PHONE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DESTROY_MOBILE_PHONE);
```

**返回值:** `void` → `void`

**说明:** Destroys the currently active mobile phone.

---

### SET_MOBILE_PHONE_SCALE

**Hash:** `0xCBDD322A73D6D932` | **JHash:** `0x09BCF1BE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOBILE_PHONE_SCALE(float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_PHONE_SCALE, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The minimum/default is 500.0f. If you plan to make it bigger set it's position as well. Also this seems to need to be called in a loop as when you close the phone the scale is reset. If not in a loop you'd need to call it everytime before you re-open the phone.

---

### SET_MOBILE_PHONE_ROTATION

**Hash:** `0xBB779C0CA917E865` | **JHash:** `0x209C28CF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOBILE_PHONE_ROTATION(float rotX, float rotY, float rotZ, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_PHONE_ROTATION, rotX, rotY, rotZ, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Last parameter is unknown and always zero.

---

### GET_MOBILE_PHONE_ROTATION

**Hash:** `0x1CEFB61F193070AE` | **JHash:** `0x17A29F23` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MOBILE_PHONE_ROTATION(Vector3* rotation, Vehicle p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MOBILE_PHONE_ROTATION, rotation, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rotation` | `Vector3*` | `Vector3*` | |
| `p1` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_MOBILE_PHONE_POSITION

**Hash:** `0x693A5C6D6734085B` | **JHash:** `0x841800B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOBILE_PHONE_POSITION(float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_PHONE_POSITION, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_MOBILE_PHONE_POSITION

**Hash:** `0x584FDFDA48805B86` | **JHash:** `0xB2E1E1A0` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MOBILE_PHONE_POSITION(Vector3* position)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MOBILE_PHONE_POSITION, position);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `position` | `Vector3*` | `Vector3*` | |

**返回值:** `void` → `void`

---

### SCRIPT_IS_MOVING_MOBILE_PHONE_OFFSCREEN

**Hash:** `0xF511F759238A5122` | **JHash:** `0x29828690` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_IS_MOVING_MOBILE_PHONE_OFFSCREEN(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_IS_MOVING_MOBILE_PHONE_OFFSCREEN, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If bool Toggle = true so the mobile is hide to screen.
If bool Toggle = false so the mobile is show to screen.

---

### CAN_PHONE_BE_SEEN_ON_SCREEN

**Hash:** `0xC4E2813898C97A4B` | **JHash:** `0x5F978584` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PHONE_BE_SEEN_ON_SCREEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PHONE_BE_SEEN_ON_SCREEN);
```

**返回值:** `BOOL` → `bool`

**说明:** This one is weird and seems to return a TRUE state regardless of whether the phone is visible on screen or tucked away.


I can confirm the above. This function is hard-coded to always return 1.

---

### SET_MOBILE_PHONE_DOF_STATE

**Hash:** `0x375A706A5C2FD084` | **Build:** `372`

**C++ 原型:**
```cpp
void SET_MOBILE_PHONE_DOF_STATE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOBILE_PHONE_DOF_STATE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CELL_SET_INPUT

**Hash:** `0x95C9E72F3D7DEC9B` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_SET_INPUT(int direction)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_SET_INPUT, direction);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `direction` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** For move the finger of player, the value of int goes 1 at 5.

---

### CELL_HORIZONTAL_MODE_TOGGLE

**Hash:** `0x44E44169EF70138E` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_HORIZONTAL_MODE_TOGGLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_HORIZONTAL_MODE_TOGGLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** if the bool "Toggle" is "true" so the phone is lean.
if the bool "Toggle" is "false" so the phone is not lean.

---

### CELL_CAM_ACTIVATE

**Hash:** `0xFDE8F069C542D126` | **JHash:** `0x234C1AE9` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_ACTIVATE(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_ACTIVATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CELL_CAM_ACTIVATE_SELFIE_MODE

**Hash:** `0x015C49A93E3E086E` | **JHash:** `0x4479B304` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_ACTIVATE_SELFIE_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_ACTIVATE_SELFIE_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CELL_CAM_ACTIVATE_SHALLOW_DOF_MODE

**Hash:** `0xA2CCBE62CD4C91A4` | **JHash:** `0xC273BB4D` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_ACTIVATE_SHALLOW_DOF_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_ACTIVATE_SHALLOW_DOF_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_SIDE_OFFSET_SCALING

**Hash:** `0x1B0B4AEED5B9B41C` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_SIDE_OFFSET_SCALING(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_SIDE_OFFSET_SCALING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_HORZ_PAN_OFFSET

**Hash:** `0x53F4892D18EC90A4` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_HORZ_PAN_OFFSET(float horizontalPan)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_HORZ_PAN_OFFSET, horizontalPan);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `horizontalPan` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_VERT_PAN_OFFSET

**Hash:** `0x3117D84EFA60F77B` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_VERT_PAN_OFFSET(float vertPan)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_VERT_PAN_OFFSET, vertPan);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vertPan` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_ROLL_OFFSET

**Hash:** `0x15E69E2802C24B8D` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_ROLL_OFFSET(float roll)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_ROLL_OFFSET, roll);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `roll` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_DISTANCE_SCALING

**Hash:** `0xAC2890471901861C` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_DISTANCE_SCALING(float distanceScaling)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_DISTANCE_SCALING, distanceScaling);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distanceScaling` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_HEAD_YAW_OFFSET

**Hash:** `0xD6ADE981781FCA09` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_HEAD_YAW_OFFSET(float yaw)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_HEAD_YAW_OFFSET, yaw);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `yaw` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_HEAD_ROLL_OFFSET

**Hash:** `0xF1E22DC13F5EEBAD` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_HEAD_ROLL_OFFSET(float roll)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_HEAD_ROLL_OFFSET, roll);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `roll` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_SET_SELFIE_MODE_HEAD_PITCH_OFFSET

**Hash:** `0x466DA42C89865553` | **Build:** `323`

**C++ 原型:**
```cpp
void CELL_CAM_SET_SELFIE_MODE_HEAD_PITCH_OFFSET(float pitch)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CELL_CAM_SET_SELFIE_MODE_HEAD_PITCH_OFFSET, pitch);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pitch` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CELL_CAM_IS_CHAR_VISIBLE_NO_FACE_CHECK

**Hash:** `0x439E9BC95B7E7FBE` | **JHash:** `0xBEA88097` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CELL_CAM_IS_CHAR_VISIBLE_NO_FACE_CHECK(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CELL_CAM_IS_CHAR_VISIBLE_NO_FACE_CHECK, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_MOBILE_PHONE_RENDER_ID

**Hash:** `0xB4A53E05F68B6FA1` | **JHash:** `0x88E4FECE` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_MOBILE_PHONE_RENDER_ID(int* renderId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_MOBILE_PHONE_RENDER_ID, renderId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `renderId` | `int*` | `int*` | |

**返回值:** `void` → `void`

---


---

## MONEY

**包含 356 个 Native 函数**

### NETWORK_INITIALIZE_CASH

**Hash:** `0x3DA5ECD1A56CBA6D` | **JHash:** `0x66DA9935` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_INITIALIZE_CASH(int wallet, int bank)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_INITIALIZE_CASH, wallet, bank);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `wallet` | `int` | `int` | |
| `bank` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_DELETE_CHARACTER

**Hash:** `0x05A50AF38947EB8D` | **JHash:** `0xA9F7E9C3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DELETE_CHARACTER(int characterSlot, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DELETE_CHARACTER, characterSlot, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Note the 2nd parameters are always 1, 0. I have a feeling it deals with your money, wallet, bank. So when you delete the character it of course wipes the wallet cash at that time. So if that was the case, it would be eg, NETWORK_DELETE_CHARACTER(characterIndex, deleteWalletCash, deleteBankCash);

---

### NETWORK_MANUAL_DELETE_CHARACTER

**Hash:** `0x821418C727FCACD7` | **Build:** `791`

**C++ 原型:**
```cpp
void NETWORK_MANUAL_DELETE_CHARACTER(int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_MANUAL_DELETE_CHARACTER, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_PLAYER_IS_HIGH_EARNER

**Hash:** `0xFB2456B2040A6A67` | **JHash:** `0x63F093A6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PLAYER_IS_HIGH_EARNER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PLAYER_IS_HIGH_EARNER);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_CHARACTER_WALLET

**Hash:** `0xA921DED15FDF28F5` | **JHash:** `0x19F0C471` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_CHARACTER_WALLET(int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_CHARACTER_WALLET, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GIVE_PLAYER_JOBSHARE_CASH

**Hash:** `0xFB18DF9CB95E0105` | **JHash:** `0xC6047FDB` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GIVE_PLAYER_JOBSHARE_CASH(int amount, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GIVE_PLAYER_JOBSHARE_CASH, amount, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_RECEIVE_PLAYER_JOBSHARE_CASH

**Hash:** `0x56A3B51944C50598` | **JHash:** `0x4ED71C1A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_RECEIVE_PLAYER_JOBSHARE_CASH(int value, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_RECEIVE_PLAYER_JOBSHARE_CASH, value, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_CAN_SHARE_JOB_CASH

**Hash:** `0x1C2473301B1C66BA` | **JHash:** `0xA27B9FE8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SHARE_JOB_CASH(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SHARE_JOB_CASH);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_REFUND_CASH

**Hash:** `0xF9C812CD7C46E817` | **JHash:** `0x07C92F21` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REFUND_CASH(int index, const char* context, const char* reason, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REFUND_CASH, index, context, reason, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `context` | `const char*` | `string` | |
| `reason` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** index
-------
See function sub_1005 in am_boat_taxi.ysc

context
----------
"BACKUP_VAGOS"
"BACKUP_LOST"
"BACKUP_FAMILIES"
"HIRE_MUGGER"
"HIRE_MERCENARY"
"BUY_CARDROPOFF"
"HELI_PICKUP"
"BOAT_PICKUP"
"CLEAR_WANTED"
"HEAD_2_HEAD"
"CHALLENGE"
"SHARE_LAST_JOB"
"DEFAULT"

reason
---------
"NOTREACHTARGET"
"TARGET_ESCAPE"
"DELIVERY_FAIL"
"NOT_USED"
"TEAM_QUIT"
"SERVER_ERROR"
"RECEIVE_LJ_L"
"CHALLENGE_PLAYER_LEFT"
"DEFAULT"

unk
-----
Unknown bool value



---

### NETWORK_DEDUCT_CASH

**Hash:** `0x18B7AE224B087E26` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DEDUCT_CASH(int amount, const char* p1, const char* p2, BOOL p3, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DEDUCT_CASH, amount, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_MONEY_CAN_BET

**Hash:** `0x81404F3DC124FE5B` | **JHash:** `0x8474E6F0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_MONEY_CAN_BET(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_MONEY_CAN_BET, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_BET

**Hash:** `0x3A54E33660DED67F` | **JHash:** `0xE3802533` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_BET(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_BET, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CASINO_CAN_BET

**Hash:** `0x158C16F5E4CF41F8` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_CAN_BET(Hash hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_CAN_BET, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** GTAO_CASINO_HOUSE
GTAO_CASINO_INSIDETRACK
GTAO_CASINO_LUCKYWHEEL
GTAO_CASINO_BLACKJACK
GTAO_CASINO_ROULETTE
GTAO_CASINO_SLOTS
GTAO_CASINO_PURCHASE_CHIPS

NETWORK_C*

---

### NETWORK_CASINO_CAN_BET_PVC

**Hash:** `0x394DCDB9E836B7A9` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_CAN_BET_PVC(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_CAN_BET_PVC);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CASINO_CAN_BET_AMOUNT

**Hash:** `0xF62F6D9528358FE4` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_CAN_BET_AMOUNT(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_CAN_BET_AMOUNT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CASINO_CAN_BUY_CHIPS_PVC

**Hash:** `0x8968D4D8C6C40C11` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_CAN_BUY_CHIPS_PVC(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_CAN_BUY_CHIPS_PVC);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CASINO_BUY_CHIPS

**Hash:** `0x3BD101471C7F9EEC` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_BUY_CHIPS(int p0, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_BUY_CHIPS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CASINO_SELL_CHIPS

**Hash:** `0xED44897CB336F480` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_CASINO_SELL_CHIPS(int p0, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CASINO_SELL_CHIPS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DEFER_CASH_TRANSACTIONS_UNTIL_SHOP_SAVE

**Hash:** `0xCD0F5B5D932AE473` | **JHash:** `0xC4CD928D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DEFER_CASH_TRANSACTIONS_UNTIL_SHOP_SAVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DEFER_CASH_TRANSACTIONS_UNTIL_SHOP_SAVE);
```

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### CAN_PAY_AMOUNT_TO_BOSS

**Hash:** `0x9777734DAD16992F` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL CAN_PAY_AMOUNT_TO_BOSS(int p0, int p1, int amount, int* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PAY_AMOUNT_TO_BOSS, p0, p1, amount, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `amount` | `int` | `int` | |
| `p3` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_EARN_FROM_PICKUP

**Hash:** `0xED1517D3AF17C698` | **JHash:** `0x70A0ED62` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_PICKUP(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_PICKUP, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_CASHING_OUT

**Hash:** `0x718FBBF67414FA36` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CASHING_OUT(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CASHING_OUT, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_GANGATTACK_PICKUP

**Hash:** `0xA03D4ACE0A3284CE` | **JHash:** `0x33C20BC4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_GANGATTACK_PICKUP(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_GANGATTACK_PICKUP, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ASSASSINATE_TARGET_KILLED

**Hash:** `0xFA700D8A9905F78A` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ASSASSINATE_TARGET_KILLED(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ASSASSINATE_TARGET_KILLED, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_ROB_ARMORED_CARS

**Hash:** `0xF514621E8EA463D0` | **JHash:** `0x30B3EC0A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_ROB_ARMORED_CARS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_ROB_ARMORED_CARS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** For the money bags that drop a max of $40,000. Often called 40k bags.

Most likely NETWORK_EARN_FROM_ROB***

---

### NETWORK_EARN_FROM_CRATE_DROP

**Hash:** `0xB1CC1B9EC3007A2A` | **JHash:** `0xEAF04923` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CRATE_DROP(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CRATE_DROP, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BETTING

**Hash:** `0x827A5BA1A44ACA6D` | **JHash:** `0xA0F7F07C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BETTING(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BETTING, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_JOB

**Hash:** `0xB2CC4836834E8A98` | **JHash:** `0x0B6997FC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_JOB(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_JOB, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_JOBX2

**Hash:** `0xDEBBF584665411D0` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_JOBX2(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_JOBX2, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_PREMIUM_JOB

**Hash:** `0xC8407624CEF2354B` | **Build:** `791`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_PREMIUM_JOB(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_PREMIUM_JOB, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BEND_JOB

**Hash:** `0x61326EE6DF15B0CA` | **JHash:** `0xBB293400` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BEND_JOB(int amount, const char* heistHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BEND_JOB, amount, heistHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `heistHash` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_CHALLENGE_WIN

**Hash:** `0x2B171E6B2F64D8DF` | **JHash:** `0x2BEFB6C4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CHALLENGE_WIN(Any p0, Any* p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CHALLENGE_WIN, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BOUNTY

**Hash:** `0x131BB5DA15453ACF` | **JHash:** `0x127F2DAE` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BOUNTY(int amount, Any* gamerHandle, Any* p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BOUNTY, amount, gamerHandle, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_IMPORT_EXPORT

**Hash:** `0xF92A014A634442D6` | **JHash:** `0xF11FC458` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_IMPORT_EXPORT(int amount, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_IMPORT_EXPORT, amount, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_HOLDUPS

**Hash:** `0x45B8154E077D9E4D` | **JHash:** `0xE6B90E9C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_HOLDUPS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_HOLDUPS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_PROPERTY

**Hash:** `0x849648349D77F5C5` | **JHash:** `0x9BE4F7E1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_PROPERTY(int amount, Hash propertyName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_PROPERTY, amount, propertyName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `propertyName` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_AI_TARGET_KILL

**Hash:** `0x515B4A22E4D3C6D7` | **JHash:** `0x866004A8` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_AI_TARGET_KILL(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_AI_TARGET_KILL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** DSPORT

---

### NETWORK_EARN_FROM_NOT_BADSPORT

**Hash:** `0x4337511FA8221D36` | **JHash:** `0xCC068380` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_NOT_BADSPORT(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_NOT_BADSPORT, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_VEHICLE

**Hash:** `0xB539BD8A4C1EECF8` | **JHash:** `0xF803589D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_VEHICLE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_VEHICLE, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_PERSONAL_VEHICLE

**Hash:** `0x3F4D00167E41E0AD` | **JHash:** `0x96B8BEE8` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_PERSONAL_VEHICLE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_PERSONAL_VEHICLE, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_DAILY_OBJECTIVES

**Hash:** `0x6EA318C91C1A8786` | **JHash:** `0xAB6BD72F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_DAILY_OBJECTIVES(int amount, const char* type, int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_DAILY_OBJECTIVES, amount, type, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `type` | `const char*` | `string` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** type either Monthly,Weekly,Daily

---

### NETWORK_EARN_FROM_AMBIENT_JOB

**Hash:** `0xFB6DB092FBAE29E6` | **JHash:** `0xA14CC95D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_AMBIENT_JOB(int p0, const char* p1, Any* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_AMBIENT_JOB, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Example for p1: "AM_DISTRACT_COPS"

---

### NETWORK_EARN_FROM_JOB_BONUS

**Hash:** `0x6816FB4416760775` | **JHash:** `0xBEE23713` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_JOB_BONUS(Any p0, Any* p1, Any* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_JOB_BONUS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_CRIMINAL_MASTERMIND

**Hash:** `0xFA009A62990671D4` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CRIMINAL_MASTERMIND(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CRIMINAL_MASTERMIND, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_HEIST_AWARD

**Hash:** `0x9D4FDBB035229669` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_HEIST_AWARD(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_HEIST_AWARD, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FIRST_TIME_BONUS

**Hash:** `0x11B0A20C493F7E36` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_FIRST_TIME_BONUS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FIRST_TIME_BONUS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GOON

**Hash:** `0xCDA1C62BE2777802` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_EARN_GOON(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GOON, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BOSS

**Hash:** `0x08B0CA7A6AB3AC32` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_EARN_BOSS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BOSS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AGENCY

**Hash:** `0x0CB1BE0633C024A8` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_EARN_AGENCY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AGENCY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_WAREHOUSE

**Hash:** `0x3E4ADAFF1830F146` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_WAREHOUSE(int amount, int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_WAREHOUSE, amount, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_CONTRABAND

**Hash:** `0xECA658CE2A4E5A72` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CONTRABAND(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CONTRABAND, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_DESTROYING_CONTRABAND

**Hash:** `0x84C0116D012E8FC2` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_DESTROYING_CONTRABAND(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_DESTROYING_CONTRABAND, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_SMUGGLER_WORK

**Hash:** `0x6B7E4FB50D5F3D65` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_SMUGGLER_WORK(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_SMUGGLER_WORK, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_HANGAR_TRADE

**Hash:** `0x31BA138F6304FB9F` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_HANGAR_TRADE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_HANGAR_TRADE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_PURCHASE_CLUB_HOUSE

**Hash:** `0x55A1E095DB052FA5` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_EARN_PURCHASE_CLUB_HOUSE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_PURCHASE_CLUB_HOUSE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BUSINESS_PRODUCT

**Hash:** `0x8586789730B10CAF` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BUSINESS_PRODUCT(int amount, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BUSINESS_PRODUCT, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_VEHICLE_EXPORT

**Hash:** `0xEDEAD9A91EC768B3` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_VEHICLE_EXPORT(int amount, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_VEHICLE_EXPORT, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_SMUGGLER_AGENCY

**Hash:** `0xDEE612F2D71B0308` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_EARN_SMUGGLER_AGENCY(int amount, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_SMUGGLER_AGENCY, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BOUNTY_HUNTER_REWARD

**Hash:** `0xF6B170F9A02E9E87` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_BOUNTY_HUNTER_REWARD(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BOUNTY_HUNTER_REWARD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BUSINESS_BATTLE

**Hash:** `0x42FCE14F50F27291` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BUSINESS_BATTLE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BUSINESS_BATTLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_CLUB_MANAGEMENT_PARTICIPATION

**Hash:** `0xA75EAC69F59E96E7` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_CLUB_MANAGEMENT_PARTICIPATION(Any p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_CLUB_MANAGEMENT_PARTICIPATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_FMBB_PHONECALL_MISSION

**Hash:** `0xC5156361F26E2212` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_FMBB_PHONECALL_MISSION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_FMBB_PHONECALL_MISSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_BUSINESS_HUB_SELL

**Hash:** `0x0B39CF0D53F1C883` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_BUSINESS_HUB_SELL(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_BUSINESS_HUB_SELL, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_FMBB_BOSS_WORK

**Hash:** `0x1FDA0AA679C9919B` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_FMBB_BOSS_WORK(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_FMBB_BOSS_WORK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FMBB_WAGE_BONUS

**Hash:** `0xFFFBA1B1F7C0B6F4` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_FMBB_WAGE_BONUS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FMBB_WAGE_BONUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_CAN_SPEND_MONEY

**Hash:** `0xAB3CAA6B422164DA` | **JHash:** `0x5AA379D9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SPEND_MONEY(Any p0, BOOL p1, BOOL p2, BOOL p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SPEND_MONEY, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_SPEND_MONEY2

**Hash:** `0x7303E27CC6532080` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SPEND_MONEY2(Any p0, BOOL p1, BOOL p2, BOOL p3, Any* p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SPEND_MONEY2, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any*` | `object*` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_BUY_ITEM

**Hash:** `0xF0077C797F66A355` | **JHash:** `0xA07B6368` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_ITEM(int amount, Hash item, Any p2, Any p3, BOOL p4, const char* item_name, Any p6, Any p7, Any p8, BOOL p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_ITEM, amount, item, p2, p3, p4, item_name, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `item` | `Hash` | `uint` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `BOOL` | `bool` | |
| `item_name` | `const char*` | `string` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_TAXI

**Hash:** `0x17C3A7D31EAE39F9` | **JHash:** `0x1F3DB3E3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_TAXI(int amount, BOOL p1, BOOL p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_TAXI, amount, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_PAY_EMPLOYEE_WAGE

**Hash:** `0x5FD5ED82CBBE9989` | **JHash:** `0xBE70849B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_PAY_EMPLOYEE_WAGE(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_PAY_EMPLOYEE_WAGE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_PAY_MATCH_ENTRY_FEE

**Hash:** `0x9346E14F2AF74D46` | **JHash:** `0x224A3488` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_PAY_MATCH_ENTRY_FEE(int amount, const char* matchId, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_PAY_MATCH_ENTRY_FEE, amount, matchId, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `matchId` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BETTING

**Hash:** `0x1C436FD11FFA692F` | **JHash:** `0xF8A07513` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BETTING(int amount, int p1, const char* matchId, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BETTING, amount, p1, matchId, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `matchId` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_WAGER

**Hash:** `0xD99DB210089617FE` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SPENT_WAGER(Any p0, Any p1, int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_WAGER, p0, p1, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_IN_STRIPCLUB

**Hash:** `0xEE99784E4467689C` | **JHash:** `0x8957038E` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_IN_STRIPCLUB(Any p0, BOOL p1, Any p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_IN_STRIPCLUB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_HEALTHCARE

**Hash:** `0xD9B067E55253E3DD` | **JHash:** `0x832150E5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_HEALTHCARE(int cost, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_HEALTHCARE, cost, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cost` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_AIRSTRIKE

**Hash:** `0x763B4BD305338F19` | **JHash:** `0x40470683` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_AIRSTRIKE(int cost, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_AIRSTRIKE, cost, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cost` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 = 0 (always)
p2 = 1 (always)

---

### NETWORK_BUY_BACKUP_GANG

**Hash:** `0xA3EDDAA42411D3B9` | **JHash:** `0x2AD2EC51` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_BACKUP_GANG(int p0, int p1, BOOL p2, BOOL p3, int npcProvider)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_BACKUP_GANG, p0, p1, p2, p3, npcProvider);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `npcProvider` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_HELI_STRIKE

**Hash:** `0x81AA4610E3FD3A69` | **JHash:** `0x047547D4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_HELI_STRIKE(int cost, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_HELI_STRIKE, cost, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cost` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 = 0 (always)
p2 = 1 (always)

---

### NETWORK_SPENT_AMMO_DROP

**Hash:** `0xB162DC95C0A3317B` | **JHash:** `0x4B643076` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_AMMO_DROP(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_AMMO_DROP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_BOUNTY

**Hash:** `0x7B718E197453F2D9` | **JHash:** `0xCB89CBE0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_BOUNTY(int amount, Player victim, BOOL p2, BOOL p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_BOUNTY, amount, victim, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `victim` | `Player` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 is just an assumption. p2 was false and p3 was true.

---

### NETWORK_BUY_PROPERTY

**Hash:** `0x650A08A280870AF6` | **JHash:** `0x7D479AAB` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_PROPERTY(int cost, Hash propertyName, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_PROPERTY, cost, propertyName, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cost` | `int` | `int` | |
| `propertyName` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_SMOKES

**Hash:** `0x75AF80E61248EEBD` | **JHash:** `0xC0AB03EB` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_SMOKES(int p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_SMOKES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HELI_PICKUP

**Hash:** `0x7BF1D73DB2ECA492` | **JHash:** `0x27EEBCAB` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HELI_PICKUP(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HELI_PICKUP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BOAT_PICKUP

**Hash:** `0x524EE43A37232C00` | **JHash:** `0xB241CABD` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BOAT_PICKUP(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BOAT_PICKUP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BULL_SHARK

**Hash:** `0xA6DD8458CE24012C` | **JHash:** `0xDE7D398C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BULL_SHARK(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BULL_SHARK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_CASH_DROP

**Hash:** `0x289016EC778D60E0` | **JHash:** `0x87BD1D11` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CASH_DROP(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CASH_DROP, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HIRE_MUGGER

**Hash:** `0xE404BFB981665BF0` | **JHash:** `0xE792C4A5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HIRE_MUGGER(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HIRE_MUGGER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Only used once in a script (am_contact_requests)
p1 = 0
p2 = 1

---

### NETWORK_SPENT_ROBBED_BY_MUGGER

**Hash:** `0x995A65F15F581359` | **JHash:** `0xE6AAA0D5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_ROBBED_BY_MUGGER(int amount, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_ROBBED_BY_MUGGER, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HIRE_MERCENARY

**Hash:** `0xE7B80E2BF9D80BD6` | **JHash:** `0x99CF02C4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HIRE_MERCENARY(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HIRE_MERCENARY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_WANTEDLEVEL

**Hash:** `0xE1B13771A843C4F6` | **JHash:** `0xE7CB4F95` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_WANTEDLEVEL(Any p0, Any* p1, BOOL p2, BOOL p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_WANTEDLEVEL, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_OFFTHERADAR

**Hash:** `0xA628A745E2275C5D` | **JHash:** `0x20DDCF2F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_OFFTHERADAR(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_OFFTHERADAR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_REVEAL_PLAYERS

**Hash:** `0x6E176F1B18BC0637` | **JHash:** `0x2F7836E2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_REVEAL_PLAYERS(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_REVEAL_PLAYERS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_CARWASH

**Hash:** `0xEC03C719DB2F4306` | **JHash:** `0x8283E028` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CARWASH(Any p0, Any p1, Any p2, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CARWASH, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_CINEMA

**Hash:** `0x6B38ECB05A63A685` | **JHash:** `0x1100CAF5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CINEMA(Any p0, Any p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CINEMA, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_TELESCOPE

**Hash:** `0x7FE61782AD94CC09` | **JHash:** `0xAE7FF044` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_TELESCOPE(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_TELESCOPE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HOLDUPS

**Hash:** `0xD9B86B9872039763` | **JHash:** `0x1B3803B1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HOLDUPS(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HOLDUPS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_PASSIVE_MODE

**Hash:** `0x6D3A430D1A809179` | **JHash:** `0x7E97C92C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_PASSIVE_MODE(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_PASSIVE_MODE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BANK_INTEREST

**Hash:** `0xCA230C9682556CF1` | **JHash:** `0xF02E92D7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BANK_INTEREST(int p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BANK_INTEREST, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PROSTITUTES

**Hash:** `0xB21B89501CFAC79E` | **JHash:** `0x78436D07` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PROSTITUTES(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PROSTITUTES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_ARREST_BAIL

**Hash:** `0x812F5488B1B2A299` | **JHash:** `0x5AEE2FC1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_ARREST_BAIL(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_ARREST_BAIL, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PAY_VEHICLE_INSURANCE_PREMIUM

**Hash:** `0x9FF28D88C766E3E8` | **JHash:** `0x4E665BB2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PAY_VEHICLE_INSURANCE_PREMIUM(int amount, Hash vehicleModel, Any* gamerHandle, BOOL notBankrupt, BOOL hasTheMoney)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PAY_VEHICLE_INSURANCE_PREMIUM, amount, vehicleModel, gamerHandle, notBankrupt, hasTheMoney);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `vehicleModel` | `Hash` | `uint` | |
| `gamerHandle` | `Any*` | `object*` | |
| `notBankrupt` | `BOOL` | `bool` | |
| `hasTheMoney` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** According to how I understood this in the freemode script alone,
The first parameter is determined by a function named, func_5749 within the freemode script which has a list of all the vehicles and a set price to return which some vehicles deals with globals as well. So the first parameter is basically the set in stone insurance cost it's gonna charge you for that specific vehicle model.

The second parameter whoever put it was right, they call GET_ENTITY_MODEL with the vehicle as the paremeter.

The third parameter is the network handle as they call their little struct<13> func or atleast how the script decompiled it to look which in lamens terms just returns the network handle of the previous owner based on DECOR_GET_INT(vehicle, "Previous_Owner").

The fourth parameter is a bool that returns true/false depending on if your bank balance is greater then 0.

The fifth and last parameter is a bool that returns true/false depending on if you have the money for the car based on the cost returned by func_5749. In the freemode script eg,
bool hasTheMoney = MONEY::_GET_BANK_BALANCE() < carCost.

---

### NETWORK_SPENT_CALL_PLAYER

**Hash:** `0xACDE7185B374177C` | **JHash:** `0x1A89B5FC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CALL_PLAYER(Any p0, Any* p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CALL_PLAYER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BOUNTY

**Hash:** `0x29B260B84947DFCC` | **JHash:** `0x3401FC96` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BOUNTY(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BOUNTY, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_FROM_ROCKSTAR

**Hash:** `0x6A445B64ED7ABEB5` | **JHash:** `0x54198922` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_FROM_ROCKSTAR(int p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_FROM_ROCKSTAR, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_EARNED_FROM_BANK_AND_WALLETS

**Hash:** `0x9B5016A6433A68C5` | **JHash:** `0xBD64F1B7` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_SPEND_EARNED_FROM_BANK_AND_WALLETS(int amount)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_SPEND_EARNED_FROM_BANK_AND_WALLETS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Hardcoded to return 0.

---

### PROCESS_CASH_GIFT

**Hash:** `0x20194D48EAEC9A41` | **JHash:** `0xC5D8B1E9` | **Build:** `323`

**C++ 原型:**
```cpp
const char* PROCESS_CASH_GIFT(int* p0, int* p1, const char* p2)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.PROCESS_CASH_GIFT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |
| `p1` | `int*` | `int*` | |
| `p2` | `const char*` | `string` | |

**返回值:** `const char*` → `string`

**说明:** This isn't a hash collision.

---

### NETWORK_SPENT_MOVE_SUBMARINE

**Hash:** `0xCD4D66B43B1DD28D` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPENT_MOVE_SUBMARINE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_MOVE_SUBMARINE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PLAYER_HEALTHCARE

**Hash:** `0x7C99101F7FCE2EE5` | **JHash:** `0x3D96A21C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PLAYER_HEALTHCARE(int p0, int p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PLAYER_HEALTHCARE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_NO_COPS

**Hash:** `0xD5BB406F4E04019F` | **JHash:** `0x2E51C61C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_NO_COPS(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_NO_COPS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_CARGO_SOURCING

**Hash:** `0x948705F6F9C50824` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CARGO_SOURCING(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CARGO_SOURCING, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_REQUEST_JOB

**Hash:** `0x8204DA7934DF3155` | **JHash:** `0xD57A5125` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_REQUEST_JOB(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_REQUEST_JOB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_REQUEST_HEIST

**Hash:** `0x9D26502BB97BFE62` | **JHash:** `0xF4287778` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_REQUEST_HEIST(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_REQUEST_HEIST, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_FAIRGROUND_RIDE

**Hash:** `0x8A7B3952DD64D2B5` | **JHash:** `0xD9622D64` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BUY_FAIRGROUND_RIDE(int amount, Any p1, BOOL p2, BOOL p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_FAIRGROUND_RIDE, amount, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** The first parameter is the amount spent which is store in a global when this native is called. The global returns 10. Which is the price for both rides.

The last 3 parameters are, 
2,0,1 in the am_ferriswheel.c
1,0,1 in the am_rollercoaster.c


---

### NETWORK_ECONOMY_HAS_FIXED_CRAZY_NUMBERS

**Hash:** `0x7C4FCCD2E4DEB394` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ECONOMY_HAS_FIXED_CRAZY_NUMBERS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ECONOMY_HAS_FIXED_CRAZY_NUMBERS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SPENT_JOB_SKIP

**Hash:** `0x28F174A67B8D0C2F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SPENT_JOB_SKIP(int amount, const char* matchId, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_JOB_SKIP, amount, matchId, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `matchId` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BOSS_GOON

**Hash:** `0xFFBE02CD385356BD` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL NETWORK_SPENT_BOSS_GOON(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SPENT_BOSS_GOON, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SPEND_GOON

**Hash:** `0x08A1B82B91900682` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GOON(int p0, int p1, int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GOON, p0, p1, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BOSS

**Hash:** `0xDBC966A01C02BCA7` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BOSS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BOSS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_MOVE_YACHT

**Hash:** `0xE7DF4E0545DFB56E` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SPENT_MOVE_YACHT(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_MOVE_YACHT, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_RENAME_ORGANIZATION

**Hash:** `0xFC4EE00A7B3BFB76` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SPENT_RENAME_ORGANIZATION(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_RENAME_ORGANIZATION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_BUY_CONTRABAND_MISSION

**Hash:** `0x30FD873ECE50E9F6` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_BUY_CONTRABAND_MISSION(int p0, int p1, Hash p2, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BUY_CONTRABAND_MISSION, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `Hash` | `uint` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_SERVICE_HELI

**Hash:** `0x112209CE0290C03A` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_SERVICE_HELI(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_SERVICE_HELI, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_SERVICE_VEHICLE

**Hash:** `0xED5FD7AF10F5E262` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_SERVICE_VEHICLE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_SERVICE_VEHICLE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_SERVICE_SNACK

**Hash:** `0x0D30EB83668E63C5` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_SERVICE_SNACK(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_SERVICE_SNACK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_SERVICE_DANCER

**Hash:** `0xB49ECA122467D05F` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_SERVICE_DANCER(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_SERVICE_DANCER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_SERVICE_IMPOUND

**Hash:** `0xE23ADC6FCB1F29AE` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_SERVICE_IMPOUND(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_SERVICE_IMPOUND, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PA_HELI_PICKUP

**Hash:** `0x0FE8E1FCD2B86B33` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PA_HELI_PICKUP(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PA_HELI_PICKUP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_OFFICE_PROPERTY

**Hash:** `0x69EF772B192614C1` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_OFFICE_PROPERTY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_OFFICE_PROPERTY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_OFFICE_PROPERTY

**Hash:** `0x8E243837643D9583` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_OFFICE_PROPERTY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_OFFICE_PROPERTY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_WAREHOUSE_PROPERTY

**Hash:** `0xBD0EFB25CCA8F97A` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_WAREHOUSE_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_WAREHOUSE_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_WAREHOUSE_PROPERTY

**Hash:** `0xA95F667A755725DA` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_WAREHOUSE_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_WAREHOUSE_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_IMPEXP_WAREHOUSE_PROPERTY

**Hash:** `0x33981D6804E62F49` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_IMPEXP_WAREHOUSE_PROPERTY(int amount, Any* data, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_IMPEXP_WAREHOUSE_PROPERTY, amount, data, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `data` | `Any*` | `object*` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_IMPEXP_WAREHOUSE_PROPERTY

**Hash:** `0x4128464231E3CA0B` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_IMPEXP_WAREHOUSE_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_IMPEXP_WAREHOUSE_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_TRADE_IMPEXP_WAREHOUSE_PROPERTY

**Hash:** `0x2FAB6614CE22E196` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_TRADE_IMPEXP_WAREHOUSE_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_TRADE_IMPEXP_WAREHOUSE_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_ORDER_WAREHOUSE_VEHICLE

**Hash:** `0x05F04155A226FBBF` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_ORDER_WAREHOUSE_VEHICLE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_ORDER_WAREHOUSE_VEHICLE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_ORDER_BODYGUARD_VEHICLE

**Hash:** `0xE8B0B270B6E7C76E` | **Build:** `757`

**C++ 原型:**
```cpp
void NETWORK_SPENT_ORDER_BODYGUARD_VEHICLE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_ORDER_BODYGUARD_VEHICLE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_JUKEBOX

**Hash:** `0x5BCDE0F640C773D2` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_JUKEBOX(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_JUKEBOX, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_CLUB_HOUSE

**Hash:** `0x998E18CEB44487FC` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_CLUB_HOUSE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_CLUB_HOUSE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_CLUB_HOUSE

**Hash:** `0xFA07759E6FDDD7CF` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_CLUB_HOUSE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_CLUB_HOUSE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_BUSINESS_PROPERTY

**Hash:** `0x6FD97159FE3C971A` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_BUSINESS_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_BUSINESS_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_BUSINESS_PROPERTY

**Hash:** `0x675D19C6067CAE08` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_BUSINESS_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_BUSINESS_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_TRADE_BUSINESS_PROPERTY

**Hash:** `0xA51B086B0B2C0F7A` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_TRADE_BUSINESS_PROPERTY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_TRADE_BUSINESS_PROPERTY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_MC_ABILITY

**Hash:** `0xD7CCCBA28C4ECAF0` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_MC_ABILITY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_MC_ABILITY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PAY_BUSINESS_SUPPLIES

**Hash:** `0x0035BB914316F1E3` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PAY_BUSINESS_SUPPLIES(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PAY_BUSINESS_SUPPLIES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_CHANGE_APPEARANCE

**Hash:** `0x5F456788B05FAEAC` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_CHANGE_APPEARANCE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_CHANGE_APPEARANCE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_VEHICLE_EXPORT_MODS

**Hash:** `0xA75CCF58A60A5FD1` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_VEHICLE_EXPORT_MODS(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_VEHICLE_EXPORT_MODS, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_OFFICE_GARAGE

**Hash:** `0xB4C2EC463672474E` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_OFFICE_GARAGE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_OFFICE_GARAGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_OFFICE_GARAGE

**Hash:** `0x2AFC2D19B50797F2` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_OFFICE_GARAGE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_OFFICE_GARAGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_IMPORT_EXPORT_REPAIR

**Hash:** `0xC1952F3773BA18FE` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SPENT_IMPORT_EXPORT_REPAIR(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_IMPORT_EXPORT_REPAIR, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_HANGAR

**Hash:** `0xCCB339CC970452DA` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_HANGAR(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_HANGAR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_HANGAR

**Hash:** `0x615EB504B0788DAF` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_HANGAR(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_HANGAR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HANGAR_UTILITY_CHARGES

**Hash:** `0xB18AC2ECBB15CB6A` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HANGAR_UTILITY_CHARGES(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HANGAR_UTILITY_CHARGES, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_HANGAR_STAFF_CHARGES

**Hash:** `0xB1F1346FD57685D7` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_SPENT_HANGAR_STAFF_CHARGES(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_HANGAR_STAFF_CHARGES, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_TRUCK

**Hash:** `0xAC272C0AE01B4BD8` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_TRUCK(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_TRUCK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_TRUCK

**Hash:** `0x365E877C61D6988B` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_TRUCK(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_TRUCK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_BUNKER

**Hash:** `0x12D148D26538D0F9` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_BUNKER(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_BUNKER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPRADE_BUNKER

**Hash:** `0x0C82D21A77C22D49` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPRADE_BUNKER(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPRADE_BUNKER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_SELL_BUNKER

**Hash:** `0x9251B6ABF2D0A5B4` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_SELL_BUNKER(int amount, Hash bunkerHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_SELL_BUNKER, amount, bunkerHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `bunkerHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BALLISTIC_EQUIPMENT

**Hash:** `0x5D97630A8A0EF123` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BALLISTIC_EQUIPMENT(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BALLISTIC_EQUIPMENT, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_RDR_BONUS

**Hash:** `0x7A5349B773584675` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_RDR_BONUS(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_RDR_BONUS, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_WAGE_PAYMENT

**Hash:** `0x35F8DA0E8A31EF1B` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_EARN_WAGE_PAYMENT(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_WAGE_PAYMENT, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_WAGE_PAYMENT_BONUS

**Hash:** `0x005ACA7100BD101D` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_EARN_WAGE_PAYMENT_BONUS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_WAGE_PAYMENT_BONUS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_BASE

**Hash:** `0x4EA3F425C7744D21` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_BASE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_BASE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_BASE

**Hash:** `0x3DD3F33A5D55EA6F` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_BASE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_BASE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_BUY_TILTROTOR

**Hash:** `0x0CCE73BC7A11E885` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPENT_BUY_TILTROTOR(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_BUY_TILTROTOR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_TILTROTOR

**Hash:** `0x165E135D6DFA2907` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_TILTROTOR(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_TILTROTOR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_EMPLOY_ASSASSINS

**Hash:** `0x5BBBD92186E1F1C5` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPENT_EMPLOY_ASSASSINS(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_EMPLOY_ASSASSINS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GANGOPS_CANNON

**Hash:** `0x771ADB0E7635B7BF` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GANGOPS_CANNON(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GANGOPS_CANNON, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GANGOPS_SKIP_MISSION

**Hash:** `0xDA947AE8880D5C18` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GANGOPS_SKIP_MISSION(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GANGOPS_SKIP_MISSION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CASINO_HEIST_SKIP_MISSION

**Hash:** `0x487009DD91D93429` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CASINO_HEIST_SKIP_MISSION(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CASINO_HEIST_SKIP_MISSION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_SELL_BASE

**Hash:** `0x0E1E2FF3F4EC11AA` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_SELL_BASE(int amount, Hash baseNameHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_SELL_BASE, amount, baseNameHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `baseNameHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_TARGET_REFUND

**Hash:** `0x5B669CF2299A271F` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_TARGET_REFUND(int amount, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_TARGET_REFUND, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_WAGES

**Hash:** `0x2DCB19ABAB0380A8` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_WAGES(int amount, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_WAGES, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_WAGES_BONUS

**Hash:** `0x15BB2A5C757EB91F` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_WAGES_BONUS(int amount, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_WAGES_BONUS, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DAR_CHALLENGE

**Hash:** `0xCAC672087B4A24AB` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_DAR_CHALLENGE(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DAR_CHALLENGE, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DOOMSDAY_FINALE_BONUS

**Hash:** `0x128A747F4A230952` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_DOOMSDAY_FINALE_BONUS(int amount, Hash vehicleHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DOOMSDAY_FINALE_BONUS, amount, vehicleHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `vehicleHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_AWARD

**Hash:** `0xA9A31475F530DFDA` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_AWARD(int amount, const char* p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_AWARD, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_ELITE

**Hash:** `0x2597A0D4A4FC2C77` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_ELITE(int amount, const char* p1, int actIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_ELITE, amount, p1, actIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `actIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SERVICE_EARN_GANGOPS_RIVAL_DELIVERY

**Hash:** `0x1B882107C23A9022` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SERVICE_EARN_GANGOPS_RIVAL_DELIVERY(int earnedMoney)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SERVICE_EARN_GANGOPS_RIVAL_DELIVERY, earnedMoney);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `earnedMoney` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GANGOPS_START_STRAND

**Hash:** `0xA19EC0786E326E06` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GANGOPS_START_STRAND(int type, int amount, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GANGOPS_START_STRAND, type, amount, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |
| `amount` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GANGOPS_TRIP_SKIP

**Hash:** `0x5ECE6FD7B4EC8D6A` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GANGOPS_TRIP_SKIP(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GANGOPS_TRIP_SKIP, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_PREP_PARTICIPATION

**Hash:** `0xED26584F6BDCBBFD` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_PREP_PARTICIPATION(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_PREP_PARTICIPATION, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_SETUP

**Hash:** `0xA9160796D47A2CF8` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_SETUP(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_SETUP, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_GANGOPS_FINALE

**Hash:** `0x1C121FC9545E0D52` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_EARN_GANGOPS_FINALE(int amount, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_GANGOPS_FINALE, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GANGOPS_REPAIR_COST

**Hash:** `0x2A7CEC72C3443BCC` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GANGOPS_REPAIR_COST(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GANGOPS_REPAIR_COST, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_NIGHTCLUB

**Hash:** `0xE0F82D68C7039158` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_NIGHTCLUB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_NIGHTCLUB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_NIGHTCLUB_DANCING

**Hash:** `0xB4DEAE67F35E2ACD` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_NIGHTCLUB_DANCING(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_NIGHTCLUB_DANCING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BB_EVENT_BONUS

**Hash:** `0xFDD8D2440DAF1590` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_BB_EVENT_BONUS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BB_EVENT_BONUS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_HACKER_TRUCK

**Hash:** `0x2A93C46AAB1EACC9` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_HACKER_TRUCK(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_HACKER_TRUCK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_HACKER_TRUCK

**Hash:** `0x226C284C830D0CA8` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_HACKER_TRUCK(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_HACKER_TRUCK, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_HACKER_TRUCK

**Hash:** `0xE8815FE993896AD3` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_HACKER_TRUCK(Any p0, int amount, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_HACKER_TRUCK, p0, amount, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `amount` | `int` | `int` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_PURCHASE_NIGHTCLUB_AND_WAREHOUSE

**Hash:** `0xED76D195E6E3BF7F` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_PURCHASE_NIGHTCLUB_AND_WAREHOUSE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_PURCHASE_NIGHTCLUB_AND_WAREHOUSE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_UPGRADE_NIGHTCLUB_AND_WAREHOUSE

**Hash:** `0x1DC9B749E7AE282B` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_UPGRADE_NIGHTCLUB_AND_WAREHOUSE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_UPGRADE_NIGHTCLUB_AND_WAREHOUSE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_NIGHTCLUB_AND_WAREHOUSE

**Hash:** `0xC6E74CF8C884C880` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_EARN_NIGHTCLUB_AND_WAREHOUSE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_NIGHTCLUB_AND_WAREHOUSE, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_NIGHTCLUB_AND_WAREHOUSE

**Hash:** `0x65482BFD0923C8A1` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPEND_NIGHTCLUB_AND_WAREHOUSE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_NIGHTCLUB_AND_WAREHOUSE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_RDR_HATCHET_BONUS

**Hash:** `0xE284D46FFDB82E36` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_RDR_HATCHET_BONUS(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_RDR_HATCHET_BONUS, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_NIGHTCLUB_ENTRY_FEE

**Hash:** `0x876056684281655D` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_NIGHTCLUB_ENTRY_FEE(Player player, int amount, Any p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_NIGHTCLUB_ENTRY_FEE, player, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_NIGHTCLUB_BAR_DRINK

**Hash:** `0xDD21B016E4289465` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPEND_NIGHTCLUB_BAR_DRINK(int amount, Any p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_NIGHTCLUB_BAR_DRINK, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BOUNTY_HUNTER_MISSION

**Hash:** `0x1BEA0CD93470BB1F` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BOUNTY_HUNTER_MISSION(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BOUNTY_HUNTER_MISSION, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_REHIRE_DJ

**Hash:** `0xF6C8A544E4CF14FC` | **Build:** `1493`

**C++ 原型:**
```cpp
void NETWORK_SPENT_REHIRE_DJ(int amount, Any p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_REHIRE_DJ, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPENT_ARENA_JOIN_SPECTATOR

**Hash:** `0x14EAEA58F93B55AF` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPENT_ARENA_JOIN_SPECTATOR(int amount, Any p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPENT_ARENA_JOIN_SPECTATOR, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARENA_SKILL_LEVEL_PROGRESSION

**Hash:** `0xE08256F972C7BB2C` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARENA_SKILL_LEVEL_PROGRESSION(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARENA_SKILL_LEVEL_PROGRESSION, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARENA_CAREER_PROGRESSION

**Hash:** `0x0F99F70C61F14619` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARENA_CAREER_PROGRESSION(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARENA_CAREER_PROGRESSION, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_MAKE_IT_RAIN

**Hash:** `0xE5F5A060439C2F5D` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_MAKE_IT_RAIN(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_MAKE_IT_RAIN, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_ARENA

**Hash:** `0x40D5DA9550B7CB46` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_ARENA(int amount, BOOL p1, BOOL p2, const char* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_ARENA, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_ARENA

**Hash:** `0x037ABB06825D7AB1` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_ARENA(int amount, BOOL p1, BOOL p2, const char* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_ARENA, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_ARENA_SPECTATOR_BOX

**Hash:** `0x7049BF858601DC0F` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_ARENA_SPECTATOR_BOX(int amount, int type, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_ARENA_SPECTATOR_BOX, amount, type, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `type` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** type either, 1 for cam spectate, 2 for drone

---

### NETWORK_SPEND_SPIN_THE_WHEEL_PAYMENT

**Hash:** `0x9A5BD1D0000B339C` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SPIN_THE_WHEEL_PAYMENT(int amount, Any p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SPIN_THE_WHEEL_PAYMENT, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_SPIN_THE_WHEEL_CASH

**Hash:** `0x676C48776CACBB5A` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_SPIN_THE_WHEEL_CASH(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_SPIN_THE_WHEEL_CASH, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_ARENA_PREMIUM

**Hash:** `0x619496D837EFD920` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SPEND_ARENA_PREMIUM(int amount, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_ARENA_PREMIUM, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARENA_WAR

**Hash:** `0x631F1CB8FB4130AA` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARENA_WAR(int amount, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARENA_WAR, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARENA_WAR_ASSASSINATE_TARGET

**Hash:** `0x5E7AE8AABE8B7C0D` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARENA_WAR_ASSASSINATE_TARGET(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARENA_WAR_ASSASSINATE_TARGET, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARENA_WAR_EVENT_CARGO

**Hash:** `0xA82959062361B259` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARENA_WAR_EVENT_CARGO(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARENA_WAR_EVENT_CARGO, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_RC_TIME_TRIAL

**Hash:** `0xDFF49EE984E7AAE8` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_RC_TIME_TRIAL(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_RC_TIME_TRIAL, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DAILY_OBJECTIVE_EVENT

**Hash:** `0x5128DF14A5BB86FC` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_DAILY_OBJECTIVE_EVENT(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DAILY_OBJECTIVE_EVENT, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CASINO_MEMBERSHIP

**Hash:** `0xFBBE0570EDF39D46` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CASINO_MEMBERSHIP(int amount, BOOL p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CASINO_MEMBERSHIP, amount, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_CASINO

**Hash:** `0x34A6FC4D06C4DA0F` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_CASINO(int amount, BOOL p1, BOOL p2, Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_CASINO, amount, p1, p2, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_CASINO

**Hash:** `0x4740D62BC1B4EBEA` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_CASINO(int amount, BOOL p1, BOOL p2, Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_CASINO, amount, p1, p2, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CASINO_GENERIC

**Hash:** `0x88BF9B612B84D3C3` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CASINO_GENERIC(int amount, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CASINO_GENERIC, amount, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_TIME_TRIAL_WIN

**Hash:** `0x0819DB99FD2FBBD8` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_TIME_TRIAL_WIN(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_TIME_TRIAL_WIN, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_COLLECTABLES_ACTION_FIGURES

**Hash:** `0x5517F90043466049` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_COLLECTABLES_ACTION_FIGURES(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_COLLECTABLES_ACTION_FIGURES, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_COLLECTABLE_COMPLETED_COLLECTION

**Hash:** `0x83AD64F53F4E9483` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_COLLECTABLE_COMPLETED_COLLECTION(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_COLLECTABLE_COMPLETED_COLLECTION, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_SELL_PRIZE_VEHICLE

**Hash:** `0x8BCB27A057DF7B7F` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_SELL_PRIZE_VEHICLE(int amount, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_SELL_PRIZE_VEHICLE, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_MISSION_REWARD

**Hash:** `0x566FD402B25787DE` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_MISSION_REWARD(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_MISSION_REWARD, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_STORY_MISSION_REWARD

**Hash:** `0xAC95ED552157E092` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_STORY_MISSION_REWARD(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_STORY_MISSION_REWARD, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_MISSION_PARTICIPATION

**Hash:** `0x09E8F18641BE2575` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_MISSION_PARTICIPATION(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_MISSION_PARTICIPATION, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_AWARD

**Hash:** `0x973A9781A34F8DEB` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_AWARD(int amount, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_AWARD, amount, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_ARCADE

**Hash:** `0x870289A558348378` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_ARCADE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_ARCADE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_ARCADE

**Hash:** `0x5574637681911FDA` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_ARCADE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_ARCADE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CASINO_HEIST

**Hash:** `0xD30E8392F407C328` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CASINO_HEIST(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9, Any p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CASINO_HEIST, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_ARCADE_MGMT

**Hash:** `0xB5B58E24868CB09E` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_ARCADE_MGMT(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_ARCADE_MGMT, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_PLAY_ARCADE

**Hash:** `0xEAD3D81F2C3A1458` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_PLAY_ARCADE(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_PLAY_ARCADE, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_ARCADE

**Hash:** `0x43AA7FAC4E6D6687` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SPEND_ARCADE(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_ARCADE, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_HEIST

**Hash:** `0x72E7C7B9615FA3C3` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_HEIST(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_HEIST, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_UPGRADE_ARCADE

**Hash:** `0x4C3B75694F7E0D9C` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_UPGRADE_ARCADE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_UPGRADE_ARCADE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ARCADE

**Hash:** `0xD29334ED1A256DBF` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_ARCADE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ARCADE, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_COLLECTABLES

**Hash:** `0xA95CFB4E02390842` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_COLLECTABLES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_COLLECTABLES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CHALLENGE

**Hash:** `0x0DD362F14F18942A` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_CHALLENGE(int amount, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CHALLENGE, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CASINO_HEIST_AWARDS

**Hash:** `0x3EC7471E6909798A` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_EARN_CASINO_HEIST_AWARDS(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CASINO_HEIST_AWARDS, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_COLLECTABLE_ITEM

**Hash:** `0x84FF63BD4966F33D` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_EARN_COLLECTABLE_ITEM(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_COLLECTABLE_ITEM, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_COLLECTABLE_COMPLETED_COLLECTION

**Hash:** `0x5C9B198AF5A54FA6` | **Build:** `2060`

**C++ 原型:**
```cpp
void NETWORK_EARN_COLLECTABLE_COMPLETED_COLLECTION(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_COLLECTABLE_COMPLETED_COLLECTION, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_YATCH_MISSION

**Hash:** `0xDE68E30D89F97132` | **Build:** `2060`

**C++ 原型:**
```cpp
void NETWORK_EARN_YATCH_MISSION(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_YATCH_MISSION, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DISPATCH_CALL

**Hash:** `0xE2E244AB823B4483` | **Build:** `2060`

**C++ 原型:**
```cpp
void NETWORK_EARN_DISPATCH_CALL(int amount, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DISPATCH_CALL, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BEACH_PARTY

**Hash:** `0x54ABA22FA6371249` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BEACH_PARTY(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BEACH_PARTY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SUBMARINE

**Hash:** `0x6C8BC1488527AAAB` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SUBMARINE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SUBMARINE, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CASINO_CLUB

**Hash:** `0xC991C255AA6D90B2` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CASINO_CLUB(int amount1, Any p1, BOOL p2, Any p3, int p4, int p5, int p6, int amount2, Any p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CASINO_CLUB, amount1, p1, p2, p3, p4, p5, p6, amount2, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount1` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `int` | `int` | |
| `amount2` | `int` | `int` | |
| `p8` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_SUB

**Hash:** `0x90CD7C6871FBF1B4` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_SUB(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_SUB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_SUB

**Hash:** `0x89049A84065CE68E` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_SUB(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_SUB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_ISLAND_HEIST

**Hash:** `0xE86689E5F82DE429` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_SPEND_ISLAND_HEIST(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_ISLAND_HEIST, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_ISLAND_HEIST

**Hash:** `0xD21D111C46BA9F15` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_EARN_ISLAND_HEIST(int amount1, Any p1, Any p2, Any p3, int amount2, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_ISLAND_HEIST, amount1, p1, p2, p3, amount2, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount1` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `amount2` | `int` | `int` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BEACH_PARTY_LOST_FOUND

**Hash:** `0xA51338E0DCCD4065` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_EARN_BEACH_PARTY_LOST_FOUND(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BEACH_PARTY_LOST_FOUND, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FROM_ISLAND_HEIST_DJ_MISSION

**Hash:** `0xE2BB399D90942091` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_EARN_FROM_ISLAND_HEIST_DJ_MISSION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FROM_ISLAND_HEIST_DJ_MISSION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CAR_CLUB_MEMBERSHIP

**Hash:** `0x1464E17207CD36E2` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CAR_CLUB_MEMBERSHIP(int amount1, Any p1, Any p2, int amount2, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CAR_CLUB_MEMBERSHIP, amount1, p1, p2, amount2, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount1` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `amount2` | `int` | `int` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CAR_CLUB_BAR

**Hash:** `0x925227803A0EAA1B` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CAR_CLUB_BAR(Any p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CAR_CLUB_BAR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_AUTOSHOP_MODIFY

**Hash:** `0x9BEA350D7C48061B` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_AUTOSHOP_MODIFY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_AUTOSHOP_MODIFY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_CAR_CLUB_TAKEOVER

**Hash:** `0xD1E46824E6FB92B5` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_CAR_CLUB_TAKEOVER(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_CAR_CLUB_TAKEOVER, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_AUTOSHOP

**Hash:** `0xEEB7E5D1FEB20869` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_AUTOSHOP(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_AUTOSHOP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_AUTOSHOP

**Hash:** `0xDD829AA198FDC46C` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_AUTOSHOP(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_AUTOSHOP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AUTOSHOP_BUSINESS

**Hash:** `0x36A7FD5A7194B03E` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_AUTOSHOP_BUSINESS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AUTOSHOP_BUSINESS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AUTOSHOP_INCOME

**Hash:** `0xC66D1CF99ED7FE25` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_AUTOSHOP_INCOME(Any p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AUTOSHOP_INCOME, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_CARCLUB_MEMBERSHIP

**Hash:** `0xBC6227792A188E2E` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_CARCLUB_MEMBERSHIP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_CARCLUB_MEMBERSHIP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DAILY_VEHICLE

**Hash:** `0x533073E8A596008C` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_DAILY_VEHICLE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DAILY_VEHICLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_DAILY_VEHICLE_BONUS

**Hash:** `0xFE65AFE7308E32B2` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_DAILY_VEHICLE_BONUS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_DAILY_VEHICLE_BONUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_TUNER_AWARD

**Hash:** `0xB846F547D3792DF6` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_TUNER_AWARD(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_TUNER_AWARD, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_TUNER_ROBBERY

**Hash:** `0xBCB266247193AC61` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_TUNER_ROBBERY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_TUNER_ROBBERY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_UPGRADE_AUTOSHOP

**Hash:** `0xC10322A8D3E061EE` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_EARN_UPGRADE_AUTOSHOP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_UPGRADE_AUTOSHOP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_INTERACTION_MENU_ABILITY

**Hash:** `0x93AA4165CB67E925` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_INTERACTION_MENU_ABILITY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_INTERACTION_MENU_ABILITY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SET_COMMON_FIELDS

**Hash:** `0xB9F7A469460E7A4A` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SET_COMMON_FIELDS(Any p0, Any p1, Any p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SET_COMMON_FIELDS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SET_DISCOUNT

**Hash:** `0x7E2F4E8F44CAF4E0` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SET_DISCOUNT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SET_DISCOUNT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUY_AGENCY

**Hash:** `0xEA8CD3C9B3C35884` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUY_AGENCY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUY_AGENCY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_UPGRADE_AGENCY

**Hash:** `0x6CCA64840589A3B6` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_UPGRADE_AGENCY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_UPGRADE_AGENCY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_AGENCY

**Hash:** `0x1B2120405080125C` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_AGENCY(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_AGENCY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_HIDDEN

**Hash:** `0xBF8793B91EA094A7` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_HIDDEN(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_HIDDEN, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SOURCE_BIKE

**Hash:** `0xD9DF467CBE4398C8` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SOURCE_BIKE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SOURCE_BIKE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_COMP_SUV

**Hash:** `0xD86581F9E7CDA383` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_COMP_SUV(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_COMP_SUV, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SUV_FST_TRVL

**Hash:** `0x61A2DF64ED2D396E` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SUV_FST_TRVL(int p0, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SUV_FST_TRVL, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_SUPPLY

**Hash:** `0xEBD482B82ACB8BAD` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_SUPPLY(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_SUPPLY, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BIKE_SHOP

**Hash:** `0x923AEA8E78F8DF0B` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BIKE_SHOP(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BIKE_SHOP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_VEHICLE_REQUESTED

**Hash:** `0x02D24A35A9CC3503` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SPEND_VEHICLE_REQUESTED(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_VEHICLE_REQUESTED, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_GUNRUNNING

**Hash:** `0x2CEB0E0BC2A77C05` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SPEND_GUNRUNNING(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_GUNRUNNING, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AGENCY_SAFE

**Hash:** `0x663B4B9D11742A12` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AGENCY_SAFE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AGENCY_SAFE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AWARD_CONTRACT

**Hash:** `0x146D4EB6D22A403F` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AWARD_CONTRACT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AWARD_CONTRACT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AGENCY_CONTRACT

**Hash:** `0x38482AD49CB905C7` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AGENCY_CONTRACT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AGENCY_CONTRACT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AWARD_PHONE

**Hash:** `0x7397A115030F1BE3` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AWARD_PHONE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AWARD_PHONE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AGENCY_PHONE

**Hash:** `0xE29F3D5FA63B1B82` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AGENCY_PHONE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AGENCY_PHONE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AWARD_FIXER_MISSION

**Hash:** `0x88D6C327D6C57C45` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AWARD_FIXER_MISSION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AWARD_FIXER_MISSION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FIXER_PREP

**Hash:** `0x6283E5DE4C4460C6` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_FIXER_PREP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FIXER_PREP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FIXER_FINALE

**Hash:** `0xBA154373C5FE51E8` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_FIXER_FINALE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FIXER_FINALE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FIXER_AGENCY_SHORT_TRIP

**Hash:** `0xF4A8E57460BF2037` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_FIXER_AGENCY_SHORT_TRIP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FIXER_AGENCY_SHORT_TRIP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_AWARD_SHORT_TRIP

**Hash:** `0x5B4DBDED84D6A420` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_AWARD_SHORT_TRIP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_AWARD_SHORT_TRIP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_FIXER_RIVAL_DELIVERY

**Hash:** `0x235D41210B3A1A5E` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_FIXER_RIVAL_DELIVERY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_FIXER_RIVAL_DELIVERY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_UPGRADE_AGENCY

**Hash:** `0xD07C7C3F1995108C` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_EARN_UPGRADE_AGENCY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_UPGRADE_AGENCY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_APARTMENT_UTILITIES

**Hash:** `0x1254B5B3925EFD3D` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_APARTMENT_UTILITIES(int amount, BOOL p1, BOOL p2, Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_APARTMENT_UTILITIES, amount, p1, p2, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_SPEND_BUSINESS_PROPERTY_FEES

**Hash:** `0x92D1CFDA1227FF1C` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SPEND_BUSINESS_PROPERTY_FEES(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SPEND_BUSINESS_PROPERTY_FEES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_SIGHTSEEING_REWARD

**Hash:** `0x45087AE480B233AC` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_EARN_SIGHTSEEING_REWARD(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_SIGHTSEEING_REWARD, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BIKER_SHOP

**Hash:** `0x2C5809EB9DF57257` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_EARN_BIKER_SHOP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BIKER_SHOP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_EARN_BIKER

**Hash:** `0x71BEC32FA466E105` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_EARN_BIKER(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EARN_BIKER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_YOHAN_SOURCE_GOODS

**Hash:** `0x59498BC8B1C8B15C` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_YOHAN_SOURCE_GOODS(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_YOHAN_SOURCE_GOODS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_BUY_MFGARAGE

**Hash:** `0xA2ED36DCF0FCA413` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_BUY_MFGARAGE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_BUY_MFGARAGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_UPGRADE_MFGARAGE

**Hash:** `0xCF8F346DDDC66643` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_UPGRADE_MFGARAGE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_UPGRADE_MFGARAGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_BUY_SUPPLIES

**Hash:** `0xB48185C0CA67B16B` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_BUY_SUPPLIES(int p0, BOOL p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_BUY_SUPPLIES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_BUY_ACID_LAB

**Hash:** `0xF1E26A7924327152` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_BUY_ACID_LAB(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_BUY_ACID_LAB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_UPGRADE_ACID_LAB_EQUIPMENT

**Hash:** `0x110EE9D486C23126` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_UPGRADE_ACID_LAB_EQUIPMENT(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_UPGRADE_ACID_LAB_EQUIPMENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_UPGRADE_ACID_LAB_ARMOR

**Hash:** `0xA7D541C9ACD63133` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_UPGRADE_ACID_LAB_ARMOR(int p0, BOOL p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_UPGRADE_ACID_LAB_ARMOR, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_UPGRADE_ACID_LAB_SCOOP

**Hash:** `0x2940558E05BCC2EC` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_UPGRADE_ACID_LAB_SCOOP(int p0, BOOL p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_UPGRADE_ACID_LAB_SCOOP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_UPGRADE_ACID_LAB_MINES

**Hash:** `0x4B99AB08C92C54E4` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_UPGRADE_ACID_LAB_MINES(int p0, BOOL p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_UPGRADE_ACID_LAB_MINES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_RENAME_ACID_LAB

**Hash:** `0x842B1C5AF61ACDE9` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_RENAME_ACID_LAB(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_RENAME_ACID_LAB, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPEND_RENAME_ACID_PRODUCT

**Hash:** `0x446798F7495DD7D8` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_SPEND_RENAME_ACID_PRODUCT(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPEND_RENAME_ACID_PRODUCT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_JUGGALO_MISSION

**Hash:** `0xDDF047577F1A02A7` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_JUGGALO_MISSION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_JUGGALO_MISSION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_ACID_LAB

**Hash:** `0xD1A8165767AD2D23` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_ACID_LAB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_ACID_LAB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_DAILY_STASH

**Hash:** `0xC30650FA74A19D02` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_DAILY_STASH(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_DAILY_STASH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_DEAD_DROP

**Hash:** `0xD01EBAEA1F905EF6` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_DEAD_DROP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_DEAD_DROP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_RANDOM_EVENT

**Hash:** `0xBEAFBB1B98B7EF55` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_RANDOM_EVENT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_RANDOM_EVENT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AWARD_TAXI

**Hash:** `0xA914768AD35CD3A5` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AWARD_TAXI(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AWARD_TAXI, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_STREET_DEALER

**Hash:** `0xA81017EE1324FDFE` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_STREET_DEALER(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_STREET_DEALER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_SELL_ACID

**Hash:** `0x79B656937DF6DF5D` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_SELL_ACID(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_SELL_ACID, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_SETUP_PARTICIPATION_ACID_LAB

**Hash:** `0xE3942D59E8A7F70D` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_SETUP_PARTICIPATION_ACID_LAB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_SETUP_PARTICIPATION_ACID_LAB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_SOURCE_PARTICIPATION_ACID_LAB

**Hash:** `0x136F11B5DF1B304D` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_SOURCE_PARTICIPATION_ACID_LAB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_SOURCE_PARTICIPATION_ACID_LAB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_SELL_PARTICIPATION_ACID_LAB

**Hash:** `0xCA3EF9B09A8D76B4` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_SELL_PARTICIPATION_ACID_LAB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_SELL_PARTICIPATION_ACID_LAB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_JUGGALO_STORY_MISSION

**Hash:** `0xE01D10BA8CD53621` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_JUGGALO_STORY_MISSION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_JUGGALO_STORY_MISSION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_JUGGALO_STORY_MISSION_PARTICIPATION

**Hash:** `0x40FF6CCCC476185C` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_JUGGALO_STORY_MISSION_PARTICIPATION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_JUGGALO_STORY_MISSION_PARTICIPATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_FOOLIGAN_JOB

**Hash:** `0xCE4452AE85F5E252` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_FOOLIGAN_JOB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_FOOLIGAN_JOB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** JUGGALO_PHONE_MISSION...

---

### _NETWORK_EARN_FOOLIGAN_JOB_PARTICIPATION

**Hash:** `0xC376B92D0E060970` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_FOOLIGAN_JOB_PARTICIPATION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_FOOLIGAN_JOB_PARTICIPATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** JUGGALO_PHONE_MISSION_PARTICIPATION...

---

### _NETWORK_EARN_TAXI_JOB

**Hash:** `0x991E1588FAD9019D` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_TAXI_JOB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_TAXI_JOB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_DAILY_STASH_HOUSE_COMPLETED

**Hash:** `0xCABC9874AFA70D6D` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_DAILY_STASH_HOUSE_COMPLETED(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_DAILY_STASH_HOUSE_COMPLETED, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_DAILY_STASH_HOUSE_PARTICIPATION

**Hash:** `0x9C0C6BD0F94CE391` | **Build:** `2802`

**C++ 原型:**
```cpp
void _NETWORK_EARN_DAILY_STASH_HOUSE_PARTICIPATION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_DAILY_STASH_HOUSE_PARTICIPATION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_AVENGER

**Hash:** `0x55F006B9D4A46C1D` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_EARN_AVENGER(int amount, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_AVENGER, amount, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Used for SERVICE_EARN_AVENGER_OPERATIONS & SERVICE_EARN_AVENGER_OPS_BONUS

---

### _NETWORK_EARN_SMUGGLER_OPS

**Hash:** `0xDEA273D5F8A9661A` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_EARN_SMUGGLER_OPS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_SMUGGLER_OPS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_BONUS_OBJECTIVE

**Hash:** `0xDCEF983C24191997` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_EARN_BONUS_OBJECTIVE(int amount, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_BONUS_OBJECTIVE, amount, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_PROGRESS_HUB

**Hash:** `0xF8332B06F0EECC9C` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_EARN_PROGRESS_HUB(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_PROGRESS_HUB, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPENT_AIR_FREIGHT

**Hash:** `0x8C7E8D6F96C9E948` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_SPENT_AIR_FREIGHT(int hangarCargoSourcingPrice, BOOL fromBank, BOOL fromBankAndWallet, int cost, int warehouseId, int warehouseSlot, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPENT_AIR_FREIGHT, hangarCargoSourcingPrice, fromBank, fromBankAndWallet, cost, warehouseId, warehouseSlot, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hangarCargoSourcingPrice` | `int` | `int` | |
| `fromBank` | `BOOL` | `bool` | |
| `fromBankAndWallet` | `BOOL` | `bool` | |
| `cost` | `int` | `int` | |
| `warehouseId` | `int` | `int` | |
| `warehouseSlot` | `int` | `int` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPENT_SKIP_CARGO_SOURCE_SETUP

**Hash:** `0xED1B407BADA42CEC` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_SPENT_SKIP_CARGO_SOURCE_SETUP(int amount, BOOL fromBank, BOOL fromBankAndWallet, int cost)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPENT_SKIP_CARGO_SOURCE_SETUP, amount, fromBank, fromBankAndWallet, cost);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `fromBank` | `BOOL` | `bool` | |
| `fromBankAndWallet` | `BOOL` | `bool` | |
| `cost` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _NETWORK_SPENT_STEALTH_MODULE

**Hash:** `0x95CE79A6939C537A` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_SPENT_STEALTH_MODULE(int amount, BOOL fromBank, BOOL fromBankAndWallet, Hash p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPENT_STEALTH_MODULE, amount, fromBank, fromBankAndWallet, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `fromBank` | `BOOL` | `bool` | |
| `fromBankAndWallet` | `BOOL` | `bool` | |
| `p3` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Hash p3 = STEALTH_MODULE

---

### _NETWORK_SPENT_MISSILE_JAMMER

**Hash:** `0xD687100F616163F4` | **Build:** `2944`

**C++ 原型:**
```cpp
void _NETWORK_SPENT_MISSILE_JAMMER(int amount, BOOL fromBank, BOOL fromBankAndWallet, Hash p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPENT_MISSILE_JAMMER, amount, fromBank, fromBankAndWallet, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `fromBank` | `BOOL` | `bool` | |
| `fromBankAndWallet` | `BOOL` | `bool` | |
| `p3` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Hash p3 = MISSILE_JAMMER

---

### _NETWORK_SPENT_GENERIC

**Hash:** `0x2803B027479FB640` | **Build:** `3095`

**C++ 原型:**
```cpp
void _NETWORK_SPENT_GENERIC(int price, BOOL p1, BOOL p2, Hash stat, Hash spent, const char* p5, const char* p6, Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SPENT_GENERIC, price, p1, p2, stat, spent, p5, p6, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `price` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `stat` | `Hash` | `uint` | |
| `spent` | `Hash` | `uint` | |
| `p5` | `const char*` | `string` | |
| `p6` | `const char*` | `string` | |
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _NETWORK_EARN_GENERIC

**Hash:** `0xBF7B5BB7ED890380` | **Build:** `3095`

**C++ 原型:**
```cpp
void _NETWORK_EARN_GENERIC(int amount, Hash earn, const char* p2, const char* p3, Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_EARN_GENERIC, amount, earn, p2, p3, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `earn` | `Hash` | `uint` | |
| `p2` | `const char*` | `string` | |
| `p3` | `const char*` | `string` | |
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** _NETWORK_EARN_G*

---

### _NETWORK_CLEAR_TRANSACTION_TELEMETRY_NONCE

**Hash:** `0xE03B9F95556E48E9` | **Build:** `3095`

**C++ 原型:**
```cpp
void _NETWORK_CLEAR_TRANSACTION_TELEMETRY_NONCE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_CLEAR_TRANSACTION_TELEMETRY_NONCE);
```

**返回值:** `void` → `void`

---

### NETWORK_GET_VC_BANK_BALANCE

**Hash:** `0x76EF28DA05EA395A` | **JHash:** `0x16184FB5` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_VC_BANK_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_VC_BANK_BALANCE);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_VC_WALLET_BALANCE

**Hash:** `0xA40F9C2623F6A8B5` | **JHash:** `0x4F5B781C` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_VC_WALLET_BALANCE(int characterSlot)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_VC_WALLET_BALANCE, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_VC_BALANCE

**Hash:** `0x5CBAD97E059E1B94` | **JHash:** `0xADF8F882` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_VC_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_VC_BALANCE);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_EVC_BALANCE

**Hash:** `0x5D1E75F91C07DEE5` | **JHash:** `0xF1EDB7F4` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_EVC_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_EVC_BALANCE);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_PVC_BALANCE

**Hash:** `0x4F54F3B6C202FB4E` | **JHash:** `0x56485B56` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PVC_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PVC_BALANCE);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_STRING_WALLET_BALANCE

**Hash:** `0xF9B10B529DCFB33B` | **JHash:** `0xC1A11435` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_STRING_WALLET_BALANCE(int characterSlot)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_STRING_WALLET_BALANCE, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_GET_STRING_BANK_BALANCE

**Hash:** `0xA6FA3979BED01B81` | **JHash:** `0xAA7EA3BD` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_STRING_BANK_BALANCE(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_STRING_BANK_BALANCE);
```

**返回值:** `const char*` → `string`

---

### NETWORK_GET_STRING_BANK_WALLET_BALANCE

**Hash:** `0x700AF71AE615E6DD` | **JHash:** `0x7D4FD28C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_STRING_BANK_WALLET_BALANCE(int character)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_STRING_BANK_WALLET_BALANCE, character);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `character` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_GET_CAN_SPEND_FROM_WALLET

**Hash:** `0xED5AB8860415BABA` | **JHash:** `0x0EA2188C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_CAN_SPEND_FROM_WALLET(int amount, int characterSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_CAN_SPEND_FROM_WALLET, amount, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if wallet balance >= amount.

---

### NETWORK_GET_CAN_SPEND_FROM_BANK

**Hash:** `0xA31FD6A0865B6D14` | **JHash:** `0xABED6020` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_CAN_SPEND_FROM_BANK(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_CAN_SPEND_FROM_BANK, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if bank balance >= amount.

---

### NETWORK_GET_CAN_SPEND_FROM_BANK_AND_WALLET

**Hash:** `0xDC18531D7019A535` | **JHash:** `0xCA23FD25` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_CAN_SPEND_FROM_BANK_AND_WALLET(int amount, int characterSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_CAN_SPEND_FROM_BANK_AND_WALLET, amount, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if bank balance + wallet balance >= amount.

---

### NETWORK_GET_PVC_TRANSFER_BALANCE

**Hash:** `0x13A8DE2FD77D04F3` | **JHash:** `0xC670C814` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PVC_TRANSFER_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PVC_TRANSFER_BALANCE);
```

**返回值:** `int` → `int`

**说明:** Retturns the same value as NETWORK_GET_REMAINING_TRANSFER_BALANCE.

---

### NETWORK_GET_CAN_TRANSFER_CASH

**Hash:** `0x08E8EEADFD0DC4A0` | **JHash:** `0x03461981` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_CAN_TRANSFER_CASH(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_CAN_TRANSFER_CASH, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns false if amount > wallet balance or daily transfer limit has been hit.

---

### NETWORK_CAN_RECEIVE_PLAYER_CASH

**Hash:** `0x5D17BE59D2123284` | **JHash:** `0x41F5F10E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_RECEIVE_PLAYER_CASH(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_RECEIVE_PLAYER_CASH, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_REMAINING_TRANSFER_BALANCE

**Hash:** `0xEA560AC9EEB1E19B` | **JHash:** `0xA9021211` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_REMAINING_TRANSFER_BALANCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_REMAINING_TRANSFER_BALANCE);
```

**返回值:** `int` → `int`

**说明:** Returns the same value as NETWORK_GET_PVC_TRANSFER_BALANCE.

---

### WITHDRAW_VC

**Hash:** `0xF70EFA14FE091429` | **JHash:** `0x8B755993` | **Build:** `323`

**C++ 原型:**
```cpp
int WITHDRAW_VC(int amount)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.WITHDRAW_VC, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Does nothing and always returns 0.

---

### DEPOSIT_VC

**Hash:** `0xE260E0BB9CD995AC` | **JHash:** `0x8F266745` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DEPOSIT_VC(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DEPOSIT_VC, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Does nothing and always returns false.

---

### HAS_VC_WITHDRAWAL_COMPLETED

**Hash:** `0xE154B48B68EF72BC` | **JHash:** `0x531E4892` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_VC_WITHDRAWAL_COMPLETED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VC_WITHDRAWAL_COMPLETED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### WAS_VC_WITHDRAWAL_SUCCESSFUL

**Hash:** `0x6FCF8DDEA146C45B` | **JHash:** `0xB96C7ABE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_VC_WITHDRAWAL_SUCCESSFUL(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_VC_WITHDRAWAL_SUCCESSFUL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---


---

## NETSHOPPING

**包含 41 个 Native 函数**

### NET_GAMESERVER_USE_SERVER_TRANSACTIONS

**Hash:** `0x7D2708796355B20B` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_USE_SERVER_TRANSACTIONS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_USE_SERVER_TRANSACTIONS);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_CATALOG_ITEM_IS_VALID

**Hash:** `0xBD4D7EAF8A30F637` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CATALOG_ITEM_IS_VALID(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CATALOG_ITEM_IS_VALID, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_CATALOG_ITEM_KEY_IS_VALID

**Hash:** `0x247F0F73A182EA0B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CATALOG_ITEM_KEY_IS_VALID(Hash hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CATALOG_ITEM_KEY_IS_VALID, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_GET_PRICE

**Hash:** `0xC27009422FCCA88D` | **Build:** `323`

**C++ 原型:**
```cpp
int NET_GAMESERVER_GET_PRICE(Hash itemHash, Hash categoryHash, BOOL p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_GAMESERVER_GET_PRICE, itemHash, categoryHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemHash` | `Hash` | `uint` | |
| `categoryHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** bool is always true in game scripts

---

### NET_GAMESERVER_CATALOG_IS_VALID

**Hash:** `0x3C4487461E9B0DCB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CATALOG_IS_VALID(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CATALOG_IS_VALID);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_IS_CATALOG_CURRENT

**Hash:** `0x2B949A1E6AEC8F6A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_IS_CATALOG_CURRENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_IS_CATALOG_CURRENT);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_GET_CATALOG_CLOUD_CRC

**Hash:** `0x85F6C9ABA1DE2BCF` | **Build:** `323`

**C++ 原型:**
```cpp
Hash NET_GAMESERVER_GET_CATALOG_CLOUD_CRC(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.NET_GAMESERVER_GET_CATALOG_CLOUD_CRC);
```

**返回值:** `Hash` → `uint`

---

### NET_GAMESERVER_REFRESH_SERVER_CATALOG

**Hash:** `0x357B152EF96C30B6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_REFRESH_SERVER_CATALOG(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_REFRESH_SERVER_CATALOG);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_RETRIEVE_CATALOG_REFRESH_STATUS

**Hash:** `0xCF38DAFBB49EDE5E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_RETRIEVE_CATALOG_REFRESH_STATUS(int* state)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_RETRIEVE_CATALOG_REFRESH_STATUS, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `state` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_INIT_SESSION

**Hash:** `0xE3E5A7C64CA2C6ED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_INIT_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_INIT_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_RETRIEVE_INIT_SESSION_STATUS

**Hash:** `0x0395CB47B022E62C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_RETRIEVE_INIT_SESSION_STATUS(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_RETRIEVE_INIT_SESSION_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_START_SESSION

**Hash:** `0xA135AC892A58FC07` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_START_SESSION(int charSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_START_SESSION, charSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `charSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_START_SESSION_PENDING

**Hash:** `0x72EB7BA9B69BF6AB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_START_SESSION_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_START_SESSION_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_RETRIEVE_START_SESSION_STATUS

**Hash:** `0x170910093218C8B9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_RETRIEVE_START_SESSION_STATUS(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_RETRIEVE_START_SESSION_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_RETRIEVE_SESSION_ERROR_CODE

**Hash:** `0xC13C38E47EA5DF31` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_RETRIEVE_SESSION_ERROR_CODE(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_RETRIEVE_SESSION_ERROR_CODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_IS_SESSION_VALID

**Hash:** `0xB24F0944DA203D9E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_IS_SESSION_VALID(int charSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_IS_SESSION_VALID, charSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `charSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_CLEAR_SESSION

**Hash:** `0x74A0FD0688F1EE45` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CLEAR_SESSION(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CLEAR_SESSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_SESSION_APPLY_RECEIVED_DATA

**Hash:** `0x2F41D51BA3BCD1F1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_SESSION_APPLY_RECEIVED_DATA(int charSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_SESSION_APPLY_RECEIVED_DATA, charSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `charSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_IS_SESSION_REFRESH_PENDING

**Hash:** `0x810E8431C0614BF9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_IS_SESSION_REFRESH_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_IS_SESSION_REFRESH_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_START_SESSION_RESTART

**Hash:** `0x35A1B3E1D1315CFA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_START_SESSION_RESTART(BOOL inventory, BOOL playerbalance)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_START_SESSION_RESTART, inventory, playerbalance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `inventory` | `BOOL` | `bool` | |
| `playerbalance` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Note: only one of the arguments can be set to true at a time

---

### NET_GAMESERVER_TRANSACTION_IN_PROGRESS

**Hash:** `0x613F125BA3BD2EB9` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_TRANSACTION_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_TRANSACTION_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_GET_SESSION_STATE_AND_STATUS

**Hash:** `0x897433D292B44130` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_GET_SESSION_STATE_AND_STATUS(int* p0, BOOL* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_GET_SESSION_STATE_AND_STATUS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |
| `p1` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_START

**Hash:** `0x279F08B1A4B29B7E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_START(int* transactionId, Hash categoryHash, Hash actionHash, int flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_START, transactionId, categoryHash, actionHash, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int*` | `int*` | |
| `categoryHash` | `Hash` | `uint` | |
| `actionHash` | `Hash` | `uint` | |
| `flags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_END

**Hash:** `0xFA336E7F40C0A0D0` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_END(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_END);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_IS_ACTIVE

**Hash:** `0xA65568121DF2EA26` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_IS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_IS_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_ADD_ITEM

**Hash:** `0xF30980718C8ED876` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_ADD_ITEM(Any* itemData, int quantity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_ADD_ITEM, itemData, quantity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `itemData` | `Any*` | `object*` | |
| `quantity` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_IS_FULL

**Hash:** `0x27F76CC6C55AD30E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_IS_FULL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_IS_FULL);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BASKET_APPLY_SERVER_DATA

**Hash:** `0xE1A0450ED46A7812` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BASKET_APPLY_SERVER_DATA(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BASKET_APPLY_SERVER_DATA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_CHECKOUT_START

**Hash:** `0x39BE7CEA8D9CC8E6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CHECKOUT_START(int transactionId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CHECKOUT_START, transactionId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_CHECKOUT_PENDING

**Hash:** `0x4B64CD6D18474126` | **Build:** `3570`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_CHECKOUT_PENDING(int transactionId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_CHECKOUT_PENDING, transactionId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_BEGIN_SERVICE

**Hash:** `0x3C5FD37B5499582E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_BEGIN_SERVICE(int* transactionId, Hash categoryHash, Hash itemHash, Hash actionTypeHash, int value, int flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_BEGIN_SERVICE, transactionId, categoryHash, itemHash, actionTypeHash, value, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int*` | `int*` | |
| `categoryHash` | `Hash` | `uint` | |
| `itemHash` | `Hash` | `uint` | |
| `actionTypeHash` | `Hash` | `uint` | |
| `value` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_END_SERVICE

**Hash:** `0xE2A99A9B524BEFFF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_END_SERVICE(int transactionId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_END_SERVICE, transactionId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_DELETE_CHARACTER

**Hash:** `0x51F1A8E48C3D2F6D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_DELETE_CHARACTER(int slot, BOOL transfer, Hash reason)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_DELETE_CHARACTER, slot, transfer, reason);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `slot` | `int` | `int` | |
| `transfer` | `BOOL` | `bool` | |
| `reason` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_DELETE_CHARACTER_GET_STATUS

**Hash:** `0x0A6D923DFFC9BD89` | **Build:** `323`

**C++ 原型:**
```cpp
int NET_GAMESERVER_DELETE_CHARACTER_GET_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_GAMESERVER_DELETE_CHARACTER_GET_STATUS);
```

**返回值:** `int` → `int`

---

### NET_GAMESERVER_DELETE_SET_TELEMETRY_NONCE_SEED

**Hash:** `0x112CEF1615A1139F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_DELETE_SET_TELEMETRY_NONCE_SEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_DELETE_SET_TELEMETRY_NONCE_SEED);
```

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_TRANSFER_BANK_TO_WALLET

**Hash:** `0xD47A2C1BA117471D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_TRANSFER_BANK_TO_WALLET(int charSlot, int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_TRANSFER_BANK_TO_WALLET, charSlot, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `charSlot` | `int` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_TRANSFER_WALLET_TO_BANK

**Hash:** `0xC2F7FE5309181C7D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_TRANSFER_WALLET_TO_BANK(int charSlot, int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_TRANSFER_WALLET_TO_BANK, charSlot, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `charSlot` | `int` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NET_GAMESERVER_TRANSFER_BANK_TO_WALLET_GET_STATUS

**Hash:** `0x23789E777D14CE44` | **Build:** `323`

**C++ 原型:**
```cpp
int NET_GAMESERVER_TRANSFER_BANK_TO_WALLET_GET_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_GAMESERVER_TRANSFER_BANK_TO_WALLET_GET_STATUS);
```

**返回值:** `int` → `int`

**说明:** Same as 0x350AA5EBC03D3BD2

---

### NET_GAMESERVER_TRANSFER_WALLET_TO_BANK_GET_STATUS

**Hash:** `0x350AA5EBC03D3BD2` | **Build:** `323`

**C++ 原型:**
```cpp
int NET_GAMESERVER_TRANSFER_WALLET_TO_BANK_GET_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_GAMESERVER_TRANSFER_WALLET_TO_BANK_GET_STATUS);
```

**返回值:** `int` → `int`

**说明:** Same as 0x23789E777D14CE44

---

### NET_GAMESERVER_TRANSFER_CASH_SET_TELEMETRY_NONCE_SEED

**Hash:** `0x498C1E05CE5F7877` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_TRANSFER_CASH_SET_TELEMETRY_NONCE_SEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_TRANSFER_CASH_SET_TELEMETRY_NONCE_SEED);
```

**返回值:** `BOOL` → `bool`

**说明:** Used to be NETWORK_SHOP_CASH_TRANSFER_SET_TELEMETRY_NONCE_SEED

---

### NET_GAMESERVER_SET_TELEMETRY_NONCE_SEED

**Hash:** `0x9507D4271988E1AE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NET_GAMESERVER_SET_TELEMETRY_NONCE_SEED(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NET_GAMESERVER_SET_TELEMETRY_NONCE_SEED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---


---

## NETWORK

**包含 878 个 Native 函数**

### GET_ONLINE_VERSION

**Hash:** `0xFCA9373EF340AC0A` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_ONLINE_VERSION(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_ONLINE_VERSION);
```

**返回值:** `const char*` → `string`

**说明:** Online version is defined here: update\update.rpf\common\data\version.txt

Example:

[ONLINE_VERSION_NUMBER]
1.33

_GET_ONLINE_VERSION() will return "1.33"

---

### NETWORK_IS_SIGNED_IN

**Hash:** `0x054354A99211EB96` | **JHash:** `0xADD0B40F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SIGNED_IN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SIGNED_IN);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the player is signed into Social Club.

---

### NETWORK_IS_SIGNED_ONLINE

**Hash:** `0x1077788E268557C2` | **JHash:** `0x6E5BDCE2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SIGNED_ONLINE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SIGNED_ONLINE);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the game is not in offline mode.

seemed not to work for some ppl

---

### NETWORK_IS_NP_AVAILABLE

**Hash:** `0xBD545D44CCE70597` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_NP_AVAILABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_NP_AVAILABLE);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### NETWORK_IS_NP_PENDING

**Hash:** `0xEBCAB9E5048434F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_NP_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_NP_PENDING);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### NETWORK_GET_NP_UNAVAILABLE_REASON

**Hash:** `0x74FB3E29E6D10FA9` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NP_UNAVAILABLE_REASON(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NP_UNAVAILABLE_REASON);
```

**返回值:** `int` → `int`

**说明:** Hardcoded to return zero.

==== PS4 specific info ====

Returns some sort of unavailable reason:
-1 = REASON_INVALID
 0 = REASON_OTHER
 1 = REASON_SYSTEM_UPDATE
 2 = REASON_GAME_UPDATE
 3 = REASON_SIGNED_OUT
 4 = REASON_AGE
 5 = REASON_CONNECTION

=================================

---

### NETWORK_IS_CONNETED_TO_NP_PRESENCE

**Hash:** `0x7808619F31FF22DB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CONNETED_TO_NP_PRESENCE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CONNETED_TO_NP_PRESENCE);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### NETWORK_IS_LOGGED_IN_TO_PSN

**Hash:** `0xA0FA4EC6A05DA44E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_LOGGED_IN_TO_PSN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_LOGGED_IN_TO_PSN);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### NETWORK_HAS_VALID_ROS_CREDENTIALS

**Hash:** `0x85443FF4C328F53B` | **JHash:** `0x3FB40673` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_VALID_ROS_CREDENTIALS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_VALID_ROS_CREDENTIALS);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the signed-in user has valid Rockstar Online Services (ROS) credentials.

---

### NETWORK_IS_REFRESHING_ROS_CREDENTIALS

**Hash:** `0x8D11E61A4ABF49CC` | **JHash:** `0xBDC8F63E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_REFRESHING_ROS_CREDENTIALS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_REFRESHING_ROS_CREDENTIALS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_CLOUD_AVAILABLE

**Hash:** `0x9A4CF4F48AD77302` | **JHash:** `0xC7FF5AFC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CLOUD_AVAILABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CLOUD_AVAILABLE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_SOCIAL_CLUB_ACCOUNT

**Hash:** `0x67A5589628E0CFF6` | **JHash:** `0x66EC713F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_SOCIAL_CLUB_ACCOUNT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_SOCIAL_CLUB_ACCOUNT);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ARE_SOCIAL_CLUB_POLICIES_CURRENT

**Hash:** `0xBA9775570DB788CF` | **JHash:** `0x358D1D77` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ARE_SOCIAL_CLUB_POLICIES_CURRENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ARE_SOCIAL_CLUB_POLICIES_CURRENT);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_HOST

**Hash:** `0x8DB296B814EDDA07` | **JHash:** `0xE46AC10F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_HOST(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_HOST);
```

**返回值:** `BOOL` → `bool`

**说明:** If you are host, returns true else returns false.

---

### NETWORK_GET_HOST_PLAYER_INDEX

**Hash:** `0x8251FB94DC4FDFC8` | **Build:** `2612`

**C++ 原型:**
```cpp
Player NETWORK_GET_HOST_PLAYER_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_HOST_PLAYER_INDEX);
```

**返回值:** `Player` → `int`

---

### NETWORK_WAS_GAME_SUSPENDED

**Hash:** `0x4237E822315D8BA9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_WAS_GAME_SUSPENDED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_WAS_GAME_SUSPENDED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ONLINE_PRIVILEGES

**Hash:** `0x25CB5A9F37BFD063` | **JHash:** `0xEF63BFDF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ONLINE_PRIVILEGES(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ONLINE_PRIVILEGES);
```

**返回值:** `BOOL` → `bool`

---

### _NETWORK_MULTIPLAYER_CROSSPLAY_NOT_ALLOWED

**Hash:** `0xA6BC0D9BCF9662FA` | **Build:** `3504`

**C++ 原型:**
```cpp
BOOL _NETWORK_MULTIPLAYER_CROSSPLAY_NOT_ALLOWED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_MULTIPLAYER_CROSSPLAY_NOT_ALLOWED);
```

**返回值:** `BOOL` → `bool`

**说明:** For the XboxPC version this returns true if XPRIVILEGE_MULTIPLAYER_SESSIONS is granted but XPRIVILEGE_CROSS_PLAY is NOT granted.
For more information, see https://learn.microsoft.com/en-us/gaming/gdk/_content/gc/live/features/identity/privileges/concepts/live-user-privileges-client#privilege-ids-to-check

Always returns false for non-XboxPC versions.

---

### NETWORK_HAS_AGE_RESTRICTIONS

**Hash:** `0x1353F87E89946207` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_AGE_RESTRICTIONS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_AGE_RESTRICTIONS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_USER_CONTENT_PRIVILEGES

**Hash:** `0x72D918C99BCACC54` | **JHash:** `0x1F88819D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_USER_CONTENT_PRIVILEGES(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_USER_CONTENT_PRIVILEGES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_COMMUNICATION_PRIVILEGES

**Hash:** `0xAEEF48CDF5B6CE7C` | **JHash:** `0x2D817A5E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_COMMUNICATION_PRIVILEGES(int p0, Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_COMMUNICATION_PRIVILEGES, p0, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _NETWORK_HAVE_PLATFORM_COMMUNICATION_PRIVILEGES

**Hash:** `0xE1E02509169C124E` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_HAVE_PLATFORM_COMMUNICATION_PRIVILEGES(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_HAVE_PLATFORM_COMMUNICATION_PRIVILEGES);
```

**返回值:** `BOOL` → `bool`

**说明:** Appears to be PlayStation-specific. Always returns true on other platforms if signed in with the primary user profile

---

### NETWORK_CHECK_ONLINE_PRIVILEGES

**Hash:** `0x78321BEA235FD8CD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_ONLINE_PRIVILEGES(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_ONLINE_PRIVILEGES, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CHECK_USER_CONTENT_PRIVILEGES

**Hash:** `0x595F028698072DD9` | **JHash:** `0xBB54AA3D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_USER_CONTENT_PRIVILEGES(int p0, int p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_USER_CONTENT_PRIVILEGES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CHECK_COMMUNICATION_PRIVILEGES

**Hash:** `0x83F28CE49FBBFFBA` | **JHash:** `0xA79E5B05` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_COMMUNICATION_PRIVILEGES(int p0, int p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_COMMUNICATION_PRIVILEGES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CHECK_TEXT_COMMUNICATION_PRIVILEGES

**Hash:** `0x07EAB372C8841D99` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_TEXT_COMMUNICATION_PRIVILEGES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_TEXT_COMMUNICATION_PRIVILEGES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_USING_ONLINE_PROMOTION

**Hash:** `0x906CA41A4B74ECA4` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_USING_ONLINE_PROMOTION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_USING_ONLINE_PROMOTION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SHOULD_SHOW_PROMOTION_ALERT_SCREEN

**Hash:** `0x023ACAB2DC9DC4A4` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL NETWORK_SHOULD_SHOW_PROMOTION_ALERT_SCREEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SHOULD_SHOW_PROMOTION_ALERT_SCREEN);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_SOCIAL_NETWORKING_SHARING_PRIV

**Hash:** `0x76BF03FADBF154F5` | **JHash:** `0xEB864288` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_SOCIAL_NETWORKING_SHARING_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_SOCIAL_NETWORKING_SHARING_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_AGE_GROUP

**Hash:** `0x9614B71F8ADB982B` | **JHash:** `0xAB07CBBE` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_AGE_GROUP(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_AGE_GROUP);
```

**返回值:** `int` → `int`

---

### NETWORK_CHECK_PRIVILEGES

**Hash:** `0x0CF6CC51AA18F0F8` | **Build:** `1180`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_PRIVILEGES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_PRIVILEGES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PRIVILEGE_CHECK_IN_PROGRESS

**Hash:** `0x64E5C4CC82847B73` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PRIVILEGE_CHECK_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PRIVILEGE_CHECK_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return false.

---

### NETWORK_SET_PRIVILEGE_CHECK_RESULT_NOT_NEEDED

**Hash:** `0x1F7BC3539F9E0224` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_SET_PRIVILEGE_CHECK_RESULT_NOT_NEEDED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_PRIVILEGE_CHECK_RESULT_NOT_NEEDED);
```

**返回值:** `void` → `void`

---

### NETWORK_RESOLVE_PRIVILEGE_USER_CONTENT

**Hash:** `0xDE9225854F37BF72` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL NETWORK_RESOLVE_PRIVILEGE_USER_CONTENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_RESOLVE_PRIVILEGE_USER_CONTENT);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return true.

---

### NETWORK_HAVE_PLATFORM_SUBSCRIPTION

**Hash:** `0x5EA784D197556507` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_PLATFORM_SUBSCRIPTION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_PLATFORM_SUBSCRIPTION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLATFORM_SUBSCRIPTION_CHECK_PENDING

**Hash:** `0xA8ACB6459542A8C8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLATFORM_SUBSCRIPTION_CHECK_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLATFORM_SUBSCRIPTION_CHECK_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SHOW_ACCOUNT_UPGRADE_UI

**Hash:** `0x83FE8D7229593017` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SHOW_ACCOUNT_UPGRADE_UI(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SHOW_ACCOUNT_UPGRADE_UI);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_SHOWING_SYSTEM_UI_OR_RECENTLY_REQUESTED_UPSELL

**Hash:** `0x7788DFE15016A182` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SHOWING_SYSTEM_UI_OR_RECENTLY_REQUESTED_UPSELL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SHOWING_SYSTEM_UI_OR_RECENTLY_REQUESTED_UPSELL);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_NEED_TO_START_NEW_GAME_BUT_BLOCKED

**Hash:** `0x53C10C8BD774F2C9` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL NETWORK_NEED_TO_START_NEW_GAME_BUT_BLOCKED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_NEED_TO_START_NEW_GAME_BUT_BLOCKED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_BAIL

**Hash:** `0x580CE4438479CC61` | **JHash:** `0x60E1FEDF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_BAIL(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_BAIL);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_BAIL

**Hash:** `0x95914459A87EBA28` | **JHash:** `0x87D79A04` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BAIL(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BAIL, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_ON_RETURN_TO_SINGLE_PLAYER

**Hash:** `0x283B6062A2C01E9B` | **JHash:** `0x96E28FE2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ON_RETURN_TO_SINGLE_PLAYER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ON_RETURN_TO_SINGLE_PLAYER);
```

**返回值:** `void` → `void`

---

### NETWORK_TRANSITION_START

**Hash:** `0x8B4FFC790CA131EF` | **Build:** `617`

**C++ 原型:**
```cpp
BOOL NETWORK_TRANSITION_START(int p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TRANSITION_START, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_TRANSITION_ADD_STAGE

**Hash:** `0xC3BFED92026A2AAD` | **Build:** `617`

**C++ 原型:**
```cpp
BOOL NETWORK_TRANSITION_ADD_STAGE(Hash hash, int p1, int p2, int state, int p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TRANSITION_ADD_STAGE, hash, p1, p2, state, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `state` | `int` | `int` | |
| `p4` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_TRANSITION_FINISH

**Hash:** `0x04918A41BC9B8157` | **Build:** `617`

**C++ 原型:**
```cpp
BOOL NETWORK_TRANSITION_FINISH(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TRANSITION_FINISH, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_ACCESS_MULTIPLAYER

**Hash:** `0xAF50DA1A3F8B1BA4` | **JHash:** `0xA520B982` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_ACCESS_MULTIPLAYER(int* loadingState)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_ACCESS_MULTIPLAYER, loadingState);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `loadingState` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** 11 - Need to download tunables.
12 - Need to download background script.

Returns 1 if the multiplayer is loaded, otherwhise 0.

---

### NETWORK_CHECK_CAN_ACCESS_AND_ALERT

**Hash:** `0x6FA9825D0B5A721B` | **Build:** `3570`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_CAN_ACCESS_AND_ALERT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_CAN_ACCESS_AND_ALERT);
```

**返回值:** `BOOL` → `bool`

---

### _NETWORK_GET_ACCESS_CODE_LABEL_HEADING

**Hash:** `0x967E6FB554E1B6DE` | **Build:** `3570`

**C++ 原型:**
```cpp
const char* _NETWORK_GET_ACCESS_CODE_LABEL_HEADING(int accessCode)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_GET_ACCESS_CODE_LABEL_HEADING, accessCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `accessCode` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### _NETWORK_GET_ACCESS_CODE_LABEL_BODY

**Hash:** `0x214CA1730793EBA8` | **Build:** `3570`

**C++ 原型:**
```cpp
const char* _NETWORK_GET_ACCESS_CODE_LABEL_BODY(int accessCode)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_GET_ACCESS_CODE_LABEL_BODY, accessCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `accessCode` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_IS_MULTIPLAYER_DISABLED

**Hash:** `0x9747292807126EDA` | **JHash:** `0x05518C0F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_MULTIPLAYER_DISABLED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_MULTIPLAYER_DISABLED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_ENTER_MULTIPLAYER

**Hash:** `0x7E782A910C362C25` | **JHash:** `0x4A23B9C9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_ENTER_MULTIPLAYER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_ENTER_MULTIPLAYER);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_DO_FREEROAM_QUICKMATCH

**Hash:** `0x330ED4D05491934F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_DO_FREEROAM_QUICKMATCH(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_DO_FREEROAM_QUICKMATCH, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_DO_FRIEND_MATCHMAKING

**Hash:** `0x2CFC76E0D087C994` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_DO_FRIEND_MATCHMAKING(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_DO_FRIEND_MATCHMAKING, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_DO_CREW_MATCHMAKING

**Hash:** `0x94BC51E9449D917F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_DO_CREW_MATCHMAKING(int crewId, int p1, int p2, int maxPlayers)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_DO_CREW_MATCHMAKING, crewId, p1, p2, maxPlayers);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `crewId` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `maxPlayers` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p4 seems to be unused in 1.60/build 2628

---

### NETWORK_SESSION_DO_ACTIVITY_QUICKMATCH

**Hash:** `0xBE3E347A87ACEB82` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_DO_ACTIVITY_QUICKMATCH(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_DO_ACTIVITY_QUICKMATCH, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_HOST

**Hash:** `0x6F3D4ED9BEE4E61D` | **JHash:** `0x6716460F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_HOST(int p0, int maxPlayers, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_HOST, p0, maxPlayers, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `maxPlayers` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Does nothing in online but in offline it will cause the screen to fade to black. Nothing happens past then, the screen will sit at black until you restart GTA. Other stuff must be needed to actually host a session.

---

### NETWORK_SESSION_HOST_CLOSED

**Hash:** `0xED34C0C02C098BB7` | **JHash:** `0x8AC9EA19` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_HOST_CLOSED(int p0, int maxPlayers)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_HOST_CLOSED, p0, maxPlayers);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `maxPlayers` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_HOST_FRIENDS_ONLY

**Hash:** `0xB9CFD27A5D578D83` | **JHash:** `0x26864403` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_HOST_FRIENDS_ONLY(int p0, int maxPlayers)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_HOST_FRIENDS_ONLY, p0, maxPlayers);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `maxPlayers` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Does nothing in online but in offline it will cause the screen to fade to black. Nothing happens past then, the screen will sit at black until you restart GTA. Other stuff must be needed to actually host a session.

---

### NETWORK_SESSION_IS_CLOSED_FRIENDS

**Hash:** `0xFBCFA2EA2E206890` | **JHash:** `0x56E75FE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_CLOSED_FRIENDS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_CLOSED_FRIENDS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_IS_CLOSED_CREW

**Hash:** `0x74732C6CA90DA2B4` | **JHash:** `0xA95299B9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_CLOSED_CREW(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_CLOSED_CREW);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_IS_SOLO

**Hash:** `0xF3929C2379B60CCE` | **JHash:** `0x3D2C1916` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_SOLO(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_SOLO);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_IS_PRIVATE

**Hash:** `0xCEF70AA5B3F89BA1` | **JHash:** `0xDB67785D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_PRIVATE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_PRIVATE);
```

**返回值:** `BOOL` → `bool`

---

### _NETWORK_SESSION_LEAVE_INCLUDING_REASON

**Hash:** `0xE0128328CF1FD9F4` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_SESSION_LEAVE_INCLUDING_REASON(int leaveFlags, int leaveReason)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_SESSION_LEAVE_INCLUDING_REASON, leaveFlags, leaveReason);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `leaveFlags` | `int` | `int` | |
| `leaveReason` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_LEAVE

**Hash:** `0xB9351A07A0D458B1` | **Build:** `2060`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_LEAVE(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_LEAVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_KICK_PLAYER

**Hash:** `0xFA8904DC5F304220` | **JHash:** `0x1E20138A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_KICK_PLAYER(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_KICK_PLAYER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** Only works as host.

---

### NETWORK_SESSION_GET_KICK_VOTE

**Hash:** `0xD6D09A6F32F49EF1` | **JHash:** `0x8A559D26` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_GET_KICK_VOTE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_GET_KICK_VOTE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_RESERVE_SLOTS_TRANSITION

**Hash:** `0x041C7F2A6C9894E6` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_RESERVE_SLOTS_TRANSITION(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_RESERVE_SLOTS_TRANSITION, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_JOIN_PREVIOUSLY_FAILED_SESSION

**Hash:** `0x59DF79317F85A7E0` | **JHash:** `0x6CDAA7D2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_JOIN_PREVIOUSLY_FAILED_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_JOIN_PREVIOUSLY_FAILED_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_JOIN_PREVIOUSLY_FAILED_TRANSITION

**Hash:** `0xFFE1E5B792D92B34` | **JHash:** `0xBF21818E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_JOIN_PREVIOUSLY_FAILED_TRANSITION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_JOIN_PREVIOUSLY_FAILED_TRANSITION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_SET_MATCHMAKING_GROUP

**Hash:** `0x49EC8030F5015F8B` | **JHash:** `0x3C3E2AB6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_MATCHMAKING_GROUP(int matchmakingGroup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_MATCHMAKING_GROUP, matchmakingGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `matchmakingGroup` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_MATCHMAKING_GROUP_MAX

**Hash:** `0x8B6A4DD0AF9CE215` | **JHash:** `0x5F29A7E0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_MATCHMAKING_GROUP_MAX(int playerType, int playerCount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_MATCHMAKING_GROUP_MAX, playerType, playerCount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `playerType` | `int` | `int` | |
| `playerCount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** playerType is an unsigned int from 0 to 4
0 = regular joiner
4 = spectator

---

### NETWORK_SESSION_GET_MATCHMAKING_GROUP_FREE

**Hash:** `0x56CE820830EF040B` | **JHash:** `0x36EAD960` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_SESSION_GET_MATCHMAKING_GROUP_FREE(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_SESSION_GET_MATCHMAKING_GROUP_FREE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_SESSION_ADD_ACTIVE_MATCHMAKING_GROUP

**Hash:** `0xCAE55F48D3D7875C` | **JHash:** `0x5BE529F7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_ADD_ACTIVE_MATCHMAKING_GROUP(int groupId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_ADD_ACTIVE_MATCHMAKING_GROUP, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** groupId range: [0, 4]

---

### NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT

**Hash:** `0xF49ABC20D8552257` | **JHash:** `0x454C7B67` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_GET_UNIQUE_CREW_LIMIT

**Hash:** `0xCDC936BF35EDCB73` | **Build:** `3095`

**C++ 原型:**
```cpp
int NETWORK_SESSION_GET_UNIQUE_CREW_LIMIT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_SESSION_GET_UNIQUE_CREW_LIMIT);
```

**返回值:** `int` → `int`

---

### NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT_TRANSITION

**Hash:** `0x4811BBAC21C5FCD5` | **JHash:** `0xE5961511` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT_TRANSITION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_UNIQUE_CREW_LIMIT_TRANSITION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_UNIQUE_CREW_ONLY_CREWS_TRANSITION

**Hash:** `0x5539C3EBF104A53A` | **JHash:** `0xAE396263` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_UNIQUE_CREW_ONLY_CREWS_TRANSITION(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_UNIQUE_CREW_ONLY_CREWS_TRANSITION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_CREW_LIMIT_MAX_MEMBERS_TRANSITION

**Hash:** `0x702BC4D605522539` | **JHash:** `0x913FD7D6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_CREW_LIMIT_MAX_MEMBERS_TRANSITION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_CREW_LIMIT_MAX_MEMBERS_TRANSITION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_MATCHMAKING_PROPERTY_ID

**Hash:** `0x3F52E880AAF6C8CA` | **JHash:** `0xB3D9A67F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_MATCHMAKING_PROPERTY_ID(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_MATCHMAKING_PROPERTY_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_MATCHMAKING_MENTAL_STATE

**Hash:** `0xF1EEA2DDA9FFA69D` | **JHash:** `0x6CC062FC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_MATCHMAKING_MENTAL_STATE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_MATCHMAKING_MENTAL_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 in the decompiled scripts is always the stat mesh_texblend * 0.07 to int

---

### NETWORK_SESSION_SET_NUM_BOSSES

**Hash:** `0x59D421683D31835A` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_NUM_BOSSES(int num)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_NUM_BOSSES, num);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `num` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_SCRIPT_VALIDATE_JOIN

**Hash:** `0x1153FA02A659051C` | **JHash:** `0x57F9BC83` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_SCRIPT_VALIDATE_JOIN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_SCRIPT_VALIDATE_JOIN);
```

**返回值:** `void` → `void`

---

### NETWORK_SESSION_VALIDATE_JOIN

**Hash:** `0xC19F6C8E7865A6FF` | **JHash:** `0xF3768F90` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_VALIDATE_JOIN(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_VALIDATE_JOIN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_ADD_FOLLOWERS

**Hash:** `0x236406F60CF216D6` | **JHash:** `0x0EC62629` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ADD_FOLLOWERS(int* p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_FOLLOWERS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** ..

---

### NETWORK_CLEAR_FOLLOWERS

**Hash:** `0x058F43EC59A8631A` | **JHash:** `0x5E557307` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_FOLLOWERS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_FOLLOWERS);
```

**返回值:** `void` → `void`

---

### NETWORK_GET_GLOBAL_MULTIPLAYER_CLOCK

**Hash:** `0x6D03BFBD643B2A02` | **JHash:** `0x74E8C53E` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_GLOBAL_MULTIPLAYER_CLOCK(int* hours, int* minutes, int* seconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_GLOBAL_MULTIPLAYER_CLOCK, hours, minutes, seconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hours` | `int*` | `int*` | |
| `minutes` | `int*` | `int*` | |
| `seconds` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_SET_GAMEMODE

**Hash:** `0x600F8CB31C7AAB6E` | **JHash:** `0x959E43A3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_SET_GAMEMODE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_SET_GAMEMODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_GET_HOST_AIM_PREFERENCE

**Hash:** `0xDFFA5BE8381C3314` | **Build:** `463`

**C++ 原型:**
```cpp
int NETWORK_SESSION_GET_HOST_AIM_PREFERENCE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_SESSION_GET_HOST_AIM_PREFERENCE);
```

**返回值:** `int` → `int`

---

### NETWORK_FIND_GAMERS_IN_CREW

**Hash:** `0xE532D6811B3A4D2A` | **JHash:** `0x7771AB83` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_FIND_GAMERS_IN_CREW(int crewId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_FIND_GAMERS_IN_CREW, crewId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `crewId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_FIND_MATCHED_GAMERS

**Hash:** `0xF7B2CFDE5C9F700D` | **JHash:** `0xA13045D4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_FIND_MATCHED_GAMERS(int attribute, float fallbackLimit, float lowerLimit, float upperLimit)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_FIND_MATCHED_GAMERS, attribute, fallbackLimit, lowerLimit, upperLimit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `attribute` | `int` | `int` | |
| `fallbackLimit` | `float` | `float` | |
| `lowerLimit` | `float` | `float` | |
| `upperLimit` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Uses attributes to find players with similar stats. Upper/Lower limit must be above zero or the fallback limit +/-0.1 is used.
There can be up to 15 attributes, they are as follows:

0 = Races
1 = Parachuting
2 = Horde
3 = Darts
4 = Arm Wrestling
5 = Tennis
6 = Golf
7 = Shooting Range
8 = Deathmatch
9 = MPPLY_MCMWIN/MPPLY_CRMISSION

---

### NETWORK_IS_FINDING_GAMERS

**Hash:** `0xDDDF64C91BFCF0AA` | **JHash:** `0xA6DAA79F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FINDING_GAMERS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FINDING_GAMERS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_DID_FIND_GAMERS_SUCCEED

**Hash:** `0xF9B83B77929D8863` | **JHash:** `0xBEDC4503` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DID_FIND_GAMERS_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DID_FIND_GAMERS_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_NUM_FOUND_GAMERS

**Hash:** `0xA1B043EE79A916FB` | **JHash:** `0xF4B80C7E` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_FOUND_GAMERS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_FOUND_GAMERS);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_FOUND_GAMER

**Hash:** `0x9DCFF2AFB68B3476` | **JHash:** `0xA08C9141` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_FOUND_GAMER(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_FOUND_GAMER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_FOUND_GAMERS

**Hash:** `0x6D14CCEE1B40381A` | **JHash:** `0x6AA9A154` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_FOUND_GAMERS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_FOUND_GAMERS);
```

**返回值:** `void` → `void`

---

### NETWORK_QUEUE_GAMER_FOR_STATUS

**Hash:** `0x85A0EF54A500882C` | **JHash:** `0x42BD0780` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_QUEUE_GAMER_FOR_STATUS(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_QUEUE_GAMER_FOR_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_GAMER_STATUS_FROM_QUEUE

**Hash:** `0x2CC848A861D01493` | **JHash:** `0xBEB98840` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_GAMER_STATUS_FROM_QUEUE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_GAMER_STATUS_FROM_QUEUE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_GETTING_GAMER_STATUS

**Hash:** `0x94A8394D150B013A` | **JHash:** `0x08029970` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GETTING_GAMER_STATUS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GETTING_GAMER_STATUS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_DID_GET_GAMER_STATUS_SUCCEED

**Hash:** `0x5AE17C6B0134B7F1` | **JHash:** `0xC871E745` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DID_GET_GAMER_STATUS_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DID_GET_GAMER_STATUS_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_GAMER_STATUS_RESULT

**Hash:** `0x02A8BEC6FD9AF660` | **JHash:** `0xB5ABC4B4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_GAMER_STATUS_RESULT(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_GAMER_STATUS_RESULT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_GET_GAMER_STATUS

**Hash:** `0x86E0660E4F5C956D` | **JHash:** `0x3F7EDBBD` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_GET_GAMER_STATUS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_GET_GAMER_STATUS);
```

**返回值:** `void` → `void`

---

### NETWORK_SESSION_JOIN_INVITE

**Hash:** `0xC6F8AB8A4189CF3A` | **JHash:** `0x3D6360B5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_JOIN_INVITE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_JOIN_INVITE);
```

**返回值:** `void` → `void`

---

### NETWORK_SESSION_CANCEL_INVITE

**Hash:** `0x2FBF47B1B36D36F9` | **JHash:** `0x20317535` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_CANCEL_INVITE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_CANCEL_INVITE);
```

**返回值:** `void` → `void`

---

### NETWORK_SESSION_FORCE_CANCEL_INVITE

**Hash:** `0xA29177F7703B5644` | **JHash:** `0x3FD49D3B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_FORCE_CANCEL_INVITE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_FORCE_CANCEL_INVITE);
```

**返回值:** `void` → `void`

---

### NETWORK_HAS_PENDING_INVITE

**Hash:** `0xAC8C7B9B88C4A668` | **JHash:** `0x0C207D6E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_PENDING_INVITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_PENDING_INVITE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_CONFIRMED_INVITE

**Hash:** `0xC42DD763159F3461` | **JHash:** `0xFBBAC350` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CONFIRMED_INVITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CONFIRMED_INVITE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_REQUEST_INVITE_CONFIRMED_EVENT

**Hash:** `0x62A0296C1BB1CEB3` | **JHash:** `0x0907A6BF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REQUEST_INVITE_CONFIRMED_EVENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REQUEST_INVITE_CONFIRMED_EVENT);
```

**返回值:** `BOOL` → `bool`

**说明:** Triggers a CEventNetworkInviteConfirmed event

---

### NETWORK_SESSION_WAS_INVITED

**Hash:** `0x23DFB504655D0CE4` | **JHash:** `0x6A0BEA60` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_WAS_INVITED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_WAS_INVITED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_GET_INVITER

**Hash:** `0xE57397B4A3429DD0` | **JHash:** `0xE9C6B3FD` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_GET_INVITER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_GET_INVITER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_IS_AWAITING_INVITE_RESPONSE

**Hash:** `0xD313DE83394AF134` | **JHash:** `0x3EA9D44C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_AWAITING_INVITE_RESPONSE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_AWAITING_INVITE_RESPONSE);
```

**返回值:** `BOOL` → `bool`

**说明:** Seems to be true while "Getting GTA Online session details" shows up.

---

### NETWORK_SESSION_IS_DISPLAYING_INVITE_CONFIRMATION

**Hash:** `0xBDB6F89C729CF388` | **JHash:** `0x9374523F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_DISPLAYING_INVITE_CONFIRMATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_DISPLAYING_INVITE_CONFIRMATION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SUPPRESS_INVITE

**Hash:** `0xA0682D67EF1FBA3D` | **JHash:** `0x323DC78C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SUPPRESS_INVITE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SUPPRESS_INVITE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_BLOCK_INVITES

**Hash:** `0x34F9E9049454A7A0` | **JHash:** `0xD156FD1A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BLOCK_INVITES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BLOCK_INVITES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_BLOCK_JOIN_QUEUE_INVITES

**Hash:** `0xCFEB8AF24FC1D0BB` | **JHash:** `0x8AB4890B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BLOCK_JOIN_QUEUE_INVITES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BLOCK_JOIN_QUEUE_INVITES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_CAN_RECEIVE_RS_INVITES

**Hash:** `0x68980414688F7F9D` | **Build:** `2612`

**C++ 原型:**
```cpp
void NETWORK_SET_CAN_RECEIVE_RS_INVITES(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_CAN_RECEIVE_RS_INVITES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_STORE_INVITE_THROUGH_RESTART

**Hash:** `0xF814FEC6A19FD6E0` | **JHash:** `0x32B7A076` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_STORE_INVITE_THROUGH_RESTART(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_STORE_INVITE_THROUGH_RESTART);
```

**返回值:** `void` → `void`

---

### NETWORK_ALLOW_INVITE_PROCESS_IN_PLAYER_SWITCH

**Hash:** `0x6B07B9CE4D390375` | **JHash:** `0x0FCE995D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ALLOW_INVITE_PROCESS_IN_PLAYER_SWITCH(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ALLOW_INVITE_PROCESS_IN_PLAYER_SWITCH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_SCRIPT_READY_FOR_EVENTS

**Hash:** `0x7AC752103856FB20` | **JHash:** `0xA639DCA2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_SCRIPT_READY_FOR_EVENTS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_SCRIPT_READY_FOR_EVENTS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_OFFLINE_INVITE_PENDING

**Hash:** `0x74698374C45701D2` | **JHash:** `0x70ED476A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_OFFLINE_INVITE_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_OFFLINE_INVITE_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_OFFLINE_INVITE_PENDING

**Hash:** `0x140E6A44870A11CE` | **JHash:** `0x50507BED` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_OFFLINE_INVITE_PENDING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_OFFLINE_INVITE_PENDING);
```

**返回值:** `void` → `void`

---

### _NETWORK_INVITE_GET_JOIN_FAIL_REASON

**Hash:** `0x7B335F84501145BB` | **Build:** `3179`

**C++ 原型:**
```cpp
const char* _NETWORK_INVITE_GET_JOIN_FAIL_REASON(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_INVITE_GET_JOIN_FAIL_REASON);
```

**返回值:** `const char*` → `string`

**说明:** Retrieves the failed invite join alert reason

---

### _NETWORK_INVITE_CLEAR_JOIN_FAIL_REASON

**Hash:** `0x8EF5F5811A940F82` | **Build:** `3179`

**C++ 原型:**
```cpp
void _NETWORK_INVITE_CLEAR_JOIN_FAIL_REASON(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_INVITE_CLEAR_JOIN_FAIL_REASON);
```

**返回值:** `void` → `void`

**说明:** Clears the failed invite join alert reason

---

### NETWORK_SESSION_HOST_SINGLE_PLAYER

**Hash:** `0xC74C33FCA52856D5` | **JHash:** `0xF3B1CA85` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_HOST_SINGLE_PLAYER(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_HOST_SINGLE_PLAYER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Loads up the map that is loaded when beeing in mission creator
Player gets placed in a mix between online/offline mode
p0 is always 2 in R* scripts.

Appears to be patched in gtav b757 (game gets terminated) alonside with most other network natives to prevent online modding ~ghost30812

---

### NETWORK_SESSION_LEAVE_SINGLE_PLAYER

**Hash:** `0x3442775428FD2DAA` | **JHash:** `0xC692F86A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_LEAVE_SINGLE_PLAYER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_LEAVE_SINGLE_PLAYER);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_GAME_IN_PROGRESS

**Hash:** `0x10FAB35428CCC9D7` | **JHash:** `0x09B88E3E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GAME_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GAME_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_SESSION_ACTIVE

**Hash:** `0xD83C2B94E7508980` | **JHash:** `0x715CB8C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SESSION_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SESSION_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_IN_SESSION

**Hash:** `0xCA97246103B63917` | **JHash:** `0x4BC4105E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### _NETWORK_IS_AMERICAS_VERSION

**Hash:** `0x0292BD7F3766CEBC` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_IS_AMERICAS_VERSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_IS_AMERICAS_VERSION);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return 0.

---

### NETWORK_IS_SESSION_STARTED

**Hash:** `0x9DE624D2FC4B603F` | **JHash:** `0x9D854A37` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SESSION_STARTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SESSION_STARTED);
```

**返回值:** `BOOL` → `bool`

**说明:** This checks if player is playing on gta online or not.
Please add an if and block your mod if this is "true".

---

### NETWORK_IS_SESSION_BUSY

**Hash:** `0xF4435D66A8E2905E` | **JHash:** `0x8592152D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SESSION_BUSY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SESSION_BUSY);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_SESSION_END

**Hash:** `0x4EEBC3694E49C572` | **JHash:** `0xE1FCCDBA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SESSION_END(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SESSION_END);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_GAME_MODE

**Hash:** `0x4C9034162368E206` | **Build:** `2060`

**C++ 原型:**
```cpp
int NETWORK_GET_GAME_MODE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_GAME_MODE);
```

**返回值:** `int` → `int`

---

### NETWORK_SESSION_MARK_VISIBLE

**Hash:** `0x271CC6AB59EBF9A5` | **JHash:** `0x7017257D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_MARK_VISIBLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_MARK_VISIBLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_IS_VISIBLE

**Hash:** `0xBA416D68C631496A` | **JHash:** `0x4977AC28` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_VISIBLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_VISIBLE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_BLOCK_JOIN_REQUESTS

**Hash:** `0xA73667484D7037C3` | **JHash:** `0xE6EEF8AF` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_BLOCK_JOIN_REQUESTS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_BLOCK_JOIN_REQUESTS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_CHANGE_SLOTS

**Hash:** `0xB4AB419E0D86ACAE` | **JHash:** `0x6BB93227` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_CHANGE_SLOTS(int slots, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_CHANGE_SLOTS, slots, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `slots` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** num player slots allowed in session, seems to work? 32 max

---

### NETWORK_SESSION_GET_PRIVATE_SLOTS

**Hash:** `0x53AFD64C6758F2F9` | **JHash:** `0xE642F2FD` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_SESSION_GET_PRIVATE_SLOTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_SESSION_GET_PRIVATE_SLOTS);
```

**返回值:** `int` → `int`

---

### NETWORK_SESSION_VOICE_HOST

**Hash:** `0x9C1556705F864230` | **JHash:** `0x345C2980` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_VOICE_HOST(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_VOICE_HOST);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_VOICE_LEAVE

**Hash:** `0x6793E42BE02B575D` | **JHash:** `0xE566C7DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_VOICE_LEAVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_VOICE_LEAVE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_VOICE_CONNECT_TO_PLAYER

**Hash:** `0xABD5E88B8A2D3DB2` | **JHash:** `0x9DFD89E6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_VOICE_CONNECT_TO_PLAYER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_VOICE_CONNECT_TO_PLAYER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_VOICE_RESPOND_TO_REQUEST

**Hash:** `0x7F8413B7FC2AA6B9` | **JHash:** `0x075321B5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_VOICE_RESPOND_TO_REQUEST(BOOL p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_VOICE_RESPOND_TO_REQUEST, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_VOICE_SET_TIMEOUT

**Hash:** `0x5B8ED3DB018927B1` | **JHash:** `0x6EFC2FD0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SESSION_VOICE_SET_TIMEOUT(int timeout)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SESSION_VOICE_SET_TIMEOUT, timeout);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeout` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SESSION_IS_IN_VOICE_SESSION

**Hash:** `0x855BC38818F6F684` | **JHash:** `0x60AA4AA1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_IN_VOICE_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_IN_VOICE_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_IS_VOICE_SESSION_ACTIVE

**Hash:** `0xB5D3453C98456528` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_VOICE_SESSION_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_VOICE_SESSION_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SESSION_IS_VOICE_SESSION_BUSY

**Hash:** `0xEF0912DDF7C4CB4B` | **JHash:** `0x132CA01E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SESSION_IS_VOICE_SESSION_BUSY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SESSION_IS_VOICE_SESSION_BUSY);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SEND_TEXT_MESSAGE

**Hash:** `0x3A214F2EC889B100` | **JHash:** `0xAFFEA720` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SEND_TEXT_MESSAGE(const char* message, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SEND_TEXT_MESSAGE, message, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `message` | `const char*` | `string` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** Message is limited to 64 characters.

---

### NETWORK_SET_ACTIVITY_SPECTATOR

**Hash:** `0x75138790B4359A74` | **JHash:** `0xFC9AD060` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_ACTIVITY_SPECTATOR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ACTIVITY_SPECTATOR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_ACTIVITY_SPECTATOR

**Hash:** `0x12103B9E0C9F92FB` | **JHash:** `0xAF329720` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ACTIVITY_SPECTATOR(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ACTIVITY_SPECTATOR);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_ACTIVITY_PLAYER_MAX

**Hash:** `0x0E4F77F7B9D74D84` | **JHash:** `0x04CB2AB4` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SET_ACTIVITY_PLAYER_MAX(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ACTIVITY_PLAYER_MAX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_ACTIVITY_SPECTATOR_MAX

**Hash:** `0x9D277B76D1D12222` | **JHash:** `0x74E0BC0A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_ACTIVITY_SPECTATOR_MAX(int maxSpectators)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ACTIVITY_SPECTATOR_MAX, maxSpectators);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxSpectators` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_ACTIVITY_PLAYER_NUM

**Hash:** `0x73E2B500410DA5A2` | **JHash:** `0x31F951FD` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_ACTIVITY_PLAYER_NUM(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_ACTIVITY_PLAYER_NUM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### NETWORK_IS_ACTIVITY_SPECTATOR_FROM_HANDLE

**Hash:** `0x2763BBAA72A7BCB9` | **JHash:** `0x58F1DF7D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ACTIVITY_SPECTATOR_FROM_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ACTIVITY_SPECTATOR_FROM_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HOST_TRANSITION

**Hash:** `0xA60BB5CE242BB254` | **JHash:** `0x146764FB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HOST_TRANSITION(int p0, int p1, int p2, int p3, Any p4, BOOL p5, BOOL p6, int p7, Any p8, int p9)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HOST_TRANSITION, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `Any` | `object` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `int` | `int` | |
| `p8` | `Any` | `object` | |
| `p9` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p0: Unknown int
p1: Unknown int
p2: Unknown int
p3: Unknown int
p4: Unknown always 0 in decompiled scripts
p5: BOOL purpose unknown, both 0 and 1 are used in decompiled scripts.
p6: BOOL purpose unknown, both 0 and 1 are used in decompiled scripts.
p7: Unknown int, it's an int according to decompiled scripts, however the value is always 0 or 1.
p8: Unknown int, it's an int according to decompiled scripts, however the value is always 0 or 1.
p9: Unknown int, sometimes 0, but also 32768 or 16384 appear in decompiled scripst, maybe a flag of some sort?

From what I can tell it looks like it does the following:
Creates/hosts a new transition to another online session, using this in FiveM will result in other players being disconencted from the server/preventing them from joining. This is most likely because I entered the wrong session parameters since they're pretty much all unknown right now.
You also need to use `NetworkJoinTransition(Player player)` and `NetworkLaunchTransition()`.

---

### NETWORK_DO_TRANSITION_QUICKMATCH

**Hash:** `0x71FB0EBCD4915D56` | **JHash:** `0x2FF65C0B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_QUICKMATCH(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_QUICKMATCH, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DO_TRANSITION_QUICKMATCH_ASYNC

**Hash:** `0xA091A5E44F0072E5` | **JHash:** `0x47D61C99` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_QUICKMATCH_ASYNC(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_QUICKMATCH_ASYNC, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DO_TRANSITION_QUICKMATCH_WITH_GROUP

**Hash:** `0x9C4AB58491FDC98A` | **JHash:** `0x5CE60A11` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_QUICKMATCH_WITH_GROUP(Any p0, Any p1, Any p2, Any p3, Any* p4, Any p5, Any p6, Any p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_QUICKMATCH_WITH_GROUP, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any*` | `object*` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_JOIN_GROUP_ACTIVITY

**Hash:** `0xA06509A691D12BE4` | **JHash:** `0x0D7E5CF9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_JOIN_GROUP_ACTIVITY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_JOIN_GROUP_ACTIVITY);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_GROUP_ACTIVITY

**Hash:** `0x1888694923EF4591` | **JHash:** `0x157D44D3` | **Build:** `393`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_GROUP_ACTIVITY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_GROUP_ACTIVITY);
```

**返回值:** `void` → `void`

---

### NETWORK_RETAIN_ACTIVITY_GROUP

**Hash:** `0xB13E88E655E5A3BC` | **JHash:** `0x36A5F2DA` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_RETAIN_ACTIVITY_GROUP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_RETAIN_ACTIVITY_GROUP);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_TRANSITION_CLOSED_FRIENDS

**Hash:** `0x6512765E3BE78C50` | **JHash:** `0x95FFACF8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_CLOSED_FRIENDS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_CLOSED_FRIENDS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_CLOSED_CREW

**Hash:** `0x0DBD5D7E3C5BEC3B` | **JHash:** `0xB97A966B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_CLOSED_CREW(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_CLOSED_CREW);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_SOLO

**Hash:** `0x5DC577201723960A` | **JHash:** `0x063DC4A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_SOLO(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_SOLO);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_PRIVATE

**Hash:** `0x5A6AA44FF8E931E6` | **JHash:** `0x104DDE8D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_PRIVATE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_PRIVATE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_NUM_TRANSITION_NON_ASYNC_GAMERS

**Hash:** `0x617F49C2668E6155` | **Build:** `757`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_TRANSITION_NON_ASYNC_GAMERS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_TRANSITION_NON_ASYNC_GAMERS);
```

**返回值:** `int` → `int`

---

### NETWORK_MARK_AS_PREFERRED_ACTIVITY

**Hash:** `0x261E97AD7BCF3D40` | **JHash:** `0x7EF353E1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_MARK_AS_PREFERRED_ACTIVITY(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_MARK_AS_PREFERRED_ACTIVITY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_MARK_AS_WAITING_ASYNC

**Hash:** `0x39917E1B4CB0F911` | **JHash:** `0xF60986FC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_MARK_AS_WAITING_ASYNC(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_MARK_AS_WAITING_ASYNC, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_IN_PROGRESS_FINISH_TIME

**Hash:** `0x2CE9D95E4051AECD` | **Build:** `678`

**C++ 原型:**
```cpp
void NETWORK_SET_IN_PROGRESS_FINISH_TIME(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IN_PROGRESS_FINISH_TIME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_TRANSITION_CREATOR_HANDLE

**Hash:** `0xEF26739BCD9907D5` | **JHash:** `0x1DD01FE7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_TRANSITION_CREATOR_HANDLE(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TRANSITION_CREATOR_HANDLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_CLEAR_TRANSITION_CREATOR_HANDLE

**Hash:** `0xFB3272229A82C759` | **JHash:** `0x8BB336F7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_TRANSITION_CREATOR_HANDLE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_TRANSITION_CREATOR_HANDLE);
```

**返回值:** `void` → `void`

---

### NETWORK_INVITE_GAMERS_TO_TRANSITION

**Hash:** `0x4A595C32F77DFF76` | **JHash:** `0x5332E645` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_INVITE_GAMERS_TO_TRANSITION(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_INVITE_GAMERS_TO_TRANSITION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_GAMER_INVITED_TO_TRANSITION

**Hash:** `0xCA2C8073411ECDB6` | **JHash:** `0x17F1C69D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_GAMER_INVITED_TO_TRANSITION(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_GAMER_INVITED_TO_TRANSITION, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_LEAVE_TRANSITION

**Hash:** `0xD23A1A815D21DB19` | **JHash:** `0x3A3599B7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_LEAVE_TRANSITION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_LEAVE_TRANSITION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_LAUNCH_TRANSITION

**Hash:** `0x2DCF46CB1A4F0884` | **JHash:** `0xE3570BA2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_LAUNCH_TRANSITION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_LAUNCH_TRANSITION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_DO_NOT_LAUNCH_FROM_JOIN_AS_MIGRATED_HOST

**Hash:** `0xA2E9C1AB8A92E8CD` | **JHash:** `0x8D014B23` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_DO_NOT_LAUNCH_FROM_JOIN_AS_MIGRATED_HOST(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_DO_NOT_LAUNCH_FROM_JOIN_AS_MIGRATED_HOST, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Appears to set whether a transition should be started when the session is migrating.

---

### NETWORK_CANCEL_TRANSITION_MATCHMAKING

**Hash:** `0x023782EFC70585EE` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_CANCEL_TRANSITION_MATCHMAKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CANCEL_TRANSITION_MATCHMAKING);
```

**返回值:** `void` → `void`

---

### NETWORK_BAIL_TRANSITION

**Hash:** `0xEAA572036990CD1B` | **JHash:** `0xB59D74CA` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_BAIL_TRANSITION(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BAIL_TRANSITION, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_DO_TRANSITION_TO_GAME

**Hash:** `0x3E9BB38102A589B0` | **JHash:** `0x1B2114D2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_TO_GAME(BOOL p0, int maxPlayers)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_TO_GAME, p0, maxPlayers);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `maxPlayers` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DO_TRANSITION_TO_NEW_GAME

**Hash:** `0x4665F51EFED00034` | **JHash:** `0x58AFBE63` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_TO_NEW_GAME(BOOL p0, int maxPlayers, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_TO_NEW_GAME, p0, maxPlayers, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `maxPlayers` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DO_TRANSITION_TO_FREEMODE

**Hash:** `0x3AAD8B2FCA1E289F` | **JHash:** `0xC7CB8ADF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_TO_FREEMODE(Any* p0, Any p1, BOOL p2, int players, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_TO_FREEMODE, p0, p1, p2, players, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |
| `players` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 is true 3/4 of the occurrences I found.
'players' is the number of players for a session. On PS3/360 it's always 18. On PC it's 32.

---

### NETWORK_DO_TRANSITION_TO_NEW_FREEMODE

**Hash:** `0x9E80A5BA8109F974` | **JHash:** `0xAD13375E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DO_TRANSITION_TO_NEW_FREEMODE(Any* p0, Any p1, int players, BOOL p3, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DO_TRANSITION_TO_NEW_FREEMODE, p0, p1, players, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `players` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_TO_GAME

**Hash:** `0x9D7696D8F4FA6CB7` | **JHash:** `0x17146B2B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_TO_GAME(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_TO_GAME);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_TRANSITION_MEMBERS

**Hash:** `0x73B000F7FBC55829` | **JHash:** `0x31F19263` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_TRANSITION_MEMBERS(Any* data, int dataCount)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_TRANSITION_MEMBERS, data, dataCount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `dataCount` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns count.

---

### NETWORK_APPLY_TRANSITION_PARAMETER

**Hash:** `0x521638ADA1BA0D18` | **JHash:** `0xCEE79711` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_APPLY_TRANSITION_PARAMETER(int p0, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_APPLY_TRANSITION_PARAMETER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_APPLY_TRANSITION_PARAMETER_STRING

**Hash:** `0xEBEFC2E77084F599` | **JHash:** `0xE0C28DB5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_APPLY_TRANSITION_PARAMETER_STRING(int p0, const char* string, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_APPLY_TRANSITION_PARAMETER_STRING, p0, string, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `string` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SEND_TRANSITION_GAMER_INSTRUCTION

**Hash:** `0x31D1D2B858D25E6B` | **JHash:** `0x468B0884` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SEND_TRANSITION_GAMER_INSTRUCTION(Any* gamerHandle, const char* p1, int p2, int p3, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SEND_TRANSITION_GAMER_INSTRUCTION, gamerHandle, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `p1` | `const char*` | `string` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_MARK_TRANSITION_GAMER_AS_FULLY_JOINED

**Hash:** `0x5728BB6D63E3FF1D` | **JHash:** `0x03383F57` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_MARK_TRANSITION_GAMER_AS_FULLY_JOINED(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_MARK_TRANSITION_GAMER_AS_FULLY_JOINED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_HOST

**Hash:** `0x0B824797C9BF2159` | **JHash:** `0x0C0900BC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_HOST(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_HOST);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_HOST_FROM_HANDLE

**Hash:** `0x6B5C83BA3EFE6A10` | **JHash:** `0x0E2854C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_HOST_FROM_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_HOST_FROM_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_TRANSITION_HOST

**Hash:** `0x65042B9774C4435E` | **JHash:** `0x73098D40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_TRANSITION_HOST(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_TRANSITION_HOST, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_IN_TRANSITION

**Hash:** `0x68049AEFF83D8F0A` | **JHash:** `0xC3CDB626` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_TRANSITION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_TRANSITION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_STARTED

**Hash:** `0x53FA83401D9C07FE` | **JHash:** `0x7917E111` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_STARTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_STARTED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_BUSY

**Hash:** `0x520F3282A53D26B7` | **JHash:** `0xA357A2C6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_BUSY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_BUSY);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_MATCHMAKING

**Hash:** `0x292564C735375EDF` | **JHash:** `0x8262C70E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_MATCHMAKING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_MATCHMAKING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TRANSITION_LEAVE_POSTPONED

**Hash:** `0xC571D0E77D8BBC29` | **JHash:** `0x4F4BB300` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_LEAVE_POSTPONED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_LEAVE_POSTPONED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_TRANSITION_SET_IN_PROGRESS

**Hash:** `0x1398582B7F72B3ED` | **Build:** `505`

**C++ 原型:**
```cpp
void NETWORK_TRANSITION_SET_IN_PROGRESS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRANSITION_SET_IN_PROGRESS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_TRANSITION_SET_CONTENT_CREATOR

**Hash:** `0x1F8E00FB18239600` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_TRANSITION_SET_CONTENT_CREATOR(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRANSITION_SET_CONTENT_CREATOR, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_TRANSITION_SET_ACTIVITY_ISLAND

**Hash:** `0xF6F4383B7C92F11A` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_TRANSITION_SET_ACTIVITY_ISLAND(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRANSITION_SET_ACTIVITY_ISLAND, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_OPEN_TRANSITION_MATCHMAKING

**Hash:** `0x2B3A8F7CA3A38FDE` | **JHash:** `0xC71E607B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OPEN_TRANSITION_MATCHMAKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OPEN_TRANSITION_MATCHMAKING);
```

**返回值:** `void` → `void`

---

### NETWORK_CLOSE_TRANSITION_MATCHMAKING

**Hash:** `0x43F4DBA69710E01E` | **JHash:** `0x82D32D07` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLOSE_TRANSITION_MATCHMAKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLOSE_TRANSITION_MATCHMAKING);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_TRANSITION_OPEN_TO_MATCHMAKING

**Hash:** `0x37A4494483B9F5C9` | **JHash:** `0xC901AA9F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_OPEN_TO_MATCHMAKING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_OPEN_TO_MATCHMAKING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_TRANSITION_VISIBILITY_LOCK

**Hash:** `0x0C978FDA19692C2C` | **JHash:** `0x4AF0ADF5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_TRANSITION_VISIBILITY_LOCK(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TRANSITION_VISIBILITY_LOCK, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_TRANSITION_VISIBILITY_LOCKED

**Hash:** `0xD0A484CB2F829FBE` | **JHash:** `0xF637166E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TRANSITION_VISIBILITY_LOCKED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TRANSITION_VISIBILITY_LOCKED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_TRANSITION_ACTIVITY_ID

**Hash:** `0x30DE938B516F0AD2` | **JHash:** `0xCCA9C022` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_TRANSITION_ACTIVITY_ID(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TRANSITION_ACTIVITY_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_CHANGE_TRANSITION_SLOTS

**Hash:** `0xEEEDA5E6D7080987` | **JHash:** `0x1E5F6AEF` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CHANGE_TRANSITION_SLOTS(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CHANGE_TRANSITION_SLOTS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_TRANSITION_BLOCK_JOIN_REQUESTS

**Hash:** `0x973D76AA760A6CB6` | **JHash:** `0x0532DDD2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_TRANSITION_BLOCK_JOIN_REQUESTS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRANSITION_BLOCK_JOIN_REQUESTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_HAS_PLAYER_STARTED_TRANSITION

**Hash:** `0x9AC9CCBFA8C29795` | **JHash:** `0x4ABD1E59` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_PLAYER_STARTED_TRANSITION(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_PLAYER_STARTED_TRANSITION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ARE_TRANSITION_DETAILS_VALID

**Hash:** `0x2615AA2A695930C1` | **JHash:** `0xCDEBCCE7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ARE_TRANSITION_DETAILS_VALID(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ARE_TRANSITION_DETAILS_VALID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_JOIN_TRANSITION

**Hash:** `0x9D060B08CD63321A` | **JHash:** `0xB054EC4B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_JOIN_TRANSITION(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_JOIN_TRANSITION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** int handle[76];
          NETWORK_HANDLE_FROM_FRIEND(iSelectedPlayer, &handle[0], 13);
          Player uVar2 = NETWORK_GET_PLAYER_FROM_GAMER_HANDLE(&handle[0]);
          NETWORK_JOIN_TRANSITION(uVar2);
nothing doin.

---

### NETWORK_HAS_INVITED_GAMER_TO_TRANSITION

**Hash:** `0x7284A47B3540E6CF` | **JHash:** `0x4F41DF6B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_INVITED_GAMER_TO_TRANSITION(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_INVITED_GAMER_TO_TRANSITION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_TRANSITION_INVITE_BEEN_ACKED

**Hash:** `0x3F9990BF5F22759C` | **JHash:** `0x00F26CDC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_TRANSITION_INVITE_BEEN_ACKED(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_TRANSITION_INVITE_BEEN_ACKED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_ACTIVITY_SESSION

**Hash:** `0x05095437424397FA` | **JHash:** `0x577DAA8A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ACTIVITY_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ACTIVITY_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_DISABLE_REALTIME_MULTIPLAYER

**Hash:** `0x236905C700FDB54D` | **Build:** `2612`

**C++ 原型:**
```cpp
void NETWORK_DISABLE_REALTIME_MULTIPLAYER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DISABLE_REALTIME_MULTIPLAYER);
```

**返回值:** `void` → `void`

---

### NETWORK_SET_PRESENCE_SESSION_INVITES_BLOCKED

**Hash:** `0x4A9FDE3A5A6D0437` | **JHash:** `0x18F03AFD` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_PRESENCE_SESSION_INVITES_BLOCKED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_PRESENCE_SESSION_INVITES_BLOCKED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing. It's just a nullsub.

---

### NETWORK_SEND_INVITE_VIA_PRESENCE

**Hash:** `0xC3C7A6AFDB244624` | **JHash:** `0x8B99B72B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SEND_INVITE_VIA_PRESENCE(Any* gamerHandle, const char* p1, int dataCount, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SEND_INVITE_VIA_PRESENCE, gamerHandle, p1, dataCount, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `p1` | `const char*` | `string` | |
| `dataCount` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SEND_TRANSITION_INVITE_VIA_PRESENCE

**Hash:** `0xC116FF9B4D488291` | **JHash:** `0x877C0E1C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SEND_TRANSITION_INVITE_VIA_PRESENCE(Any* gamerHandle, const char* p1, int dataCount, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SEND_TRANSITION_INVITE_VIA_PRESENCE, gamerHandle, p1, dataCount, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `p1` | `const char*` | `string` | |
| `dataCount` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SEND_IMPORTANT_TRANSITION_INVITE_VIA_PRESENCE

**Hash:** `0x1171A97A3D3981B6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SEND_IMPORTANT_TRANSITION_INVITE_VIA_PRESENCE(Any* gamerHandle, const char* p1, int dataCount, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SEND_IMPORTANT_TRANSITION_INVITE_VIA_PRESENCE, gamerHandle, p1, dataCount, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `p1` | `const char*` | `string` | |
| `dataCount` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Contains the string "NETWORK_SEND_PRESENCE_TRANSITION_INVITE" but so does 0xC116FF9B4D488291; seems to fit alphabetically here, tho.

---

### NETWORK_GET_PRESENCE_INVITE_INDEX_BY_ID

**Hash:** `0x742B58F723233ED9` | **JHash:** `0x5E832444` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PRESENCE_INVITE_INDEX_BY_ID(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PRESENCE_INVITE_INDEX_BY_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_NUM_PRESENCE_INVITES

**Hash:** `0xCEFA968912D0F78D` | **JHash:** `0x3FDA00F3` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_PRESENCE_INVITES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_PRESENCE_INVITES);
```

**返回值:** `int` → `int`

---

### NETWORK_ACCEPT_PRESENCE_INVITE

**Hash:** `0xFA91550DF9318B22` | **JHash:** `0xE5DA4CED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCEPT_PRESENCE_INVITE(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCEPT_PRESENCE_INVITE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_REMOVE_PRESENCE_INVITE

**Hash:** `0xF0210268DB0974B1` | **JHash:** `0x93C665FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REMOVE_PRESENCE_INVITE(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REMOVE_PRESENCE_INVITE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRESENCE_INVITE_ID

**Hash:** `0xDFF09646E12EC386` | **JHash:** `0xD50DF46C` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PRESENCE_INVITE_ID(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PRESENCE_INVITE_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_PRESENCE_INVITE_INVITER

**Hash:** `0x4962CC4AA2F345B7` | **JHash:** `0x19EC65D9` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_PRESENCE_INVITE_INVITER(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_PRESENCE_INVITE_INVITER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_GET_PRESENCE_INVITE_HANDLE

**Hash:** `0x38D5B0FEBB086F75` | **JHash:** `0xB2451429` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRESENCE_INVITE_HANDLE(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRESENCE_INVITE_HANDLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRESENCE_INVITE_SESSION_ID

**Hash:** `0x26E1CD96B0903D60` | **JHash:** `0xC5E0C989` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PRESENCE_INVITE_SESSION_ID(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PRESENCE_INVITE_SESSION_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_PRESENCE_INVITE_CONTENT_ID

**Hash:** `0x24409FC4C55CB22D` | **JHash:** `0xA4302183` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_PRESENCE_INVITE_CONTENT_ID(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_PRESENCE_INVITE_CONTENT_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_GET_PRESENCE_INVITE_PLAYLIST_LENGTH

**Hash:** `0xD39B3FFF8FFDD5BF` | **JHash:** `0x51B2D848` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PRESENCE_INVITE_PLAYLIST_LENGTH(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PRESENCE_INVITE_PLAYLIST_LENGTH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_PRESENCE_INVITE_PLAYLIST_CURRENT

**Hash:** `0x728C4CC7920CD102` | **JHash:** `0x4677C656` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PRESENCE_INVITE_PLAYLIST_CURRENT(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PRESENCE_INVITE_PLAYLIST_CURRENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_PRESENCE_INVITE_FROM_ADMIN

**Hash:** `0x3DBF2DF0AEB7D289` | **JHash:** `0xF5E3401C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRESENCE_INVITE_FROM_ADMIN(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRESENCE_INVITE_FROM_ADMIN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRESENCE_INVITE_IS_TOURNAMENT

**Hash:** `0x8806CEBFABD3CE05` | **JHash:** `0x7D593B4C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRESENCE_INVITE_IS_TOURNAMENT(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRESENCE_INVITE_IS_TOURNAMENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_FOLLOW_INVITE

**Hash:** `0x76D9B976C4C09FDE` | **JHash:** `0xE96CFE7D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_FOLLOW_INVITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_FOLLOW_INVITE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACTION_FOLLOW_INVITE

**Hash:** `0xC88156EBB786F8D5` | **JHash:** `0xAB969F00` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACTION_FOLLOW_INVITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACTION_FOLLOW_INVITE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_FOLLOW_INVITE

**Hash:** `0x439BFDE3CD0610F6` | **JHash:** `0x3242F952` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLEAR_FOLLOW_INVITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLEAR_FOLLOW_INVITE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_REMOVE_AND_CANCEL_ALL_INVITES

**Hash:** `0xEBF8284D8CADEB53` | **JHash:** `0x9773F36A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_AND_CANCEL_ALL_INVITES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_AND_CANCEL_ALL_INVITES);
```

**返回值:** `void` → `void`

---

### NETWORK_REMOVE_TRANSITION_INVITE

**Hash:** `0x7524B431B2E6F7EE` | **JHash:** `0xFDE84CB7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_TRANSITION_INVITE(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_TRANSITION_INVITE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_REMOVE_ALL_TRANSITION_INVITE

**Hash:** `0x726E0375C7A26368` | **JHash:** `0xF7134E73` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_ALL_TRANSITION_INVITE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_ALL_TRANSITION_INVITE);
```

**返回值:** `void` → `void`

---

### NETWORK_REMOVE_AND_CANCEL_ALL_TRANSITION_INVITES

**Hash:** `0xF083835B70BA9BFE` | **JHash:** `0xC47352E7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_AND_CANCEL_ALL_TRANSITION_INVITES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_AND_CANCEL_ALL_TRANSITION_INVITES);
```

**返回值:** `void` → `void`

---

### NETWORK_INVITE_GAMERS

**Hash:** `0x9D80CD1D0E6327DE` | **JHash:** `0x52FB8074` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_INVITE_GAMERS(Any* p0, Any p1, Any* p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_INVITE_GAMERS, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_INVITED_GAMER

**Hash:** `0x4D86CD31E8976ECE` | **JHash:** `0xEC651BC0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_INVITED_GAMER(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_INVITED_GAMER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_MADE_INVITE_DECISION

**Hash:** `0x71DC455F5CD1C2B1` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_MADE_INVITE_DECISION(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_MADE_INVITE_DECISION, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_INVITE_REPLY_STATUS

**Hash:** `0x3855FB5EB2C5E8B2` | **Build:** `505`

**C++ 原型:**
```cpp
int NETWORK_GET_INVITE_REPLY_STATUS(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_INVITE_REPLY_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU

**Hash:** `0x74881E6BCAE2327C` | **JHash:** `0x72BA00CE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU

**Hash:** `0x7206F674F2A3B1BB` | **JHash:** `0xFD95899E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SET_CURRENTLY_SELECTED_GAMER_HANDLE_FROM_INVITE_MENU, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_INVITE_ON_CALL_FOR_INVITE_MENU

**Hash:** `0x66F010A4B031A331` | **JHash:** `0x11378777` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_INVITE_ON_CALL_FOR_INVITE_MENU(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_INVITE_ON_CALL_FOR_INVITE_MENU, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_CHECK_DATA_MANAGER_SUCCEEDED_FOR_HANDLE

**Hash:** `0x44B37CDCAE765AAE` | **JHash:** `0x13301529` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_DATA_MANAGER_SUCCEEDED_FOR_HANDLE(int p0, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_DATA_MANAGER_SUCCEEDED_FOR_HANDLE, p0, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CHECK_DATA_MANAGER_FOR_HANDLE

**Hash:** `0x4AD490AE1536933B` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_DATA_MANAGER_FOR_HANDLE(Any p0, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_DATA_MANAGER_FOR_HANDLE, p0, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_INVITE_FAILED_MESSAGE_FOR_INVITE_MENU

**Hash:** `0x0D77A82DC2D0DA59` | **JHash:** `0x0808D4CC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_INVITE_FAILED_MESSAGE_FOR_INVITE_MENU(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_INVITE_FAILED_MESSAGE_FOR_INVITE_MENU, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### FILLOUT_PM_PLAYER_LIST

**Hash:** `0xCBBD7C4991B64809` | **JHash:** `0xCE40F423` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FILLOUT_PM_PLAYER_LIST(Any* gamerHandle, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FILLOUT_PM_PLAYER_LIST, gamerHandle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### FILLOUT_PM_PLAYER_LIST_WITH_NAMES

**Hash:** `0x716B6DB9D1886106` | **JHash:** `0xB8DF604E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FILLOUT_PM_PLAYER_LIST_WITH_NAMES(Any* p0, Any* p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FILLOUT_PM_PLAYER_LIST_WITH_NAMES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### REFRESH_PLAYER_LIST_STATS

**Hash:** `0xE26CCFF8094D8C74` | **JHash:** `0xF49C1533` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REFRESH_PLAYER_LIST_STATS(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REFRESH_PLAYER_LIST_STATS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_CURRENT_DATA_MANAGER_HANDLE

**Hash:** `0x796A87B3B68D1F3D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SET_CURRENT_DATA_MANAGER_HANDLE(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SET_CURRENT_DATA_MANAGER_HANDLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_IN_PLATFORM_PARTY

**Hash:** `0x2FC5650B0271CB57` | **JHash:** `0xA812B6CB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_PLATFORM_PARTY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_PLATFORM_PARTY);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return false.

---

### NETWORK_GET_PLATFORM_PARTY_MEMBER_COUNT

**Hash:** `0x01ABCE5E7CBDA196` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PLATFORM_PARTY_MEMBER_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLATFORM_PARTY_MEMBER_COUNT);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_PLATFORM_PARTY_MEMBERS

**Hash:** `0x120364DE2845DAF8` | **JHash:** `0xF30E5814` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PLATFORM_PARTY_MEMBERS(Any* data, int dataSize)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLATFORM_PARTY_MEMBERS, data, dataSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `dataSize` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_IS_IN_PLATFORM_PARTY_CHAT

**Hash:** `0xFD8B834A8BA05048` | **JHash:** `0xC6609191` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_PLATFORM_PARTY_CHAT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_PLATFORM_PARTY_CHAT);
```

**返回值:** `BOOL` → `bool`

**说明:** Hardcoded to return false.

---

### NETWORK_IS_CHATTING_IN_PLATFORM_PARTY

**Hash:** `0x8DE9945BCC9AEC52` | **JHash:** `0x51367B34` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CHATTING_IN_PLATFORM_PARTY(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CHATTING_IN_PLATFORM_PARTY, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** This would be nice to see if someone is in party chat, but 2 sad notes.
1) It only becomes true if said person is speaking in that party at the time.
2) It will never, become true unless you are in that party with said person.

---

### NETWORK_CAN_QUEUE_FOR_PREVIOUS_SESSION_JOIN

**Hash:** `0x2BF66D2E7414F686` | **JHash:** `0x9156EFC0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_QUEUE_FOR_PREVIOUS_SESSION_JOIN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_QUEUE_FOR_PREVIOUS_SESSION_JOIN);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_QUEUING_FOR_SESSION_JOIN

**Hash:** `0x14922ED3E38761F0` | **JHash:** `0x8FA6EE0E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_QUEUING_FOR_SESSION_JOIN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_QUEUING_FOR_SESSION_JOIN);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLEAR_QUEUED_JOIN_REQUEST

**Hash:** `0x6CE50E47F5543D0C` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_QUEUED_JOIN_REQUEST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_QUEUED_JOIN_REQUEST);
```

**返回值:** `void` → `void`

---

### NETWORK_SEND_QUEUED_JOIN_REQUEST

**Hash:** `0xFA2888E3833C8E96` | **JHash:** `0x7F70C15A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SEND_QUEUED_JOIN_REQUEST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SEND_QUEUED_JOIN_REQUEST);
```

**返回值:** `void` → `void`

---

### NETWORK_REMOVE_ALL_QUEUED_JOIN_REQUESTS

**Hash:** `0x25D990F8E0E3F13C` | **JHash:** `0x4FD83F5B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_ALL_QUEUED_JOIN_REQUESTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_ALL_QUEUED_JOIN_REQUESTS);
```

**返回值:** `void` → `void`

---

### NETWORK_SEED_RANDOM_NUMBER_GENERATOR

**Hash:** `0xF1B84178F8674195` | **JHash:** `0x41702C8A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SEED_RANDOM_NUMBER_GENERATOR(int seed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SEED_RANDOM_NUMBER_GENERATOR, seed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `seed` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_RANDOM_INT

**Hash:** `0x599E4FA1F87EB5FF` | **JHash:** `0x208DD848` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_RANDOM_INT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_RANDOM_INT);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_RANDOM_INT_RANGED

**Hash:** `0xE30CF56F1EFA5F43` | **JHash:** `0xF9B6426D` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_RANDOM_INT_RANGED(int rangeStart, int rangeEnd)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_RANDOM_INT_RANGED, rangeStart, rangeEnd);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rangeStart` | `int` | `int` | |
| `rangeEnd` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Same as GET_RANDOM_INT_IN_RANGE

---

### _NETWORK_GET_RANDOM_FLOAT_RANGED

**Hash:** `0x04BD27B5ACB67067` | **Build:** `2802`

**C++ 原型:**
```cpp
float _NETWORK_GET_RANDOM_FLOAT_RANGED(float rangeStart, float rangeEnd)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._NETWORK_GET_RANDOM_FLOAT_RANGED, rangeStart, rangeEnd);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rangeStart` | `float` | `float` | |
| `rangeEnd` | `float` | `float` | |

**返回值:** `float` → `float`

---

### NETWORK_PLAYER_IS_CHEATER

**Hash:** `0x655B91F1495A9090` | **JHash:** `0xA51DC214` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_PLAYER_IS_CHEATER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PLAYER_IS_CHEATER);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_PLAYER_GET_CHEATER_REASON

**Hash:** `0x172F75B6EE2233BA` | **JHash:** `0x1720ABA6` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_PLAYER_GET_CHEATER_REASON(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_PLAYER_GET_CHEATER_REASON);
```

**返回值:** `int` → `int`

---

### NETWORK_PLAYER_IS_BADSPORT

**Hash:** `0x19D8DA0E5A68045A` | **JHash:** `0xA19708E3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_PLAYER_IS_BADSPORT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PLAYER_IS_BADSPORT);
```

**返回值:** `BOOL` → `bool`

---

### REMOTE_CHEATER_PLAYER_DETECTED

**Hash:** `0x472841A026D26D8B` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL REMOTE_CHEATER_PLAYER_DETECTED(Player player, int a, int b)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REMOTE_CHEATER_PLAYER_DETECTED, player, a, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `a` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### BAD_SPORT_PLAYER_LEFT_DETECTED

**Hash:** `0xEC5E3AF5289DCA81` | **JHash:** `0x4C2C6B6A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BAD_SPORT_PLAYER_LEFT_DETECTED(Any* gamerHandle, int event, int amountReceived)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BAD_SPORT_PLAYER_LEFT_DETECTED, gamerHandle, event, amountReceived);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `event` | `int` | `int` | |
| `amountReceived` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ADD_INVALID_OBJECT_MODEL

**Hash:** `0x7F562DBC212E81F9` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_ADD_INVALID_OBJECT_MODEL(Hash modelHash, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_INVALID_OBJECT_MODEL, modelHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_REMOVE_INVALID_OBJECT_MODEL

**Hash:** `0x791EDB5803B2F468` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_REMOVE_INVALID_OBJECT_MODEL(Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMOVE_INVALID_OBJECT_MODEL, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### NETWORK_CLEAR_INVALID_OBJECT_MODELS

**Hash:** `0x03B2F03A53D85E41` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_INVALID_OBJECT_MODELS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_INVALID_OBJECT_MODELS);
```

**返回值:** `void` → `void`

---

### NETWORK_APPLY_PED_SCAR_DATA

**Hash:** `0xE66C690248F11150` | **JHash:** `0x4818ACD0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_APPLY_PED_SCAR_DATA(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_APPLY_PED_SCAR_DATA, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT

**Hash:** `0x1CA59E306ECB80A5` | **JHash:** `0x470810ED` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT(int maxNumMissionParticipants, BOOL p1, int instanceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT, maxNumMissionParticipants, p1, instanceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxNumMissionParticipants` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `instanceId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_TRY_TO_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT

**Hash:** `0xD1110739EEADB592` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_TRY_TO_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT(Any p0, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TRY_TO_SET_THIS_SCRIPT_IS_NETWORK_SCRIPT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_THIS_SCRIPT_IS_NETWORK_SCRIPT

**Hash:** `0x2910669969E9535E` | **JHash:** `0xD9BF6549` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_THIS_SCRIPT_IS_NETWORK_SCRIPT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_THIS_SCRIPT_IS_NETWORK_SCRIPT);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_MAX_NUM_PARTICIPANTS

**Hash:** `0xA6C90FBC38E395EE` | **JHash:** `0xCCD8C02D` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_MAX_NUM_PARTICIPANTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_MAX_NUM_PARTICIPANTS);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_NUM_PARTICIPANTS

**Hash:** `0x18D0456E86604654` | **JHash:** `0x3E25A3C5` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_PARTICIPANTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_PARTICIPANTS);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_SCRIPT_STATUS

**Hash:** `0x57D158647A6BFABF` | **JHash:** `0x2BE9235A` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_SCRIPT_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_SCRIPT_STATUS);
```

**返回值:** `int` → `int`

---

### NETWORK_REGISTER_HOST_BROADCAST_VARIABLES

**Hash:** `0x3E9B2F01C50DF595` | **JHash:** `0xDAF3B0AE` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REGISTER_HOST_BROADCAST_VARIABLES(int* vars, int numVars, const char* debugName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REGISTER_HOST_BROADCAST_VARIABLES, vars, numVars, debugName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vars` | `int*` | `int*` | |
| `numVars` | `int` | `int` | |
| `debugName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_REGISTER_PLAYER_BROADCAST_VARIABLES

**Hash:** `0x3364AA97340CA215` | **JHash:** `0xBE3D32B4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REGISTER_PLAYER_BROADCAST_VARIABLES(int* vars, int numVars, const char* debugName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REGISTER_PLAYER_BROADCAST_VARIABLES, vars, numVars, debugName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vars` | `int*` | `int*` | |
| `numVars` | `int` | `int` | |
| `debugName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_REGISTER_HIGH_FREQUENCY_HOST_BROADCAST_VARIABLES

**Hash:** `0xEA8C0DDB10E2822A` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_REGISTER_HIGH_FREQUENCY_HOST_BROADCAST_VARIABLES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REGISTER_HIGH_FREQUENCY_HOST_BROADCAST_VARIABLES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_REGISTER_HIGH_FREQUENCY_PLAYER_BROADCAST_VARIABLES

**Hash:** `0xD6D7478CA62B8D41` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_REGISTER_HIGH_FREQUENCY_PLAYER_BROADCAST_VARIABLES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REGISTER_HIGH_FREQUENCY_PLAYER_BROADCAST_VARIABLES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_FINISH_BROADCASTING_DATA

**Hash:** `0x64F62AFB081E260D` | **JHash:** `0xA71A1D2A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_FINISH_BROADCASTING_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_FINISH_BROADCASTING_DATA);
```

**返回值:** `void` → `void`

---

### _NETWORK_GET_HOST_BROADCAST_DATA_SIZE_UNSYNCED

**Hash:** `0x72C8588ABE22C972` | **Build:** `3586`

**C++ 原型:**
```cpp
int _NETWORK_GET_HOST_BROADCAST_DATA_SIZE_UNSYNCED(Hash scriptNameHash, int instance, Hash positionHash, int handlerNum)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_HOST_BROADCAST_DATA_SIZE_UNSYNCED, scriptNameHash, instance, positionHash, handlerNum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptNameHash` | `Hash` | `uint` | |
| `instance` | `int` | `int` | |
| `positionHash` | `Hash` | `uint` | |
| `handlerNum` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _NETWORK_GET_PLAYER_BROADCAST_DATA_SIZE_UNSYNCED

**Hash:** `0xB99CD664FD4720A1` | **Build:** `3586`

**C++ 原型:**
```cpp
int _NETWORK_GET_PLAYER_BROADCAST_DATA_SIZE_UNSYNCED(Hash scriptNameHash, int instance, Hash positionHash, int handlerNum)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_PLAYER_BROADCAST_DATA_SIZE_UNSYNCED, scriptNameHash, instance, positionHash, handlerNum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptNameHash` | `Hash` | `uint` | |
| `instance` | `int` | `int` | |
| `positionHash` | `Hash` | `uint` | |
| `handlerNum` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _NETWORK_GET_BROADCAST_DATA_HOST_UPDATE_SIZE

**Hash:** `0xE448693B3EA3B92C` | **Build:** `3586`

**C++ 原型:**
```cpp
int _NETWORK_GET_BROADCAST_DATA_HOST_UPDATE_SIZE(Hash scriptNameHash, int instance, Hash positionHash, int handlerNum)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_BROADCAST_DATA_HOST_UPDATE_SIZE, scriptNameHash, instance, positionHash, handlerNum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptNameHash` | `Hash` | `uint` | |
| `instance` | `int` | `int` | |
| `positionHash` | `Hash` | `uint` | |
| `handlerNum` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _NETWORK_GET_BROADCAST_DATA_PLAYER_UPDATE_SIZE

**Hash:** `0xAC3F722321800755` | **Build:** `3586`

**C++ 原型:**
```cpp
int _NETWORK_GET_BROADCAST_DATA_PLAYER_UPDATE_SIZE(Hash scriptNameHash, int instance, Hash positionHash, int handlerNum)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_BROADCAST_DATA_PLAYER_UPDATE_SIZE, scriptNameHash, instance, positionHash, handlerNum);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptNameHash` | `Hash` | `uint` | |
| `instance` | `int` | `int` | |
| `positionHash` | `Hash` | `uint` | |
| `handlerNum` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_HAS_RECEIVED_HOST_BROADCAST_DATA

**Hash:** `0x5D10B3795F3FC886` | **JHash:** `0x0B739F53` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_RECEIVED_HOST_BROADCAST_DATA(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_RECEIVED_HOST_BROADCAST_DATA);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PLAYER_INDEX

**Hash:** `0x24FB80D107371267` | **JHash:** `0xBE1C1506` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_PLAYER_INDEX(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLAYER_INDEX, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Player` → `int`

---

### NETWORK_GET_PARTICIPANT_INDEX

**Hash:** `0x1B84DF6AF2A46938` | **JHash:** `0xC4D91094` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PARTICIPANT_INDEX(int index)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PARTICIPANT_INDEX, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_PLAYER_INDEX_FROM_PED

**Hash:** `0x6C0E2E0125610278` | **JHash:** `0x40DBF464` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_PLAYER_INDEX_FROM_PED(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLAYER_INDEX_FROM_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Player` → `int`

**说明:** Returns the Player associated to a given Ped when in an online session.

---

### NETWORK_GET_NUM_CONNECTED_PLAYERS

**Hash:** `0xA4A79DD2D9600654` | **JHash:** `0xF7952E62` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_CONNECTED_PLAYERS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_CONNECTED_PLAYERS);
```

**返回值:** `int` → `int`

**说明:** Returns the amount of players connected in the current session. Only works when connected to a session/server.

---

### NETWORK_IS_PLAYER_CONNECTED

**Hash:** `0x93DC1BE4E1ABE9D1` | **JHash:** `0x168EE2C2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_CONNECTED(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_CONNECTED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_TOTAL_NUM_PLAYERS

**Hash:** `0xCF61D4B4702EE9EB` | **JHash:** `0xF4F13B06` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_TOTAL_NUM_PLAYERS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_TOTAL_NUM_PLAYERS);
```

**返回值:** `int` → `int`

---

### NETWORK_IS_PARTICIPANT_ACTIVE

**Hash:** `0x6FF8FF40B6357D45` | **JHash:** `0x4E2C348B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PARTICIPANT_ACTIVE(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PARTICIPANT_ACTIVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_ACTIVE

**Hash:** `0xB8DFD30D6973E135` | **JHash:** `0x43657B17` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_ACTIVE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_ACTIVE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_A_PARTICIPANT

**Hash:** `0x3CA58F6CB7CBD784` | **JHash:** `0xB08B6992` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_A_PARTICIPANT(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_A_PARTICIPANT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_HOST_OF_THIS_SCRIPT

**Hash:** `0x83CD99A1E6061AB5` | **JHash:** `0x6970BA94` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_HOST_OF_THIS_SCRIPT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_HOST_OF_THIS_SCRIPT);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_HOST_OF_THIS_SCRIPT

**Hash:** `0xC7B4D79B01FA7A5C` | **JHash:** `0x89EA7B54` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_HOST_OF_THIS_SCRIPT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_HOST_OF_THIS_SCRIPT);
```

**返回值:** `Player` → `int`

---

### NETWORK_GET_HOST_OF_SCRIPT

**Hash:** `0x1D6A14F1F9A736FC` | **JHash:** `0x9C95D0BB` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_HOST_OF_SCRIPT(const char* scriptName, int instance_id, int position_hash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_HOST_OF_SCRIPT, scriptName, instance_id, position_hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `instance_id` | `int` | `int` | |
| `position_hash` | `int` | `int` | |

**返回值:** `Player` → `int`

**说明:** scriptName examples:
"freemode", "AM_CR_SecurityVan", ...

Most of the time, these values are used:
instance_id = -1
position_hash = 0

---

### NETWORK_GET_HOST_OF_THREAD

**Hash:** `0xD3DEBE2991FE8098` | **Build:** `3717`

**C++ 原型:**
```cpp
Player NETWORK_GET_HOST_OF_THREAD(int threadId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_HOST_OF_THREAD, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `Player` → `int`

---

### NETWORK_SET_MISSION_FINISHED

**Hash:** `0x3B3D11CD9FFCDFC9` | **JHash:** `0x3083FAD7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_MISSION_FINISHED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_MISSION_FINISHED);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_SCRIPT_ACTIVE

**Hash:** `0x9D40DF90FAD26098` | **JHash:** `0x4A65250C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SCRIPT_ACTIVE(const char* scriptName, int instance_id, BOOL p2, int position_hash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SCRIPT_ACTIVE, scriptName, instance_id, p2, position_hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `instance_id` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `position_hash` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_SCRIPT_ACTIVE_BY_HASH

**Hash:** `0xDA7DE67F5FE5EE13` | **Build:** `2245`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_SCRIPT_ACTIVE_BY_HASH(Hash scriptHash, int p1, BOOL p2, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_SCRIPT_ACTIVE_BY_HASH, scriptHash, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_THREAD_A_NETWORK_SCRIPT

**Hash:** `0x560B423D73015E77` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_THREAD_A_NETWORK_SCRIPT(int threadId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_THREAD_A_NETWORK_SCRIPT, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_NUM_SCRIPT_PARTICIPANTS

**Hash:** `0x3658E8CD94FC121A` | **JHash:** `0x8F7D9F46` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_SCRIPT_PARTICIPANTS(const char* scriptName, int instance_id, int position_hash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_SCRIPT_PARTICIPANTS, scriptName, instance_id, position_hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |
| `instance_id` | `int` | `int` | |
| `position_hash` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_INSTANCE_ID_OF_THIS_SCRIPT

**Hash:** `0x638A3A81733086DB` | **JHash:** `0xDB8B5D71` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_INSTANCE_ID_OF_THIS_SCRIPT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_INSTANCE_ID_OF_THIS_SCRIPT);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_POSITION_HASH_OF_THIS_SCRIPT

**Hash:** `0x257ED0FADF750BCF` | **Build:** `2372`

**C++ 原型:**
```cpp
Hash NETWORK_GET_POSITION_HASH_OF_THIS_SCRIPT(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.NETWORK_GET_POSITION_HASH_OF_THIS_SCRIPT);
```

**返回值:** `Hash` → `uint`

---

### NETWORK_IS_PLAYER_A_PARTICIPANT_ON_SCRIPT

**Hash:** `0x1AD5B71586B94820` | **JHash:** `0xCEA55F4C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_A_PARTICIPANT_ON_SCRIPT(Player player, const char* script, int instance_id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_A_PARTICIPANT_ON_SCRIPT, player, script, instance_id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `script` | `const char*` | `string` | |
| `instance_id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_PREVENT_SCRIPT_HOST_MIGRATION

**Hash:** `0x2302C0264EA58D31` | **JHash:** `0x8DCFE77D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_PREVENT_SCRIPT_HOST_MIGRATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_PREVENT_SCRIPT_HOST_MIGRATION);
```

**返回值:** `void` → `void`

---

### NETWORK_REQUEST_TO_BE_HOST_OF_THIS_SCRIPT

**Hash:** `0x741A3D8380319A81` | **JHash:** `0x331D9A27` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REQUEST_TO_BE_HOST_OF_THIS_SCRIPT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REQUEST_TO_BE_HOST_OF_THIS_SCRIPT);
```

**返回值:** `void` → `void`

---

### PARTICIPANT_ID

**Hash:** `0x90986E8876CE0A83` | **JHash:** `0x9C35A221` | **Build:** `323`

**C++ 原型:**
```cpp
Player PARTICIPANT_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PARTICIPANT_ID);
```

**返回值:** `Player` → `int`

**说明:** Return the local Participant ID

---

### PARTICIPANT_ID_TO_INT

**Hash:** `0x57A3BDDAD8E5AA0A` | **JHash:** `0x907498B0` | **Build:** `323`

**C++ 原型:**
```cpp
int PARTICIPANT_ID_TO_INT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PARTICIPANT_ID_TO_INT);
```

**返回值:** `int` → `int`

**说明:** Return the local Participant ID.

This native is exactly the same as 'PARTICIPANT_ID' native.

---

### NETWORK_GET_KILLER_OF_PLAYER

**Hash:** `0x2DA41ED6E1FCD7A5` | **Build:** `463`

**C++ 原型:**
```cpp
Player NETWORK_GET_KILLER_OF_PLAYER(Player player, Hash* weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_KILLER_OF_PLAYER, player, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |

**返回值:** `Player` → `int`

---

### NETWORK_GET_DESTROYER_OF_NETWORK_ID

**Hash:** `0x7A1ADEEF01740A24` | **JHash:** `0x4FCA6436` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_DESTROYER_OF_NETWORK_ID(int netId, Hash* weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_DESTROYER_OF_NETWORK_ID, netId, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |

**返回值:** `Player` → `int`

---

### NETWORK_GET_DESTROYER_OF_ENTITY

**Hash:** `0xC434133D9BA52777` | **Build:** `463`

**C++ 原型:**
```cpp
Player NETWORK_GET_DESTROYER_OF_ENTITY(Entity entity, Hash* weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_DESTROYER_OF_ENTITY, entity, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |

**返回值:** `Player` → `int`

---

### NETWORK_GET_ASSISTED_KILL_OF_ENTITY

**Hash:** `0x83660B734994124D` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_ASSISTED_KILL_OF_ENTITY(Player player, Entity entity, int* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_ASSISTED_KILL_OF_ENTITY, player, entity, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |
| `p2` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** NETWORK_GET_ASSISTED_DAMAGE_OF_ENTITY that ensures the entity is dead (IS_ENTITY_DEAD)

---

### NETWORK_GET_ASSISTED_DAMAGE_OF_ENTITY

**Hash:** `0x4CACA84440FA26F6` | **JHash:** `0x28A45454` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_ASSISTED_DAMAGE_OF_ENTITY(Player player, Entity entity, int* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_ASSISTED_DAMAGE_OF_ENTITY, player, entity, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |
| `p2` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_ENTITY_KILLER_OF_PLAYER

**Hash:** `0x42B2DAA6B596F5F8` | **JHash:** `0xA7E7E04F` | **Build:** `323`

**C++ 原型:**
```cpp
Entity NETWORK_GET_ENTITY_KILLER_OF_PLAYER(Player player, Hash* weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_ENTITY_KILLER_OF_PLAYER, player, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |

**返回值:** `Entity` → `int`

---

### NETWORK_SET_CURRENT_PUBLIC_CONTENT_ID

**Hash:** `0x2C863ACDCD12B3DB` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SET_CURRENT_PUBLIC_CONTENT_ID(const char* missionId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_CURRENT_PUBLIC_CONTENT_ID, missionId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `missionId` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_CURRENT_CHAT_OPTION

**Hash:** `0x3DAD00265FBF356B` | **Build:** `2802`

**C++ 原型:**
```cpp
void NETWORK_SET_CURRENT_CHAT_OPTION(int newChatOption)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_CURRENT_CHAT_OPTION, newChatOption);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `newChatOption` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_CURRENT_SPAWN_LOCATION_OPTION

**Hash:** `0xAA6D5451DC3448B6` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SET_CURRENT_SPAWN_LOCATION_OPTION(Hash mpSettingSpawn)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_CURRENT_SPAWN_LOCATION_OPTION, mpSettingSpawn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mpSettingSpawn` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** mpSettingSpawn:

enum eMpSettingSpawn
{
	MP_SETTING_SPAWN_NULL,
	MP_SETTING_SPAWN_PROPERTY,
	MP_SETTING_SPAWN_LAST_POSITION,
	MP_SETTING_SPAWN_GARAGE,
	MP_SETTING_SPAWN_RANDOM,
	MP_SETTING_SPAWN_PRIVATE_YACHT,
	MP_SETTING_SPAWN_OFFICE,
	MP_SETTING_SPAWN_CLUBHOUSE,
	MP_SETTING_SPAWN_IE_WAREHOUSE,
	MP_SETTING_SPAWN_BUNKER,
	MP_SETTING_SPAWN_HANGAR,
	MP_SETTING_SPAWN_DEFUNCT_BASE,
	MP_SETTING_SPAWN_NIGHTCLUB,
	MP_SETTING_SPAWN_ARENA_GARAGE,
	MP_SETTING_SPAWN_CASINO_APARTMENT,
	MP_SETTING_SPAWN_ARCADE,
	MP_SETTING_SPAWN_SUBMARINE,
	MP_SETTING_SPAWN_CAR_MEET,
	MP_SETTING_SPAWN_AUTO_SHOP,
	MP_SETTING_SPAWN_FIXER_HQ,
	MP_SETTING_SPAWN_MAX,
};

---

### NETWORK_SET_VEHICLE_DRIVEN_IN_TEST_DRIVE

**Hash:** `0x8C70252FC40F320B` | **Build:** `2699`

**C++ 原型:**
```cpp
void NETWORK_SET_VEHICLE_DRIVEN_IN_TEST_DRIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_VEHICLE_DRIVEN_IN_TEST_DRIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used by MetricVEHICLE_DIST_DRIVEN

---

### NETWORK_SET_VEHICLE_DRIVEN_LOCATION

**Hash:** `0xA0CE91E47531D3BB` | **Build:** `2944`

**C++ 原型:**
```cpp
void NETWORK_SET_VEHICLE_DRIVEN_LOCATION(Hash location)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_VEHICLE_DRIVEN_LOCATION, location);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `location` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Sets 'loc' variable used in MetricVEHICLE_DIST_DRIVEN

---

### NETWORK_RESURRECT_LOCAL_PLAYER

**Hash:** `0xEA23C49EAA83ACFB` | **JHash:** `0xF1F9D4B4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_RESURRECT_LOCAL_PLAYER(float x, float y, float z, float heading, BOOL p4, BOOL changetime, BOOL p6, int p7, int p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_RESURRECT_LOCAL_PLAYER, x, y, z, heading, p4, changetime, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `changetime` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `int` | `int` | |
| `p8` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_LOCAL_PLAYER_INVINCIBLE_TIME

**Hash:** `0x2D95C7E2D7E07307` | **JHash:** `0xFEA9B85C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_LOCAL_PLAYER_INVINCIBLE_TIME(int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_LOCAL_PLAYER_INVINCIBLE_TIME, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_LOCAL_PLAYER_INVINCIBLE

**Hash:** `0x8A8694B48715B000` | **JHash:** `0x8DE13B36` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_LOCAL_PLAYER_INVINCIBLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_LOCAL_PLAYER_INVINCIBLE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_DISABLE_INVINCIBLE_FLASHING

**Hash:** `0x9DD368BF06983221` | **JHash:** `0x8D27280E` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DISABLE_INVINCIBLE_FLASHING(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DISABLE_INVINCIBLE_FLASHING, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_PATCH_POST_CUTSCENE_HS4F_TUN_ENT

**Hash:** `0xF0BC9BCD24A511D5` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_PATCH_POST_CUTSCENE_HS4F_TUN_ENT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_PATCH_POST_CUTSCENE_HS4F_TUN_ENT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_LOCAL_PLAYER_SYNC_LOOK_AT

**Hash:** `0x524FF0AEFF9C3973` | **JHash:** `0xB72F086D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_LOCAL_PLAYER_SYNC_LOOK_AT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_LOCAL_PLAYER_SYNC_LOOK_AT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_HAS_ENTITY_BEEN_REGISTERED_WITH_THIS_THREAD

**Hash:** `0xB07D3185E11657A5` | **JHash:** `0xEDA68956` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_ENTITY_BEEN_REGISTERED_WITH_THIS_THREAD(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_ENTITY_BEEN_REGISTERED_WITH_THIS_THREAD, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_NETWORK_ID_FROM_ENTITY

**Hash:** `0xA11700682F3AD45C` | **JHash:** `0x9E35DAB6` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NETWORK_ID_FROM_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NETWORK_ID_FROM_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_ENTITY_FROM_NETWORK_ID

**Hash:** `0xCE4E5D9B0A4FF560` | **JHash:** `0x5B912C3F` | **Build:** `323`

**C++ 原型:**
```cpp
Entity NETWORK_GET_ENTITY_FROM_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_ENTITY_FROM_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `Entity` → `int`

---

### NETWORK_GET_ENTITY_IS_NETWORKED

**Hash:** `0xC7827959479DCC78` | **JHash:** `0xD7F934F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_ENTITY_IS_NETWORKED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_ENTITY_IS_NETWORKED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_ENTITY_IS_LOCAL

**Hash:** `0x0991549DE4D64762` | **JHash:** `0x813353ED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_ENTITY_IS_LOCAL(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_ENTITY_IS_LOCAL, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_REGISTER_ENTITY_AS_NETWORKED

**Hash:** `0x06FAACD625D80CAA` | **JHash:** `0x31A630A4` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REGISTER_ENTITY_AS_NETWORKED(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REGISTER_ENTITY_AS_NETWORKED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_UNREGISTER_NETWORKED_ENTITY

**Hash:** `0x7368E683BB9038D6` | **JHash:** `0x5C645F64` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_UNREGISTER_NETWORKED_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_UNREGISTER_NETWORKED_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_DOES_NETWORK_ID_EXIST

**Hash:** `0x38CE16C96BD11344` | **JHash:** `0xB8D2C99E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DOES_NETWORK_ID_EXIST(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DOES_NETWORK_ID_EXIST, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DOES_ENTITY_EXIST_WITH_NETWORK_ID

**Hash:** `0x18A47D074708FD68` | **JHash:** `0x1E2E3177` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DOES_ENTITY_EXIST_WITH_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DOES_ENTITY_EXIST_WITH_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_REQUEST_CONTROL_OF_NETWORK_ID

**Hash:** `0xA670B3662FAFFBD0` | **JHash:** `0x9262A60A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REQUEST_CONTROL_OF_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REQUEST_CONTROL_OF_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_CONTROL_OF_NETWORK_ID

**Hash:** `0x4D36070FE0215186` | **JHash:** `0x92E77D21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CONTROL_OF_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CONTROL_OF_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_NETWORK_ID_REMOTELY_CONTROLLED

**Hash:** `0x7242F8B741CE1086` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_NETWORK_ID_REMOTELY_CONTROLLED(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_NETWORK_ID_REMOTELY_CONTROLLED, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the specified network id is controlled by someone else.

---

### NETWORK_REQUEST_CONTROL_OF_ENTITY

**Hash:** `0xB69317BF5E782347` | **JHash:** `0xA05FEBD7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REQUEST_CONTROL_OF_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REQUEST_CONTROL_OF_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_REQUEST_CONTROL_OF_DOOR

**Hash:** `0x870DDFD5A4A796E4` | **JHash:** `0xF60DAAF6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REQUEST_CONTROL_OF_DOOR(int doorID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REQUEST_CONTROL_OF_DOOR, doorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_CONTROL_OF_ENTITY

**Hash:** `0x01BF60A500E28887` | **JHash:** `0x005FD797` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CONTROL_OF_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CONTROL_OF_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_CONTROL_OF_PICKUP

**Hash:** `0x5BC9495F0B3B6FA6` | **JHash:** `0xF7784FC8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CONTROL_OF_PICKUP(Pickup pickup)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CONTROL_OF_PICKUP, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_CONTROL_OF_DOOR

**Hash:** `0xCB3C68ADB06195DF` | **JHash:** `0x136326EC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CONTROL_OF_DOOR(Hash doorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CONTROL_OF_DOOR, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_DOOR_NETWORKED

**Hash:** `0xC01E93FAC20C3346` | **JHash:** `0xD14D9C07` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_DOOR_NETWORKED(Hash doorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_DOOR_NETWORKED, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### VEH_TO_NET

**Hash:** `0xB4C94523F023419C` | **JHash:** `0xF17634EB` | **Build:** `323`

**C++ 原型:**
```cpp
int VEH_TO_NET(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.VEH_TO_NET, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** calls from vehicle to net.


---

### PED_TO_NET

**Hash:** `0x0EDEC3C276198689` | **JHash:** `0x86A0B759` | **Build:** `323`

**C++ 原型:**
```cpp
int PED_TO_NET(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PED_TO_NET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** gets the network id of a ped

---

### OBJ_TO_NET

**Hash:** `0x99BFDC94A603E541` | **JHash:** `0x1E05F29F` | **Build:** `323`

**C++ 原型:**
```cpp
int OBJ_TO_NET(Object object)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.OBJ_TO_NET, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `int` → `int`

**说明:** Lets objects spawn online simply do it like this:

int createdObject = OBJ_TO_NET(CREATE_OBJECT_NO_OFFSET(oball, pCoords.x, pCoords.y, pCoords.z, 1, 0, 0));

---

### NET_TO_VEH

**Hash:** `0x367B936610BA360C` | **JHash:** `0x7E02FAEA` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle NET_TO_VEH(int netHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_TO_VEH, netHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

---

### NET_TO_PED

**Hash:** `0xBDCD95FC216A8B3E` | **JHash:** `0x87717DD4` | **Build:** `323`

**C++ 原型:**
```cpp
Ped NET_TO_PED(int netHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_TO_PED, netHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `int` | `int` | |

**返回值:** `Ped` → `int`

**说明:** gets the ped id of a network id

---

### NET_TO_OBJ

**Hash:** `0xD8515F5FEA14CB3F` | **JHash:** `0x27AA14D8` | **Build:** `323`

**C++ 原型:**
```cpp
Object NET_TO_OBJ(int netHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_TO_OBJ, netHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `int` | `int` | |

**返回值:** `Object` → `int`

**说明:** gets the object id of a network id

---

### NET_TO_ENT

**Hash:** `0xBFFEAB45A9A9094A` | **JHash:** `0x5E149683` | **Build:** `323`

**C++ 原型:**
```cpp
Entity NET_TO_ENT(int netHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NET_TO_ENT, netHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `int` | `int` | |

**返回值:** `Entity` → `int`

**说明:** gets the entity id of a network id

---

### NETWORK_GET_LOCAL_HANDLE

**Hash:** `0xE86051786B66CD8E` | **JHash:** `0x08023B16` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_LOCAL_HANDLE(Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_LOCAL_HANDLE, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_HANDLE_FROM_USER_ID

**Hash:** `0xDCD51DD8F87AEC5C` | **JHash:** `0x74C2C1B7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_HANDLE_FROM_USER_ID(const char* userId, Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_HANDLE_FROM_USER_ID, userId, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `userId` | `const char*` | `string` | |
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_HANDLE_FROM_MEMBER_ID

**Hash:** `0xA0FD21BED61E5C4C` | **JHash:** `0x9BFC9FE2` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_HANDLE_FROM_MEMBER_ID(const char* memberId, Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_HANDLE_FROM_MEMBER_ID, memberId, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `memberId` | `const char*` | `string` | |
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_HANDLE_FROM_PLAYER

**Hash:** `0x388EB2B86C73B6B3` | **JHash:** `0xD3498917` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_HANDLE_FROM_PLAYER(Player player, Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_HANDLE_FROM_PLAYER, player, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_HASH_FROM_PLAYER_HANDLE

**Hash:** `0xBC1D768F2F5D6C05` | **JHash:** `0xF8D7AF3B` | **Build:** `323`

**C++ 原型:**
```cpp
Hash NETWORK_HASH_FROM_PLAYER_HANDLE(Player player)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.NETWORK_HASH_FROM_PLAYER_HANDLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Hash` → `uint`

---

### NETWORK_HASH_FROM_GAMER_HANDLE

**Hash:** `0x58575AC3CF2CA8EC` | **JHash:** `0x5AC9F04D` | **Build:** `323`

**C++ 原型:**
```cpp
Hash NETWORK_HASH_FROM_GAMER_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.NETWORK_HASH_FROM_GAMER_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `Hash` → `uint`

---

### NETWORK_HANDLE_FROM_FRIEND

**Hash:** `0xD45CB817D7E177D2` | **JHash:** `0x3B0BB3A3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_HANDLE_FROM_FRIEND(int friendIndex, Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_HANDLE_FROM_FRIEND, friendIndex, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendIndex` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GAMERTAG_FROM_HANDLE_START

**Hash:** `0x9F0C0A981D73FA56` | **JHash:** `0xEBA00C2A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GAMERTAG_FROM_HANDLE_START(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GAMERTAG_FROM_HANDLE_START, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GAMERTAG_FROM_HANDLE_PENDING

**Hash:** `0xB071E27958EF4CF0` | **JHash:** `0xF000828E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GAMERTAG_FROM_HANDLE_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GAMERTAG_FROM_HANDLE_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GAMERTAG_FROM_HANDLE_SUCCEEDED

**Hash:** `0xFD00798DBA7523DD` | **JHash:** `0x89C2B5EA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GAMERTAG_FROM_HANDLE_SUCCEEDED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GAMERTAG_FROM_HANDLE_SUCCEEDED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_GAMERTAG_FROM_HANDLE

**Hash:** `0x426141162EBE5CDB` | **JHash:** `0xA18A1B26` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_GAMERTAG_FROM_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_GAMERTAG_FROM_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `const char*` → `string`

---

### NETWORK_DISPLAYNAMES_FROM_HANDLES_START

**Hash:** `0xD66C9E72B3CC4982` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_DISPLAYNAMES_FROM_HANDLES_START(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_DISPLAYNAMES_FROM_HANDLES_START, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** Hardcoded to return -1.

---

### NETWORK_GET_DISPLAYNAMES_FROM_HANDLES

**Hash:** `0x58CC181719256197` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_DISPLAYNAMES_FROM_HANDLES(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_DISPLAYNAMES_FROM_HANDLES, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 0.

---

### NETWORK_ARE_HANDLES_THE_SAME

**Hash:** `0x57DBA049E110F217` | **JHash:** `0x45975AE3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ARE_HANDLES_THE_SAME(Any* gamerHandle1, Any* gamerHandle2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ARE_HANDLES_THE_SAME, gamerHandle1, gamerHandle2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle1` | `Any*` | `object*` | |
| `gamerHandle2` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_HANDLE_VALID

**Hash:** `0x6F79B93B0A8E4133` | **JHash:** `0xF0996C6E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_HANDLE_VALID(Any* gamerHandle, int gamerHandleSize)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_HANDLE_VALID, gamerHandle, gamerHandleSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `gamerHandleSize` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PLAYER_FROM_GAMER_HANDLE

**Hash:** `0xCE5F689CF5A0A49D` | **JHash:** `0x2E96EF1E` | **Build:** `323`

**C++ 原型:**
```cpp
Player NETWORK_GET_PLAYER_FROM_GAMER_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLAYER_FROM_GAMER_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `Player` → `int`

---

### NETWORK_MEMBER_ID_FROM_GAMER_HANDLE

**Hash:** `0xC82630132081BB6F` | **JHash:** `0x62EF0A63` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_MEMBER_ID_FROM_GAMER_HANDLE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_MEMBER_ID_FROM_GAMER_HANDLE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `const char*` → `string`

---

### NETWORK_IS_GAMER_IN_MY_SESSION

**Hash:** `0x0F10B05DDF8D16E9` | **JHash:** `0x59127716` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GAMER_IN_MY_SESSION(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GAMER_IN_MY_SESSION, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SHOW_PROFILE_UI

**Hash:** `0x859ED1CEA343FCA8` | **JHash:** `0xF00A20B0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SHOW_PROFILE_UI(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SHOW_PROFILE_UI, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_PLAYER_GET_NAME

**Hash:** `0x7718D2E2060837D2` | **JHash:** `0xCE48F260` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_PLAYER_GET_NAME(Player player)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_PLAYER_GET_NAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns the name of a given player. Returns "**Invalid**" if rlGamerInfo of the given player cannot be retrieved or the player doesn't exist.

---

### NETWORK_PLAYER_GET_USERID

**Hash:** `0x4927FC39CD0869A0` | **JHash:** `0x4EC0D983` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_PLAYER_GET_USERID(Player player, int* userID)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_PLAYER_GET_USERID, player, userID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `userID` | `int*` | `int*` | |

**返回值:** `const char*` → `string`

**说明:** Returns a string of the player's Rockstar Id. 
Takes a 24 char buffer. Returns the buffer or "**Invalid**" if rlGamerInfo of the given player cannot be retrieved or the player doesn't exist.

---

### NETWORK_PLAYER_IS_ROCKSTAR_DEV

**Hash:** `0x544ABDDA3B409B6D` | **JHash:** `0xF6659045` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_PLAYER_IS_ROCKSTAR_DEV(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PLAYER_IS_ROCKSTAR_DEV, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if a specific value (BYTE) in CNetGamePlayer is nonzero.
Returns always false in Singleplayer.

No longer used for dev checks since first mods were released on PS3 & 360.
R* now checks with the IS_DLC_PRESENT native for the dlc hash 2532323046,
if that is present it will unlock dev stuff.

---

### NETWORK_PLAYER_INDEX_IS_CHEATER

**Hash:** `0x565E430DB3B05BEC` | **JHash:** `0xD265B049` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_PLAYER_INDEX_IS_CHEATER(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PLAYER_INDEX_IS_CHEATER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ENTITY_GET_OBJECT_ID

**Hash:** `0x815F18AD865F057F` | **Build:** `463`

**C++ 原型:**
```cpp
int NETWORK_ENTITY_GET_OBJECT_ID(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_ENTITY_GET_OBJECT_ID, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_ENTITY_FROM_OBJECT_ID

**Hash:** `0x37D5F739FD494675` | **Build:** `505`

**C++ 原型:**
```cpp
Entity NETWORK_GET_ENTITY_FROM_OBJECT_ID(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_ENTITY_FROM_OBJECT_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `Entity` → `int`

**说明:** I've had this return the player's ped handle sometimes, but also other random entities.
Whatever p0 is, it's at least not synced to other players.
At least not all the time, some p0 values actually output the same entity, (different handle of course, but same entity).
But another p0 value may return an entity for player x, but not for player y (it'll just return -1 even if the entity exists on both clients).

Returns an entity handle or -1, value changes based on p0's value.

---

### NETWORK_IS_INACTIVE_PROFILE

**Hash:** `0x7E58745504313A2E` | **JHash:** `0x95481343` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_INACTIVE_PROFILE(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_INACTIVE_PROFILE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### _NETWORK_LOAD_GAMER_DISPLAY_NAME

**Hash:** `0x338ECE3637937BC2` | **Build:** `3258`

**C++ 原型:**
```cpp
const char* _NETWORK_LOAD_GAMER_DISPLAY_NAME(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_LOAD_GAMER_DISPLAY_NAME, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `const char*` → `string`

**说明:** Returns the name from given friend gamer handle.

---

### NETWORK_GET_MAX_FRIENDS

**Hash:** `0xAFEBB0D5D8F687D2` | **JHash:** `0x048171BC` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_MAX_FRIENDS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_MAX_FRIENDS);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_FRIEND_COUNT

**Hash:** `0x203F1CFD823B27A4` | **JHash:** `0xA396ACDE` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_FRIEND_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_FRIEND_COUNT);
```

**返回值:** `int` → `int`

---

### NETWORK_GET_FRIEND_NAME

**Hash:** `0xE11EBBB2A783FE8B` | **JHash:** `0x97420B6D` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_FRIEND_NAME(int friendIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_FRIEND_NAME, friendIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_GET_FRIEND_DISPLAY_NAME

**Hash:** `0x4164F227D052E293` | **Build:** `323`

**C++ 原型:**
```cpp
const char* NETWORK_GET_FRIEND_DISPLAY_NAME(int friendIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.NETWORK_GET_FRIEND_DISPLAY_NAME, friendIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### NETWORK_IS_FRIEND_ONLINE

**Hash:** `0x425A44533437B64D` | **JHash:** `0xE0A42430` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND_ONLINE(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND_ONLINE, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_FRIEND_HANDLE_ONLINE

**Hash:** `0x87EB7A3FFCB314DB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND_HANDLE_ONLINE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND_HANDLE_ONLINE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_FRIEND_IN_SAME_TITLE

**Hash:** `0x2EA9A3BEDF3F17B8` | **JHash:** `0xC54365C2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND_IN_SAME_TITLE(const char* friendName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND_IN_SAME_TITLE, friendName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** In scripts R* calls 'NETWORK_GET_FRIEND_NAME' in this param.

---

### NETWORK_IS_FRIEND_IN_MULTIPLAYER

**Hash:** `0x57005C18827F3A28` | **JHash:** `0x400BDDD9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND_IN_MULTIPLAYER(const char* friendName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND_IN_MULTIPLAYER, friendName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_FRIEND

**Hash:** `0x1A24A179F9B31654` | **JHash:** `0x2DA4C282` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PENDING_FRIEND

**Hash:** `0x0BE73DA6984A6E33` | **JHash:** `0x5C85FF81` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PENDING_FRIEND(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PENDING_FRIEND, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### NETWORK_IS_ADDING_FRIEND

**Hash:** `0x6EA101606F6E4D81` | **JHash:** `0xBB7EC8C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ADDING_FRIEND(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ADDING_FRIEND);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ADD_FRIEND

**Hash:** `0x8E02D73914064223` | **JHash:** `0x20E5B3EE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ADD_FRIEND(Any* gamerHandle, const char* message)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ADD_FRIEND, gamerHandle, message);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `message` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_FRIEND_INDEX_ONLINE

**Hash:** `0xBAD8F2A42B844821` | **JHash:** `0x94AE7172` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_FRIEND_INDEX_ONLINE(int friendIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_FRIEND_INDEX_ONLINE, friendIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `friendIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_PLAYER_IS_PASSIVE

**Hash:** `0x1B857666604B1A74` | **JHash:** `0x76A9FEB6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_PLAYER_IS_PASSIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_PLAYER_IS_PASSIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_PLAYER_OWNS_WAYPOINT

**Hash:** `0x82377B65E943F72D` | **JHash:** `0xB802B671` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PLAYER_OWNS_WAYPOINT(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PLAYER_OWNS_WAYPOINT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_SET_WAYPOINT

**Hash:** `0xC927EC229934AF60` | **JHash:** `0x009E68F3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SET_WAYPOINT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SET_WAYPOINT);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IGNORE_REMOTE_WAYPOINTS

**Hash:** `0x4C2A9FDC22377075` | **Build:** `372`

**C++ 原型:**
```cpp
void NETWORK_IGNORE_REMOTE_WAYPOINTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_IGNORE_REMOTE_WAYPOINTS);
```

**返回值:** `void` → `void`

---

### _NETWORK_DOES_COMMUNICATION_GROUP_HAVE_PERMISSION

**Hash:** `0xDBDF80673BBA3D65` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_DOES_COMMUNICATION_GROUP_HAVE_PERMISSION(int communicationType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_DOES_COMMUNICATION_GROUP_HAVE_PERMISSION, communicationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** communicationType: 0 = VOICE; 1 = TEXT_CHAT; 2 = TEXT_MESSAGE; 3 = EMAIL; 4 = USER_CONTENT; 5 = USER_TEXT

---

### _NETWORK_DOES_COMMUNICATION_GROUP_HAVE_SETTINGS_ENABLED

**Hash:** `0x20C12650830A64EC` | **Build:** `3570`

**C++ 原型:**
```cpp
BOOL _NETWORK_DOES_COMMUNICATION_GROUP_HAVE_SETTINGS_ENABLED(int communicationType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_DOES_COMMUNICATION_GROUP_HAVE_SETTINGS_ENABLED, communicationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _NETWORK_GET_COMMUNICATION_GROUP_FLAGS

**Hash:** `0x40DF02F371F40883` | **Build:** `3095`

**C++ 原型:**
```cpp
int _NETWORK_GET_COMMUNICATION_GROUP_FLAGS(int communicationType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_COMMUNICATION_GROUP_FLAGS, communicationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns communicationGroupFlag
communicationType: see 0xDBDF80673BBA3D65

enum eCommunicationGroupFlag
{
	COMMUNICATION_GROUP_LOCAL_PLAYER = 1 << 0,
	COMMUNICATION_GROUP_FRIENDS = 1 << 1,
	COMMUNICATION_GROUP_SMALL_CREW = 1 << 2,
	COMMUNICATION_GROUP_LARGE_CREW = 1 << 3,
	COMMUNICATION_GROUP_RECENT_PLAYER = 1 << 4,
	COMMUNICATION_GROUP_SAME_SESSION = 1 << 5,
	COMMUNICATION_GROUP_SAME_TEAM = 1 << 6,
	COMMUNICATION_GROUP_INVALID = 1 << 7,
};

---

### _NETWORK_GET_COMMUNICATION_GROUP_DEFAULT_FLAGS

**Hash:** `0x97F35B898D2D067F` | **Build:** `3570`

**C++ 原型:**
```cpp
int _NETWORK_GET_COMMUNICATION_GROUP_DEFAULT_FLAGS(int communicationType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_COMMUNICATION_GROUP_DEFAULT_FLAGS, communicationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _NETWORK_GET_COMMUNICATION_GROUP_VALUE

**Hash:** `0xBF66ACD9AE81A99F` | **Build:** `3570`

**C++ 原型:**
```cpp
int _NETWORK_GET_COMMUNICATION_GROUP_VALUE(int communicationType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_COMMUNICATION_GROUP_VALUE, communicationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _NETWORK_SET_COMMUNICATION_GROUP_FLAGS

**Hash:** `0xE549F846DE7D32D5` | **Build:** `3095`

**C++ 原型:**
```cpp
void _NETWORK_SET_COMMUNICATION_GROUP_FLAGS(int communicationType, int communicationGroupFlag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SET_COMMUNICATION_GROUP_FLAGS, communicationType, communicationGroupFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `communicationType` | `int` | `int` | |
| `communicationGroupFlag` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** communicationType: see 0xDBDF80673BBA3D65
communicationGroupFlag: see 0x40DF02F371F40883

---

### _NETWORK_HAS_PLAYER_PASSED_CHECK_TYPE

**Hash:** `0x62E29CDA11F9C230` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL _NETWORK_HAS_PLAYER_PASSED_CHECK_TYPE(int checkType, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_HAS_PLAYER_PASSED_CHECK_TYPE, checkType, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `checkType` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** _NETWORK_HAS_P*
checkType 3 is similar as using 0x9F633448E4C73207

---

### NETWORK_IS_PLAYER_ON_BLOCKLIST

**Hash:** `0xAD4326FCA30D62F8` | **Build:** `2802`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_ON_BLOCKLIST(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_ON_BLOCKLIST, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_SCRIPT_AUTOMUTED

**Hash:** `0xB309EBEA797E001F` | **JHash:** `0x5C0AB2A9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SET_SCRIPT_AUTOMUTED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SET_SCRIPT_AUTOMUTED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_AUTOMUTE_OVERRIDE

**Hash:** `0x26F07DD83A5F7F98` | **JHash:** `0x9A176B6E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_AUTOMUTE_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_AUTOMUTE_OVERRIDE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_HEADSET

**Hash:** `0xE870F9F1F7B4F1FA` | **JHash:** `0xA7DC5657` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_HEADSET(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_HEADSET);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_LOOK_AT_TALKERS

**Hash:** `0x7D395EA61622E116` | **JHash:** `0x5C05B7E1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_LOOK_AT_TALKERS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_LOOK_AT_TALKERS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_PUSH_TO_TALK_ACTIVE

**Hash:** `0xC0D2AF00BCC234CA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PUSH_TO_TALK_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PUSH_TO_TALK_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GAMER_HAS_HEADSET

**Hash:** `0xF2FD55CB574BCC55` | **JHash:** `0xD036DA4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GAMER_HAS_HEADSET(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GAMER_HAS_HEADSET, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_GAMER_TALKING

**Hash:** `0x71C33B22606CD88A` | **JHash:** `0x99B58DBC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GAMER_TALKING(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GAMER_TALKING, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_PERMISSIONS_HAS_GAMER_RECORD

**Hash:** `0x559EBF901A8C68E0` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL NETWORK_PERMISSIONS_HAS_GAMER_RECORD(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PERMISSIONS_HAS_GAMER_RECORD, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_COMMUNICATE_WITH_GAMER

**Hash:** `0x8F5D1AD832AEB06C` | **JHash:** `0xD05EB7F6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_COMMUNICATE_WITH_GAMER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_COMMUNICATE_WITH_GAMER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_TEXT_CHAT_WITH_GAMER

**Hash:** `0xA150A4F065806B1F` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_TEXT_CHAT_WITH_GAMER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_TEXT_CHAT_WITH_GAMER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_GAMER_MUTED_BY_ME

**Hash:** `0xCE60DE011B6C7978` | **JHash:** `0x001B4046` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GAMER_MUTED_BY_ME(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GAMER_MUTED_BY_ME, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_AM_I_MUTED_BY_GAMER

**Hash:** `0xDF02A2C93F1F26DA` | **JHash:** `0x7685B333` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_AM_I_MUTED_BY_GAMER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_AM_I_MUTED_BY_GAMER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_GAMER_BLOCKED_BY_ME

**Hash:** `0xE944C4F5AF1B5883` | **JHash:** `0x3FDCC8D7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_GAMER_BLOCKED_BY_ME(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_GAMER_BLOCKED_BY_ME, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_AM_I_BLOCKED_BY_GAMER

**Hash:** `0x15337C7C268A27B2` | **JHash:** `0xD19B312C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_AM_I_BLOCKED_BY_GAMER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_AM_I_BLOCKED_BY_GAMER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_VIEW_GAMER_USER_CONTENT

**Hash:** `0xB57A49545BA53CE7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_VIEW_GAMER_USER_CONTENT(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_VIEW_GAMER_USER_CONTENT, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_VIEW_GAMER_USER_CONTENT_RESULT

**Hash:** `0xCCA4318E1AB03F1F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_VIEW_GAMER_USER_CONTENT_RESULT(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_VIEW_GAMER_USER_CONTENT_RESULT, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### _NETWORK_CAN_TEXT_FROM_GAMER_BE_VIEWED

**Hash:** `0x9F633448E4C73207` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL _NETWORK_CAN_TEXT_FROM_GAMER_BE_VIEWED(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_CAN_TEXT_FROM_GAMER_BE_VIEWED, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** NETWORK_CAN_*

---

### NETWORK_CAN_PLAY_MULTIPLAYER_WITH_GAMER

**Hash:** `0x07DD29D5E22763F1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_PLAY_MULTIPLAYER_WITH_GAMER(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_PLAY_MULTIPLAYER_WITH_GAMER, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_GAMER_PLAY_MULTIPLAYER_WITH_ME

**Hash:** `0x135F9B7B7ADD2185` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_GAMER_PLAY_MULTIPLAYER_WITH_ME(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_GAMER_PLAY_MULTIPLAYER_WITH_ME, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_SEND_LOCAL_INVITE

**Hash:** `0x021ABCBD98EC4320` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_SEND_LOCAL_INVITE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_SEND_LOCAL_INVITE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CAN_RECEIVE_LOCAL_INVITE

**Hash:** `0x421E34C55F125964` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL NETWORK_CAN_RECEIVE_LOCAL_INVITE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CAN_RECEIVE_LOCAL_INVITE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_TALKING

**Hash:** `0x031E11F3D447647E` | **JHash:** `0xDA9FD9DB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_TALKING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_TALKING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** returns true if someone is screaming or talking in a microphone

---

### NETWORK_PLAYER_HAS_HEADSET

**Hash:** `0x3FB99A8B08D18FD6` | **JHash:** `0x451FB6B6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_PLAYER_HAS_HEADSET(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_PLAYER_HAS_HEADSET, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_MUTED_BY_ME

**Hash:** `0x8C71288AE68EDE39` | **JHash:** `0x7A21050E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_MUTED_BY_ME(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_MUTED_BY_ME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_AM_I_MUTED_BY_PLAYER

**Hash:** `0x9D6981DFC91A8604` | **JHash:** `0xE128F2B0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_AM_I_MUTED_BY_PLAYER(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_AM_I_MUTED_BY_PLAYER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_BLOCKED_BY_ME

**Hash:** `0x57AF1F8E27483721` | **JHash:** `0xAE4F4560` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_BLOCKED_BY_ME(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_BLOCKED_BY_ME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_AM_I_BLOCKED_BY_PLAYER

**Hash:** `0x87F395D957D4353D` | **JHash:** `0x953EF45E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_AM_I_BLOCKED_BY_PLAYER(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_AM_I_BLOCKED_BY_PLAYER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PLAYER_LOUDNESS

**Hash:** `0x21A1684A25C2867F` | **JHash:** `0xF2F67014` | **Build:** `323`

**C++ 原型:**
```cpp
float NETWORK_GET_PLAYER_LOUDNESS(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.NETWORK_GET_PLAYER_LOUDNESS, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### NETWORK_SET_TALKER_PROXIMITY

**Hash:** `0xCBF12D65F95AD686` | **JHash:** `0x67555C66` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_TALKER_PROXIMITY(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TALKER_PROXIMITY, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_TALKER_PROXIMITY

**Hash:** `0x84F0F13120B4E098` | **JHash:** `0x19991ADD` | **Build:** `323`

**C++ 原型:**
```cpp
float NETWORK_GET_TALKER_PROXIMITY(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.NETWORK_GET_TALKER_PROXIMITY);
```

**返回值:** `float` → `float`

---

### NETWORK_SET_VOICE_ACTIVE

**Hash:** `0xBABEC9E69A91C57B` | **JHash:** `0x8011247F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_VOICE_ACTIVE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_VOICE_ACTIVE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_REMAIN_IN_GAME_CHAT

**Hash:** `0xCFEB46DCD7D8D5EB` | **JHash:** `0x1A3EA6CD` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REMAIN_IN_GAME_CHAT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REMAIN_IN_GAME_CHAT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_OVERRIDE_TRANSITION_CHAT

**Hash:** `0xAF66059A131AA269` | **JHash:** `0xCAB21090` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_TRANSITION_CHAT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_TRANSITION_CHAT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_TEAM_ONLY_CHAT

**Hash:** `0xD5B4883AC32F24C3` | **JHash:** `0x3813019A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_TEAM_ONLY_CHAT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TEAM_ONLY_CHAT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_SCRIPT_CONTROLLING_TEAMS

**Hash:** `0x265559DA40B3F327` | **Build:** `573`

**C++ 原型:**
```cpp
void NETWORK_SET_SCRIPT_CONTROLLING_TEAMS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_SCRIPT_CONTROLLING_TEAMS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_SAME_TEAM_AS_LOCAL_PLAYER

**Hash:** `0x4348BFDA56023A2F` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL NETWORK_SET_SAME_TEAM_AS_LOCAL_PLAYER(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SET_SAME_TEAM_AS_LOCAL_PLAYER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_OVERRIDE_TEAM_RESTRICTIONS

**Hash:** `0x6F697A66CE78674E` | **JHash:** `0xC8CC9E75` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_TEAM_RESTRICTIONS(int team, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_TEAM_RESTRICTIONS, team, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `team` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_OVERRIDE_SPECTATOR_MODE

**Hash:** `0x70DA3BF8DACD3210` | **JHash:** `0xA0FD42D3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_OVERRIDE_SPECTATOR_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_OVERRIDE_SPECTATOR_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_OVERRIDE_TUTORIAL_SESSION_CHAT

**Hash:** `0x3C5C1E2C2FF814B1` | **JHash:** `0xC9DDA85B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_OVERRIDE_TUTORIAL_SESSION_CHAT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_OVERRIDE_TUTORIAL_SESSION_CHAT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_PROXIMITY_AFFECTS_TEAM

**Hash:** `0x9D7AFCBF21C51712` | **JHash:** `0xE5219410` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_PROXIMITY_AFFECTS_TEAM(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_PROXIMITY_AFFECTS_TEAM, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_NO_SPECTATOR_CHAT

**Hash:** `0xF46A1E03E8755980` | **JHash:** `0xD33AFF79` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_NO_SPECTATOR_CHAT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_NO_SPECTATOR_CHAT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_IGNORE_SPECTATOR_CHAT_LIMITS_SAME_TEAM

**Hash:** `0x6A5D89D7769A40D8` | **JHash:** `0x4FFEFE43` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_IGNORE_SPECTATOR_CHAT_LIMITS_SAME_TEAM(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IGNORE_SPECTATOR_CHAT_LIMITS_SAME_TEAM, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_OVERRIDE_CHAT_RESTRICTIONS

**Hash:** `0x3039AE5AD2C9C0C4` | **JHash:** `0x74EE2D8B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_CHAT_RESTRICTIONS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_CHAT_RESTRICTIONS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Could possibly bypass being muted or automatically muted

---

### NETWORK_OVERRIDE_SEND_RESTRICTIONS

**Hash:** `0x97DD4C5944CC2E6A` | **JHash:** `0x6C344AE3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_SEND_RESTRICTIONS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_SEND_RESTRICTIONS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This is used alongside the native,
'NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS'. Read its description for more info.

---

### NETWORK_OVERRIDE_SEND_RESTRICTIONS_ALL

**Hash:** `0x57B192B4D4AD23D5` | **JHash:** `0x2F98B405` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_SEND_RESTRICTIONS_ALL(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_SEND_RESTRICTIONS_ALL, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS

**Hash:** `0xDDF73E2B1FEC5AB4` | **JHash:** `0x95F1C60D` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** R* uses this to hear all player when spectating. 
It allows you to hear other online players when their chat is on none, crew and or friends

---

### NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS_ALL

**Hash:** `0x0FF2862B61A58AF9` | **JHash:** `0x1BCD3DDF` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS_ALL(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_RECEIVE_RESTRICTIONS_ALL, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p0 is always false in scripts.

---

### NETWORK_SET_VOICE_CHANNEL

**Hash:** `0xEF6212C2EFEF1A23` | **JHash:** `0x3974879F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_VOICE_CHANNEL(int channel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_VOICE_CHANNEL, channel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `channel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_CLEAR_VOICE_CHANNEL

**Hash:** `0xE036A705F989E049` | **JHash:** `0x9ECF722A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_VOICE_CHANNEL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_VOICE_CHANNEL);
```

**返回值:** `void` → `void`

---

### NETWORK_APPLY_VOICE_PROXIMITY_OVERRIDE

**Hash:** `0xDBD2056652689917` | **JHash:** `0xF1E84832` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_APPLY_VOICE_PROXIMITY_OVERRIDE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_APPLY_VOICE_PROXIMITY_OVERRIDE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### NETWORK_CLEAR_VOICE_PROXIMITY_OVERRIDE

**Hash:** `0xF03755696450470C` | **JHash:** `0x7F9B9052` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_VOICE_PROXIMITY_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_VOICE_PROXIMITY_OVERRIDE);
```

**返回值:** `void` → `void`

---

### NETWORK_ENABLE_VOICE_BANDWIDTH_RESTRICTION

**Hash:** `0x5E3AA4CA2B6FB0EE` | **JHash:** `0x7BBEA8CF` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ENABLE_VOICE_BANDWIDTH_RESTRICTION(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ENABLE_VOICE_BANDWIDTH_RESTRICTION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_DISABLE_VOICE_BANDWIDTH_RESTRICTION

**Hash:** `0xCA575C391FEA25CC` | **JHash:** `0xE797A4B6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DISABLE_VOICE_BANDWIDTH_RESTRICTION(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DISABLE_VOICE_BANDWIDTH_RESTRICTION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_MUTE_COUNT_FOR_PLAYER

**Hash:** `0xADB57E5B663CCA8B` | **JHash:** `0x92268BB5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_MUTE_COUNT_FOR_PLAYER(Player p0, float* p1, float* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_MUTE_COUNT_FOR_PLAYER, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Player` | `int` | |
| `p1` | `float*` | `float*` | |
| `p2` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** NETWORK_GET_M[A-U]

---

### NETWORK_SET_SPECTATOR_TO_NON_SPECTATOR_TEXT_CHAT

**Hash:** `0x8EF52ACAECC51D9C` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SET_SPECTATOR_TO_NON_SPECTATOR_TEXT_CHAT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_SPECTATOR_TO_NON_SPECTATOR_TEXT_CHAT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_TEXT_CHAT_IS_TYPING

**Hash:** `0x5FCF4D7069B09026` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_TEXT_CHAT_IS_TYPING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TEXT_CHAT_IS_TYPING);
```

**返回值:** `BOOL` → `bool`

**说明:** Same as _IS_TEXT_CHAT_ACTIVE, except it does not check if the text chat HUD component is initialized, and therefore may crash.

---

### SHUTDOWN_AND_LAUNCH_SINGLE_PLAYER_GAME

**Hash:** `0x593850C16A36B692` | **JHash:** `0x92B7351C` | **Build:** `323`

**C++ 原型:**
```cpp
void SHUTDOWN_AND_LAUNCH_SINGLE_PLAYER_GAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHUTDOWN_AND_LAUNCH_SINGLE_PLAYER_GAME);
```

**返回值:** `void` → `void`

**说明:** Starts a new singleplayer game (at the prologue).

---

### SHUTDOWN_AND_LOAD_MOST_RECENT_SAVE

**Hash:** `0x9ECA15ADFE141431` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL SHUTDOWN_AND_LOAD_MOST_RECENT_SAVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SHUTDOWN_AND_LOAD_MOST_RECENT_SAVE);
```

**返回值:** `BOOL` → `bool`

**说明:** In singleplayer this will re-load your game.

In FiveM / GTA:Online this disconnects you from the session, and starts loading single player, however you still remain connected to the server (only if you're the host, if you're not then you also (most likely) get disconnected from the server) and other players will not be able to join until you exit the game.

You might need to DoScreenFadeIn and ShutdownLoadingScreen otherwise you probably won't end up loading into SP at all.

Somewhat related note: opening the pause menu after loading into this 'singleplayer' mode crashes the game.

---

### NETWORK_SET_FRIENDLY_FIRE_OPTION

**Hash:** `0xF808475FA571D823` | **JHash:** `0x6BAF95FA` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_FRIENDLY_FIRE_OPTION(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_FRIENDLY_FIRE_OPTION, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_RICH_PRESENCE

**Hash:** `0x1DCCACDCFC569362` | **JHash:** `0x932A6CED` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_RICH_PRESENCE(int p0, int p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_RICH_PRESENCE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### NETWORK_SET_RICH_PRESENCE_STRING

**Hash:** `0x3E200C2BCF4164EB` | **JHash:** `0x017E6777` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_RICH_PRESENCE_STRING(int p0, const char* textLabel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_RICH_PRESENCE_STRING, p0, textLabel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `textLabel` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### NETWORK_GET_TIMEOUT_TIME

**Hash:** `0x5ED0356A0CE3A34F` | **JHash:** `0xE1F86C6A` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_TIMEOUT_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_TIMEOUT_TIME);
```

**返回值:** `int` → `int`

---

### NETWORK_LEAVE_PED_BEHIND_BEFORE_WARP

**Hash:** `0x9769F811D1785B03` | **JHash:** `0xBE6A30C3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_LEAVE_PED_BEHIND_BEFORE_WARP(Player player, float x, float y, float z, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_LEAVE_PED_BEHIND_BEFORE_WARP, player, x, y, z, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p4 and p5 are always 0 in scripts

---

### NETWORK_LEAVE_PED_BEHIND_BEFORE_CUTSCENE

**Hash:** `0xBF22E0F32968E967` | **JHash:** `0x22E03AD0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_LEAVE_PED_BEHIND_BEFORE_CUTSCENE(Player player, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_LEAVE_PED_BEHIND_BEFORE_CUTSCENE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_ALL_STICKY_BOMBS_FROM_ENTITY

**Hash:** `0x715135F4B82AC90D` | **JHash:** `0xCEAE5AFC` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_STICKY_BOMBS_FROM_ENTITY(Entity entity, Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_STICKY_BOMBS_FROM_ENTITY, entity, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** entity must be a valid entity; ped can be NULL

---

### NETWORK_KEEP_ENTITY_COLLISION_DISABLED_AFTER_ANIM_SCENE

**Hash:** `0x17C9E241111A674D` | **Build:** `2060`

**C++ 原型:**
```cpp
void NETWORK_KEEP_ENTITY_COLLISION_DISABLED_AFTER_ANIM_SCENE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_KEEP_ENTITY_COLLISION_DISABLED_AFTER_ANIM_SCENE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_ANY_PLAYER_NEAR

**Hash:** `0x2E4C123D1C8A710E` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ANY_PLAYER_NEAR(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ANY_PLAYER_NEAR, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_SERVICE_IS_VALID

**Hash:** `0x579CCED0265D4896` | **JHash:** `0xF5F4BD95` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_SERVICE_IS_VALID(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_SERVICE_IS_VALID);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_PLAYER_IS_ACTIVE

**Hash:** `0xB124B57F571D8F18` | **JHash:** `0xAB8319A3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_PLAYER_IS_ACTIVE(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_PLAYER_IS_ACTIVE, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_PLAYER_GET_DESC

**Hash:** `0xEEE6EACBE8874FBA` | **JHash:** `0x6EE4A282` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_PLAYER_GET_DESC(Any* clanDesc, int bufferSize, Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_PLAYER_GET_DESC, clanDesc, bufferSize, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clanDesc` | `Any*` | `object*` | |
| `bufferSize` | `int` | `int` | |
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** bufferSize is 35 in the scripts.

bufferSize is the elementCount of p0(desc), sizeof(p0) == 280 == p1*8 == 35 * 8, p2(netHandle) is obtained from NETWORK::NETWORK_HANDLE_FROM_PLAYER.  And no, I can't explain why 35 * sizeof(int) == 280 and not 140, but I'll get back to you on that.

the answer is: because p0 an int64_t* / int64_t[35].  and FYI p2 is an int64_t[13]

https://pastebin.com/cSZniHak

---

### NETWORK_CLAN_IS_ROCKSTAR_CLAN

**Hash:** `0x7543BB439F63792B` | **JHash:** `0x54E79E9C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_IS_ROCKSTAR_CLAN(Any* clanDesc, int bufferSize)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_IS_ROCKSTAR_CLAN, clanDesc, bufferSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clanDesc` | `Any*` | `object*` | |
| `bufferSize` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** bufferSize is 35 in the scripts.

---

### NETWORK_CLAN_GET_UI_FORMATTED_TAG

**Hash:** `0xF45352426FF3A4F0` | **JHash:** `0xF633805A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLAN_GET_UI_FORMATTED_TAG(Any* clanDesc, int bufferSize, char* formattedTag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLAN_GET_UI_FORMATTED_TAG, clanDesc, bufferSize, formattedTag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clanDesc` | `Any*` | `object*` | |
| `bufferSize` | `int` | `int` | |
| `formattedTag` | `char*` | `string` | |

**返回值:** `void` → `void`

**说明:** bufferSize is 35 in the scripts.

---

### NETWORK_CLAN_GET_LOCAL_MEMBERSHIPS_COUNT

**Hash:** `0x1F471B79ACC90BEF` | **JHash:** `0x807B3450` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_CLAN_GET_LOCAL_MEMBERSHIPS_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_CLAN_GET_LOCAL_MEMBERSHIPS_COUNT);
```

**返回值:** `int` → `int`

---

### NETWORK_CLAN_GET_MEMBERSHIP_DESC

**Hash:** `0x48DE78AF2C8885B8` | **JHash:** `0x3369DD1F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_GET_MEMBERSHIP_DESC(Any* memberDesc, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_GET_MEMBERSHIP_DESC, memberDesc, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `memberDesc` | `Any*` | `object*` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_DOWNLOAD_MEMBERSHIP

**Hash:** `0xA989044E70010ABE` | **JHash:** `0x8E8CB520` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_DOWNLOAD_MEMBERSHIP(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_DOWNLOAD_MEMBERSHIP, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_DOWNLOAD_MEMBERSHIP_PENDING

**Hash:** `0x5B9E023DC6EBEDC0` | **JHash:** `0x1FDB590F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_DOWNLOAD_MEMBERSHIP_PENDING(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_DOWNLOAD_MEMBERSHIP_PENDING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_ANY_DOWNLOAD_MEMBERSHIP_PENDING

**Hash:** `0xB3F64A6A91432477` | **JHash:** `0x83ED8E08` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_ANY_DOWNLOAD_MEMBERSHIP_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_ANY_DOWNLOAD_MEMBERSHIP_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_REMOTE_MEMBERSHIPS_ARE_IN_CACHE

**Hash:** `0xBB6E6FEE99D866B2` | **JHash:** `0x40202867` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_REMOTE_MEMBERSHIPS_ARE_IN_CACHE(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_REMOTE_MEMBERSHIPS_ARE_IN_CACHE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_GET_MEMBERSHIP_COUNT

**Hash:** `0xAAB11F6C4ADBC2C1` | **JHash:** `0x25924010` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_CLAN_GET_MEMBERSHIP_COUNT(int* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_CLAN_GET_MEMBERSHIP_COUNT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `int` → `int`

---

### NETWORK_CLAN_GET_MEMBERSHIP_VALID

**Hash:** `0x48A59CF88D43DF0E` | **JHash:** `0x48914F6A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_GET_MEMBERSHIP_VALID(int* p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_GET_MEMBERSHIP_VALID, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_GET_MEMBERSHIP

**Hash:** `0xC8BC2011F67B3411` | **JHash:** `0xCDC4A590` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_GET_MEMBERSHIP(int* p0, Any* clanMembership, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_GET_MEMBERSHIP, p0, clanMembership, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |
| `clanMembership` | `Any*` | `object*` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_JOIN

**Hash:** `0x9FAAA4F4FC71F87F` | **JHash:** `0x79C916C5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_JOIN(int clanDesc)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_JOIN, clanDesc);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clanDesc` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_CREWINFO_GET_STRING_VALUE

**Hash:** `0x729E3401F0430686` | **JHash:** `0xBDA90BAC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_CREWINFO_GET_STRING_VALUE(const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_CREWINFO_GET_STRING_VALUE, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Only documented...

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### NETWORK_CLAN_CREWINFO_GET_CREWRANKTITLE

**Hash:** `0x2B51EDBEFC301339` | **JHash:** `0x8E952B12` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_CREWINFO_GET_CREWRANKTITLE(int p0, const char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_CREWINFO_GET_CREWRANKTITLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_HAS_CREWINFO_METADATA_BEEN_RECEIVED

**Hash:** `0xC32EA7A2F6CA7557` | **JHash:** `0x966C90FD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_HAS_CREWINFO_METADATA_BEEN_RECEIVED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_HAS_CREWINFO_METADATA_BEEN_RECEIVED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_GET_EMBLEM_TXD_NAME

**Hash:** `0x5835D9CD92E83184` | **JHash:** `0xBA672146` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_GET_EMBLEM_TXD_NAME(Any* netHandle, char* txdName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_GET_EMBLEM_TXD_NAME, netHandle, txdName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `Any*` | `object*` | |
| `txdName` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_REQUEST_EMBLEM

**Hash:** `0x13518FF1C6B28938` | **JHash:** `0x7963FA4D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_REQUEST_EMBLEM(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_REQUEST_EMBLEM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_IS_EMBLEM_READY

**Hash:** `0xA134777FF7F33331` | **JHash:** `0x88B13CDC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CLAN_IS_EMBLEM_READY(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CLAN_IS_EMBLEM_READY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CLAN_RELEASE_EMBLEM

**Hash:** `0x113E6E3E50E286B0` | **JHash:** `0xD6E3D5EA` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLAN_RELEASE_EMBLEM(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLAN_RELEASE_EMBLEM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_CLEAR

**Hash:** `0x9AA46BADAD0E27ED` | **JHash:** `0xE22445DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRIMARY_CLAN_DATA_CLEAR(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_CLEAR);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_CANCEL

**Hash:** `0x042E4B70B93E6054` | **JHash:** `0x455DDF5C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_PRIMARY_CLAN_DATA_CANCEL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_CANCEL);
```

**返回值:** `void` → `void`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_START

**Hash:** `0xCE86D8191B762107` | **JHash:** `0x89DB0EC7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRIMARY_CLAN_DATA_START(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_START, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_PENDING

**Hash:** `0xB5074DB804E28CE7` | **JHash:** `0xA4EF02F3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRIMARY_CLAN_DATA_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_SUCCESS

**Hash:** `0x5B4F04F19376A0BA` | **JHash:** `0x068A054E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRIMARY_CLAN_DATA_SUCCESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_SUCCESS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PRIMARY_CLAN_DATA_NEW

**Hash:** `0xC080FF658B2E41DA` | **JHash:** `0x9B8631EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_GET_PRIMARY_CLAN_DATA_NEW(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_GET_PRIMARY_CLAN_DATA_NEW, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SET_NETWORK_ID_CAN_MIGRATE

**Hash:** `0x299EEB23175895FC` | **JHash:** `0x47C8E5FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_CAN_MIGRATE(int netId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_CAN_MIGRATE, netId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Whether or not another player is allowed to take control of the entity

---

### SET_NETWORK_ID_EXISTS_ON_ALL_MACHINES

**Hash:** `0xE05E81A888FA63C8` | **JHash:** `0x68D486B2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_EXISTS_ON_ALL_MACHINES(int netId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_EXISTS_ON_ALL_MACHINES, netId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_ID_ALWAYS_EXISTS_FOR_PLAYER

**Hash:** `0xA8A024587329F36A` | **JHash:** `0x4D15FDB1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_ALWAYS_EXISTS_FOR_PLAYER(int netId, Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_ALWAYS_EXISTS_FOR_PLAYER, netId, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_ID_CAN_BE_REASSIGNED

**Hash:** `0x9D724B400A7E8FFC` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_CAN_BE_REASSIGNED(int netId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_CAN_BE_REASSIGNED, netId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** "No Reassign" in CPhysicalScriptGameStateDataNode

---

### NETWORK_SET_ENTITY_CAN_BLEND

**Hash:** `0xD830567D88A1E873` | **JHash:** `0xDE8C0DB8` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_ENTITY_CAN_BLEND(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ENTITY_CAN_BLEND, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_OBJECT_CAN_BLEND_WHEN_FIXED

**Hash:** `0x0379DAF89BA09AA5` | **Build:** `944`

**C++ 原型:**
```cpp
void NETWORK_SET_OBJECT_CAN_BLEND_WHEN_FIXED(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_OBJECT_CAN_BLEND_WHEN_FIXED, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_ENTITY_ONLY_EXISTS_FOR_PARTICIPANTS

**Hash:** `0xF1CA12B18AEF5298` | **JHash:** `0x09CBC4B0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_ENTITY_ONLY_EXISTS_FOR_PARTICIPANTS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ENTITY_ONLY_EXISTS_FOR_PARTICIPANTS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** if set to true other network players can't see it
if set to false other network player can see it
=========================================
^^ I attempted this by grabbing an object with GET_ENTITY_PLAYER_IS_FREE_AIMING_AT and setting this naive no matter the toggle he could still see it.

pc or last gen?

^^ last-gen

---

### SET_NETWORK_ID_VISIBLE_IN_CUTSCENE

**Hash:** `0xA6928482543022B4` | **JHash:** `0x199E75EF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_VISIBLE_IN_CUTSCENE(int netId, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_VISIBLE_IN_CUTSCENE, netId, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_HACK

**Hash:** `0x32EBD154CB6B8B99` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_HACK(int netId, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_HACK, netId, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_REMAIN_HACK

**Hash:** `0x76B3F29D3F967692` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_REMAIN_HACK(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_VISIBLE_IN_CUTSCENE_REMAIN_HACK, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_CUTSCENE_ENTITIES

**Hash:** `0xAAA553E7DD28A457` | **JHash:** `0xFD0F7EDD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_CUTSCENE_ENTITIES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_CUTSCENE_ENTITIES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ARE_CUTSCENE_ENTITIES_NETWORKED

**Hash:** `0x66D6A5E9C511214A` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL ARE_CUTSCENE_ENTITIES_NETWORKED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_CUTSCENE_ENTITIES_NETWORKED);
```

**返回值:** `BOOL` → `bool`

**说明:** Getter for SET_NETWORK_CUTSCENE_ENTITIES.

---

### SET_NETWORK_ID_PASS_CONTROL_IN_TUTORIAL

**Hash:** `0x3FA36981311FA4FF` | **JHash:** `0x00AE4E17` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_ID_PASS_CONTROL_IN_TUTORIAL(int netId, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ID_PASS_CONTROL_IN_TUTORIAL, netId, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _NETWORK_SET_TUTORIAL_SPECIAL_SESSION

**Hash:** `0x35AEB4AC7C73C6E1` | **Build:** `3717`

**C++ 原型:**
```cpp
void _NETWORK_SET_TUTORIAL_SPECIAL_SESSION(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SET_TUTORIAL_SPECIAL_SESSION, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _NETWORK_IS_SPECIAL_TUTORIAL_SESSION

**Hash:** `0x4666CD7431DE98E2` | **Build:** `3717`

**C++ 原型:**
```cpp
BOOL _NETWORK_IS_SPECIAL_TUTORIAL_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_IS_SPECIAL_TUTORIAL_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### IS_NETWORK_ID_OWNED_BY_PARTICIPANT

**Hash:** `0xA1607996431332DF` | **JHash:** `0xEA5176C0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NETWORK_ID_OWNED_BY_PARTICIPANT(int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NETWORK_ID_OWNED_BY_PARTICIPANT, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_REMOTE_PLAYER_VISIBLE_IN_CUTSCENE

**Hash:** `0x96320E6549DAE7B4` | **Build:** `2802`

**C++ 原型:**
```cpp
void SET_REMOTE_PLAYER_VISIBLE_IN_CUTSCENE(Player player, BOOL locallyVisible)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REMOTE_PLAYER_VISIBLE_IN_CUTSCENE, player, locallyVisible);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `locallyVisible` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_LOCAL_PLAYER_VISIBLE_IN_CUTSCENE

**Hash:** `0xD1065D68947E7B6E` | **JHash:** `0x59F3479B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_VISIBLE_IN_CUTSCENE(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_VISIBLE_IN_CUTSCENE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_LOCAL_PLAYER_INVISIBLE_LOCALLY

**Hash:** `0xE5F773C1A1D9D168` | **JHash:** `0x764F6222` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_INVISIBLE_LOCALLY(BOOL bIncludePlayersVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_INVISIBLE_LOCALLY, bIncludePlayersVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `bIncludePlayersVehicle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_LOCAL_PLAYER_VISIBLE_LOCALLY

**Hash:** `0x7619364C82D3BF14` | **JHash:** `0x324B56DB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_VISIBLE_LOCALLY(BOOL bIncludePlayersVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_VISIBLE_LOCALLY, bIncludePlayersVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `bIncludePlayersVehicle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_INVISIBLE_LOCALLY

**Hash:** `0x12B37D54667DB0B8` | **JHash:** `0x18227209` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_INVISIBLE_LOCALLY(Player player, BOOL bIncludePlayersVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_INVISIBLE_LOCALLY, player, bIncludePlayersVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `bIncludePlayersVehicle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_VISIBLE_LOCALLY

**Hash:** `0xFAA10F1FAFB11AF2` | **JHash:** `0xBA2BB4B4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_VISIBLE_LOCALLY(Player player, BOOL bIncludePlayersVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_VISIBLE_LOCALLY, player, bIncludePlayersVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `bIncludePlayersVehicle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FADE_OUT_LOCAL_PLAYER

**Hash:** `0x416DBD4CD6ED8DD2` | **JHash:** `0x8FA7CEBD` | **Build:** `323`

**C++ 原型:**
```cpp
void FADE_OUT_LOCAL_PLAYER(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FADE_OUT_LOCAL_PLAYER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Hardcoded to not work in SP.

---

### NETWORK_FADE_OUT_ENTITY

**Hash:** `0xDE564951F95E09ED` | **JHash:** `0x47EDEE56` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_FADE_OUT_ENTITY(Entity entity, BOOL normal, BOOL slow)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_FADE_OUT_ENTITY, entity, normal, slow);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `normal` | `BOOL` | `bool` | |
| `slow` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** normal - transition like when your coming out of LSC
slow - transition like when you walk into a mission
 

---

### NETWORK_FADE_IN_ENTITY

**Hash:** `0x1F4ED342ACEFE62D` | **JHash:** `0x9B9FCD02` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_FADE_IN_ENTITY(Entity entity, BOOL state, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_FADE_IN_ENTITY, entity, state, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `state` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** state - 0 does 5 fades
state - 1 does 6 fades

p3: setting to 1 made vehicle fade in slower, probably "slow" as per NETWORK_FADE_OUT_ENTITY

---

### NETWORK_IS_PLAYER_FADING

**Hash:** `0x631DC5DFF4B110E3` | **JHash:** `0x065C597B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_FADING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_FADING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_ENTITY_FADING

**Hash:** `0x422F32CC7E56ABAD` | **JHash:** `0xB5087ACF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ENTITY_FADING(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ENTITY_FADING, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_IN_CUTSCENE

**Hash:** `0xE73092F4157CD126` | **JHash:** `0xE0A619BD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_IN_CUTSCENE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_IN_CUTSCENE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENTITY_VISIBLE_IN_CUTSCENE

**Hash:** `0xE0031D3C8F36AB82` | **JHash:** `0xDBFB067B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_VISIBLE_IN_CUTSCENE(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_VISIBLE_IN_CUTSCENE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_LOCALLY_INVISIBLE

**Hash:** `0xE135A9FF3F5D05D8` | **JHash:** `0x51ADCC5F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_LOCALLY_INVISIBLE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_LOCALLY_INVISIBLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the provided entity visible for yourself for the current frame.

---

### SET_ENTITY_LOCALLY_VISIBLE

**Hash:** `0x241E289B5C059EDC` | **JHash:** `0x235A57B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_LOCALLY_VISIBLE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_LOCALLY_VISIBLE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### IS_DAMAGE_TRACKER_ACTIVE_ON_NETWORK_ID

**Hash:** `0x6E192E33AD436366` | **JHash:** `0x597063BA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DAMAGE_TRACKER_ACTIVE_ON_NETWORK_ID(int netID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DAMAGE_TRACKER_ACTIVE_ON_NETWORK_ID, netID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ACTIVATE_DAMAGE_TRACKER_ON_NETWORK_ID

**Hash:** `0xD45B1FFCCD52FF19` | **JHash:** `0x95D07BA5` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_DAMAGE_TRACKER_ON_NETWORK_ID(int netID, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_DAMAGE_TRACKER_ON_NETWORK_ID, netID, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netID` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_DAMAGE_TRACKER_ACTIVE_ON_PLAYER

**Hash:** `0xB2092A1EAA7FD45F` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL IS_DAMAGE_TRACKER_ACTIVE_ON_PLAYER(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DAMAGE_TRACKER_ACTIVE_ON_PLAYER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ACTIVATE_DAMAGE_TRACKER_ON_PLAYER

**Hash:** `0xBEC0816FF5ACBCDA` | **Build:** `757`

**C++ 原型:**
```cpp
void ACTIVATE_DAMAGE_TRACKER_ON_PLAYER(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_DAMAGE_TRACKER_ON_PLAYER, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_SPHERE_VISIBLE_TO_ANOTHER_MACHINE

**Hash:** `0xD82CF8E64C8729D8` | **JHash:** `0x23C5274E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPHERE_VISIBLE_TO_ANOTHER_MACHINE(float p0, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPHERE_VISIBLE_TO_ANOTHER_MACHINE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### IS_SPHERE_VISIBLE_TO_PLAYER

**Hash:** `0xDC3A310219E5DA62` | **JHash:** `0xE9FCFB32` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPHERE_VISIBLE_TO_PLAYER(Any p0, float p1, float p2, float p3, float p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPHERE_VISIBLE_TO_PLAYER, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### RESERVE_NETWORK_MISSION_OBJECTS

**Hash:** `0x4E5C93BD0C32FBF8` | **JHash:** `0x391DF4F3` | **Build:** `323`

**C++ 原型:**
```cpp
void RESERVE_NETWORK_MISSION_OBJECTS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_NETWORK_MISSION_OBJECTS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESERVE_NETWORK_MISSION_PEDS

**Hash:** `0xB60FEBA45333D36F` | **JHash:** `0x54998C37` | **Build:** `323`

**C++ 原型:**
```cpp
void RESERVE_NETWORK_MISSION_PEDS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_NETWORK_MISSION_PEDS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESERVE_NETWORK_MISSION_VEHICLES

**Hash:** `0x76B02E21ED27A469` | **JHash:** `0x5062875E` | **Build:** `323`

**C++ 原型:**
```cpp
void RESERVE_NETWORK_MISSION_VEHICLES(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_NETWORK_MISSION_VEHICLES, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESERVE_LOCAL_NETWORK_MISSION_OBJECTS

**Hash:** `0x797F9C5E661D920E` | **Build:** `1290`

**C++ 原型:**
```cpp
void RESERVE_LOCAL_NETWORK_MISSION_OBJECTS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_LOCAL_NETWORK_MISSION_OBJECTS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESERVE_LOCAL_NETWORK_MISSION_PEDS

**Hash:** `0x2C8DF5D129595281` | **Build:** `1493`

**C++ 原型:**
```cpp
void RESERVE_LOCAL_NETWORK_MISSION_PEDS(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_LOCAL_NETWORK_MISSION_PEDS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESERVE_LOCAL_NETWORK_MISSION_VEHICLES

**Hash:** `0x42613035157E4208` | **Build:** `1103`

**C++ 原型:**
```cpp
void RESERVE_LOCAL_NETWORK_MISSION_VEHICLES(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESERVE_LOCAL_NETWORK_MISSION_VEHICLES, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### CAN_REGISTER_MISSION_OBJECTS

**Hash:** `0x800DD4721A8B008B` | **JHash:** `0x7F85DFDE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_OBJECTS(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_OBJECTS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_REGISTER_MISSION_PEDS

**Hash:** `0xBCBF4FEF9FA5D781` | **JHash:** `0xCCAA5CE9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_PEDS(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_PEDS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_REGISTER_MISSION_VEHICLES

**Hash:** `0x7277F1F2E085EE74` | **JHash:** `0x818B6830` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_VEHICLES(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_VEHICLES, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_REGISTER_MISSION_PICKUPS

**Hash:** `0x0A49D1CB6E34AF72` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_PICKUPS(int amount)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_PICKUPS, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_REGISTER_MISSION_DOORS

**Hash:** `0xE16AA70CE9BEEDC3` | **Build:** `877`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_DOORS(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_DOORS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### CAN_REGISTER_MISSION_ENTITIES

**Hash:** `0x69778E7564BADE6D` | **JHash:** `0x83794008` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_REGISTER_MISSION_ENTITIES(int ped_amt, int vehicle_amt, int object_amt, int pickup_amt)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_REGISTER_MISSION_ENTITIES, ped_amt, vehicle_amt, object_amt, pickup_amt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped_amt` | `int` | `int` | |
| `vehicle_amt` | `int` | `int` | |
| `object_amt` | `int` | `int` | |
| `pickup_amt` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_NUM_RESERVED_MISSION_OBJECTS

**Hash:** `0xAA81B5F10BC43AC2` | **JHash:** `0x16A80CD6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_RESERVED_MISSION_OBJECTS(BOOL p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_RESERVED_MISSION_OBJECTS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** p0 appears to be for MP

---

### GET_NUM_RESERVED_MISSION_PEDS

**Hash:** `0x1F13D5AE5CB17E17` | **JHash:** `0x6C25975C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_RESERVED_MISSION_PEDS(BOOL p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_RESERVED_MISSION_PEDS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** p0 appears to be for MP

---

### GET_NUM_RESERVED_MISSION_VEHICLES

**Hash:** `0xCF3A965906452031` | **JHash:** `0xA9A308F3` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_RESERVED_MISSION_VEHICLES(BOOL p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_RESERVED_MISSION_VEHICLES, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** p0 appears to be for MP

---

### GET_NUM_CREATED_MISSION_OBJECTS

**Hash:** `0x12B6281B6C6706C0` | **JHash:** `0x603FA104` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_CREATED_MISSION_OBJECTS(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_CREATED_MISSION_OBJECTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### GET_NUM_CREATED_MISSION_PEDS

**Hash:** `0xCB215C4B56A7FAE7` | **JHash:** `0xD8FEC4F8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_CREATED_MISSION_PEDS(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_CREATED_MISSION_PEDS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### GET_NUM_CREATED_MISSION_VEHICLES

**Hash:** `0x0CD9AB83489430EA` | **JHash:** `0x20527695` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_CREATED_MISSION_VEHICLES(BOOL p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_CREATED_MISSION_VEHICLES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### GET_RESERVED_MISSION_ENTITIES_IN_AREA

**Hash:** `0xE42D626EEC94E5D9` | **Build:** `1290`

**C++ 原型:**
```cpp
void GET_RESERVED_MISSION_ENTITIES_IN_AREA(float x, float y, float z, Any p3, Any* out1, Any* out2, Any* out3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_RESERVED_MISSION_ENTITIES_IN_AREA, x, y, z, p3, out1, out2, out3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `Any` | `object` | |
| `out1` | `Any*` | `object*` | |
| `out2` | `Any*` | `object*` | |
| `out3` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### GET_MAX_NUM_NETWORK_OBJECTS

**Hash:** `0xC7BE335216B5EC7C` | **JHash:** `0x8687E285` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_NUM_NETWORK_OBJECTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_NUM_NETWORK_OBJECTS);
```

**返回值:** `int` → `int`

---

### GET_MAX_NUM_NETWORK_PEDS

**Hash:** `0x0C1F7D49C39D2289` | **JHash:** `0x744AC008` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_NUM_NETWORK_PEDS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_NUM_NETWORK_PEDS);
```

**返回值:** `int` → `int`

---

### GET_MAX_NUM_NETWORK_VEHICLES

**Hash:** `0x0AFCE529F69B21FF` | **JHash:** `0xC3A12135` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_NUM_NETWORK_VEHICLES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_NUM_NETWORK_VEHICLES);
```

**返回值:** `int` → `int`

---

### GET_MAX_NUM_NETWORK_PICKUPS

**Hash:** `0xA72835064DD63E4C` | **JHash:** `0x6A036061` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_NUM_NETWORK_PICKUPS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_NUM_NETWORK_PICKUPS);
```

**返回值:** `int` → `int`

---

### NETWORK_SET_OBJECT_SCOPE_DISTANCE

**Hash:** `0xBA7F0B77D80A4EB7` | **Build:** `372`

**C++ 原型:**
```cpp
void NETWORK_SET_OBJECT_SCOPE_DISTANCE(Object object, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_OBJECT_SCOPE_DISTANCE, object, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

---

### NETWORK_ALLOW_CLONING_WHILE_IN_TUTORIAL

**Hash:** `0x0F1A4B45B7693B95` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_ALLOW_CLONING_WHILE_IN_TUTORIAL(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ALLOW_CLONING_WHILE_IN_TUTORIAL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_TASK_CUTSCENE_INSCOPE_MULTIPLER

**Hash:** `0xC6FCEE21C6FCEE21` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_SET_TASK_CUTSCENE_INSCOPE_MULTIPLER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_TASK_CUTSCENE_INSCOPE_MULTIPLER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** A value between 1.0 and 5.0

---

### _NETWORK_HIDE_ENTITY_IN_TUTORIAL_SESSION

**Hash:** `0xCFE359CCCFE359CC` | **Build:** `3258`

**C++ 原型:**
```cpp
void _NETWORK_HIDE_ENTITY_IN_TUTORIAL_SESSION(int netHandle, BOOL hide)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_HIDE_ENTITY_IN_TUTORIAL_SESSION, netHandle, hide);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `int` | `int` | |
| `hide` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_NETWORK_TIME

**Hash:** `0x7A5487FE9FAA6B48` | **JHash:** `0x998103C2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NETWORK_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NETWORK_TIME);
```

**返回值:** `int` → `int`

---

### GET_NETWORK_TIME_ACCURATE

**Hash:** `0x89023FBBF9200E9F` | **JHash:** `0x98AA48E5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NETWORK_TIME_ACCURATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NETWORK_TIME_ACCURATE);
```

**返回值:** `int` → `int`

**说明:** Returns the same value as GET_NETWORK_TIME in freemode, but as opposed to `GET_NETWORK_TIME` it always gets the most recent time, instead of once per tick.
Could be used for benchmarking since it can return times in ticks.

---

### HAS_NETWORK_TIME_STARTED

**Hash:** `0x46718ACEEDEAFC84` | **JHash:** `0x4538C4A2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_NETWORK_TIME_STARTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_NETWORK_TIME_STARTED);
```

**返回值:** `BOOL` → `bool`

---

### GET_TIME_OFFSET

**Hash:** `0x017008CCDAD48503` | **JHash:** `0x2E079AE6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_OFFSET(int timeA, int timeB)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_OFFSET, timeA, timeB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeA` | `int` | `int` | |
| `timeB` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Adds the first argument to the second.

---

### IS_TIME_LESS_THAN

**Hash:** `0xCB2CF5148012C8D0` | **JHash:** `0x50EF8FC6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TIME_LESS_THAN(int timeA, int timeB)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TIME_LESS_THAN, timeA, timeB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeA` | `int` | `int` | |
| `timeB` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Subtracts the second argument from the first, then returns whether the result is negative.

---

### IS_TIME_MORE_THAN

**Hash:** `0xDE350F8651E4346C` | **JHash:** `0xBBB6DF61` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TIME_MORE_THAN(int timeA, int timeB)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TIME_MORE_THAN, timeA, timeB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeA` | `int` | `int` | |
| `timeB` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Subtracts the first argument from the second, then returns whether the result is negative.

---

### IS_TIME_EQUAL_TO

**Hash:** `0xF5BC95857BD6D512` | **JHash:** `0x8B4D1C06` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TIME_EQUAL_TO(int timeA, int timeB)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TIME_EQUAL_TO, timeA, timeB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeA` | `int` | `int` | |
| `timeB` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the two times are equal; otherwise returns false.

---

### GET_TIME_DIFFERENCE

**Hash:** `0xA2C6FC031D46FFF0` | **JHash:** `0x5666A837` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_DIFFERENCE(int timeA, int timeB)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_DIFFERENCE, timeA, timeB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `timeA` | `int` | `int` | |
| `timeB` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Subtracts the second argument from the first.

---

### GET_TIME_AS_STRING

**Hash:** `0x9E23B1777A927DAD` | **JHash:** `0x8218944E` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_TIME_AS_STRING(int time)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_TIME_AS_STRING, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `time` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_CLOUD_TIME_AS_STRING

**Hash:** `0xF12E6CD06C73D69E` | **Build:** `1103`

**C++ 原型:**
```cpp
const char* GET_CLOUD_TIME_AS_STRING(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CLOUD_TIME_AS_STRING);
```

**返回值:** `const char*` → `string`

**说明:** Same as GET_CLOUD_TIME_AS_INT but returns the value as a hex string (%I64X).

---

### GET_CLOUD_TIME_AS_INT

**Hash:** `0x9A73240B49945C76` | **JHash:** `0xF2FDF2E0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CLOUD_TIME_AS_INT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOUD_TIME_AS_INT);
```

**返回值:** `int` → `int`

**说明:** Returns POSIX timestamp, an int representing the cloud time.

---

### CONVERT_POSIX_TIME

**Hash:** `0xAC97AF97FA68E5D5` | **JHash:** `0xBB7CCE49` | **Build:** `323`

**C++ 原型:**
```cpp
void CONVERT_POSIX_TIME(int posixTime, Any* timeStructure)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CONVERT_POSIX_TIME, posixTime, timeStructure);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posixTime` | `int` | `int` | |
| `timeStructure` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Takes the specified time and writes it to the structure specified in the second argument.

struct date_time
{
    int year;
    int PADDING1;
    int month;
    int PADDING2;
    int day;
    int PADDING3;
    int hour;
    int PADDING4;
    int minute;
    int PADDING5;
    int second;
    int PADDING6;
};

---

### NETWORK_SET_IN_SPECTATOR_MODE

**Hash:** `0x423DE3854BB50894` | **JHash:** `0x5C4C8458` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_IN_SPECTATOR_MODE(BOOL toggle, Ped playerPed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IN_SPECTATOR_MODE, toggle, playerPed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `playerPed` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_IN_SPECTATOR_MODE_EXTENDED

**Hash:** `0x419594E137637120` | **JHash:** `0x54058F5F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_IN_SPECTATOR_MODE_EXTENDED(BOOL toggle, Ped playerPed, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IN_SPECTATOR_MODE_EXTENDED, toggle, playerPed, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `playerPed` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_IN_FREE_CAM_MODE

**Hash:** `0xFC18DB55AE19E046` | **JHash:** `0xA7E36020` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_IN_FREE_CAM_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IN_FREE_CAM_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_ANTAGONISTIC_TO_PLAYER

**Hash:** `0x5C707A667DF8B9FA` | **JHash:** `0x64235620` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_ANTAGONISTIC_TO_PLAYER(BOOL toggle, Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_ANTAGONISTIC_TO_PLAYER, toggle, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_IN_SPECTATOR_MODE

**Hash:** `0x048746E388762E11` | **JHash:** `0x3EAD9DB8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_SPECTATOR_MODE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_SPECTATOR_MODE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_IN_MP_CUTSCENE

**Hash:** `0x9CA5DE655269FEC4` | **JHash:** `0x8434CB43` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_IN_MP_CUTSCENE(BOOL p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_IN_MP_CUTSCENE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_IN_MP_CUTSCENE

**Hash:** `0x6CC27C9FA2040220` | **JHash:** `0x4BB33316` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_MP_CUTSCENE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_MP_CUTSCENE);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_PLAYER_IN_MP_CUTSCENE

**Hash:** `0x63F9EE203C3619F2` | **JHash:** `0x56F961E4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_IN_MP_CUTSCENE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_IN_MP_CUTSCENE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HIDE_PROJECTILE_IN_CUTSCENE

**Hash:** `0xFAC18E7356BD3210` | **Build:** `1180`

**C++ 原型:**
```cpp
void NETWORK_HIDE_PROJECTILE_IN_CUTSCENE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_HIDE_PROJECTILE_IN_CUTSCENE);
```

**返回值:** `void` → `void`

---

### SET_NETWORK_VEHICLE_RESPOT_TIMER

**Hash:** `0xEC51713AB6EC36E8` | **JHash:** `0x2C30912D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_VEHICLE_RESPOT_TIMER(int netId, int time, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_VEHICLE_RESPOT_TIMER, netId, time, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `time` | `int` | `int` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_NETWORK_VEHICLE_RUNNING_RESPOT_TIMER

**Hash:** `0xDD7CEF5B3A4DA8A6` | **Build:** `2802`

**C++ 原型:**
```cpp
BOOL IS_NETWORK_VEHICLE_RUNNING_RESPOT_TIMER(int networkID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NETWORK_VEHICLE_RUNNING_RESPOT_TIMER, networkID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `networkID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_NETWORK_VEHICLE_AS_GHOST

**Hash:** `0x6274C4712850841E` | **JHash:** `0xEA235081` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NETWORK_VEHICLE_AS_GHOST(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_VEHICLE_AS_GHOST, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NETWORK_VEHICLE_MAX_POSITION_DELTA_MULTIPLIER

**Hash:** `0xA2A707979FE754DC` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_NETWORK_VEHICLE_MAX_POSITION_DELTA_MULTIPLIER(Vehicle vehicle, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_VEHICLE_MAX_POSITION_DELTA_MULTIPLIER, vehicle, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** rage::netBlenderLinInterp::GetPositionMaxForUpdateLevel

---

### SET_NETWORK_ENABLE_HIGH_SPEED_EDGE_FALL_DETECTION

**Hash:** `0x838DA0936A24ED4D` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_NETWORK_ENABLE_HIGH_SPEED_EDGE_FALL_DETECTION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NETWORK_ENABLE_HIGH_SPEED_EDGE_FALL_DETECTION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables a periodic ShapeTest within the NetBlender and invokes rage::netBlenderLinInterp::GoStraightToTarget (or some functional wrapper).

---

### SET_LOCAL_PLAYER_AS_GHOST

**Hash:** `0x5FFE9B4144F9712F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_AS_GHOST(BOOL toggle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_AS_GHOST, toggle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ENTITY_A_GHOST

**Hash:** `0x21D04D7BC538C146` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_A_GHOST(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_A_GHOST, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_NON_PARTICIPANTS_OF_THIS_SCRIPT_AS_GHOSTS

**Hash:** `0x13F1FCB111B820B0` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_NON_PARTICIPANTS_OF_THIS_SCRIPT_AS_GHOSTS(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NON_PARTICIPANTS_OF_THIS_SCRIPT_AS_GHOSTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_REMOTE_PLAYER_AS_GHOST

**Hash:** `0xA7C511FA1C5BDA38` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_REMOTE_PLAYER_AS_GHOST(Player player, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REMOTE_PLAYER_AS_GHOST, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables ghosting between specific players

---

### SET_GHOST_ALPHA

**Hash:** `0x658500AE6D723A7E` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_GHOST_ALPHA(int alpha)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GHOST_ALPHA, alpha);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `alpha` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Must be a value between 1 and 254

---

### RESET_GHOST_ALPHA

**Hash:** `0x17330EBF2F2124A8` | **Build:** `791`

**C++ 原型:**
```cpp
void RESET_GHOST_ALPHA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_GHOST_ALPHA);
```

**返回值:** `void` → `void`

**说明:** Resets the entity ghost alpha to the default value (128)

---

### SET_ENTITY_GHOSTED_FOR_GHOST_PLAYERS

**Hash:** `0x4BA166079D658ED4` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_ENTITY_GHOSTED_FOR_GHOST_PLAYERS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_GHOSTED_FOR_GHOST_PLAYERS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INVERT_GHOSTING

**Hash:** `0xD7B6C73CAD419BCF` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_INVERT_GHOSTING(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INVERT_GHOSTING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ENTITY_IN_GHOST_COLLISION

**Hash:** `0x7EF7649B64D7FF10` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_IN_GHOST_COLLISION(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_IN_GHOST_COLLISION, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _SET_FREEMODE_REPORT_DATA

**Hash:** `0x527803286A8B6C81` | **Build:** `3258`

**C++ 原型:**
```cpp
void _SET_FREEMODE_REPORT_DATA(Any* gamerHandle, Any* reportData)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_FREEMODE_REPORT_DATA, gamerHandle, reportData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `reportData` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** reportData includes mc, ceo, yacht and licenceplate names

---

### IS_OBJECT_REASSIGNMENT_IN_PROGRESS

**Hash:** `0x28123C8B056CC8AA` | **Build:** `3274`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_REASSIGNMENT_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_REASSIGNMENT_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### USE_PLAYER_COLOUR_INSTEAD_OF_TEAM_COLOUR

**Hash:** `0x77758139EC9B66C7` | **JHash:** `0x4DD46DAE` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_PLAYER_COLOUR_INSTEAD_OF_TEAM_COLOUR(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_PLAYER_COLOUR_INSTEAD_OF_TEAM_COLOUR, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_CREATE_SYNCHRONISED_SCENE

**Hash:** `0x7CD6BC4C2BBDD526` | **JHash:** `0xB06FE3FE` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_CREATE_SYNCHRONISED_SCENE(float x, float y, float z, float xRot, float yRot, float zRot, int rotationOrder, BOOL useOcclusionPortal, BOOL looped, float p9, float animTime, float p11)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_CREATE_SYNCHRONISED_SCENE, x, y, z, xRot, yRot, zRot, rotationOrder, useOcclusionPortal, looped, p9, animTime, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |
| `useOcclusionPortal` | `BOOL` | `bool` | |
| `looped` | `BOOL` | `bool` | |
| `p9` | `float` | `float` | |
| `animTime` | `float` | `float` | |
| `p11` | `float` | `float` | |

**返回值:** `int` → `int`

---

### NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE

**Hash:** `0x742A637471BCECD9` | **JHash:** `0xB386713E` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE(Ped ped, int netScene, const char* animDict, const char* animnName, float speed, float speedMultiplier, int duration, int flag, float playbackRate, Any p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE, ped, netScene, animDict, animnName, speed, speedMultiplier, duration, flag, playbackRate, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `netScene` | `int` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animnName` | `const char*` | `string` | |
| `speed` | `float` | `float` | |
| `speedMultiplier` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `flag` | `int` | `int` | |
| `playbackRate` | `float` | `float` | |
| `p9` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE_WITH_IK

**Hash:** `0xA5EAFE473E45C442` | **Build:** `1290`

**C++ 原型:**
```cpp
void NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE_WITH_IK(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_PED_TO_SYNCHRONISED_SCENE_WITH_IK, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_ADD_ENTITY_TO_SYNCHRONISED_SCENE

**Hash:** `0xF2404D68CBC855FA` | **JHash:** `0x10DD636C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ADD_ENTITY_TO_SYNCHRONISED_SCENE(Entity entity, int netScene, const char* animDict, const char* animName, float speed, float speedMulitiplier, int flag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_ENTITY_TO_SYNCHRONISED_SCENE, entity, netScene, animDict, animName, speed, speedMulitiplier, flag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `netScene` | `int` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `speed` | `float` | `float` | |
| `speedMulitiplier` | `float` | `float` | |
| `flag` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_ADD_MAP_ENTITY_TO_SYNCHRONISED_SCENE

**Hash:** `0x45F35C0EDC33B03B` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_ADD_MAP_ENTITY_TO_SYNCHRONISED_SCENE(int netScene, Hash modelHash, float x, float y, float z, float p5, const char* p6, float p7, float p8, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_MAP_ENTITY_TO_SYNCHRONISED_SCENE, netScene, modelHash, x, y, z, p5, p6, p7, p8, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `const char*` | `string` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Similar structure as NETWORK_ADD_ENTITY_TO_SYNCHRONISED_SCENE but it includes this time a hash.
In casino_slots it is used one time in a synced scene involving a ped and the slot machine?

---

### NETWORK_ADD_SYNCHRONISED_SCENE_CAMERA

**Hash:** `0xCF8BD3B0BD6D42D7` | **JHash:** `0xBFFE8B5C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ADD_SYNCHRONISED_SCENE_CAMERA(int netScene, const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ADD_SYNCHRONISED_SCENE_CAMERA, netScene, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### NETWORK_ATTACH_SYNCHRONISED_SCENE_TO_ENTITY

**Hash:** `0x478DCBD2A98B705A` | **JHash:** `0x3FE5B222` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ATTACH_SYNCHRONISED_SCENE_TO_ENTITY(int netScene, Entity entity, int bone)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ATTACH_SYNCHRONISED_SCENE_TO_ENTITY, netScene, entity, bone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `bone` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_START_SYNCHRONISED_SCENE

**Hash:** `0x9A1B3FCDB36C8697` | **JHash:** `0xA9DFDC40` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_START_SYNCHRONISED_SCENE(int netScene)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_START_SYNCHRONISED_SCENE, netScene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_STOP_SYNCHRONISED_SCENE

**Hash:** `0xC254481A4574CB2F` | **JHash:** `0x97B1CDF6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_STOP_SYNCHRONISED_SCENE(int netScene)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_STOP_SYNCHRONISED_SCENE, netScene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_LOCAL_SCENE_FROM_NETWORK_ID

**Hash:** `0x02C40BF885C567B6` | **JHash:** `0x16AED87B` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_LOCAL_SCENE_FROM_NETWORK_ID(int netId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_LOCAL_SCENE_FROM_NETWORK_ID, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_FORCE_LOCAL_USE_OF_SYNCED_SCENE_CAMERA

**Hash:** `0xC9B43A33D09CADA7` | **JHash:** `0xC3AA2EC7` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_FORCE_LOCAL_USE_OF_SYNCED_SCENE_CAMERA(int netScene)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_FORCE_LOCAL_USE_OF_SYNCED_SCENE_CAMERA, netScene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netScene` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_ALLOW_REMOTE_SYNCED_SCENE_LOCAL_PLAYER_REQUESTS

**Hash:** `0x144DA052257AE7D8` | **Build:** `1103`

**C++ 原型:**
```cpp
void NETWORK_ALLOW_REMOTE_SYNCED_SCENE_LOCAL_PLAYER_REQUESTS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ALLOW_REMOTE_SYNCED_SCENE_LOCAL_PLAYER_REQUESTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_FIND_LARGEST_BUNCH_OF_PLAYERS

**Hash:** `0xFB1F9381E80FA13F` | **JHash:** `0x0679CE71` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_FIND_LARGEST_BUNCH_OF_PLAYERS(int p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_FIND_LARGEST_BUNCH_OF_PLAYERS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p0 is always 0. p1 is pointing to a global.

---

### NETWORK_START_RESPAWN_SEARCH_FOR_PLAYER

**Hash:** `0x5A6FFA2433E2F14C` | **JHash:** `0xC62E77B3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_START_RESPAWN_SEARCH_FOR_PLAYER(Player player, float x, float y, float z, float radius, float p5, float p6, float p7, int flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_START_RESPAWN_SEARCH_FOR_PLAYER, player, x, y, z, radius, p5, p6, p7, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** One of the first things it does is get the players ped.
Then it calls a function that is used in some tasks and ped based functions.
p5, p6, p7 is another coordinate (or zero), often related to `GET_BLIP_COORDS, in the decompiled scripts.

---

### NETWORK_START_RESPAWN_SEARCH_IN_ANGLED_AREA_FOR_PLAYER

**Hash:** `0x4BA92A18502BCA61` | **JHash:** `0x74D6B13C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_START_RESPAWN_SEARCH_IN_ANGLED_AREA_FOR_PLAYER(Player player, float x1, float y1, float z1, float x2, float y2, float z2, float width, float p8, float p9, float p10, int flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_START_RESPAWN_SEARCH_IN_ANGLED_AREA_FOR_PLAYER, player, x1, y1, z1, x2, y2, z2, width, p8, p9, p10, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p8, p9, p10 is another coordinate, or zero, often related to `GET_BLIP_COORDS in the decompiled scripts.

---

### NETWORK_QUERY_RESPAWN_RESULTS

**Hash:** `0x3C891A251567DFCE` | **JHash:** `0x90700C7D` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_QUERY_RESPAWN_RESULTS(Any* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_QUERY_RESPAWN_RESULTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### NETWORK_CANCEL_RESPAWN_SEARCH

**Hash:** `0xFB8F2A6F3DF08CBE` | **JHash:** `0x44BFB619` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CANCEL_RESPAWN_SEARCH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CANCEL_RESPAWN_SEARCH);
```

**返回值:** `void` → `void`

---

### NETWORK_GET_RESPAWN_RESULT

**Hash:** `0x371EA43692861CF1` | **JHash:** `0xDDFE9FBC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_RESPAWN_RESULT(int randomInt, Vector3* coordinates, float* heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_RESPAWN_RESULT, randomInt, coordinates, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `randomInt` | `int` | `int` | |
| `coordinates` | `Vector3*` | `Vector3*` | |
| `heading` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** Based on scripts such as in freemode.c how they call their vars vVar and fVar the 2nd and 3rd param it a Vector3 and Float, but the first is based on get_random_int_in_range..

---

### NETWORK_GET_RESPAWN_RESULT_FLAGS

**Hash:** `0x6C34F1208B8923FD` | **JHash:** `0x03287FD2` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_RESPAWN_RESULT_FLAGS(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_RESPAWN_RESULT_FLAGS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_START_SOLO_TUTORIAL_SESSION

**Hash:** `0x17E0198B3882C2CB` | **JHash:** `0x408A9436` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_START_SOLO_TUTORIAL_SESSION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_START_SOLO_TUTORIAL_SESSION);
```

**返回值:** `void` → `void`

---

### NETWORK_ALLOW_GANG_TO_JOIN_TUTORIAL_SESSION

**Hash:** `0xFB680D403909DC70` | **JHash:** `0xFFB2ADA1` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ALLOW_GANG_TO_JOIN_TUTORIAL_SESSION(int teamId, int instanceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ALLOW_GANG_TO_JOIN_TUTORIAL_SESSION, teamId, instanceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `teamId` | `int` | `int` | |
| `instanceId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** teamId must be < 3, instanceId must be < 64

---

### NETWORK_END_TUTORIAL_SESSION

**Hash:** `0xD0AFAFF5A51D72F7` | **JHash:** `0xBA57E53E` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_END_TUTORIAL_SESSION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_END_TUTORIAL_SESSION);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_IN_TUTORIAL_SESSION

**Hash:** `0xADA24309FE08DACF` | **JHash:** `0x34DD7B28` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_IN_TUTORIAL_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_IN_TUTORIAL_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_WAITING_POP_CLEAR_TUTORIAL_SESSION

**Hash:** `0xB37E4E6A2388CA7B` | **JHash:** `0x755A2B3E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_WAITING_POP_CLEAR_TUTORIAL_SESSION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_WAITING_POP_CLEAR_TUTORIAL_SESSION);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_TUTORIAL_SESSION_CHANGE_PENDING

**Hash:** `0x35F0B98A8387274D` | **JHash:** `0xA003C40B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TUTORIAL_SESSION_CHANGE_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TUTORIAL_SESSION_CHANGE_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_PLAYER_TUTORIAL_SESSION_INSTANCE

**Hash:** `0x3B39236746714134` | **JHash:** `0x5E1020CC` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_PLAYER_TUTORIAL_SESSION_INSTANCE(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLAYER_TUTORIAL_SESSION_INSTANCE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_ARE_PLAYERS_IN_SAME_TUTORIAL_SESSION

**Hash:** `0x9DE986FC9A87C474` | **JHash:** `0xE66A0B40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ARE_PLAYERS_IN_SAME_TUTORIAL_SESSION(Player player, int index)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ARE_PLAYERS_IN_SAME_TUTORIAL_SESSION, player, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_BLOCK_PROXY_MIGRATION_BETWEEN_TUTORIAL_SESSIONS

**Hash:** `0xFEA7A352DDB34D52` | **Build:** `2545`

**C++ 原型:**
```cpp
void NETWORK_BLOCK_PROXY_MIGRATION_BETWEEN_TUTORIAL_SESSIONS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_BLOCK_PROXY_MIGRATION_BETWEEN_TUTORIAL_SESSIONS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### NETWORK_CONCEAL_PLAYER

**Hash:** `0xBBDF066252829606` | **JHash:** `0x72052DB3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CONCEAL_PLAYER(Player player, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CONCEAL_PLAYER, player, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_PLAYER_CONCEALED

**Hash:** `0x919B3C98ED8292F9` | **JHash:** `0xB0313590` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_PLAYER_CONCEALED(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_PLAYER_CONCEALED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_CONCEAL_ENTITY

**Hash:** `0x1632BE0AC1E62876` | **Build:** `877`

**C++ 原型:**
```cpp
void NETWORK_CONCEAL_ENTITY(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CONCEAL_ENTITY, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_IS_ENTITY_CONCEALED

**Hash:** `0x71302EC70689052A` | **Build:** `877`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_ENTITY_CONCEALED(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_ENTITY_CONCEALED, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Note: This only works for vehicles, which appears to be a bug (since the setter _does_ work for every entity type and the name is 99% correct).

---

### NETWORK_OVERRIDE_CLOCK_TIME

**Hash:** `0xE679E3E06E363892` | **JHash:** `0xC077BCD6` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_CLOCK_TIME(int hours, int minutes, int seconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_CLOCK_TIME, hours, minutes, seconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hours` | `int` | `int` | |
| `minutes` | `int` | `int` | |
| `seconds` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Works in Singleplayer too.
Passing wrong data (e.g. hours above 23) will cause the game to crash.

---

### NETWORK_OVERRIDE_CLOCK_RATE

**Hash:** `0x42BF1D2E723B6D7E` | **Build:** `2189`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_CLOCK_RATE(int ms)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_CLOCK_RATE, ms);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ms` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_CLEAR_CLOCK_TIME_OVERRIDE

**Hash:** `0xD972DF67326F966E` | **JHash:** `0xC4AEAF49` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CLEAR_CLOCK_TIME_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CLEAR_CLOCK_TIME_OVERRIDE);
```

**返回值:** `void` → `void`

---

### _NETWORK_CLEAR_CLOCK_SYNC_TIME_OVERRIDE

**Hash:** `0x0C1BF187985B15C8` | **Build:** `3717`

**C++ 原型:**
```cpp
void _NETWORK_CLEAR_CLOCK_SYNC_TIME_OVERRIDE(BOOL startGlobalTransition, int transitionTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_CLEAR_CLOCK_SYNC_TIME_OVERRIDE, startGlobalTransition, transitionTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startGlobalTransition` | `BOOL` | `bool` | |
| `transitionTime` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SYNC_CLOCK_TIME_OVERRIDE

**Hash:** `0xCBD02360C5E16871` | **Build:** `3570`

**C++ 原型:**
```cpp
void NETWORK_SYNC_CLOCK_TIME_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SYNC_CLOCK_TIME_OVERRIDE);
```

**返回值:** `void` → `void`

**说明:** Does nothing in final builds.

---

### NETWORK_IS_CLOCK_TIME_OVERRIDDEN

**Hash:** `0xD7C95D322FF57522` | **JHash:** `0x2465296D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CLOCK_TIME_OVERRIDDEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CLOCK_TIME_OVERRIDDEN);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ADD_ENTITY_AREA

**Hash:** `0x494C8FB299290269` | **JHash:** `0x51030E5B` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_ADD_ENTITY_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_ADD_ENTITY_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `int` → `int`

---

### NETWORK_ADD_ENTITY_ANGLED_AREA

**Hash:** `0x376C6375BA60293A` | **JHash:** `0xCD69BEA1` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_ADD_ENTITY_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_ADD_ENTITY_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** To remove, see: NETWORK_REMOVE_ENTITY_AREA
See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.

---

### NETWORK_ADD_CLIENT_ENTITY_AREA

**Hash:** `0x25B99872D588A101` | **JHash:** `0x4C2C2B12` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_ADD_CLIENT_ENTITY_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_ADD_CLIENT_ENTITY_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `int` → `int`

---

### NETWORK_ADD_CLIENT_ENTITY_ANGLED_AREA

**Hash:** `0x2B1C623823DB0D9D` | **Build:** `1103`

**C++ 原型:**
```cpp
int NETWORK_ADD_CLIENT_ENTITY_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_ADD_CLIENT_ENTITY_ANGLED_AREA, x1, y1, z1, x2, y2, z2, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `int` → `int`

---

### NETWORK_REMOVE_ENTITY_AREA

**Hash:** `0x93CF869BAA0C4874` | **JHash:** `0xEAB97F25` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REMOVE_ENTITY_AREA(int areaHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REMOVE_ENTITY_AREA, areaHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `areaHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ENTITY_AREA_DOES_EXIST

**Hash:** `0xE64A3CA08DFA37A9` | **JHash:** `0x69956127` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ENTITY_AREA_DOES_EXIST(int areaHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ENTITY_AREA_DOES_EXIST, areaHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `areaHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ENTITY_AREA_HAVE_ALL_REPLIED

**Hash:** `0x4DF7CFFF471A7FB1` | **JHash:** `0xCB1CD6D3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ENTITY_AREA_HAVE_ALL_REPLIED(int areaHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ENTITY_AREA_HAVE_ALL_REPLIED, areaHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `areaHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ENTITY_AREA_IS_OCCUPIED

**Hash:** `0x4A2D4E8BF4265B0F` | **JHash:** `0xC6D53AA0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ENTITY_AREA_IS_OCCUPIED(int areaHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ENTITY_AREA_IS_OCCUPIED, areaHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `areaHandle` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_USE_HIGH_PRECISION_BLENDING

**Hash:** `0x2B1813ABA29016C5` | **JHash:** `0x155465EE` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_USE_HIGH_PRECISION_BLENDING(int netID, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_USE_HIGH_PRECISION_BLENDING, netID, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netID` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_CUSTOM_ARENA_BALL_PARAMS

**Hash:** `0xA6FCECCF4721D679` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_SET_CUSTOM_ARENA_BALL_PARAMS(int netId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_CUSTOM_ARENA_BALL_PARAMS, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_ENTITY_USE_HIGH_PRECISION_ROTATION

**Hash:** `0x95BAF97C82464629` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_ENTITY_USE_HIGH_PRECISION_ROTATION(int netId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ENTITY_USE_HIGH_PRECISION_ROTATION, netId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_REQUEST_CLOUD_BACKGROUND_SCRIPTS

**Hash:** `0x924426BFFD82E915` | **JHash:** `0x98EFB921` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_REQUEST_CLOUD_BACKGROUND_SCRIPTS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_REQUEST_CLOUD_BACKGROUND_SCRIPTS);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_IS_CLOUD_BACKGROUND_SCRIPT_REQUEST_PENDING

**Hash:** `0x8132C0EB8B2B3293` | **JHash:** `0x20AB933A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CLOUD_BACKGROUND_SCRIPT_REQUEST_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CLOUD_BACKGROUND_SCRIPT_REQUEST_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_REQUEST_CLOUD_TUNABLES

**Hash:** `0x42FB3B532D526E6C` | **JHash:** `0xD760CAD5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_REQUEST_CLOUD_TUNABLES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REQUEST_CLOUD_TUNABLES);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_TUNABLE_CLOUD_REQUEST_PENDING

**Hash:** `0x0467C11ED88B7D28` | **JHash:** `0x47978D7F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TUNABLE_CLOUD_REQUEST_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TUNABLE_CLOUD_REQUEST_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_TUNABLE_CLOUD_CRC

**Hash:** `0x10BD227A753B0D84` | **JHash:** `0x231CFD12` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_TUNABLE_CLOUD_CRC(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_TUNABLE_CLOUD_CRC);
```

**返回值:** `int` → `int`

**说明:** Actually returns the version (TUNABLE_VERSION)

---

### _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS

**Hash:** `0x014A73449675121D` | **Build:** `3258`

**C++ 原型:**
```cpp
void _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS(Any* tunableContextData)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS, tunableContextData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContextData` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Sets up tunable contexts for _NETWORK_GET_TUNABLES_REGISTRATION_{BOOL|INT|FLOAT}

---

### _NETWORK_CLEAR_TUNABLES_REGISTRATION_CONTEXTS

**Hash:** `0x0C87C83C8950432B` | **Build:** `3258`

**C++ 原型:**
```cpp
void _NETWORK_CLEAR_TUNABLES_REGISTRATION_CONTEXTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_CLEAR_TUNABLES_REGISTRATION_CONTEXTS);
```

**返回值:** `void` → `void`

**说明:** Clears tunable contexts that were set up using _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS

---

### _NETWORK_GET_TUNABLES_REGISTRATION_BOOL

**Hash:** `0xB327CF1B8C2C0EA3` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL _NETWORK_GET_TUNABLES_REGISTRATION_BOOL(Hash tunableName, BOOL defaultValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_GET_TUNABLES_REGISTRATION_BOOL, tunableName, defaultValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableName` | `Hash` | `uint` | |
| `defaultValue` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the given tunable exists and returns its value. Otherwise returns defaultValue.
Possible tunable contexts must first be set up using _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS.

---

### _NETWORK_GET_TUNABLES_REGISTRATION_INT

**Hash:** `0x0D94071E55F4C9CE` | **Build:** `3258`

**C++ 原型:**
```cpp
int _NETWORK_GET_TUNABLES_REGISTRATION_INT(Hash tunableName, int defaultValue)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_TUNABLES_REGISTRATION_INT, tunableName, defaultValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableName` | `Hash` | `uint` | |
| `defaultValue` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Checks if the given tunable exists and returns its value. Otherwise returns defaultValue.
Possible tunable contexts must first be set up using _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS.

---

### _NETWORK_GET_TUNABLES_REGISTRATION_FLOAT

**Hash:** `0x367E5E33E7F0DD1A` | **Build:** `3258`

**C++ 原型:**
```cpp
float _NETWORK_GET_TUNABLES_REGISTRATION_FLOAT(Hash tunableName, float defaultValue)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._NETWORK_GET_TUNABLES_REGISTRATION_FLOAT, tunableName, defaultValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableName` | `Hash` | `uint` | |
| `defaultValue` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Checks if the given tunable exists and returns its value. Otherwise returns defaultValue.
Possible tunable contexts must first be set up using _NETWORK_SET_TUNABLES_REGISTRATION_CONTEXTS.

---

### NETWORK_DOES_TUNABLE_EXIST

**Hash:** `0x85E5F8B9B898B20A` | **JHash:** `0x9FCE9C9A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DOES_TUNABLE_EXIST(const char* tunableContext, const char* tunableName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DOES_TUNABLE_EXIST, tunableContext, tunableName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `const char*` | `string` | |
| `tunableName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_INT

**Hash:** `0x8BE1146DFD5D4468` | **JHash:** `0xE4B3726A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_INT(const char* tunableContext, const char* tunableName, int* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_INT, tunableContext, tunableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `const char*` | `string` | |
| `tunableName` | `const char*` | `string` | |
| `value` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_FLOAT

**Hash:** `0xE5608CA7BC163A5F` | **JHash:** `0x41E8912A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_FLOAT(const char* tunableContext, const char* tunableName, float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_FLOAT, tunableContext, tunableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `const char*` | `string` | |
| `tunableName` | `const char*` | `string` | |
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_BOOL

**Hash:** `0xAA6A47A573ABB75A` | **JHash:** `0x8A04E1FE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_BOOL(const char* tunableContext, const char* tunableName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_BOOL, tunableContext, tunableName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `const char*` | `string` | |
| `tunableName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_DOES_TUNABLE_EXIST_HASH

**Hash:** `0xE4E53E1419D81127` | **JHash:** `0x25915CB9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_DOES_TUNABLE_EXIST_HASH(Hash tunableContext, Hash tunableName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_DOES_TUNABLE_EXIST_HASH, tunableContext, tunableName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `Hash` | `uint` | |
| `tunableName` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_MODIFICATION_DETECTION_CLEAR

**Hash:** `0xFAFC23AEE23868DB` | **Build:** `393`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_MODIFICATION_DETECTION_CLEAR(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_MODIFICATION_DETECTION_CLEAR);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_INT_HASH

**Hash:** `0x40FCE03E50E8DBE8` | **JHash:** `0xB2E83B75` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_INT_HASH(Hash tunableContext, Hash tunableName, int* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_INT_HASH, tunableContext, tunableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `Hash` | `uint` | |
| `tunableName` | `Hash` | `uint` | |
| `value` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_INT_MODIFICATION_DETECTION_REGISTRATION_HASH

**Hash:** `0x3A8B55FDA4C8DDEF` | **Build:** `393`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_INT_MODIFICATION_DETECTION_REGISTRATION_HASH(Hash contextHash, Hash nameHash, int* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_INT_MODIFICATION_DETECTION_REGISTRATION_HASH, contextHash, nameHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |
| `nameHash` | `Hash` | `uint` | |
| `value` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_FLOAT_HASH

**Hash:** `0x972BC203BBC4C4D5` | **JHash:** `0x3972551F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_FLOAT_HASH(Hash tunableContext, Hash tunableName, float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_FLOAT_HASH, tunableContext, tunableName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `Hash` | `uint` | |
| `tunableName` | `Hash` | `uint` | |
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_FLOAT_MODIFICATION_DETECTION_REGISTRATION_HASH

**Hash:** `0x1950DAE9848A4739` | **Build:** `393`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_FLOAT_MODIFICATION_DETECTION_REGISTRATION_HASH(Hash contextHash, Hash nameHash, float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_FLOAT_MODIFICATION_DETECTION_REGISTRATION_HASH, contextHash, nameHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |
| `nameHash` | `Hash` | `uint` | |
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_BOOL_HASH

**Hash:** `0xEA16B69D93D71A45` | **JHash:** `0x18867C61` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_BOOL_HASH(Hash tunableContext, Hash tunableName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_BOOL_HASH, tunableContext, tunableName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `Hash` | `uint` | |
| `tunableName` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_ACCESS_TUNABLE_BOOL_MODIFICATION_DETECTION_REGISTRATION_HASH

**Hash:** `0x697F508861875B42` | **Build:** `393`

**C++ 原型:**
```cpp
BOOL NETWORK_ACCESS_TUNABLE_BOOL_MODIFICATION_DETECTION_REGISTRATION_HASH(Hash contextHash, Hash nameHash, BOOL* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_ACCESS_TUNABLE_BOOL_MODIFICATION_DETECTION_REGISTRATION_HASH, contextHash, nameHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |
| `nameHash` | `Hash` | `uint` | |
| `value` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_TRY_ACCESS_TUNABLE_BOOL_HASH

**Hash:** `0xC7420099936CE286` | **JHash:** `0x8AFE3D90` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_TRY_ACCESS_TUNABLE_BOOL_HASH(Hash tunableContext, Hash tunableName, BOOL defaultValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_TRY_ACCESS_TUNABLE_BOOL_HASH, tunableContext, tunableName, defaultValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `tunableContext` | `Hash` | `uint` | |
| `tunableName` | `Hash` | `uint` | |
| `defaultValue` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns defaultValue if the tunable doesn't exist.

---

### NETWORK_GET_CONTENT_MODIFIER_LIST_ID

**Hash:** `0x187382F8A3E0A6C3` | **JHash:** `0xA78571CA` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_CONTENT_MODIFIER_LIST_ID(Hash contentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_CONTENT_MODIFIER_LIST_ID, contentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Return the content modifier id (the tunables context if you want) of a specific content.

It takes the content hash (which is the mission id hash), and return the content modifier id, used as the tunables context.

The mission id can be found on the Social club, for example, 'socialclub.rockstargames.com/games/gtav/jobs/job/A8M6Bz8MLEC5xngvDCzGwA'

'A8M6Bz8MLEC5xngvDCzGwA' is the mission id, so the game hash this and use it as the parameter for this native.


---

### NETWORK_GET_BONE_ID_OF_FATAL_HIT

**Hash:** `0x7DB53B37A2F211A0` | **JHash:** `0x053BB329` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_BONE_ID_OF_FATAL_HIT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_BONE_ID_OF_FATAL_HIT);
```

**返回值:** `int` → `int`

---

### NETWORK_RESET_BODY_TRACKER

**Hash:** `0x72433699B4E6DD64` | **JHash:** `0x3914463F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_RESET_BODY_TRACKER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_RESET_BODY_TRACKER);
```

**返回值:** `void` → `void`

---

### NETWORK_GET_NUMBER_BODY_TRACKER_HITS

**Hash:** `0xD38C4A6D047C019D` | **JHash:** `0x17CBC608` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUMBER_BODY_TRACKER_HITS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUMBER_BODY_TRACKER_HITS);
```

**返回值:** `int` → `int`

---

### NETWORK_HAS_BONE_BEEN_HIT_BY_KILLER

**Hash:** `0x2E0BF682CC778D49` | **JHash:** `0xBFAA349B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_BONE_BEEN_HIT_BY_KILLER(int boneIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_BONE_BEEN_HIT_BY_KILLER, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `boneIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_SET_ATTRIBUTE_DAMAGE_TO_PLAYER

**Hash:** `0x0EDE326D47CD0F3E` | **JHash:** `0xBEB7281A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_SET_ATTRIBUTE_DAMAGE_TO_PLAYER(Ped ped, Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SET_ATTRIBUTE_DAMAGE_TO_PLAYER, ped, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_DAMAGE

**Hash:** `0x890E2C5ABED7236D` | **Build:** `1365`

**C++ 原型:**
```cpp
void NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_DAMAGE(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_DAMAGE, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Allows vehicle wheels to be destructible even when the vehicle entity is invincible.

---

### _NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_PED_DAMAGE

**Hash:** `0xE9D0244ACBEE1BC4` | **Build:** `3258`

**C++ 原型:**
```cpp
void _NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_PED_DAMAGE(Entity entity, BOOL trigger)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_PED_DAMAGE, entity, trigger);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `trigger` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_WEAPON_HASH

**Hash:** `0x38B7C51AB1EDC7D8` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_WEAPON_HASH(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_TRIGGER_DAMAGE_EVENT_FOR_ZERO_WEAPON_HASH, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_NO_LONGER_NEEDED

**Hash:** `0x3FC795691834481D` | **Build:** `1868`

**C++ 原型:**
```cpp
void NETWORK_SET_NO_LONGER_NEEDED(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_NO_LONGER_NEEDED, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_EXPLODE_VEHICLE

**Hash:** `0x301A42153C9AD707` | **JHash:** `0x0E1B38AE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_EXPLODE_VEHICLE(Vehicle vehicle, BOOL isAudible, BOOL isInvisible, int netId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_EXPLODE_VEHICLE, vehicle, isAudible, isInvisible, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `netId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** In the console script dumps, this is only referenced once. 
NETWORK::NETWORK_EXPLODE_VEHICLE(vehicle, 1, 0, 0);

^^^^^ That must be PC script dumps? In X360 Script Dumps it is reference a few times with 2 differences in the parameters.
Which as you see below is 1, 0, 0 + 1, 1, 0 + 1, 0, and a *param?

am_plane_takedown.c 
network_explode_vehicle(net_to_veh(Local_40.imm_2), 1, 1, 0);

armenian2.c 
network_explode_vehicle(Local_80[6 <2>], 1, 0, 0);

fm_horde_controler.c
network_explode_vehicle(net_to_veh(*uParam0), 1, 0, *uParam0);

fm_mission_controller.c, has 6 hits so not going to list them.

Side note, setting the first parameter to 0 seems to mute sound or so?

Seems it's like ADD_EXPLOSION, etc. the first 2 params. The 3rd atm no need to worry since it always seems to be 0.


---

### NETWORK_EXPLODE_HELI

**Hash:** `0x2A5E0621DD815A9A` | **JHash:** `0x955B31BF` | **Build:** `463`

**C++ 原型:**
```cpp
void NETWORK_EXPLODE_HELI(Vehicle vehicle, BOOL isAudible, BOOL isInvisible, int netId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_EXPLODE_HELI, vehicle, isAudible, isInvisible, netId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |
| `netId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_USE_LOGARITHMIC_BLENDING_THIS_FRAME

**Hash:** `0xCD71A4ECAB22709E` | **JHash:** `0xBC54371B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_USE_LOGARITHMIC_BLENDING_THIS_FRAME(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_USE_LOGARITHMIC_BLENDING_THIS_FRAME, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_OVERRIDE_COORDS_AND_HEADING

**Hash:** `0xA7E30DE9272B6D49` | **JHash:** `0x644141C5` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_OVERRIDE_COORDS_AND_HEADING(Entity entity, float x, float y, float z, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_OVERRIDE_COORDS_AND_HEADING, entity, x, y, z, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### NETWORK_ENABLE_EXTRA_VEHICLE_ORIENTATION_BLEND_CHECKS

**Hash:** `0xE6717E652B8C8D8A` | **Build:** `617`

**C++ 原型:**
```cpp
void NETWORK_ENABLE_EXTRA_VEHICLE_ORIENTATION_BLEND_CHECKS(int netId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ENABLE_EXTRA_VEHICLE_ORIENTATION_BLEND_CHECKS, netId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_DISABLE_PROXIMITY_MIGRATION

**Hash:** `0x407091CF6037118E` | **JHash:** `0x9F82917F` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DISABLE_PROXIMITY_MIGRATION(int netID)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DISABLE_PROXIMITY_MIGRATION, netID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netID` | `int` | `int` | |

**返回值:** `void` → `void`

---

### NETWORK_SET_PLAYER_MENTAL_STATE

**Hash:** `0x367EF5E2F439B4C6` | **JHash:** `0x53C9563C` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_PLAYER_MENTAL_STATE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_PLAYER_MENTAL_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 in the decompiled scripts is always the stat mesh_texblend * 0.07 to int

---

### NETWORK_SET_MINIMUM_RANK_FOR_MISSION

**Hash:** `0x94538037EE44F5CF` | **JHash:** `0x6B97075B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SET_MINIMUM_RANK_FOR_MISSION(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SET_MINIMUM_RANK_FOR_MISSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_CACHE_LOCAL_PLAYER_HEAD_BLEND_DATA

**Hash:** `0xBD0BE0BFC927EAC1` | **JHash:** `0x94AB35A9` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_CACHE_LOCAL_PLAYER_HEAD_BLEND_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CACHE_LOCAL_PLAYER_HEAD_BLEND_DATA);
```

**返回值:** `void` → `void`

---

### NETWORK_HAS_CACHED_PLAYER_HEAD_BLEND_DATA

**Hash:** `0x237D5336A9A54108` | **JHash:** `0x7B2A0337` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_CACHED_PLAYER_HEAD_BLEND_DATA(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_CACHED_PLAYER_HEAD_BLEND_DATA, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_APPLY_CACHED_PLAYER_HEAD_BLEND_DATA

**Hash:** `0x99B72C7ABDE5C910` | **JHash:** `0x99F58A07` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_APPLY_CACHED_PLAYER_HEAD_BLEND_DATA(Ped ped, Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_APPLY_CACHED_PLAYER_HEAD_BLEND_DATA, ped, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _NETWORK_SET_IGNORE_VEHICLE_RAMMED_BY_NON_VEHICLE

**Hash:** `0x67F7C81C1395672A` | **Build:** `3717`

**C++ 原型:**
```cpp
void _NETWORK_SET_IGNORE_VEHICLE_RAMMED_BY_NON_VEHICLE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_SET_IGNORE_VEHICLE_RAMMED_BY_NON_VEHICLE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_NUM_COMMERCE_ITEMS

**Hash:** `0xF2EAC213D5EA0623` | **JHash:** `0x965EA007` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_COMMERCE_ITEMS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_COMMERCE_ITEMS);
```

**返回值:** `int` → `int`

---

### IS_COMMERCE_DATA_VALID

**Hash:** `0xEA14EEF5B7CD2C30` | **JHash:** `0xEEFC8A55` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COMMERCE_DATA_VALID(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COMMERCE_DATA_VALID);
```

**返回值:** `BOOL` → `bool`

---

### TRIGGER_COMMERCE_DATA_FETCH

**Hash:** `0xB606E6CC59664972` | **JHash:** `0x866D1B67` | **Build:** `323`

**C++ 原型:**
```cpp
void TRIGGER_COMMERCE_DATA_FETCH(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_COMMERCE_DATA_FETCH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### IS_COMMERCE_DATA_FETCH_IN_PROGRESS

**Hash:** `0x1D4DC17C38FEAFF0` | **JHash:** `0xED4A272F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COMMERCE_DATA_FETCH_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COMMERCE_DATA_FETCH_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

---

### GET_COMMERCE_ITEM_ID

**Hash:** `0x662635855957C411` | **JHash:** `0x4ACF110C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_COMMERCE_ITEM_ID(int index)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_COMMERCE_ITEM_ID, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_COMMERCE_ITEM_NAME

**Hash:** `0xB4271092CA7EDF48` | **JHash:** `0x1AA3A0D5` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_COMMERCE_ITEM_NAME(int index)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_COMMERCE_ITEM_NAME, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_COMMERCE_PRODUCT_PRICE

**Hash:** `0xCA94551B50B4932C` | **JHash:** `0x37877757` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_COMMERCE_PRODUCT_PRICE(int index)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_COMMERCE_PRODUCT_PRICE, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_COMMERCE_ITEM_NUM_CATS

**Hash:** `0x2A7776C709904AB0` | **JHash:** `0x1CF89DA5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_COMMERCE_ITEM_NUM_CATS(int index)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_COMMERCE_ITEM_NUM_CATS, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_COMMERCE_ITEM_CAT

**Hash:** `0x6F44CBF56D79FAC0` | **JHash:** `0x16E53875` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_COMMERCE_ITEM_CAT(int index, int index2)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_COMMERCE_ITEM_CAT, index, index2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `index2` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** index2 is unused

---

### OPEN_COMMERCE_STORE

**Hash:** `0x58C21165F6545892` | **JHash:** `0x365C50EE` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_COMMERCE_STORE(const char* p0, const char* p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_COMMERCE_STORE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `const char*` | `string` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_COMMERCE_STORE_OPEN

**Hash:** `0x2EAC52B4019E2782` | **JHash:** `0x25E2DBA9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COMMERCE_STORE_OPEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COMMERCE_STORE_OPEN);
```

**返回值:** `BOOL` → `bool`

---

### SET_STORE_ENABLED

**Hash:** `0x9641A9FF718E9C5E` | **JHash:** `0xC1F6443B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STORE_ENABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STORE_ENABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Access to the store for shark cards etc...

---

### REQUEST_COMMERCE_ITEM_IMAGE

**Hash:** `0xA2F952104FC6DD4B` | **JHash:** `0x1FDC75DC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_COMMERCE_ITEM_IMAGE(int index)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_COMMERCE_ITEM_IMAGE, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_ALL_COMMERCE_ITEM_IMAGES

**Hash:** `0x72D0706CD6CCDB58` | **JHash:** `0xCA7A0A49` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_ALL_COMMERCE_ITEM_IMAGES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_ALL_COMMERCE_ITEM_IMAGES);
```

**返回值:** `void` → `void`

---

### GET_COMMERCE_ITEM_TEXTURENAME

**Hash:** `0x722F5D28B61C5EA8` | **JHash:** `0x44A58B0A` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_COMMERCE_ITEM_TEXTURENAME(int index)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_COMMERCE_ITEM_TEXTURENAME, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### IS_STORE_AVAILABLE_TO_USER

**Hash:** `0x883D79C4071E18B3` | **JHash:** `0xD32FA11F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STORE_AVAILABLE_TO_USER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STORE_AVAILABLE_TO_USER);
```

**返回值:** `BOOL` → `bool`

---

### DELAY_MP_STORE_OPEN

**Hash:** `0x265635150FB0D82E` | **JHash:** `0xA7FA70AE` | **Build:** `323`

**C++ 原型:**
```cpp
void DELAY_MP_STORE_OPEN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELAY_MP_STORE_OPEN);
```

**返回值:** `void` → `void`

---

### RESET_STORE_NETWORK_GAME_TRACKING

**Hash:** `0x444C4525ECE0A4B9` | **JHash:** `0xCC7DCE24` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_STORE_NETWORK_GAME_TRACKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_STORE_NETWORK_GAME_TRACKING);
```

**返回值:** `void` → `void`

---

### IS_USER_OLD_ENOUGH_TO_ACCESS_STORE

**Hash:** `0x59328EB08C5CEB2B` | **JHash:** `0x70F6D3AD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_USER_OLD_ENOUGH_TO_ACCESS_STORE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_USER_OLD_ENOUGH_TO_ACCESS_STORE);
```

**返回值:** `BOOL` → `bool`

---

### SET_LAST_VIEWED_SHOP_ITEM

**Hash:** `0xFAE628F1E9ADB239` | **JHash:** `0xD59A822B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LAST_VIEWED_SHOP_ITEM(Hash p0, int p1, Hash p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LAST_VIEWED_SHOP_ITEM, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `p2` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_USER_PREMIUM_ACCESS

**Hash:** `0x754615490A029508` | **Build:** `1290`

**C++ 原型:**
```cpp
int GET_USER_PREMIUM_ACCESS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_USER_PREMIUM_ACCESS);
```

**返回值:** `int` → `int`

**说明:** Checks some commerce stuff

---

### GET_USER_STARTER_ACCESS

**Hash:** `0x155467ACA0F55705` | **Build:** `1290`

**C++ 原型:**
```cpp
int GET_USER_STARTER_ACCESS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_USER_STARTER_ACCESS);
```

**返回值:** `int` → `int`

**说明:** Checks some commerce stuff

---

### CLOUD_DELETE_MEMBER_FILE

**Hash:** `0xC64DED7EF0D2FE37` | **JHash:** `0x2B7B57B3` | **Build:** `323`

**C++ 原型:**
```cpp
int CLOUD_DELETE_MEMBER_FILE(const char* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CLOUD_DELETE_MEMBER_FILE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### CLOUD_HAS_REQUEST_COMPLETED

**Hash:** `0x4C61B39930D045DA` | **JHash:** `0xBAF52DD8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CLOUD_HAS_REQUEST_COMPLETED(int requestId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CLOUD_HAS_REQUEST_COMPLETED, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLOUD_DID_REQUEST_SUCCEED

**Hash:** `0x3A3D5568AF297CD5` | **JHash:** `0x9B9AFFF1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CLOUD_DID_REQUEST_SUCCEED(int requestId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CLOUD_DID_REQUEST_SUCCEED, requestId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `requestId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLOUD_CHECK_AVAILABILITY

**Hash:** `0x4F18196C8D38768D` | **JHash:** `0xC38E9DB0` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOUD_CHECK_AVAILABILITY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOUD_CHECK_AVAILABILITY);
```

**返回值:** `void` → `void`

**说明:** Downloads prod.cloud.rockstargames.com/titles/gta5/[platform]/check.json

---

### CLOUD_IS_CHECKING_AVAILABILITY

**Hash:** `0xC7ABAC5DE675EE3B` | **JHash:** `0x32A4EB22` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CLOUD_IS_CHECKING_AVAILABILITY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CLOUD_IS_CHECKING_AVAILABILITY);
```

**返回值:** `BOOL` → `bool`

---

### CLOUD_GET_AVAILABILITY_CHECK_RESULT

**Hash:** `0x0B0CC10720653F3B` | **JHash:** `0x9262744C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CLOUD_GET_AVAILABILITY_CHECK_RESULT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CLOUD_GET_AVAILABILITY_CHECK_RESULT);
```

**返回值:** `BOOL` → `bool`

---

### GET_CONTENT_TO_LOAD_TYPE

**Hash:** `0x8B0C2964BA471961` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CONTENT_TO_LOAD_TYPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CONTENT_TO_LOAD_TYPE);
```

**返回值:** `int` → `int`

**说明:** This function is hard-coded to always return 0.

---

### GET_IS_LAUNCH_FROM_LIVE_AREA

**Hash:** `0x88B588B41FF7868E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_LAUNCH_FROM_LIVE_AREA(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_LAUNCH_FROM_LIVE_AREA);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### GET_IS_LIVE_AREA_LAUNCH_WITH_CONTENT

**Hash:** `0x67FC09BC554A75E5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_LIVE_AREA_LAUNCH_WITH_CONTENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_LIVE_AREA_LAUNCH_WITH_CONTENT);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### CLEAR_SERVICE_EVENT_ARGUMENTS

**Hash:** `0x966DD84FB6A46017` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_SERVICE_EVENT_ARGUMENTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_SERVICE_EVENT_ARGUMENTS);
```

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### UGC_COPY_CONTENT

**Hash:** `0x152D90E4C1B4738A` | **JHash:** `0x08243B79` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_COPY_CONTENT(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_COPY_CONTENT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_IS_CREATING

**Hash:** `0x9FEDF86898F100E9` | **JHash:** `0x798D6C27` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_IS_CREATING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_IS_CREATING);
```

**返回值:** `BOOL` → `bool`

---

### UGC_HAS_CREATE_FINISHED

**Hash:** `0x5E24341A7F92A74B` | **JHash:** `0xE69E8D0D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_HAS_CREATE_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_CREATE_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_DID_CREATE_SUCCEED

**Hash:** `0x24E4E51FC16305F9` | **JHash:** `0x742075FE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_DID_CREATE_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_DID_CREATE_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CREATE_RESULT

**Hash:** `0xFBC5E768C7A77A6A` | **JHash:** `0xCE569932` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CREATE_RESULT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CREATE_RESULT);
```

**返回值:** `int` → `int`

---

### UGC_GET_CREATE_CONTENT_ID

**Hash:** `0xC55A0B40FFB1ED23` | **JHash:** `0x82146BE9` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CREATE_CONTENT_ID(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CREATE_CONTENT_ID);
```

**返回值:** `const char*` → `string`

---

### UGC_CLEAR_CREATE_RESULT

**Hash:** `0x17440AA15D1D3739` | **JHash:** `0x133FF2D5` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_CLEAR_CREATE_RESULT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_CLEAR_CREATE_RESULT);
```

**返回值:** `void` → `void`

---

### UGC_QUERY_MY_CONTENT

**Hash:** `0x9BF438815F5D96EA` | **JHash:** `0xCBA7242F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_QUERY_MY_CONTENT(Any p0, Any p1, Any* p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_QUERY_MY_CONTENT, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_QUERY_BY_CATEGORY

**Hash:** `0x692D58DF40657E8C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_QUERY_BY_CATEGORY(Any p0, Any p1, Any p2, const char* p3, Any p4, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_QUERY_BY_CATEGORY, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `const char*` | `string` | |
| `p4` | `Any` | `object` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### UGC_QUERY_BY_CONTENT_ID

**Hash:** `0x158EC424F35EC469` | **JHash:** `0xDED82A6E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_QUERY_BY_CONTENT_ID(const char* contentId, BOOL latestVersion, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_QUERY_BY_CONTENT_ID, contentId, latestVersion, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `latestVersion` | `BOOL` | `bool` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_QUERY_BY_CONTENT_IDS

**Hash:** `0xC7397A83F7A2A462` | **JHash:** `0x47B3C35E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_QUERY_BY_CONTENT_IDS(Any* data, int count, BOOL latestVersion, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_QUERY_BY_CONTENT_IDS, data, count, latestVersion, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `count` | `int` | `int` | |
| `latestVersion` | `BOOL` | `bool` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_QUERY_MOST_RECENTLY_CREATED_CONTENT

**Hash:** `0x6D4CB481FAC835E8` | **JHash:** `0x40CF0783` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_QUERY_MOST_RECENTLY_CREATED_CONTENT(int offset, int count, const char* contentTypeName, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_QUERY_MOST_RECENTLY_CREATED_CONTENT, offset, count, contentTypeName, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `offset` | `int` | `int` | |
| `count` | `int` | `int` | |
| `contentTypeName` | `const char*` | `string` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_BOOKMARKED_CONTENT

**Hash:** `0xD5A4B59980401588` | **JHash:** `0x4609D596` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_BOOKMARKED_CONTENT(Any p0, Any p1, const char* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_BOOKMARKED_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `const char*` | `string` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_MY_CONTENT

**Hash:** `0x3195F8DD0D531052` | **JHash:** `0x4C2C0D1F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_MY_CONTENT(Any p0, Any p1, const char* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_MY_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `const char*` | `string` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_FRIEND_CONTENT

**Hash:** `0xF9E1CCAE8BA4C281` | **JHash:** `0x9EFBD5D1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_FRIEND_CONTENT(Any p0, Any p1, const char* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_FRIEND_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `const char*` | `string` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CREW_CONTENT

**Hash:** `0x9F6E2821885CAEE2` | **JHash:** `0xA6D8B798` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CREW_CONTENT(Any p0, Any p1, Any p2, const char* p3, Any* p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CREW_CONTENT, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `const char*` | `string` | |
| `p4` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_GET_BY_CATEGORY

**Hash:** `0x678BB03C1A3BD51E` | **JHash:** `0x67E74842` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_GET_BY_CATEGORY(Any p0, Any p1, Any p2, const char* p3, Any* p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_GET_BY_CATEGORY, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `const char*` | `string` | |
| `p4` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_GET_BY_CONTENT_ID

**Hash:** `0x815E5E3073DA1D67` | **JHash:** `0xE123C7AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_GET_BY_CONTENT_ID(const char* contentId, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_GET_BY_CONTENT_ID, contentId, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_GET_BY_CONTENT_IDS

**Hash:** `0xB8322EEB38BE7C26` | **JHash:** `0x22C33603` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_GET_BY_CONTENT_IDS(Any* data, int dataCount, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_GET_BY_CONTENT_IDS, data, dataCount, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `dataCount` | `int` | `int` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_MOST_RECENTLY_CREATED_CONTENT

**Hash:** `0xA7862BC5ED1DFD7E` | **JHash:** `0x37F5BD93` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_MOST_RECENTLY_CREATED_CONTENT(Any p0, Any p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_MOST_RECENTLY_CREATED_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_MOST_RECENTLY_PLAYED_CONTENT

**Hash:** `0x97A770BEEF227E2B` | **JHash:** `0x1CFB3F51` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_MOST_RECENTLY_PLAYED_CONTENT(Any p0, Any p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_MOST_RECENTLY_PLAYED_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_TOP_RATED_CONTENT

**Hash:** `0x5324A0E3E4CE3570` | **JHash:** `0x87D1E6BD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_TOP_RATED_CONTENT(Any p0, Any p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_TOP_RATED_CONTENT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_CANCEL_QUERY

**Hash:** `0xE9B99B6853181409` | **JHash:** `0x021D5A94` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_CANCEL_QUERY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_CANCEL_QUERY);
```

**返回值:** `void` → `void`

---

### UGC_IS_GETTING

**Hash:** `0xD53ACDBEF24A46E8` | **JHash:** `0x4908A514` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_IS_GETTING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_IS_GETTING);
```

**返回值:** `BOOL` → `bool`

---

### UGC_HAS_GET_FINISHED

**Hash:** `0x02ADA21EA2F6918F` | **JHash:** `0x50296140` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_HAS_GET_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_GET_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_DID_GET_SUCCEED

**Hash:** `0x941E5306BCD7C2C7` | **JHash:** `0x3970B0DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_DID_GET_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_DID_GET_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_WAS_QUERY_FORCE_CANCELLED

**Hash:** `0xC87E740D9F3872CC` | **JHash:** `0xC1487110` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_WAS_QUERY_FORCE_CANCELLED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_WAS_QUERY_FORCE_CANCELLED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_GET_QUERY_RESULT

**Hash:** `0xEDF7F927136C224B` | **JHash:** `0xCC2356E3` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_QUERY_RESULT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_QUERY_RESULT);
```

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_NUM

**Hash:** `0xE0A6138401BCB837` | **JHash:** `0x2DE69817` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_NUM(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_NUM);
```

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_TOTAL

**Hash:** `0x769951E2455E2EB5` | **JHash:** `0x81BD8D3B` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_TOTAL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_TOTAL);
```

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_HASH

**Hash:** `0x3A17A27D75C74887` | **JHash:** `0x8E1D8F78` | **Build:** `323`

**C++ 原型:**
```cpp
Hash UGC_GET_CONTENT_HASH(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.UGC_GET_CONTENT_HASH);
```

**返回值:** `Hash` → `uint`

---

### UGC_CLEAR_QUERY_RESULTS

**Hash:** `0xBA96394A0EECFA65` | **JHash:** `0x0D35DD93` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_CLEAR_QUERY_RESULTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_CLEAR_QUERY_RESULTS);
```

**返回值:** `void` → `void`

---

### UGC_GET_CONTENT_USER_ID

**Hash:** `0xCD67AD041A394C9C` | **JHash:** `0x8F3137E6` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_USER_ID(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_USER_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### UGC_GET_CONTENT_CREATOR_GAMER_HANDLE

**Hash:** `0x584770794D758C18` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_CREATOR_GAMER_HANDLE(int p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_CREATOR_GAMER_HANDLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_CREATED_BY_LOCAL_PLAYER

**Hash:** `0x8C8D2739BA44AF0F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_CREATED_BY_LOCAL_PLAYER(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_CREATED_BY_LOCAL_PLAYER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_USER_NAME

**Hash:** `0x703F12425ECA8BF5` | **JHash:** `0xB9137BA7` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_USER_NAME(Any p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_USER_NAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `const char*` → `string`

---

### UGC_GET_CONTENT_IS_USING_SC_NICKNAME

**Hash:** `0xAEAB987727C5A8A4` | **JHash:** `0x9FEEAA9C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_IS_USING_SC_NICKNAME(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_IS_USING_SC_NICKNAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_CATEGORY

**Hash:** `0xA7BAB11E7C9C6C5A` | **JHash:** `0x5E8A7559` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_CATEGORY(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_CATEGORY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_ID

**Hash:** `0x55AA95F481D694D2` | **JHash:** `0x331AEABF` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_ID(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Return the mission id of a job.

---

### UGC_GET_ROOT_CONTENT_ID

**Hash:** `0xC0173D6BFF4E0348` | **JHash:** `0x0E5E8E5C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_ROOT_CONTENT_ID(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_ROOT_CONTENT_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Return the root content id of a job.

---

### UGC_GET_CONTENT_NAME

**Hash:** `0xBF09786A7FCAB582` | **JHash:** `0xA5A0C695` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_NAME(Any p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_NAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `const char*` → `string`

---

### UGC_GET_CONTENT_DESCRIPTION

**Hash:** `0x4A56710BAB5C4DB4` | **Build:** `3407`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_DESCRIPTION(int index)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_DESCRIPTION, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### UGC_GET_CONTENT_DESCRIPTION_HASH

**Hash:** `0x7CF0448787B23758` | **JHash:** `0x91534C6E` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_DESCRIPTION_HASH(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_DESCRIPTION_HASH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_PATH

**Hash:** `0xBAF6BABF9E7CCC13` | **JHash:** `0x744A9EA5` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CONTENT_PATH(int p0, int p1)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CONTENT_PATH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### UGC_GET_CONTENT_UPDATED_DATE

**Hash:** `0xCFD115B373C0DF63` | **JHash:** `0xA19A238D` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_GET_CONTENT_UPDATED_DATE(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_GET_CONTENT_UPDATED_DATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### UGC_GET_CONTENT_FILE_VERSION

**Hash:** `0x37025B27D9B658B1` | **JHash:** `0xFF7D44E6` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_FILE_VERSION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_FILE_VERSION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_HAS_LO_RES_PHOTO

**Hash:** `0x1D610EB0FEA716D9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_HAS_LO_RES_PHOTO(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_HAS_LO_RES_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_HAS_HI_RES_PHOTO

**Hash:** `0x7FCC39C46C3C03BD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_HAS_HI_RES_PHOTO(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_HAS_HI_RES_PHOTO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_LANGUAGE

**Hash:** `0x32DD916F3F7C9672` | **JHash:** `0xA2C5BD9D` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_LANGUAGE(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_LANGUAGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_IS_PUBLISHED

**Hash:** `0x3054F114121C21EA` | **JHash:** `0xA850DDE1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_IS_PUBLISHED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_IS_PUBLISHED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_IS_VERIFIED

**Hash:** `0xA9240A96C74CCA13` | **JHash:** `0x8F6754AE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_IS_VERIFIED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_IS_VERIFIED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_RATING

**Hash:** `0x1ACCFBA3D8DAB2EE` | **JHash:** `0x1E34953F` | **Build:** `323`

**C++ 原型:**
```cpp
float UGC_GET_CONTENT_RATING(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.UGC_GET_CONTENT_RATING, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `float` → `float`

---

### UGC_GET_CONTENT_RATING_COUNT

**Hash:** `0x759299C5BB31D2A9` | **JHash:** `0x771FE190` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_RATING_COUNT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_RATING_COUNT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_RATING_POSITIVE_COUNT

**Hash:** `0x87E5C46C187FE0AE` | **JHash:** `0x3276D9D3` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_RATING_POSITIVE_COUNT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_RATING_POSITIVE_COUNT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_RATING_NEGATIVE_COUNT

**Hash:** `0x4E548C0D7AE39FF9` | **JHash:** `0x41A0FB02` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CONTENT_RATING_NEGATIVE_COUNT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CONTENT_RATING_NEGATIVE_COUNT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UGC_GET_CONTENT_HAS_PLAYER_RECORD

**Hash:** `0x70EA8DA57840F9BE` | **JHash:** `0x11DC0F27` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_HAS_PLAYER_RECORD(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_HAS_PLAYER_RECORD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CONTENT_HAS_PLAYER_BOOKMARKED

**Hash:** `0x993CBE59D350D225` | **JHash:** `0x0DEB3F5A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CONTENT_HAS_PLAYER_BOOKMARKED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CONTENT_HAS_PLAYER_BOOKMARKED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_REQUEST_CONTENT_DATA_FROM_INDEX

**Hash:** `0x171DF6A0C07FB3DC` | **JHash:** `0x84315226` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_REQUEST_CONTENT_DATA_FROM_INDEX(int p0, int p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_REQUEST_CONTENT_DATA_FROM_INDEX, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `int` → `int`

---

### UGC_REQUEST_CONTENT_DATA_FROM_PARAMS

**Hash:** `0x7FD2990AF016795E` | **JHash:** `0x38FC2EEB` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_REQUEST_CONTENT_DATA_FROM_PARAMS(const char* contentTypeName, const char* contentId, int p2, int p3, int p4)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_REQUEST_CONTENT_DATA_FROM_PARAMS, contentTypeName, contentId, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentTypeName` | `const char*` | `string` | |
| `contentId` | `const char*` | `string` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |

**返回值:** `int` → `int`

---

### UGC_REQUEST_CACHED_DESCRIPTION

**Hash:** `0x5E0165278F6339EE` | **JHash:** `0x1C4F9FDB` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_REQUEST_CACHED_DESCRIPTION(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_REQUEST_CACHED_DESCRIPTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### UGC_IS_DESCRIPTION_REQUEST_IN_PROGRESS

**Hash:** `0x2D5DC831176D0114` | **JHash:** `0xA69AE16C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_IS_DESCRIPTION_REQUEST_IN_PROGRESS(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_IS_DESCRIPTION_REQUEST_IN_PROGRESS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_HAS_DESCRIPTION_REQUEST_FINISHED

**Hash:** `0xEBFA8D50ADDC54C4` | **JHash:** `0xF50BC67A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_HAS_DESCRIPTION_REQUEST_FINISHED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_DESCRIPTION_REQUEST_FINISHED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_DID_DESCRIPTION_REQUEST_SUCCEED

**Hash:** `0x162C23CA83ED0A62` | **JHash:** `0xB3BBD241` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_DID_DESCRIPTION_REQUEST_SUCCEED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_DID_DESCRIPTION_REQUEST_SUCCEED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CACHED_DESCRIPTION

**Hash:** `0x40F7E66472DF3E5C` | **JHash:** `0x70A2845C` | **Build:** `323`

**C++ 原型:**
```cpp
const char* UGC_GET_CACHED_DESCRIPTION(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.UGC_GET_CACHED_DESCRIPTION, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `const char*` → `string`

---

### UGC_RELEASE_CACHED_DESCRIPTION

**Hash:** `0x5A34CD9C3C5BEC44` | **JHash:** `0x346B506C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_RELEASE_CACHED_DESCRIPTION(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_RELEASE_CACHED_DESCRIPTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_RELEASE_ALL_CACHED_DESCRIPTIONS

**Hash:** `0x68103E2247887242` | **JHash:** `0x0095DB71` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_RELEASE_ALL_CACHED_DESCRIPTIONS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_RELEASE_ALL_CACHED_DESCRIPTIONS);
```

**返回值:** `void` → `void`

---

### UGC_HAS_PERMISSION_TO_WRITE

**Hash:** `0xC33E7CBC06EC1A8D` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL UGC_HAS_PERMISSION_TO_WRITE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_PERMISSION_TO_WRITE);
```

**返回值:** `BOOL` → `bool`

---

### UGC_PUBLISH

**Hash:** `0x1DE0F5F50D723CAA` | **JHash:** `0xAD334B40` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_PUBLISH(const char* contentId, const char* baseContentId, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_PUBLISH, contentId, baseContentId, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `baseContentId` | `const char*` | `string` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_SET_BOOKMARKED

**Hash:** `0x274A1519DFC1094F` | **JHash:** `0x980D45D7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_SET_BOOKMARKED(const char* contentId, BOOL bookmarked, const char* contentTypeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_SET_BOOKMARKED, contentId, bookmarked, contentTypeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `const char*` | `string` | |
| `bookmarked` | `BOOL` | `bool` | |
| `contentTypeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_SET_DELETED

**Hash:** `0xD05D1A6C74DA3498` | **JHash:** `0x48CCC328` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_SET_DELETED(Any* p0, BOOL p1, const char* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_SET_DELETED, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### UGC_IS_MODIFYING

**Hash:** `0x45E816772E93A9DB` | **JHash:** `0x8E664EFD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_IS_MODIFYING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_IS_MODIFYING);
```

**返回值:** `BOOL` → `bool`

---

### UGC_HAS_MODIFY_FINISHED

**Hash:** `0x299EF3C576773506` | **JHash:** `0x611E0BE2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_HAS_MODIFY_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_MODIFY_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_DID_MODIFY_SUCCEED

**Hash:** `0x793FF272D5B365F4` | **JHash:** `0xF0211AC1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_DID_MODIFY_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_DID_MODIFY_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_GET_MODIFY_RESULT

**Hash:** `0x5A0A3D1A186A5508` | **JHash:** `0x1F0DD8AF` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_MODIFY_RESULT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_MODIFY_RESULT);
```

**返回值:** `int` → `int`

---

### UGC_CLEAR_MODIFY_RESULT

**Hash:** `0xA1E5E0204A6FCC70` | **JHash:** `0x405ECA16` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_CLEAR_MODIFY_RESULT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_CLEAR_MODIFY_RESULT);
```

**返回值:** `void` → `void`

---

### UGC_GET_CREATORS_BY_USER_ID

**Hash:** `0xB746D20B17F2A229` | **JHash:** `0x9567392B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_GET_CREATORS_BY_USER_ID(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_GET_CREATORS_BY_USER_ID, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### UGC_HAS_QUERY_CREATORS_FINISHED

**Hash:** `0x63B406D7884BFA95` | **JHash:** `0xF79FFF3C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_HAS_QUERY_CREATORS_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_HAS_QUERY_CREATORS_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_DID_QUERY_CREATORS_SUCCEED

**Hash:** `0x4D02279C83BE69FE` | **JHash:** `0xA7F3F82B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_DID_QUERY_CREATORS_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_DID_QUERY_CREATORS_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### UGC_GET_CREATOR_NUM

**Hash:** `0x597F8DBA9B206FC7` | **JHash:** `0x410C61D1` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_GET_CREATOR_NUM(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_GET_CREATOR_NUM);
```

**返回值:** `int` → `int`

---

### UGC_LOAD_OFFLINE_QUERY

**Hash:** `0x5CAE833B0EE0C500` | **JHash:** `0x0D4F845D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_LOAD_OFFLINE_QUERY(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_LOAD_OFFLINE_QUERY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### UGC_CLEAR_OFFLINE_QUERY

**Hash:** `0x61A885D3F7CFEE9A` | **JHash:** `0xE13C1F7F` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_CLEAR_OFFLINE_QUERY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_CLEAR_OFFLINE_QUERY);
```

**返回值:** `void` → `void`

---

### UGC_SET_QUERY_DATA_FROM_OFFLINE

**Hash:** `0xF98DDE0A8ED09323` | **JHash:** `0x213C6D36` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_SET_QUERY_DATA_FROM_OFFLINE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_SET_QUERY_DATA_FROM_OFFLINE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### UGC_SET_USING_OFFLINE_CONTENT

**Hash:** `0xFD75DABC0957BF33` | **JHash:** `0x511E6F50` | **Build:** `323`

**C++ 原型:**
```cpp
void UGC_SET_USING_OFFLINE_CONTENT(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UGC_SET_USING_OFFLINE_CONTENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### UGC_IS_LANGUAGE_SUPPORTED

**Hash:** `0xF53E48461B71EECB` | **JHash:** `0xB4668B23` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UGC_IS_LANGUAGE_SUPPORTED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UGC_IS_LANGUAGE_SUPPORTED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_POST_COMPLETED_HEIST

**Hash:** `0x098AB65B9ED9A9EC` | **JHash:** `0x30B51753` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_POST_COMPLETED_HEIST(const char* heistName, int cashEarned, int xpEarned)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_POST_COMPLETED_HEIST, heistName, cashEarned, xpEarned);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `heistName` | `const char*` | `string` | |
| `cashEarned` | `int` | `int` | |
| `xpEarned` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_POST_CREATE_CHARACTER

**Hash:** `0xDC48473142545431` | **JHash:** `0x02DAD93F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_POST_CREATE_CHARACTER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_POST_CREATE_CHARACTER);
```

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_POST_COMPLETED_MILESTONE

**Hash:** `0x0AE1F1653B554AB9` | **JHash:** `0x2D947814` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_POST_COMPLETED_MILESTONE(int milestoneId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_POST_COMPLETED_MILESTONE, milestoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `milestoneId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_HAS_POST_COMPLETED

**Hash:** `0x62B9FEC9A11F10EF` | **JHash:** `0x37A28C26` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_HAS_POST_COMPLETED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_HAS_POST_COMPLETED);
```

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_DID_POST_SUCCEED

**Hash:** `0xA75E2B6733DA5142` | **JHash:** `0x11E8B5CD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_DID_POST_SUCCEED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_DID_POST_SUCCEED);
```

**返回值:** `BOOL` → `bool`

---

### FACEBOOK_CAN_POST_TO_FACEBOOK

**Hash:** `0x43865688AE10F0D7` | **JHash:** `0x429AEAB3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FACEBOOK_CAN_POST_TO_FACEBOOK(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FACEBOOK_CAN_POST_TO_FACEBOOK);
```

**返回值:** `BOOL` → `bool`

---

### TEXTURE_DOWNLOAD_REQUEST

**Hash:** `0x16160DA74A8E74A2` | **JHash:** `0xAD546CC3` | **Build:** `323`

**C++ 原型:**
```cpp
int TEXTURE_DOWNLOAD_REQUEST(Any* gamerHandle, const char* filePath, const char* name, BOOL p3)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TEXTURE_DOWNLOAD_REQUEST, gamerHandle, filePath, name, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |
| `filePath` | `const char*` | `string` | |
| `name` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### TITLE_TEXTURE_DOWNLOAD_REQUEST

**Hash:** `0x0B203B4AFDE53A4F` | **JHash:** `0x1856D008` | **Build:** `323`

**C++ 原型:**
```cpp
int TITLE_TEXTURE_DOWNLOAD_REQUEST(const char* filePath, const char* name, BOOL p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TITLE_TEXTURE_DOWNLOAD_REQUEST, filePath, name, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `filePath` | `const char*` | `string` | |
| `name` | `const char*` | `string` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### UGC_TEXTURE_DOWNLOAD_REQUEST

**Hash:** `0x308F96458B7087CC` | **JHash:** `0x68C9AF69` | **Build:** `323`

**C++ 原型:**
```cpp
int UGC_TEXTURE_DOWNLOAD_REQUEST(const char* p0, int p1, int p2, int p3, const char* p4, BOOL p5)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.UGC_TEXTURE_DOWNLOAD_REQUEST, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `const char*` | `string` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### TEXTURE_DOWNLOAD_RELEASE

**Hash:** `0x487EB90B98E9FB19` | **JHash:** `0xEE8D9E70` | **Build:** `323`

**C++ 原型:**
```cpp
void TEXTURE_DOWNLOAD_RELEASE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TEXTURE_DOWNLOAD_RELEASE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TEXTURE_DOWNLOAD_HAS_FAILED

**Hash:** `0x5776ED562C134687` | **JHash:** `0xE4547765` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TEXTURE_DOWNLOAD_HAS_FAILED(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TEXTURE_DOWNLOAD_HAS_FAILED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### TEXTURE_DOWNLOAD_GET_NAME

**Hash:** `0x3448505B6E35262D` | **JHash:** `0xA40EF65A` | **Build:** `323`

**C++ 原型:**
```cpp
const char* TEXTURE_DOWNLOAD_GET_NAME(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.TEXTURE_DOWNLOAD_GET_NAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_STATUS_OF_TEXTURE_DOWNLOAD

**Hash:** `0x8BD6C6DEA20E82C6` | **JHash:** `0x03225BA3` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATUS_OF_TEXTURE_DOWNLOAD(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATUS_OF_TEXTURE_DOWNLOAD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** 0 = succeeded
1 = pending
2 = failed

---

### NETWORK_CHECK_ROS_LINK_WENTDOWN_NOT_NET

**Hash:** `0x60EDD13EB3AC1FF3` | **JHash:** `0x4DEBC227` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_CHECK_ROS_LINK_WENTDOWN_NOT_NET(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_CHECK_ROS_LINK_WENTDOWN_NOT_NET);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if profile setting 901 is set to true and sets it to false.

---

### NETWORK_SHOULD_SHOW_STRICT_NAT_WARNING

**Hash:** `0x82A2B386716608F1` | **Build:** `393`

**C++ 原型:**
```cpp
BOOL NETWORK_SHOULD_SHOW_STRICT_NAT_WARNING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SHOULD_SHOW_STRICT_NAT_WARNING);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the NAT type is Strict (3) and a certain number of connections have failed.

---

### NETWORK_IS_CABLE_CONNECTED

**Hash:** `0xEFFB25453D8600F9` | **JHash:** `0x5C065D55` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CABLE_CONNECTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CABLE_CONNECTED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_SCS_PRIVATE_MSG_PRIV

**Hash:** `0x66B59CFFD78467AF` | **JHash:** `0x0CA1167F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_SCS_PRIVATE_MSG_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_SCS_PRIVATE_MSG_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ROS_SOCIAL_CLUB_PRIV

**Hash:** `0x606E4D3E3CCCF3EB` | **JHash:** `0x424C6E27` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ROS_SOCIAL_CLUB_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ROS_SOCIAL_CLUB_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ROS_BANNED_PRIV

**Hash:** `0x8020A73847E0CA7D` | **JHash:** `0xD3BBE42F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ROS_BANNED_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ROS_BANNED_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ROS_CREATE_TICKET_PRIV

**Hash:** `0xA0AD7E2AF5349F61` | **JHash:** `0xBDBB5948` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ROS_CREATE_TICKET_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ROS_CREATE_TICKET_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ROS_MULTIPLAYER_PRIV

**Hash:** `0x5F91D5D0B36AA310` | **JHash:** `0x97287D68` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ROS_MULTIPLAYER_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ROS_MULTIPLAYER_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAVE_ROS_LEADERBOARD_WRITE_PRIV

**Hash:** `0x422D396F80A96547` | **JHash:** `0xC6EA802E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAVE_ROS_LEADERBOARD_WRITE_PRIV(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAVE_ROS_LEADERBOARD_WRITE_PRIV);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_ROS_PRIVILEGE

**Hash:** `0xA699957E60D80214` | **JHash:** `0xFD261E30` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_ROS_PRIVILEGE(int index)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_ROS_PRIVILEGE, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** index is always 18 in scripts

---

### NETWORK_HAS_ROS_PRIVILEGE_END_DATE

**Hash:** `0xC22912B1D85F26B1` | **JHash:** `0x8570DD34` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_ROS_PRIVILEGE_END_DATE(int privilege, int* banType, Any* timeData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_ROS_PRIVILEGE_END_DATE, privilege, banType, timeData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `privilege` | `int` | `int` | |
| `banType` | `int*` | `int*` | |
| `timeData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_ROS_PRIVILEGE_PLAYED_LAST_GEN

**Hash:** `0x593570C289A77688` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_ROS_PRIVILEGE_PLAYED_LAST_GEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_ROS_PRIVILEGE_PLAYED_LAST_GEN);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_HAS_ROS_PRIVILEGE_SPECIAL_EDITION_CONTENT

**Hash:** `0x91B87C55093DE351` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_ROS_PRIVILEGE_SPECIAL_EDITION_CONTENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_ROS_PRIVILEGE_SPECIAL_EDITION_CONTENT);
```

**返回值:** `BOOL` → `bool`

---

### _NETWORK_HAS_ROS_PRIVILEGE_MP_TEXT_COMMUNICATION

**Hash:** `0xD9719341663C385F` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_HAS_ROS_PRIVILEGE_MP_TEXT_COMMUNICATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_HAS_ROS_PRIVILEGE_MP_TEXT_COMMUNICATION);
```

**返回值:** `BOOL` → `bool`

**说明:** Checks for privilege 29

---

### _NETWORK_HAS_ROS_PRIVILEGE_MP_VOICE_COMMUNICATION

**Hash:** `0x8956A309BE90057C` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _NETWORK_HAS_ROS_PRIVILEGE_MP_VOICE_COMMUNICATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_HAS_ROS_PRIVILEGE_MP_VOICE_COMMUNICATION);
```

**返回值:** `BOOL` → `bool`

**说明:** Checks for privilege 30

---

### _NETWORK_HAS_ROS_PRIVILEGE_REPORTING

**Hash:** `0x9BA54B3CFB82ADDD` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL _NETWORK_HAS_ROS_PRIVILEGE_REPORTING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._NETWORK_HAS_ROS_PRIVILEGE_REPORTING);
```

**返回值:** `BOOL` → `bool`

**说明:** Checks for privilege 31

---

### NETWORK_START_USER_CONTENT_PERMISSIONS_CHECK

**Hash:** `0xDEB2B99A1AF1A2A6` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_START_USER_CONTENT_PERMISSIONS_CHECK(Any* netHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_START_USER_CONTENT_PERMISSIONS_CHECK, netHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `netHandle` | `Any*` | `object*` | |

**返回值:** `int` → `int`

**说明:** Always returns -1. Seems to be XB1 specific.

---

### NETWORK_SKIP_RADIO_RESET_NEXT_CLOSE

**Hash:** `0x9465E683B12D3F6B` | **JHash:** `0x273C6180` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SKIP_RADIO_RESET_NEXT_CLOSE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SKIP_RADIO_RESET_NEXT_CLOSE);
```

**返回值:** `void` → `void`

---

### NETWORK_SKIP_RADIO_RESET_NEXT_OPEN

**Hash:** `0xCA59CCAE5D01E4CE` | **Build:** `1734`

**C++ 原型:**
```cpp
void NETWORK_SKIP_RADIO_RESET_NEXT_OPEN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SKIP_RADIO_RESET_NEXT_OPEN);
```

**返回值:** `void` → `void`

---

### NETWORK_SKIP_RADIO_WARNING

**Hash:** `0x659CF2EF7F550C4F` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL NETWORK_SKIP_RADIO_WARNING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_SKIP_RADIO_WARNING);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if dinput8.dll is present in the game directory.
You will get following error message if that is true: "You are attempting to access GTA Online servers with an altered version of the game."

---

### NETWORK_FORCE_LOCAL_PLAYER_SCAR_SYNC

**Hash:** `0xB7C7F6AD6424304B` | **JHash:** `0x371BBA08` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_FORCE_LOCAL_PLAYER_SCAR_SYNC(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_FORCE_LOCAL_PLAYER_SCAR_SYNC);
```

**返回值:** `void` → `void`

**说明:** NETWORK_F[I-O]

---

### NETWORK_DISABLE_LEAVE_REMOTE_PED_BEHIND

**Hash:** `0xC505036A35AFD01B` | **JHash:** `0xA100CC97` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DISABLE_LEAVE_REMOTE_PED_BEHIND(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DISABLE_LEAVE_REMOTE_PED_BEHIND, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_ALLOW_REMOTE_ATTACHMENT_MODIFICATION

**Hash:** `0x267C78C60E806B9A` | **JHash:** `0xBB2D33D3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ALLOW_REMOTE_ATTACHMENT_MODIFICATION(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ALLOW_REMOTE_ATTACHMENT_MODIFICATION, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_SHOW_CHAT_RESTRICTION_MSC

**Hash:** `0x6BFF5F84102DF80A` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SHOW_CHAT_RESTRICTION_MSC(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SHOW_CHAT_RESTRICTION_MSC, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### NETWORK_SHOW_PSN_UGC_RESTRICTION

**Hash:** `0x5C497525F803486B` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_SHOW_PSN_UGC_RESTRICTION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_SHOW_PSN_UGC_RESTRICTION);
```

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### NETWORK_IS_TITLE_UPDATE_REQUIRED

**Hash:** `0x6FB7BB3607D27FA2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_TITLE_UPDATE_REQUIRED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_TITLE_UPDATE_REQUIRED);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### NETWORK_QUIT_MP_TO_DESKTOP

**Hash:** `0x45A83257ED02D9BC` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_QUIT_MP_TO_DESKTOP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_QUIT_MP_TO_DESKTOP);
```

**返回值:** `void` → `void`

---

### NETWORK_IS_CONNECTED_VIA_RELAY

**Hash:** `0x16D3D49902F697BB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_IS_CONNECTED_VIA_RELAY(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_IS_CONNECTED_VIA_RELAY, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_GET_AVERAGE_LATENCY

**Hash:** `0xD414BE129BB81B32` | **Build:** `323`

**C++ 原型:**
```cpp
float NETWORK_GET_AVERAGE_LATENCY(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.NETWORK_GET_AVERAGE_LATENCY, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### NETWORK_GET_AVERAGE_PING

**Hash:** `0x0E3A041ED6AC2B45` | **Build:** `323`

**C++ 原型:**
```cpp
float NETWORK_GET_AVERAGE_PING(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.NETWORK_GET_AVERAGE_PING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

**说明:** Same as NETWORK_GET_AVERAGE_LATENCY

---

### NETWORK_GET_AVERAGE_PACKET_LOSS

**Hash:** `0x350C23949E43686C` | **Build:** `323`

**C++ 原型:**
```cpp
float NETWORK_GET_AVERAGE_PACKET_LOSS(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.NETWORK_GET_AVERAGE_PACKET_LOSS, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### NETWORK_GET_NUM_UNACKED_RELIABLES

**Hash:** `0xFF8FCF9FFC458A1C` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_NUM_UNACKED_RELIABLES(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_NUM_UNACKED_RELIABLES, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_UNRELIABLE_RESEND_COUNT

**Hash:** `0x3765C3A3E8192E10` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_UNRELIABLE_RESEND_COUNT(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_UNRELIABLE_RESEND_COUNT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_GET_HIGHEST_RELIABLE_RESEND_COUNT

**Hash:** `0x52C1EADAF7B10302` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_GET_HIGHEST_RELIABLE_RESEND_COUNT(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_HIGHEST_RELIABLE_RESEND_COUNT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_REPORT_CODE_TAMPER

**Hash:** `0x5626D9D6810730D5` | **Build:** `350`

**C++ 原型:**
```cpp
void NETWORK_REPORT_CODE_TAMPER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_REPORT_CODE_TAMPER);
```

**返回值:** `void` → `void`

---

### _NETWORK_GET_DUMP_OF_ASSET_VERIFIER

**Hash:** `0x69D05E1EAD74B9DD` | **Build:** `3717`

**C++ 原型:**
```cpp
void _NETWORK_GET_DUMP_OF_ASSET_VERIFIER(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_GET_DUMP_OF_ASSET_VERIFIER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### NETWORK_GET_LAST_ENTITY_POS_RECEIVED_OVER_NETWORK

**Hash:** `0x64D779659BC37B19` | **Build:** `393`

**C++ 原型:**
```cpp
Vector3 NETWORK_GET_LAST_ENTITY_POS_RECEIVED_OVER_NETWORK(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.NETWORK_GET_LAST_ENTITY_POS_RECEIVED_OVER_NETWORK, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### NETWORK_GET_LAST_PLAYER_POS_RECEIVED_OVER_NETWORK

**Hash:** `0x125E6D638B8605D4` | **Build:** `393`

**C++ 原型:**
```cpp
Vector3 NETWORK_GET_LAST_PLAYER_POS_RECEIVED_OVER_NETWORK(Player player)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.NETWORK_GET_LAST_PLAYER_POS_RECEIVED_OVER_NETWORK, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Returns the coordinates of another player.
Does not work if you enter your own player id as p0 (will return `(0.0, 0.0, 0.0)` in that case).

---

### NETWORK_GET_LAST_VEL_RECEIVED_OVER_NETWORK

**Hash:** `0x33DE49EDF4DDE77A` | **Build:** `1103`

**C++ 原型:**
```cpp
Vector3 NETWORK_GET_LAST_VEL_RECEIVED_OVER_NETWORK(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.NETWORK_GET_LAST_VEL_RECEIVED_OVER_NETWORK, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Used by NetBlender

---

### NETWORK_GET_PREDICTED_VELOCITY

**Hash:** `0xAA5FAFCD2C5F5E47` | **Build:** `1103`

**C++ 原型:**
```cpp
Vector3 NETWORK_GET_PREDICTED_VELOCITY(Entity entity, float maxSpeedToPredict)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.NETWORK_GET_PREDICTED_VELOCITY, entity, maxSpeedToPredict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `maxSpeedToPredict` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### NETWORK_DUMP_NET_IF_CONFIG

**Hash:** `0xAEDF1BC1C133D6E3` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_DUMP_NET_IF_CONFIG(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_DUMP_NET_IF_CONFIG);
```

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### NETWORK_GET_SIGNALLING_INFO

**Hash:** `0x2555CF7DA5473794` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_SIGNALLING_INFO(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_SIGNALLING_INFO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### NETWORK_GET_NET_STATISTICS_INFO

**Hash:** `0x6FD992C4A1C1B986` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_GET_NET_STATISTICS_INFO(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_GET_NET_STATISTICS_INFO, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### NETWORK_GET_PLAYER_ACCOUNT_ID

**Hash:** `0xDB663CC9FF3407A9` | **Build:** `1734`

**C++ 原型:**
```cpp
int NETWORK_GET_PLAYER_ACCOUNT_ID(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_GET_PLAYER_ACCOUNT_ID, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### NETWORK_HAS_SC_MEMBERSHIP

**Hash:** `0xAB286B2192A71E32` | **Build:** `3717`

**C++ 原型:**
```cpp
BOOL NETWORK_HAS_SC_MEMBERSHIP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_HAS_SC_MEMBERSHIP);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_UGC_NAV

**Hash:** `0xC1447451DDB512F0` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_UGC_NAV(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_UGC_NAV, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _NETWORK_GET_GAME_RESTART_REASON

**Hash:** `0x7F7E8401F81CB65B` | **Build:** `3323`

**C++ 原型:**
```cpp
int _NETWORK_GET_GAME_RESTART_REASON(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._NETWORK_GET_GAME_RESTART_REASON);
```

**返回值:** `int` → `int`

**说明:** enum eNetworkGameRestartReason
{
	BE_RESTART_SERVICE_NOT_RUNNING = 1, 	// BattlEye service not running
	BE_RESTART_SERVICE_NEEDS_UPDATE, 		// BattlEye service needs an update
	BE_RESTART_UNKNOWN_REASON,				// Unknown BattlEye reason
	BE_RESTART_EF_DISABLED_WITH_BE,			// BattlEye Enforcement Flag is disabled, but BattlEye is active
	BE_RESTART_EF_ENABLED_NO_BE,			// BattlEye Enforcement Flag is enabled, but BattlEye is not active
	BE_RESTART_BE_MODULES_FAILED_TO_LOAD,	// BattlEye modules failed to load
	NUM_NETWORK_GAME_RESTART_REASONS
};

---

### _NETWORK_CONFIRM_GAME_RESTART

**Hash:** `0x0A141818CA2311AD` | **Build:** `3323`

**C++ 原型:**
```cpp
void _NETWORK_CONFIRM_GAME_RESTART(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._NETWORK_CONFIRM_GAME_RESTART);
```

**返回值:** `void` → `void`

**说明:** If the restart reason is 4 (BE_RESTART_EF_DISABLED_WITH_BE), will cause the game to exit with exit code 622011. Otherwise exits with code 622010. Has no effect when a restart hasn't been requested (reason == 0 or reason > 6).

Note that this 'confirmation' itself does not restart the game, a call to QUIT_GAME or similar is required afterwards.

---

### _NETWORK_GET_GAME_RESTART_REASON_MESSAGE_LABEL

**Hash:** `0x47B11D51FC50A259` | **Build:** `3323`

**C++ 原型:**
```cpp
const char* _NETWORK_GET_GAME_RESTART_REASON_MESSAGE_LABEL(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_GET_GAME_RESTART_REASON_MESSAGE_LABEL);
```

**返回值:** `const char*` → `string`

**说明:** Returns the restart reason as a string, see _NETWORK_GET_GAME_RESTART_REASON. Returns a null pointer when a restart hasn't been requested (reason == 0 or reason > 6).

---

### _NETWORK_GET_BATTLEYE_ERROR_MESSAGE_LABEL

**Hash:** `0xCBA50F371E45B90D` | **Build:** `3323`

**C++ 原型:**
```cpp
const char* _NETWORK_GET_BATTLEYE_ERROR_MESSAGE_LABEL(int errorCode)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash._NETWORK_GET_BATTLEYE_ERROR_MESSAGE_LABEL, errorCode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `errorCode` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** 0x20211000 = HUD_BE_ERROR_FAILED_UNKNOWN
0x20212000 = HUD_BE_ERROR_FAILED_CLIENT_INIT
0x20213000 = HUD_BE_ERROR_FAILED_SERVER_INIT
0x20214000 = HUD_BE_ERROR_FAILED_AUTH_ATTEMPTS
0x20215000 = HUD_BE_ERROR_UNTRUSTED_HOST
0x20216000 = HUD_BE_ERROR_KICKED
0x20217000 = HUD_BE_ERROR_BANNED
0x50140000 = HUD_BE_BLOCK_BANNED

Note: an invalid error code returns a null pointer!

---


---

## OBJECT

**包含 166 个 Native 函数**

### CREATE_OBJECT

**Hash:** `0x509D5878EB39E842` | **JHash:** `0x2F7AA05C` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_OBJECT(Hash modelHash, float x, float y, float z, BOOL isNetwork, BOOL bScriptHostObj, BOOL dynamic)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_OBJECT, modelHash, x, y, z, isNetwork, bScriptHostObj, dynamic);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostObj` | `BOOL` | `bool` | |
| `dynamic` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

**说明:** List of object models that can be created without any additional effort like making sure ytyp is loaded etc: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ObjectList.ini

---

### CREATE_OBJECT_NO_OFFSET

**Hash:** `0x9A294B2138ABB884` | **JHash:** `0x58040420` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_OBJECT_NO_OFFSET(Hash modelHash, float x, float y, float z, BOOL isNetwork, BOOL bScriptHostObj, BOOL dynamic, Any p7)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_OBJECT_NO_OFFSET, modelHash, x, y, z, isNetwork, bScriptHostObj, dynamic, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostObj` | `BOOL` | `bool` | |
| `dynamic` | `BOOL` | `bool` | |
| `p7` | `Any` | `object` | |

**返回值:** `Object` → `int`

**说明:** List of object models that can be created without any additional effort like making sure ytyp is loaded etc: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ObjectList.ini

---

### DELETE_OBJECT

**Hash:** `0x539E0AE3E6634B9F` | **JHash:** `0xD6EF9DA7` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_OBJECT(Object* object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_OBJECT, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Deletes the specified object, then sets the handle pointed to by the pointer to NULL.

---

### PLACE_OBJECT_ON_GROUND_PROPERLY

**Hash:** `0x58A850EAEE20FAA3` | **JHash:** `0x8F95A20B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLACE_OBJECT_ON_GROUND_PROPERLY(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLACE_OBJECT_ON_GROUND_PROPERLY, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLACE_OBJECT_ON_GROUND_OR_OBJECT_PROPERLY

**Hash:** `0xD76EEEF746057FD6` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL PLACE_OBJECT_ON_GROUND_OR_OBJECT_PROPERLY(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLACE_OBJECT_ON_GROUND_OR_OBJECT_PROPERLY, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ROTATE_OBJECT

**Hash:** `0xAFE24E4D29249E4A` | **Build:** `1734`

**C++ 原型:**
```cpp
BOOL ROTATE_OBJECT(Object object, float p1, float p2, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ROTATE_OBJECT, object, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SLIDE_OBJECT

**Hash:** `0x2FDFF4107B8C1147` | **JHash:** `0x63BFA7A0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SLIDE_OBJECT(Object object, float toX, float toY, float toZ, float speedX, float speedY, float speedZ, BOOL collision)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SLIDE_OBJECT, object, toX, toY, toZ, speedX, speedY, speedZ, collision);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toX` | `float` | `float` | |
| `toY` | `float` | `float` | |
| `toZ` | `float` | `float` | |
| `speedX` | `float` | `float` | |
| `speedY` | `float` | `float` | |
| `speedZ` | `float` | `float` | |
| `collision` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the object has finished moving.

If false, moves the object towards the specified X, Y and Z coordinates with the specified X, Y and Z speed.

See also: https://gtagmodding.com/opcode-database/opcode/034E/
Has to be looped until it returns true.

---

### SET_OBJECT_TARGETTABLE

**Hash:** `0x8A7391690F5AFD81` | **JHash:** `0x3F88CD86` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_TARGETTABLE(Object object, BOOL targettable, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_TARGETTABLE, object, targettable, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `targettable` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _SET_OBJECT_TARGETTABLE_BY_PLAYER

**Hash:** `0xB39F03368DB0CAA2` | **Build:** `3258`

**C++ 原型:**
```cpp
void _SET_OBJECT_TARGETTABLE_BY_PLAYER(Object object, BOOL setFlag34, BOOL setFlag35)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_OBJECT_TARGETTABLE_BY_PLAYER, object, setFlag34, setFlag35);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `setFlag34` | `BOOL` | `bool` | |
| `setFlag35` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the 34th and 35th object flags related to player peds.

---

### SET_OBJECT_FORCE_VEHICLES_TO_AVOID

**Hash:** `0x77F33F2CCF64B3AA` | **JHash:** `0x483C5C88` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_FORCE_VEHICLES_TO_AVOID(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_FORCE_VEHICLES_TO_AVOID, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Overrides a flag on the object which determines if the object should be avoided by a vehicle in task CTaskVehicleGoToPointWithAvoidanceAutomobile.

---

### GET_CLOSEST_OBJECT_OF_TYPE

**Hash:** `0xE143FA2249364369` | **JHash:** `0x45619B33` | **Build:** `323`

**C++ 原型:**
```cpp
Object GET_CLOSEST_OBJECT_OF_TYPE(float x, float y, float z, float radius, Hash modelHash, BOOL isMission, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOSEST_OBJECT_OF_TYPE, x, y, z, radius, modelHash, isMission, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `isMission` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

**说明:** Has 8 params in the latest patches.

isMission - if true doesn't return mission objects

---

### HAS_OBJECT_BEEN_BROKEN

**Hash:** `0x8ABFB70C49CC43E2` | **JHash:** `0xFE21F891` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_OBJECT_BEEN_BROKEN(Object object, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_OBJECT_BEEN_BROKEN, object, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_BROKEN

**Hash:** `0x761B0E69AC4D007E` | **JHash:** `0x6FC0353D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_BROKEN(float p0, float p1, float p2, float p3, Hash modelHash, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_BROKEN, p0, p1, p2, p3, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_COMPLETELY_DESTROYED

**Hash:** `0x46494A2475701343` | **JHash:** `0x7DB578DD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_COMPLETELY_DESTROYED(float x, float y, float z, float radius, Hash modelHash, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CLOSEST_OBJECT_OF_TYPE_BEEN_COMPLETELY_DESTROYED, x, y, z, radius, modelHash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_HAS_OBJECT_BEEN_COMPLETELY_DESTROYED

**Hash:** `0x2542269291C6AC84` | **Build:** `1180`

**C++ 原型:**
```cpp
BOOL GET_HAS_OBJECT_BEEN_COMPLETELY_DESTROYED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_HAS_OBJECT_BEEN_COMPLETELY_DESTROYED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_OFFSET_FROM_COORD_AND_HEADING_IN_WORLD_COORDS

**Hash:** `0x163E252DE035A133` | **JHash:** `0x87A42A12` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_OFFSET_FROM_COORD_AND_HEADING_IN_WORLD_COORDS(float xPos, float yPos, float zPos, float heading, float xOffset, float yOffset, float zOffset)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_OFFSET_FROM_COORD_AND_HEADING_IN_WORLD_COORDS, xPos, yPos, zPos, heading, xOffset, yOffset, zOffset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_COORDS_AND_ROTATION_OF_CLOSEST_OBJECT_OF_TYPE

**Hash:** `0x163F8B586BC95F2A` | **JHash:** `0x65213FC3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_COORDS_AND_ROTATION_OF_CLOSEST_OBJECT_OF_TYPE(float x, float y, float z, float radius, Hash modelHash, Vector3* outPosition, Vector3* outRotation, int rotationOrder)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_COORDS_AND_ROTATION_OF_CLOSEST_OBJECT_OF_TYPE, x, y, z, radius, modelHash, outPosition, outRotation, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `outRotation` | `Vector3*` | `Vector3*` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_STATE_OF_CLOSEST_DOOR_OF_TYPE

**Hash:** `0xF82D8F1926A02C3D` | **JHash:** `0x38C951A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STATE_OF_CLOSEST_DOOR_OF_TYPE(Hash type, float x, float y, float z, BOOL locked, float heading, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STATE_OF_CLOSEST_DOOR_OF_TYPE, type, x, y, z, locked, heading, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `locked` | `BOOL` | `bool` | |
| `heading` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Hardcoded to not work in multiplayer.


Used to lock/unlock doors to interior areas of the game.

(Possible) Door Types:

https://pastebin.com/9S2m3qA4

Heading is either 1, 0 or -1 in the scripts. Means default closed(0) or opened either into(1) or out(-1) of the interior.
Locked means that the heading is locked.  
p6 is always 0. 

225 door types, model names and coords found in stripclub.c4:
https://pastebin.com/gywnbzsH

get door info: https://pastebin.com/i14rbekD

---

### GET_STATE_OF_CLOSEST_DOOR_OF_TYPE

**Hash:** `0xEDC1A5B84AEF33FF` | **JHash:** `0x4B44A83D` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_STATE_OF_CLOSEST_DOOR_OF_TYPE(Hash type, float x, float y, float z, BOOL* locked, float* heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_STATE_OF_CLOSEST_DOOR_OF_TYPE, type, x, y, z, locked, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `locked` | `BOOL*` | `bool*` | |
| `heading` | `float*` | `float*` | |

**返回值:** `void` → `void`

**说明:** locked is 0 if no door is found
locked is 0 if door is unlocked
locked is 1 if door is found and unlocked.

-------------
the locked bool is either 0(unlocked)(false) or 1(locked)(true)

---

### SET_LOCKED_UNSTREAMED_IN_DOOR_OF_TYPE

**Hash:** `0x9B12F9A24FABEDB0` | **JHash:** `0x4E0A260B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCKED_UNSTREAMED_IN_DOOR_OF_TYPE(Hash modelHash, float x, float y, float z, BOOL locked, float xRotMult, float yRotMult, float zRotMult)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCKED_UNSTREAMED_IN_DOOR_OF_TYPE, modelHash, x, y, z, locked, xRotMult, yRotMult, zRotMult);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `locked` | `BOOL` | `bool` | |
| `xRotMult` | `float` | `float` | |
| `yRotMult` | `float` | `float` | |
| `zRotMult` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Hardcoded not to work in multiplayer environments.
When you set locked to 0 the door open and to 1 the door close
OBJECT::SET_LOCKED_UNSTREAMED_IN_DOOR_OF_TYPE(${prop_gate_prison_01}, 1845.0, 2605.0, 45.0, 0, 0.0, 50.0, 0);  //door open

OBJECT::SET_LOCKED_UNSTREAMED_IN_DOOR_OF_TYPE(${prop_gate_prison_01}, 1845.0, 2605.0, 45.0, 1, 0.0, 50.0, 0);  //door close

---

### PLAY_OBJECT_AUTO_START_ANIM

**Hash:** `0x006E4B040ED37EC3` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAY_OBJECT_AUTO_START_ANIM(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_OBJECT_AUTO_START_ANIM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ADD_DOOR_TO_SYSTEM

**Hash:** `0x6F8838D03D1DC226` | **JHash:** `0x9D2D778D` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_DOOR_TO_SYSTEM(Hash doorHash, Hash modelHash, float x, float y, float z, BOOL p5, BOOL scriptDoor, BOOL isLocal, Any p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_DOOR_TO_SYSTEM, doorHash, modelHash, x, y, z, p5, scriptDoor, isLocal, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `scriptDoor` | `BOOL` | `bool` | |
| `isLocal` | `BOOL` | `bool` | |
| `p8` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** doorHash has to be unique. scriptDoor false; relies upon getNetworkGameScriptHandler. isLocal On true disables the creation CRequestDoorEvent's in DOOR_SYSTEM_SET_DOOR_STATE.
p5 only set to true in single player native scripts.
If scriptDoor is true, register the door on the script handler host (note: there's a hardcap on the number of script IDs that can be added to the system at a given time). If scriptDoor and isLocal are both false, the door is considered to be in a "Persists w/o netobj" state.

door hashes normally look like PROP_[int]_DOOR_[int] for interior doors and PROP_BUILDING_[int]_DOOR_[int] exterior doors but you can just make up your own hash if you want
All doors need to be registered with ADD_DOOR_TO_SYSTEM before they can be manipulated with the door natives and the easiest way to get door models is just find the door in codewalker.

Example: AddDoorToSystem("PROP_43_DOOR_0", "hei_v_ilev_fh_heistdoor2", -1456.818, -520.5037, 69.67043, 0, 0, 0)

---

### REMOVE_DOOR_FROM_SYSTEM

**Hash:** `0x464D8E1427156FE4` | **JHash:** `0x00253286` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_DOOR_FROM_SYSTEM(Hash doorHash, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_DOOR_FROM_SYSTEM, doorHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** CDoor and CDoorSystemData still internally allocated (and their associations between doorHash, modelHash, and coordinates).
Only its NetObj removed and flag ``*(v2 + 192) |= 8u`` (1604 retail) toggled.

---

### DOOR_SYSTEM_SET_DOOR_STATE

**Hash:** `0x6BAB9442830C7F53` | **JHash:** `0xDF83DB47` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_DOOR_STATE(Hash doorHash, int state, BOOL requestDoor, BOOL forceUpdate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_DOOR_STATE, doorHash, state, requestDoor, forceUpdate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `state` | `int` | `int` | |
| `requestDoor` | `BOOL` | `bool` | |
| `forceUpdate` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Lockstates not applied and CNetObjDoor's not created until DOOR_SYSTEM_GET_IS_PHYSICS_LOADED returns true.
`requestDoor` on true, and when door system is configured to, i.e., "persists w/o netobj", generate a CRequestDoorEvent.
`forceUpdate` on true, forces an update on the door system (same path as netObjDoor_applyDoorStuff)
Door lock states:
0: UNLOCKED
1: LOCKED
2: DOORSTATE_FORCE_LOCKED_UNTIL_OUT_OF_AREA
3: DOORSTATE_FORCE_UNLOCKED_THIS_FRAME
4: DOORSTATE_FORCE_LOCKED_THIS_FRAME
5: DOORSTATE_FORCE_OPEN_THIS_FRAME
6: DOORSTATE_FORCE_CLOSED_THIS_FRAME

---

### DOOR_SYSTEM_GET_DOOR_STATE

**Hash:** `0x160AA1B32F6139B8` | **JHash:** `0xD42A41C2` | **Build:** `323`

**C++ 原型:**
```cpp
int DOOR_SYSTEM_GET_DOOR_STATE(Hash doorHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DOOR_SYSTEM_GET_DOOR_STATE, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### DOOR_SYSTEM_GET_DOOR_PENDING_STATE

**Hash:** `0x4BC2854478F3A749` | **JHash:** `0xD649B7E1` | **Build:** `323`

**C++ 原型:**
```cpp
int DOOR_SYSTEM_GET_DOOR_PENDING_STATE(Hash doorHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DOOR_SYSTEM_GET_DOOR_PENDING_STATE, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### DOOR_SYSTEM_SET_AUTOMATIC_RATE

**Hash:** `0x03C27E13B42A0E82` | **JHash:** `0x4F44AF21` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_AUTOMATIC_RATE(Hash doorHash, float rate, BOOL requestDoor, BOOL forceUpdate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_AUTOMATIC_RATE, doorHash, rate, requestDoor, forceUpdate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `rate` | `float` | `float` | |
| `requestDoor` | `BOOL` | `bool` | |
| `forceUpdate` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Includes networking check: ownership vs. or the door itself **isn't** networked.
`forceUpdate` on true invokes DOOR_SYSTEM_SET_DOOR_STATE otherwise requestDoor is unused.

---

### DOOR_SYSTEM_SET_AUTOMATIC_DISTANCE

**Hash:** `0x9BA001CB45CBF627` | **JHash:** `0x47531446` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_AUTOMATIC_DISTANCE(Hash doorHash, float distance, BOOL requestDoor, BOOL forceUpdate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_AUTOMATIC_DISTANCE, doorHash, distance, requestDoor, forceUpdate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `distance` | `float` | `float` | |
| `requestDoor` | `BOOL` | `bool` | |
| `forceUpdate` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** `forceUpdate` on true invokes DOOR_SYSTEM_SET_DOOR_STATE otherwise requestDoor is unused.

---

### DOOR_SYSTEM_SET_OPEN_RATIO

**Hash:** `0xB6E6FBA95C7324AC` | **JHash:** `0x34883DE3` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_OPEN_RATIO(Hash doorHash, float ajar, BOOL requestDoor, BOOL forceUpdate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_OPEN_RATIO, doorHash, ajar, requestDoor, forceUpdate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `ajar` | `float` | `float` | |
| `requestDoor` | `BOOL` | `bool` | |
| `forceUpdate` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the ajar angle of a door.
Ranges from -1.0 to 1.0, and 0.0 is closed / default.
`forceUpdate` on true invokes DOOR_SYSTEM_SET_DOOR_STATE otherwise requestDoor is unused.

---

### DOOR_SYSTEM_GET_AUTOMATIC_DISTANCE

**Hash:** `0xE851471AEFC3374F` | **Build:** `1868`

**C++ 原型:**
```cpp
float DOOR_SYSTEM_GET_AUTOMATIC_DISTANCE(Hash doorHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.DOOR_SYSTEM_GET_AUTOMATIC_DISTANCE, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

---

### DOOR_SYSTEM_GET_OPEN_RATIO

**Hash:** `0x65499865FCA6E5EC` | **JHash:** `0xB74C3BD7` | **Build:** `323`

**C++ 原型:**
```cpp
float DOOR_SYSTEM_GET_OPEN_RATIO(Hash doorHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.DOOR_SYSTEM_GET_OPEN_RATIO, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

---

### DOOR_SYSTEM_GET_IS_SPRING_REMOVED

**Hash:** `0x8562FD8AB1E94D39` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL DOOR_SYSTEM_GET_IS_SPRING_REMOVED(Hash doorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOOR_SYSTEM_GET_IS_SPRING_REMOVED, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### DOOR_SYSTEM_SET_SPRING_REMOVED

**Hash:** `0xC485E07E4F0B7958` | **JHash:** `0xB4A9A558` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_SPRING_REMOVED(Hash doorHash, BOOL removed, BOOL requestDoor, BOOL forceUpdate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_SPRING_REMOVED, doorHash, removed, requestDoor, forceUpdate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `removed` | `BOOL` | `bool` | |
| `requestDoor` | `BOOL` | `bool` | |
| `forceUpdate` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Includes networking check: ownership vs. or the door itself **isn't** networked.
`forceUpdate` on true invokes DOOR_SYSTEM_SET_DOOR_STATE otherwise requestDoor is unused.

---

### DOOR_SYSTEM_SET_HOLD_OPEN

**Hash:** `0xD9B71952F78A2640` | **JHash:** `0xECE58AE0` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_HOLD_OPEN(Hash doorHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_HOLD_OPEN, doorHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Includes networking check: ownership vs. or the door itself **isn't** networked.

---

### DOOR_SYSTEM_SET_DOOR_OPEN_FOR_RACES

**Hash:** `0xA85A21582451E951` | **JHash:** `0xF736227C` | **Build:** `323`

**C++ 原型:**
```cpp
void DOOR_SYSTEM_SET_DOOR_OPEN_FOR_RACES(Hash doorHash, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DOOR_SYSTEM_SET_DOOR_OPEN_FOR_RACES, doorHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Some property related to gates. Native name between ``DOOR_SYSTEM_SET_AUTOMATIC_RATE`` and ``DOOR_SYSTEM_SET_DOOR_STATE``.

---

### IS_DOOR_REGISTERED_WITH_SYSTEM

**Hash:** `0xC153C43EA202C8C1` | **JHash:** `0x5AFCD8A1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DOOR_REGISTERED_WITH_SYSTEM(Hash doorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DOOR_REGISTERED_WITH_SYSTEM, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** if (OBJECT::IS_DOOR_REGISTERED_WITH_SYSTEM(doorHash)) 
{
    OBJECT::REMOVE_DOOR_FROM_SYSTEM(doorHash);
}

---

### IS_DOOR_CLOSED

**Hash:** `0xC531EE8A1145A149` | **JHash:** `0x48659CD7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DOOR_CLOSED(Hash doorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DOOR_CLOSED, doorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `doorHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### OPEN_ALL_BARRIERS_FOR_RACE

**Hash:** `0xC7F29CA00F46350E` | **JHash:** `0x9BF33E41` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_ALL_BARRIERS_FOR_RACE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_ALL_BARRIERS_FOR_RACE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLOSE_ALL_BARRIERS_FOR_RACE

**Hash:** `0x701FDA1E82076BA4` | **JHash:** `0xF592AD10` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_ALL_BARRIERS_FOR_RACE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_ALL_BARRIERS_FOR_RACE);
```

**返回值:** `void` → `void`

**说明:** Clears the fields sets by 0xC7F29CA00F46350E (1604 retail: 0x1424A7A10, 0x1424A7A11) and iterates over the global CDoor's bucket-list.
Related to its "Pre-networked state"?

---

### DOOR_SYSTEM_GET_IS_PHYSICS_LOADED

**Hash:** `0xDF97CDD4FC08FD34` | **JHash:** `0x17FF9393` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOOR_SYSTEM_GET_IS_PHYSICS_LOADED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOOR_SYSTEM_GET_IS_PHYSICS_LOADED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### DOOR_SYSTEM_FIND_EXISTING_DOOR

**Hash:** `0x589F80B325CC82C5` | **JHash:** `0xE9AE494F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOOR_SYSTEM_FIND_EXISTING_DOOR(float x, float y, float z, Hash modelHash, Hash* outDoorHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOOR_SYSTEM_FIND_EXISTING_DOOR, x, y, z, modelHash, outDoorHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `outDoorHash` | `Hash*` | `uint*` | |

**返回值:** `BOOL` → `bool`

**说明:** Search radius: 0.5

---

### IS_GARAGE_EMPTY

**Hash:** `0x90E47239EA1980B8` | **JHash:** `0xA8B37DEA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_GARAGE_EMPTY(Hash garageHash, BOOL p1, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_GARAGE_EMPTY, garageHash, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_ENTIRELY_INSIDE_GARAGE

**Hash:** `0x024A60DEB0EA69F0` | **JHash:** `0xC33ED360` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_ENTIRELY_INSIDE_GARAGE(Hash garageHash, Player player, float p2, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_ENTIRELY_INSIDE_GARAGE, garageHash, player, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `player` | `Player` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_PARTIALLY_INSIDE_GARAGE

**Hash:** `0x1761DC5D8471CBAA` | **JHash:** `0x41924877` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_PARTIALLY_INSIDE_GARAGE(Hash garageHash, Player player, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_PARTIALLY_INSIDE_GARAGE, garageHash, player, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `player` | `Player` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ARE_ENTITIES_ENTIRELY_INSIDE_GARAGE

**Hash:** `0x85B6C850546FDDE2` | **JHash:** `0x4BD59750` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ENTITIES_ENTIRELY_INSIDE_GARAGE(Hash garageHash, BOOL p1, BOOL p2, BOOL p3, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ENTITIES_ENTIRELY_INSIDE_GARAGE, garageHash, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_ANY_ENTITY_ENTIRELY_INSIDE_GARAGE

**Hash:** `0x673ED815D6E323B7` | **JHash:** `0x7B44D659` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_ENTITY_ENTIRELY_INSIDE_GARAGE(Hash garageHash, BOOL p1, BOOL p2, BOOL p3, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_ENTITY_ENTIRELY_INSIDE_GARAGE, garageHash, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_OBJECT_ENTIRELY_INSIDE_GARAGE

**Hash:** `0x372EF6699146A1E4` | **JHash:** `0x142C8F76` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_ENTIRELY_INSIDE_GARAGE(Hash garageHash, Entity entity, float p2, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_ENTIRELY_INSIDE_GARAGE, garageHash, entity, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `entity` | `Entity` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Despite the name, it does work for any entity type.

---

### IS_OBJECT_PARTIALLY_INSIDE_GARAGE

**Hash:** `0xF0EED5A6BC7B237A` | **JHash:** `0x95A9AB2B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_PARTIALLY_INSIDE_GARAGE(Hash garageHash, Entity entity, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_PARTIALLY_INSIDE_GARAGE, garageHash, entity, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `entity` | `Entity` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Despite the name, it does work for any entity type.

---

### CLEAR_GARAGE

**Hash:** `0xDA05194260CDCDF9` | **Build:** `678`

**C++ 原型:**
```cpp
void CLEAR_GARAGE(Hash garageHash, BOOL isNetwork)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GARAGE, garageHash, isNetwork);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `isNetwork` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_OBJECTS_INSIDE_GARAGE

**Hash:** `0x190428512B240692` | **JHash:** `0xA565E27E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_OBJECTS_INSIDE_GARAGE(Hash garageHash, BOOL vehicles, BOOL peds, BOOL objects, BOOL isNetwork)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_OBJECTS_INSIDE_GARAGE, garageHash, vehicles, peds, objects, isNetwork);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `vehicles` | `BOOL` | `bool` | |
| `peds` | `BOOL` | `bool` | |
| `objects` | `BOOL` | `bool` | |
| `isNetwork` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_TIDYING_UP_IN_GARAGE

**Hash:** `0x659F9D71F52843F8` | **Build:** `1290`

**C++ 原型:**
```cpp
void DISABLE_TIDYING_UP_IN_GARAGE(int id, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_TIDYING_UP_IN_GARAGE, id, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a flag. A valid id is 0x157DC10D

---

### ENABLE_SAVING_IN_GARAGE

**Hash:** `0xF2E1A7133DD356A6` | **JHash:** `0x43BB7E48` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_SAVING_IN_GARAGE(Hash garageHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_SAVING_IN_GARAGE, garageHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLOSE_SAFEHOUSE_GARAGES

**Hash:** `0x66A49D021870FE88` | **JHash:** `0x6158959E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_SAFEHOUSE_GARAGES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_SAFEHOUSE_GARAGES);
```

**返回值:** `void` → `void`

---

### DOES_OBJECT_OF_TYPE_EXIST_AT_COORDS

**Hash:** `0xBFA48E2FF417213F` | **JHash:** `0x23FF2BA4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_OBJECT_OF_TYPE_EXIST_AT_COORDS(float x, float y, float z, float radius, Hash hash, BOOL p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_OBJECT_OF_TYPE_EXIST_AT_COORDS, x, y, z, radius, hash, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `hash` | `Hash` | `uint` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p5 is usually 0.

---

### IS_POINT_IN_ANGLED_AREA

**Hash:** `0x2A70BAE8883E4C81` | **JHash:** `0x73BCFFDC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_POINT_IN_ANGLED_AREA(float xPos, float yPos, float zPos, float x1, float y1, float z1, float x2, float y2, float z2, float width, BOOL debug, BOOL includeZ)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_POINT_IN_ANGLED_AREA, xPos, yPos, zPos, x1, y1, z1, x2, y2, z2, width, debug, includeZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xPos` | `float` | `float` | |
| `yPos` | `float` | `float` | |
| `zPos` | `float` | `float` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `debug` | `BOOL` | `bool` | |
| `includeZ` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** An angled area is an X-Z oriented rectangle with three parameters:
1. origin: the mid-point along a base edge of the rectangle;
2. extent: the mid-point of opposite base edge on the other Z;
3. width: the length of the base edge; (named derived from logging strings ``CNetworkRoadNodeWorldStateData``).

The oriented rectangle can then be derived from the direction of the two points (``norm(origin - extent)``), its orthonormal, and the width, e.g:
1. golf_mp https://i.imgur.com/JhsQAK9.png
2. am_taxi https://i.imgur.com/TJWCZaT.jpg

---

### SET_OBJECT_ALLOW_LOW_LOD_BUOYANCY

**Hash:** `0x4D89D607CB3DD1D2` | **JHash:** `0x19B17769` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_ALLOW_LOW_LOD_BUOYANCY(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_ALLOW_LOW_LOD_BUOYANCY, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Overrides the climbing/blocking flags of the object, used in the native scripts mostly for "prop_dock_bouy_*"

---

### SET_OBJECT_PHYSICS_PARAMS

**Hash:** `0xF6DF6E90DE7DF90F` | **JHash:** `0xE8D11C58` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_PHYSICS_PARAMS(Object object, float weight, float p2, float p3, float p4, float p5, float gravity, float p7, float p8, float p9, float p10, float buoyancy)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_PHYSICS_PARAMS, object, weight, p2, p3, p4, p5, gravity, p7, p8, p9, p10, buoyancy);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `weight` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `gravity` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `float` | `float` | |
| `buoyancy` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Adjust the physics parameters of a prop, or otherwise known as "object". This is useful for simulated gravity.

Other parameters seem to be unknown.

p2: seems to be weight and gravity related. Higher value makes the obj fall faster. Very sensitive?
p3: seems similar to p2
p4: makes obj fall slower the higher the value
p5: similar to p4

---

### GET_OBJECT_FRAGMENT_DAMAGE_HEALTH

**Hash:** `0xB6FBFD079B8D0596` | **JHash:** `0xF0B330AD` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_OBJECT_FRAGMENT_DAMAGE_HEALTH(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_OBJECT_FRAGMENT_DAMAGE_HEALTH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `float` → `float`

---

### SET_ACTIVATE_OBJECT_PHYSICS_AS_SOON_AS_IT_IS_UNFROZEN

**Hash:** `0x406137F8EF90EAF5` | **JHash:** `0x3E263AE1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ACTIVATE_OBJECT_PHYSICS_AS_SOON_AS_IT_IS_UNFROZEN(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ACTIVATE_OBJECT_PHYSICS_AS_SOON_AS_IT_IS_UNFROZEN, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ANY_OBJECT_NEAR_POINT

**Hash:** `0x397DC58FF00298D1` | **JHash:** `0xE9E46941` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_OBJECT_NEAR_POINT(float x, float y, float z, float range, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_OBJECT_NEAR_POINT, x, y, z, range, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_OBJECT_NEAR_POINT

**Hash:** `0x8C90FE4B381BA60A` | **JHash:** `0x50A62C43` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_NEAR_POINT(Hash objectHash, float x, float y, float z, float range)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_NEAR_POINT, objectHash, x, y, z, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `objectHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_OBJECT_HIGH_DETAIL_MODEL

**Hash:** `0x4A39DB43E47CF3AA` | **JHash:** `0xE3261B35` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_OBJECT_HIGH_DETAIL_MODEL(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_OBJECT_HIGH_DETAIL_MODEL, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### BREAK_OBJECT_FRAGMENT_CHILD

**Hash:** `0xE7E4C198B0185900` | **JHash:** `0x1E82C2AE` | **Build:** `323`

**C++ 原型:**
```cpp
void BREAK_OBJECT_FRAGMENT_CHILD(Object p0, Any p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BREAK_OBJECT_FRAGMENT_CHILD, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Object` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DAMAGE_OBJECT_FRAGMENT_CHILD

**Hash:** `0xE05F6AEEFEB0BB02` | **Build:** `1180`

**C++ 原型:**
```cpp
void DAMAGE_OBJECT_FRAGMENT_CHILD(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DAMAGE_OBJECT_FRAGMENT_CHILD, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### FIX_OBJECT_FRAGMENT

**Hash:** `0xF9C1681347C8BD15` | **Build:** `323`

**C++ 原型:**
```cpp
void FIX_OBJECT_FRAGMENT(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FIX_OBJECT_FRAGMENT, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### TRACK_OBJECT_VISIBILITY

**Hash:** `0xB252BC036B525623` | **JHash:** `0x46D06B9A` | **Build:** `323`

**C++ 原型:**
```cpp
void TRACK_OBJECT_VISIBILITY(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRACK_OBJECT_VISIBILITY, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### IS_OBJECT_VISIBLE

**Hash:** `0x8B32ACE6326A7546` | **JHash:** `0xF4FD8AE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_VISIBLE(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_VISIBLE, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_OBJECT_IS_SPECIAL_GOLFBALL

**Hash:** `0xC6033D32241F6FB5` | **JHash:** `0xF4A1A14A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_IS_SPECIAL_GOLFBALL(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_IS_SPECIAL_GOLFBALL, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_OBJECT_TAKES_DAMAGE_FROM_COLLIDING_WITH_BUILDINGS

**Hash:** `0xEB6F1A9B5510A5D2` | **JHash:** `0xAF016CC1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_TAKES_DAMAGE_FROM_COLLIDING_WITH_BUILDINGS(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_TAKES_DAMAGE_FROM_COLLIDING_WITH_BUILDINGS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ALLOW_DAMAGE_EVENTS_FOR_NON_NETWORKED_OBJECTS

**Hash:** `0xABDABF4E1EDECBFA` | **Build:** `1365`

**C++ 原型:**
```cpp
void ALLOW_DAMAGE_EVENTS_FOR_NON_NETWORKED_OBJECTS(BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_DAMAGE_EVENTS_FOR_NON_NETWORKED_OBJECTS, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CUTSCENES_WEAPON_FLASHLIGHT_ON_THIS_FRAME

**Hash:** `0xBCE595371A5FBAAF` | **JHash:** `0x3A68AA46` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CUTSCENES_WEAPON_FLASHLIGHT_ON_THIS_FRAME(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUTSCENES_WEAPON_FLASHLIGHT_ON_THIS_FRAME, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Requires a component_at_*_flsh to be attached to the weapon object

---

### GET_RAYFIRE_MAP_OBJECT

**Hash:** `0xB48FCED898292E52` | **JHash:** `0xA286DE96` | **Build:** `323`

**C++ 原型:**
```cpp
Object GET_RAYFIRE_MAP_OBJECT(float x, float y, float z, float radius, const char* name)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RAYFIRE_MAP_OBJECT, x, y, z, radius, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `name` | `const char*` | `string` | |

**返回值:** `Object` → `int`

**说明:** Example:
OBJECT::GET_RAYFIRE_MAP_OBJECT(-809.9619750976562, 170.919, 75.7406997680664, 3.0, "des_tvsmash");

---

### SET_STATE_OF_RAYFIRE_MAP_OBJECT

**Hash:** `0x5C29F698D404C5E1` | **JHash:** `0x21F51560` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STATE_OF_RAYFIRE_MAP_OBJECT(Object object, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STATE_OF_RAYFIRE_MAP_OBJECT, object, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Defines the state of a destructible object.
Use the GET_RAYFIRE_MAP_OBJECT native to find an object's handle with its name / coords.
State 2 == object just spawned
State 4 == Beginning of the animation
State 6 == Start animation
State 9 == End of the animation

---

### GET_STATE_OF_RAYFIRE_MAP_OBJECT

**Hash:** `0x899BA936634A322E` | **JHash:** `0xF1B8817A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_STATE_OF_RAYFIRE_MAP_OBJECT(Object object)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_STATE_OF_RAYFIRE_MAP_OBJECT, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `int` → `int`

**说明:** Get a destructible object's state.
Substract 1 to get the real state.
See SET_STATE_OF_RAYFIRE_MAP_OBJECT to see the different states
For example, if the object just spawned (state 2), the native will return 3.

---

### DOES_RAYFIRE_MAP_OBJECT_EXIST

**Hash:** `0x52AF537A0C5B8AAD` | **JHash:** `0xE08C834D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_RAYFIRE_MAP_OBJECT_EXIST(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_RAYFIRE_MAP_OBJECT_EXIST, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if a destructible object with this handle exists, false otherwise.  

---

### GET_RAYFIRE_MAP_OBJECT_ANIM_PHASE

**Hash:** `0x260EE4FDBDF4DB01` | **JHash:** `0x020497DE` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_RAYFIRE_MAP_OBJECT_ANIM_PHASE(Object object)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_RAYFIRE_MAP_OBJECT_ANIM_PHASE, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `float` → `float`

**说明:** `object`: The des-object handle to get the animation progress from.
Return value is a float between 0.0 and 1.0, 0.0 is the beginning of the animation, 1.0 is the end. Value resets to 0.0 instantly after reaching 1.0.

---

### CREATE_PICKUP

**Hash:** `0xFBA08C503DD5FA58` | **JHash:** `0x5E14DF68` | **Build:** `323`

**C++ 原型:**
```cpp
Pickup CREATE_PICKUP(Hash pickupHash, float posX, float posY, float posZ, int p4, int value, BOOL p6, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PICKUP, pickupHash, posX, posY, posZ, p4, value, p6, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `p4` | `int` | `int` | |
| `value` | `int` | `int` | |
| `p6` | `BOOL` | `bool` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Pickup` → `int`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### CREATE_PICKUP_ROTATE

**Hash:** `0x891804727E0A98B7` | **JHash:** `0xF015BFE2` | **Build:** `323`

**C++ 原型:**
```cpp
Pickup CREATE_PICKUP_ROTATE(Hash pickupHash, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, int flag, int amount, Any p9, BOOL p10, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PICKUP_ROTATE, pickupHash, posX, posY, posZ, rotX, rotY, rotZ, flag, amount, p9, p10, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `flag` | `int` | `int` | |
| `amount` | `int` | `int` | |
| `p9` | `Any` | `object` | |
| `p10` | `BOOL` | `bool` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Pickup` → `int`

**说明:** flags:
8 (1 << 3): place on ground
512 (1 << 9): spin around

Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### FORCE_PICKUP_ROTATE_FACE_UP

**Hash:** `0x394CD08E31313C28` | **Build:** `944`

**C++ 原型:**
```cpp
void FORCE_PICKUP_ROTATE_FACE_UP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PICKUP_ROTATE_FACE_UP);
```

**返回值:** `void` → `void`

---

### SET_CUSTOM_PICKUP_WEAPON_HASH

**Hash:** `0x826D1EE4D1CAFC78` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_CUSTOM_PICKUP_WEAPON_HASH(Hash pickupHash, Pickup pickup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CUSTOM_PICKUP_WEAPON_HASH, pickupHash, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `pickup` | `Pickup` | `int` | |

**返回值:** `void` → `void`

---

### CREATE_AMBIENT_PICKUP

**Hash:** `0x673966A0C0FD7171` | **JHash:** `0x17B99CE7` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_AMBIENT_PICKUP(Hash pickupHash, float posX, float posY, float posZ, int flags, int value, Hash modelHash, BOOL p7, BOOL p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_AMBIENT_PICKUP, pickupHash, posX, posY, posZ, flags, value, modelHash, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `value` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### CREATE_NON_NETWORKED_AMBIENT_PICKUP

**Hash:** `0x9C93764223E29C50` | **Build:** `2372`

**C++ 原型:**
```cpp
Object CREATE_NON_NETWORKED_AMBIENT_PICKUP(Hash pickupHash, float posX, float posY, float posZ, int flags, int value, Hash modelHash, BOOL p7, BOOL p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_NON_NETWORKED_AMBIENT_PICKUP, pickupHash, posX, posY, posZ, flags, value, modelHash, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `value` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

---

### BLOCK_PLAYERS_FOR_AMBIENT_PICKUP

**Hash:** `0x1E3F1B1B891A2AAA` | **Build:** `573`

**C++ 原型:**
```cpp
void BLOCK_PLAYERS_FOR_AMBIENT_PICKUP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_PLAYERS_FOR_AMBIENT_PICKUP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CREATE_PORTABLE_PICKUP

**Hash:** `0x2EAF1FDB2FB55698` | **JHash:** `0x8C886BE5` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_PORTABLE_PICKUP(Hash pickupHash, float x, float y, float z, BOOL placeOnGround, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PORTABLE_PICKUP, pickupHash, x, y, z, placeOnGround, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `placeOnGround` | `BOOL` | `bool` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Object` → `int`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### CREATE_NON_NETWORKED_PORTABLE_PICKUP

**Hash:** `0x125494B98A21AAF7` | **JHash:** `0x56A02502` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_NON_NETWORKED_PORTABLE_PICKUP(Hash pickupHash, float x, float y, float z, BOOL placeOnGround, Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_NON_NETWORKED_PORTABLE_PICKUP, pickupHash, x, y, z, placeOnGround, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `placeOnGround` | `BOOL` | `bool` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `Object` → `int`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### ATTACH_PORTABLE_PICKUP_TO_PED

**Hash:** `0x8DC39368BDD57755` | **JHash:** `0x184F6AB3` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_PORTABLE_PICKUP_TO_PED(Object pickupObject, Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_PORTABLE_PICKUP_TO_PED, pickupObject, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupObject` | `Object` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### DETACH_PORTABLE_PICKUP_FROM_PED

**Hash:** `0xCF463D1E9A0AECB1` | **JHash:** `0x1D094562` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_PORTABLE_PICKUP_FROM_PED(Object pickupObject)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_PORTABLE_PICKUP_FROM_PED, pickupObject);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupObject` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### FORCE_PORTABLE_PICKUP_LAST_ACCESSIBLE_POSITION_SETTING

**Hash:** `0x5CE2E45A5CE2E45A` | **Build:** `2545`

**C++ 原型:**
```cpp
void FORCE_PORTABLE_PICKUP_LAST_ACCESSIBLE_POSITION_SETTING(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PORTABLE_PICKUP_LAST_ACCESSIBLE_POSITION_SETTING, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### HIDE_PORTABLE_PICKUP_WHEN_DETACHED

**Hash:** `0x867458251D47CCB2` | **JHash:** `0x1F63B042` | **Build:** `463`

**C++ 原型:**
```cpp
void HIDE_PORTABLE_PICKUP_WHEN_DETACHED(Object pickupObject, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_PORTABLE_PICKUP_WHEN_DETACHED, pickupObject, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupObject` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_MAX_NUM_PORTABLE_PICKUPS_CARRIED_BY_PLAYER

**Hash:** `0x0BF3B3BD47D79C08` | **JHash:** `0x7EFBA039` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MAX_NUM_PORTABLE_PICKUPS_CARRIED_BY_PLAYER(Hash modelHash, int number)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MAX_NUM_PORTABLE_PICKUPS_CARRIED_BY_PLAYER, modelHash, number);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `number` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_LOCAL_PLAYER_CAN_COLLECT_PORTABLE_PICKUPS

**Hash:** `0x78857FC65CADB909` | **JHash:** `0xA3CDF152` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_CAN_COLLECT_PORTABLE_PICKUPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_CAN_COLLECT_PORTABLE_PICKUPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_SAFE_PICKUP_COORDS

**Hash:** `0x6E16BC2503FF1FF0` | **JHash:** `0x618B5F67` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_SAFE_PICKUP_COORDS(float x, float y, float z, float p3, float p4)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_SAFE_PICKUP_COORDS, x, y, z, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### ADD_EXTENDED_PICKUP_PROBE_AREA

**Hash:** `0xD4A7A435B3710D05` | **Build:** `1290`

**C++ 原型:**
```cpp
void ADD_EXTENDED_PICKUP_PROBE_AREA(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_EXTENDED_PICKUP_PROBE_AREA, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Adds an area that seems to be related to pickup physics behavior.
Max amount of areas is 10. Only works in multiplayer.

---

### CLEAR_EXTENDED_PICKUP_PROBE_AREAS

**Hash:** `0xB7C6D80FB371659A` | **Build:** `1290`

**C++ 原型:**
```cpp
void CLEAR_EXTENDED_PICKUP_PROBE_AREAS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_EXTENDED_PICKUP_PROBE_AREAS);
```

**返回值:** `void` → `void`

**说明:** Clears all areas created by ADD_EXTENDED_PICKUP_PROBE_AREA

---

### GET_PICKUP_COORDS

**Hash:** `0x225B8B35C88029B3` | **JHash:** `0xC2E1E2C5` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PICKUP_COORDS(Pickup pickup)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PICKUP_COORDS, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### SUPPRESS_PICKUP_SOUND_FOR_PICKUP

**Hash:** `0x8DCA505A5C196F05` | **Build:** `1180`

**C++ 原型:**
```cpp
void SUPPRESS_PICKUP_SOUND_FOR_PICKUP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_PICKUP_SOUND_FOR_PICKUP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REMOVE_ALL_PICKUPS_OF_TYPE

**Hash:** `0x27F9D613092159CF` | **JHash:** `0x40062C53` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_PICKUPS_OF_TYPE(Hash pickupHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_PICKUPS_OF_TYPE, pickupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### HAS_PICKUP_BEEN_COLLECTED

**Hash:** `0x80EC48E6679313F9` | **JHash:** `0x0BE5CCED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PICKUP_BEEN_COLLECTED(Pickup pickup)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PICKUP_BEEN_COLLECTED, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_PICKUP

**Hash:** `0x3288D8ACAECD2AB2` | **JHash:** `0x64A7A0E0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PICKUP(Pickup pickup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PICKUP, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `void` → `void`

---

### CREATE_MONEY_PICKUPS

**Hash:** `0x0589B5E791CE9B2B` | **JHash:** `0x36C9A5EA` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_MONEY_PICKUPS(float x, float y, float z, int value, int amount, Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_MONEY_PICKUPS, x, y, z, value, amount, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `value` | `int` | `int` | |
| `amount` | `int` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Spawns one or more money pickups.

x: The X-component of the world position to spawn the money pickups at.
y: The Y-component of the world position to spawn the money pickups at.
z: The Z-component of the world position to spawn the money pickups at.
value: The combined value of the pickups (in dollars).
amount: The number of pickups to spawn.
model: The model to use, or 0 for default money model.

Example:
CREATE_MONEY_PICKUPS(x, y, z, 1000, 3, 0x684a97ae);

Spawns 3 spray cans that'll collectively give $1000 when picked up. (Three spray cans, each giving $334, $334, $332 = $1000).

==============================================

Max is 2000 in MP. So if you put the amount to 20, but the value to $400,000 eg. They will only be able to pickup 20 - $2,000 bags. So, $40,000

---

### DOES_PICKUP_EXIST

**Hash:** `0xAFC1CA75AD4074D1` | **JHash:** `0x9C6DA0B3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PICKUP_EXIST(Pickup pickup)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PICKUP_EXIST, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_PICKUP_OBJECT_EXIST

**Hash:** `0xD9EFB6DBF7DAAEA3` | **JHash:** `0xE0B32108` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PICKUP_OBJECT_EXIST(Object pickupObject)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PICKUP_OBJECT_EXIST, pickupObject);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupObject` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PICKUP_OBJECT

**Hash:** `0x5099BC55630B25AE` | **JHash:** `0x6052E62E` | **Build:** `323`

**C++ 原型:**
```cpp
Object GET_PICKUP_OBJECT(Pickup pickup)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PICKUP_OBJECT, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `Object` → `int`

---

### IS_OBJECT_A_PICKUP

**Hash:** `0xFC481C641EBBD27D` | **JHash:** `0x93D23399` | **Build:** `1365`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_A_PICKUP(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_A_PICKUP, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_OBJECT_A_PORTABLE_PICKUP

**Hash:** `0x0378C08504160D0D` | **JHash:** `0x0F0E94FF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_OBJECT_A_PORTABLE_PICKUP(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_OBJECT_A_PORTABLE_PICKUP, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_PICKUP_OF_TYPE_EXIST_IN_AREA

**Hash:** `0xF9C36251F6E48E33` | **JHash:** `0xF139681B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_PICKUP_OF_TYPE_EXIST_IN_AREA(Hash pickupHash, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_PICKUP_OF_TYPE_EXIST_IN_AREA, pickupHash, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### SET_PICKUP_REGENERATION_TIME

**Hash:** `0x78015C9B4B3ECC9D` | **JHash:** `0xAB11267D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PICKUP_REGENERATION_TIME(Pickup pickup, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_REGENERATION_TIME, pickup, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FORCE_PICKUP_REGENERATE

**Hash:** `0x758A5C1B3B1E1990` | **Build:** `1011`

**C++ 原型:**
```cpp
void FORCE_PICKUP_REGENERATE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PICKUP_REGENERATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE

**Hash:** `0x616093EC6B139DD9` | **JHash:** `0x7FADB4B9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(Player player, Hash pickupHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE, player, pickupHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `pickupHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disabling/enabling a player from getting pickups. From the scripts:

OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_portable_package}, 0);
OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_portable_package}, 0);
OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_portable_package}, 1);
OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_portable_package}, 0);
OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_armour_standard}, 0);
OBJECT::SET_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_OF_TYPE(PLAYER::PLAYER_ID(), ${pickup_armour_standard}, 1);

Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### SET_LOCAL_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_WITH_MODEL

**Hash:** `0x88EAEC617CD26926` | **JHash:** `0x3A8F1BF7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LOCAL_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_WITH_MODEL(Hash modelHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LOCAL_PLAYER_PERMITTED_TO_COLLECT_PICKUPS_WITH_MODEL, modelHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Maximum amount of pickup models that can be disallowed is 30.

---

### ALLOW_ALL_PLAYERS_TO_COLLECT_PICKUPS_OF_TYPE

**Hash:** `0xFDC07C58E8AAB715` | **Build:** `1734`

**C++ 原型:**
```cpp
void ALLOW_ALL_PLAYERS_TO_COLLECT_PICKUPS_OF_TYPE(Hash pickupHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_ALL_PLAYERS_TO_COLLECT_PICKUPS_OF_TYPE, pickupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### SET_TEAM_PICKUP_OBJECT

**Hash:** `0x53E0DF1A2A3CF0CA` | **JHash:** `0x77687DC5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TEAM_PICKUP_OBJECT(Object object, Any p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TEAM_PICKUP_OBJECT, object, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PREVENT_COLLECTION_OF_PORTABLE_PICKUP

**Hash:** `0x92AEFB5F6E294023` | **JHash:** `0xCBB5F9B6` | **Build:** `323`

**C++ 原型:**
```cpp
void PREVENT_COLLECTION_OF_PORTABLE_PICKUP(Object object, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PREVENT_COLLECTION_OF_PORTABLE_PICKUP, object, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_OBJECT_GLOW_WHEN_UNCOLLECTABLE

**Hash:** `0x27F248C3FEBFAAD3` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_GLOW_WHEN_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_GLOW_WHEN_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_PICKUP_GLOW_DISABLED

**Hash:** `0x08BD8BA5BDE2C2FA` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_PICKUP_GLOW_DISABLED(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_PICKUP_GLOW_DISABLED, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_GLOW_OFFSET

**Hash:** `0x0596843B34B95CE5` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_PICKUP_GLOW_OFFSET(Pickup pickup, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_GLOW_OFFSET, pickup, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0.51. This native is called before SET_PICKUP_REGENERATION_TIME in all occurances.

---

### SET_PICKUP_OBJECT_GLOW_OFFSET

**Hash:** `0xA08FE5E49BDC39DD` | **JHash:** `0x276A7807` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_GLOW_OFFSET(Pickup pickup, float p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_GLOW_OFFSET, pickup, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 is always -0.2 in scripts and p2 is always true in scripts.

---

### SET_OBJECT_GLOW_IN_SAME_TEAM

**Hash:** `0x62454A641B41F3C5` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_OBJECT_GLOW_IN_SAME_TEAM(Pickup pickup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_GLOW_IN_SAME_TEAM, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_OBJECT_ARROW_MARKER

**Hash:** `0x39A5FB7EAF150840` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_ARROW_MARKER(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_ARROW_MARKER, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ALLOW_PICKUP_ARROW_MARKER_WHEN_UNCOLLECTABLE

**Hash:** `0x834344A414C7C85D` | **Build:** `2372`

**C++ 原型:**
```cpp
void ALLOW_PICKUP_ARROW_MARKER_WHEN_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PICKUP_ARROW_MARKER_WHEN_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_DEFAULT_AMMO_FOR_WEAPON_PICKUP

**Hash:** `0xDB41D07A45A6D4B7` | **JHash:** `0x000E92DC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_DEFAULT_AMMO_FOR_WEAPON_PICKUP(Hash pickupHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DEFAULT_AMMO_FOR_WEAPON_PICKUP, pickupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### SET_PICKUP_GENERATION_RANGE_MULTIPLIER

**Hash:** `0x318516E02DE3ECE2` | **JHash:** `0x9879AC51` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PICKUP_GENERATION_RANGE_MULTIPLIER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_GENERATION_RANGE_MULTIPLIER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PICKUP_GENERATION_RANGE_MULTIPLIER

**Hash:** `0xB3ECA65C7317F174` | **Build:** `944`

**C++ 原型:**
```cpp
float GET_PICKUP_GENERATION_RANGE_MULTIPLIER(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PICKUP_GENERATION_RANGE_MULTIPLIER);
```

**返回值:** `float` → `float`

---

### SET_ONLY_ALLOW_AMMO_COLLECTION_WHEN_LOW

**Hash:** `0x31F924B53EADDF65` | **JHash:** `0xDB18FA01` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ONLY_ALLOW_AMMO_COLLECTION_WHEN_LOW(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ONLY_ALLOW_AMMO_COLLECTION_WHEN_LOW, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_UNCOLLECTABLE

**Hash:** `0x1C1B69FAE509BA97` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_PICKUP_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_TRANSPARENT_WHEN_UNCOLLECTABLE

**Hash:** `0x858EC9FD25DE04AA` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_PICKUP_TRANSPARENT_WHEN_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_TRANSPARENT_WHEN_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_HIDDEN_WHEN_UNCOLLECTABLE

**Hash:** `0x3ED2B83AB2E82799` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_PICKUP_HIDDEN_WHEN_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_HIDDEN_WHEN_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_OBJECT_TRANSPARENT_WHEN_UNCOLLECTABLE

**Hash:** `0x8881C98A31117998` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_TRANSPARENT_WHEN_UNCOLLECTABLE(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_TRANSPARENT_WHEN_UNCOLLECTABLE, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_OBJECT_ALPHA_WHEN_TRANSPARENT

**Hash:** `0x8CFF648FBD7330F1` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_ALPHA_WHEN_TRANSPARENT(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_ALPHA_WHEN_TRANSPARENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 is either 0 or 50 in scripts.

---

### SET_PORTABLE_PICKUP_PERSIST

**Hash:** `0x46F3ADD1E2D5BAF2` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PORTABLE_PICKUP_PERSIST(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PORTABLE_PICKUP_PERSIST, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ALLOW_PORTABLE_PICKUP_TO_MIGRATE_TO_NON_PARTICIPANTS

**Hash:** `0x641F272B52E2F0F8` | **Build:** `877`

**C++ 原型:**
```cpp
void ALLOW_PORTABLE_PICKUP_TO_MIGRATE_TO_NON_PARTICIPANTS(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PORTABLE_PICKUP_TO_MIGRATE_TO_NON_PARTICIPANTS, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_ACTIVATE_PHYSICS_ON_UNFIXED_PICKUP

**Hash:** `0x4C134B4DF76025D0` | **Build:** `1180`

**C++ 原型:**
```cpp
void FORCE_ACTIVATE_PHYSICS_ON_UNFIXED_PICKUP(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ACTIVATE_PHYSICS_ON_UNFIXED_PICKUP, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ALLOW_PICKUP_BY_NONE_PARTICIPANT

**Hash:** `0xAA059C615DE9DD03` | **Build:** `1180`

**C++ 原型:**
```cpp
void ALLOW_PICKUP_BY_NONE_PARTICIPANT(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PICKUP_BY_NONE_PARTICIPANT, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SUPPRESS_PICKUP_REWARD_TYPE

**Hash:** `0xF92099527DB8E2A7` | **JHash:** `0xA7E936FD` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_PICKUP_REWARD_TYPE(int rewardType, BOOL suppress)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_PICKUP_REWARD_TYPE, rewardType, suppress);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rewardType` | `int` | `int` | |
| `suppress` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enum ePickupRewardType
{
    PICKUP_REWARD_TYPE_AMMO = (1 << 0),
    PICKUP_REWARD_TYPE_BULLET_MP = (1 << 1),
    PICKUP_REWARD_TYPE_MISSILE_MP = (1 << 2),
    PICKUP_REWARD_TYPE_GRENADE_LAUNCHER_MP = (1 << 3),
    PICKUP_REWARD_TYPE_ARMOUR = (1 << 4),
    PICKUP_REWARD_TYPE_HEALTH = (1 << 5),
    PICKUP_REWARD_TYPE_HEALTH_VARIABLE = PICKUP_REWARD_TYPE_HEALTH,
    PICKUP_REWARD_TYPE_MONEY_FIXED = (1 << 6),
    PICKUP_REWARD_TYPE_MONEY_VARIABLE = PICKUP_REWARD_TYPE_MONEY_FIXED,
    PICKUP_REWARD_TYPE_WEAPON = (1 << 7),
    PICKUP_REWARD_TYPE_STAT = (1 << 8),
    PICKUP_REWARD_TYPE_STAT_VARIABLE = PICKUP_REWARD_TYPE_STAT,
    PICKUP_REWARD_TYPE_VEHICLE_FIX = (1 << 9),
    PICKUP_REWARD_TYPE_FIREWORK_MP = (1 << 10),
};

---

### CLEAR_ALL_PICKUP_REWARD_TYPE_SUPPRESSION

**Hash:** `0xA2C1F5E92AFE49ED` | **JHash:** `0xB241806C` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_PICKUP_REWARD_TYPE_SUPPRESSION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_PICKUP_REWARD_TYPE_SUPPRESSION);
```

**返回值:** `void` → `void`

---

### CLEAR_PICKUP_REWARD_TYPE_SUPPRESSION

**Hash:** `0x762DB2D380B48D04` | **JHash:** `0xD1BAAFB7` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PICKUP_REWARD_TYPE_SUPPRESSION(int rewardType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PICKUP_REWARD_TYPE_SUPPRESSION, rewardType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rewardType` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RENDER_FAKE_PICKUP_GLOW

**Hash:** `0x3430676B11CDF21D` | **JHash:** `0x63B02FAD` | **Build:** `323`

**C++ 原型:**
```cpp
void RENDER_FAKE_PICKUP_GLOW(float x, float y, float z, int colorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RENDER_FAKE_PICKUP_GLOW, x, y, z, colorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `colorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** draws circular marker at pos
-1 = none
0 = red
1 = green
2 = blue
3 = green larger
4 = nothing
5 = green small

---

### SET_PICKUP_OBJECT_COLLECTABLE_IN_VEHICLE

**Hash:** `0x7813E8B8C4AE4799` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_PICKUP_OBJECT_COLLECTABLE_IN_VEHICLE(Pickup pickup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_OBJECT_COLLECTABLE_IN_VEHICLE, pickup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_TRACK_DAMAGE_EVENTS

**Hash:** `0xBFFE53AE7E67FCDC` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_PICKUP_TRACK_DAMAGE_EVENTS(Pickup pickup, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_TRACK_DAMAGE_EVENTS, pickup, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickup` | `Pickup` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENTITY_FLAG_SUPPRESS_SHADOW

**Hash:** `0xD05A3241B9A86F19` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_ENTITY_FLAG_SUPPRESS_SHADOW(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_FLAG_SUPPRESS_SHADOW, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets entity+38 to C (when false) or 0xFF3f (when true)

---

### SET_ENTITY_FLAG_RENDER_SMALL_SHADOW

**Hash:** `0xB2D0BDE54F0E8E5A` | **JHash:** `0x132B6D92` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENTITY_FLAG_RENDER_SMALL_SHADOW(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENTITY_FLAG_RENDER_SMALL_SHADOW, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_WEAPON_TYPE_FROM_PICKUP_TYPE

**Hash:** `0x08F96CA6C551AD51` | **JHash:** `0xEDD01937` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_WEAPON_TYPE_FROM_PICKUP_TYPE(Hash pickupHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPON_TYPE_FROM_PICKUP_TYPE, pickupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### GET_PICKUP_TYPE_FROM_WEAPON_HASH

**Hash:** `0xD6429A016084F1A5` | **Build:** `1290`

**C++ 原型:**
```cpp
Hash GET_PICKUP_TYPE_FROM_WEAPON_HASH(Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PICKUP_TYPE_FROM_WEAPON_HASH, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the pickup hash for the given weapon hash

---

### IS_PICKUP_WEAPON_OBJECT_VALID

**Hash:** `0x11D1E53A726891FE` | **JHash:** `0x883DAB2D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PICKUP_WEAPON_OBJECT_VALID(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PICKUP_WEAPON_OBJECT_VALID, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_OBJECT_TINT_INDEX

**Hash:** `0xE84EB93729C5F36A` | **Build:** `757`

**C++ 原型:**
```cpp
int GET_OBJECT_TINT_INDEX(Object object)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_OBJECT_TINT_INDEX, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `int` → `int`

---

### SET_OBJECT_TINT_INDEX

**Hash:** `0x971DA0055324D033` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_OBJECT_TINT_INDEX(Object object, int textureVariation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_TINT_INDEX, object, textureVariation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `textureVariation` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum ObjectPaintVariants
{
 Pacific = 0,
  Azure = 1,
    Nautical = 2,
 Continental = 3,
  Battleship = 4,
   Intrepid = 5,
 Uniform = 6,
  Classico = 7,
 Mediterranean = 8,
    Command = 9,
  Mariner = 10,
 Ruby = 11,
    Vintage = 12,
 Pristine = 13,
    Merchant = 14,
    Voyager = 15
};

---

### SET_TINT_INDEX_CLOSEST_BUILDING_OF_TYPE

**Hash:** `0xF12E33034D887F66` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL SET_TINT_INDEX_CLOSEST_BUILDING_OF_TYPE(float x, float y, float z, float radius, Hash modelHash, int textureVariation)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_TINT_INDEX_CLOSEST_BUILDING_OF_TYPE, x, y, z, radius, modelHash, textureVariation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `textureVariation` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PROP_TINT_INDEX

**Hash:** `0x31574B1B41268673` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_PROP_TINT_INDEX(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROP_TINT_INDEX, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PROP_LIGHT_COLOR

**Hash:** `0x5F048334B4A4E774` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL SET_PROP_LIGHT_COLOR(Object object, BOOL p1, int r, int g, int b)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PROP_LIGHT_COLOR, object, p1, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PROP_LIGHT_OVERRIDEN

**Hash:** `0xADF084FB8F075D06` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL IS_PROP_LIGHT_OVERRIDEN(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PROP_LIGHT_OVERRIDEN, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_OBJECT_IS_VISIBLE_IN_MIRRORS

**Hash:** `0x3B2FD68DB5F8331C` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_OBJECT_IS_VISIBLE_IN_MIRRORS(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_IS_VISIBLE_IN_MIRRORS, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_OBJECT_SPEED_BOOST_AMOUNT

**Hash:** `0x96EE0EBA0163DF80` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_OBJECT_SPEED_BOOST_AMOUNT(Object object, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_SPEED_BOOST_AMOUNT, object, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_OBJECT_SPEED_BOOST_DURATION

**Hash:** `0xDF6CA0330F2E737B` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_OBJECT_SPEED_BOOST_DURATION(Object object, float duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_SPEED_BOOST_DURATION, object, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `duration` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CONVERT_OLD_PICKUP_TYPE_TO_NEW

**Hash:** `0x5EAAD83F8CFB4575` | **JHash:** `0x6AE36192` | **Build:** `323`

**C++ 原型:**
```cpp
Hash CONVERT_OLD_PICKUP_TYPE_TO_NEW(Hash pickupHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.CONVERT_OLD_PICKUP_TYPE_TO_NEW, pickupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pickupHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** returns pickup hash.

Full list of pickup types by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pickupTypes.json

---

### SET_FORCE_OBJECT_THIS_FRAME

**Hash:** `0xF538081986E49E9D` | **JHash:** `0x3DA41C1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_OBJECT_THIS_FRAME(float x, float y, float z, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_OBJECT_THIS_FRAME, x, y, z, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ONLY_CLEAN_UP_OBJECT_WHEN_OUT_OF_RANGE

**Hash:** `0xADBE4809F19F927A` | **JHash:** `0x2048A7DD` | **Build:** `323`

**C++ 原型:**
```cpp
void ONLY_CLEAN_UP_OBJECT_WHEN_OUT_OF_RANGE(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ONLY_CLEAN_UP_OBJECT_WHEN_OUT_OF_RANGE, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_COLLISIONS_BETWEEN_CARS_AND_CAR_PARACHUTE

**Hash:** `0x8CAAB2BD3EA58BD4` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_DISABLE_COLLISIONS_BETWEEN_CARS_AND_CAR_PARACHUTE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_COLLISIONS_BETWEEN_CARS_AND_CAR_PARACHUTE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PROJECTILES_SHOULD_EXPLODE_ON_CONTACT

**Hash:** `0x63ECF581BC70E363` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_PROJECTILES_SHOULD_EXPLODE_ON_CONTACT(Entity entity, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROJECTILES_SHOULD_EXPLODE_ON_CONTACT, entity, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_DRIVE_ARTICULATED_JOINT

**Hash:** `0x911024442F4898F0` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_DRIVE_ARTICULATED_JOINT(Object object, BOOL toggle, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVE_ARTICULATED_JOINT, object, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Activate the physics to: "xs_prop_arena_{flipper,wall,bollard,turntable,pit}"

---

### SET_DRIVE_ARTICULATED_JOINT_WITH_INFLICTOR

**Hash:** `0xB20834A7DD3D8896` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_DRIVE_ARTICULATED_JOINT_WITH_INFLICTOR(Object object, BOOL toggle, int p2, Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVE_ARTICULATED_JOINT_WITH_INFLICTOR, object, toggle, p2, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_OBJECT_IS_A_PRESSURE_PLATE

**Hash:** `0x734E1714D077DA9A` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_OBJECT_IS_A_PRESSURE_PLATE(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OBJECT_IS_A_PRESSURE_PLATE, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_WEAPON_IMPACTS_APPLY_GREATER_FORCE

**Hash:** `0x1A6CBB06E2D0D79D` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_WEAPON_IMPACTS_APPLY_GREATER_FORCE(Object object, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_IMPACTS_APPLY_GREATER_FORCE, object, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_IS_ARTICULATED_JOINT_AT_MIN_ANGLE

**Hash:** `0x43C677F1E1158005` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_IS_ARTICULATED_JOINT_AT_MIN_ANGLE(Object object, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_ARTICULATED_JOINT_AT_MIN_ANGLE, object, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_IS_ARTICULATED_JOINT_AT_MAX_ANGLE

**Hash:** `0x3BD770D281982DB5` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_IS_ARTICULATED_JOINT_AT_MAX_ANGLE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_ARTICULATED_JOINT_AT_MAX_ANGLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_IS_OBJECT_ARTICULATED

**Hash:** `0x1C57C94A6446492A` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_IS_OBJECT_ARTICULATED(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IS_OBJECT_ARTICULATED, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IS_OBJECT_BALL

**Hash:** `0xB5B7742424BD4445` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_IS_OBJECT_BALL(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IS_OBJECT_BALL, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---


---

## PAD

**包含 50 个 Native 函数**

### IS_CONTROL_ENABLED

**Hash:** `0x1CEA6BFDF248E5D9` | **JHash:** `0x9174AF84` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROL_ENABLED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROL_ENABLED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: 0: PLAYER_CONTROL, 1: CAMERA_CONTROL, 2: FRONTEND_CONTROL
For more info, see https://docs.fivem.net/docs/game-references/controls/

---

### IS_CONTROL_PRESSED

**Hash:** `0xF3A21BCD95725A4A` | **JHash:** `0x517A4384` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROL_PRESSED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROL_PRESSED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether a control is currently pressed.
control: see IS_CONTROL_ENABLED

---

### IS_CONTROL_RELEASED

**Hash:** `0x648EE3E7F38877DD` | **JHash:** `0x1F91A06E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROL_RELEASED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROL_RELEASED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether a control is currently _not_ pressed.
control: see IS_CONTROL_ENABLED

---

### IS_CONTROL_JUST_PRESSED

**Hash:** `0x580417101DDB492F` | **JHash:** `0x4487F579` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROL_JUST_PRESSED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROL_JUST_PRESSED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether a control was newly pressed since the last check.
control: see IS_CONTROL_ENABLED

---

### IS_CONTROL_JUST_RELEASED

**Hash:** `0x50F940259D3841E6` | **JHash:** `0x2314444B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROL_JUST_RELEASED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROL_JUST_RELEASED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether a control was newly released since the last check.
control: see IS_CONTROL_ENABLED

---

### _IS_CONTROL_HELD_DOWN

**Hash:** `0xE9CB8C56E90D5079` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _IS_CONTROL_HELD_DOWN(int control, int action, int duration)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._IS_CONTROL_HELD_DOWN, control, action, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_CONTROL_VALUE

**Hash:** `0xD95E79E8686D2C27` | **JHash:** `0xC526F3C6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CONTROL_VALUE(int control, int action)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CONTROL_VALUE, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** control: see IS_CONTROL_ENABLED

---

### GET_CONTROL_NORMAL

**Hash:** `0xEC3C9B8D5327B563` | **JHash:** `0x5DE226A5` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CONTROL_NORMAL(int control, int action)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CONTROL_NORMAL, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** Returns the value of GET_CONTROL_VALUE normalized (i.e. a real number value between -1 and 1)

control: see IS_CONTROL_ENABLED

---

### SET_USE_ADJUSTED_MOUSE_COORDS

**Hash:** `0x5B73C77D9EB66E24` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_USE_ADJUSTED_MOUSE_COORDS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_ADJUSTED_MOUSE_COORDS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_CONTROL_UNBOUND_NORMAL

**Hash:** `0x5B84D09CEC5209C5` | **JHash:** `0xC49343BB` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_CONTROL_UNBOUND_NORMAL(int control, int action)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_CONTROL_UNBOUND_NORMAL, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** Seems to return values between -1 and 1 for controls like gas and steering.

control: see IS_CONTROL_ENABLED

---

### SET_CONTROL_VALUE_NEXT_FRAME

**Hash:** `0xE8A25867FBA3B05E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_CONTROL_VALUE_NEXT_FRAME(int control, int action, float value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_CONTROL_VALUE_NEXT_FRAME, control, action, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** This is for simulating player input.
value is a float value from 0 - 1

control: see IS_CONTROL_ENABLED

---

### IS_DISABLED_CONTROL_PRESSED

**Hash:** `0xE2587F8CBBD87B1D` | **JHash:** `0x32A93544` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DISABLED_CONTROL_PRESSED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DISABLED_CONTROL_PRESSED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

---

### IS_DISABLED_CONTROL_RELEASED

**Hash:** `0xFB6C4072E9A32E92` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL IS_DISABLED_CONTROL_RELEASED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DISABLED_CONTROL_RELEASED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

---

### IS_DISABLED_CONTROL_JUST_PRESSED

**Hash:** `0x91AEF906BCA88877` | **JHash:** `0xEE6ABD32` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DISABLED_CONTROL_JUST_PRESSED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DISABLED_CONTROL_JUST_PRESSED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

---

### IS_DISABLED_CONTROL_JUST_RELEASED

**Hash:** `0x305C8DCD79DA8B0F` | **JHash:** `0xD6A679E1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DISABLED_CONTROL_JUST_RELEASED(int control, int action)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DISABLED_CONTROL_JUST_RELEASED, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

---

### GET_DISABLED_CONTROL_NORMAL

**Hash:** `0x11E65974A982637C` | **JHash:** `0x66FF4FAA` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_DISABLED_CONTROL_NORMAL(int control, int action)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_DISABLED_CONTROL_NORMAL, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** control: see IS_CONTROL_ENABLED

---

### GET_DISABLED_CONTROL_UNBOUND_NORMAL

**Hash:** `0x4F8A26A890FD62FB` | **JHash:** `0xF2A65A4C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_DISABLED_CONTROL_UNBOUND_NORMAL(int control, int action)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_DISABLED_CONTROL_UNBOUND_NORMAL, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** The "disabled" variant of GET_CONTROL_UNBOUND_NORMAL.

control: see IS_CONTROL_ENABLED

---

### GET_CONTROL_HOW_LONG_AGO

**Hash:** `0xD7D22F5592AED8BA` | **JHash:** `0x0E8EF929` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CONTROL_HOW_LONG_AGO(int control)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CONTROL_HOW_LONG_AGO, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns time in ms since last input.

control: see IS_CONTROL_ENABLED

---

### IS_USING_KEYBOARD_AND_MOUSE

**Hash:** `0xA571D46727E2B718` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_USING_KEYBOARD_AND_MOUSE(int control)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_USING_KEYBOARD_AND_MOUSE, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: unused parameter

---

### IS_USING_CURSOR

**Hash:** `0x13337B38DB572509` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_USING_CURSOR(int control)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_USING_CURSOR, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

---

### SET_CURSOR_POSITION

**Hash:** `0xFC695459D4D0E219` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_CURSOR_POSITION(float x, float y)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_CURSOR_POSITION, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### IS_USING_REMOTE_PLAY

**Hash:** `0x23F09EADC01449D6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_USING_REMOTE_PLAY(int control)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_USING_REMOTE_PLAY, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: see IS_CONTROL_ENABLED

Hardcoded to return false.

---

### HAVE_CONTROLS_CHANGED

**Hash:** `0x6CD79468A1E595C6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAVE_CONTROLS_CHANGED(int control)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_CONTROLS_CHANGED, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** control: unused parameter

---

### GET_CONTROL_INSTRUCTIONAL_BUTTONS_STRING

**Hash:** `0x0499D7B09FC9B407` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CONTROL_INSTRUCTIONAL_BUTTONS_STRING(int control, int action, BOOL allowXOSwap)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CONTROL_INSTRUCTIONAL_BUTTONS_STRING, control, action, allowXOSwap);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |
| `allowXOSwap` | `BOOL` | `bool` | |

**返回值:** `const char*` → `string`

**说明:** allowXOSwap appears to always be true.

EG:
GET_CONTROL_INSTRUCTIONAL_BUTTON (2, 201, 1) /*INPUT_FRONTEND_ACCEPT (e.g. Enter button)*/
GET_CONTROL_INSTRUCTIONAL_BUTTON (2, 202, 1) /*INPUT_FRONTEND_CANCEL (e.g. ESC button)*/
GET_CONTROL_INSTRUCTIONAL_BUTTON (2, 51, 1) /*INPUT_CONTEXT (e.g. E button)*/

gtaforums.com/topic/819070-c-draw-instructional-buttons-scaleform-movie/#entry1068197378

control: unused parameter

---

### GET_CONTROL_GROUP_INSTRUCTIONAL_BUTTONS_STRING

**Hash:** `0x80C2FD58D720C801` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CONTROL_GROUP_INSTRUCTIONAL_BUTTONS_STRING(int control, int controlGroup, BOOL allowXOSwap)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CONTROL_GROUP_INSTRUCTIONAL_BUTTONS_STRING, control, controlGroup, allowXOSwap);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `controlGroup` | `int` | `int` | |
| `allowXOSwap` | `BOOL` | `bool` | |

**返回值:** `const char*` → `string`

**说明:** control: unused parameter

---

### SET_CONTROL_LIGHT_EFFECT_COLOR

**Hash:** `0x8290252FFF36ACB5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONTROL_LIGHT_EFFECT_COLOR(int control, int red, int green, int blue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONTROL_LIGHT_EFFECT_COLOR, control, red, green, blue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `red` | `int` | `int` | |
| `green` | `int` | `int` | |
| `blue` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### CLEAR_CONTROL_LIGHT_EFFECT

**Hash:** `0xCB0360EFEFB2580D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_CONTROL_LIGHT_EFFECT(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_CONTROL_LIGHT_EFFECT, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### SET_CONTROL_SHAKE

**Hash:** `0x48B3886C1358D0D5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONTROL_SHAKE(int control, int duration, int frequency)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONTROL_SHAKE, control, duration, frequency);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `duration` | `int` | `int` | |
| `frequency` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED
duration in milliseconds 
frequency should range from about 10 (slow vibration) to 255 (very fast)

example:
SET_CONTROL_SHAKE(PLAYER_CONTROL, 100, 200);

---

### SET_CONTROL_TRIGGER_SHAKE

**Hash:** `0x14D29BB12D47F68C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONTROL_TRIGGER_SHAKE(int control, int leftDuration, int leftFrequency, int rightDuration, int rightFrequency)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONTROL_TRIGGER_SHAKE, control, leftDuration, leftFrequency, rightDuration, rightFrequency);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `leftDuration` | `int` | `int` | |
| `leftFrequency` | `int` | `int` | |
| `rightDuration` | `int` | `int` | |
| `rightFrequency` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### STOP_CONTROL_SHAKE

**Hash:** `0x38C16A305E8CDC8D` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_CONTROL_SHAKE(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_CONTROL_SHAKE, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### SET_CONTROL_SHAKE_SUPPRESSED_ID

**Hash:** `0xF239400E16C23E08` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONTROL_SHAKE_SUPPRESSED_ID(int control, int uniqueId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONTROL_SHAKE_SUPPRESSED_ID, control, uniqueId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `uniqueId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### CLEAR_CONTROL_SHAKE_SUPPRESSED_ID

**Hash:** `0xA0CEFCEA390AAB9B` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_CONTROL_SHAKE_SUPPRESSED_ID(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_CONTROL_SHAKE_SUPPRESSED_ID, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### IS_LOOK_INVERTED

**Hash:** `0x77B612531280010D` | **JHash:** `0x313434B2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_LOOK_INVERTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_LOOK_INVERTED);
```

**返回值:** `BOOL` → `bool`

---

### IS_MOUSE_LOOK_INVERTED

**Hash:** `0xE1615EC03B3BB4FD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOUSE_LOOK_INVERTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOUSE_LOOK_INVERTED);
```

**返回值:** `BOOL` → `bool`

**说明:** Used with IS_LOOK_INVERTED() and negates its affect.

--

Not sure how the person above got that description, but here's an actual example:

if (PAD::IS_USING_KEYBOARD_AND_MOUSE(2)) {
    if (a_5) {
        if (PAD::IS_LOOK_INVERTED()) {
            a_3 *= -1;
        }
        if (PAD::IS_MOUSE_LOOK_INVERTED()) {
            a_3 *= -1;
        }
    }
}

---

### GET_LOCAL_PLAYER_AIM_STATE

**Hash:** `0xBB41AFBBBC0A0287` | **JHash:** `0x81802053` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LOCAL_PLAYER_AIM_STATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LOCAL_PLAYER_AIM_STATE);
```

**返回值:** `int` → `int`

**说明:** Hard-coded to return 3 if using KBM, otherwise same behavior as GET_LOCAL_PLAYER_GAMEPAD_AIM_STATE.

---

### GET_LOCAL_PLAYER_GAMEPAD_AIM_STATE

**Hash:** `0x59B9A7AF4C95133C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LOCAL_PLAYER_GAMEPAD_AIM_STATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LOCAL_PLAYER_GAMEPAD_AIM_STATE);
```

**返回值:** `int` → `int`

**说明:** Returns the local player's targeting mode. See PLAYER::SET_PLAYER_TARGETING_MODE.

---

### GET_IS_USING_ALTERNATE_HANDBRAKE

**Hash:** `0x25AAA32BDC98F2A3` | **Build:** `1365`

**C++ 原型:**
```cpp
BOOL GET_IS_USING_ALTERNATE_HANDBRAKE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_USING_ALTERNATE_HANDBRAKE);
```

**返回值:** `BOOL` → `bool`

---

### GET_IS_USING_ALTERNATE_DRIVEBY

**Hash:** `0x0F70731BACCFBB96` | **JHash:** `0xC1AFABD5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_USING_ALTERNATE_DRIVEBY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_USING_ALTERNATE_DRIVEBY);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns profile setting 225.

---

### GET_ALLOW_MOVEMENT_WHILE_ZOOMED

**Hash:** `0xFC859E2374407556` | **JHash:** `0xC0823820` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_ALLOW_MOVEMENT_WHILE_ZOOMED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ALLOW_MOVEMENT_WHILE_ZOOMED);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns profile setting 17.

---

### SET_PLAYERPAD_SHAKES_WHEN_CONTROLLER_DISABLED

**Hash:** `0x798FDEB5B1575088` | **JHash:** `0xA86BD91F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYERPAD_SHAKES_WHEN_CONTROLLER_DISABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYERPAD_SHAKES_WHEN_CONTROLLER_DISABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INPUT_EXCLUSIVE

**Hash:** `0xEDE476E5EE29EDB1` | **JHash:** `0x4E8E29E6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INPUT_EXCLUSIVE(int control, int action)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INPUT_EXCLUSIVE, control, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### DISABLE_CONTROL_ACTION

**Hash:** `0xFE99B66D079CF6BC` | **JHash:** `0x3800C0DC` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_CONTROL_ACTION(int control, int action, BOOL disableRelatedActions)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_CONTROL_ACTION, control, action, disableRelatedActions);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |
| `disableRelatedActions` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### ENABLE_CONTROL_ACTION

**Hash:** `0x351220255D64C155` | **JHash:** `0xD2753551` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_CONTROL_ACTION(int control, int action, BOOL enableRelatedActions)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_CONTROL_ACTION, control, action, enableRelatedActions);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |
| `action` | `int` | `int` | |
| `enableRelatedActions` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### DISABLE_ALL_CONTROL_ACTIONS

**Hash:** `0x5F4B6931816E599B` | **JHash:** `0x16753CF4` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_ALL_CONTROL_ACTIONS(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_ALL_CONTROL_ACTIONS, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### ENABLE_ALL_CONTROL_ACTIONS

**Hash:** `0xA5FFE9B05F199DE7` | **JHash:** `0xFC2F119F` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_ALL_CONTROL_ACTIONS(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_ALL_CONTROL_ACTIONS, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### INIT_PC_SCRIPTED_CONTROLS

**Hash:** `0x3D42B92563939375` | **JHash:** `0xD2C80B2E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL INIT_PC_SCRIPTED_CONTROLS(const char* schemeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.INIT_PC_SCRIPTED_CONTROLS, schemeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `schemeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Used in carsteal3 script with schemeName = "Carsteal4_spycar".

---

### SWITCH_PC_SCRIPTED_CONTROLS

**Hash:** `0x4683149ED1DDE7A1` | **JHash:** `0xBBFC9050` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SWITCH_PC_SCRIPTED_CONTROLS(const char* schemeName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SWITCH_PC_SCRIPTED_CONTROLS, schemeName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `schemeName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Same as INIT_PC_SCRIPTED_CONTROLS

---

### SHUTDOWN_PC_SCRIPTED_CONTROLS

**Hash:** `0x643ED62D5EA3BEBD` | **JHash:** `0x42140FF9` | **Build:** `323`

**C++ 原型:**
```cpp
void SHUTDOWN_PC_SCRIPTED_CONTROLS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHUTDOWN_PC_SCRIPTED_CONTROLS);
```

**返回值:** `void` → `void`

---

### ALLOW_ALTERNATIVE_SCRIPT_CONTROLS_LAYOUT

**Hash:** `0x7F4724035FDCA1DD` | **JHash:** `0x2CEDE6C5` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_ALTERNATIVE_SCRIPT_CONTROLS_LAYOUT(int control)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_ALTERNATIVE_SCRIPT_CONTROLS_LAYOUT, control);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `control` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** control: see IS_CONTROL_ENABLED

---

### _GET_GAMEPAD_TYPE

**Hash:** `0x18E474F40EF05F10` | **Build:** `3570`

**C++ 原型:**
```cpp
int _GET_GAMEPAD_TYPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_GAMEPAD_TYPE);
```

**返回值:** `int` → `int`

**说明:** Always returns zero.

---


---

## PATHFIND

**包含 60 个 Native 函数**

### SET_ROADS_IN_AREA

**Hash:** `0xBF1A602B5BA52FEE` | **JHash:** `0xEBC7B918` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROADS_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL nodeEnabled, BOOL unknown2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROADS_IN_AREA, x1, y1, z1, x2, y2, z2, nodeEnabled, unknown2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `nodeEnabled` | `BOOL` | `bool` | |
| `unknown2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When nodeEnabled is set to false, all nodes in the area get disabled.
`GET_VEHICLE_NODE_IS_SWITCHED_OFF` returns true afterwards.
If it's true, `GET_VEHICLE_NODE_IS_SWITCHED_OFF` returns false.

---

### SET_ROADS_IN_ANGLED_AREA

**Hash:** `0x1A5AA1208AF5DB59` | **JHash:** `0xBD088F4B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROADS_IN_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width, BOOL unknown1, BOOL unknown2, BOOL unknown3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROADS_IN_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width, unknown1, unknown2, unknown3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `unknown1` | `BOOL` | `bool` | |
| `unknown2` | `BOOL` | `bool` | |
| `unknown3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** unknown3 is related to `SEND_SCRIPT_WORLD_STATE_EVENT > CNetworkRoadNodeWorldStateData` in networked environments.
See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.

---

### SET_PED_PATHS_IN_AREA

**Hash:** `0x34F060F4BF92E018` | **JHash:** `0x2148EA84` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATHS_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL p6, Any p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATHS_IN_AREA, x1, y1, z1, x2, y2, z2, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_SAFE_COORD_FOR_PED

**Hash:** `0xB61C8E878A4199CA` | **JHash:** `0xB370270A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_SAFE_COORD_FOR_PED(float x, float y, float z, BOOL onGround, Vector3* outPosition, int flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SAFE_COORD_FOR_PED, x, y, z, onGround, outPosition, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `onGround` | `BOOL` | `bool` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `flags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Flags are:
1 = 1 = B02_IsFootpath
2 = 4 = !B15_InteractionUnk
4 = 0x20 = !B14_IsInterior
8 = 0x40 = !B07_IsWater
16 = 0x200 = B17_IsFlatGround
When onGround == true outPosition is a position located on the nearest pavement.

When a safe coord could not be found the result of a function is false and outPosition == Vector3.Zero.

In the scripts these flags are used: 0, 14, 12, 16, 20, 21, 28. 0 is most commonly used, then 16. 

16 works for me, 0 crashed the script.

---

### GET_CLOSEST_VEHICLE_NODE

**Hash:** `0x240A18690AE96513` | **JHash:** `0x6F5F1E6C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_VEHICLE_NODE(float x, float y, float z, Vector3* outPosition, int nodeFlags, float p5, float p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_VEHICLE_NODE, x, y, z, outPosition, nodeFlags, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `nodeFlags` | `int` | `int` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** https://gtaforums.com/topic/843561-pathfind-node-types

---

### GET_CLOSEST_MAJOR_VEHICLE_NODE

**Hash:** `0x2EABE3B06F58C1BE` | **JHash:** `0x04B5F15B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_MAJOR_VEHICLE_NODE(float x, float y, float z, Vector3* outPosition, float unknown1, float unknown2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_MAJOR_VEHICLE_NODE, x, y, z, outPosition, unknown1, unknown2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `unknown1` | `float` | `float` | |
| `unknown2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Get the closest vehicle node to a given position.

---

### GET_CLOSEST_VEHICLE_NODE_WITH_HEADING

**Hash:** `0xFF071FB798B803B0` | **JHash:** `0x8BD5759B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_VEHICLE_NODE_WITH_HEADING(float x, float y, float z, Vector3* outPosition, float* outHeading, int nodeType, float p6, float p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_VEHICLE_NODE_WITH_HEADING, x, y, z, outPosition, outHeading, nodeType, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `outHeading` | `float*` | `float*` | |
| `nodeType` | `int` | `int` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** p5, p6 and p7 seems to be about the same as p4, p5 and p6 for GET_CLOSEST_VEHICLE_NODE. p6 and/or p7 has something to do with finding a node on the same path/road and same direction(at least for this native, something to do with the heading maybe). Edit this when you find out more.

nodeType: 0 = main roads, 1 = any dry path, 3 = water
p6 is always 3.0
p7 is always 0

gtaforums.com/topic/843561-pathfind-node-types

Example of usage, moving vehicle to closest path/road:
Vector3 coords = ENTITY::GET_ENTITY_COORDS(playerVeh, true);
Vector3 closestVehicleNodeCoords; 
float roadHeading; 
PATHFIND::GET_CLOSEST_VEHICLE_NODE_WITH_HEADING(coords.x, coords.y, coords.z, &closestVehicleNodeCoords, &roadHeading, 1, 3, 0); 
ENTITY::SET_ENTITY_HEADING(playerVeh, roadHeading);
ENTITY::SET_ENTITY_COORDS(playerVeh, closestVehicleNodeCoords.x, closestVehicleNodeCoords.y, closestVehicleNodeCoords.z, 1, 0, 0, 1);
VEHICLE::SET_VEHICLE_ON_GROUND_PROPERLY(playerVeh);

------------------------------------------------------------------
C# Example (ins1de) : https://pastebin.com/fxtMWAHD

---

### GET_NTH_CLOSEST_VEHICLE_NODE

**Hash:** `0xE50E52416CCF948B` | **JHash:** `0xF125BFCC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_NTH_CLOSEST_VEHICLE_NODE(float x, float y, float z, int nthClosest, Vector3* outPosition, int nodeFlags, float unknown1, float unknown2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_NTH_CLOSEST_VEHICLE_NODE, x, y, z, nthClosest, outPosition, nodeFlags, unknown1, unknown2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `nthClosest` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `nodeFlags` | `int` | `int` | |
| `unknown1` | `float` | `float` | |
| `unknown2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### GET_NTH_CLOSEST_VEHICLE_NODE_ID

**Hash:** `0x22D7275A79FE8215` | **JHash:** `0x3F358BEA` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NTH_CLOSEST_VEHICLE_NODE_ID(float x, float y, float z, int nth, int nodeFlags, float p5, float p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NTH_CLOSEST_VEHICLE_NODE_ID, x, y, z, nth, nodeFlags, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `nth` | `int` | `int` | |
| `nodeFlags` | `int` | `int` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** Returns the id.

---

### GET_NTH_CLOSEST_VEHICLE_NODE_WITH_HEADING

**Hash:** `0x80CA6A8B6C094CC4` | **JHash:** `0x7349C856` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_NTH_CLOSEST_VEHICLE_NODE_WITH_HEADING(float x, float y, float z, int nthClosest, Vector3* outPosition, float* outHeading, int* outNumLanes, int nodeFlags, float unknown3, float unknown4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_NTH_CLOSEST_VEHICLE_NODE_WITH_HEADING, x, y, z, nthClosest, outPosition, outHeading, outNumLanes, nodeFlags, unknown3, unknown4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `nthClosest` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `outHeading` | `float*` | `float*` | |
| `outNumLanes` | `int*` | `int*` | |
| `nodeFlags` | `int` | `int` | |
| `unknown3` | `float` | `float` | |
| `unknown4` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Get the nth closest vehicle node and its heading.

---

### GET_NTH_CLOSEST_VEHICLE_NODE_ID_WITH_HEADING

**Hash:** `0x6448050E9C2A7207` | **JHash:** `0xC1AEB88D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NTH_CLOSEST_VEHICLE_NODE_ID_WITH_HEADING(float x, float y, float z, int nthClosest, Vector3* outPosition, float* outHeading, int nodeFlags, float p7, float p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NTH_CLOSEST_VEHICLE_NODE_ID_WITH_HEADING, x, y, z, nthClosest, outPosition, outHeading, nodeFlags, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `nthClosest` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `outHeading` | `float*` | `float*` | |
| `nodeFlags` | `int` | `int` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |

**返回值:** `int` → `int`

---

### GET_NTH_CLOSEST_VEHICLE_NODE_FAVOUR_DIRECTION

**Hash:** `0x45905BE8654AE067` | **JHash:** `0x928A4DEC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_NTH_CLOSEST_VEHICLE_NODE_FAVOUR_DIRECTION(float x, float y, float z, float desiredX, float desiredY, float desiredZ, int nthClosest, Vector3* outPosition, float* outHeading, int nodeFlags, float p10, float p11)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_NTH_CLOSEST_VEHICLE_NODE_FAVOUR_DIRECTION, x, y, z, desiredX, desiredY, desiredZ, nthClosest, outPosition, outHeading, nodeFlags, p10, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `desiredX` | `float` | `float` | |
| `desiredY` | `float` | `float` | |
| `desiredZ` | `float` | `float` | |
| `nthClosest` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `outHeading` | `float*` | `float*` | |
| `nodeFlags` | `int` | `int` | |
| `p10` | `float` | `float` | |
| `p11` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** See gtaforums.com/topic/843561-pathfind-node-types for node type info. 0 = paved road only, 1 = any road, 3 = water

p10 always equals 3.0
p11 always equals 0

---

### GET_VEHICLE_NODE_PROPERTIES

**Hash:** `0x0568566ACBB5DEDC` | **JHash:** `0xCC90110B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_NODE_PROPERTIES(float x, float y, float z, int* density, int* flags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_NODE_PROPERTIES, x, y, z, density, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `density` | `int*` | `int*` | |
| `flags` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets the density and flags of the closest node to the specified position.
Density is a value between 0 and 15, indicating how busy the road is.
Flags is a bit field.

---

### IS_VEHICLE_NODE_ID_VALID

**Hash:** `0x1EAF30FCFBF5AF74` | **JHash:** `0x57DFB1EF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_NODE_ID_VALID(int vehicleNodeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_NODE_ID_VALID, vehicleNodeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleNodeId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the id is non zero.

---

### GET_VEHICLE_NODE_POSITION

**Hash:** `0x703123E5E7D429C2` | **JHash:** `0xE38E252D` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_NODE_POSITION(int nodeId, Vector3* outPosition)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_NODE_POSITION, nodeId, outPosition);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeId` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |

**返回值:** `void` → `void`

**说明:** Calling this with an invalid node id, will crash the game.
Note that IS_VEHICLE_NODE_ID_VALID simply checks if nodeId is not zero. It does not actually ensure that the id is valid.
Eg. IS_VEHICLE_NODE_ID_VALID(1) will return true, but will crash when calling GET_VEHICLE_NODE_POSITION().

---

### GET_VEHICLE_NODE_IS_GPS_ALLOWED

**Hash:** `0xA2AE5C478B96E3B6` | **JHash:** `0xEE4B1219` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_NODE_IS_GPS_ALLOWED(int nodeID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_NODE_IS_GPS_ALLOWED, nodeID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns false for nodes that aren't used for GPS routes.
Example:
Nodes in Fort Zancudo and LSIA are false

---

### GET_VEHICLE_NODE_IS_SWITCHED_OFF

**Hash:** `0x4F5070AA58F69279` | **JHash:** `0x56737A3C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_NODE_IS_SWITCHED_OFF(int nodeID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_NODE_IS_SWITCHED_OFF, nodeID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true when the node is Offroad. Alleys, some dirt roads, and carparks return true.
Normal roads where plenty of Peds spawn will return false

---

### GET_CLOSEST_ROAD

**Hash:** `0x132F52BBA570FE92` | **JHash:** `0x567B0E11` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_ROAD(float x, float y, float z, float p3, int p4, Vector3* p5, Vector3* p6, Any* p7, Any* p8, float* p9, BOOL p10)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_ROAD, x, y, z, p3, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `int` | `int` | |
| `p5` | `Vector3*` | `Vector3*` | |
| `p6` | `Vector3*` | `Vector3*` | |
| `p7` | `Any*` | `object*` | |
| `p8` | `Any*` | `object*` | |
| `p9` | `float*` | `float*` | |
| `p10` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 seems to be always 1.0f in the scripts

---

### LOAD_ALL_PATH_NODES

**Hash:** `0xC2AB6BFE34E92F8B` | **Build:** `2802`

**C++ 原型:**
```cpp
BOOL LOAD_ALL_PATH_NODES(BOOL set)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LOAD_ALL_PATH_NODES, set);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `set` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SET_ALLOW_STREAM_PROLOGUE_NODES

**Hash:** `0x228E5C6AD4D74BFD` | **JHash:** `0xD6A3B458` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALLOW_STREAM_PROLOGUE_NODES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_STREAM_PROLOGUE_NODES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_STREAM_HEIST_ISLAND_NODES

**Hash:** `0xF74B1FFA4A15FBEA` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_ALLOW_STREAM_HEIST_ISLAND_NODES(int type)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_STREAM_HEIST_ISLAND_NODES, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Activates Cayo Perico path nodes if passed `1`. GPS navigation will start working, maybe more stuff will change, not sure. It seems if you try to unload (pass `0`) when close to the island, your game might crash.

---

### ARE_NODES_LOADED_FOR_AREA

**Hash:** `0xF7B79A50B905A30D` | **JHash:** `0x86E80A17` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_NODES_LOADED_FOR_AREA(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_NODES_LOADED_FOR_AREA, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_PATH_NODES_IN_AREA_THIS_FRAME

**Hash:** `0x07FB139B592FA687` | **JHash:** `0x2CDA5012` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_PATH_NODES_IN_AREA_THIS_FRAME(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_PATH_NODES_IN_AREA_THIS_FRAME, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Used internally for long range tasks

---

### SET_ROADS_BACK_TO_ORIGINAL

**Hash:** `0x1EE7063B80FFC77C` | **JHash:** `0x86AC4A85` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROADS_BACK_TO_ORIGINAL(float p0, float p1, float p2, float p3, float p4, float p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROADS_BACK_TO_ORIGINAL, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_ROADS_BACK_TO_ORIGINAL_IN_ANGLED_AREA

**Hash:** `0x0027501B9F3B407E` | **JHash:** `0x9DB5D209` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ROADS_BACK_TO_ORIGINAL_IN_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width, Any p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROADS_BACK_TO_ORIGINAL_IN_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `p7` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** See IS_POINT_IN_ANGLED_AREA for the definition of an angled area.
bool p7 - always 1

---

### SET_AMBIENT_PED_RANGE_MULTIPLIER_THIS_FRAME

**Hash:** `0x0B919E1FB47CC4E0` | **JHash:** `0x3C5085E4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_PED_RANGE_MULTIPLIER_THIS_FRAME(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_PED_RANGE_MULTIPLIER_THIS_FRAME, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ADJUST_AMBIENT_PED_SPAWN_DENSITIES_THIS_FRAME

**Hash:** `0xAA76052DDA9BFC3E` | **JHash:** `0xD0F51299` | **Build:** `323`

**C++ 原型:**
```cpp
void ADJUST_AMBIENT_PED_SPAWN_DENSITIES_THIS_FRAME(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADJUST_AMBIENT_PED_SPAWN_DENSITIES_THIS_FRAME, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_PATHS_BACK_TO_ORIGINAL

**Hash:** `0xE04B48F2CC926253` | **JHash:** `0x3F1ABDA4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATHS_BACK_TO_ORIGINAL(float x1, float y1, float z1, float x2, float y2, float z2, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATHS_BACK_TO_ORIGINAL, x1, y1, z1, x2, y2, z2, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p6 is always 0

---

### GET_RANDOM_VEHICLE_NODE

**Hash:** `0x93E0DB8440B73A7D` | **JHash:** `0xAD1476EA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_RANDOM_VEHICLE_NODE(float x, float y, float z, float radius, BOOL p4, BOOL p5, BOOL p6, Vector3* outPosition, int* nodeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_RANDOM_VEHICLE_NODE, x, y, z, radius, p4, p5, p6, outPosition, nodeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `outPosition` | `Vector3*` | `Vector3*` | |
| `nodeId` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### GET_SPAWN_COORDS_FOR_VEHICLE_NODE

**Hash:** `0x809549AFC7AEC597` | **Build:** `2944`

**C++ 原型:**
```cpp
void GET_SPAWN_COORDS_FOR_VEHICLE_NODE(int nodeAddress, float towardsCoorsX, float towardsCoorsY, float towardsCoorsZ, Vector3* centrePoint, float* heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_SPAWN_COORDS_FOR_VEHICLE_NODE, nodeAddress, towardsCoorsX, towardsCoorsY, towardsCoorsZ, centrePoint, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeAddress` | `int` | `int` | |
| `towardsCoorsX` | `float` | `float` | |
| `towardsCoorsY` | `float` | `float` | |
| `towardsCoorsZ` | `float` | `float` | |
| `centrePoint` | `Vector3*` | `Vector3*` | |
| `heading` | `float*` | `float*` | |

**返回值:** `void` → `void`

---

### GET_STREET_NAME_AT_COORD

**Hash:** `0x2EB41072B4C1E4C0` | **JHash:** `0xDEBEEFCF` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_STREET_NAME_AT_COORD(float x, float y, float z, Hash* streetName, Hash* crossingRoad)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_STREET_NAME_AT_COORD, x, y, z, streetName, crossingRoad);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `streetName` | `Hash*` | `uint*` | |
| `crossingRoad` | `Hash*` | `uint*` | |

**返回值:** `void` → `void`

**说明:** Determines the name of the street which is the closest to the given coordinates.

x,y,z - the coordinates of the street
streetName - returns a hash to the name of the street the coords are on
crossingRoad - if the coordinates are on an intersection, a hash to the name of the crossing road

Note: the names are returned as hashes, the strings can be returned using the function HUD::GET_STREET_NAME_FROM_HASH_KEY.

---

### GENERATE_DIRECTIONS_TO_COORD

**Hash:** `0xF90125F1F79ECDF8` | **JHash:** `0xED35C094` | **Build:** `323`

**C++ 原型:**
```cpp
int GENERATE_DIRECTIONS_TO_COORD(float x, float y, float z, BOOL p3, int* direction, float* p5, float* distToNxJunction)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GENERATE_DIRECTIONS_TO_COORD, x, y, z, p3, direction, p5, distToNxJunction);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `BOOL` | `bool` | |
| `direction` | `int*` | `int*` | |
| `p5` | `float*` | `float*` | |
| `distToNxJunction` | `float*` | `float*` | |

**返回值:** `int` → `int`

**说明:** p3 is 0 in the only game script occurrence (trevor3) but 1 doesn't seem to make a difference

distToNxJunction seems to be the distance in metres * 10.0f

direction:
0 = This happens randomly during the drive for seemingly no reason but if you consider that this native is only used in trevor3, it seems to mean "Next frame, stop whatever's being said and tell the player the direction."
1 = Route is being calculated or the player is going in the wrong direction
2 = Please Proceed the Highlighted Route
3 = In (distToNxJunction) Turn Left
4 = In (distToNxJunction) Turn Right
5 = In (distToNxJunction) Keep Straight
6 = In (distToNxJunction) Turn Sharply To The Left
7 = In (distToNxJunction) Turn Sharply To The Right
8 = Route is being recalculated or the navmesh is confusing. This happens randomly during the drive but consistently at {2044.0358, 2996.6116, 44.9717} if you face towards the bar and the route needs you to turn right. In that particular case, it could be a bug with how the turn appears to be 270 deg. CCW instead of "right." Either way, this seems to be the engine saying "I don't know the route right now."

return value set to 0 always

---

### SET_IGNORE_NO_GPS_FLAG

**Hash:** `0x72751156E7678833` | **JHash:** `0xB72CF194` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IGNORE_NO_GPS_FLAG(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IGNORE_NO_GPS_FLAG, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IGNORE_NO_GPS_FLAG_UNTIL_FIRST_NORMAL_NODE

**Hash:** `0x1FC289A0C3FF470F` | **JHash:** `0x90DF7A4C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IGNORE_NO_GPS_FLAG_UNTIL_FIRST_NORMAL_NODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IGNORE_NO_GPS_FLAG_UNTIL_FIRST_NORMAL_NODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** See: SET_BLIP_ROUTE

---

### SET_GPS_DISABLED_ZONE

**Hash:** `0xDC20483CD3DD5201` | **JHash:** `0x720B8073` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_DISABLED_ZONE(float x1, float y1, float z1, float x2, float y2, float z3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_DISABLED_ZONE, x1, y1, z1, x2, y2, z3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_GPS_BLIP_ROUTE_LENGTH

**Hash:** `0xBBB45C3CF5C8AA85` | **JHash:** `0x4B770634` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_GPS_BLIP_ROUTE_LENGTH(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_GPS_BLIP_ROUTE_LENGTH);
```

**返回值:** `int` → `int`

---

### GET_POS_ALONG_GPS_TYPE_ROUTE

**Hash:** `0xF3162836C28F9DA5` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_POS_ALONG_GPS_TYPE_ROUTE(Vector3* result, BOOL p1, float p2, int p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_POS_ALONG_GPS_TYPE_ROUTE, result, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `result` | `Vector3*` | `Vector3*` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `float` | `float` | |
| `p3` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p3 can be 0, 1 or 2.

---

### GET_GPS_BLIP_ROUTE_FOUND

**Hash:** `0x869DAACBBE9FA006` | **JHash:** `0x286F82CC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_GPS_BLIP_ROUTE_FOUND(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_GPS_BLIP_ROUTE_FOUND);
```

**返回值:** `BOOL` → `bool`

---

### GET_ROAD_BOUNDARY_USING_HEADING

**Hash:** `0xA0F8A7517A273C05` | **JHash:** `0x5E440AC7` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL GET_ROAD_BOUNDARY_USING_HEADING(float x, float y, float z, float heading, Vector3* outPosition)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ROAD_BOUNDARY_USING_HEADING, x, y, z, heading, outPosition);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `outPosition` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

---

### GET_POSITION_BY_SIDE_OF_ROAD

**Hash:** `0x16F46FB18C8009E4` | **JHash:** `0xF6422F9A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_POSITION_BY_SIDE_OF_ROAD(float x, float y, float z, int p3, Vector3* outPosition)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_POSITION_BY_SIDE_OF_ROAD, x, y, z, p3, outPosition);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p3` | `int` | `int` | |
| `outPosition` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

---

### IS_POINT_ON_ROAD

**Hash:** `0x125BF4ABFC536B09` | **JHash:** `0xCF198055` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_POINT_ON_ROAD(float x, float y, float z, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_POINT_ON_ROAD, x, y, z, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified position is on a road.
The vehicle parameter is not implemented (ignored).

---

### GET_NEXT_GPS_DISABLED_ZONE_INDEX

**Hash:** `0xD3A6A0EF48823A8C` | **JHash:** `0xBE92551F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NEXT_GPS_DISABLED_ZONE_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NEXT_GPS_DISABLED_ZONE_INDEX);
```

**返回值:** `int` → `int`

**说明:** Gets the next zone that has been disabled using SET_GPS_DISABLED_ZONE_AT_INDEX.

---

### SET_GPS_DISABLED_ZONE_AT_INDEX

**Hash:** `0xD0BC1C6FB18EE154` | **JHash:** `0x98BDB311` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GPS_DISABLED_ZONE_AT_INDEX(float x1, float y1, float z1, float x2, float y2, float z2, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GPS_DISABLED_ZONE_AT_INDEX, x1, y1, z1, x2, y2, z2, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Disables the GPS route displayed on the minimap while within a certain zone (area). When in a disabled zone and creating a waypoint, the GPS route is not shown on the minimap until you are outside of the zone. When disabled, the direct distance is shown on minimap opposed to distance to travel. Seems to only work before setting a waypoint.
You can clear the disabled zone with CLEAR_GPS_DISABLED_ZONE_AT_INDEX.

**Setting a waypoint at the same coordinate:**
Disabled Zone: https://i.imgur.com/P9VUuxM.png
Enabled Zone (normal): https://i.imgur.com/BPi24aw.png

---

### CLEAR_GPS_DISABLED_ZONE_AT_INDEX

**Hash:** `0x2801D0012266DF07` | **JHash:** `0x59090745` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_GPS_DISABLED_ZONE_AT_INDEX(int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_GPS_DISABLED_ZONE_AT_INDEX, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Clears a disabled GPS route area from a certain index previously set using `SET_GPS_DISABLED_ZONE_AT_INDEX`.

---

### ADD_NAVMESH_REQUIRED_REGION

**Hash:** `0x387EAD7EE42F6685` | **JHash:** `0x12B086EA` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_NAVMESH_REQUIRED_REGION(float x, float y, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_NAVMESH_REQUIRED_REGION, x, y, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### REMOVE_NAVMESH_REQUIRED_REGIONS

**Hash:** `0x916F0A3CDEC3445E` | **JHash:** `0x637BB680` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_NAVMESH_REQUIRED_REGIONS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_NAVMESH_REQUIRED_REGIONS);
```

**返回值:** `void` → `void`

---

### IS_NAVMESH_REQUIRED_REGION_IN_USE

**Hash:** `0x705A844002B39DC0` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL IS_NAVMESH_REQUIRED_REGION_IN_USE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NAVMESH_REQUIRED_REGION_IN_USE);
```

**返回值:** `BOOL` → `bool`

---

### DISABLE_NAVMESH_IN_AREA

**Hash:** `0x4C8872D8CDBE1B8B` | **JHash:** `0x6E37F132` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_NAVMESH_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_NAVMESH_IN_AREA, x1, y1, z1, x2, y2, z2, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Set toggle true to disable navmesh.
Set toggle false to enable navmesh.

---

### ARE_ALL_NAVMESH_REGIONS_LOADED

**Hash:** `0x8415D95B194A3AEA` | **JHash:** `0x34C4E789` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ALL_NAVMESH_REGIONS_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ALL_NAVMESH_REGIONS_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### IS_NAVMESH_LOADED_IN_AREA

**Hash:** `0xF813C7E63F9062A5` | **JHash:** `0x4C2BA99E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NAVMESH_LOADED_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NAVMESH_LOADED_IN_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether navmesh for the region is loaded. The region is a rectangular prism defined by it's top left deepest corner to it's bottom right shallowest corner.

If you can re-word this so it makes more sense, please do. I'm horrible with words sometimes...

---

### GET_NUM_NAVMESHES_EXISTING_IN_AREA

**Hash:** `0x01708E8DD3FF8C65` | **JHash:** `0x76AFFBB5` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_NAVMESHES_EXISTING_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_NAVMESHES_EXISTING_IN_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `int` → `int`

---

### ADD_NAVMESH_BLOCKING_OBJECT

**Hash:** `0xFCD5C8E06E502F5A` | **JHash:** `0x2952BA56` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_NAVMESH_BLOCKING_OBJECT(float p0, float p1, float p2, float p3, float p4, float p5, float p6, BOOL p7, Any p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_NAVMESH_BLOCKING_OBJECT, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### UPDATE_NAVMESH_BLOCKING_OBJECT

**Hash:** `0x109E99373F290687` | **JHash:** `0x4E9776D0` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_NAVMESH_BLOCKING_OBJECT(Any p0, float p1, float p2, float p3, float p4, float p5, float p6, float p7, Any p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_NAVMESH_BLOCKING_OBJECT, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REMOVE_NAVMESH_BLOCKING_OBJECT

**Hash:** `0x46399A7895957C0E` | **JHash:** `0x098602B0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_NAVMESH_BLOCKING_OBJECT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_NAVMESH_BLOCKING_OBJECT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DOES_NAVMESH_BLOCKING_OBJECT_EXIST

**Hash:** `0x0EAEB0DB4B132399` | **JHash:** `0x4B67D7EE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_NAVMESH_BLOCKING_OBJECT_EXIST(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_NAVMESH_BLOCKING_OBJECT_EXIST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_APPROX_HEIGHT_FOR_POINT

**Hash:** `0x29C24BFBED8AB8FB` | **JHash:** `0x3FE8C5A0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_APPROX_HEIGHT_FOR_POINT(float x, float y)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_APPROX_HEIGHT_FOR_POINT, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Returns CGameWorldHeightMap's maximum Z value at specified point (grid node).

---

### GET_APPROX_HEIGHT_FOR_AREA

**Hash:** `0x8ABE8608576D9CE3` | **JHash:** `0x3ED21C90` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_APPROX_HEIGHT_FOR_AREA(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_APPROX_HEIGHT_FOR_AREA, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Returns CGameWorldHeightMap's maximum Z among all grid nodes that intersect with the specified rectangle.

---

### GET_APPROX_FLOOR_FOR_POINT

**Hash:** `0x336511A34F2E5185` | **JHash:** `0xA07C5B7D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_APPROX_FLOOR_FOR_POINT(float x, float y)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_APPROX_FLOOR_FOR_POINT, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Returns CGameWorldHeightMap's minimum Z value at specified point (grid node).

---

### GET_APPROX_FLOOR_FOR_AREA

**Hash:** `0x3599D741C9AC6310` | **JHash:** `0x76751DD4` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_APPROX_FLOOR_FOR_AREA(float x1, float y1, float x2, float y2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_APPROX_FLOOR_FOR_AREA, x1, y1, x2, y2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Returns CGameWorldHeightMap's minimum Z among all grid nodes that intersect with the specified rectangle.

---

### CALCULATE_TRAVEL_DISTANCE_BETWEEN_POINTS

**Hash:** `0xADD95C7005C4A197` | **JHash:** `0xB114489B` | **Build:** `323`

**C++ 原型:**
```cpp
float CALCULATE_TRAVEL_DISTANCE_BETWEEN_POINTS(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.CALCULATE_TRAVEL_DISTANCE_BETWEEN_POINTS, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** Calculates the travel distance between a set of points.

Doesn't seem to correlate with distance on gps sometimes.
This function returns the value 100000.0 over long distances, seems to be a failure mode result, potentially occurring when not all path nodes are loaded into pathfind.

---


---

## PED

**包含 615 个 Native 函数**

### CREATE_PED

**Hash:** `0xD49F9B0955C367DE` | **JHash:** `0x0389EF71` | **Build:** `323`

**C++ 原型:**
```cpp
Ped CREATE_PED(int pedType, Hash modelHash, float x, float y, float z, float heading, BOOL isNetwork, BOOL bScriptHostPed)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PED, pedType, modelHash, x, y, z, heading, isNetwork, bScriptHostPed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedType` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostPed` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

**说明:** https://alloc8or.re/gta5/doc/enums/ePedType.txt

Full list of peds by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/peds.json

---

### DELETE_PED

**Hash:** `0x9614299DCB53E54B` | **JHash:** `0x13EFB9A0` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_PED(Ped* ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Deletes the specified ped, then sets the handle pointed to by the pointer to NULL.

---

### CLONE_PED

**Hash:** `0xEF29A16337FACADB` | **JHash:** `0x8C8A8D6E` | **Build:** `323`

**C++ 原型:**
```cpp
Ped CLONE_PED(Ped ped, BOOL isNetwork, BOOL bScriptHostPed, BOOL copyHeadBlendFlag)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CLONE_PED, ped, isNetwork, bScriptHostPed, copyHeadBlendFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostPed` | `BOOL` | `bool` | |
| `copyHeadBlendFlag` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

---

### CLONE_PED_ALT

**Hash:** `0x668FD40BCBA5DE48` | **Build:** `463`

**C++ 原型:**
```cpp
Ped CLONE_PED_ALT(Ped ped, BOOL isNetwork, BOOL bScriptHostPed, BOOL copyHeadBlendFlag, BOOL p4)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CLONE_PED_ALT, ped, isNetwork, bScriptHostPed, copyHeadBlendFlag, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostPed` | `BOOL` | `bool` | |
| `copyHeadBlendFlag` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

---

### CLONE_PED_TO_TARGET

**Hash:** `0xE952D6431689AD9A` | **JHash:** `0xFC70EEC7` | **Build:** `323`

**C++ 原型:**
```cpp
void CLONE_PED_TO_TARGET(Ped ped, Ped targetPed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLONE_PED_TO_TARGET, ped, targetPed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Copies ped's components and props to targetPed.

---

### CLONE_PED_TO_TARGET_ALT

**Hash:** `0x148B08C2D2ACB884` | **Build:** `463`

**C++ 原型:**
```cpp
void CLONE_PED_TO_TARGET_ALT(Ped ped, Ped targetPed, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLONE_PED_TO_TARGET_ALT, ped, targetPed, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_IN_VEHICLE

**Hash:** `0xA3EE4A07279BB9DB` | **JHash:** `0x7DA6BC83` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_VEHICLE(Ped ped, Vehicle vehicle, BOOL atGetIn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_VEHICLE, ped, vehicle, atGetIn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `atGetIn` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified ped is in the specified vehicle.

If 'atGetIn' is false, the function will not return true until the ped is sitting in the vehicle and is about to close the door. If it's true, the function returns true the moment the ped starts to get onto the seat (after opening the door). Eg. if false, and the ped is getting into a submersible, the function will not return true until the ped has descended down into the submersible and gotten into the seat, while if it's true, it'll return true the moment the hatch has been opened and the ped is about to descend into the submersible.

---

### IS_PED_IN_MODEL

**Hash:** `0x796D90EFB19AA332` | **JHash:** `0xA6438D4B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_MODEL(Ped ped, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_MODEL, ped, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_ANY_VEHICLE

**Hash:** `0x997ABD671D25CA0B` | **JHash:** `0x3B0171EE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_VEHICLE(Ped ped, BOOL atGetIn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_VEHICLE, ped, atGetIn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `atGetIn` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified ped is in any vehicle.

If 'atGetIn' is false, the function will not return true until the ped is sitting in the vehicle and is about to close the door. If it's true, the function returns true the moment the ped starts to get onto the seat (after opening the door). Eg. if false, and the ped is getting into a submersible, the function will not return true until the ped has descended down into the submersible and gotten into the seat, while if it's true, it'll return true the moment the hatch has been opened and the ped is about to descend into the submersible.

---

### IS_COP_PED_IN_AREA_3D

**Hash:** `0x16EC4839969F9F5E` | **JHash:** `0xB98DB96B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COP_PED_IN_AREA_3D(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COP_PED_IN_AREA_3D, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** xyz - relative to the world origin.

---

### IS_PED_INJURED

**Hash:** `0x84A2DD9AC37C35C1` | **JHash:** `0x2530A087` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_INJURED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_INJURED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether this ped's health is below its injured threshold.

The default threshold is 100.

---

### IS_PED_HURT

**Hash:** `0x5983BB449D7FDB12` | **JHash:** `0x69DFA0AF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HURT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HURT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified ped is hurt.

---

### IS_PED_FATALLY_INJURED

**Hash:** `0xD839450756ED5A80` | **JHash:** `0xBADA0093` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_FATALLY_INJURED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_FATALLY_INJURED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether this ped's health is below its fatally injured threshold. The default threshold is 100.
If the handle is invalid, the function returns true.

---

### IS_PED_DEAD_OR_DYING

**Hash:** `0x3317DEDB88C95038` | **JHash:** `0xCBDB7739` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_DEAD_OR_DYING(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DEAD_OR_DYING, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Seems to consistently return true if the ped is dead.

p1 is always passed 1 in the scripts.

I suggest to remove "OR_DYING" part, because it does not detect dying phase.

That's what the devs call it, cry about it.

lol

---

### IS_CONVERSATION_PED_DEAD

**Hash:** `0xE0A0AEC214B1FABA` | **JHash:** `0x1FA39EFE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONVERSATION_PED_DEAD(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONVERSATION_PED_DEAD, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_AIMING_FROM_COVER

**Hash:** `0x3998B1276A3300E5` | **JHash:** `0xDEBAB2AF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_AIMING_FROM_COVER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_AIMING_FROM_COVER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_RELOADING

**Hash:** `0x24B100C68C645951` | **JHash:** `0x961E1745` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RELOADING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RELOADING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified ped is reloading.

---

### IS_PED_A_PLAYER

**Hash:** `0x12534C348C6CB68B` | **JHash:** `0x404794CA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_A_PLAYER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_A_PLAYER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the given ped has a valid pointer to CPlayerInfo in its CPed class. That's all.

---

### CREATE_PED_INSIDE_VEHICLE

**Hash:** `0x7DD959874C1FD534` | **JHash:** `0x3000F092` | **Build:** `323`

**C++ 原型:**
```cpp
Ped CREATE_PED_INSIDE_VEHICLE(Vehicle vehicle, int pedType, Hash modelHash, int seat, BOOL isNetwork, BOOL bScriptHostPed)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PED_INSIDE_VEHICLE, vehicle, pedType, modelHash, seat, isNetwork, bScriptHostPed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `pedType` | `int` | `int` | |
| `modelHash` | `Hash` | `uint` | |
| `seat` | `int` | `int` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostPed` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

**说明:** pedType: see CREATE_PED

Full list of peds by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/peds.json

---

### SET_PED_DESIRED_HEADING

**Hash:** `0xAA5A7ECE2AA8FE70` | **JHash:** `0x961458F9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DESIRED_HEADING(Ped ped, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DESIRED_HEADING, ped, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### FORCE_ALL_HEADING_VALUES_TO_ALIGN

**Hash:** `0xFF287323B0E2C69A` | **JHash:** `0x290421BE` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ALL_HEADING_VALUES_TO_ALIGN(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ALL_HEADING_VALUES_TO_ALIGN, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_FACING_PED

**Hash:** `0xD71649DB0A545AA3` | **JHash:** `0x0B775838` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_FACING_PED(Ped ped, Ped otherPed, float angle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_FACING_PED, ped, otherPed, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `otherPed` | `Ped` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** angle is ped's view cone

---

### IS_PED_IN_MELEE_COMBAT

**Hash:** `0x4E209B2C1EAD5159` | **JHash:** `0xFD7814A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_MELEE_COMBAT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_MELEE_COMBAT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Notes: The function only returns true while the ped is: 
A.) Swinging a random melee attack (including pistol-whipping)

B.) Reacting to being hit by a melee attack (including pistol-whipping)

C.) Is locked-on to an enemy (arms up, strafing/skipping in the default fighting-stance, ready to dodge+counter). 

You don't have to be holding the melee-targetting button to be in this stance; you stay in it by default for a few seconds after swinging at someone. If you do a sprinting punch, it returns true for the duration of the punch animation and then returns false again, even if you've punched and made-angry many peds

---

### IS_PED_STOPPED

**Hash:** `0x530944F6F4B8A214` | **JHash:** `0xA0DC0B87` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_STOPPED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_STOPPED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the ped doesn't do any movement. If the ped is being pushed forwards by using APPLY_FORCE_TO_ENTITY for example, the function returns false.

---

### IS_PED_SHOOTING_IN_AREA

**Hash:** `0x7E9DFE24AC1E58EF` | **JHash:** `0x741BF04F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SHOOTING_IN_AREA(Ped ped, float x1, float y1, float z1, float x2, float y2, float z2, BOOL p7, BOOL p8)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SHOOTING_IN_AREA, ped, x1, y1, z1, x2, y2, z2, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_ANY_PED_SHOOTING_IN_AREA

**Hash:** `0xA0D3D71EA1086C55` | **JHash:** `0x91833867` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_PED_SHOOTING_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_PED_SHOOTING_IN_AREA, x1, y1, z1, x2, y2, z2, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_SHOOTING

**Hash:** `0x34616828CD07F1A1` | **JHash:** `0xE7C3405E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SHOOTING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SHOOTING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified ped is shooting.

---

### SET_PED_ACCURACY

**Hash:** `0x7AEFB85C1D49DEB6` | **JHash:** `0x6C17122E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ACCURACY(Ped ped, int accuracy)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ACCURACY, ped, accuracy);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `accuracy` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** accuracy = 0-100, 100 being perfectly accurate

---

### GET_PED_ACCURACY

**Hash:** `0x37F4AD56ECBC0CD6` | **JHash:** `0x0A2A0AA0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_ACCURACY(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_ACCURACY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### SET_AMBIENT_LAW_PED_ACCURACY_MODIFIER

**Hash:** `0x87DDEB611B329A9C` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_AMBIENT_LAW_PED_ACCURACY_MODIFIER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_LAW_PED_ACCURACY_MODIFIER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_PED_MODEL

**Hash:** `0xC9D55B1A358A5BF7` | **JHash:** `0x5F1DDFCB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_MODEL(Ped ped, Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_MODEL, ped, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### EXPLODE_PED_HEAD

**Hash:** `0x2D05CED3A38D0F3A` | **JHash:** `0x05CC1380` | **Build:** `323`

**C++ 原型:**
```cpp
void EXPLODE_PED_HEAD(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXPLODE_PED_HEAD, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Forces the ped to fall back and kills it.

It doesn't really explode the ped's head but it kills the ped

---

### REMOVE_PED_ELEGANTLY

**Hash:** `0xAC6D445B994DF95E` | **JHash:** `0x4FFB8C6C` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PED_ELEGANTLY(Ped* ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PED_ELEGANTLY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Judging purely from a quick disassembly, if the ped is in a vehicle, the ped will be deleted immediately. If not, it'll be marked as no longer needed. - very elegant..

---

### ADD_ARMOUR_TO_PED

**Hash:** `0x5BA652A0CD14DF2F` | **JHash:** `0xF686B26E` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_ARMOUR_TO_PED(Ped ped, int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_ARMOUR_TO_PED, ped, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Same as SET_PED_ARMOUR, but ADDS 'amount' to the armor the Ped already has.

---

### SET_PED_ARMOUR

**Hash:** `0xCEA04D83135264CC` | **JHash:** `0x4E3A0CC4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ARMOUR(Ped ped, int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ARMOUR, ped, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the armor of the specified ped.

ped: The Ped to set the armor of.
amount: A value between 0 and 100 indicating the value to set the Ped's armor to.

---

### SET_PED_INTO_VEHICLE

**Hash:** `0xF75B0D629E1C063D` | **JHash:** `0x07500C79` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INTO_VEHICLE(Ped ped, Vehicle vehicle, int seatIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INTO_VEHICLE, ped, vehicle, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Ped: The ped to warp.
vehicle: The vehicle to warp the ped into.
Seat_Index: [-1 is driver seat, -2 first free passenger seat]

Moreinfo of Seat Index
DriverSeat = -1
Passenger = 0
Left Rear = 1
RightRear = 2

---

### SET_PED_ALLOW_VEHICLES_OVERRIDE

**Hash:** `0x3C028C636A414ED9` | **JHash:** `0x58A80BD5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALLOW_VEHICLES_OVERRIDE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALLOW_VEHICLES_OVERRIDE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CAN_CREATE_RANDOM_PED

**Hash:** `0x3E8349C08E4B82E4` | **JHash:** `0xF9ABE88F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_CREATE_RANDOM_PED(BOOL p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_CREATE_RANDOM_PED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### CREATE_RANDOM_PED

**Hash:** `0xB4AC7D0CF06BFE8F` | **JHash:** `0x5A949543` | **Build:** `323`

**C++ 原型:**
```cpp
Ped CREATE_RANDOM_PED(float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_RANDOM_PED, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `Ped` → `int`

**说明:** vb.net
Dim ped_handle As Integer
                    With Game.Player.Character
                        Dim pos As Vector3 = .Position + .ForwardVector * 3
                        ped_handle = Native.Function.Call(Of Integer)(Hash.CREATE_RANDOM_PED, pos.X, pos.Y, pos.Z)
                    End With

Creates a Ped at the specified location, returns the Ped Handle.  
Ped will not act until SET_PED_AS_NO_LONGER_NEEDED is called.

---

### CREATE_RANDOM_PED_AS_DRIVER

**Hash:** `0x9B62392B474F44A0` | **JHash:** `0xB927CE9A` | **Build:** `323`

**C++ 原型:**
```cpp
Ped CREATE_RANDOM_PED_AS_DRIVER(Vehicle vehicle, BOOL returnHandle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_RANDOM_PED_AS_DRIVER, vehicle, returnHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `returnHandle` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

---

### CAN_CREATE_RANDOM_DRIVER

**Hash:** `0xB8EB95E5B4E56978` | **JHash:** `0x99861609` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_CREATE_RANDOM_DRIVER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_CREATE_RANDOM_DRIVER);
```

**返回值:** `BOOL` → `bool`

---

### CAN_CREATE_RANDOM_BIKE_RIDER

**Hash:** `0xEACEEDA81751915C` | **JHash:** `0x7018BE31` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_CREATE_RANDOM_BIKE_RIDER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_CREATE_RANDOM_BIKE_RIDER);
```

**返回值:** `BOOL` → `bool`

---

### SET_PED_MOVE_ANIMS_BLEND_OUT

**Hash:** `0x9E8C908F41584ECD` | **JHash:** `0x20E01957` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MOVE_ANIMS_BLEND_OUT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOVE_ANIMS_BLEND_OUT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_DRAGGED_OUT

**Hash:** `0xC1670E958EEE24E5` | **JHash:** `0xAA7F1131` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_DRAGGED_OUT(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_DRAGGED_OUT, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_ALLOW_HURT_COMBAT_FOR_ALL_MISSION_PEDS

**Hash:** `0xF2BEBCDFAFDAA19E` | **JHash:** `0x6CD58238` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALLOW_HURT_COMBAT_FOR_ALL_MISSION_PEDS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALLOW_HURT_COMBAT_FOR_ALL_MISSION_PEDS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** ntoggle was always false except in one instance (b678).

The one time this is set to true seems to do with when you fail the mission.

---

### IS_PED_MALE

**Hash:** `0x6D9F5FAA7488BA46` | **JHash:** `0x90950455` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_MALE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_MALE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true/false if the ped is/isn't male.

---

### IS_PED_HUMAN

**Hash:** `0xB980061DA992779D` | **JHash:** `0x194BB7B0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HUMAN(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HUMAN, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true/false if the ped is/isn't humanoid.

---

### GET_VEHICLE_PED_IS_IN

**Hash:** `0x9A9112A0FE9A4713` | **JHash:** `0xAFE92319` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_PED_IS_IN(Ped ped, BOOL includeEntering)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_PED_IS_IN, ped, includeEntering);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `includeEntering` | `BOOL` | `bool` | |

**返回值:** `Vehicle` → `int`

**说明:** Gets the vehicle the specified Ped is in. Returns 0 if the ped is/was not in a vehicle.

---

### RESET_PED_LAST_VEHICLE

**Hash:** `0xBB8DE8CF6A8DD8BB` | **JHash:** `0x5E3B5942` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_LAST_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_LAST_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Resets the value for the last vehicle driven by the Ped.

---

### SET_PED_DENSITY_MULTIPLIER_THIS_FRAME

**Hash:** `0x95E3D6257B166CF2` | **JHash:** `0x039C82BB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DENSITY_MULTIPLIER_THIS_FRAME(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DENSITY_MULTIPLIER_THIS_FRAME, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_SCENARIO_PED_DENSITY_MULTIPLIER_THIS_FRAME

**Hash:** `0x7A556143A1C03898` | **JHash:** `0x2909ABF0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENARIO_PED_DENSITY_MULTIPLIER_THIS_FRAME(float p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENARIO_PED_DENSITY_MULTIPLIER_THIS_FRAME, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SUPPRESS_AMBIENT_PED_AGGRESSIVE_CLEANUP_THIS_FRAME

**Hash:** `0x5A7F62FDA59759BD` | **JHash:** `0xB48C0C04` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_AMBIENT_PED_AGGRESSIVE_CLEANUP_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_AMBIENT_PED_AGGRESSIVE_CLEANUP_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_SCRIPTED_CONVERSION_COORD_THIS_FRAME

**Hash:** `0x5086C7843552CF85` | **JHash:** `0x25EA2AA5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPTED_CONVERSION_COORD_THIS_FRAME(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPTED_CONVERSION_COORD_THIS_FRAME, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_NON_CREATION_AREA

**Hash:** `0xEE01041D559983EA` | **JHash:** `0x7A97283F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_NON_CREATION_AREA(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_NON_CREATION_AREA, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The distance between these points, is the diagonal of a box (remember it's 3D).

---

### CLEAR_PED_NON_CREATION_AREA

**Hash:** `0x2E05208086BA0651` | **JHash:** `0x6F7043A3` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_NON_CREATION_AREA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_NON_CREATION_AREA);
```

**返回值:** `void` → `void`

---

### INSTANTLY_FILL_PED_POPULATION

**Hash:** `0x4759CC730F947C81` | **JHash:** `0x8C555ADD` | **Build:** `323`

**C++ 原型:**
```cpp
void INSTANTLY_FILL_PED_POPULATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INSTANTLY_FILL_PED_POPULATION);
```

**返回值:** `void` → `void`

---

### IS_PED_ON_MOUNT

**Hash:** `0x460BC76A0E10655E` | **JHash:** `0x43103006` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ON_MOUNT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ON_MOUNT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Same function call as PED::GET_MOUNT, aka just returns 0

---

### GET_MOUNT

**Hash:** `0xE7E11B8DCBED1058` | **JHash:** `0xDD31EC4E` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_MOUNT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MOUNT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Ped` → `int`

**说明:** 
Function just returns 0
void __fastcall ped__get_mount(NativeContext *a1)
{
  NativeContext *v1; // rbx@1

  v1 = a1;
  GetAddressOfPedFromScriptHandle(a1->Args->Arg1);
  v1->Returns->Item1= 0;
}

---

### IS_PED_ON_VEHICLE

**Hash:** `0x67722AEB798E5FAB` | **JHash:** `0xA1AE7CC7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ON_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ON_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified ped is on top of any vehicle.

Return 1 when ped is on vehicle.
Return 0 when ped is not on a vehicle.


---

### IS_PED_ON_SPECIFIC_VEHICLE

**Hash:** `0xEC5F66E459AF3BB2` | **JHash:** `0x63CB4603` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ON_SPECIFIC_VEHICLE(Ped ped, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ON_SPECIFIC_VEHICLE, ped, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_MONEY

**Hash:** `0xA9C8960E8684C1B5` | **JHash:** `0x40D90BF2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MONEY(Ped ped, int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MONEY, ped, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Maximum possible amount of money on MP is 2000. ~JX

-----------------------------------------------------------------------------

Maximum amount that a ped can theoretically have is 65535 (0xFFFF) since the amount is stored as an unsigned short (uint16_t) value.

---

### GET_PED_MONEY

**Hash:** `0x3F69145BBA87BAE7` | **JHash:** `0xEB3C4C7E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_MONEY(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_MONEY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### SET_HEALTH_SNACKS_CARRIED_BY_ALL_NEW_PEDS

**Hash:** `0xFF4803BC019852D9` | **JHash:** `0xD41C9AED` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HEALTH_SNACKS_CARRIED_BY_ALL_NEW_PEDS(float p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HEALTH_SNACKS_CARRIED_BY_ALL_NEW_PEDS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Related to Peds dropping pickup_health_snack; p0 is a value between [0.0, 1.0] that corresponds to drop rate

---

### SET_AMBIENT_PEDS_DROP_MONEY

**Hash:** `0x6B0E6172C9A4D902` | **JHash:** `0x30B98369` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_PEDS_DROP_MONEY(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_PEDS_DROP_MONEY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_BLOCK_AMBIENT_PEDS_FROM_DROPPING_WEAPONS_THIS_FRAME

**Hash:** `0xC73EFFC5E043A8BA` | **Build:** `3258`

**C++ 原型:**
```cpp
void _SET_BLOCK_AMBIENT_PEDS_FROM_DROPPING_WEAPONS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_BLOCK_AMBIENT_PEDS_FROM_DROPPING_WEAPONS_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_BLOCKING_OF_NON_TEMPORARY_EVENTS_FOR_AMBIENT_PEDS_THIS_FRAME

**Hash:** `0x9911F4A24485F653` | **JHash:** `0x02A080C8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLOCKING_OF_NON_TEMPORARY_EVENTS_FOR_AMBIENT_PEDS_THIS_FRAME(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLOCKING_OF_NON_TEMPORARY_EVENTS_FOR_AMBIENT_PEDS_THIS_FRAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_SUFFERS_CRITICAL_HITS

**Hash:** `0xEBD76F2359F190AC` | **JHash:** `0x6F6FC7E6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SUFFERS_CRITICAL_HITS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SUFFERS_CRITICAL_HITS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Ped no longer takes critical damage modifiers if set to FALSE.
Example: Headshotting a player no longer one shots them. Instead they will take the same damage as a torso shot.

---

### SET_PED_UPPER_BODY_DAMAGE_ONLY

**Hash:** `0xAFC976FD0580C7B3` | **JHash:** `0x1572022A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_UPPER_BODY_DAMAGE_ONLY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_UPPER_BODY_DAMAGE_ONLY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_SITTING_IN_VEHICLE

**Hash:** `0xA808AA1D79230FC2` | **JHash:** `0xDDDE26FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SITTING_IN_VEHICLE(Ped ped, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SITTING_IN_VEHICLE, ped, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Detect if ped is sitting in the specified vehicle
[True/False]

---

### IS_PED_SITTING_IN_ANY_VEHICLE

**Hash:** `0x826AA586EDB9FEF8` | **JHash:** `0x0EA9CA03` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SITTING_IN_ANY_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SITTING_IN_ANY_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Detect if ped is in any vehicle
[True/False]

---

### IS_PED_ON_FOOT

**Hash:** `0x01FEE67DB37F59B2` | **JHash:** `0xC60D0785` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ON_FOOT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ON_FOOT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_ON_ANY_BIKE

**Hash:** `0x94495889E22C6479` | **JHash:** `0x4D885B2E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ON_ANY_BIKE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ON_ANY_BIKE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_PLANTING_BOMB

**Hash:** `0xC70B5FAE151982D8` | **JHash:** `0x0EDAC574` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PLANTING_BOMB(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PLANTING_BOMB, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_DEAD_PED_PICKUP_COORDS

**Hash:** `0xCD5003B097200F36` | **JHash:** `0x129F9DC1` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_DEAD_PED_PICKUP_COORDS(Ped ped, float p1, float p2)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_DEAD_PED_PICKUP_COORDS, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### IS_PED_IN_ANY_BOAT

**Hash:** `0x2E0E1C2B4F6CB339` | **JHash:** `0x1118A947` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_BOAT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_BOAT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_ANY_SUB

**Hash:** `0xFBFC01CCFB35D99E` | **JHash:** `0xE65F8059` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_SUB(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_SUB, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_ANY_HELI

**Hash:** `0x298B91AE825E5705` | **JHash:** `0x7AB5523B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_HELI(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_HELI, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_ANY_PLANE

**Hash:** `0x5FFF4CFC74D8FB80` | **JHash:** `0x51BBCE7E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_PLANE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_PLANE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_FLYING_VEHICLE

**Hash:** `0x9134873537FA419C` | **JHash:** `0xCA072485` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_FLYING_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_FLYING_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_DIES_IN_WATER

**Hash:** `0x56CEF0AC79073BDE` | **JHash:** `0x604C872B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DIES_IN_WATER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DIES_IN_WATER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PED_DIES_IN_WATER

**Hash:** `0x65671A4FB8218930` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL GET_PED_DIES_IN_WATER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_DIES_IN_WATER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_DIES_IN_SINKING_VEHICLE

**Hash:** `0xD718A22995E2B4BC` | **JHash:** `0x8D4D9ABB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DIES_IN_SINKING_VEHICLE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DIES_IN_SINKING_VEHICLE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PED_ARMOUR

**Hash:** `0x9483AF821605B1D8` | **JHash:** `0x2CE311A7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_ARMOUR(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_ARMOUR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_STAY_IN_VEHICLE_WHEN_JACKED

**Hash:** `0xEDF4079F9D54C9A1` | **JHash:** `0xB014A09C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STAY_IN_VEHICLE_WHEN_JACKED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STAY_IN_VEHICLE_WHEN_JACKED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_SHOT_IN_VEHICLE

**Hash:** `0xC7EF1BA83230BA07` | **JHash:** `0x5DB7B3A9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_SHOT_IN_VEHICLE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_SHOT_IN_VEHICLE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PED_LAST_DAMAGE_BONE

**Hash:** `0xD75960F6BD9EA49C` | **JHash:** `0xAB933841` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_LAST_DAMAGE_BONE(Ped ped, int* outBone)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_LAST_DAMAGE_BONE, ped, outBone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `outBone` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_PED_LAST_DAMAGE_BONE

**Hash:** `0x8EF6B7AC68E2F01B` | **JHash:** `0x56CB715E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_LAST_DAMAGE_BONE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_LAST_DAMAGE_BONE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_AI_WEAPON_DAMAGE_MODIFIER

**Hash:** `0x1B1E2A40A65B8521` | **JHash:** `0x516E30EE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AI_WEAPON_DAMAGE_MODIFIER(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AI_WEAPON_DAMAGE_MODIFIER, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_AI_WEAPON_DAMAGE_MODIFIER

**Hash:** `0xEA16670E7BA4743C` | **JHash:** `0x6E965420` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_AI_WEAPON_DAMAGE_MODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_AI_WEAPON_DAMAGE_MODIFIER);
```

**返回值:** `void` → `void`

---

### SET_AI_MELEE_WEAPON_DAMAGE_MODIFIER

**Hash:** `0x66460DEDDD417254` | **JHash:** `0x0F9A401F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AI_MELEE_WEAPON_DAMAGE_MODIFIER(float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AI_MELEE_WEAPON_DAMAGE_MODIFIER, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_AI_MELEE_WEAPON_DAMAGE_MODIFIER

**Hash:** `0x46E56A7CD1D63C3F` | **JHash:** `0x97886238` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_AI_MELEE_WEAPON_DAMAGE_MODIFIER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_AI_MELEE_WEAPON_DAMAGE_MODIFIER);
```

**返回值:** `void` → `void`

---

### SET_TREAT_AS_AMBIENT_PED_FOR_DRIVER_LOCKON

**Hash:** `0x2F3C3D9F50681DE4` | **JHash:** `0xCC9D7F1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TREAT_AS_AMBIENT_PED_FOR_DRIVER_LOCKON(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TREAT_AS_AMBIENT_PED_FOR_DRIVER_LOCKON, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_TARGETTED

**Hash:** `0x63F58F7C80513AAD` | **JHash:** `0x75C49F74` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_TARGETTED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_TARGETTED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_TARGETTED_BY_TEAM

**Hash:** `0xBF1CA77833E58F2C` | **JHash:** `0xB103A8E1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_TARGETTED_BY_TEAM(Ped ped, int team, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_TARGETTED_BY_TEAM, ped, team, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `team` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_TARGETTED_BY_PLAYER

**Hash:** `0x66B57B72E0836A76` | **JHash:** `0xD050F490` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_TARGETTED_BY_PLAYER(Ped ped, Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_TARGETTED_BY_PLAYER, ped, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_LOCKON_TO_PED_IF_FRIENDLY

**Hash:** `0x061CB768363D6424` | **JHash:** `0x7DA12905` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALLOW_LOCKON_TO_PED_IF_FRIENDLY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_LOCKON_TO_PED_IF_FRIENDLY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_USE_CAMERA_HEADING_FOR_DESIRED_DIRECTION_LOCK_ON_TEST

**Hash:** `0xFD325494792302D7` | **JHash:** `0x7F67671D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_USE_CAMERA_HEADING_FOR_DESIRED_DIRECTION_LOCK_ON_TEST(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_CAMERA_HEADING_FOR_DESIRED_DIRECTION_LOCK_ON_TEST, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_IN_ANY_POLICE_VEHICLE

**Hash:** `0x0BD04E29640C9C12` | **JHash:** `0x84FA790D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_POLICE_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_POLICE_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FORCE_PED_TO_OPEN_PARACHUTE

**Hash:** `0x16E42E800B472221` | **JHash:** `0xA819680B` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_PED_TO_OPEN_PARACHUTE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PED_TO_OPEN_PARACHUTE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_IN_PARACHUTE_FREE_FALL

**Hash:** `0x7DCE8BDA0F1C1200` | **JHash:** `0xCD71F11B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_PARACHUTE_FREE_FALL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_PARACHUTE_FREE_FALL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_FALLING

**Hash:** `0xFB92A102F1C4DFA3` | **JHash:** `0xABF77334` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_FALLING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_FALLING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_JUMPING

**Hash:** `0xCEDABC5900A0BF97` | **JHash:** `0x07E5BC0E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_JUMPING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_JUMPING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_LANDING

**Hash:** `0x412F1364FA066CFB` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL IS_PED_LANDING(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_LANDING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_DOING_A_BEAST_JUMP

**Hash:** `0x451D05012CCEC234` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL IS_PED_DOING_A_BEAST_JUMP(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DOING_A_BEAST_JUMP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_CLIMBING

**Hash:** `0x53E8CB4F48BFE623` | **JHash:** `0xBCE03D35` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_CLIMBING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_CLIMBING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_VAULTING

**Hash:** `0x117C70D1F5730B5E` | **JHash:** `0xC3169BDA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_VAULTING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_VAULTING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_DIVING

**Hash:** `0x5527B8246FEF9B11` | **JHash:** `0x7BC5BF3C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_DIVING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DIVING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_JUMPING_OUT_OF_VEHICLE

**Hash:** `0x433DDFFE2044B636` | **JHash:** `0xB19215F6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_JUMPING_OUT_OF_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_JUMPING_OUT_OF_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_OPENING_DOOR

**Hash:** `0x26AF0E8E30BD2A2C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_OPENING_DOOR(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_OPENING_DOOR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the ped is currently opening a door (CTaskOpenDoor).

---

### GET_PED_PARACHUTE_STATE

**Hash:** `0x79CFD9827CC979B6` | **JHash:** `0x7D4BC475` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_PARACHUTE_STATE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_PARACHUTE_STATE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns:

-1: Normal
0: Wearing parachute on back
1: Parachute opening
2: Parachute open
3: Falling to doom (e.g. after exiting parachute)

Normal means no parachute?

---

### GET_PED_PARACHUTE_LANDING_TYPE

**Hash:** `0x8B9F1FC6AE8166C0` | **JHash:** `0x01F3B035` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_PARACHUTE_LANDING_TYPE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_PARACHUTE_LANDING_TYPE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** -1: no landing
0: landing on both feet
1: stumbling
2: rolling
3: ragdoll

---

### SET_PED_PARACHUTE_TINT_INDEX

**Hash:** `0x333FC8DB079B7186` | **JHash:** `0x5AEFEC3A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PARACHUTE_TINT_INDEX(Ped ped, int tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PARACHUTE_TINT_INDEX, ped, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `tintIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_PARACHUTE_TINT_INDEX

**Hash:** `0xEAF5F7E5AE7C6C9D` | **JHash:** `0xE9E7FAC5` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PED_PARACHUTE_TINT_INDEX(Ped ped, int* outTintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PED_PARACHUTE_TINT_INDEX, ped, outTintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `outTintIndex` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_PED_RESERVE_PARACHUTE_TINT_INDEX

**Hash:** `0xE88DA0751C22A2AD` | **JHash:** `0x177EFC79` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RESERVE_PARACHUTE_TINT_INDEX(Ped ped, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RESERVE_PARACHUTE_TINT_INDEX, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CREATE_PARACHUTE_BAG_OBJECT

**Hash:** `0x8C4F3BF23B6237DB` | **JHash:** `0x19198A16` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_PARACHUTE_BAG_OBJECT(Ped ped, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_PARACHUTE_BAG_OBJECT, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

---

### SET_PED_DUCKING

**Hash:** `0x030983CA930B692D` | **JHash:** `0xB90353D7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DUCKING(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DUCKING, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This is the SET_CHAR_DUCKING from GTA IV, that makes Peds duck. This function does nothing in GTA V. It cannot set the ped as ducking in vehicles, and IS_PED_DUCKING will always return false.

---

### IS_PED_DUCKING

**Hash:** `0xD125AE748725C6BC` | **JHash:** `0x9199C77D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_DUCKING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DUCKING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_ANY_TAXI

**Hash:** `0x6E575D6A898AB852` | **JHash:** `0x16FD386C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_TAXI(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_TAXI, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_ID_RANGE

**Hash:** `0xF107E836A70DCE05` | **JHash:** `0xEF3B4ED9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ID_RANGE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ID_RANGE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_HIGHLY_PERCEPTIVE

**Hash:** `0x52D59AB61DDC05DD` | **JHash:** `0x9A2180FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HIGHLY_PERCEPTIVE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HIGHLY_PERCEPTIVE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_COP_PERCEPTION_OVERRIDES

**Hash:** `0x2F074C904D85129E` | **Build:** `393`

**C++ 原型:**
```cpp
void SET_COP_PERCEPTION_OVERRIDES(float seeingRange, float seeingRangePeripheral, float hearingRange, float visualFieldMinAzimuthAngle, float visualFieldMaxAzimuthAngle, float fieldOfGazeMaxAngle, float p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_COP_PERCEPTION_OVERRIDES, seeingRange, seeingRangePeripheral, hearingRange, visualFieldMinAzimuthAngle, visualFieldMaxAzimuthAngle, fieldOfGazeMaxAngle, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `seeingRange` | `float` | `float` | |
| `seeingRangePeripheral` | `float` | `float` | |
| `hearingRange` | `float` | `float` | |
| `visualFieldMinAzimuthAngle` | `float` | `float` | |
| `visualFieldMaxAzimuthAngle` | `float` | `float` | |
| `fieldOfGazeMaxAngle` | `float` | `float` | |
| `p6` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_INJURED_ON_GROUND_BEHAVIOUR

**Hash:** `0xEC4B4B3B9908052A` | **JHash:** `0xF30658D2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INJURED_ON_GROUND_BEHAVIOUR(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INJURED_ON_GROUND_BEHAVIOUR, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DISABLE_PED_INJURED_ON_GROUND_BEHAVIOUR

**Hash:** `0x733C87D4CE22BEA2` | **JHash:** `0x43709044` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PED_INJURED_ON_GROUND_BEHAVIOUR(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PED_INJURED_ON_GROUND_BEHAVIOUR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_SEEING_RANGE

**Hash:** `0xF29CF591C4BF6CEE` | **JHash:** `0x4BD72FE8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SEEING_RANGE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SEEING_RANGE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_HEARING_RANGE

**Hash:** `0x33A8F7F7D5F7F33C` | **JHash:** `0xB32087E0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HEARING_RANGE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEARING_RANGE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_VISUAL_FIELD_MIN_ANGLE

**Hash:** `0x2DB492222FB21E26` | **JHash:** `0x72E2E18B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_MIN_ANGLE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_MIN_ANGLE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_VISUAL_FIELD_MAX_ANGLE

**Hash:** `0x70793BDCA1E854D4` | **JHash:** `0x0CEA0F9A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_MAX_ANGLE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_MAX_ANGLE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_VISUAL_FIELD_MIN_ELEVATION_ANGLE

**Hash:** `0x7A276EB2C224D70F` | **JHash:** `0x5CC2F1B8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_MIN_ELEVATION_ANGLE(Ped ped, float angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_MIN_ELEVATION_ANGLE, ped, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native refers to the field of vision the ped has below them, starting at 0 degrees. The angle value should be negative.
-90f should let the ped see 90 degrees below them, for example.

---

### SET_PED_VISUAL_FIELD_MAX_ELEVATION_ANGLE

**Hash:** `0x78D0B67629D75856` | **JHash:** `0x39D9102F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_MAX_ELEVATION_ANGLE(Ped ped, float angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_MAX_ELEVATION_ANGLE, ped, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native refers to the field of vision the ped has above them, starting at 0 degrees. 90f would let the ped see enemies directly above of them.

---

### SET_PED_VISUAL_FIELD_PERIPHERAL_RANGE

**Hash:** `0x9C74B0BC831B753A` | **JHash:** `0xFDF2F7C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_PERIPHERAL_RANGE(Ped ped, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_PERIPHERAL_RANGE, ped, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_VISUAL_FIELD_CENTER_ANGLE

**Hash:** `0x3B6405E8AB34A907` | **JHash:** `0xE57202A1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VISUAL_FIELD_CENTER_ANGLE(Ped ped, float angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VISUAL_FIELD_CENTER_ANGLE, ped, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PED_VISUAL_FIELD_CENTER_ANGLE

**Hash:** `0xEF2C71A32CAD5FBD` | **Build:** `1493`

**C++ 原型:**
```cpp
float GET_PED_VISUAL_FIELD_CENTER_ANGLE(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PED_VISUAL_FIELD_CENTER_ANGLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### SET_PED_STEALTH_MOVEMENT

**Hash:** `0x88CBB5CEB96B7BD2` | **JHash:** `0x67E28E1D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEALTH_MOVEMENT(Ped ped, BOOL p1, const char* action)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEALTH_MOVEMENT, ped, p1, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `action` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** p1 is usually 0 in the scripts. action is either 0 or a pointer to "DEFAULT_ACTION".

---

### GET_PED_STEALTH_MOVEMENT

**Hash:** `0x7C2AC9CA66575FBF` | **JHash:** `0x40321B83` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_STEALTH_MOVEMENT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_STEALTH_MOVEMENT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the entity is in stealth mode

---

### CREATE_GROUP

**Hash:** `0x90370EBE0FEE1A3D` | **JHash:** `0x8DC0368D` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_GROUP(int unused)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_GROUP, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `unused` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Creates a new ped group.
Groups can contain up to 8 peds.

The parameter is unused.

Returns a handle to the created group, or 0 if a group couldn't be created.

---

### SET_PED_AS_GROUP_LEADER

**Hash:** `0x2A7819605465FBCE` | **JHash:** `0x7265BEA2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AS_GROUP_LEADER(Ped ped, int groupId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AS_GROUP_LEADER, ped, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `groupId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_AS_GROUP_MEMBER

**Hash:** `0x9F3480FE65DB31B5` | **JHash:** `0x0EE13F92` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AS_GROUP_MEMBER(Ped ped, int groupId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AS_GROUP_MEMBER, ped, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `groupId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_TELEPORT_TO_GROUP_LEADER

**Hash:** `0x2E2F4240B3F24647` | **JHash:** `0xD0D8BDBC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_TELEPORT_TO_GROUP_LEADER(Ped pedHandle, int groupHandle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_TELEPORT_TO_GROUP_LEADER, pedHandle, groupHandle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedHandle` | `Ped` | `int` | |
| `groupHandle` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This only will teleport the ped to the group leader if the group leader teleports (sets coords).

Only works in singleplayer

---

### REMOVE_GROUP

**Hash:** `0x8EB2F69076AF7053` | **JHash:** `0x48D72B88` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_GROUP(int groupId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_GROUP, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_PED_FROM_GROUP

**Hash:** `0xED74007FFB146BC2` | **JHash:** `0x82697713` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PED_FROM_GROUP(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PED_FROM_GROUP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_GROUP_MEMBER

**Hash:** `0x9BB01E3834671191` | **JHash:** `0x876D5363` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_GROUP_MEMBER(Ped ped, int groupId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_GROUP_MEMBER, ped, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `groupId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_HANGING_ON_TO_VEHICLE

**Hash:** `0x1C86D8AEF8254B78` | **JHash:** `0x9678D4FF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HANGING_ON_TO_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HANGING_ON_TO_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_GROUP_SEPARATION_RANGE

**Hash:** `0x4102C7858CFEE4E4` | **JHash:** `0x7B820CD5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GROUP_SEPARATION_RANGE(int groupHandle, float separationRange)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GROUP_SEPARATION_RANGE, groupHandle, separationRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupHandle` | `int` | `int` | |
| `separationRange` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the range at which members will automatically leave the group.

---

### SET_PED_MIN_GROUND_TIME_FOR_STUNGUN

**Hash:** `0xFA0675AB151073FA` | **JHash:** `0x2F0D0973` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MIN_GROUND_TIME_FOR_STUNGUN(Ped ped, int ms)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MIN_GROUND_TIME_FOR_STUNGUN, ped, ms);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ms` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Ped will stay on the ground after being stunned for at lest ms time. (in milliseconds)

---

### IS_PED_PRONE

**Hash:** `0xD6A86331A537A7B9` | **JHash:** `0x02C2A6C3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PRONE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PRONE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_COMBAT

**Hash:** `0x4859F1FC66A6278E` | **JHash:** `0xFE027CB5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_COMBAT(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_COMBAT, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks to see if ped and target are in combat with eachother. Only goes one-way: if target is engaged in combat with ped but ped has not yet reacted, the function will return false until ped starts fighting back.

p1 is usually 0 in the scripts because it gets the ped id during the task sequence. For instance: PED::IS_PED_IN_COMBAT(l_42E[4/*14*/], PLAYER::PLAYER_PED_ID()) // armenian2.ct4: 43794

---

### GET_PED_TARGET_FROM_COMBAT_PED

**Hash:** `0x32C27A11307B01CC` | **Build:** `2372`

**C++ 原型:**
```cpp
Entity GET_PED_TARGET_FROM_COMBAT_PED(Ped ped, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_TARGET_FROM_COMBAT_PED, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `Entity` → `int`

---

### CAN_PED_IN_COMBAT_SEE_TARGET

**Hash:** `0xEAD42DE3610D0721` | **JHash:** `0xCCD525E1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PED_IN_COMBAT_SEE_TARGET(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_IN_COMBAT_SEE_TARGET, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_DOING_DRIVEBY

**Hash:** `0xB2C086CC1BF8F2BF` | **JHash:** `0xAC3CEB9C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_DOING_DRIVEBY(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DOING_DRIVEBY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_JACKING

**Hash:** `0x4AE4FF911DFB61DA` | **JHash:** `0x3B321816` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_JACKING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_JACKING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BEING_JACKED

**Hash:** `0x9A497FE2DF198913` | **JHash:** `0xD45D605C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BEING_JACKED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BEING_JACKED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BEING_STUNNED

**Hash:** `0x4FBACCE3B4138EE8` | **JHash:** `0x0A66CE30` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BEING_STUNNED(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BEING_STUNNED, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 is always 0

---

### GET_PEDS_JACKER

**Hash:** `0x9B128DC36C1E04CF` | **JHash:** `0xDE1DBB59` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PEDS_JACKER(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PEDS_JACKER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Ped` → `int`

---

### GET_JACK_TARGET

**Hash:** `0x5486A79D9FBD342D` | **JHash:** `0x1D196361` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_JACK_TARGET(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_JACK_TARGET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Ped` → `int`

---

### IS_PED_FLEEING

**Hash:** `0xBBCCE00B381F8482` | **JHash:** `0x85D813C6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_FLEEING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_FLEEING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_COVER

**Hash:** `0x60DFD0691A170B88` | **JHash:** `0x972C5A8B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_COVER(Ped ped, BOOL exceptUseWeapon)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_COVER, ped, exceptUseWeapon);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `exceptUseWeapon` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 is nearly always 0 in the scripts. 

---

### IS_PED_IN_COVER_FACING_LEFT

**Hash:** `0x845333B3150583AB` | **JHash:** `0xB89DBB80` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_COVER_FACING_LEFT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_COVER_FACING_LEFT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_IN_HIGH_COVER

**Hash:** `0x6A03BF943D767C93` | **JHash:** `0xDD5D08A7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_HIGH_COVER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_HIGH_COVER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_GOING_INTO_COVER

**Hash:** `0x9F65DBC537E59AD5` | **JHash:** `0xA3589628` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_GOING_INTO_COVER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_GOING_INTO_COVER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_PINNED_DOWN

**Hash:** `0xAAD6D1ACF08F4612` | **JHash:** `0xCC78999D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_PINNED_DOWN(Ped ped, BOOL pinned, int i)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_PINNED_DOWN, ped, pinned, i);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `pinned` | `BOOL` | `bool` | |
| `i` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** i could be time. Only example in the decompiled scripts uses it as -1.

---

### _HAS_PED_CLEAR_LOS_TO_ENTITY

**Hash:** `0xA32ABFEB2A03B306` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _HAS_PED_CLEAR_LOS_TO_ENTITY(Ped ped, Entity entity, float x, float y, float z, int p5, BOOL p6, BOOL p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._HAS_PED_CLEAR_LOS_TO_ENTITY, ped, entity, x, y, z, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `int` | `int` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_SEAT_PED_IS_TRYING_TO_ENTER

**Hash:** `0x6F4C85ACD641BCD2` | **JHash:** `0xACF162E0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SEAT_PED_IS_TRYING_TO_ENTER(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SEAT_PED_IS_TRYING_TO_ENTER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_PED_IS_TRYING_TO_ENTER

**Hash:** `0x814FA8BE5449445D` | **JHash:** `0x99968B37` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_PED_IS_TRYING_TO_ENTER(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_PED_IS_TRYING_TO_ENTER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Vehicle` → `int`

---

### GET_PED_SOURCE_OF_DEATH

**Hash:** `0x93C8B64DEB84728C` | **JHash:** `0x84ADF9EB` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_PED_SOURCE_OF_DEATH(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_SOURCE_OF_DEATH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Entity` → `int`

**说明:** Returns the Entity (Ped, Vehicle, or ?Object?) that killed the 'ped'

Is best to check if the Ped is dead before asking for its killer.

---

### GET_PED_CAUSE_OF_DEATH

**Hash:** `0x16FFE42AB2D2DC59` | **JHash:** `0x63458C27` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PED_CAUSE_OF_DEATH(Ped ped)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_CAUSE_OF_DEATH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the hash of the weapon/model/object that killed the ped.

---

### GET_PED_TIME_OF_DEATH

**Hash:** `0x1E98817B311AE98A` | **JHash:** `0xDF6D5D54` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_TIME_OF_DEATH(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_TIME_OF_DEATH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### COUNT_PEDS_IN_COMBAT_WITH_TARGET

**Hash:** `0x5407B7288D0478B7` | **JHash:** `0xEF0B78E6` | **Build:** `323`

**C++ 原型:**
```cpp
int COUNT_PEDS_IN_COMBAT_WITH_TARGET(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.COUNT_PEDS_IN_COMBAT_WITH_TARGET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### COUNT_PEDS_IN_COMBAT_WITH_TARGET_WITHIN_RADIUS

**Hash:** `0x336B3D200AB007CB` | **JHash:** `0xFB18CB19` | **Build:** `323`

**C++ 原型:**
```cpp
int COUNT_PEDS_IN_COMBAT_WITH_TARGET_WITHIN_RADIUS(Ped ped, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.COUNT_PEDS_IN_COMBAT_WITH_TARGET_WITHIN_RADIUS, ped, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `int` → `int`

---

### SET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH

**Hash:** `0xADB3F206518799E8` | **JHash:** `0x423B7BA2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH(Ped ped, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH, ped, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_PED_RELATIONSHIP_GROUP_HASH

**Hash:** `0xC80A74AC829DDD92` | **JHash:** `0x79F8C18C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RELATIONSHIP_GROUP_HASH(Ped ped, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RELATIONSHIP_GROUP_HASH, ped, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_RELATIONSHIP_BETWEEN_GROUPS

**Hash:** `0xBF25EB89375A37AD` | **JHash:** `0xD4A215BA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RELATIONSHIP_BETWEEN_GROUPS(int relationship, Hash group1, Hash group2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RELATIONSHIP_BETWEEN_GROUPS, relationship, group1, group2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `relationship` | `int` | `int` | |
| `group1` | `Hash` | `uint` | |
| `group2` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Sets the relationship between two groups. This should be called twice (once for each group).

Relationship types:
0 = Companion
1 = Respect
2 = Like
3 = Neutral
4 = Dislike
5 = Hate
255 = Pedestrians

Example:
PED::SET_RELATIONSHIP_BETWEEN_GROUPS(2, l_1017, 0xA49E591C);
PED::SET_RELATIONSHIP_BETWEEN_GROUPS(2, 0xA49E591C, l_1017);

---

### CLEAR_RELATIONSHIP_BETWEEN_GROUPS

**Hash:** `0x5E29243FB56FC6D4` | **JHash:** `0x994B8C2D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_RELATIONSHIP_BETWEEN_GROUPS(int relationship, Hash group1, Hash group2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_RELATIONSHIP_BETWEEN_GROUPS, relationship, group1, group2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `relationship` | `int` | `int` | |
| `group1` | `Hash` | `uint` | |
| `group2` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Clears the relationship between two groups. This should be called twice (once for each group).

Relationship types:
0 = Companion
1 = Respect
2 = Like
3 = Neutral
4 = Dislike
5 = Hate
255 = Pedestrians
(Credits: Inco)

Example:
PED::CLEAR_RELATIONSHIP_BETWEEN_GROUPS(2, l_1017, 0xA49E591C);
PED::CLEAR_RELATIONSHIP_BETWEEN_GROUPS(2, 0xA49E591C, l_1017);

---

### ADD_RELATIONSHIP_GROUP

**Hash:** `0xF372BC22FCB88606` | **JHash:** `0x8B635546` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ADD_RELATIONSHIP_GROUP(const char* name, Hash* groupHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ADD_RELATIONSHIP_GROUP, name, groupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `groupHash` | `Hash*` | `uint*` | |

**返回值:** `BOOL` → `bool`

**说明:** Can't select void. This function returns nothing. The hash of the created relationship group is output in the second parameter.

---

### REMOVE_RELATIONSHIP_GROUP

**Hash:** `0xB6BA2444AB393DA2` | **JHash:** `0x4A1DC59A` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_RELATIONSHIP_GROUP(Hash groupHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_RELATIONSHIP_GROUP, groupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### DOES_RELATIONSHIP_GROUP_EXIST

**Hash:** `0xCC6E3B6BB69501F1` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL DOES_RELATIONSHIP_GROUP_EXIST(Hash groupHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_RELATIONSHIP_GROUP_EXIST, groupHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### GET_RELATIONSHIP_BETWEEN_PEDS

**Hash:** `0xEBA5AD3A0EAF7121` | **JHash:** `0xE254C39C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_RELATIONSHIP_BETWEEN_PEDS(Ped ped1, Ped ped2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RELATIONSHIP_BETWEEN_PEDS, ped1, ped2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped1` | `Ped` | `int` | |
| `ped2` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** Gets the relationship between two peds. This should be called twice (once for each ped).

Relationship types:
0 = Companion
1 = Respect
2 = Like
3 = Neutral
4 = Dislike
5 = Hate
255 = Pedestrians
(Credits: Inco)

Example:
PED::GET_RELATIONSHIP_BETWEEN_PEDS(2, l_1017, 0xA49E591C);
PED::GET_RELATIONSHIP_BETWEEN_PEDS(2, 0xA49E591C, l_1017);

---

### GET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH

**Hash:** `0x42FDD0F017B1E38E` | **JHash:** `0x714BD6E4` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH(Ped ped)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_RELATIONSHIP_GROUP_DEFAULT_HASH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_PED_RELATIONSHIP_GROUP_HASH

**Hash:** `0x7DBDD04862D95F04` | **JHash:** `0x354F283C` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PED_RELATIONSHIP_GROUP_HASH(Ped ped)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_RELATIONSHIP_GROUP_HASH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_RELATIONSHIP_BETWEEN_GROUPS

**Hash:** `0x9E6B70061662AE5C` | **JHash:** `0x4E372FE2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_RELATIONSHIP_BETWEEN_GROUPS(Hash group1, Hash group2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RELATIONSHIP_BETWEEN_GROUPS, group1, group2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `group1` | `Hash` | `uint` | |
| `group2` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Gets the relationship between two groups. This should be called twice (once for each group).

Relationship types:
0 = Companion
1 = Respect
2 = Like
3 = Neutral
4 = Dislike
5 = Hate
255 = Pedestrians

Example:
PED::GET_RELATIONSHIP_BETWEEN_GROUPS(l_1017, 0xA49E591C);
PED::GET_RELATIONSHIP_BETWEEN_GROUPS(0xA49E591C, l_1017);

---

### SET_RELATIONSHIP_GROUP_AFFECTS_WANTED_LEVEL

**Hash:** `0x5615E0C5EB2BC6E2` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_RELATIONSHIP_GROUP_AFFECTS_WANTED_LEVEL(Hash group, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RELATIONSHIP_GROUP_AFFECTS_WANTED_LEVEL, group, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `group` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TELL_GROUP_PEDS_IN_AREA_TO_ATTACK

**Hash:** `0xAD27D957598E49E9` | **Build:** `1290`

**C++ 原型:**
```cpp
void TELL_GROUP_PEDS_IN_AREA_TO_ATTACK(Ped ped, Any p1, float p2, Hash hash, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TELL_GROUP_PEDS_IN_AREA_TO_ATTACK, ped, p1, p2, hash, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `float` | `float` | |
| `hash` | `Hash` | `uint` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_TARGETED_WITHOUT_LOS

**Hash:** `0x4328652AE5769C71` | **JHash:** `0x7FDDC0A6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_TARGETED_WITHOUT_LOS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_TARGETED_WITHOUT_LOS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_TO_INFORM_RESPECTED_FRIENDS

**Hash:** `0x112942C6E708F70B` | **JHash:** `0xD78AC46C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_TO_INFORM_RESPECTED_FRIENDS(Ped ped, float radius, int maxFriends)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_TO_INFORM_RESPECTED_FRIENDS, ped, radius, maxFriends);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `radius` | `float` | `float` | |
| `maxFriends` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_RESPONDING_TO_EVENT

**Hash:** `0x625B774D75C87068` | **JHash:** `0x7A877554` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RESPONDING_TO_EVENT(Ped ped, Any event)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RESPONDING_TO_EVENT, ped, event);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `event` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_POS_FROM_FIRED_EVENT

**Hash:** `0xBA656A3BB01BDEA3` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL GET_POS_FROM_FIRED_EVENT(Ped ped, int eventType, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_POS_FROM_FIRED_EVENT, ped, eventType, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `eventType` | `int` | `int` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_FIRING_PATTERN

**Hash:** `0x9AC577F5A12AD8A9` | **JHash:** `0xB4629D66` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_FIRING_PATTERN(Ped ped, Hash patternHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_FIRING_PATTERN, ped, patternHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `patternHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** FIRING_PATTERN_BURST_FIRE = 0xD6FF6D61 ( 1073727030 )
FIRING_PATTERN_BURST_FIRE_IN_COVER = 0x026321F1 ( 40051185 )
FIRING_PATTERN_BURST_FIRE_DRIVEBY = 0xD31265F2 ( -753768974 )
FIRING_PATTERN_FROM_GROUND = 0x2264E5D6 ( 577037782 )
FIRING_PATTERN_DELAY_FIRE_BY_ONE_SEC = 0x7A845691 ( 2055493265 )
FIRING_PATTERN_FULL_AUTO = 0xC6EE6B4C ( -957453492 )
FIRING_PATTERN_SINGLE_SHOT = 0x5D60E4E0 ( 1566631136 )
FIRING_PATTERN_BURST_FIRE_PISTOL = 0xA018DB8A ( -1608983670 )
FIRING_PATTERN_BURST_FIRE_SMG = 0xD10DADEE ( 1863348768 )
FIRING_PATTERN_BURST_FIRE_RIFLE = 0x9C74B406 ( -1670073338 )
FIRING_PATTERN_BURST_FIRE_MG = 0xB573C5B4 ( -1250703948 )
FIRING_PATTERN_BURST_FIRE_PUMPSHOTGUN = 0x00BAC39B ( 12239771 )
FIRING_PATTERN_BURST_FIRE_HELI = 0x914E786F ( -1857128337 )
FIRING_PATTERN_BURST_FIRE_MICRO = 0x42EF03FD ( 1122960381 )
FIRING_PATTERN_SHORT_BURSTS = 0x1A92D7DF ( 445831135 )
FIRING_PATTERN_SLOW_FIRE_TANK = 0xE2CA3A71 ( -490063247 )

Firing pattern info: https://pastebin.com/Px036isB

---

### SET_PED_SHOOT_RATE

**Hash:** `0x614DA022990752DC` | **JHash:** `0xFB301746` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOOT_RATE(Ped ped, int shootRate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOOT_RATE, ped, shootRate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `shootRate` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** shootRate 0-1000

---

### SET_COMBAT_FLOAT

**Hash:** `0xFF41B4B141ED981C` | **JHash:** `0xD8B7637C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_COMBAT_FLOAT(Ped ped, int combatType, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_COMBAT_FLOAT, ped, combatType, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `combatType` | `int` | `int` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** combatType can be between 0-14. See GET_COMBAT_FLOAT below for a list of possible parameters.

---

### GET_COMBAT_FLOAT

**Hash:** `0x52DFF8A10508090A` | **JHash:** `0x511D7EF8` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_COMBAT_FLOAT(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_COMBAT_FLOAT, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** p0: Ped Handle
p1: int i | 0 <= i <= 27

p1 probably refers to the attributes configured in combatbehavior.meta. There are 13. Example:

<BlindFireChance value="0.1"/>
<WeaponShootRateModifier value="1.0"/>
<TimeBetweenBurstsInCover value="1.25"/>
<BurstDurationInCover value="2.0"/>
<TimeBetweenPeeks value="10.0"/>
<WeaponAccuracy value="0.18"/>
<FightProficiency value="0.8"/>
<StrafeWhenMovingChance value="1.0"/>
<WalkWhenStrafingChance value="0.0"/>
<AttackWindowDistanceForCover value="55.0"/>
<TimeToInvalidateInjuredTarget value="9.0"/>
<TriggerChargeTime_Near value="4.0"/>
<TriggerChargeTime_Far value="10.0"/>

-------------Confirmed by editing combatbehavior.meta:
p1:
0=BlindFireChance
1=BurstDurationInCover
3=TimeBetweenBurstsInCover
4=TimeBetweenPeeks
5=StrafeWhenMovingChance
8=WalkWhenStrafingChance
11=AttackWindowDistanceForCover
12=TimeToInvalidateInjuredTarget
16=OptimalCoverDistance


---

### GET_GROUP_SIZE

**Hash:** `0x8DE69FE35CA09A45` | **JHash:** `0xF7E1A691` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_GROUP_SIZE(int groupID, Any* p1, int* sizeInMembers)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_GROUP_SIZE, groupID, p1, sizeInMembers);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupID` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |
| `sizeInMembers` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** p1 may be a BOOL representing whether or not the group even exists

---

### DOES_GROUP_EXIST

**Hash:** `0x7C6B0C22F9F40BBE` | **JHash:** `0x935C978D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_GROUP_EXIST(int groupId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_GROUP_EXIST, groupId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PED_GROUP_INDEX

**Hash:** `0xF162E133B4E7A675` | **JHash:** `0x134E0785` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_GROUP_INDEX(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_GROUP_INDEX, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the group id of which the specified ped is a member of.

---

### IS_PED_IN_GROUP

**Hash:** `0x5891CAC5D4ACFF74` | **JHash:** `0x836D9795` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_GROUP(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_GROUP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PLAYER_PED_IS_FOLLOWING

**Hash:** `0x6A3975DEA89F9A17` | **JHash:** `0xDE7442EE` | **Build:** `323`

**C++ 原型:**
```cpp
Player GET_PLAYER_PED_IS_FOLLOWING(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_PED_IS_FOLLOWING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Player` → `int`

---

### SET_GROUP_FORMATION

**Hash:** `0xCE2F5FC3AF7E8C1E` | **JHash:** `0x08FAC739` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GROUP_FORMATION(int groupId, int formationType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GROUP_FORMATION, groupId, formationType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupId` | `int` | `int` | |
| `formationType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 0: Default
1: Circle Around Leader
2: Alternative Circle Around Leader
3: Line, with Leader at center

---

### SET_GROUP_FORMATION_SPACING

**Hash:** `0x1D9D45004C28C916` | **JHash:** `0xB1E086FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GROUP_FORMATION_SPACING(int groupId, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GROUP_FORMATION_SPACING, groupId, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupId` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_GROUP_FORMATION_DEFAULT_SPACING

**Hash:** `0x63DAB4CCB3273205` | **JHash:** `0x267FCEAD` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_GROUP_FORMATION_DEFAULT_SPACING(int groupHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_GROUP_FORMATION_DEFAULT_SPACING, groupHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupHandle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_PED_IS_USING

**Hash:** `0x6094AD011A2EA87D` | **JHash:** `0x6DE3AADA` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_PED_IS_USING(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_PED_IS_USING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Gets ID of vehicle player using. It means it can get ID at any interaction with vehicle. Enter\exit for example. And that means it is faster than GET_VEHICLE_PED_IS_IN but less safe.

---

### GET_VEHICLE_PED_IS_ENTERING

**Hash:** `0xF92691AED837A5FC` | **JHash:** `0x56E0C163` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_PED_IS_ENTERING(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_PED_IS_ENTERING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Vehicle` → `int`

---

### SET_PED_GRAVITY

**Hash:** `0x9FF447B6B6AD960A` | **JHash:** `0x3CA16652` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GRAVITY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GRAVITY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enable or disable the gravity of a ped

Examples:
PED::SET_PED_GRAVITY(PLAYER::PLAYER_PED_ID(), 0x00000001);
PED::SET_PED_GRAVITY(Local_289[iVar0 /*20*/], 0x00000001);

---

### _SET_PED_SURVIVES_BEING_OUT_OF_WATER

**Hash:** `0x100CD221F572F6E1` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _SET_PED_SURVIVES_BEING_OUT_OF_WATER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._SET_PED_SURVIVES_BEING_OUT_OF_WATER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### APPLY_DAMAGE_TO_PED

**Hash:** `0x697157CED63F18D4` | **JHash:** `0x4DC27FCF` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_DAMAGE_TO_PED(Ped ped, int damageAmount, BOOL p2, Any p3, Hash weaponType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_DAMAGE_TO_PED, ped, damageAmount, p2, p3, weaponType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `damageAmount` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |
| `weaponType` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** damages a ped with the given amount

---

### GET_TIME_PED_DAMAGED_BY_WEAPON

**Hash:** `0x36B77BB84687C318` | **JHash:** `0x0170C6E1` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_PED_DAMAGED_BY_WEAPON(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_PED_DAMAGED_BY_WEAPON, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### SET_PED_ALLOWED_TO_DUCK

**Hash:** `0xDA1F1B7BE1A8766F` | **JHash:** `0xC4D122F8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALLOWED_TO_DUCK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALLOWED_TO_DUCK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_NEVER_LEAVES_GROUP

**Hash:** `0x3DBFC55D5C9BB447` | **JHash:** `0x0E038813` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_NEVER_LEAVES_GROUP(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_NEVER_LEAVES_GROUP, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PED_TYPE

**Hash:** `0xFF059E1E4C01E63C` | **JHash:** `0xB1460D43` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_TYPE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_TYPE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** https://alloc8or.re/gta5/doc/enums/ePedType.txt

---

### SET_PED_AS_COP

**Hash:** `0xBB03C38DD3FB7FFD` | **JHash:** `0x84E7DE9F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AS_COP(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AS_COP, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Turns the desired ped into a cop. If you use this on the player ped, you will become almost invisible to cops dispatched for you. You will also report your own crimes, get a generic cop voice, get a cop-vision-cone on the radar, and you will be unable to shoot at other cops. SWAT and Army will still shoot at you. Toggling ped as "false" has no effect; you must change p0's ped model to disable the effect.

---

### SET_PED_HEALTH_PENDING_LAST_DAMAGE_EVENT_OVERRIDE_FLAG

**Hash:** `0xB3352E018D6F89DF` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_PED_HEALTH_PENDING_LAST_DAMAGE_EVENT_OVERRIDE_FLAG(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEALTH_PENDING_LAST_DAMAGE_EVENT_OVERRIDE_FLAG, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_MAX_HEALTH

**Hash:** `0xF5F6378C4F3419D3` | **JHash:** `0x5533F60B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MAX_HEALTH(Ped ped, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MAX_HEALTH, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the maximum health of a ped.

---

### GET_PED_MAX_HEALTH

**Hash:** `0x4700A416E8324EF3` | **JHash:** `0xA45B6C8D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_MAX_HEALTH(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_MAX_HEALTH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_MAX_TIME_IN_WATER

**Hash:** `0x43C851690662113D` | **JHash:** `0xFE0A106B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MAX_TIME_IN_WATER(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MAX_TIME_IN_WATER, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_MAX_TIME_UNDERWATER

**Hash:** `0x6BA428C528D9E522` | **JHash:** `0x082EF240` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MAX_TIME_UNDERWATER(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MAX_TIME_UNDERWATER, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CORPSE_RAGDOLL_FRICTION

**Hash:** `0x2735233A786B1BEF` | **JHash:** `0x373CC405` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CORPSE_RAGDOLL_FRICTION(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CORPSE_RAGDOLL_FRICTION, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_VEHICLE_FORCED_SEAT_USAGE

**Hash:** `0x952F06BEECD775CC` | **JHash:** `0x79CA237B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_VEHICLE_FORCED_SEAT_USAGE(Ped ped, Vehicle vehicle, int seatIndex, int flags, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_VEHICLE_FORCED_SEAT_USAGE, ped, vehicle, seatIndex, flags, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |
| `flags` | `int` | `int` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** seatIndex must be <= 2

---

### CLEAR_ALL_PED_VEHICLE_FORCED_SEAT_USAGE

**Hash:** `0xE6CA85E7259CE16B` | **JHash:** `0xCC1D8F66` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_PED_VEHICLE_FORCED_SEAT_USAGE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_PED_VEHICLE_FORCED_SEAT_USAGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_KNOCKED_OFF_BIKE

**Hash:** `0xB282749D5E028163` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_KNOCKED_OFF_BIKE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_KNOCKED_OFF_BIKE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### SET_PED_CAN_BE_KNOCKED_OFF_VEHICLE

**Hash:** `0x7A6535691B477C48` | **JHash:** `0x8A251612` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_KNOCKED_OFF_VEHICLE(Ped ped, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_KNOCKED_OFF_VEHICLE, ped, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** state: https://alloc8or.re/gta5/doc/enums/eKnockOffVehicle.txt

---

### CAN_KNOCK_PED_OFF_VEHICLE

**Hash:** `0x51AC07A44D4F5B8A` | **JHash:** `0xC9D098B3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_KNOCK_PED_OFF_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_KNOCK_PED_OFF_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### KNOCK_PED_OFF_VEHICLE

**Hash:** `0x45BBCBA77C29A841` | **JHash:** `0xACDD0674` | **Build:** `323`

**C++ 原型:**
```cpp
void KNOCK_PED_OFF_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.KNOCK_PED_OFF_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_COORDS_NO_GANG

**Hash:** `0x87052FE446E07247` | **JHash:** `0x9561AD98` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COORDS_NO_GANG(Ped ped, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COORDS_NO_GANG, ped, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PED_AS_GROUP_MEMBER

**Hash:** `0x51455483CF23ED97` | **JHash:** `0x9AA3CC8C` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PED_AS_GROUP_MEMBER(int groupID, int memberNumber)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_AS_GROUP_MEMBER, groupID, memberNumber);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupID` | `int` | `int` | |
| `memberNumber` | `int` | `int` | |

**返回值:** `Ped` → `int`

**说明:** from fm_mission_controller.c4 (variable names changed for clarity):

int groupID = PLAYER::GET_PLAYER_GROUP(PLAYER::PLAYER_ID());
PED::GET_GROUP_SIZE(group, &unused, &groupSize);
if (groupSize >= 1) {
. . . . for (int memberNumber = 0; memberNumber < groupSize; memberNumber++) {
. . . . . . . . Ped ped1 = PED::GET_PED_AS_GROUP_MEMBER(groupID, memberNumber);
. . . . . . . . //and so on

---

### GET_PED_AS_GROUP_LEADER

**Hash:** `0x5CCE68DBD5FE93EC` | **JHash:** `0x52873C6A` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PED_AS_GROUP_LEADER(int groupID)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_AS_GROUP_LEADER, groupID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `groupID` | `int` | `int` | |

**返回值:** `Ped` → `int`

---

### SET_PED_KEEP_TASK

**Hash:** `0x971D38760FBC02EF` | **JHash:** `0xA7EC79CE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_KEEP_TASK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_KEEP_TASK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_ALLOW_MINOR_REACTIONS_AS_MISSION_PED

**Hash:** `0x49E50BDB8BA4DAB2` | **JHash:** `0x397F06E3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALLOW_MINOR_REACTIONS_AS_MISSION_PED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALLOW_MINOR_REACTIONS_AS_MISSION_PED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_SWIMMING

**Hash:** `0x9DE327631295B4C2` | **JHash:** `0x7AB43DB8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SWIMMING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SWIMMING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_SWIMMING_UNDER_WATER

**Hash:** `0xC024869A53992F34` | **JHash:** `0x0E8D524F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SWIMMING_UNDER_WATER(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SWIMMING_UNDER_WATER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_COORDS_KEEP_VEHICLE

**Hash:** `0x9AFEFF481A85AB2E` | **JHash:** `0xD66AE1D3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COORDS_KEEP_VEHICLE(Ped ped, float posX, float posY, float posZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COORDS_KEEP_VEHICLE, ped, posX, posY, posZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** teleports ped to coords along with the vehicle ped is in

---

### SET_PED_DIES_IN_VEHICLE

**Hash:** `0x2A30922C90C9B42C` | **JHash:** `0x6FE1E440` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DIES_IN_VEHICLE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DIES_IN_VEHICLE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CREATE_RANDOM_COPS

**Hash:** `0x102E68B2024D536D` | **JHash:** `0x23441648` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CREATE_RANDOM_COPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CREATE_RANDOM_COPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CREATE_RANDOM_COPS_NOT_ON_SCENARIOS

**Hash:** `0x8A4986851C4EF6E7` | **JHash:** `0x82E548CC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CREATE_RANDOM_COPS_NOT_ON_SCENARIOS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CREATE_RANDOM_COPS_NOT_ON_SCENARIOS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CREATE_RANDOM_COPS_ON_SCENARIOS

**Hash:** `0x444CB7D7DBE6973D` | **JHash:** `0xEDC31475` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CREATE_RANDOM_COPS_ON_SCENARIOS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CREATE_RANDOM_COPS_ON_SCENARIOS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CAN_CREATE_RANDOM_COPS

**Hash:** `0x5EE2CAFF7F17770D` | **JHash:** `0xAA73DAD9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_CREATE_RANDOM_COPS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_CREATE_RANDOM_COPS);
```

**返回值:** `BOOL` → `bool`

---

### SET_PED_AS_ENEMY

**Hash:** `0x02A0C9720B854BFA` | **JHash:** `0xAE620A1B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AS_ENEMY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AS_ENEMY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_SMASH_GLASS

**Hash:** `0x1CCE141467FF42A2` | **JHash:** `0x149C60A8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_SMASH_GLASS(Ped ped, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_SMASH_GLASS, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_IN_ANY_TRAIN

**Hash:** `0x6F972C1AB75A1ED0` | **JHash:** `0x759EF63A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_ANY_TRAIN(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_ANY_TRAIN, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_GETTING_INTO_A_VEHICLE

**Hash:** `0xBB062B2B5722478E` | **JHash:** `0x90E805AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_GETTING_INTO_A_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_GETTING_INTO_A_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_TRYING_TO_ENTER_A_LOCKED_VEHICLE

**Hash:** `0x44D28D5DDFE5F68C` | **JHash:** `0x46828B4E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_TRYING_TO_ENTER_A_LOCKED_VEHICLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_TRYING_TO_ENTER_A_LOCKED_VEHICLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ENABLE_HANDCUFFS

**Hash:** `0xDF1AF8B5D56542FA` | **JHash:** `0xAC9BBA23` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENABLE_HANDCUFFS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENABLE_HANDCUFFS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** ped can not pull out a weapon when true

---

### SET_ENABLE_BOUND_ANKLES

**Hash:** `0xC52E0F855C58FC2E` | **JHash:** `0x9208D689` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENABLE_BOUND_ANKLES(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENABLE_BOUND_ANKLES, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used with SET_ENABLE_HANDCUFFS in decompiled scripts. From my observations, I have noticed that while being ragdolled you are not able to get up but you can still run. Your legs can also bend.

---

### SET_ENABLE_SCUBA

**Hash:** `0xF99F62004024D506` | **JHash:** `0x7BF61471` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENABLE_SCUBA(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENABLE_SCUBA, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables diving motion when underwater.

---

### SET_CAN_ATTACK_FRIENDLY

**Hash:** `0xB3B1CB349FF9C75D` | **JHash:** `0x47C60963` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_ATTACK_FRIENDLY(Ped ped, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_ATTACK_FRIENDLY, ped, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Setting ped to true allows the ped to shoot "friendlies".

p2 set to true when toggle is also true seams to make peds permanently unable to aim at, even if you set p2 back to false.

p1 = false & p2 = false for unable to aim at.
p1 = true & p2 = false for able to aim at. 

---

### GET_PED_ALERTNESS

**Hash:** `0xF6AA118530443FD2` | **JHash:** `0xF83E4DAF` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_ALERTNESS(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_ALERTNESS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the ped's alertness (0-3).

Values : 

0 : Neutral
1 : Heard something (gun shot, hit, etc)
2 : Knows (the origin of the event)
3 : Fully alerted (is facing the event?)

If the Ped does not exist, returns -1.

---

### SET_PED_ALERTNESS

**Hash:** `0xDBA71115ED9941A6` | **JHash:** `0x2C32D9AE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALERTNESS(Ped ped, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALERTNESS, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** value ranges from 0 to 3.

---

### SET_PED_GET_OUT_UPSIDE_DOWN_VEHICLE

**Hash:** `0xBC0ED94165A48BC2` | **JHash:** `0x89AD49FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GET_OUT_UPSIDE_DOWN_VEHICLE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GET_OUT_UPSIDE_DOWN_VEHICLE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_MOVEMENT_CLIPSET

**Hash:** `0xAF8A94EDE7712BEF` | **JHash:** `0xA817CDEB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MOVEMENT_CLIPSET(Ped ped, const char* clipSet, float transitionSpeed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOVEMENT_CLIPSET, ped, clipSet, transitionSpeed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `clipSet` | `const char*` | `string` | |
| `transitionSpeed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** transitionSpeed is the time in seconds it takes to transition from one movement clipset to another.	ransitionSpeed is usually 1.0f

List of movement clipsets:
Thanks to elsewhat for list.

 "ANIM_GROUP_MOVE_BALLISTIC"
 "ANIM_GROUP_MOVE_LEMAR_ALLEY"
 "clipset@move@trash_fast_turn"
 "FEMALE_FAST_RUNNER"
 "missfbi4prepp1_garbageman"
 "move_characters@franklin@fire"
 "move_characters@Jimmy@slow@"
 "move_characters@michael@fire"
 "move_f@flee@a"
 "move_f@scared"
 "move_f@sexy@a"
 "move_heist_lester"
 "move_injured_generic"
 "move_lester_CaneUp"
 "move_m@bag"
 "MOVE_M@BAIL_BOND_NOT_TAZERED"
 "MOVE_M@BAIL_BOND_TAZERED"
 "move_m@brave"
 "move_m@casual@d"
 "move_m@drunk@moderatedrunk"
 "MOVE_M@DRUNK@MODERATEDRUNK"
 "MOVE_M@DRUNK@MODERATEDRUNK_HEAD_UP"
 "MOVE_M@DRUNK@SLIGHTLYDRUNK"
 "MOVE_M@DRUNK@VERYDRUNK"
 "move_m@fire"
 "move_m@gangster@var_e"
 "move_m@gangster@var_f"
 "move_m@gangster@var_i"
 "move_m@JOG@"
 "MOVE_M@PRISON_GAURD"
 "MOVE_P_M_ONE"
 "MOVE_P_M_ONE_BRIEFCASE"
 "move_p_m_zero_janitor"
 "move_p_m_zero_slow"
 "move_ped_bucket"
 "move_ped_crouched"
 "move_ped_mop"
 "MOVE_M@FEMME@"
 "MOVE_F@FEMME@"
 "MOVE_M@GANGSTER@NG"
 "MOVE_F@GANGSTER@NG"
 "MOVE_M@POSH@"
 "MOVE_F@POSH@"
 "MOVE_M@TOUGH_GUY@"
 "MOVE_F@TOUGH_GUY@"

~ NotCrunchyTaco

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### RESET_PED_MOVEMENT_CLIPSET

**Hash:** `0xAA74EC0CB0AAEA2C` | **JHash:** `0xB83CEE93` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_MOVEMENT_CLIPSET(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_MOVEMENT_CLIPSET, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** If p1 is 0.0, I believe you are back to normal. 
If p1 is 1.0, it looks like you can only rotate the ped, not walk.

Using the following code to reset back to normal
PED::RESET_PED_MOVEMENT_CLIPSET(PLAYER::PLAYER_PED_ID(), 0.0);

---

### SET_PED_STRAFE_CLIPSET

**Hash:** `0x29A28F3F8CF6D854` | **JHash:** `0x0BACF010` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STRAFE_CLIPSET(Ped ped, const char* clipSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STRAFE_CLIPSET, ped, clipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `clipSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### RESET_PED_STRAFE_CLIPSET

**Hash:** `0x20510814175EA477` | **JHash:** `0xF1967A12` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_STRAFE_CLIPSET(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_STRAFE_CLIPSET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_WEAPON_MOVEMENT_CLIPSET

**Hash:** `0x2622E35B77D3ACA2` | **JHash:** `0xF8BE54DC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_WEAPON_MOVEMENT_CLIPSET(Ped ped, const char* clipSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WEAPON_MOVEMENT_CLIPSET, ped, clipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `clipSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### RESET_PED_WEAPON_MOVEMENT_CLIPSET

**Hash:** `0x97B0DB5B4AA74E77` | **JHash:** `0xC60C9ACD` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_WEAPON_MOVEMENT_CLIPSET(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_WEAPON_MOVEMENT_CLIPSET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_DRIVE_BY_CLIPSET_OVERRIDE

**Hash:** `0xED34AB6C5CB36520` | **JHash:** `0xD4C73595` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DRIVE_BY_CLIPSET_OVERRIDE(Ped ped, const char* clipset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DRIVE_BY_CLIPSET_OVERRIDE, ped, clipset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `clipset` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_DRIVE_BY_CLIPSET_OVERRIDE

**Hash:** `0x4AFE3690D7E0B5AC` | **JHash:** `0xAEC9163B` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_DRIVE_BY_CLIPSET_OVERRIDE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_DRIVE_BY_CLIPSET_OVERRIDE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE

**Hash:** `0x9DBA107B4937F809` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE(Ped ped, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Found in the b617d scripts:
PED::SET_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE(v_7, "trevor_heist_cover_2h");

---

### CLEAR_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE

**Hash:** `0xC79196DCB36F6121` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_MOTION_IN_COVER_CLIPSET_OVERRIDE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_FALL_UPPER_BODY_CLIPSET_OVERRIDE

**Hash:** `0x80054D7FCC70EEC6` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_FALL_UPPER_BODY_CLIPSET_OVERRIDE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_FALL_UPPER_BODY_CLIPSET_OVERRIDE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_IN_VEHICLE_CONTEXT

**Hash:** `0x530071295899A8C6` | **JHash:** `0x27F25C0E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_IN_VEHICLE_CONTEXT(Ped ped, Hash context)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_IN_VEHICLE_CONTEXT, ped, context);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `context` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** PED::SET_PED_IN_VEHICLE_CONTEXT(l_128, MISC::GET_HASH_KEY("MINI_PROSTITUTE_LOW_PASSENGER"));
PED::SET_PED_IN_VEHICLE_CONTEXT(l_128, MISC::GET_HASH_KEY("MINI_PROSTITUTE_LOW_RESTRICTED_PASSENGER"));
PED::SET_PED_IN_VEHICLE_CONTEXT(l_3212, MISC::GET_HASH_KEY("MISS_FAMILY1_JIMMY_SIT"));
PED::SET_PED_IN_VEHICLE_CONTEXT(l_3212, MISC::GET_HASH_KEY("MISS_FAMILY1_JIMMY_SIT_REAR"));
PED::SET_PED_IN_VEHICLE_CONTEXT(l_95, MISC::GET_HASH_KEY("MISS_FAMILY2_JIMMY_BICYCLE"));
PED::SET_PED_IN_VEHICLE_CONTEXT(num3, MISC::GET_HASH_KEY("MISSFBI2_MICHAEL_DRIVEBY"));
PED::SET_PED_IN_VEHICLE_CONTEXT(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("MISS_ARMENIAN3_FRANKLIN_TENSE"));
PED::SET_PED_IN_VEHICLE_CONTEXT(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("MISSFBI5_TREVOR_DRIVING"));

---

### RESET_PED_IN_VEHICLE_CONTEXT

**Hash:** `0x22EF8FF8778030EB` | **JHash:** `0x3C94D88A` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_IN_VEHICLE_CONTEXT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_IN_VEHICLE_CONTEXT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_SCRIPTED_SCENARIO_PED_USING_CONDITIONAL_ANIM

**Hash:** `0x6EC47A344923E1ED` | **JHash:** `0x3C30B447` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCRIPTED_SCENARIO_PED_USING_CONDITIONAL_ANIM(Ped ped, const char* animDict, const char* anim)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCRIPTED_SCENARIO_PED_USING_CONDITIONAL_ANIM, ped, animDict, anim);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `anim` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### SET_PED_ALTERNATE_WALK_ANIM

**Hash:** `0x6C60394CB4F75E9A` | **JHash:** `0x895E1D67` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALTERNATE_WALK_ANIM(Ped ped, const char* animDict, const char* animName, float p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALTERNATE_WALK_ANIM, ped, animDict, animName, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `p3` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### CLEAR_PED_ALTERNATE_WALK_ANIM

**Hash:** `0x8844BBFCE30AA9E9` | **JHash:** `0x5736FB23` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_ALTERNATE_WALK_ANIM(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_ALTERNATE_WALK_ANIM, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_ALTERNATE_MOVEMENT_ANIM

**Hash:** `0x90A43CC281FFAB46` | **JHash:** `0xBA84FD8C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ALTERNATE_MOVEMENT_ANIM(Ped ped, int stance, const char* animDictionary, const char* animationName, float p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ALTERNATE_MOVEMENT_ANIM, ped, stance, animDictionary, animationName, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `stance` | `int` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animationName` | `const char*` | `string` | |
| `p4` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** stance:
0 = idle
1 = walk
2 = running

p5 = usually set to true

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### CLEAR_PED_ALTERNATE_MOVEMENT_ANIM

**Hash:** `0xD8D19675ED5FBDCE` | **JHash:** `0x7A7F5BC3` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_ALTERNATE_MOVEMENT_ANIM(Ped ped, int stance, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_ALTERNATE_MOVEMENT_ANIM, ped, stance, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `stance` | `int` | `int` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_GESTURE_GROUP

**Hash:** `0xDDF803377F94AAA8` | **JHash:** `0x170DA109` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GESTURE_GROUP(Ped ped, const char* animGroupGesture)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GESTURE_GROUP, ped, animGroupGesture);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animGroupGesture` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** From the scripts:
PED::SET_PED_GESTURE_GROUP(PLAYER::PLAYER_PED_ID(),
"ANIM_GROUP_GESTURE_MISS_FRA0");
PED::SET_PED_GESTURE_GROUP(PLAYER::PLAYER_PED_ID(),
"ANIM_GROUP_GESTURE_MISS_DocksSetup1");

---

### GET_ANIM_INITIAL_OFFSET_POSITION

**Hash:** `0xBE22B26DD764C040` | **JHash:** `0xC59D4268` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ANIM_INITIAL_OFFSET_POSITION(const char* animDict, const char* animName, float x, float y, float z, float xRot, float yRot, float zRot, float p8, int p9)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ANIM_INITIAL_OFFSET_POSITION, animDict, animName, x, y, z, xRot, yRot, zRot, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_ANIM_INITIAL_OFFSET_ROTATION

**Hash:** `0x4B805E6046EE9E47` | **JHash:** `0x5F7789E6` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ANIM_INITIAL_OFFSET_ROTATION(const char* animDict, const char* animName, float x, float y, float z, float xRot, float yRot, float zRot, float p8, int p9)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ANIM_INITIAL_OFFSET_ROTATION, animDict, animName, x, y, z, xRot, yRot, zRot, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### GET_PED_DRAWABLE_VARIATION

**Hash:** `0x67F3780DD425D4FC` | **JHash:** `0x29850FE2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_DRAWABLE_VARIATION(Ped ped, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_DRAWABLE_VARIATION, ped, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Ids
0 - Head
1 - Beard
2 - Hair
3 - Torso
4 - Legs
5 - Hands
6 - Foot
7 - ------
8 - Accessories 1
9 - Accessories 2
10- Decals
11 - Auxiliary parts for torso

---

### GET_NUMBER_OF_PED_DRAWABLE_VARIATIONS

**Hash:** `0x27561561732A7842` | **JHash:** `0x9754C27D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PED_DRAWABLE_VARIATIONS(Ped ped, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PED_DRAWABLE_VARIATIONS, ped, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_PED_TEXTURE_VARIATION

**Hash:** `0x04A355E041E004E6` | **JHash:** `0xC0A8590A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_TEXTURE_VARIATION(Ped ped, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_TEXTURE_VARIATION, ped, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_NUMBER_OF_PED_TEXTURE_VARIATIONS

**Hash:** `0x8F7156A3142A6BAD` | **JHash:** `0x83D9FBE7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PED_TEXTURE_VARIATIONS(Ped ped, int componentId, int drawableId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PED_TEXTURE_VARIATIONS, ped, componentId, drawableId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_NUMBER_OF_PED_PROP_DRAWABLE_VARIATIONS

**Hash:** `0x5FAF9754E789FB47` | **JHash:** `0xC9780B95` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PED_PROP_DRAWABLE_VARIATIONS(Ped ped, int propId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PED_PROP_DRAWABLE_VARIATIONS, ped, propId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `propId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS

**Hash:** `0xA6E7F1CEB523E171` | **JHash:** `0x4892B882` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS(Ped ped, int propId, int drawableId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS, ped, propId, drawableId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `propId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Need to check behavior when drawableId = -1

- Doofy.Ass
Why this function doesn't work and return nill value?
GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS(PLAYER.PLAYER_PED_ID(), 0, 5)

tick: scripts/addins/menu_execute.lua:51: attempt to call field 'GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS' (a nil value)


List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_PED_PALETTE_VARIATION

**Hash:** `0xE3DD5F2A84B42281` | **JHash:** `0xEF1BC082` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_PALETTE_VARIATION(Ped ped, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_PALETTE_VARIATION, ped, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### GET_MP_OUTFIT_DATA_FROM_METADATA

**Hash:** `0x9E30E91FB03A2CAF` | **JHash:** `0x52559BB6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MP_OUTFIT_DATA_FROM_METADATA(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MP_OUTFIT_DATA_FROM_METADATA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### GET_FM_MALE_SHOP_PED_APPAREL_ITEM_INDEX

**Hash:** `0x1E77FA7A62EE6C4C` | **JHash:** `0x021F5B36` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FM_MALE_SHOP_PED_APPAREL_ITEM_INDEX(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FM_MALE_SHOP_PED_APPAREL_ITEM_INDEX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_FM_FEMALE_SHOP_PED_APPAREL_ITEM_INDEX

**Hash:** `0xF033419D1B81FAE8` | **JHash:** `0x8FDDB5DB` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_FM_FEMALE_SHOP_PED_APPAREL_ITEM_INDEX(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_FM_FEMALE_SHOP_PED_APPAREL_ITEM_INDEX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### IS_PED_COMPONENT_VARIATION_VALID

**Hash:** `0xE825F6B6CEA7671D` | **JHash:** `0x952ABD9A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_COMPONENT_VARIATION_VALID(Ped ped, int componentId, int drawableId, int textureId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_COMPONENT_VARIATION_VALID, ped, componentId, drawableId, textureId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the component variation is valid, this works great for randomizing components using loops.

List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

Full list of ped components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedComponentVariations.json

---

### SET_PED_COMPONENT_VARIATION

**Hash:** `0x262B14F48D29DE80` | **JHash:** `0xD4F7B05C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COMPONENT_VARIATION(Ped ped, int componentId, int drawableId, int textureId, int paletteId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COMPONENT_VARIATION, ped, componentId, drawableId, textureId, paletteId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |
| `paletteId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** paletteId: 0 to 3.

componentId:
enum ePedVarComp
{
	PV_COMP_INVALID = -1,
	PV_COMP_HEAD,
	PV_COMP_BERD,
	PV_COMP_HAIR,
	PV_COMP_UPPR,
	PV_COMP_LOWR,
	PV_COMP_HAND,
	PV_COMP_FEET,
	PV_COMP_TEEF,
	PV_COMP_ACCS,
	PV_COMP_TASK,
	PV_COMP_DECL,
	PV_COMP_JBIB,
	PV_COMP_MAX
};

Examples: https://gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

Full list of ped components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedComponentVariations.json

---

### SET_PED_RANDOM_COMPONENT_VARIATION

**Hash:** `0xC8A9481A01E63C28` | **JHash:** `0x4111BA46` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RANDOM_COMPONENT_VARIATION(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RANDOM_COMPONENT_VARIATION, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0 in R* scripts.

List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### SET_PED_RANDOM_PROPS

**Hash:** `0xC44AA05345C992C6` | **JHash:** `0xE3318E0E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RANDOM_PROPS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RANDOM_PROPS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### SET_PED_DEFAULT_COMPONENT_VARIATION

**Hash:** `0x45EEE61580806D63` | **JHash:** `0xC866A984` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DEFAULT_COMPONENT_VARIATION(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DEFAULT_COMPONENT_VARIATION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets Ped Default Clothes

---

### SET_PED_BLEND_FROM_PARENTS

**Hash:** `0x137BBD05230DB22D` | **JHash:** `0x837BD370` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_BLEND_FROM_PARENTS(Ped ped, Any p1, Any p2, float p3, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_BLEND_FROM_PARENTS, ped, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_HEAD_BLEND_DATA

**Hash:** `0x9414E18B9434C2FE` | **JHash:** `0x60746B88` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HEAD_BLEND_DATA(Ped ped, int shapeFirstID, int shapeSecondID, int shapeThirdID, int skinFirstID, int skinSecondID, int skinThirdID, float shapeMix, float skinMix, float thirdMix, BOOL isParent)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEAD_BLEND_DATA, ped, shapeFirstID, shapeSecondID, shapeThirdID, skinFirstID, skinSecondID, skinThirdID, shapeMix, skinMix, thirdMix, isParent);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `shapeFirstID` | `int` | `int` | |
| `shapeSecondID` | `int` | `int` | |
| `shapeThirdID` | `int` | `int` | |
| `skinFirstID` | `int` | `int` | |
| `skinSecondID` | `int` | `int` | |
| `skinThirdID` | `int` | `int` | |
| `shapeMix` | `float` | `float` | |
| `skinMix` | `float` | `float` | |
| `thirdMix` | `float` | `float` | |
| `isParent` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The "shape" parameters control the shape of the ped's face. The "skin" parameters control the skin tone. ShapeMix and skinMix control how much the first and second IDs contribute,(typically mother and father.) ThirdMix overrides the others in favor of the third IDs. IsParent is set for "children" of the player character's grandparents during old-gen character creation. It has unknown effect otherwise.

The IDs start at zero and go Male Non-DLC, Female Non-DLC, Male DLC, and Female DLC.

!!!Can someone add working example for this???

try this:
       headBlendData headData;
       GET_PED_HEAD_BLEND_DATA(PLAYER_PED_ID(), &headData);

       SET_PED_HEAD_BLEND_DATA(PLAYER_PED_ID(), headData.shapeFirst, headData.shapeSecond, headData.shapeThird, headData.skinFirst, headData.skinSecond
          , headData.skinThird, headData.shapeMix, headData.skinMix, headData.skinThird, 0);


For more info please refer to this topic. 
gtaforums.com/topic/858970-all-gtao-face-ids-pedset-ped-head-blend-data-explained

---

### GET_PED_HEAD_BLEND_DATA

**Hash:** `0x2746BD9D88C5C5D0` | **JHash:** `0x44E1680C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_HEAD_BLEND_DATA(Ped ped, Any* headBlendData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_HEAD_BLEND_DATA, ped, headBlendData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `headBlendData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** The pointer is to a padded struct that matches the arguments to SET_PED_HEAD_BLEND_DATA(...). There are 4 bytes of padding after each field.
pass this struct in the second parameter 
struct headBlendData
{
    int shapeFirst;
    int padding1;
    int shapeSecond;
    int padding2;
    int shapeThird;
    int padding3;
    int skinFirst;
    int padding4;
    int skinSecond;
    int padding5;
    int skinThird;
    int padding6;
    float shapeMix;
    int padding7;
    float skinMix;
    int padding8;
    float thirdMix;
    int padding9;
    bool isParent;
};

---

### UPDATE_PED_HEAD_BLEND_DATA

**Hash:** `0x723538F61C647C5A` | **JHash:** `0x5CB76219` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_PED_HEAD_BLEND_DATA(Ped ped, float shapeMix, float skinMix, float thirdMix)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_PED_HEAD_BLEND_DATA, ped, shapeMix, skinMix, thirdMix);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `shapeMix` | `float` | `float` | |
| `skinMix` | `float` | `float` | |
| `thirdMix` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** See SET_PED_HEAD_BLEND_DATA().

---

### SET_HEAD_BLEND_EYE_COLOR

**Hash:** `0x50B56988B170AFDF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HEAD_BLEND_EYE_COLOR(Ped ped, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HEAD_BLEND_EYE_COLOR, ped, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Used for freemode (online) characters.

For some reason, the scripts use a rounded float for the index.
Indexes:
1. black
2. very light blue/green
3. dark blue
4. brown
5. darker brown
6. light brown
7. blue
8. light blue
9. pink
10. yellow
11. purple
12. black
13. dark green
14. light brown
15. yellow/black pattern
16. light colored spiral pattern
17. shiny red
18. shiny half blue/half red
19. half black/half light blue
20. white/red perimter
21. green snake
22. red snake
23. dark blue snake
24. dark yellow
25. bright yellow
26. all black
28. red small pupil
29. devil blue/black
30. white small pupil
31. glossed over

---

### GET_HEAD_BLEND_EYE_COLOR

**Hash:** `0x76BBA2CEE66D47E9` | **Build:** `1011`

**C++ 原型:**
```cpp
int GET_HEAD_BLEND_EYE_COLOR(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_HEAD_BLEND_EYE_COLOR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** A getter for _SET_PED_EYE_COLOR. Returns -1 if fails to get.

---

### SET_PED_HEAD_OVERLAY

**Hash:** `0x48F44967FA05CC1E` | **JHash:** `0xD28DBA90` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HEAD_OVERLAY(Ped ped, int overlayID, int index, float opacity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEAD_OVERLAY, ped, overlayID, index, opacity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `overlayID` | `int` | `int` | |
| `index` | `int` | `int` | |
| `opacity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** OverlayID ranges from 0 to 12, index from 0 to _GET_NUM_OVERLAY_VALUES(overlayID)-1, and opacity from 0.0 to 1.0. 

overlayID       Part                  Index, to disable
0               Blemishes             0 - 23, 255
1               Facial Hair           0 - 28, 255
2               Eyebrows              0 - 33, 255
3               Ageing                0 - 14, 255
4               Makeup                0 - 74, 255
5               Blush                 0 - 6, 255
6               Complexion            0 - 11, 255
7               Sun Damage            0 - 10, 255
8               Lipstick              0 - 9, 255
9               Moles/Freckles        0 - 17, 255
10              Chest Hair            0 - 16, 255
11              Body Blemishes        0 - 11, 255
12              Add Body Blemishes    0 - 1, 255

---

### GET_PED_HEAD_OVERLAY

**Hash:** `0xA60EF3B6461A4D43` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HEAD_OVERLAY(Ped ped, int overlayID)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HEAD_OVERLAY, ped, overlayID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `overlayID` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Likely a char, if that overlay is not set, e.i. "None" option, returns 255;

This might be the once removed native GET_PED_HEAD_OVERLAY.

---

### GET_PED_HEAD_OVERLAY_NUM

**Hash:** `0xCF1CE768BB43480E` | **JHash:** `0xFF43C18D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HEAD_OVERLAY_NUM(int overlayID)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HEAD_OVERLAY_NUM, overlayID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `overlayID` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_HEAD_OVERLAY_TINT

**Hash:** `0x497BF74A7B9CB952` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HEAD_OVERLAY_TINT(Ped ped, int overlayID, int colorType, int colorID, int secondColorID)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEAD_OVERLAY_TINT, ped, overlayID, colorType, colorID, secondColorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `overlayID` | `int` | `int` | |
| `colorType` | `int` | `int` | |
| `colorID` | `int` | `int` | |
| `secondColorID` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 

ColorType is 1 for eyebrows, beards, and chest hair; 2 for blush and lipstick; and 0 otherwise, though not called in those cases.

Called after SET_PED_HEAD_OVERLAY().

---

### SET_PED_HAIR_TINT

**Hash:** `0x4CFFC65454C93A49` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HAIR_TINT(Ped ped, int colorID, int highlightColorID)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HAIR_TINT, ped, colorID, highlightColorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `colorID` | `int` | `int` | |
| `highlightColorID` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_NUM_PED_HAIR_TINTS

**Hash:** `0xE5C0CF872C2AD150` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_PED_HAIR_TINTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_PED_HAIR_TINTS);
```

**返回值:** `int` → `int`

---

### GET_NUM_PED_MAKEUP_TINTS

**Hash:** `0xD1F7CA1535D22818` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_PED_MAKEUP_TINTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_PED_MAKEUP_TINTS);
```

**返回值:** `int` → `int`

---

### GET_PED_HAIR_TINT_COLOR

**Hash:** `0x4852FC386E2E1BB5` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PED_HAIR_TINT_COLOR(int hairColorIndex, int* outR, int* outG, int* outB)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PED_HAIR_TINT_COLOR, hairColorIndex, outR, outG, outB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hairColorIndex` | `int` | `int` | |
| `outR` | `int*` | `int*` | |
| `outG` | `int*` | `int*` | |
| `outB` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Input: Haircolor index, value between 0 and 63 (inclusive).
Output: RGB values for the haircolor specified in the input.

This is used with the hair color swatches scaleform.
Use `GET_PED_MAKEUP_TINT_COLOR` to get the makeup colors.

---

### GET_PED_MAKEUP_TINT_COLOR

**Hash:** `0x013E5CFC38CD5387` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PED_MAKEUP_TINT_COLOR(int makeupColorIndex, int* outR, int* outG, int* outB)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PED_MAKEUP_TINT_COLOR, makeupColorIndex, outR, outG, outB);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `makeupColorIndex` | `int` | `int` | |
| `outR` | `int*` | `int*` | |
| `outG` | `int*` | `int*` | |
| `outB` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Input: Makeup color index, value between 0 and 63 (inclusive).
Output: RGB values for the makeup color specified in the input.

This is used with the makeup color swatches scaleform.
Use `GET_PED_HAIR_TINT_COLOR` to get the hair colors.

---

### IS_PED_HAIR_TINT_FOR_CREATOR

**Hash:** `0xED6D8E27A43B8CDE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HAIR_TINT_FOR_CREATOR(int colorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HAIR_TINT_FOR_CREATOR, colorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_DEFAULT_SECONDARY_TINT_FOR_CREATOR

**Hash:** `0xEA9960D07DADCF10` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_DEFAULT_SECONDARY_TINT_FOR_CREATOR(int colorId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DEFAULT_SECONDARY_TINT_FOR_CREATOR, colorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### IS_PED_LIPSTICK_TINT_FOR_CREATOR

**Hash:** `0x3E802F11FBE27674` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_LIPSTICK_TINT_FOR_CREATOR(int colorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_LIPSTICK_TINT_FOR_CREATOR, colorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BLUSH_TINT_FOR_CREATOR

**Hash:** `0xF41B5D290C99A3D6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BLUSH_TINT_FOR_CREATOR(int colorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BLUSH_TINT_FOR_CREATOR, colorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_HAIR_TINT_FOR_BARBER

**Hash:** `0xE0D36E5D9E99CC21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HAIR_TINT_FOR_BARBER(int colorID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HAIR_TINT_FOR_BARBER, colorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_DEFAULT_SECONDARY_TINT_FOR_BARBER

**Hash:** `0xAAA6A3698A69E048` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_DEFAULT_SECONDARY_TINT_FOR_BARBER(int colorID)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_DEFAULT_SECONDARY_TINT_FOR_BARBER, colorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorID` | `int` | `int` | |

**返回值:** `int` → `int`

---

### IS_PED_LIPSTICK_TINT_FOR_BARBER

**Hash:** `0x0525A2C2562F3CD4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_LIPSTICK_TINT_FOR_BARBER(int colorID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_LIPSTICK_TINT_FOR_BARBER, colorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BLUSH_TINT_FOR_BARBER

**Hash:** `0x604E810189EE3A59` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BLUSH_TINT_FOR_BARBER(int colorID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BLUSH_TINT_FOR_BARBER, colorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BLUSH_FACEPAINT_TINT_FOR_BARBER

**Hash:** `0x09E7ECA981D9B210` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL IS_PED_BLUSH_FACEPAINT_TINT_FOR_BARBER(int colorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BLUSH_FACEPAINT_TINT_FOR_BARBER, colorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `colorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_TINT_INDEX_FOR_LAST_GEN_HAIR_TEXTURE

**Hash:** `0xC56FBF2F228E1DAC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TINT_INDEX_FOR_LAST_GEN_HAIR_TEXTURE(Hash modelHash, int drawableId, int textureId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TINT_INDEX_FOR_LAST_GEN_HAIR_TEXTURE, modelHash, drawableId, textureId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_MICRO_MORPH

**Hash:** `0x71A5C1DBA060049E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MICRO_MORPH(Ped ped, int index, float scale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MICRO_MORPH, ped, index, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `index` | `int` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the various freemode face features, e.g. nose length, chin shape. Scale ranges from -1.0 to 1.0.



0 - Nose Width (Thin/Wide)

1 - Nose Peak (Up/Down)

2 - Nose Length (Long/Short)

3 - Nose Bone Curveness (Crooked/Curved)

4 - Nose Tip (Up/Down)

5 - Nose Bone Twist (Left/Right)

6 - Eyebrow (Up/Down)

7 - Eyebrow (In/Out)

8 - Cheek Bones (Up/Down)

9 - Cheek Sideways Bone Size (In/Out)

10 - Cheek Bones Width (Puffed/Gaunt)

11 - Eye Opening (Both) (Wide/Squinted)

12 - Lip Thickness (Both) (Fat/Thin)

13 - Jaw Bone Width (Narrow/Wide)

14 - Jaw Bone Shape (Round/Square)

15 - Chin Bone (Up/Down)

16 - Chin Bone Length (In/Out or Backward/Forward)

17 - Chin Bone Shape (Pointed/Square)

18 - Chin Hole (Chin Bum)

19 - Neck Thickness (Thin/Thick)

---

### HAS_PED_HEAD_BLEND_FINISHED

**Hash:** `0x654CD0A825161131` | **JHash:** `0x2B1BD9C5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_HEAD_BLEND_FINISHED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_HEAD_BLEND_FINISHED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FINALIZE_HEAD_BLEND

**Hash:** `0x4668D80430D6C299` | **JHash:** `0x894314A4` | **Build:** `323`

**C++ 原型:**
```cpp
void FINALIZE_HEAD_BLEND(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FINALIZE_HEAD_BLEND, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_HEAD_BLEND_PALETTE_COLOR

**Hash:** `0xCC9682B8951C5229` | **JHash:** `0x57E5B3F9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HEAD_BLEND_PALETTE_COLOR(Ped ped, int r, int g, int b, int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HEAD_BLEND_PALETTE_COLOR, ped, r, g, b, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p4 seems to vary from 0 to 3.
Preview: https://gfycat.com/MaleRareAmazonparrot

---

### DISABLE_HEAD_BLEND_PALETTE_COLOR

**Hash:** `0xA21C118553BBDF02` | **JHash:** `0xC6F36292` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_HEAD_BLEND_PALETTE_COLOR(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_HEAD_BLEND_PALETTE_COLOR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_HEAD_BLEND_FIRST_INDEX

**Hash:** `0x68D353AB88B97E0C` | **JHash:** `0x211DEFEC` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HEAD_BLEND_FIRST_INDEX(int type)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HEAD_BLEND_FIRST_INDEX, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Type equals 0 for male non-dlc, 1 for female non-dlc, 2 for male dlc, and 3 for female dlc.

Used when calling SET_PED_HEAD_BLEND_DATA.

---

### GET_PED_HEAD_BLEND_NUM_HEADS

**Hash:** `0x5EF37013A6539C9D` | **JHash:** `0x095D3BD8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HEAD_BLEND_NUM_HEADS(int type)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HEAD_BLEND_NUM_HEADS, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `type` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Type equals 0 for male non-dlc, 1 for female non-dlc, 2 for male dlc, and 3 for female dlc.

---

### SET_PED_PRELOAD_VARIATION_DATA

**Hash:** `0x39D55A620FCB6A3A` | **JHash:** `0x45F3BDFB` | **Build:** `323`

**C++ 原型:**
```cpp
int SET_PED_PRELOAD_VARIATION_DATA(Ped ped, int slot, int drawableId, int textureId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_PED_PRELOAD_VARIATION_DATA, ped, slot, drawableId, textureId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `slot` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `textureId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** from extreme3.c4
PED::SET_PED_PRELOAD_VARIATION_DATA(PLAYER::PLAYER_PED_ID(), 8, PED::GET_PED_DRAWABLE_VARIATION(PLAYER::PLAYER_PED_ID(), 8), PED::GET_PED_TEXTURE_VARIATION(PLAYER::PLAYER_PED_ID(), 8));

p1 is probably componentId

---

### HAS_PED_PRELOAD_VARIATION_DATA_FINISHED

**Hash:** `0x66680A92700F43DF` | **JHash:** `0xC6517D52` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_PRELOAD_VARIATION_DATA_FINISHED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_PRELOAD_VARIATION_DATA_FINISHED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_PED_PRELOAD_VARIATION_DATA

**Hash:** `0x5AAB586FFEC0FD96` | **JHash:** `0x6435F67F` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_PED_PRELOAD_VARIATION_DATA(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_PED_PRELOAD_VARIATION_DATA, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_PRELOAD_PROP_DATA

**Hash:** `0x2B16A3BFF1FBCE49` | **JHash:** `0xC0E23671` | **Build:** `323`

**C++ 原型:**
```cpp
int SET_PED_PRELOAD_PROP_DATA(Ped ped, int componentId, int drawableId, int TextureId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_PED_PRELOAD_PROP_DATA, ped, componentId, drawableId, TextureId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `TextureId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### HAS_PED_PRELOAD_PROP_DATA_FINISHED

**Hash:** `0x784002A632822099` | **JHash:** `0x3B0CA391` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_PRELOAD_PROP_DATA_FINISHED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_PRELOAD_PROP_DATA_FINISHED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_PED_PRELOAD_PROP_DATA

**Hash:** `0xF79F9DEF0AADE61A` | **JHash:** `0xFD103BA7` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_PED_PRELOAD_PROP_DATA(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_PED_PRELOAD_PROP_DATA, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_PROP_INDEX

**Hash:** `0x898CC20EA75BACD8` | **JHash:** `0x746DDAC0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_PROP_INDEX(Ped ped, int componentId, Any p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_PROP_INDEX, ped, componentId, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `p2` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### SET_PED_PROP_INDEX

**Hash:** `0x93376B65A266EB5F` | **JHash:** `0x0829F2E2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PROP_INDEX(Ped ped, int componentId, int drawableId, int TextureId, BOOL attach, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PROP_INDEX, ped, componentId, drawableId, TextureId, attach, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |
| `drawableId` | `int` | `int` | |
| `TextureId` | `int` | `int` | |
| `attach` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** ComponentId can be set to various things based on what category you're wanting to set
enum PedPropsData
{
    PED_PROP_HATS = 0,
    PED_PROP_GLASSES = 1,
 PED_PROP_EARS = 2,
    PED_PROP_WATCHES = 3,
};
Usage: SET_PED_PROP_INDEX(playerPed, PED_PROP_HATS, GET_NUMBER_OF_PED_PROP_DRAWABLE_VARIATIONS(playerPed, PED_PROP_HATS), GET_NUMBER_OF_PED_PROP_TEXTURE_VARIATIONS(playerPed, PED_PROP_HATS, 0), TRUE);

List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### KNOCK_OFF_PED_PROP

**Hash:** `0x6FD7816A36615F48` | **JHash:** `0x08D8B180` | **Build:** `323`

**C++ 原型:**
```cpp
void KNOCK_OFF_PED_PROP(Ped ped, BOOL p1, BOOL p2, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.KNOCK_OFF_PED_PROP, ped, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### CLEAR_PED_PROP

**Hash:** `0x0943E5B8E078E76E` | **JHash:** `0x2D23D743` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_PROP(Ped ped, int propId, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_PROP, ped, propId, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `propId` | `int` | `int` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### CLEAR_ALL_PED_PROPS

**Hash:** `0xCD8A7537A9B52F06` | **JHash:** `0x81DF8B43` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ALL_PED_PROPS(Ped ped, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ALL_PED_PROPS, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### DROP_AMBIENT_PROP

**Hash:** `0xAFF4710E2A0A6C12` | **JHash:** `0x728D3D53` | **Build:** `323`

**C++ 原型:**
```cpp
void DROP_AMBIENT_PROP(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DROP_AMBIENT_PROP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_PROP_TEXTURE_INDEX

**Hash:** `0xE131A28626F81AB2` | **JHash:** `0x922A6653` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_PROP_TEXTURE_INDEX(Ped ped, int componentId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_PROP_TEXTURE_INDEX, ped, componentId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `componentId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### CLEAR_PED_PARACHUTE_PACK_VARIATION

**Hash:** `0x1280804F7CFD2D6C` | **JHash:** `0x7BCD8991` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_PARACHUTE_PACK_VARIATION(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_PARACHUTE_PACK_VARIATION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_SCUBA_GEAR_VARIATION

**Hash:** `0x36C6984C3ED0C911` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SCUBA_GEAR_VARIATION(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SCUBA_GEAR_VARIATION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** This native sets a scuba mask for freemode models and an oxygen bottle for player_* models. It works on freemode and player_* models.

---

### CLEAR_PED_SCUBA_GEAR_VARIATION

**Hash:** `0xB50EB4CCB29704AC` | **JHash:** `0x080275EE` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_SCUBA_GEAR_VARIATION(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_SCUBA_GEAR_VARIATION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Removes the scubagear (for mp male: component id: 8, drawableId: 123, textureId: any) from peds. Does not play the 'remove scuba gear' animation, but instantly removes it.

---

### IS_USING_PED_SCUBA_GEAR_VARIATION

**Hash:** `0xFEC9A3B1820F3331` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_USING_PED_SCUBA_GEAR_VARIATION(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_USING_PED_SCUBA_GEAR_VARIATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_BLOCKING_OF_NON_TEMPORARY_EVENTS

**Hash:** `0x9F8AA94D6D97DBF4` | **JHash:** `0xDFE34E4A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BLOCKING_OF_NON_TEMPORARY_EVENTS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BLOCKING_OF_NON_TEMPORARY_EVENTS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** works with TASK::TASK_SET_BLOCKING_OF_NON_TEMPORARY_EVENTS to make a ped completely oblivious to all events going on around him

---

### SET_PED_BOUNDS_ORIENTATION

**Hash:** `0x4F5F651ACCC9C4CF` | **JHash:** `0xCFA20D68` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_BOUNDS_ORIENTATION(Ped ped, float p1, float p2, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_BOUNDS_ORIENTATION, ped, p1, p2, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### REGISTER_TARGET

**Hash:** `0x2F25D9AEFA34FBA2` | **JHash:** `0x50A95442` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_TARGET(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_TARGET, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** PED::REGISTER_TARGET(l_216, PLAYER::PLAYER_PED_ID()); from re_prisonbreak.txt.

l_216 = RECSBRobber1

---

### REGISTER_HATED_TARGETS_AROUND_PED

**Hash:** `0x9222F300BF8354FE` | **JHash:** `0x7F87559E` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_HATED_TARGETS_AROUND_PED(Ped ped, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_HATED_TARGETS_AROUND_PED, ped, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Based on TASK_COMBAT_HATED_TARGETS_AROUND_PED, the parameters are likely similar (PedHandle, and area to attack in).

---

### GET_RANDOM_PED_AT_COORD

**Hash:** `0x876046A8E3A4B71C` | **JHash:** `0xDC8239EB` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_RANDOM_PED_AT_COORD(float x, float y, float z, float xRadius, float yRadius, float zRadius, int pedType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_PED_AT_COORD, x, y, z, xRadius, yRadius, zRadius, pedType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `xRadius` | `float` | `float` | |
| `yRadius` | `float` | `float` | |
| `zRadius` | `float` | `float` | |
| `pedType` | `int` | `int` | |

**返回值:** `Ped` → `int`

**说明:** Gets a random ped in the x/y/zRadius near the x/y/z coordinates passed. 

Ped Types:
Any = -1
Player = 1
Male = 4 
Female = 5 
Cop = 6
Human = 26
SWAT = 27 
Animal = 28
Army = 29

---

### GET_CLOSEST_PED

**Hash:** `0xC33AB876A77F8164` | **JHash:** `0x8F6C1F55` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CLOSEST_PED(float x, float y, float z, float radius, BOOL p4, BOOL p5, Ped* outPed, BOOL p7, BOOL p8, int pedType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CLOSEST_PED, x, y, z, radius, p4, p5, outPed, p7, p8, pedType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `outPed` | `Ped*` | `int*` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `pedType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets the closest ped in a radius.

Ped Types:
Any ped = -1
Player = 1
Male = 4 
Female = 5 
Cop = 6
Human = 26
SWAT = 27 
Animal = 28
Army = 29

------------------
P4 P5 P7 P8
1  0  x  x  = return nearest walking Ped
1  x  0  x  = return nearest walking Ped
x  1  1  x  = return Ped you are using
0  0  x  x  = no effect
0  x  0  x  = no effect

x = can be 1 or 0. Does not have any obvious changes.

This function does not return ped who is:
1. Standing still
2. Driving
3. Fleeing
4. Attacking

This function only work if the ped is:
1. walking normally.
2. waiting to cross a road.

Note: PED::GET_PED_NEARBY_PEDS works for more peds.

---

### SET_SCENARIO_PEDS_TO_BE_RETURNED_BY_NEXT_COMMAND

**Hash:** `0x14F19A8782C8071E` | **JHash:** `0x85615FD0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENARIO_PEDS_TO_BE_RETURNED_BY_NEXT_COMMAND(BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENARIO_PEDS_TO_BE_RETURNED_BY_NEXT_COMMAND, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a value indicating whether scenario peds should be returned by the next call to a command that returns peds. Eg. GET_CLOSEST_PED.

---

### GET_CAN_PED_BE_GRABBED_BY_SCRIPT

**Hash:** `0x03EA03AF85A85CB7` | **JHash:** `0x18DD76A1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CAN_PED_BE_GRABBED_BY_SCRIPT(Ped ped, BOOL p1, BOOL p2, BOOL p3, BOOL p4, BOOL p5, BOOL p6, BOOL p7, Any p8)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CAN_PED_BE_GRABBED_BY_SCRIPT, ped, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_DRIVER_RACING_MODIFIER

**Hash:** `0xDED5AF5A0EA4B297` | **JHash:** `0x6D55B3B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVER_RACING_MODIFIER(Ped driver, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVER_RACING_MODIFIER, driver, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Scripts use 0.2, 0.5 and 1.0. Value must be >= 0.0 && <= 1.0

---

### SET_DRIVER_ABILITY

**Hash:** `0xB195FFA8042FC5C3` | **JHash:** `0xAAD4012C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVER_ABILITY(Ped driver, float ability)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVER_ABILITY, driver, ability);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `ability` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The function specifically verifies the value is equal to, or less than 1.0f. If it is greater than 1.0f, the function does nothing at all.

---

### SET_DRIVER_AGGRESSIVENESS

**Hash:** `0xA731F608CA104E3C` | **JHash:** `0x8B02A8FB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVER_AGGRESSIVENESS(Ped driver, float aggressiveness)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVER_AGGRESSIVENESS, driver, aggressiveness);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `aggressiveness` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** range 0.0f - 1.0f

---

### CAN_PED_RAGDOLL

**Hash:** `0x128F79EDCECE4FD5` | **JHash:** `0xC0EFB7A3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PED_RAGDOLL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_RAGDOLL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Prevents the ped from going limp.

[Example: Can prevent peds from falling when standing on moving vehicles.]

---

### SET_PED_TO_RAGDOLL

**Hash:** `0xAE99FB955581844A` | **JHash:** `0x83CB5052` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_TO_RAGDOLL(Ped ped, int time1, int time2, int ragdollType, BOOL p4, BOOL p5, BOOL p6)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_TO_RAGDOLL, ped, time1, time2, ragdollType, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `time1` | `int` | `int` | |
| `time2` | `int` | `int` | |
| `ragdollType` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p4/p5: Unused in TU27
Ragdoll Types:
**0**: CTaskNMRelax
**1**: CTaskNMScriptControl: Hardcoded not to work in networked environments.
**Else**: CTaskNMBalance
time1- Time(ms) Ped is in ragdoll mode; only applies to ragdoll types 0 and not 1.

time2- Unknown time, in milliseconds

ragdollType-
0 : Normal ragdoll
1 : Falls with stiff legs/body
2 : Narrow leg stumble(may not fall)
3 : Wide leg stumble(may not fall)

p4, p5, p6- No idea. In R*'s scripts they are usually either "true, true, false" or "false, false, false".




EDIT 3/11/16: unclear what 'mircoseconds' mean-- a microsecond is 1000x a ms, so time2 must be 1000x time1?  more testing needed.  -sob

Edit Mar 21, 2017: removed part about time2 being the microseconds version of time1. this just isn't correct. time2 is in milliseconds, and time1 and time2 don't seem to be connected in any way.

---

### SET_PED_TO_RAGDOLL_WITH_FALL

**Hash:** `0xD76632D99E4966C8` | **JHash:** `0xFA12E286` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_TO_RAGDOLL_WITH_FALL(Ped ped, int time, int p2, int ragdollType, float x, float y, float z, float velocity, float p8, float p9, float p10, float p11, float p12, float p13)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_TO_RAGDOLL_WITH_FALL, ped, time, p2, ragdollType, x, y, z, velocity, p8, p9, p10, p11, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `time` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `ragdollType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `velocity` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `float` | `float` | |
| `p11` | `float` | `float` | |
| `p12` | `float` | `float` | |
| `p13` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Return variable is never used in R*'s scripts.

Not sure what p2 does. It seems like it would be a time judging by it's usage in R*'s scripts, but didn't seem to affect anything in my testings.

enum eRagdollType
{
 RD_MALE=0,
 RD_FEMALE = 1,
 RD_MALE_LARGE = 2,
 RD_CUSTOM = 3,
}

x, y, and z are coordinates, most likely to where the ped will fall.

p8 to p13 are always 0f in R*'s scripts.

(Simplified) Example of the usage of the function from R*'s scripts:
ped::set_ped_to_ragdoll_with_fall(ped, 1500, 2000, 1, -entity::get_entity_forward_vector(ped), 1f, 0f, 0f, 0f, 0f, 0f, 0f);


---

### SET_PED_RAGDOLL_ON_COLLISION

**Hash:** `0xF0A4F1BBF4FA7497` | **JHash:** `0x2654A0F4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RAGDOLL_ON_COLLISION(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RAGDOLL_ON_COLLISION, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Causes Ped to ragdoll on collision with any object (e.g Running into trashcan). If applied to player you will sometimes trip on the sidewalk.

---

### IS_PED_RAGDOLL

**Hash:** `0x47E4E977581C5B55` | **JHash:** `0xC833BBE1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RAGDOLL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RAGDOLL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** If the ped handle passed through the parenthesis is in a ragdoll state this will return true.

---

### IS_PED_RUNNING_RAGDOLL_TASK

**Hash:** `0xE3B6097CC25AA69E` | **JHash:** `0x44A153F2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RUNNING_RAGDOLL_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RUNNING_RAGDOLL_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_RAGDOLL_FORCE_FALL

**Hash:** `0x01F6594B923B9251` | **JHash:** `0x20A5BDE0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RAGDOLL_FORCE_FALL(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RAGDOLL_FORCE_FALL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### RESET_PED_RAGDOLL_TIMER

**Hash:** `0x9FA4664CF62E47E8` | **JHash:** `0xF2865370` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_RAGDOLL_TIMER(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_RAGDOLL_TIMER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_RAGDOLL

**Hash:** `0xB128377056A54E2A` | **JHash:** `0xCF1384C4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_RAGDOLL(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_RAGDOLL, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_RUNNING_MELEE_TASK

**Hash:** `0xD1871251F3B5ACD7` | **JHash:** `0xBD4236F6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RUNNING_MELEE_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RUNNING_MELEE_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_RUNNING_MOBILE_PHONE_TASK

**Hash:** `0x2AFE52F782F25775` | **JHash:** `0xFB2AFED1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RUNNING_MOBILE_PHONE_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RUNNING_MOBILE_PHONE_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_MOBILE_PHONE_TO_PED_EAR

**Hash:** `0xA3F3564A5B3646C0` | **JHash:** `0x97353375` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOBILE_PHONE_TO_PED_EAR(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOBILE_PHONE_TO_PED_EAR, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_RAGDOLL_BLOCKING_FLAGS

**Hash:** `0x26695EC767728D84` | **JHash:** `0x9C8F830D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RAGDOLL_BLOCKING_FLAGS(Ped ped, int blockingFlag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RAGDOLL_BLOCKING_FLAGS, ped, blockingFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `blockingFlag` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Works for both player and peds,

enum eRagdollBlockingFlags
{
 RBF_BULLET_IMPACT = 0,
 RBF_VEHICLE_IMPACT = 1,
 RBF_FIRE = 2,
 RBF_ELECTROCUTION = 3,
 RBF_PLAYER_IMPACT = 4,
 RBF_EXPLOSION = 5,0
 RBF_IMPACT_OBJECT = 6,
 RBF_MELEE = 7,
 RBF_RUBBER_BULLET = 8,
 RBF_FALLING = 9,
 RBF_WATER_JET = 10,
 RBF_DROWNING = 11,
 _0x9F52E2C4 = 12,
 RBF_PLAYER_BUMP = 13,
 RBF_PLAYER_RAGDOLL_BUMP = 14,
 RBF_PED_RAGDOLL_BUMP = 15,
 RBF_VEHICLE_GRAB = 16,
 RBF_SMOKE_GRENADE = 17,
};



---

### CLEAR_RAGDOLL_BLOCKING_FLAGS

**Hash:** `0xD86D101FCFD00A4B` | **JHash:** `0x77CBA290` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_RAGDOLL_BLOCKING_FLAGS(Ped ped, int blockingFlag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_RAGDOLL_BLOCKING_FLAGS, ped, blockingFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `blockingFlag` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** See SET_RAGDOLL_BLOCKING_FLAGS for flags

---

### SET_PED_ANGLED_DEFENSIVE_AREA

**Hash:** `0xC7F76DF27A5045A1` | **JHash:** `0x3EFBDD9B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ANGLED_DEFENSIVE_AREA(Ped ped, float p1, float p2, float p3, float p4, float p5, float p6, float p7, BOOL p8, BOOL p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ANGLED_DEFENSIVE_AREA, ped, p1, p2, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_SPHERE_DEFENSIVE_AREA

**Hash:** `0x9D3151A373974804` | **JHash:** `0xBD96D8E8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SPHERE_DEFENSIVE_AREA(Ped ped, float x, float y, float z, float radius, BOOL p5, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SPHERE_DEFENSIVE_AREA, ped, x, y, z, radius, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_PED

**Hash:** `0xF9B8F91AAD3B953E` | **JHash:** `0x40638BDC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_PED(Ped ped, Ped target, float xOffset, float yOffset, float zOffset, float radius, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_PED, ped, target, xOffset, yOffset, zOffset, radius, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_VEHICLE

**Hash:** `0xE4723DB6E736CCFF` | **JHash:** `0x4763B2C6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_VEHICLE(Ped ped, Vehicle target, float xOffset, float yOffset, float zOffset, float radius, BOOL p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DEFENSIVE_SPHERE_ATTACHED_TO_VEHICLE, ped, target, xOffset, yOffset, zOffset, radius, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Vehicle` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_DEFENSIVE_AREA_ATTACHED_TO_PED

**Hash:** `0x4EF47FE21698A8B6` | **JHash:** `0x74BDA7CE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DEFENSIVE_AREA_ATTACHED_TO_PED(Ped ped, Ped attachPed, float p2, float p3, float p4, float p5, float p6, float p7, float p8, BOOL p9, BOOL p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DEFENSIVE_AREA_ATTACHED_TO_PED, ped, attachPed, p2, p3, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `attachPed` | `Ped` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_DEFENSIVE_AREA_DIRECTION

**Hash:** `0x413C6C763A4AFFAD` | **JHash:** `0xB66B0C9A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DEFENSIVE_AREA_DIRECTION(Ped ped, float p1, float p2, float p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DEFENSIVE_AREA_DIRECTION, ped, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_PED_DEFENSIVE_AREA

**Hash:** `0x74D4E028107450A9` | **JHash:** `0x34AAAFA5` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PED_DEFENSIVE_AREA(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PED_DEFENSIVE_AREA, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Ped will no longer get angry when you stay near him.

---

### GET_PED_DEFENSIVE_AREA_POSITION

**Hash:** `0x3C06B8786DD94CD1` | **JHash:** `0xCB65198D` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PED_DEFENSIVE_AREA_POSITION(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PED_DEFENSIVE_AREA_POSITION, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Vector3` → `Vector3`

---

### IS_PED_DEFENSIVE_AREA_ACTIVE

**Hash:** `0xBA63D9FE45412247` | **JHash:** `0x6D88BCD3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_DEFENSIVE_AREA_ACTIVE(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_DEFENSIVE_AREA_ACTIVE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_PREFERRED_COVER_SET

**Hash:** `0x8421EB4DA7E391B9` | **JHash:** `0xF3B7EFBF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PREFERRED_COVER_SET(Ped ped, Any itemSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PREFERRED_COVER_SET, ped, itemSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `itemSet` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REMOVE_PED_PREFERRED_COVER_SET

**Hash:** `0xFDDB234CF74073D9` | **JHash:** `0xA0134498` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PED_PREFERRED_COVER_SET(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PED_PREFERRED_COVER_SET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### REVIVE_INJURED_PED

**Hash:** `0x8D8ACD8388CD99CE` | **JHash:** `0x14D3E6E3` | **Build:** `323`

**C++ 原型:**
```cpp
void REVIVE_INJURED_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REVIVE_INJURED_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** It will revive/cure the injured ped. The condition is ped must not be dead.

Upon setting and converting the health int, found, if health falls below 5, the ped will lay on the ground in pain(Maximum default health is 100).

This function is well suited there.

---

### RESURRECT_PED

**Hash:** `0x71BC8E838B9C6035` | **JHash:** `0xA4B82097` | **Build:** `323`

**C++ 原型:**
```cpp
void RESURRECT_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESURRECT_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** This function will simply bring the dead person back to life.

Try not to use it alone, since using this function alone, will make peds fall through ground in hell(well for the most of the times).

Instead, before calling this function, you may want to declare the position, where your Resurrected ped to be spawn at.(For instance, Around 2 floats of Player's current position.) 

Also, disabling any assigned task immediately helped in the number of scenarios, where If you want peds to perform certain decided tasks.

---

### SET_PED_NAME_DEBUG

**Hash:** `0x98EFA132A4117BE1` | **JHash:** `0x20D6273E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_NAME_DEBUG(Ped ped, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_NAME_DEBUG, ped, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

*untested but char *name could also be a hash for a localized string

---

### GET_PED_EXTRACTED_DISPLACEMENT

**Hash:** `0xE0AF41401ADF87E3` | **JHash:** `0x5231F901` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PED_EXTRACTED_DISPLACEMENT(Ped ped, BOOL worldSpace)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PED_EXTRACTED_DISPLACEMENT, ped, worldSpace);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `worldSpace` | `BOOL` | `bool` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the offset the specified ped has moved since the previous tick.

If worldSpace is false, the returned offset is relative to the ped. That is, if the ped has moved 1 meter right and 5 meters forward, it'll return 1,5,0.

If worldSpace is true, the returned offset is relative to the world. That is, if the ped has moved 1 meter on the X axis and 5 meters on the Y axis, it'll return 1,5,0.

---

### SET_PED_DIES_WHEN_INJURED

**Hash:** `0x5BA7919BED300023` | **JHash:** `0xE94E24D4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DIES_WHEN_INJURED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DIES_WHEN_INJURED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_ENABLE_WEAPON_BLOCKING

**Hash:** `0x97A790315D3831FD` | **JHash:** `0x4CAD1A4A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ENABLE_WEAPON_BLOCKING(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ENABLE_WEAPON_BLOCKING, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SPECIAL_FUNCTION_DO_NOT_USE

**Hash:** `0xF9ACF4A08098EA25` | **JHash:** `0x141CC936` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_FUNCTION_DO_NOT_USE(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_FUNCTION_DO_NOT_USE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 was always 1 (true).

Kicks the ped from the current vehicle and keeps the rendering-focus on this ped (also disables its collision). If doing this for your player ped, you'll still be able to drive the vehicle.

---

### RESET_PED_VISIBLE_DAMAGE

**Hash:** `0x3AC1F7B898F30C05` | **JHash:** `0xC4BC4841` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PED_VISIBLE_DAMAGE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PED_VISIBLE_DAMAGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### APPLY_PED_BLOOD_DAMAGE_BY_ZONE

**Hash:** `0x816F6981C60BF53B` | **JHash:** `0x1E54DB12` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_BLOOD_DAMAGE_BY_ZONE(Ped ped, Any p1, float p2, float p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_BLOOD_DAMAGE_BY_ZONE, ped, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### APPLY_PED_BLOOD

**Hash:** `0x83F7E01C7B769A26` | **JHash:** `0x376CE3C0` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_BLOOD(Ped ped, int boneIndex, float xRot, float yRot, float zRot, const char* woundType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_BLOOD, ped, boneIndex, xRot, yRot, zRot, woundType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `boneIndex` | `int` | `int` | |
| `xRot` | `float` | `float` | |
| `yRot` | `float` | `float` | |
| `zRot` | `float` | `float` | |
| `woundType` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** woundTypes:
- soak_splat
- wound_sheet
- BulletSmall
- BulletLarge
- ShotgunSmall
- ShotgunSmallMonolithic
- ShotgunLarge
- ShotgunLargeMonolithic
- NonFatalHeadshot
- stab
- BasicSlash
- Scripted_Ped_Splash_Back
- BackSplash

---

### APPLY_PED_BLOOD_BY_ZONE

**Hash:** `0x3311E47B91EDCBBC` | **JHash:** `0x8F3F3A9C` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_BLOOD_BY_ZONE(Ped ped, int p1, float p2, float p3, const char* p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_BLOOD_BY_ZONE, ped, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### APPLY_PED_BLOOD_SPECIFIC

**Hash:** `0xEF0D582CBF2D9B0F` | **JHash:** `0xFC13CE80` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_BLOOD_SPECIFIC(Ped ped, int p1, float p2, float p3, float p4, float p5, int p6, float p7, const char* p8)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_BLOOD_SPECIFIC, ped, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `int` | `int` | |
| `p7` | `float` | `float` | |
| `p8` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### APPLY_PED_DAMAGE_DECAL

**Hash:** `0x397C38AA7B4A5F83` | **JHash:** `0x8A13A41F` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_DAMAGE_DECAL(Ped ped, int damageZone, float xOffset, float yOffset, float heading, float scale, float alpha, int variation, BOOL fadeIn, const char* decalName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_DAMAGE_DECAL, ped, damageZone, xOffset, yOffset, heading, scale, alpha, variation, fadeIn, decalName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `damageZone` | `int` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `scale` | `float` | `float` | |
| `alpha` | `float` | `float` | |
| `variation` | `int` | `int` | |
| `fadeIn` | `BOOL` | `bool` | |
| `decalName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** enum eDamageZone
{
	DZ_Torso = 0,
	DZ_Head,
	DZ_LeftArm,
	DZ_RightArm,
	DZ_LeftLeg,
	DZ_RightLeg,
};

Decal Names:
scar
blushing
cs_flush_anger
cs_flush_anger_face
bruise
bruise_large
herpes
ArmorBullet
basic_dirt_cloth
basic_dirt_skin
cs_trev1_dirt

APPLY_PED_DAMAGE_DECAL(ped, 1, 0.5f, 0.513f, 0f, 1f, unk, 0, 0, "blushing");

---

### APPLY_PED_DAMAGE_PACK

**Hash:** `0x46DF918788CB093F` | **JHash:** `0x208D0CB8` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_PED_DAMAGE_PACK(Ped ped, const char* damagePack, float damage, float mult)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_PED_DAMAGE_PACK, ped, damagePack, damage, mult);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `damagePack` | `const char*` | `string` | |
| `damage` | `float` | `float` | |
| `mult` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Damage Packs:

"SCR_TrevorTreeBang"
"HOSPITAL_0"
"HOSPITAL_1"
"HOSPITAL_2"
"HOSPITAL_3"
"HOSPITAL_4"
"HOSPITAL_5"
"HOSPITAL_6"
"HOSPITAL_7"
"HOSPITAL_8"
"HOSPITAL_9"
"SCR_Dumpster"
"BigHitByVehicle"
"SCR_Finale_Michael_Face"
"SCR_Franklin_finb"
"SCR_Finale_Michael"
"SCR_Franklin_finb2"
"Explosion_Med"
"SCR_Torture"
"SCR_TracySplash"
"Skin_Melee_0"

Additional damage packs:

gist.github.com/alexguirre/f3f47f75ddcf617f416f3c8a55ae2227
Full list of ped damage packs by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedDamagePacks.json

---

### CLEAR_PED_BLOOD_DAMAGE

**Hash:** `0x8FE22675A5A45817` | **JHash:** `0xF7ADC960` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_BLOOD_DAMAGE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_BLOOD_DAMAGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_BLOOD_DAMAGE_BY_ZONE

**Hash:** `0x56E3B78C5408D9F4` | **JHash:** `0xF210BE69` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_BLOOD_DAMAGE_BY_ZONE(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_BLOOD_DAMAGE_BY_ZONE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Somehow related to changing ped's clothes.

---

### HIDE_PED_BLOOD_DAMAGE_BY_ZONE

**Hash:** `0x62AB793144DE75DC` | **JHash:** `0x0CB6C4ED` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_PED_BLOOD_DAMAGE_BY_ZONE(Ped ped, Any p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_PED_BLOOD_DAMAGE_BY_ZONE, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_DAMAGE_DECAL_BY_ZONE

**Hash:** `0x523C79AEEFCC4A2A` | **JHash:** `0x70AA5B7D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_DAMAGE_DECAL_BY_ZONE(Ped ped, int p1, const char* p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_DAMAGE_DECAL_BY_ZONE, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** p1: from 0 to 5 in the b617d scripts.
p2: "blushing" and "ALL" found in the b617d scripts.

---

### GET_PED_DECORATIONS_STATE

**Hash:** `0x71EAB450D86954A1` | **JHash:** `0x47187F7F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_DECORATIONS_STATE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_DECORATIONS_STATE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### MARK_PED_DECORATIONS_AS_CLONED_FROM_LOCAL_PLAYER

**Hash:** `0x2B694AFCF64E6994` | **Build:** `323`

**C++ 原型:**
```cpp
void MARK_PED_DECORATIONS_AS_CLONED_FROM_LOCAL_PLAYER(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MARK_PED_DECORATIONS_AS_CLONED_FROM_LOCAL_PLAYER, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_WETNESS

**Hash:** `0x9C720776DAA43E7E` | **JHash:** `0x629F15BD` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_WETNESS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_WETNESS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** It clears the wetness of the selected Ped/Player. Clothes have to be wet to notice the difference.

---

### SET_PED_WETNESS_HEIGHT

**Hash:** `0x44CB6447D2571AA0` | **JHash:** `0x7B33289A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_WETNESS_HEIGHT(Ped ped, float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WETNESS_HEIGHT, ped, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** It adds the wetness level to the player clothing/outfit. As if player just got out from water surface.



---

### SET_PED_WETNESS_ENABLED_THIS_FRAME

**Hash:** `0xB5485E4907B53019` | **JHash:** `0xBDE749F7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_WETNESS_ENABLED_THIS_FRAME(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WETNESS_ENABLED_THIS_FRAME, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** combined with PED::SET_PED_WETNESS_HEIGHT(), this native makes the ped drenched in water up to the height specified in the other function

---

### SET_PED_WETNESS

**Hash:** `0xAC0BB4D87777CAE2` | **Build:** `2802`

**C++ 原型:**
```cpp
void SET_PED_WETNESS(Ped ped, float wetLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WETNESS, ped, wetLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `wetLevel` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_ENV_DIRT

**Hash:** `0x6585D955A68452A5` | **JHash:** `0xA993915F` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_ENV_DIRT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_ENV_DIRT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_SWEAT

**Hash:** `0x27B0405F59637D1F` | **JHash:** `0x76A1DB9F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SWEAT(Ped ped, float sweat)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SWEAT, ped, sweat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `sweat` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sweat is set to 100.0 or 0.0 in the decompiled scripts.

---

### ADD_PED_DECORATION_FROM_HASHES

**Hash:** `0x5F5D1665E352A839` | **JHash:** `0x70559AC7` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PED_DECORATION_FROM_HASHES(Ped ped, Hash collection, Hash overlay)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PED_DECORATION_FROM_HASHES, ped, collection, overlay);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `collection` | `Hash` | `uint` | |
| `overlay` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Applies an Item from a PedDecorationCollection to a ped. These include tattoos and shirt decals.

collection - PedDecorationCollection filename hash
overlay - Item name hash

Example:
Entry inside "mpbeach_overlays.xml" -
<Item>
  <uvPos x="0.500000" y="0.500000" />
  <scale x="0.600000" y="0.500000" />
  <rotation value="0.000000" />
  <nameHash>FM_Hair_Fuzz</nameHash>
  <txdHash>mp_hair_fuzz</txdHash>
  <txtHash>mp_hair_fuzz</txtHash>
  <zone>ZONE_HEAD</zone>
  <type>TYPE_TATTOO</type>
  <faction>FM</faction>
  <garment>All</garment>
  <gender>GENDER_DONTCARE</gender>
  <award />
  <awardLevel />
</Item>

Code:
PED::ADD_PED_DECORATION_FROM_HASHES(PLAYER::PLAYER_PED_ID(), MISC::GET_HASH_KEY("mpbeach_overlays"), MISC::GET_HASH_KEY("fm_hair_fuzz"))

Full list of ped overlays / decorations by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedOverlayCollections.json

---

### ADD_PED_DECORATION_FROM_HASHES_IN_CORONA

**Hash:** `0x5619BFA07CFD7833` | **JHash:** `0x8CD3E487` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PED_DECORATION_FROM_HASHES_IN_CORONA(Ped ped, Hash collection, Hash overlay)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PED_DECORATION_FROM_HASHES_IN_CORONA, ped, collection, overlay);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `collection` | `Hash` | `uint` | |
| `overlay` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of ped overlays / decorations by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedOverlayCollections.json

---

### GET_PED_DECORATION_ZONE_FROM_HASHES

**Hash:** `0x9FD452BFBE7A7A8B` | **JHash:** `0x3543019E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_DECORATION_ZONE_FROM_HASHES(Hash collection, Hash overlay)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_DECORATION_ZONE_FROM_HASHES, collection, overlay);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `collection` | `Hash` | `uint` | |
| `overlay` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns the zoneID for the overlay if it is a member of collection.
enum ePedDecorationZone
{
	ZONE_TORSO = 0,
	ZONE_HEAD = 1,
	ZONE_LEFT_ARM = 2,
	ZONE_RIGHT_ARM = 3,
	ZONE_LEFT_LEG = 4,
	ZONE_RIGHT_LEG = 5,
	ZONE_MEDALS = 6,
	ZONE_INVALID = 7
};

Full list of ped overlays / decorations by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/pedOverlayCollections.json

---

### CLEAR_PED_DECORATIONS

**Hash:** `0x0E5173C163976E38` | **JHash:** `0xD4496BF3` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_DECORATIONS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_DECORATIONS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_DECORATIONS_LEAVE_SCARS

**Hash:** `0xE3B27E70CEAB9F0C` | **JHash:** `0xEFD58EB9` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_DECORATIONS_LEAVE_SCARS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_DECORATIONS_LEAVE_SCARS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### WAS_PED_SKELETON_UPDATED

**Hash:** `0x11B499C1E0FF8559` | **JHash:** `0xF7E2FBAD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_PED_SKELETON_UPDATED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_PED_SKELETON_UPDATED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Despite this function's name, it simply returns whether the specified handle is a Ped.

---

### GET_PED_BONE_COORDS

**Hash:** `0x17C07FC640E86B4E` | **JHash:** `0x4579CAB1` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PED_BONE_COORDS(Ped ped, int boneId, float offsetX, float offsetY, float offsetZ)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PED_BONE_COORDS, ped, boneId, offsetX, offsetY, offsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `boneId` | `int` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the position of the specified bone of the specified ped.

ped: The ped to get the position of a bone from.
boneId: The ID of the bone to get the position from. This is NOT the index.
offsetX: The X-component of the offset to add to the position relative to the bone's rotation.
offsetY: The Y-component of the offset to add to the position relative to the bone's rotation.
offsetZ: The Z-component of the offset to add to the position relative to the bone's rotation.

---

### CREATE_NM_MESSAGE

**Hash:** `0x418EF2A1BCE56685` | **JHash:** `0x1CFBFD4B` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_NM_MESSAGE(BOOL startImmediately, int messageId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_NM_MESSAGE, startImmediately, messageId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startImmediately` | `BOOL` | `bool` | |
| `messageId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Creates a new NaturalMotion message.

startImmediately: If set to true, the character will perform the message the moment it receives it by GIVE_PED_NM_MESSAGE. If false, the Ped will get the message but won't perform it yet. While it's a boolean value, if negative, the message will not be initialized.
messageId: The ID of the NaturalMotion message.

If a message already exists, this function does nothing. A message exists until the point it has been successfully dispatched by GIVE_PED_NM_MESSAGE.

---

### GIVE_PED_NM_MESSAGE

**Hash:** `0xB158DFCCC56E5C5B` | **JHash:** `0x737C3689` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_PED_NM_MESSAGE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_PED_NM_MESSAGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Sends the message that was created by a call to CREATE_NM_MESSAGE to the specified Ped.

If a message hasn't been created already, this function does nothing.
If the Ped is not ragdolled with Euphoria enabled, this function does nothing.
The following call can be used to ragdoll the Ped with Euphoria enabled: SET_PED_TO_RAGDOLL(ped, 4000, 5000, 1, 1, 1, 0);

Call order:
SET_PED_TO_RAGDOLL
CREATE_NM_MESSAGE
GIVE_PED_NM_MESSAGE

Multiple messages can be chained. Eg. to make the ped stagger and swing his arms around, the following calls can be made:
SET_PED_TO_RAGDOLL(ped, 4000, 5000, 1, 1, 1, 0);
CREATE_NM_MESSAGE(true, 0); // stopAllBehaviours - Stop all other behaviours, in case the Ped is already doing some Euphoria stuff.
GIVE_PED_NM_MESSAGE(ped); // Dispatch message to Ped.
CREATE_NM_MESSAGE(true, 1151); // staggerFall - Attempt to walk while falling.
GIVE_PED_NM_MESSAGE(ped); // Dispatch message to Ped.
CREATE_NM_MESSAGE(true, 372); // armsWindmill - Swing arms around.
GIVE_PED_NM_MESSAGE(ped); // Dispatch message to Ped.

---

### ADD_SCENARIO_BLOCKING_AREA

**Hash:** `0x1B5C85C612E5256E` | **JHash:** `0xA38C0234` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_SCENARIO_BLOCKING_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL p6, BOOL p7, BOOL p8, BOOL p9, Any p10)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_SCENARIO_BLOCKING_AREA, x1, y1, z1, x2, y2, z2, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `BOOL` | `bool` | |
| `p10` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### REMOVE_SCENARIO_BLOCKING_AREAS

**Hash:** `0xD37401D78A929A49` | **JHash:** `0x4DDF845F` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_SCENARIO_BLOCKING_AREAS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SCENARIO_BLOCKING_AREAS);
```

**返回值:** `void` → `void`

---

### REMOVE_SCENARIO_BLOCKING_AREA

**Hash:** `0x31D16B74C6E29D66` | **JHash:** `0x4483EF06` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_SCENARIO_BLOCKING_AREA(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SCENARIO_BLOCKING_AREA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SCENARIO_PEDS_SPAWN_IN_SPHERE_AREA

**Hash:** `0x28157D43CF600981` | **JHash:** `0x80EAD297` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENARIO_PEDS_SPAWN_IN_SPHERE_AREA(float x, float y, float z, float range, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENARIO_PEDS_SPAWN_IN_SPHERE_AREA, x, y, z, range, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `range` | `float` | `float` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DOES_SCENARIO_BLOCKING_AREA_EXISTS

**Hash:** `0x8A24B067D175A7BD` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL DOES_SCENARIO_BLOCKING_AREA_EXISTS(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCENARIO_BLOCKING_AREA_EXISTS, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_USING_SCENARIO

**Hash:** `0x1BF094736DD62C2E` | **JHash:** `0x0F65B0D4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_USING_SCENARIO(Ped ped, const char* scenario)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_USING_SCENARIO, ped, scenario);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `scenario` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of ped scenarios by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenariosCompact.json

---

### IS_PED_USING_ANY_SCENARIO

**Hash:** `0x57AB4A3080F85143` | **JHash:** `0x195EF5B7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_USING_ANY_SCENARIO(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_USING_ANY_SCENARIO, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_PANIC_EXIT_SCENARIO

**Hash:** `0xFE07FF6495D52E2A` | **JHash:** `0x59DE73AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_PANIC_EXIT_SCENARIO(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_PANIC_EXIT_SCENARIO, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### TOGGLE_SCENARIO_PED_COWER_IN_PLACE

**Hash:** `0x9A77DFD295E29B09` | **JHash:** `0xC08FE5F6` | **Build:** `323`

**C++ 原型:**
```cpp
void TOGGLE_SCENARIO_PED_COWER_IN_PLACE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_SCENARIO_PED_COWER_IN_PLACE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TRIGGER_PED_SCENARIO_PANICEXITTOFLEE

**Hash:** `0x25361A96E0F7E419` | **JHash:** `0x58C0F6CF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TRIGGER_PED_SCENARIO_PANICEXITTOFLEE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TRIGGER_PED_SCENARIO_PANICEXITTOFLEE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_SHOULD_PLAY_DIRECTED_NORMAL_SCENARIO_EXIT

**Hash:** `0xEC6935EBE0847B90` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_SHOULD_PLAY_DIRECTED_NORMAL_SCENARIO_EXIT(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_SHOULD_PLAY_DIRECTED_NORMAL_SCENARIO_EXIT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_SHOULD_PLAY_NORMAL_SCENARIO_EXIT

**Hash:** `0xA3A9299C4F2ADB98` | **JHash:** `0x033F43FA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOULD_PLAY_NORMAL_SCENARIO_EXIT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOULD_PLAY_NORMAL_SCENARIO_EXIT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_SHOULD_PLAY_IMMEDIATE_SCENARIO_EXIT

**Hash:** `0xF1C03A5352243A30` | **JHash:** `0x4C684C81` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOULD_PLAY_IMMEDIATE_SCENARIO_EXIT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOULD_PLAY_IMMEDIATE_SCENARIO_EXIT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_SHOULD_PLAY_FLEE_SCENARIO_EXIT

**Hash:** `0xEEED8FAFEC331A70` | **JHash:** `0x7B4C3E6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_SHOULD_PLAY_FLEE_SCENARIO_EXIT(Ped ped, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_SHOULD_PLAY_FLEE_SCENARIO_EXIT, ped, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_SHOULD_IGNORE_SCENARIO_EXIT_COLLISION_CHECKS

**Hash:** `0x425AECF167663F48` | **JHash:** `0x5BC276AE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOULD_IGNORE_SCENARIO_EXIT_COLLISION_CHECKS(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOULD_IGNORE_SCENARIO_EXIT_COLLISION_CHECKS, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_SHOULD_IGNORE_SCENARIO_NAV_CHECKS

**Hash:** `0x5B6010B3CBC29095` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOULD_IGNORE_SCENARIO_NAV_CHECKS(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOULD_IGNORE_SCENARIO_NAV_CHECKS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_SHOULD_PROBE_FOR_SCENARIO_EXITS_IN_ONE_FRAME

**Hash:** `0xCEDA60A74219D064` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOULD_PROBE_FOR_SCENARIO_EXITS_IN_ONE_FRAME(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOULD_PROBE_FOR_SCENARIO_EXITS_IN_ONE_FRAME, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_GESTURING

**Hash:** `0xC30BDAEE47256C13` | **Build:** `1868`

**C++ 原型:**
```cpp
BOOL IS_PED_GESTURING(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_GESTURING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### RESET_FACIAL_IDLE_ANIM

**Hash:** `0x007FDE5A7897E426` | **Build:** `2802`

**C++ 原型:**
```cpp
void RESET_FACIAL_IDLE_ANIM(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_FACIAL_IDLE_ANIM, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### PLAY_FACIAL_ANIM

**Hash:** `0xE1E65CA8AC9C00ED` | **JHash:** `0x1F6CCDDE` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_FACIAL_ANIM(Ped ped, const char* animName, const char* animDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_FACIAL_ANIM, ped, animName, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animName` | `const char*` | `string` | |
| `animDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### SET_FACIAL_CLIPSET

**Hash:** `0x5687C7F05B39E401` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_FACIAL_CLIPSET(Ped ped, const char* animDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FACIAL_CLIPSET, ped, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Clipsets:
facials@gen_female@base
facials@gen_male@base
facials@p_m_zero@base

Typically followed with SET_FACIAL_IDLE_ANIM_OVERRIDE:
mood_drunk_1
mood_stressed_1
mood_happy_1
mood_talking_1


---

### SET_FACIAL_IDLE_ANIM_OVERRIDE

**Hash:** `0xFFC24B988B938B38` | **JHash:** `0x9BA19C13` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FACIAL_IDLE_ANIM_OVERRIDE(Ped ped, const char* animName, const char* animDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FACIAL_IDLE_ANIM_OVERRIDE, ped, animName, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animName` | `const char*` | `string` | |
| `animDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### CLEAR_FACIAL_IDLE_ANIM_OVERRIDE

**Hash:** `0x726256CC1EEB182F` | **JHash:** `0x5244F4E2` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_FACIAL_IDLE_ANIM_OVERRIDE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_FACIAL_IDLE_ANIM_OVERRIDE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_PLAY_GESTURE_ANIMS

**Hash:** `0xBAF20C5432058024` | **JHash:** `0xE131E3B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_GESTURE_ANIMS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_GESTURE_ANIMS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_PLAY_VISEME_ANIMS

**Hash:** `0xF833DDBA3B104D43` | **JHash:** `0xA2FDAF27` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_VISEME_ANIMS(Ped ped, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_VISEME_ANIMS, ped, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p2 usually 0

---

### SET_PED_IS_IGNORED_BY_AUTO_OPEN_DOORS

**Hash:** `0x33A60D8BDD6E508C` | **JHash:** `0xADB2511A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_IS_IGNORED_BY_AUTO_OPEN_DOORS(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_IS_IGNORED_BY_AUTO_OPEN_DOORS, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_PLAY_AMBIENT_ANIMS

**Hash:** `0x6373D1349925A70E` | **JHash:** `0xF8053081` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_AMBIENT_ANIMS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_AMBIENT_ANIMS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_PLAY_AMBIENT_BASE_ANIMS

**Hash:** `0x0EB0585D15254740` | **JHash:** `0x5720A5DD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_AMBIENT_BASE_ANIMS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_AMBIENT_BASE_ANIMS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TRIGGER_IDLE_ANIMATION_ON_PED

**Hash:** `0xC2EE020F5FB4DB53` | **JHash:** `0xB7CD0A49` | **Build:** `323`

**C++ 原型:**
```cpp
void TRIGGER_IDLE_ANIMATION_ON_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_IDLE_ANIMATION_ON_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_ARM_IK

**Hash:** `0x6C3B4D6D13B4C841` | **JHash:** `0x343B4DE0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_ARM_IK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_ARM_IK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_BODY_RECOIL_IK

**Hash:** `0xE84EC1735FB39663` | **Build:** `3717`

**C++ 原型:**
```cpp
void SET_PED_CAN_BODY_RECOIL_IK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BODY_RECOIL_IK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_HEAD_IK

**Hash:** `0xC11C18092C5530DC` | **JHash:** `0xD3B04476` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_HEAD_IK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_HEAD_IK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_LEG_IK

**Hash:** `0x73518ECE2485412B` | **JHash:** `0x9955BC6F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_LEG_IK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_LEG_IK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_TORSO_IK

**Hash:** `0xF2B7106D37947CE0` | **JHash:** `0x8E5D4EAB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_TORSO_IK(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_TORSO_IK, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_TORSO_REACT_IK

**Hash:** `0xF5846EDB26A98A24` | **JHash:** `0x7B0040A8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_TORSO_REACT_IK(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_TORSO_REACT_IK, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_TORSO_VEHICLE_IK

**Hash:** `0x6647C5F6F5792496` | **JHash:** `0x0FDA62DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_TORSO_VEHICLE_IK(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_TORSO_VEHICLE_IK, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_USE_AUTO_CONVERSATION_LOOKAT

**Hash:** `0xEC4686EC06434678` | **JHash:** `0x584C5178` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_USE_AUTO_CONVERSATION_LOOKAT(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_USE_AUTO_CONVERSATION_LOOKAT, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_HEADTRACKING_PED

**Hash:** `0x5CD3CB88A7F8850D` | **JHash:** `0x2A5DF721` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HEADTRACKING_PED(Ped ped1, Ped ped2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HEADTRACKING_PED, ped1, ped2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped1` | `Ped` | `int` | |
| `ped2` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_HEADTRACKING_ENTITY

**Hash:** `0x813A0A7C9D2E831F` | **JHash:** `0x233C9ACF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HEADTRACKING_ENTITY(Ped ped, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HEADTRACKING_ENTITY, ped, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_PRIMARY_LOOKAT

**Hash:** `0xCD17B554996A8D9E` | **JHash:** `0x6DEF6F1C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PRIMARY_LOOKAT(Ped ped, Ped lookAt)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PRIMARY_LOOKAT, ped, lookAt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `lookAt` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** This is only called once in the scripts.

sub_1CD9(&l_49, 0, getElem(3, &l_34, 4), "MICHAEL", 0, 1);
                    sub_1CA8("WORLD_HUMAN_SMOKING", 2);
                    PED::SET_PED_PRIMARY_LOOKAT(getElem(3, &l_34, 4), PLAYER::PLAYER_PED_ID());

---

### SET_PED_CLOTH_PIN_FRAMES

**Hash:** `0x78C4E9961DB3EB5B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CLOTH_PIN_FRAMES(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CLOTH_PIN_FRAMES, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_CLOTH_PACKAGE_INDEX

**Hash:** `0x82A3D6D9CC2CB8E3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CLOTH_PACKAGE_INDEX(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CLOTH_PACKAGE_INDEX, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_CLOTH_PRONE

**Hash:** `0xA660FAF550EB37E5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CLOTH_PRONE(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CLOTH_PRONE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CONFIG_FLAG

**Hash:** `0x1913FE4CBF41C463` | **JHash:** `0x9CFBE10D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CONFIG_FLAG(Ped ped, int flagId, BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CONFIG_FLAG, ped, flagId, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flagId` | `int` | `int` | |
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enum ePedConfigFlags
{
	_CPED_CONFIG_FLAG_0xC63DE95E = 1,
	CPED_CONFIG_FLAG_NoCriticalHits = 2,
	CPED_CONFIG_FLAG_DrownsInWater = 3,
	CPED_CONFIG_FLAG_DisableReticuleFixedLockon = 4,
	_CPED_CONFIG_FLAG_0x37D196F4 = 5,
	_CPED_CONFIG_FLAG_0xE2462399 = 6,
	CPED_CONFIG_FLAG_UpperBodyDamageAnimsOnly = 7,
	_CPED_CONFIG_FLAG_0xEDDEB838 = 8,
	_CPED_CONFIG_FLAG_0xB398B6FD = 9,
	_CPED_CONFIG_FLAG_0xF6664E68 = 10,
	_CPED_CONFIG_FLAG_0xA05E7CA3 = 11,
	_CPED_CONFIG_FLAG_0xCE394045 = 12,
	CPED_CONFIG_FLAG_NeverLeavesGroup = 13,
	_CPED_CONFIG_FLAG_0xCD8D1411 = 14,
	_CPED_CONFIG_FLAG_0xB031F1A9 = 15,
	_CPED_CONFIG_FLAG_0xFE65BEE3 = 16,
	CPED_CONFIG_FLAG_BlockNonTemporaryEvents = 17,
	_CPED_CONFIG_FLAG_0x380165BD = 18,
	_CPED_CONFIG_FLAG_0x07C045C7 = 19,
	_CPED_CONFIG_FLAG_0x583B5E2D = 20,
	_CPED_CONFIG_FLAG_0x475EDA58 = 21,
	_CPED_CONFIG_FLAG_0x8629D05B = 22,
	_CPED_CONFIG_FLAG_0x1522968B = 23,
	CPED_CONFIG_FLAG_IgnoreSeenMelee = 24,
	_CPED_CONFIG_FLAG_0x4CC09C4B = 25,
	_CPED_CONFIG_FLAG_0x034F3053 = 26,
	_CPED_CONFIG_FLAG_0xD91BA7CC = 27,
	_CPED_CONFIG_FLAG_0x5C8DC66E = 28,
	CPED_CONFIG_FLAG_GetOutUndriveableVehicle = 29,
	_CPED_CONFIG_FLAG_0x6580B9D2 = 30,
	_CPED_CONFIG_FLAG_0x0EF7A297 = 31,
	CPED_CONFIG_FLAG_WillFlyThruWindscreen = 32,
	CPED_CONFIG_FLAG_DieWhenRagdoll = 33,
	CPED_CONFIG_FLAG_HasHelmet = 34,
	CPED_CONFIG_FLAG_UseHelmet = 35,
	CPED_CONFIG_FLAG_DontTakeOffHelmet = 36,
	_CPED_CONFIG_FLAG_0xB130D17B = 37,
	_CPED_CONFIG_FLAG_0x5F071200 = 38,
	CPED_CONFIG_FLAG_DisableEvasiveDives = 39,
	_CPED_CONFIG_FLAG_0xC287AAFF = 40,
	_CPED_CONFIG_FLAG_0x203328CC = 41,
	CPED_CONFIG_FLAG_DontInfluenceWantedLevel = 42,
	CPED_CONFIG_FLAG_DisablePlayerLockon = 43,
	CPED_CONFIG_FLAG_DisableLockonToRandomPeds = 44,
	CPED_CONFIG_FLAG_AllowLockonToFriendlyPlayers = 45,
	_CPED_CONFIG_FLAG_0xDB115BFA = 46,
	CPED_CONFIG_FLAG_PedBeingDeleted = 47,
	CPED_CONFIG_FLAG_BlockWeaponSwitching = 48,
	_CPED_CONFIG_FLAG_0xF8E99565 = 49,
	_CPED_CONFIG_FLAG_0xDD17FEE6 = 50,
	_CPED_CONFIG_FLAG_0x7ED9B2C9 = 51,
	_CPED_CONFIG_FLAG_NoCollison = 52,
	_CPED_CONFIG_FLAG_0x5A6C1F6E = 53,
	_CPED_CONFIG_FLAG_0xD749FC41 = 54,
	_CPED_CONFIG_FLAG_0x357F63F3 = 55,
	_CPED_CONFIG_FLAG_0xC5E60961 = 56,
	_CPED_CONFIG_FLAG_0x29275C3E = 57,
	CPED_CONFIG_FLAG_IsFiring = 58,
	CPED_CONFIG_FLAG_WasFiring = 59,
	CPED_CONFIG_FLAG_IsStanding = 60,
	CPED_CONFIG_FLAG_WasStanding = 61,
	CPED_CONFIG_FLAG_InVehicle = 62,
	CPED_CONFIG_FLAG_OnMount = 63,
	CPED_CONFIG_FLAG_AttachedToVehicle = 64,
	CPED_CONFIG_FLAG_IsSwimming = 65,
	CPED_CONFIG_FLAG_WasSwimming = 66,
	CPED_CONFIG_FLAG_IsSkiing = 67,
	CPED_CONFIG_FLAG_IsSitting = 68,
	CPED_CONFIG_FLAG_KilledByStealth = 69,
	CPED_CONFIG_FLAG_KilledByTakedown = 70,
	CPED_CONFIG_FLAG_Knockedout = 71,
	_CPED_CONFIG_FLAG_0x3E3C4560 = 72,
	_CPED_CONFIG_FLAG_0x2994C7B7 = 73,
	_CPED_CONFIG_FLAG_0x6D59D275 = 74,
	CPED_CONFIG_FLAG_UsingCoverPoint = 75,
	CPED_CONFIG_FLAG_IsInTheAir = 76,
	_CPED_CONFIG_FLAG_0x2D493FB7 = 77,
	CPED_CONFIG_FLAG_IsAimingGun = 78,
	_CPED_CONFIG_FLAG_0x14D69875 = 79,
	_CPED_CONFIG_FLAG_0x40B05311 = 80,
	_CPED_CONFIG_FLAG_0x8B230BC5 = 81,
	_CPED_CONFIG_FLAG_0xC74E5842 = 82,
	_CPED_CONFIG_FLAG_0x9EA86147 = 83,
	_CPED_CONFIG_FLAG_0x674C746C = 84,
	_CPED_CONFIG_FLAG_0x3E56A8C2 = 85,
	_CPED_CONFIG_FLAG_0xC144A1EF = 86,
	_CPED_CONFIG_FLAG_0x0548512D = 87,
	_CPED_CONFIG_FLAG_0x31C93909 = 88,
	_CPED_CONFIG_FLAG_0xA0269315 = 89,
	_CPED_CONFIG_FLAG_0xD4D59D4D = 90,
	_CPED_CONFIG_FLAG_0x411D4420 = 91,
	_CPED_CONFIG_FLAG_0xDF4AEF0D = 92,
	CPED_CONFIG_FLAG_ForcePedLoadCover = 93,
	_CPED_CONFIG_FLAG_0x300E4CD3 = 94,
	_CPED_CONFIG_FLAG_0xF1C5BF04 = 95,
	_CPED_CONFIG_FLAG_0x89C2EF13 = 96,
	CPED_CONFIG_FLAG_VaultFromCover = 97,
	_CPED_CONFIG_FLAG_0x02A852C8 = 98,
	_CPED_CONFIG_FLAG_0x3D9407F1 = 99,
	_CPED_CONFIG_FLAG_IsDrunk = 100, // 0x319B4558
	CPED_CONFIG_FLAG_ForcedAim = 101,
	_CPED_CONFIG_FLAG_0xB942D71A = 102,
	_CPED_CONFIG_FLAG_0xD26C55A8 = 103,
	CPED_CONFIG_FLAG_OpenDoorArmIK = 104,
	CPED_CONFIG_FLAG_ForceReload = 105,
	CPED_CONFIG_FLAG_DontActivateRagdollFromVehicleImpact = 106,
	CPED_CONFIG_FLAG_DontActivateRagdollFromBulletImpact = 107,
	CPED_CONFIG_FLAG_DontActivateRagdollFromExplosions = 108,
	CPED_CONFIG_FLAG_DontActivateRagdollFromFire = 109,
	CPED_CONFIG_FLAG_DontActivateRagdollFromElectrocution = 110,
	_CPED_CONFIG_FLAG_0x83C0A4BF = 111,
	_CPED_CONFIG_FLAG_0x0E0FAF8C = 112,
	CPED_CONFIG_FLAG_KeepWeaponHolsteredUnlessFired = 113,
	_CPED_CONFIG_FLAG_0x43B80B79 = 114,
	_CPED_CONFIG_FLAG_0x0D2A9309 = 115,
	CPED_CONFIG_FLAG_GetOutBurningVehicle = 116,
	CPED_CONFIG_FLAG_BumpedByPlayer = 117,
	CPED_CONFIG_FLAG_RunFromFiresAndExplosions = 118,
	CPED_CONFIG_FLAG_TreatAsPlayerDuringTargeting = 119,
	CPED_CONFIG_FLAG_IsHandCuffed = 120,
	CPED_CONFIG_FLAG_IsAnkleCuffed = 121,
	CPED_CONFIG_FLAG_DisableMelee = 122,
	CPED_CONFIG_FLAG_DisableUnarmedDrivebys = 123,
	CPED_CONFIG_FLAG_JustGetsPulledOutWhenElectrocuted = 124,
	_CPED_CONFIG_FLAG_0x5FED6BFD = 125,
	CPED_CONFIG_FLAG_WillNotHotwireLawEnforcementVehicle = 126,
	CPED_CONFIG_FLAG_WillCommandeerRatherThanJack = 127,
	CPED_CONFIG_FLAG_CanBeAgitated = 128,
	CPED_CONFIG_FLAG_ForcePedToFaceLeftInCover = 129,
	CPED_CONFIG_FLAG_ForcePedToFaceRightInCover = 130,
	CPED_CONFIG_FLAG_BlockPedFromTurningInCover = 131,
	CPED_CONFIG_FLAG_KeepRelationshipGroupAfterCleanUp = 132,
	CPED_CONFIG_FLAG_ForcePedToBeDragged = 133,
	CPED_CONFIG_FLAG_PreventPedFromReactingToBeingJacked = 134,
	CPED_CONFIG_FLAG_IsScuba = 135,
	CPED_CONFIG_FLAG_WillArrestRatherThanJack = 136,
	CPED_CONFIG_FLAG_RemoveDeadExtraFarAway = 137,
	CPED_CONFIG_FLAG_RidingTrain = 138,
	CPED_CONFIG_FLAG_ArrestResult = 139,
	CPED_CONFIG_FLAG_CanAttackFriendly = 140,
	CPED_CONFIG_FLAG_WillJackAnyPlayer = 141,
	_CPED_CONFIG_FLAG_0x6901E731 = 142,
	_CPED_CONFIG_FLAG_0x9EC9BF6C = 143,
	CPED_CONFIG_FLAG_WillJackWantedPlayersRatherThanStealCar = 144,
	CPED_CONFIG_FLAG_ShootingAnimFlag = 145,
	CPED_CONFIG_FLAG_DisableLadderClimbing = 146,
	CPED_CONFIG_FLAG_StairsDetected = 147,
	CPED_CONFIG_FLAG_SlopeDetected = 148,
	_CPED_CONFIG_FLAG_0x1A15670B = 149,
	CPED_CONFIG_FLAG_CowerInsteadOfFlee = 150,
	CPED_CONFIG_FLAG_CanActivateRagdollWhenVehicleUpsideDown = 151,
	CPED_CONFIG_FLAG_AlwaysRespondToCriesForHelp = 152,
	CPED_CONFIG_FLAG_DisableBloodPoolCreation = 153,
	CPED_CONFIG_FLAG_ShouldFixIfNoCollision = 154,
	CPED_CONFIG_FLAG_CanPerformArrest = 155,
	CPED_CONFIG_FLAG_CanPerformUncuff = 156,
	CPED_CONFIG_FLAG_CanBeArrested = 157,
	_CPED_CONFIG_FLAG_0xF7960FF5 = 158,
	CPED_CONFIG_FLAG_PlayerPreferFrontSeatMP = 159,
	_CPED_CONFIG_FLAG_0x0C6C3099 = 160,
	_CPED_CONFIG_FLAG_0x645F927A = 161,
	_CPED_CONFIG_FLAG_0xA86549B9 = 162,
	_CPED_CONFIG_FLAG_0x8AAF337A = 163,
	_CPED_CONFIG_FLAG_0x13BAA6E7 = 164,
	_CPED_CONFIG_FLAG_0x5FB9D1F5 = 165,
	CPED_CONFIG_FLAG_IsInjured = 166,
	CPED_CONFIG_FLAG_DontEnterVehiclesInPlayersGroup = 167,
	_CPED_CONFIG_FLAG_0xD8072639 = 168,
	CPED_CONFIG_FLAG_PreventAllMeleeTaunts = 169,
	CPED_CONFIG_FLAG_ForceDirectEntry = 170,
	CPED_CONFIG_FLAG_AlwaysSeeApproachingVehicles = 171,
	CPED_CONFIG_FLAG_CanDiveAwayFromApproachingVehicles = 172,
	CPED_CONFIG_FLAG_AllowPlayerToInterruptVehicleEntryExit = 173,
	CPED_CONFIG_FLAG_OnlyAttackLawIfPlayerIsWanted = 174,
	_CPED_CONFIG_FLAG_0x90008BFA = 175,
	_CPED_CONFIG_FLAG_0x07C7A910 = 176,
	CPED_CONFIG_FLAG_PedsJackingMeDontGetIn = 177,
	_CPED_CONFIG_FLAG_0xCE4E8BE2 = 178,
	CPED_CONFIG_FLAG_PedIgnoresAnimInterruptEvents = 179,
	CPED_CONFIG_FLAG_IsInCustody = 180,
	CPED_CONFIG_FLAG_ForceStandardBumpReactionThresholds = 181,
	CPED_CONFIG_FLAG_LawWillOnlyAttackIfPlayerIsWanted = 182,
	CPED_CONFIG_FLAG_IsAgitated = 183,
	CPED_CONFIG_FLAG_PreventAutoShuffleToDriversSeat = 184,
	CPED_CONFIG_FLAG_UseKinematicModeWhenStationary = 185,
	CPED_CONFIG_FLAG_EnableWeaponBlocking = 186,
	CPED_CONFIG_FLAG_HasHurtStarted = 187,
	CPED_CONFIG_FLAG_DisableHurt = 188,
	CPED_CONFIG_FLAG_PlayerIsWeird = 189,
	_CPED_CONFIG_FLAG_0x32FC208B = 190,
	_CPED_CONFIG_FLAG_0x0C296E5A = 191,
	_CPED_CONFIG_FLAG_0xE63B73EC = 192,
	CPED_CONFIG_FLAG_DoNothingWhenOnFootByDefault = 193,
	CPED_CONFIG_FLAG_UsingScenario = 194,
	CPED_CONFIG_FLAG_VisibleOnScreen = 195,
	_CPED_CONFIG_FLAG_0xD88C58A1 = 196,
	_CPED_CONFIG_FLAG_0x5A3DCF43 = 197,
	_CPED_CONFIG_FLAG_0xEA02B420 = 198,
	CPED_CONFIG_FLAG_DontActivateRagdollOnVehicleCollisionWhenDead = 199,
	CPED_CONFIG_FLAG_HasBeenInArmedCombat = 200,
	_CPED_CONFIG_FLAG_0x5E6466F6 = 201,
	CPED_CONFIG_FLAG_Avoidance_Ignore_All = 202,
	CPED_CONFIG_FLAG_Avoidance_Ignored_by_All = 203,
	CPED_CONFIG_FLAG_Avoidance_Ignore_Group1 = 204,
	CPED_CONFIG_FLAG_Avoidance_Member_of_Group1 = 205,
	CPED_CONFIG_FLAG_ForcedToUseSpecificGroupSeatIndex = 206,
	_CPED_CONFIG_FLAG_0x415B26B9 = 207,
	CPED_CONFIG_FLAG_DisableExplosionReactions = 208,
	CPED_CONFIG_FLAG_DodgedPlayer = 209,
	CPED_CONFIG_FLAG_WaitingForPlayerControlInterrupt = 210,
	CPED_CONFIG_FLAG_ForcedToStayInCover = 211,
	CPED_CONFIG_FLAG_GeneratesSoundEvents = 212,
	CPED_CONFIG_FLAG_ListensToSoundEvents = 213,
	CPED_CONFIG_FLAG_AllowToBeTargetedInAVehicle = 214,
	CPED_CONFIG_FLAG_WaitForDirectEntryPointToBeFreeWhenExiting = 215,
	CPED_CONFIG_FLAG_OnlyRequireOnePressToExitVehicle = 216,
	CPED_CONFIG_FLAG_ForceExitToSkyDive = 217,
	_CPED_CONFIG_FLAG_0x3C7DF9DF = 218,
	_CPED_CONFIG_FLAG_0x848FFEF2 = 219,
	CPED_CONFIG_FLAG_DontEnterLeadersVehicle = 220,
	CPED_CONFIG_FLAG_DisableExitToSkyDive = 221,
	_CPED_CONFIG_FLAG_0x84F722FA = 222,
	_CPED_CONFIG_FLAG_Shrink = 223, // 0xD1B87B1F
	_CPED_CONFIG_FLAG_0x728AA918 = 224,
	CPED_CONFIG_FLAG_DisablePotentialToBeWalkedIntoResponse = 225,
	CPED_CONFIG_FLAG_DisablePedAvoidance = 226,
	CPED_CONFIG_FLAG_ForceRagdollUponDeath = 227,
	_CPED_CONFIG_FLAG_0x1EA7225F = 228,
	CPED_CONFIG_FLAG_DisablePanicInVehicle = 229,
	CPED_CONFIG_FLAG_AllowedToDetachTrailer = 230,
	_CPED_CONFIG_FLAG_0xFC3E572D = 231,
	_CPED_CONFIG_FLAG_0x08E9F9CF = 232,
	_CPED_CONFIG_FLAG_0x2D3BA52D = 233,
	_CPED_CONFIG_FLAG_0xFD2F53EA = 234,
	_CPED_CONFIG_FLAG_0x31A1B03B = 235,
	CPED_CONFIG_FLAG_IsHoldingProp = 236,
	CPED_CONFIG_FLAG_BlocksPathingWhenDead = 237,
	_CPED_CONFIG_FLAG_0xCE57C9A3 = 238,
	_CPED_CONFIG_FLAG_0x26149198 = 239,
	CPED_CONFIG_FLAG_ForceSkinCharacterCloth = 240,
	CPED_CONFIG_FLAG_LeaveEngineOnWhenExitingVehicles = 241,
	CPED_CONFIG_FLAG_PhoneDisableTextingAnimations = 242,
	CPED_CONFIG_FLAG_PhoneDisableTalkingAnimations = 243,
	CPED_CONFIG_FLAG_PhoneDisableCameraAnimations = 244,
	CPED_CONFIG_FLAG_DisableBlindFiringInShotReactions = 245,
	CPED_CONFIG_FLAG_AllowNearbyCoverUsage = 246,
	_CPED_CONFIG_FLAG_0x0C754ACA = 247,
	CPED_CONFIG_FLAG_CanPlayInCarIdles = 248,
	CPED_CONFIG_FLAG_CanAttackNonWantedPlayerAsLaw = 249,
	CPED_CONFIG_FLAG_WillTakeDamageWhenVehicleCrashes = 250,
	CPED_CONFIG_FLAG_AICanDrivePlayerAsRearPassenger = 251,
	CPED_CONFIG_FLAG_PlayerCanJackFriendlyPlayers = 252,
	CPED_CONFIG_FLAG_OnStairs = 253,
	_CPED_CONFIG_FLAG_0xE1A2F73F = 254,
	CPED_CONFIG_FLAG_AIDriverAllowFriendlyPassengerSeatEntry = 255,
	_CPED_CONFIG_FLAG_0xF1EB20A9 = 256,
	CPED_CONFIG_FLAG_AllowMissionPedToUseInjuredMovement = 257,
	_CPED_CONFIG_FLAG_0x329DCF1A = 258,
	_CPED_CONFIG_FLAG_0x8D90DD1B = 259,
	_CPED_CONFIG_FLAG_0xB8A292B7 = 260,
	CPED_CONFIG_FLAG_PreventUsingLowerPrioritySeats = 261,
	_CPED_CONFIG_FLAG_0x2AF558F0 = 262,
	_CPED_CONFIG_FLAG_0x82251455 = 263,
	_CPED_CONFIG_FLAG_0x30CF498B = 264,
	_CPED_CONFIG_FLAG_0xE1CD50AF = 265,
	_CPED_CONFIG_FLAG_0x72E4AE48 = 266,
	_CPED_CONFIG_FLAG_0xC2657EA1 = 267,
	CPED_CONFIG_FLAG_TeleportToLeaderVehicle = 268,
	CPED_CONFIG_FLAG_Avoidance_Ignore_WeirdPedBuffer = 269,
	CPED_CONFIG_FLAG_OnStairSlope = 270,
	_CPED_CONFIG_FLAG_0xA0897933 = 271,
	CPED_CONFIG_FLAG_DontBlipCop = 272,
	CPED_CONFIG_FLAG_ClimbedShiftedFence = 273,
	_CPED_CONFIG_FLAG_0xF7823618 = 274,
	CPED_CONFIG_FLAG_KillWhenTrapped = 275,
	CPED_CONFIG_FLAG_EdgeDetected = 276,
	_CPED_CONFIG_FLAG_0x92B67896 = 277,
	_CPED_CONFIG_FLAG_0xCAD677C9 = 278,
	CPED_CONFIG_FLAG_AvoidTearGas = 279,
	_CPED_CONFIG_FLAG_0x5276AC7B = 280,
	CPED_CONFIG_FLAG_DisableGoToWritheWhenInjured = 281,
	CPED_CONFIG_FLAG_OnlyUseForcedSeatWhenEnteringHeliInGroup = 282,
	_CPED_CONFIG_FLAG_0x9139724D = 283,
	_CPED_CONFIG_FLAG_0xA1457461 = 284,
	CPED_CONFIG_FLAG_DisableWeirdPedEvents = 285,
	CPED_CONFIG_FLAG_ShouldChargeNow = 286,
	CPED_CONFIG_FLAG_RagdollingOnBoat = 287,
	CPED_CONFIG_FLAG_HasBrandishedWeapon = 288,
	_CPED_CONFIG_FLAG_0x1B9EE8A1 = 289,
	_CPED_CONFIG_FLAG_0xF3F5758C = 290,
	_CPED_CONFIG_FLAG_0x2A9307F1 = 291,
	_CPED_CONFIG_FLAG_FreezePosition = 292, // 0x7403D216
	_CPED_CONFIG_FLAG_0xA06A3C6C = 293,
	CPED_CONFIG_FLAG_DisableShockingEvents = 294,
	_CPED_CONFIG_FLAG_0xF8DA25A5 = 295,
	CPED_CONFIG_FLAG_NeverReactToPedOnRoof = 296,
	_CPED_CONFIG_FLAG_0xB31F1187 = 297,
	_CPED_CONFIG_FLAG_0x84315402 = 298,
	CPED_CONFIG_FLAG_DisableShockingDrivingOnPavementEvents = 299,
	_CPED_CONFIG_FLAG_0xC7829B67 = 300,
	CPED_CONFIG_FLAG_DisablePedConstraints = 301,
	CPED_CONFIG_FLAG_ForceInitialPeekInCover = 302,
	_CPED_CONFIG_FLAG_0x2ADA871B = 303,
	_CPED_CONFIG_FLAG_0x47BC8A58 = 304,
	CPED_CONFIG_FLAG_DisableJumpingFromVehiclesAfterLeader = 305,
	_CPED_CONFIG_FLAG_0x4A133C50 = 306,
	_CPED_CONFIG_FLAG_0xC58099C3 = 307,
	_CPED_CONFIG_FLAG_0xF3D76D41 = 308,
	_CPED_CONFIG_FLAG_0xB0EEE9F2 = 309,
	CPED_CONFIG_FLAG_IsInCluster = 310,
	CPED_CONFIG_FLAG_ShoutToGroupOnPlayerMelee = 311,
	CPED_CONFIG_FLAG_IgnoredByAutoOpenDoors = 312,
	_CPED_CONFIG_FLAG_0xD4136C22 = 313,
	CPED_CONFIG_FLAG_ForceIgnoreMeleeActiveCombatant = 314,
	CPED_CONFIG_FLAG_CheckLoSForSoundEvents = 315,
	_CPED_CONFIG_FLAG_0xD5C98277 = 316,
	CPED_CONFIG_FLAG_CanSayFollowedByPlayerAudio = 317,
	CPED_CONFIG_FLAG_ActivateRagdollFromMinorPlayerContact = 318,
	_CPED_CONFIG_FLAG_0xD8BE1D54 = 319,
	CPED_CONFIG_FLAG_ForcePoseCharacterCloth = 320,
	CPED_CONFIG_FLAG_HasClothCollisionBounds = 321,
	CPED_CONFIG_FLAG_HasHighHeels = 322,
	_CPED_CONFIG_FLAG_0x86B01E54 = 323,
	CPED_CONFIG_FLAG_DontBehaveLikeLaw = 324,
	_CPED_CONFIG_FLAG_0xC03B736C = 325, // SpawnedAtScenario?
	CPED_CONFIG_FLAG_DisablePoliceInvestigatingBody = 326,
	CPED_CONFIG_FLAG_DisableWritheShootFromGround = 327,
	CPED_CONFIG_FLAG_LowerPriorityOfWarpSeats = 328,
	CPED_CONFIG_FLAG_DisableTalkTo = 329,
	CPED_CONFIG_FLAG_DontBlip = 330,
	CPED_CONFIG_FLAG_IsSwitchingWeapon = 331,
	CPED_CONFIG_FLAG_IgnoreLegIkRestrictions = 332,
	_CPED_CONFIG_FLAG_0x150468FD = 333,
	_CPED_CONFIG_FLAG_0x914EBD6B = 334,
	_CPED_CONFIG_FLAG_0x79AF3B6D = 335,
	_CPED_CONFIG_FLAG_0x75C7A632 = 336,
	_CPED_CONFIG_FLAG_0x52D530E2 = 337,
	_CPED_CONFIG_FLAG_0xDB2A90E0 = 338,
	CPED_CONFIG_FLAG_AllowTaskDoNothingTimeslicing = 339,
	_CPED_CONFIG_FLAG_0x12ADB567 = 340,
	_CPED_CONFIG_FLAG_0x105C8518 = 341,
	CPED_CONFIG_FLAG_NotAllowedToJackAnyPlayers = 342,
	_CPED_CONFIG_FLAG_0xED152C3E = 343,
	_CPED_CONFIG_FLAG_0xA0EFE6A8 = 344,
	CPED_CONFIG_FLAG_AlwaysLeaveTrainUponArrival = 345,
	_CPED_CONFIG_FLAG_0xCDDFE830 = 346,
	CPED_CONFIG_FLAG_OnlyWritheFromWeaponDamage = 347,
	CPED_CONFIG_FLAG_UseSloMoBloodVfx = 348,
	CPED_CONFIG_FLAG_EquipJetpack = 349,
	CPED_CONFIG_FLAG_PreventDraggedOutOfCarThreatResponse = 350,
	_CPED_CONFIG_FLAG_0xE13D1F7C = 351,
	_CPED_CONFIG_FLAG_0x40E25FB9 = 352,
	_CPED_CONFIG_FLAG_0x930629D9 = 353,
	_CPED_CONFIG_FLAG_0xECCF0C7F = 354,
	_CPED_CONFIG_FLAG_0xB6E9613B = 355,
	CPED_CONFIG_FLAG_ForceDeepSurfaceCheck = 356,
	CPED_CONFIG_FLAG_DisableDeepSurfaceAnims = 357,
	CPED_CONFIG_FLAG_DontBlipNotSynced = 358,
	CPED_CONFIG_FLAG_IsDuckingInVehicle = 359,
	CPED_CONFIG_FLAG_PreventAutoShuffleToTurretSeat = 360,
	CPED_CONFIG_FLAG_DisableEventInteriorStatusCheck = 361,
	CPED_CONFIG_FLAG_HasReserveParachute = 362,
	CPED_CONFIG_FLAG_UseReserveParachute = 363,
	CPED_CONFIG_FLAG_TreatDislikeAsHateWhenInCombat = 364,
	CPED_CONFIG_FLAG_OnlyUpdateTargetWantedIfSeen = 365,
	CPED_CONFIG_FLAG_AllowAutoShuffleToDriversSeat = 366,
	_CPED_CONFIG_FLAG_0xD7E07D37 = 367,
	_CPED_CONFIG_FLAG_0x03C4FD24 = 368,
	_CPED_CONFIG_FLAG_0x7675789A = 369,
	_CPED_CONFIG_FLAG_0xB7288A88 = 370,
	_CPED_CONFIG_FLAG_0xC06B6291 = 371,
	CPED_CONFIG_FLAG_PreventReactingToSilencedCloneBullets = 372,
	CPED_CONFIG_FLAG_DisableInjuredCryForHelpEvents = 373,
	CPED_CONFIG_FLAG_NeverLeaveTrain = 374,
	CPED_CONFIG_FLAG_DontDropJetpackOnDeath = 375,
	_CPED_CONFIG_FLAG_0x147F1FFB = 376,
	_CPED_CONFIG_FLAG_0x4376DD79 = 377,
	_CPED_CONFIG_FLAG_0xCD3DB518 = 378,
	_CPED_CONFIG_FLAG_0xFE4BA4B6 = 379,
	CPED_CONFIG_FLAG_DisableAutoEquipHelmetsInBikes = 380,
	_CPED_CONFIG_FLAG_0xBCD816CD = 381,
	_CPED_CONFIG_FLAG_0xCF02DD69 = 382,
	_CPED_CONFIG_FLAG_0xF73AFA2E = 383,
	_CPED_CONFIG_FLAG_0x80B9A9D0 = 384,
	_CPED_CONFIG_FLAG_0xF601F7EE = 385,
	_CPED_CONFIG_FLAG_0xA91350FC = 386,
	_CPED_CONFIG_FLAG_0x3AB23B96 = 387,
	CPED_CONFIG_FLAG_IsClimbingLadder = 388,
	CPED_CONFIG_FLAG_HasBareFeet = 389,
	CPED_CONFIG_FLAG_UNUSED_REPLACE_ME_2 = 390,
	CPED_CONFIG_FLAG_GoOnWithoutVehicleIfItIsUnableToGetBackToRoad = 391,
	CPED_CONFIG_FLAG_BlockDroppingHealthSnacksOnDeath = 392,
	_CPED_CONFIG_FLAG_0xC11D3E8F = 393,
	CPED_CONFIG_FLAG_ForceThreatResponseToNonFriendToFriendMeleeActions = 394,
	CPED_CONFIG_FLAG_DontRespondToRandomPedsDamage = 395,
	CPED_CONFIG_FLAG_AllowContinuousThreatResponseWantedLevelUpdates = 396,
	CPED_CONFIG_FLAG_KeepTargetLossResponseOnCleanup = 397,
	CPED_CONFIG_FLAG_PlayersDontDragMeOutOfCar = 398,
	CPED_CONFIG_FLAG_BroadcastRepondedToThreatWhenGoingToPointShooting = 399,
	CPED_CONFIG_FLAG_IgnorePedTypeForIsFriendlyWith = 400,
	CPED_CONFIG_FLAG_TreatNonFriendlyAsHateWhenInCombat = 401,
	CPED_CONFIG_FLAG_DontLeaveVehicleIfLeaderNotInVehicle = 402,
	_CPED_CONFIG_FLAG_0x5E5B9591 = 403,
	CPED_CONFIG_FLAG_AllowMeleeReactionIfMeleeProofIsOn = 404,
	_CPED_CONFIG_FLAG_0x77840177 = 405,
	_CPED_CONFIG_FLAG_0x1C7ACAC4 = 406,
	CPED_CONFIG_FLAG_UseNormalExplosionDamageWhenBlownUpInVehicle = 407,
	CPED_CONFIG_FLAG_DisableHomingMissileLockForVehiclePedInside = 408,
	CPED_CONFIG_FLAG_DisableTakeOffScubaGear = 409,
	CPED_CONFIG_FLAG_IgnoreMeleeFistWeaponDamageMult = 410,
	CPED_CONFIG_FLAG_LawPedsCanFleeFromNonWantedPlayer = 411,
	CPED_CONFIG_FLAG_ForceBlipSecurityPedsIfPlayerIsWanted = 412,
	CPED_CONFIG_FLAG_IsHolsteringWeapon = 413,
	CPED_CONFIG_FLAG_UseGoToPointForScenarioNavigation = 414,
	CPED_CONFIG_FLAG_DontClearLocalPassengersWantedLevel = 415,
	CPED_CONFIG_FLAG_BlockAutoSwapOnWeaponPickups = 416,
	CPED_CONFIG_FLAG_ThisPedIsATargetPriorityForAI = 417,
	CPED_CONFIG_FLAG_IsSwitchingHelmetVisor = 418,
	CPED_CONFIG_FLAG_ForceHelmetVisorSwitch = 419,
	_CPED_CONFIG_FLAG_0xCFF5F6DE = 420,
	CPED_CONFIG_FLAG_UseOverrideFootstepPtFx = 421,
	CPED_CONFIG_FLAG_DisableVehicleCombat = 422,
	_CPED_CONFIG_FLAG_0xFE401D26 = 423,
	CPED_CONFIG_FLAG_FallsLikeAircraft = 424,
	_CPED_CONFIG_FLAG_0x2B42AE82 = 425,
	CPED_CONFIG_FLAG_UseLockpickVehicleEntryAnimations = 426,
	CPED_CONFIG_FLAG_IgnoreInteriorCheckForSprinting = 427,
	CPED_CONFIG_FLAG_SwatHeliSpawnWithinLastSpottedLocation = 428,
	CPED_CONFIG_FLAG_DisableStartEngine = 429,
	CPED_CONFIG_FLAG_IgnoreBeingOnFire = 430,
	CPED_CONFIG_FLAG_DisableTurretOrRearSeatPreference = 431,
	CPED_CONFIG_FLAG_DisableWantedHelicopterSpawning = 432,
	CPED_CONFIG_FLAG_UseTargetPerceptionForCreatingAimedAtEvents = 433,
	CPED_CONFIG_FLAG_DisableHomingMissileLockon = 434,
	CPED_CONFIG_FLAG_ForceIgnoreMaxMeleeActiveSupportCombatants = 435,
	CPED_CONFIG_FLAG_StayInDefensiveAreaWhenInVehicle = 436,
	CPED_CONFIG_FLAG_DontShoutTargetPosition = 437,
	CPED_CONFIG_FLAG_DisableHelmetArmor = 438,
	_CPED_CONFIG_FLAG_0xCB7F3A1E = 439,
	_CPED_CONFIG_FLAG_0x50178878 = 440,
	CPED_CONFIG_FLAG_PreventVehExitDueToInvalidWeapon = 441,
	CPED_CONFIG_FLAG_IgnoreNetSessionFriendlyFireCheckForAllowDamage = 442,
	CPED_CONFIG_FLAG_DontLeaveCombatIfTargetPlayerIsAttackedByPolice = 443,
	CPED_CONFIG_FLAG_CheckLockedBeforeWarp = 444,
	CPED_CONFIG_FLAG_DontShuffleInVehicleToMakeRoom = 445,
	CPED_CONFIG_FLAG_GiveWeaponOnGetup = 446,
	CPED_CONFIG_FLAG_DontHitVehicleWithProjectiles = 447,
	CPED_CONFIG_FLAG_DisableForcedEntryForOpenVehiclesFromTryLockedDoor = 448,
	CPED_CONFIG_FLAG_FiresDummyRockets = 449,
	CPED_CONFIG_FLAG_PedIsArresting = 450,
	CPED_CONFIG_FLAG_IsDecoyPed = 451,
	CPED_CONFIG_FLAG_HasEstablishedDecoy = 452,
	CPED_CONFIG_FLAG_BlockDispatchedHelicoptersFromLanding = 453,
	CPED_CONFIG_FLAG_DontCryForHelpOnStun = 454,
	_CPED_CONFIG_FLAG_0xB68D3EAB = 455,
	CPED_CONFIG_FLAG_CanBeIncapacitated = 456,
	_CPED_CONFIG_FLAG_0x4BD5EBAD = 457,
	CPED_CONFIG_FLAG_DontChangeTargetFromMelee = 458,
};

---

### SET_PED_RESET_FLAG

**Hash:** `0xC1E8A365BF3B29F2` | **JHash:** `0xCFF6FF66` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_RESET_FLAG(Ped ped, int flagId, BOOL doReset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_RESET_FLAG, ped, flagId, doReset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flagId` | `int` | `int` | |
| `doReset` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** PED::SET_PED_RESET_FLAG(PLAYER::PLAYER_PED_ID(), 240, 1);
Known values:
PRF_PreventGoingIntoStillInVehicleState = 236 *(fanatic2.c)*

---

### GET_PED_CONFIG_FLAG

**Hash:** `0x7EE53118C892B513` | **JHash:** `0xABE98267` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_CONFIG_FLAG(Ped ped, int flagId, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_CONFIG_FLAG, ped, flagId, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flagId` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** See SET_PED_CONFIG_FLAG

---

### GET_PED_RESET_FLAG

**Hash:** `0xAF9E59B1B1FBF2A0` | **JHash:** `0x2FC10D11` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_RESET_FLAG(Ped ped, int flagId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_RESET_FLAG, ped, flagId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flagId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_GROUP_MEMBER_PASSENGER_INDEX

**Hash:** `0x0BDDB8D9EC6BCF3C` | **JHash:** `0x2AB3670B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GROUP_MEMBER_PASSENGER_INDEX(Ped ped, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GROUP_MEMBER_PASSENGER_INDEX, ped, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_EVASIVE_DIVE

**Hash:** `0x6B7A646C242A7059` | **JHash:** `0x542FEB4D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_EVASIVE_DIVE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_EVASIVE_DIVE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_EVASIVE_DIVING

**Hash:** `0x414641C26E105898` | **JHash:** `0xD82829DC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_EVASIVE_DIVING(Ped ped, Entity* evadingEntity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_EVASIVE_DIVING, ped, evadingEntity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `evadingEntity` | `Entity*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Presumably returns the Entity that the Ped is currently diving out of the way of.

var num3;
    if (PED::IS_PED_EVASIVE_DIVING(A_0, &num3) != 0)
        if (ENTITY::IS_ENTITY_A_VEHICLE(num3) != 0)

---

### SET_PED_SHOOTS_AT_COORD

**Hash:** `0x96A05E4FB321B1BA` | **JHash:** `0xFD64EAE5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_SHOOTS_AT_COORD(Ped ped, float x, float y, float z, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_SHOOTS_AT_COORD, ped, x, y, z, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_MODEL_IS_SUPPRESSED

**Hash:** `0xE163A4BCE4DE6F11` | **JHash:** `0x7820CA43` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MODEL_IS_SUPPRESSED(Hash modelHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MODEL_IS_SUPPRESSED, modelHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of peds by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/peds.json

---

### STOP_ANY_PED_MODEL_BEING_SUPPRESSED

**Hash:** `0xB47BD05FA66B40CF` | **JHash:** `0x5AD7DC55` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ANY_PED_MODEL_BEING_SUPPRESSED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ANY_PED_MODEL_BEING_SUPPRESSED);
```

**返回值:** `void` → `void`

---

### SET_PED_CAN_BE_TARGETED_WHEN_INJURED

**Hash:** `0x638C03B0F9878F57` | **JHash:** `0x6FD9A7CD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_BE_TARGETED_WHEN_INJURED(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_BE_TARGETED_WHEN_INJURED, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_GENERATES_DEAD_BODY_EVENTS

**Hash:** `0x7FB17BA2E7DECA5B` | **JHash:** `0xE9B97A2B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GENERATES_DEAD_BODY_EVENTS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GENERATES_DEAD_BODY_EVENTS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### BLOCK_PED_FROM_GENERATING_DEAD_BODY_EVENTS_WHEN_DEAD

**Hash:** `0xE43A13C9E4CCCBCF` | **JHash:** `0xFF1F6AEB` | **Build:** `323`

**C++ 原型:**
```cpp
void BLOCK_PED_FROM_GENERATING_DEAD_BODY_EVENTS_WHEN_DEAD(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BLOCK_PED_FROM_GENERATING_DEAD_BODY_EVENTS_WHEN_DEAD, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_WILL_ONLY_ATTACK_WANTED_PLAYER

**Hash:** `0x3E9679C1DFCF422C` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PED_WILL_ONLY_ATTACK_WANTED_PLAYER(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WILL_ONLY_ATTACK_WANTED_PLAYER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_RAGDOLL_FROM_PLAYER_IMPACT

**Hash:** `0xDF993EE5E90ABA25` | **JHash:** `0xE9BD733A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_RAGDOLL_FROM_PLAYER_IMPACT(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_RAGDOLL_FROM_PLAYER_IMPACT, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GIVE_PED_HELMET

**Hash:** `0x54C7C4A94367717E` | **JHash:** `0x1862A461` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_PED_HELMET(Ped ped, BOOL cannotRemove, int helmetFlag, int textureIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_PED_HELMET, ped, cannotRemove, helmetFlag, textureIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `cannotRemove` | `BOOL` | `bool` | |
| `helmetFlag` | `int` | `int` | |
| `textureIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** PoliceMotorcycleHelmet   1024    
RegularMotorcycleHelmet   4096    
FiremanHelmet 16384   
PilotHeadset  32768   
PilotHelmet   65536
--
p2 is generally 4096 or 16384 in the scripts. p1 varies between 1 and 0.

---

### REMOVE_PED_HELMET

**Hash:** `0xA7B2458D0AD6DED8` | **JHash:** `0x2086B1F0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PED_HELMET(Ped ped, BOOL instantly)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PED_HELMET, ped, instantly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `instantly` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_TAKING_OFF_HELMET

**Hash:** `0x14590DDBEDB1EC85` | **JHash:** `0xA1008E0E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_TAKING_OFF_HELMET(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_TAKING_OFF_HELMET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_HELMET

**Hash:** `0x560A43136EB58105` | **JHash:** `0xED366E53` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HELMET(Ped ped, BOOL canWearHelmet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HELMET, ped, canWearHelmet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `canWearHelmet` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_HELMET_FLAG

**Hash:** `0xC0E78D5C2CE3EB25` | **JHash:** `0x12677780` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HELMET_FLAG(Ped ped, int helmetFlag)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HELMET_FLAG, ped, helmetFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `helmetFlag` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_HELMET_PROP_INDEX

**Hash:** `0x26D83693ED99291C` | **JHash:** `0xA316D13F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HELMET_PROP_INDEX(Ped ped, int propIndex, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HELMET_PROP_INDEX, ped, propIndex, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `propIndex` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** List of component/props ID
gtaxscripting.blogspot.com/2016/04/gta-v-peds-component-and-props.html

---

### SET_PED_HELMET_VISOR_PROP_INDICES

**Hash:** `0x3F7325574E41B44D` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_PED_HELMET_VISOR_PROP_INDICES(Ped ped, BOOL p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HELMET_VISOR_PROP_INDICES, ped, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_HELMET_VISOR_UP

**Hash:** `0xB9496CE47546DB2C` | **Build:** `791`

**C++ 原型:**
```cpp
BOOL IS_PED_HELMET_VISOR_UP(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HELMET_VISOR_UP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_HELMET_TEXTURE_INDEX

**Hash:** `0xF1550C4BD22582E2` | **JHash:** `0x5F6C3328` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HELMET_TEXTURE_INDEX(Ped ped, int textureIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HELMET_TEXTURE_INDEX, ped, textureIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `textureIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_WEARING_HELMET

**Hash:** `0xF33BDFE19B309B19` | **JHash:** `0x0D680D49` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_WEARING_HELMET(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_WEARING_HELMET, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the ped passed through the parenthesis is wearing a helmet.

---

### CLEAR_PED_STORED_HAT_PROP

**Hash:** `0x687C0B594907D2E8` | **JHash:** `0x24A1284E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_STORED_HAT_PROP(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_STORED_HAT_PROP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_HELMET_STORED_HAT_PROP_INDEX

**Hash:** `0x451294E859ECC018` | **JHash:** `0x8A3A3116` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HELMET_STORED_HAT_PROP_INDEX(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HELMET_STORED_HAT_PROP_INDEX, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### GET_PED_HELMET_STORED_HAT_TEX_INDEX

**Hash:** `0x9D728C1E12BF5518` | **JHash:** `0x74EB662D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_HELMET_STORED_HAT_TEX_INDEX(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_HELMET_STORED_HAT_TEX_INDEX, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### IS_CURRENT_HEAD_PROP_A_HELMET

**Hash:** `0xF2385935BFFD4D92` | **JHash:** `0xFFF149FE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CURRENT_HEAD_PROP_A_HELMET(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CURRENT_HEAD_PROP_A_HELMET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_TO_LOAD_COVER

**Hash:** `0x332B562EEDA62399` | **JHash:** `0xCF94BA97` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_TO_LOAD_COVER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_TO_LOAD_COVER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_COWER_IN_COVER

**Hash:** `0xCB7553CDCEF4A735` | **JHash:** `0x5194658B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_COWER_IN_COVER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_COWER_IN_COVER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** It simply makes the said ped to cower behind cover object(wall, desk, car)

Peds flee attributes must be set to not to flee, first. Else, most of the peds, will just flee from gunshot sounds or any other panic situations.

---

### SET_PED_CAN_PEEK_IN_COVER

**Hash:** `0xC514825C507E3736` | **JHash:** `0xC1DAE216` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PEEK_IN_COVER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PEEK_IN_COVER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PLAYS_HEAD_ON_HORN_ANIM_WHEN_DIES_IN_VEHICLE

**Hash:** `0x94D94BF1A75AED3D` | **JHash:** `0x7C563CD2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PLAYS_HEAD_ON_HORN_ANIM_WHEN_DIES_IN_VEHICLE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PLAYS_HEAD_ON_HORN_ANIM_WHEN_DIES_IN_VEHICLE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### SET_PED_LEG_IK_MODE

**Hash:** `0xC396F5B86FF9FEBD` | **JHash:** `0xFDDB042E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_LEG_IK_MODE(Ped ped, int mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_LEG_IK_MODE, ped, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `mode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** "IK" stands for "Inverse kinematics." I assume this has something to do with how the ped uses his legs to balance. In the scripts, the second parameter is always an int with a value of 2, 0, or sometimes 1

---

### SET_PED_MOTION_BLUR

**Hash:** `0x0A986918B102B448` | **JHash:** `0xA211A128` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MOTION_BLUR(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOTION_BLUR, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_SWITCH_WEAPON

**Hash:** `0xED7F7EFE9FABF340` | **JHash:** `0xB5F8BA28` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_SWITCH_WEAPON(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_SWITCH_WEAPON, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_DIES_INSTANTLY_IN_WATER

**Hash:** `0xEEB64139BA29A7CF` | **JHash:** `0xFE2554FC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DIES_INSTANTLY_IN_WATER(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DIES_INSTANTLY_IN_WATER, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_LADDER_CLIMB_INPUT_STATE

**Hash:** `0x1A330D297AAC6BC1` | **JHash:** `0x77BB7CB8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LADDER_CLIMB_INPUT_STATE(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LADDER_CLIMB_INPUT_STATE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Only appears in lamar1 script.

---

### STOP_PED_WEAPON_FIRING_WHEN_DROPPED

**Hash:** `0xC158D28142A34608` | **JHash:** `0x4AC3421E` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PED_WEAPON_FIRING_WHEN_DROPPED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PED_WEAPON_FIRING_WHEN_DROPPED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_SCRIPTED_ANIM_SEAT_OFFSET

**Hash:** `0x5917BBA32D06C230` | **JHash:** `0x7CEFFA45` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPTED_ANIM_SEAT_OFFSET(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPTED_ANIM_SEAT_OFFSET, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_COMBAT_MOVEMENT

**Hash:** `0x4D9CA1009AFBD057` | **JHash:** `0x12E62F9E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COMBAT_MOVEMENT(Ped ped, int combatMovement)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COMBAT_MOVEMENT, ped, combatMovement);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `combatMovement` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eCombatMovement // 0x4F456B61
{
	CM_Stationary,
	CM_Defensive,
	CM_WillAdvance,
	CM_WillRetreat
};

---

### GET_PED_COMBAT_MOVEMENT

**Hash:** `0xDEA92412FCAEB3F5` | **JHash:** `0xF3E7730E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_COMBAT_MOVEMENT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_COMBAT_MOVEMENT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** See SET_PED_COMBAT_MOVEMENT

---

### SET_PED_COMBAT_ABILITY

**Hash:** `0xC7622C0D36B2FDA8` | **JHash:** `0x6C23D329` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COMBAT_ABILITY(Ped ped, int abilityLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COMBAT_ABILITY, ped, abilityLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `abilityLevel` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eCombatAbility // 0xE793438C
{
	CA_Poor,
	CA_Average,
	CA_Professional,
	CA_NumTypes
};

---

### SET_PED_COMBAT_RANGE

**Hash:** `0x3C606747B23E497B` | **JHash:** `0x8818A959` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COMBAT_RANGE(Ped ped, int combatRange)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COMBAT_RANGE, ped, combatRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `combatRange` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eCombatRange // 0xB69160F5
{
	CR_Near,
	CR_Medium,
	CR_Far,
	CR_VeryFar,
	CR_NumRanges
};

---

### GET_PED_COMBAT_RANGE

**Hash:** `0xF9D9F7F2DB8E2FA0` | **JHash:** `0x9B9B7163` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_COMBAT_RANGE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_COMBAT_RANGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** See SET_PED_COMBAT_RANGE

---

### SET_PED_COMBAT_ATTRIBUTES

**Hash:** `0x9F7794730795E019` | **JHash:** `0x81D64248` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COMBAT_ATTRIBUTES(Ped ped, int attributeId, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COMBAT_ATTRIBUTES, ped, attributeId, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `attributeId` | `int` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enum eCombatAttributes // 0x0E8E7201
{
	BF_CanUseCover = 0,
	BF_CanUseVehicles = 1,
	BF_CanDoDrivebys = 2,
	BF_CanLeaveVehicle = 3,
	BF_CanUseDynamicStrafeDecisions = 4,
	BF_AlwaysFight = 5,
	BF_0x66BB9FCC = 6,
	BF_0x6837DA41 = 7,
	BF_0xB4A13A5A = 8,
	BF_0xEE326AAD = 9,
	BF_0x7DF2CCFA = 10,
	BF_0x0036D422 = 11,
	BF_BlindFireWhenInCover = 12,
	BF_Aggressive = 13,
	BF_CanInvestigate = 14,
	BF_HasRadio = 15,
	BF_0x6BDE28D1 = 16,
	BF_AlwaysFlee = 17,
	BF_0x7852797D = 18,
	BF_0x33497B95 = 19,
	BF_CanTauntInVehicle = 20,
	BF_CanChaseTargetOnFoot = 21,
	BF_WillDragInjuredPedsToSafety = 22,
	BF_0xCD7168B8 = 23,
	BF_UseProximityFiringRate = 24,
	BF_0x48F914F8 = 25,
	BF_0x2EA543D0 = 26,
	BF_PerfectAccuracy = 27,
	BF_CanUseFrustratedAdvance = 28,
	BF_0x3D131AC1 = 29,
	BF_0x3AD95F27 = 30,
	BF_MaintainMinDistanceToTarget = 31,
	BF_0xEAD68AD2 = 32,
	BF_0xA206C2E0 = 33,
	BF_CanUsePeekingVariations = 34,
	BF_0xA5715184 = 35,
	BF_0xD5265533 = 36,
	BF_0x2B84C2BF = 37,
	BF_DisableBulletReactions = 38,
	BF_CanBust = 39,
	BF_0xAA525726 = 40,
	BF_CanCommandeerVehicles = 41,
	BF_CanFlank = 42,
	BF_SwitchToAdvanceIfCantFindCover = 43,
	BF_SwitchToDefensiveIfInCover = 44,
	BF_0xEB4786A0 = 45,
	BF_CanFightArmedPedsWhenNotArmed = 46,
	BF_0xA08E9402 = 47,
	BF_0x952EAD7D = 48,
	BF_UseEnemyAccuracyScaling = 49,
	BF_CanCharge = 50,
	BF_0xDA8C2BD3 = 51,
	BF_0x6562F017 = 52,
	BF_0xA2C3D53B = 53,
	BF_AlwaysEquipBestWeapon = 54,
	BF_CanSeeUnderwaterPeds = 55,
	BF_0xF619486B = 56,
	BF_0x61EB63A3 = 57,
	BF_DisableFleeFromCombat = 58,
	BF_0x8976D12B = 59,
	BF_CanThrowSmokeGrenade = 60,
	BF_NonMissionPedsFleeFromThisPedUnlessArmed = 61,
	BF_0x5452A10C = 62,
	BF_FleesFromInvincibleOpponents = 63,
	BF_DisableBlockFromPursueDuringVehicleChase = 64,
	BF_DisableSpinOutDuringVehicleChase = 65,
	BF_DisableCruiseInFrontDuringBlockDuringVehicleChase = 66,
	BF_0x0B404731 = 67,
	BF_DisableReactToBuddyShot = 68,
	BF_0x7FFD6AEB = 69,
	BF_0x51F4AEF8 = 70,
	BF_PermitChargeBeyondDefensiveArea = 71,
	BF_0x63E0A8E2 = 72,
	BF_0xDF974436 = 73,
	BF_0x556C080B = 74,
	BF_0xA4D50035 = 75,
	BF_SetDisableShoutTargetPositionOnCombatStart = 76,
	BF_DisableRespondedToThreatBroadcast = 77,
	BF_0xCBB01765 = 78,
	BF_0x4F862ED4 = 79,
	BF_0xEF9C7C40 = 80,
	BF_0xE51B494F = 81,
	BF_0x054D0199 = 82,
	BF_0xD36BCE94 = 83,
	BF_0xFB11F690 = 84,
	BF_0xD208A9AD = 85,
	BF_AllowDogFighting = 86,
	BF_0x07A6E531 = 87,
	BF_0x34F9317B = 88,
	BF_0x4240F5A9 = 89,
	BF_0xEE129DBD = 90,
	BF_0x053AEAD9 = 91
};

---

### SET_PED_TARGET_LOSS_RESPONSE

**Hash:** `0x0703B9079823DA4A` | **JHash:** `0xCFA613FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_TARGET_LOSS_RESPONSE(Ped ped, int responseType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_TARGET_LOSS_RESPONSE, ped, responseType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `responseType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eTargetLossResponseType
{
	TLR_ExitTask,
	TLR_NeverLoseTarget,
	TLR_SearchForTarget
};

---

### IS_PED_PERFORMING_MELEE_ACTION

**Hash:** `0xDCCA191DF9980FD7` | **JHash:** `0x139C0875` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PERFORMING_MELEE_ACTION(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PERFORMING_MELEE_ACTION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_PERFORMING_STEALTH_KILL

**Hash:** `0xFD4CCDBCC59941B7` | **JHash:** `0x9ADD7B21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PERFORMING_STEALTH_KILL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PERFORMING_STEALTH_KILL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_PERFORMING_A_COUNTER_ATTACK

**Hash:** `0xEBD0EDBA5BE957CF` | **JHash:** `0x9BE7C860` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PERFORMING_A_COUNTER_ATTACK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PERFORMING_A_COUNTER_ATTACK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BEING_STEALTH_KILLED

**Hash:** `0x863B23EFDE9C5DF2` | **JHash:** `0xD044C8AF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BEING_STEALTH_KILLED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BEING_STEALTH_KILLED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_MELEE_TARGET_FOR_PED

**Hash:** `0x18A3E9EE1297FD39` | **JHash:** `0xAFEC26A4` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_MELEE_TARGET_FOR_PED(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MELEE_TARGET_FOR_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Ped` → `int`

---

### WAS_PED_KILLED_BY_STEALTH

**Hash:** `0xF9800AA1A771B000` | **JHash:** `0x2EA4B54E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_PED_KILLED_BY_STEALTH(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_PED_KILLED_BY_STEALTH, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### WAS_PED_KILLED_BY_TAKEDOWN

**Hash:** `0x7F08E26039C7347C` | **JHash:** `0xBDD3CE69` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_PED_KILLED_BY_TAKEDOWN(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_PED_KILLED_BY_TAKEDOWN, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### WAS_PED_KNOCKED_OUT

**Hash:** `0x61767F73EACEED21` | **JHash:** `0x3993092B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAS_PED_KNOCKED_OUT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAS_PED_KNOCKED_OUT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_FLEE_ATTRIBUTES

**Hash:** `0x70A2D1137C8ED7C9` | **JHash:** `0xA717A875` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_FLEE_ATTRIBUTES(Ped ped, int attributeFlags, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_FLEE_ATTRIBUTES, ped, attributeFlags, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `attributeFlags` | `int` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** bit 1 (0x2) = use vehicle
bit 15 (0x8000) = force cower

---

### SET_PED_COWER_HASH

**Hash:** `0xA549131166868ED3` | **JHash:** `0x16F30DF4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_COWER_HASH(Ped ped, const char* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_COWER_HASH, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** p1: Only "CODE_HUMAN_STAND_COWER" found in the b617d scripts.

---

### SET_PED_STEERS_AROUND_DEAD_BODIES

**Hash:** `0x2016C603D6B8987C` | **JHash:** `0xA6F2C057` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEERS_AROUND_DEAD_BODIES(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEERS_AROUND_DEAD_BODIES, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_STEERS_AROUND_PEDS

**Hash:** `0x46F2193B3AD1D891` | **JHash:** `0x797CAE4F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEERS_AROUND_PEDS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEERS_AROUND_PEDS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_STEERS_AROUND_OBJECTS

**Hash:** `0x1509C089ADC208BF` | **JHash:** `0x3BD9B0A6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEERS_AROUND_OBJECTS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEERS_AROUND_OBJECTS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_STEERS_AROUND_VEHICLES

**Hash:** `0xEB6FB9D48DDE23EC` | **JHash:** `0x533C0651` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEERS_AROUND_VEHICLES(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEERS_AROUND_VEHICLES, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_IS_AVOIDED_BY_OTHERS

**Hash:** `0xA9B61A329BFDCBEA` | **JHash:** `0x2276DE0D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_IS_AVOIDED_BY_OTHERS(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_IS_AVOIDED_BY_OTHERS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_INCREASED_AVOIDANCE_RADIUS

**Hash:** `0x570389D1C3DE3C6B` | **JHash:** `0x59C52BE6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INCREASED_AVOIDANCE_RADIUS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INCREASED_AVOIDANCE_RADIUS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_BLOCKS_PATHING_WHEN_DEAD

**Hash:** `0x576594E8D64375E2` | **JHash:** `0x1D87DDC1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_BLOCKS_PATHING_WHEN_DEAD(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_BLOCKS_PATHING_WHEN_DEAD, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_NO_TIME_DELAY_BEFORE_SHOT

**Hash:** `0xA52D5247A4227E14` | **JHash:** `0xB52BA5F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_NO_TIME_DELAY_BEFORE_SHOT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_NO_TIME_DELAY_BEFORE_SHOT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_ANY_PED_NEAR_POINT

**Hash:** `0x083961498679DC9F` | **JHash:** `0xFBD9B050` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_PED_NEAR_POINT(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_PED_NEAR_POINT, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### FORCE_PED_AI_AND_ANIMATION_UPDATE

**Hash:** `0x2208438012482A1A` | **JHash:** `0x187B9070` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_PED_AI_AND_ANIMATION_UPDATE(Ped ped, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PED_AI_AND_ANIMATION_UPDATE, ped, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PED_HEADING_TOWARDS_POSITION

**Hash:** `0xFCF37A457CB96DC0` | **JHash:** `0x45037B9B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_HEADING_TOWARDS_POSITION(Ped ped, float x, float y, float z, float p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_HEADING_TOWARDS_POSITION, ped, x, y, z, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_PED_VISIBILITY_TRACKING

**Hash:** `0x7D7A2E43E74E2EB8` | **JHash:** `0x840D24D3` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_PED_VISIBILITY_TRACKING(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_PED_VISIBILITY_TRACKING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### REQUEST_PED_VEHICLE_VISIBILITY_TRACKING

**Hash:** `0x2BC338A7B21F4608` | **JHash:** `0x31C31DAA` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_PED_VEHICLE_VISIBILITY_TRACKING(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_PED_VEHICLE_VISIBILITY_TRACKING, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_PED_RESTRICTED_VEHICLE_VISIBILITY_TRACKING

**Hash:** `0xCD018C591F94CB43` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_PED_RESTRICTED_VEHICLE_VISIBILITY_TRACKING(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_PED_RESTRICTED_VEHICLE_VISIBILITY_TRACKING, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_PED_USE_SMALL_BBOX_VISIBILITY_TRACKING

**Hash:** `0x75BA1CB3B7D40CAF` | **JHash:** `0x9194DB71` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_PED_USE_SMALL_BBOX_VISIBILITY_TRACKING(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_PED_USE_SMALL_BBOX_VISIBILITY_TRACKING, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_TRACKED_PED_VISIBLE

**Hash:** `0x91C8E617F64188AC` | **JHash:** `0x33248CC1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TRACKED_PED_VISIBLE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TRACKED_PED_VISIBLE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** returns whether or not a ped is visible within your FOV, not this check auto's to false after a certain distance.


Target needs to be tracked.. won't work otherwise.

---

### GET_TRACKED_PED_PIXELCOUNT

**Hash:** `0x511F1A683387C7E2` | **JHash:** `0x5B1B70AA` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TRACKED_PED_PIXELCOUNT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TRACKED_PED_PIXELCOUNT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### IS_PED_TRACKED

**Hash:** `0x4C5E1F087CD10BB7` | **JHash:** `0x7EB613D9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_TRACKED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_TRACKED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### HAS_PED_RECEIVED_EVENT

**Hash:** `0x8507BCB710FA6DC0` | **JHash:** `0xECD73DB0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_RECEIVED_EVENT(Ped ped, int eventId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_RECEIVED_EVENT, ped, eventId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `eventId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_PED_SEE_HATED_PED

**Hash:** `0x6CD5A433374D4CFB` | **JHash:** `0x74A0F291` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PED_SEE_HATED_PED(Ped ped1, Ped ped2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_SEE_HATED_PED, ped1, ped2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped1` | `Ped` | `int` | |
| `ped2` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_PED_SHUFFLE_TO_OR_FROM_TURRET_SEAT

**Hash:** `0x9C6A6C19B6C0C496` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PED_SHUFFLE_TO_OR_FROM_TURRET_SEAT(Ped ped, int* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_SHUFFLE_TO_OR_FROM_TURRET_SEAT, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### CAN_PED_SHUFFLE_TO_OR_FROM_EXTRA_SEAT

**Hash:** `0x2DFC81C9B9608549` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL CAN_PED_SHUFFLE_TO_OR_FROM_EXTRA_SEAT(Ped ped, int* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_SHUFFLE_TO_OR_FROM_EXTRA_SEAT, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### GET_PED_BONE_INDEX

**Hash:** `0x3F428D08BE5AAE31` | **JHash:** `0x259C6BA2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_BONE_INDEX(Ped ped, int boneId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_BONE_INDEX, ped, boneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `boneId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** no bone= -1

boneIds:
        SKEL_ROOT = 0x0,
   SKEL_Pelvis = 0x2e28,
 SKEL_L_Thigh = 0xe39f,
    SKEL_L_Calf = 0xf9bb,
 SKEL_L_Foot = 0x3779,
 SKEL_L_Toe0 = 0x83c,
  IK_L_Foot = 0xfedd,
   PH_L_Foot = 0xe175,
   MH_L_Knee = 0xb3fe,
   SKEL_R_Thigh = 0xca72,
    SKEL_R_Calf = 0x9000,
 SKEL_R_Foot = 0xcc4d,
 SKEL_R_Toe0 = 0x512d,
 IK_R_Foot = 0x8aae,
   PH_R_Foot = 0x60e6,
   MH_R_Knee = 0x3fcf,
   RB_L_ThighRoll = 0x5c57,
  RB_R_ThighRoll = 0x192a,
  SKEL_Spine_Root = 0xe0fd,
 SKEL_Spine0 = 0x5c01,
 SKEL_Spine1 = 0x60f0,
 SKEL_Spine2 = 0x60f1,
 SKEL_Spine3 = 0x60f2,
 SKEL_L_Clavicle = 0xfcd9,
 SKEL_L_UpperArm = 0xb1c5,
 SKEL_L_Forearm = 0xeeeb,
  SKEL_L_Hand = 0x49d9,
 SKEL_L_Finger00 = 0x67f2,
 SKEL_L_Finger01 = 0xff9,
  SKEL_L_Finger02 = 0xffa,
  SKEL_L_Finger10 = 0x67f3,
 SKEL_L_Finger11 = 0x1049,
 SKEL_L_Finger12 = 0x104a,
 SKEL_L_Finger20 = 0x67f4,
 SKEL_L_Finger21 = 0x1059,
 SKEL_L_Finger22 = 0x105a,
 SKEL_L_Finger30 = 0x67f5,
 SKEL_L_Finger31 = 0x1029,
 SKEL_L_Finger32 = 0x102a,
 SKEL_L_Finger40 = 0x67f6,
 SKEL_L_Finger41 = 0x1039,
 SKEL_L_Finger42 = 0x103a,
 PH_L_Hand = 0xeb95,
   IK_L_Hand = 0x8cbd,
   RB_L_ForeArmRoll = 0xee4f,
    RB_L_ArmRoll = 0x1470,
    MH_L_Elbow = 0x58b7,
  SKEL_R_Clavicle = 0x29d2,
 SKEL_R_UpperArm = 0x9d4d,
 SKEL_R_Forearm = 0x6e5c,
  SKEL_R_Hand = 0xdead,
 SKEL_R_Finger00 = 0xe5f2,
 SKEL_R_Finger01 = 0xfa10,
 SKEL_R_Finger02 = 0xfa11,
 SKEL_R_Finger10 = 0xe5f3,
 SKEL_R_Finger11 = 0xfa60,
 SKEL_R_Finger12 = 0xfa61,
 SKEL_R_Finger20 = 0xe5f4,
 SKEL_R_Finger21 = 0xfa70,
 SKEL_R_Finger22 = 0xfa71,
 SKEL_R_Finger30 = 0xe5f5,
 SKEL_R_Finger31 = 0xfa40,
 SKEL_R_Finger32 = 0xfa41,
 SKEL_R_Finger40 = 0xe5f6,
 SKEL_R_Finger41 = 0xfa50,
 SKEL_R_Finger42 = 0xfa51,
 PH_R_Hand = 0x6f06,
   IK_R_Hand = 0x188e,
   RB_R_ForeArmRoll = 0xab22,
    RB_R_ArmRoll = 0x90ff,
    MH_R_Elbow = 0xbb0,
   SKEL_Neck_1 = 0x9995,
 SKEL_Head = 0x796e,
   IK_Head = 0x322c,
 FACIAL_facialRoot = 0xfe2c,
   FB_L_Brow_Out_000 = 0xe3db,
   FB_L_Lid_Upper_000 = 0xb2b6,
  FB_L_Eye_000 = 0x62ac,
    FB_L_CheekBone_000 = 0x542e,
  FB_L_Lip_Corner_000 = 0x74ac,
 FB_R_Lid_Upper_000 = 0xaa10,
  FB_R_Eye_000 = 0x6b52,
    FB_R_CheekBone_000 = 0x4b88,
  FB_R_Brow_Out_000 = 0x54c,
    FB_R_Lip_Corner_000 = 0x2ba6,
 FB_Brow_Centre_000 = 0x9149,
  FB_UpperLipRoot_000 = 0x4ed2,
 FB_UpperLip_000 = 0xf18f,
 FB_L_Lip_Top_000 = 0x4f37,
    FB_R_Lip_Top_000 = 0x4537,
    FB_Jaw_000 = 0xb4a0,
  FB_LowerLipRoot_000 = 0x4324,
 FB_LowerLip_000 = 0x508f,
 FB_L_Lip_Bot_000 = 0xb93b,
    FB_R_Lip_Bot_000 = 0xc33b,
    FB_Tongue_000 = 0xb987,
   RB_Neck_1 = 0x8b93,
   IK_Root = 0xdd1c

---

### GET_PED_RAGDOLL_BONE_INDEX

**Hash:** `0x2057EF813397A772` | **JHash:** `0x849F0716` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_RAGDOLL_BONE_INDEX(Ped ped, int bone)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_RAGDOLL_BONE_INDEX, ped, bone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `bone` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_ENVEFF_SCALE

**Hash:** `0xBF29516833893561` | **JHash:** `0xFC1CFC27` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ENVEFF_SCALE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ENVEFF_SCALE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Values look to be between 0.0 and 1.0
From decompiled scripts: 0.0, 0.6, 0.65, 0.8, 1.0

You are correct, just looked in IDA it breaks from the function if it's less than 0.0f or greater than 1.0f.

---

### GET_PED_ENVEFF_SCALE

**Hash:** `0x9C14D30395A51A3C` | **JHash:** `0xA3421E39` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PED_ENVEFF_SCALE(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PED_ENVEFF_SCALE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### SET_ENABLE_PED_ENVEFF_SCALE

**Hash:** `0xD2C5AA0C0E8D0F1E` | **JHash:** `0xC70F4A84` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENABLE_PED_ENVEFF_SCALE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENABLE_PED_ENVEFF_SCALE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_ENVEFF_CPV_ADD

**Hash:** `0x110F526AB784111F` | **JHash:** `0x3B882533` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ENVEFF_CPV_ADD(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ENVEFF_CPV_ADD, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** In agency_heist3b.c4, its like this 90% of the time:

PED::SET_PED_ENVEFF_CPV_ADD(ped, 0.099);
PED::SET_PED_ENVEFF_SCALE(ped, 1.0);
PED::SET_PED_ENVEFF_CPV_ADD(ped, 87, 81, 68);
PED::SET_ENABLE_PED_ENVEFF_SCALE(ped, 1);

and its like this 10% of the time:

PED::SET_PED_ENVEFF_CPV_ADD(ped, 0.2);
PED::SET_PED_ENVEFF_SCALE(ped, 0.65);
PED::SET_PED_ENVEFF_COLOR_MODULATOR(ped, 74, 69, 60);
PED::SET_ENABLE_PED_ENVEFF_SCALE(ped, 1);

---

### SET_PED_ENVEFF_COLOR_MODULATOR

**Hash:** `0xD69411AA0CEBF9E9` | **JHash:** `0x87A0C174` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_ENVEFF_COLOR_MODULATOR(Ped ped, int p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ENVEFF_COLOR_MODULATOR, ped, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Something related to the environmental effects natives.
In the "agency_heist3b" script, p1 - p3 are always under 100 - usually they are {87, 81, 68}. If SET_PED_ENVEFF_SCALE is set to 0.65 (instead of the usual 1.0), they use {74, 69, 60}

---

### SET_PED_EMISSIVE_SCALE

**Hash:** `0x4E90D746056E273D` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_PED_EMISSIVE_SCALE(Ped ped, float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_EMISSIVE_SCALE, ped, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** intensity: 0.0f - 1.0f

This native sets the emissive intensity for the given ped. It is used for different 'glow' levels on illuminated clothing.

---

### GET_PED_EMISSIVE_SCALE

**Hash:** `0x1461B28A06717D68` | **Build:** `944`

**C++ 原型:**
```cpp
float GET_PED_EMISSIVE_SCALE(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PED_EMISSIVE_SCALE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

**说明:** Use 0x4E90D746056E273D to set the illuminated clothing glow intensity for a specific ped.
Returns a float between 0.0 and 1.0 representing the current illuminated clothing glow intensity.

---

### IS_PED_SHADER_READY

**Hash:** `0x81AA517FBBA05D39` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL IS_PED_SHADER_READY(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SHADER_READY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_ENABLE_CREW_EMBLEM

**Hash:** `0xE906EC930F5FE7C8` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_PED_ENABLE_CREW_EMBLEM(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_ENABLE_CREW_EMBLEM, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_RAGDOLL_BOUNDS_UPDATE

**Hash:** `0x1216E0BFA72CC703` | **JHash:** `0x7BD26837` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_RAGDOLL_BOUNDS_UPDATE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_RAGDOLL_BOUNDS_UPDATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### SET_PED_AO_BLOB_RENDERING

**Hash:** `0x2B5AA717A181FB4C` | **JHash:** `0x98E29ED0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AO_BLOB_RENDERING(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AO_BLOB_RENDERING, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enable/disable ped shadow (ambient occlusion). https://gfycat.com/thankfulesteemedgecko

---

### IS_PED_SHELTERED

**Hash:** `0xB8B52E498014F5B0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SHELTERED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SHELTERED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CREATE_SYNCHRONIZED_SCENE

**Hash:** `0x8C18E0F9080ADD73` | **JHash:** `0xFFDDF8FA` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_SYNCHRONIZED_SCENE(float x, float y, float z, float roll, float pitch, float yaw, int p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_SYNCHRONIZED_SCENE, x, y, z, roll, pitch, yaw, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `roll` | `float` | `float` | |
| `pitch` | `float` | `float` | |
| `yaw` | `float` | `float` | |
| `p6` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** p6 always 2 (but it doesnt seem to matter...)

roll and pitch 0
yaw to Ped.rotation

---

### CREATE_SYNCHRONIZED_SCENE_AT_MAP_OBJECT

**Hash:** `0x62EC273D00187DCA` | **JHash:** `0xF3876894` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_SYNCHRONIZED_SCENE_AT_MAP_OBJECT(float x, float y, float z, float radius, Hash object)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_SYNCHRONIZED_SCENE_AT_MAP_OBJECT, x, y, z, radius, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `object` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### IS_SYNCHRONIZED_SCENE_RUNNING

**Hash:** `0x25D39B935A038A26` | **JHash:** `0x57A282F1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SYNCHRONIZED_SCENE_RUNNING(int sceneId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SYNCHRONIZED_SCENE_RUNNING, sceneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if a synchronized scene is running

---

### SET_SYNCHRONIZED_SCENE_ORIGIN

**Hash:** `0x6ACF6B7225801CD7` | **JHash:** `0x2EC2A0B2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SYNCHRONIZED_SCENE_ORIGIN(int sceneID, float x, float y, float z, float roll, float pitch, float yaw, BOOL p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SYNCHRONIZED_SCENE_ORIGIN, sceneID, x, y, z, roll, pitch, yaw, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `roll` | `float` | `float` | |
| `pitch` | `float` | `float` | |
| `yaw` | `float` | `float` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SYNCHRONIZED_SCENE_PHASE

**Hash:** `0x734292F4F0ABF6D0` | **JHash:** `0xF5AB0D98` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SYNCHRONIZED_SCENE_PHASE(int sceneID, float phase)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SYNCHRONIZED_SCENE_PHASE, sceneID, phase);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `phase` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_SYNCHRONIZED_SCENE_PHASE

**Hash:** `0xE4A310B1D7FA73CC` | **JHash:** `0xB0B2C852` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SYNCHRONIZED_SCENE_PHASE(int sceneID)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SYNCHRONIZED_SCENE_PHASE, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `float` → `float`

---

### SET_SYNCHRONIZED_SCENE_RATE

**Hash:** `0xB6C49F8A5E295A5D` | **JHash:** `0xF10112FD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SYNCHRONIZED_SCENE_RATE(int sceneID, float rate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SYNCHRONIZED_SCENE_RATE, sceneID, rate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `rate` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_SYNCHRONIZED_SCENE_RATE

**Hash:** `0xD80932D577274D40` | **JHash:** `0x89365F0D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_SYNCHRONIZED_SCENE_RATE(int sceneID)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_SYNCHRONIZED_SCENE_RATE, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `float` → `float`

---

### SET_SYNCHRONIZED_SCENE_LOOPED

**Hash:** `0xD9A897A4C6C2974F` | **JHash:** `0x32ED9F82` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SYNCHRONIZED_SCENE_LOOPED(int sceneID, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SYNCHRONIZED_SCENE_LOOPED, sceneID, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_SYNCHRONIZED_SCENE_LOOPED

**Hash:** `0x62522002E0C391BA` | **JHash:** `0x47D87A84` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SYNCHRONIZED_SCENE_LOOPED(int sceneID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SYNCHRONIZED_SCENE_LOOPED, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME

**Hash:** `0x394B9CD12435C981` | **JHash:** `0x2DE48DA1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME(int sceneID, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME, sceneID, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME

**Hash:** `0x7F2F4F13AC5257EF` | **JHash:** `0x72CF2514` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME(int sceneID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SYNCHRONIZED_SCENE_HOLD_LAST_FRAME, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ATTACH_SYNCHRONIZED_SCENE_TO_ENTITY

**Hash:** `0x272E4723B56A3B96` | **JHash:** `0xE9BA6189` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_SYNCHRONIZED_SCENE_TO_ENTITY(int sceneID, Entity entity, int boneIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_SYNCHRONIZED_SCENE_TO_ENTITY, sceneID, entity, boneIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `boneIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DETACH_SYNCHRONIZED_SCENE

**Hash:** `0x6D38F1F04CBB37EA` | **JHash:** `0x52A1CAB2` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_SYNCHRONIZED_SCENE(int sceneID)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_SYNCHRONIZED_SCENE, sceneID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sceneID` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TAKE_OWNERSHIP_OF_SYNCHRONIZED_SCENE

**Hash:** `0xCD9CC7E200A52A6F` | **JHash:** `0xBF7F9035` | **Build:** `323`

**C++ 原型:**
```cpp
void TAKE_OWNERSHIP_OF_SYNCHRONIZED_SCENE(int scene)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TAKE_OWNERSHIP_OF_SYNCHRONIZED_SCENE, scene);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scene` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FORCE_PED_MOTION_STATE

**Hash:** `0xF28965D04F570DCA` | **JHash:** `0x164DDEFF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL FORCE_PED_MOTION_STATE(Ped ped, Hash motionStateHash, BOOL p2, int p3, BOOL p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.FORCE_PED_MOTION_STATE, ped, motionStateHash, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `motionStateHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `int` | `int` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Regarding p2, p3 and p4: Most common is 0, 0, 0); followed by 0, 1, 0); and 1, 1, 0); in R* scripts. p4 is very rarely something other than 0.

enum eMotionState // 0x92A659FE
{
	MotionState_None = 0xEE717723,
	MotionState_Idle = 0x9072A713,
	MotionState_Walk = 0xD827C3DB,
	MotionState_Run = 0xFFF7E7A4,
	MotionState_Sprint = 0xBD8817DB,
	MotionState_Crouch_Idle = 0x43FB099E,
	MotionState_Crouch_Walk = 0x08C31A98,
	MotionState_Crouch_Run = 0x3593CF09,
	MotionState_DoNothing = 0x0EC17E58,
	MotionState_AnimatedVelocity = 0x551AAC43,
	MotionState_InVehicle = 0x94D9D58D,
	MotionState_Aiming = 0x3F67C6AF,
	MotionState_Diving_Idle = 0x4848CDED,
	MotionState_Diving_Swim = 0x916E828C,
	MotionState_Swimming_TreadWater = 0xD1BF11C7,
	MotionState_Dead = 0x0DBB071C,
	MotionState_Stealth_Idle = 0x422D7A25,
	MotionState_Stealth_Walk = 0x042AB6A2,
	MotionState_Stealth_Run = 0xFB0B79E1,
	MotionState_Parachuting = 0xBAC0F10B,
	MotionState_ActionMode_Idle = 0xDA40A0DC,
	MotionState_ActionMode_Walk = 0xD2905EA7,
	MotionState_ActionMode_Run = 0x31BADE14,
	MotionState_Jetpack = 0x535E6A5E
};

---

### GET_PED_CURRENT_MOVE_BLEND_RATIO

**Hash:** `0xF60165E1D2C5370B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_CURRENT_MOVE_BLEND_RATIO(Ped ped, float* speedX, float* speedY)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_CURRENT_MOVE_BLEND_RATIO, ped, speedX, speedY);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speedX` | `float*` | `float*` | |
| `speedY` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_MAX_MOVE_BLEND_RATIO

**Hash:** `0x433083750C5E064A` | **JHash:** `0xEAD0269A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MAX_MOVE_BLEND_RATIO(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MAX_MOVE_BLEND_RATIO, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_MIN_MOVE_BLEND_RATIO

**Hash:** `0x01A898D26E2333DD` | **JHash:** `0x383EC364` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MIN_MOVE_BLEND_RATIO(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MIN_MOVE_BLEND_RATIO, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_MOVE_RATE_OVERRIDE

**Hash:** `0x085BF80FA50A39D1` | **JHash:** `0x900008C6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_MOVE_RATE_OVERRIDE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOVE_RATE_OVERRIDE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Min: 0.00
Max: 10.00

Can be used in combo with fast run cheat.

When value is set to 10.00:
Sprinting without fast run cheat: 66 m/s
Sprinting with fast run cheat: 77 m/s

Needs to be looped!

Note: According to IDA for the Xbox360 xex, when they check bgt they seem to have the min to 0.0f, but the max set to 1.15f not 10.0f.

---

### SET_PED_MOVE_RATE_IN_WATER_OVERRIDE

**Hash:** `0x0B3E35AC043707D9` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_PED_MOVE_RATE_IN_WATER_OVERRIDE(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_MOVE_RATE_IN_WATER_OVERRIDE, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PED_HAS_SEXINESS_FLAG_SET

**Hash:** `0x46B05BCAE43856B0` | **JHash:** `0x79543043` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PED_HAS_SEXINESS_FLAG_SET(Ped ped, int sexinessFlag)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PED_HAS_SEXINESS_FLAG_SET, ped, sexinessFlag);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `sexinessFlag` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the specified sexiness flag is set

enum eSexinessFlags
{
 SF_JEER_AT_HOT_PED = 0,
 SF_HURRIEDFEMALES_SEXY = 1,
 SF_HOT_PERSON = 2,
 };

---

### GET_PED_NEARBY_VEHICLES

**Hash:** `0xCFF869CBFA210D82` | **JHash:** `0xCB716F68` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_NEARBY_VEHICLES(Ped ped, Any* sizeAndVehs)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_NEARBY_VEHICLES, ped, sizeAndVehs);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `sizeAndVehs` | `Any*` | `object*` | |

**返回值:** `int` → `int`

**说明:** Returns size of array, passed into the second variable.

See below for usage information.

This function actually requires a struct, where the first value is the maximum number of elements to return.  Here is a sample of how I was able to get it to work correctly, without yet knowing the struct format.

//Setup the array
 const int numElements = 10;
   const int arrSize = numElements * 2 + 2;
  Any veh[arrSize];
 //0 index is the size of the array
    veh[0] = numElements;

   int count = PED::GET_PED_NEARBY_VEHICLES(PLAYER::PLAYER_PED_ID(), veh);

 if (veh != NULL)
  {
     //Simple loop to go through results
       for (int i = 0; i < count; i++)
        {
         int offsettedID = i * 2 + 2;
          //Make sure it exists
         if (veh[offsettedID] != NULL && ENTITY::DOES_ENTITY_EXIST(veh[offsettedID]))
          {
             //Do something
            }
     }
 }  

---

### GET_PED_NEARBY_PEDS

**Hash:** `0x23F8F5FC7E8C4A6B` | **JHash:** `0x4D3325F4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_NEARBY_PEDS(Ped ped, Any* sizeAndPeds, int ignore)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_NEARBY_PEDS, ped, sizeAndPeds, ignore);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `sizeAndPeds` | `Any*` | `object*` | |
| `ignore` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** sizeAndPeds - is a pointer to an array. The array is filled with peds found nearby the ped supplied to the first argument.
ignore - ped type to ignore

Return value is the number of peds found and added to the array passed.

-----------------------------------

To make this work in most menu bases at least in C++ do it like so,

 Formatted Example: https://pastebin.com/D8an9wwp

-----------------------------------

Example: gtaforums.com/topic/789788-function-args-to-pedget-ped-nearby-peds/?p=1067386687

---

### HAVE_ALL_STREAMING_REQUESTS_COMPLETED

**Hash:** `0x7350823473013C02` | **JHash:** `0xF9FB4B71` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAVE_ALL_STREAMING_REQUESTS_COMPLETED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_ALL_STREAMING_REQUESTS_COMPLETED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_USING_ACTION_MODE

**Hash:** `0x00E73468D085F745` | **JHash:** `0x5AE7EDA2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_USING_ACTION_MODE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_USING_ACTION_MODE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_USING_ACTION_MODE

**Hash:** `0xD75ACCF5E0FB5367` | **JHash:** `0x8802F696` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_USING_ACTION_MODE(Ped ped, BOOL p1, int p2, const char* action)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_USING_ACTION_MODE, ped, p1, p2, action);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |
| `action` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** p2 is usually -1 in the scripts. action is either 0 or "DEFAULT_ACTION".

---

### SET_MOVEMENT_MODE_OVERRIDE

**Hash:** `0x781DE8FA214E87D2` | **JHash:** `0x83BAE814` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOVEMENT_MODE_OVERRIDE(Ped ped, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOVEMENT_MODE_OVERRIDE, ped, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** name: "MP_FEMALE_ACTION" found multiple times in the b617d scripts.

---

### SET_PED_CAPSULE

**Hash:** `0x364DF566EC833DE2` | **JHash:** `0xB153E1B9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAPSULE(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAPSULE, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Overrides the ped's collision capsule radius for the current tick.
Must be called every tick to be effective.

Setting this to 0.001 will allow warping through some objects.

---

### REGISTER_PEDHEADSHOT

**Hash:** `0x4462658788425076` | **JHash:** `0xFFE2667B` | **Build:** `323`

**C++ 原型:**
```cpp
int REGISTER_PEDHEADSHOT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REGISTER_PEDHEADSHOT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** gtaforums.com/topic/885580-ped-headshotmugshot-txd/

---

### REGISTER_PEDHEADSHOT_HIRES

**Hash:** `0xBA8805A1108A2515` | **Build:** `877`

**C++ 原型:**
```cpp
int REGISTER_PEDHEADSHOT_HIRES(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REGISTER_PEDHEADSHOT_HIRES, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### REGISTER_PEDHEADSHOT_TRANSPARENT

**Hash:** `0x953563CE563143AF` | **JHash:** `0x4DD03628` | **Build:** `323`

**C++ 原型:**
```cpp
int REGISTER_PEDHEADSHOT_TRANSPARENT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.REGISTER_PEDHEADSHOT_TRANSPARENT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** Similar to REGISTER_PEDHEADSHOT but creates a transparent background instead of black. Example: https://i.imgur.com/iHz8ztn.png

---

### UNREGISTER_PEDHEADSHOT

**Hash:** `0x96B1361D9B24C2FF` | **JHash:** `0x0879AE45` | **Build:** `323`

**C++ 原型:**
```cpp
void UNREGISTER_PEDHEADSHOT(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNREGISTER_PEDHEADSHOT, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** gtaforums.com/topic/885580-ped-headshotmugshot-txd/

---

### IS_PEDHEADSHOT_VALID

**Hash:** `0xA0A9668F158129A2` | **JHash:** `0x0B1080C4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PEDHEADSHOT_VALID(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PEDHEADSHOT_VALID, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** gtaforums.com/topic/885580-ped-headshotmugshot-txd/

---

### IS_PEDHEADSHOT_READY

**Hash:** `0x7085228842B13A67` | **JHash:** `0x761CD02E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PEDHEADSHOT_READY(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PEDHEADSHOT_READY, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** gtaforums.com/topic/885580-ped-headshotmugshot-txd/

---

### GET_PEDHEADSHOT_TXD_STRING

**Hash:** `0xDB4EACD4AD0A5D6B` | **JHash:** `0x76D28E96` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_PEDHEADSHOT_TXD_STRING(int id)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_PEDHEADSHOT_TXD_STRING, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** gtaforums.com/topic/885580-ped-headshotmugshot-txd/

---

### REQUEST_PEDHEADSHOT_IMG_UPLOAD

**Hash:** `0xF0DAEF2F545BEE25` | **JHash:** `0x10F2C023` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_PEDHEADSHOT_IMG_UPLOAD(int id)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_PEDHEADSHOT_IMG_UPLOAD, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_PEDHEADSHOT_IMG_UPLOAD

**Hash:** `0x5D517B27CF6ECD04` | **JHash:** `0x0DBB2FA7` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_PEDHEADSHOT_IMG_UPLOAD(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_PEDHEADSHOT_IMG_UPLOAD, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PEDHEADSHOT_IMG_UPLOAD_AVAILABLE

**Hash:** `0xEBB376779A760AA8` | **JHash:** `0x810158F8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PEDHEADSHOT_IMG_UPLOAD_AVAILABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PEDHEADSHOT_IMG_UPLOAD_AVAILABLE);
```

**返回值:** `BOOL` → `bool`

---

### HAS_PEDHEADSHOT_IMG_UPLOAD_FAILED

**Hash:** `0x876928DDDFCCC9CD` | **JHash:** `0x05023F8F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PEDHEADSHOT_IMG_UPLOAD_FAILED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PEDHEADSHOT_IMG_UPLOAD_FAILED);
```

**返回值:** `BOOL` → `bool`

---

### HAS_PEDHEADSHOT_IMG_UPLOAD_SUCCEEDED

**Hash:** `0xE8A169E666CBC541` | **JHash:** `0xAA39FD6C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PEDHEADSHOT_IMG_UPLOAD_SUCCEEDED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PEDHEADSHOT_IMG_UPLOAD_SUCCEEDED);
```

**返回值:** `BOOL` → `bool`

---

### SET_PED_HEATSCALE_OVERRIDE

**Hash:** `0xC1F6EBF9A3D55538` | **JHash:** `0xEF9142DB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_HEATSCALE_OVERRIDE(Ped ped, float heatScale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_HEATSCALE_OVERRIDE, ped, heatScale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `heatScale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DISABLE_PED_HEATSCALE_OVERRIDE

**Hash:** `0x600048C60D5C2C51` | **JHash:** `0x0688DE64` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PED_HEATSCALE_OVERRIDE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PED_HEATSCALE_OVERRIDE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SPAWNPOINTS_START_SEARCH

**Hash:** `0x2DF9038C90AD5264` | **JHash:** `0x909A1D76` | **Build:** `323`

**C++ 原型:**
```cpp
void SPAWNPOINTS_START_SEARCH(float p0, float p1, float p2, float p3, float p4, int interiorFlags, float scale, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPAWNPOINTS_START_SEARCH, p0, p1, p2, p3, p4, interiorFlags, scale, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `interiorFlags` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SPAWNPOINTS_START_SEARCH_IN_ANGLED_AREA

**Hash:** `0xB2AFF10216DEFA2F` | **JHash:** `0x4AAD0ECB` | **Build:** `323`

**C++ 原型:**
```cpp
void SPAWNPOINTS_START_SEARCH_IN_ANGLED_AREA(float x1, float y1, float z1, float x2, float y2, float z2, float width, int interiorFlags, float scale, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPAWNPOINTS_START_SEARCH_IN_ANGLED_AREA, x1, y1, z1, x2, y2, z2, width, interiorFlags, scale, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `width` | `float` | `float` | |
| `interiorFlags` | `int` | `int` | |
| `scale` | `float` | `float` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SPAWNPOINTS_CANCEL_SEARCH

**Hash:** `0xFEE4A5459472A9F8` | **JHash:** `0x492C9E46` | **Build:** `323`

**C++ 原型:**
```cpp
void SPAWNPOINTS_CANCEL_SEARCH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPAWNPOINTS_CANCEL_SEARCH);
```

**返回值:** `void` → `void`

---

### SPAWNPOINTS_IS_SEARCH_ACTIVE

**Hash:** `0x3C67506996001F5E` | **JHash:** `0x814A28F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SPAWNPOINTS_IS_SEARCH_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SPAWNPOINTS_IS_SEARCH_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SPAWNPOINTS_IS_SEARCH_COMPLETE

**Hash:** `0xA586FBEB32A53DBB` | **JHash:** `0x0B60D2BA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SPAWNPOINTS_IS_SEARCH_COMPLETE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SPAWNPOINTS_IS_SEARCH_COMPLETE);
```

**返回值:** `BOOL` → `bool`

---

### SPAWNPOINTS_IS_SEARCH_FAILED

**Hash:** `0xF445DE8DA80A1792` | **JHash:** `0x6B83ABDF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SPAWNPOINTS_IS_SEARCH_FAILED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SPAWNPOINTS_IS_SEARCH_FAILED);
```

**返回值:** `BOOL` → `bool`

---

### SPAWNPOINTS_GET_NUM_SEARCH_RESULTS

**Hash:** `0xA635C11B8C44AFC2` | **JHash:** `0xF46B4DC8` | **Build:** `323`

**C++ 原型:**
```cpp
int SPAWNPOINTS_GET_NUM_SEARCH_RESULTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SPAWNPOINTS_GET_NUM_SEARCH_RESULTS);
```

**返回值:** `int` → `int`

---

### SPAWNPOINTS_GET_SEARCH_RESULT

**Hash:** `0x280C7E3AC7F56E90` | **JHash:** `0x36A4AC65` | **Build:** `323`

**C++ 原型:**
```cpp
void SPAWNPOINTS_GET_SEARCH_RESULT(int randomInt, float* x, float* y, float* z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPAWNPOINTS_GET_SEARCH_RESULT, randomInt, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `randomInt` | `int` | `int` | |
| `x` | `float*` | `float*` | |
| `y` | `float*` | `float*` | |
| `z` | `float*` | `float*` | |

**返回值:** `void` → `void`

---

### SPAWNPOINTS_GET_SEARCH_RESULT_FLAGS

**Hash:** `0xB782F8238512BAD5` | **JHash:** `0xBA699DDF` | **Build:** `323`

**C++ 原型:**
```cpp
void SPAWNPOINTS_GET_SEARCH_RESULT_FLAGS(int p0, int* p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPAWNPOINTS_GET_SEARCH_RESULT_FLAGS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_IK_TARGET

**Hash:** `0xC32779C16FCEECD9` | **JHash:** `0x6FE5218C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IK_TARGET(Ped ped, int ikIndex, Entity entityLookAt, int boneLookAt, float offsetX, float offsetY, float offsetZ, Any p7, int blendInDuration, int blendOutDuration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IK_TARGET, ped, ikIndex, entityLookAt, boneLookAt, offsetX, offsetY, offsetZ, p7, blendInDuration, blendOutDuration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ikIndex` | `int` | `int` | |
| `entityLookAt` | `Entity` | `int` | |
| `boneLookAt` | `int` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `p7` | `Any` | `object` | |
| `blendInDuration` | `int` | `int` | |
| `blendOutDuration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FORCE_INSTANT_LEG_IK_SETUP

**Hash:** `0xED3C76ADFA6D07C4` | **JHash:** `0xFB4000DC` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_INSTANT_LEG_IK_SETUP(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_INSTANT_LEG_IK_SETUP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### REQUEST_ACTION_MODE_ASSET

**Hash:** `0x290E2780BB7AA598` | **JHash:** `0x572BA553` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ACTION_MODE_ASSET(const char* asset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ACTION_MODE_ASSET, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### HAS_ACTION_MODE_ASSET_LOADED

**Hash:** `0xE4B5F4BF2CB24E65` | **JHash:** `0xF7EB2BF1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ACTION_MODE_ASSET_LOADED(const char* asset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ACTION_MODE_ASSET_LOADED, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_ACTION_MODE_ASSET

**Hash:** `0x13E940F88470FA51` | **JHash:** `0x3F480F92` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ACTION_MODE_ASSET(const char* asset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ACTION_MODE_ASSET, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### REQUEST_STEALTH_MODE_ASSET

**Hash:** `0x2A0A62FCDEE16D4F` | **JHash:** `0x280A004A` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_STEALTH_MODE_ASSET(const char* asset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_STEALTH_MODE_ASSET, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### HAS_STEALTH_MODE_ASSET_LOADED

**Hash:** `0xE977FC5B08AF3441` | **JHash:** `0x39245667` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_STEALTH_MODE_ASSET_LOADED(const char* asset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_STEALTH_MODE_ASSET_LOADED, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_STEALTH_MODE_ASSET

**Hash:** `0x9219857D21F0E842` | **JHash:** `0x8C0B243A` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_STEALTH_MODE_ASSET(const char* asset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_STEALTH_MODE_ASSET, asset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `asset` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_PED_LOD_MULTIPLIER

**Hash:** `0xDC2C5C242AAC342B` | **JHash:** `0x1D2B5C70` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_LOD_MULTIPLIER(Ped ped, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_LOD_MULTIPLIER, ped, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_CAN_LOSE_PROPS_ON_DAMAGE

**Hash:** `0xE861D0B05C7662B8` | **JHash:** `0x2F9550C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_LOSE_PROPS_ON_DAMAGE(Ped ped, BOOL toggle, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_LOSE_PROPS_ON_DAMAGE, ped, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_FORCE_FOOTSTEP_UPDATE

**Hash:** `0x129466ED55140F8D` | **JHash:** `0x37DBC2AD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_FOOTSTEP_UPDATE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_FOOTSTEP_UPDATE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_FORCE_STEP_TYPE

**Hash:** `0xCB968B53FC7F916D` | **JHash:** `0xC0F1BC91` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_STEP_TYPE(Ped ped, BOOL p1, int type, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_STEP_TYPE, ped, p1, type, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `type` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_ANY_HOSTILE_PED_NEAR_POINT

**Hash:** `0x68772DB2B2526F9F` | **JHash:** `0x1A464167` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_HOSTILE_PED_NEAR_POINT(Ped ped, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_HOSTILE_PED_NEAR_POINT, ped, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_CAN_PLAY_IN_CAR_IDLES

**Hash:** `0x820E9892A77E97CD` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_IN_CAR_IDLES(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_IN_CAR_IDLES, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles config flag CPED_CONFIG_FLAG_CanPlayInCarIdles.

---

### IS_TARGET_PED_IN_PERCEPTION_AREA

**Hash:** `0x06087579E7AA85A9` | **JHash:** `0xD0567D41` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TARGET_PED_IN_PERCEPTION_AREA(Ped ped, Ped targetPed, float p2, float p3, float p4, float p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TARGET_PED_IN_PERCEPTION_AREA, ped, targetPed, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_POP_CONTROL_SPHERE_THIS_FRAME

**Hash:** `0xD8C3BE3EE94CAF2D` | **JHash:** `0x4BBE5E2C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POP_CONTROL_SPHERE_THIS_FRAME(float x, float y, float z, float min, float max)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POP_CONTROL_SPHERE_THIS_FRAME, x, y, z, min, max);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `min` | `float` | `float` | |
| `max` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Min and max are usually 100.0 and 200.0

---

### FORCE_ZERO_MASS_IN_COLLISIONS

**Hash:** `0xD33DAA36272177C4` | **JHash:** `0xA89A53F2` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ZERO_MASS_IN_COLLISIONS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ZERO_MASS_IN_COLLISIONS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_HIGH_FALL_DEATH

**Hash:** `0x711794453CFD692B` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_DISABLE_HIGH_FALL_DEATH(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_HIGH_FALL_DEATH, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PHONE_PALETTE_IDX

**Hash:** `0x83A169EABCDB10A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PHONE_PALETTE_IDX(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PHONE_PALETTE_IDX, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PED_STEER_BIAS

**Hash:** `0x288DF530C92DAD6F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_STEER_BIAS(Ped ped, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STEER_BIAS, ped, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_PED_SWITCHING_WEAPON

**Hash:** `0x3795688A307E1EB6` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL IS_PED_SWITCHING_WEAPON(Ped Ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SWITCHING_WEAPON, Ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `Ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PED_TREATED_AS_FRIENDLY

**Hash:** `0x0F62619393661D6E` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_PED_TREATED_AS_FRIENDLY(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_TREATED_AS_FRIENDLY, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_PED_MAP_COLLISION

**Hash:** `0xDFE68C4B787E1BFB` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_DISABLE_PED_MAP_COLLISION(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_PED_MAP_COLLISION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### ENABLE_MP_LIGHT

**Hash:** `0xEE2476B9EE4A094F` | **Build:** `1493`

**C++ 原型:**
```cpp
void ENABLE_MP_LIGHT(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_MP_LIGHT, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_MP_LIGHT_ENABLED

**Hash:** `0x88274C11CF0D866D` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL GET_MP_LIGHT_ENABLED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MP_LIGHT_ENABLED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_COVER_POINT_FOR_PED

**Hash:** `0x637822DC2AFEEBF8` | **Build:** `1493`

**C++ 原型:**
```cpp
void CLEAR_COVER_POINT_FOR_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_COVER_POINT_FOR_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_STUNT_JUMP_CAMERA

**Hash:** `0xFAB944D4D481ACCB` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_ALLOW_STUNT_JUMP_CAMERA(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_STUNT_JUMP_CAMERA, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---


---

## PHYSICS

**包含 49 个 Native 函数**

### ADD_ROPE

**Hash:** `0xE832D760399EB220` | **JHash:** `0xA592EC74` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_ROPE(float x, float y, float z, float rotX, float rotY, float rotZ, float length, int ropeType, float maxLength, float minLength, float windingSpeed, BOOL p11, BOOL p12, BOOL rigid, float p14, BOOL breakWhenShot, Any* unkPtr)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_ROPE, x, y, z, rotX, rotY, rotZ, length, ropeType, maxLength, minLength, windingSpeed, p11, p12, rigid, p14, breakWhenShot, unkPtr);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `length` | `float` | `float` | |
| `ropeType` | `int` | `int` | |
| `maxLength` | `float` | `float` | |
| `minLength` | `float` | `float` | |
| `windingSpeed` | `float` | `float` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `BOOL` | `bool` | |
| `rigid` | `BOOL` | `bool` | |
| `p14` | `float` | `float` | |
| `breakWhenShot` | `BOOL` | `bool` | |
| `unkPtr` | `Any*` | `object*` | |

**返回值:** `int` → `int`

**说明:** Creates a rope at the specific position, that extends in the specified direction when not attached to any entities.
__

Add_Rope(pos.x,pos.y,pos.z,0.0,0.0,0.0,20.0,4,20.0,1.0,0.0,false,false,false,5.0,false,NULL)

When attached, Position<vector> does not matter
When attached, Angle<vector> does not matter

Rope Type:
4 and bellow is a thick rope
5 and up are small metal wires
0 crashes the game

Max_length - Rope is forced to this length, generally best to keep this the same as your rope length.

windingSpeed - Speed the Rope is being winded, using native START_ROPE_WINDING. Set positive for winding and negative for unwinding.

Rigid - If max length is zero, and this is set to false the rope will become rigid (it will force a specific distance, what ever length is, between the objects).

breakable - Whether or not shooting the rope will break it.

unkPtr - unknown ptr, always 0 in orig scripts
__

Lengths can be calculated like so:

float distance = abs(x1 - x2) + abs(y1 - y2) + abs(z1 - z2); // Rope length


NOTES:

Rope does NOT interact with anything you attach it to, in some cases it make interact with the world AFTER it breaks (seems to occur if you set the type to -1).

Rope will sometimes contract and fall to the ground like you'd expect it to, but since it doesn't interact with the world the effect is just jaring.

---

### DELETE_ROPE

**Hash:** `0x52B4829281364649` | **JHash:** `0x748D72AF` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_ROPE(int* ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_ROPE, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### DELETE_CHILD_ROPE

**Hash:** `0xAA5D6B1888E4DB20` | **JHash:** `0xB19B4706` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_CHILD_ROPE(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_CHILD_ROPE, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### DOES_ROPE_EXIST

**Hash:** `0xFD5448BE3111ED96` | **JHash:** `0x66E4A3AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ROPE_EXIST(int* ropeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ROPE_EXIST, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### ROPE_DRAW_ENABLED

**Hash:** `0xA1AE736541B0FCA3` | **Build:** `1868`

**C++ 原型:**
```cpp
void ROPE_DRAW_ENABLED(int* ropeId, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_DRAW_ENABLED, ropeId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int*` | `int*` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ROPE_DRAW_SHADOW_ENABLED

**Hash:** `0xF159A63806BB5BA8` | **JHash:** `0x51523B8C` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_DRAW_SHADOW_ENABLED(int* ropeId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_DRAW_SHADOW_ENABLED, ropeId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int*` | `int*` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOAD_ROPE_DATA

**Hash:** `0xCBB203C04D1ABD27` | **JHash:** `0x9E8F1644` | **Build:** `323`

**C++ 原型:**
```cpp
void LOAD_ROPE_DATA(int ropeId, const char* rope_preset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOAD_ROPE_DATA, ropeId, rope_preset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `rope_preset` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Rope presets can be found in the gamefiles. One example is "ropeFamily3", it is NOT a hash but rather a string.

---

### PIN_ROPE_VERTEX

**Hash:** `0x2B320CF14146B69A` | **JHash:** `0xAE1D101B` | **Build:** `323`

**C++ 原型:**
```cpp
void PIN_ROPE_VERTEX(int ropeId, int vertex, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PIN_ROPE_VERTEX, ropeId, vertex, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `vertex` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UNPIN_ROPE_VERTEX

**Hash:** `0x4B5AE2EEE4A8F180` | **JHash:** `0xB30B552F` | **Build:** `323`

**C++ 原型:**
```cpp
void UNPIN_ROPE_VERTEX(int ropeId, int vertex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNPIN_ROPE_VERTEX, ropeId, vertex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `vertex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_ROPE_VERTEX_COUNT

**Hash:** `0x3655F544CD30F0B5` | **JHash:** `0x5131CD2C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ROPE_VERTEX_COUNT(int ropeId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ROPE_VERTEX_COUNT, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### ATTACH_ENTITIES_TO_ROPE

**Hash:** `0x3D95EC8B6D940AC3` | **JHash:** `0x7508668F` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_ENTITIES_TO_ROPE(int ropeId, Entity ent1, Entity ent2, float ent1_x, float ent1_y, float ent1_z, float ent2_x, float ent2_y, float ent2_z, float length, BOOL p10, BOOL p11, Any* p12, Any* p13)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITIES_TO_ROPE, ropeId, ent1, ent2, ent1_x, ent1_y, ent1_z, ent2_x, ent2_y, ent2_z, length, p10, p11, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `ent1` | `Entity` | `int` | |
| `ent2` | `Entity` | `int` | |
| `ent1_x` | `float` | `float` | |
| `ent1_y` | `float` | `float` | |
| `ent1_z` | `float` | `float` | |
| `ent2_x` | `float` | `float` | |
| `ent2_y` | `float` | `float` | |
| `ent2_z` | `float` | `float` | |
| `length` | `float` | `float` | |
| `p10` | `BOOL` | `bool` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `Any*` | `object*` | |
| `p13` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Attaches entity 1 to entity 2.

---

### ATTACH_ROPE_TO_ENTITY

**Hash:** `0x4B490A6832559A65` | **JHash:** `0xB25D9536` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_ROPE_TO_ENTITY(int ropeId, Entity entity, float x, float y, float z, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ROPE_TO_ENTITY, ropeId, entity, x, y, z, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The position supplied can be anywhere, and the entity should anchor relative to that point from it's origin.

---

### DETACH_ROPE_FROM_ENTITY

**Hash:** `0xBCF3026912A8647D` | **JHash:** `0x3E720BEE` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_ROPE_FROM_ENTITY(int ropeId, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_ROPE_FROM_ENTITY, ropeId, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### ROPE_SET_UPDATE_PINVERTS

**Hash:** `0xC8D667EE52114ABA` | **JHash:** `0xEAF291A0` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_SET_UPDATE_PINVERTS(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_SET_UPDATE_PINVERTS, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ROPE_SET_UPDATE_ORDER

**Hash:** `0xDC57A637A20006ED` | **JHash:** `0x80DB77A7` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_SET_UPDATE_ORDER(int ropeId, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_SET_UPDATE_ORDER, ropeId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ROPE_SET_SMOOTH_REELIN

**Hash:** `0x36CCB9BE67B970FD` | **JHash:** `0xC67D5CF6` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_SET_SMOOTH_REELIN(int ropeId, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_SET_SMOOTH_REELIN, ropeId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_ROPE_ATTACHED_AT_BOTH_ENDS

**Hash:** `0x84DE3B5FB3E666F0` | **JHash:** `0x7A18BB9C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ROPE_ATTACHED_AT_BOTH_ENDS(int* ropeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ROPE_ATTACHED_AT_BOTH_ENDS, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### GET_ROPE_LAST_VERTEX_COORD

**Hash:** `0x21BB0FBD3E217C2D` | **JHash:** `0x91F6848B` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ROPE_LAST_VERTEX_COORD(int ropeId)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ROPE_LAST_VERTEX_COORD, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_ROPE_VERTEX_COORD

**Hash:** `0xEA61CA8E80F09E4D` | **JHash:** `0x84374452` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ROPE_VERTEX_COORD(int ropeId, int vertex)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ROPE_VERTEX_COORD, ropeId, vertex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `vertex` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### START_ROPE_WINDING

**Hash:** `0x1461C72C889E343E` | **JHash:** `0x5187BED3` | **Build:** `323`

**C++ 原型:**
```cpp
void START_ROPE_WINDING(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_ROPE_WINDING, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### STOP_ROPE_WINDING

**Hash:** `0xCB2D4AB84A19AA7C` | **JHash:** `0x46826B53` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ROPE_WINDING(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ROPE_WINDING, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### START_ROPE_UNWINDING_FRONT

**Hash:** `0x538D1179EC1AA9A9` | **JHash:** `0xFC0DB4C3` | **Build:** `323`

**C++ 原型:**
```cpp
void START_ROPE_UNWINDING_FRONT(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_ROPE_UNWINDING_FRONT, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### STOP_ROPE_UNWINDING_FRONT

**Hash:** `0xFFF3A50779EFBBB3` | **JHash:** `0x2EEDB18F` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ROPE_UNWINDING_FRONT(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ROPE_UNWINDING_FRONT, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ROPE_CONVERT_TO_SIMPLE

**Hash:** `0x5389D48EFA2F079A` | **JHash:** `0x43E92628` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_CONVERT_TO_SIMPLE(int ropeId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_CONVERT_TO_SIMPLE, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ROPE_LOAD_TEXTURES

**Hash:** `0x9B9039DBF2D258C1` | **JHash:** `0xBA97CE91` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_LOAD_TEXTURES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_LOAD_TEXTURES);
```

**返回值:** `void` → `void`

**说明:** Loads rope textures for all ropes in the current scene.

---

### ROPE_ARE_TEXTURES_LOADED

**Hash:** `0xF2D0E6A75CC05597` | **JHash:** `0x5FDC1047` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ROPE_ARE_TEXTURES_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ROPE_ARE_TEXTURES_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### ROPE_UNLOAD_TEXTURES

**Hash:** `0x6CE36C35C1AC8163` | **JHash:** `0x584463E0` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_UNLOAD_TEXTURES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_UNLOAD_TEXTURES);
```

**返回值:** `void` → `void`

**说明:** Unloads rope textures for all ropes in the current scene.

---

### DOES_SCRIPT_OWN_ROPE

**Hash:** `0x271C9D3ACA5D6409` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCRIPT_OWN_ROPE(int ropeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCRIPT_OWN_ROPE, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ROPE_ATTACH_VIRTUAL_BOUND_GEOM

**Hash:** `0xBC0CE682D4D05650` | **JHash:** `0x106BA127` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_ATTACH_VIRTUAL_BOUND_GEOM(int ropeId, int p1, float p2, float p3, float p4, float p5, float p6, float p7, float p8, float p9, float p10, float p11, float p12, float p13)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_ATTACH_VIRTUAL_BOUND_GEOM, ropeId, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10, p11, p12, p13);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `float` | `float` | |
| `p10` | `float` | `float` | |
| `p11` | `float` | `float` | |
| `p12` | `float` | `float` | |
| `p13` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ROPE_CHANGE_SCRIPT_OWNER

**Hash:** `0xB1B6216CA2E7B55E` | **JHash:** `0x7C6F7668` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_CHANGE_SCRIPT_OWNER(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_CHANGE_SCRIPT_OWNER, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ROPE_SET_REFFRAMEVELOCITY_COLLIDERORDER

**Hash:** `0xB743F735C03D7810` | **JHash:** `0x686672DD` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_SET_REFFRAMEVELOCITY_COLLIDERORDER(int ropeId, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_SET_REFFRAMEVELOCITY_COLLIDERORDER, ropeId, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ROPE_GET_DISTANCE_BETWEEN_ENDS

**Hash:** `0x73040398DFF9A4A6` | **JHash:** `0xFD309DC8` | **Build:** `323`

**C++ 原型:**
```cpp
float ROPE_GET_DISTANCE_BETWEEN_ENDS(int ropeId)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.ROPE_GET_DISTANCE_BETWEEN_ENDS, ropeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |

**返回值:** `float` → `float`

---

### ROPE_FORCE_LENGTH

**Hash:** `0xD009F759A723DB1B` | **JHash:** `0xABF3130F` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_FORCE_LENGTH(int ropeId, float length)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_FORCE_LENGTH, ropeId, length);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `length` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Forces a rope to a certain length.

---

### ROPE_RESET_LENGTH

**Hash:** `0xC16DE94D9BEA14A0` | **JHash:** `0xC8A423A3` | **Build:** `323`

**C++ 原型:**
```cpp
void ROPE_RESET_LENGTH(int ropeId, float length)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROPE_RESET_LENGTH, ropeId, length);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ropeId` | `int` | `int` | |
| `length` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Reset a rope to a certain length.

---

### APPLY_IMPULSE_TO_CLOTH

**Hash:** `0xE37F721824571784` | **JHash:** `0xA2A5C9FE` | **Build:** `323`

**C++ 原型:**
```cpp
void APPLY_IMPULSE_TO_CLOTH(float posX, float posY, float posZ, float vecX, float vecY, float vecZ, float impulse)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_IMPULSE_TO_CLOTH, posX, posY, posZ, vecX, vecY, vecZ, impulse);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `vecX` | `float` | `float` | |
| `vecY` | `float` | `float` | |
| `vecZ` | `float` | `float` | |
| `impulse` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_DAMPING

**Hash:** `0xEEA3B200A6FEB65B` | **JHash:** `0xCFB37773` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DAMPING(Entity entity, int vertex, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DAMPING, entity, vertex, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `vertex` | `int` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_DAMPING

**Hash:** `0x8C520A929415BCD2` | **Build:** `3407`

**C++ 原型:**
```cpp
Vector3 GET_DAMPING(Entity entity, int type)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_DAMPING, entity, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `type` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### ACTIVATE_PHYSICS

**Hash:** `0x710311ADF0E20730` | **JHash:** `0x031711B8` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_PHYSICS(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_PHYSICS, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_CGOFFSET

**Hash:** `0xD8FA3908D7B86904` | **JHash:** `0x59910AB2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CGOFFSET(Entity entity, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CGOFFSET, entity, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CGOFFSET

**Hash:** `0x8214A4B5A7A33612` | **JHash:** `0x49A11F0D` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_CGOFFSET(Entity entity)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_CGOFFSET, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### SET_CG_AT_BOUNDCENTER

**Hash:** `0xBE520D9761FF811F` | **JHash:** `0xA5B55421` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CG_AT_BOUNDCENTER(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CG_AT_BOUNDCENTER, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### BREAK_ENTITY_GLASS

**Hash:** `0x2E648D16F6E308F3` | **JHash:** `0xD0E0402F` | **Build:** `323`

**C++ 原型:**
```cpp
void BREAK_ENTITY_GLASS(Entity entity, float p1, float p2, float p3, float p4, float p5, float p6, float p7, float p8, Any p9, BOOL p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BREAK_ENTITY_GLASS, entity, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `p9` | `Any` | `object` | |
| `p10` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_IS_ENTITY_A_FRAG

**Hash:** `0x0C112765300C7E1E` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_IS_ENTITY_A_FRAG(Object object)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_ENTITY_A_FRAG, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_DISABLE_BREAKING

**Hash:** `0x5CEC1A84620E7D5B` | **JHash:** `0xEE77C326` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_BREAKING(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_BREAKING, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RESET_DISABLE_BREAKING

**Hash:** `0xCC6E963682533882` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_DISABLE_BREAKING(Object object)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DISABLE_BREAKING, object);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_FRAG_DAMAGE

**Hash:** `0x01BA3AED21C16CFB` | **JHash:** `0x97269DC8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_FRAG_DAMAGE(Object object, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_FRAG_DAMAGE, object, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_USE_KINEMATIC_PHYSICS

**Hash:** `0x15F944730C832252` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_USE_KINEMATIC_PHYSICS(Entity entity, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_KINEMATIC_PHYSICS, entity, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IN_STUNT_MODE

**Hash:** `0x9EBD751E5787BAF2` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_IN_STUNT_MODE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IN_STUNT_MODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IN_ARENA_MODE

**Hash:** `0xAA6A6098851C396F` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_IN_ARENA_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IN_ARENA_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Related to the lower-end of a vehicles fTractionCurve, e.g., from standing starts and acceleration from low/zero speeds.

---


---

## PLAYER

**包含 250 个 Native 函数**

### GET_PLAYER_PED

**Hash:** `0x43A66C31C68491C0` | **JHash:** `0x6E31E993` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PLAYER_PED(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Ped` → `int`

**说明:** Gets the ped for a specified player index.

---

### GET_PLAYER_PED_SCRIPT_INDEX

**Hash:** `0x50FAC3A3E030A6E1` | **JHash:** `0x6AC64990` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PLAYER_PED_SCRIPT_INDEX(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_PED_SCRIPT_INDEX, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Ped` → `int`

**说明:** Does the same like PLAYER::GET_PLAYER_PED

---

### SET_PLAYER_MODEL

**Hash:** `0x00A1CADD00108836` | **JHash:** `0x774A4C54` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MODEL(Player player, Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MODEL, player, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Set the model for a specific Player. Be aware that this will destroy the current Ped for the Player and create a new one, any reference to the old ped should be reset
Make sure to request the model first and wait until it has loaded.

---

### CHANGE_PLAYER_PED

**Hash:** `0x048189FAC643DEEE` | **JHash:** `0xBE515485` | **Build:** `323`

**C++ 原型:**
```cpp
void CHANGE_PLAYER_PED(Player player, Ped ped, BOOL p2, BOOL resetDamage)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CHANGE_PLAYER_PED, player, ped, p2, resetDamage);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `ped` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `resetDamage` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_RGB_COLOUR

**Hash:** `0xE902EF951DCE178F` | **JHash:** `0x6EF43BBB` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PLAYER_RGB_COLOUR(Player player, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PLAYER_RGB_COLOUR, player, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_NUMBER_OF_PLAYERS

**Hash:** `0x407C7F91DDB46C16` | **JHash:** `0x4C1B8867` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PLAYERS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PLAYERS);
```

**返回值:** `int` → `int`

**说明:** Gets the number of players in the current session.
If not multiplayer, always returns 1.

---

### GET_PLAYER_TEAM

**Hash:** `0x37039302F4E0A008` | **JHash:** `0x9873E404` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_TEAM(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_TEAM, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

**说明:** Gets the player's team.
Does nothing in singleplayer.

---

### SET_PLAYER_TEAM

**Hash:** `0x0299FA38396A4940` | **JHash:** `0x725ADCF2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_TEAM(Player player, int team)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_TEAM, player, team);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `team` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Set player team on deathmatch and last team standing..

---

### GET_NUMBER_OF_PLAYERS_IN_TEAM

**Hash:** `0x1FC200409F10E6F1` | **Build:** `1180`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_PLAYERS_IN_TEAM(int team)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_PLAYERS_IN_TEAM, team);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `team` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_PLAYER_NAME

**Hash:** `0x6D0DE6A7B5DA71F8` | **JHash:** `0x406B4B20` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_PLAYER_NAME(Player player)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_PLAYER_NAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `const char*` → `string`

---

### GET_WANTED_LEVEL_RADIUS

**Hash:** `0x085DEB493BE80812` | **JHash:** `0x1CF7D7DA` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_WANTED_LEVEL_RADIUS(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_WANTED_LEVEL_RADIUS, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

**说明:** Remnant from GTA IV. Does nothing in GTA V.

---

### GET_PLAYER_WANTED_CENTRE_POSITION

**Hash:** `0x0C92BA89F1AF26F8` | **JHash:** `0x821F2D2C` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_PLAYER_WANTED_CENTRE_POSITION(Player player)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_PLAYER_WANTED_CENTRE_POSITION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### SET_PLAYER_WANTED_CENTRE_POSITION

**Hash:** `0x520E541A97A13354` | **JHash:** `0xF261633A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WANTED_CENTRE_POSITION(Player player, Vector3* position, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WANTED_CENTRE_POSITION, player, position, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `position` | `Vector3*` | `Vector3*` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** # Predominant call signatures
PLAYER::SET_PLAYER_WANTED_CENTRE_POSITION(PLAYER::PLAYER_ID(), ENTITY::GET_ENTITY_COORDS(PLAYER::PLAYER_PED_ID(), 1));

# Parameter value ranges
P0: PLAYER::PLAYER_ID()
P1: ENTITY::GET_ENTITY_COORDS(PLAYER::PLAYER_PED_ID(), 1)
P2: Not set by any call

---

### GET_WANTED_LEVEL_THRESHOLD

**Hash:** `0xFDD179EAF45B556C` | **JHash:** `0xD9783F6B` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WANTED_LEVEL_THRESHOLD(int wantedLevel)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WANTED_LEVEL_THRESHOLD, wantedLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `wantedLevel` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Drft

---

### SET_PLAYER_WANTED_LEVEL

**Hash:** `0x39FF19C64EF7DA5B` | **JHash:** `0xB7A0914B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WANTED_LEVEL(Player player, int wantedLevel, BOOL disableNoMission)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WANTED_LEVEL, player, wantedLevel, disableNoMission);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `wantedLevel` | `int` | `int` | |
| `disableNoMission` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Call SET_PLAYER_WANTED_LEVEL_NOW for immediate effect

wantedLevel is an integer value representing 0 to 5 stars even though the game supports the 6th wanted level but no police will appear since no definitions are present for it in the game files

disableNoMission-  Disables When Off Mission- appears to always be false


---

### SET_PLAYER_WANTED_LEVEL_NO_DROP

**Hash:** `0x340E61DE7F471565` | **JHash:** `0xED6F44F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WANTED_LEVEL_NO_DROP(Player player, int wantedLevel, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WANTED_LEVEL_NO_DROP, player, wantedLevel, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `wantedLevel` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p2 is always false in R* scripts

---

### SET_PLAYER_WANTED_LEVEL_NOW

**Hash:** `0xE0A7D1E497FFCD6F` | **JHash:** `0xAF3AFD83` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WANTED_LEVEL_NOW(Player player, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WANTED_LEVEL_NOW, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Forces any pending wanted level to be applied to the specified player immediately.

Call SET_PLAYER_WANTED_LEVEL with the desired wanted level, followed by SET_PLAYER_WANTED_LEVEL_NOW.

Second parameter is unknown (always false).

---

### ARE_PLAYER_FLASHING_STARS_ABOUT_TO_DROP

**Hash:** `0xAFAF86043E5874E9` | **JHash:** `0xE13A71C7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_PLAYER_FLASHING_STARS_ABOUT_TO_DROP(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_PLAYER_FLASHING_STARS_ABOUT_TO_DROP, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ARE_PLAYER_STARS_GREYED_OUT

**Hash:** `0x0A6EB355EE14A2DB` | **JHash:** `0x5E72AB72` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_PLAYER_STARS_GREYED_OUT(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_PLAYER_STARS_GREYED_OUT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_WANTED_AND_HAS_BEEN_SEEN_BY_COPS

**Hash:** `0x7E07C78925D5FD96` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL IS_WANTED_AND_HAS_BEEN_SEEN_BY_COPS(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WANTED_AND_HAS_BEEN_SEEN_BY_COPS, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_DISPATCH_COPS_FOR_PLAYER

**Hash:** `0xDB172424876553F4` | **JHash:** `0x48A18913` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISPATCH_COPS_FOR_PLAYER(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISPATCH_COPS_FOR_PLAYER, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PLAYER_WANTED_LEVEL_GREATER

**Hash:** `0x238DB2A2C23EE9EF` | **JHash:** `0x589A2661` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_WANTED_LEVEL_GREATER(Player player, int wantedLevel)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_WANTED_LEVEL_GREATER, player, wantedLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `wantedLevel` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_PLAYER_WANTED_LEVEL

**Hash:** `0xB302540597885499` | **JHash:** `0x54EA5BCC` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_WANTED_LEVEL(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_WANTED_LEVEL, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** This executes at the same as speed as PLAYER::SET_PLAYER_WANTED_LEVEL(player, 0, false);

PLAYER::GET_PLAYER_WANTED_LEVEL(player); executes in less than half the time. Which means that it's worth first checking if the wanted level needs to be cleared before clearing. However, this is mostly about good code practice and can important in other situations. The difference in time in this example is negligible. 

---

### IS_PLAYER_DEAD

**Hash:** `0x424D4687FA1E5652` | **JHash:** `0x140CA5A8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_DEAD(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_DEAD, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_PRESSING_HORN

**Hash:** `0xFA1E2BF8B10598F9` | **JHash:** `0xED1D1662` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_PRESSING_HORN(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_PRESSING_HORN, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_CONTROL

**Hash:** `0x8D32347D6D4C40A2` | **JHash:** `0xD17AFCD8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CONTROL(Player player, BOOL bHasControl, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CONTROL, player, bHasControl, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `bHasControl` | `BOOL` | `bool` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Flags:
SPC_AMBIENT_SCRIPT = (1 << 1),
SPC_CLEAR_TASKS = (1 << 2),
SPC_REMOVE_FIRES = (1 << 3),
SPC_REMOVE_EXPLOSIONS = (1 << 4),
SPC_REMOVE_PROJECTILES = (1 << 5),
SPC_DEACTIVATE_GADGETS = (1 << 6),
SPC_REENABLE_CONTROL_ON_DEATH = (1 << 7),
SPC_LEAVE_CAMERA_CONTROL_ON = (1 << 8),
SPC_ALLOW_PLAYER_DAMAGE = (1 << 9),
SPC_DONT_STOP_OTHER_CARS_AROUND_PLAYER = (1 << 10),
SPC_PREVENT_EVERYBODY_BACKOFF = (1 << 11),
SPC_ALLOW_PAD_SHAKE = (1 << 12)

See: https://alloc8or.re/gta5/doc/enums/eSetPlayerControlFlag.txt

---

### GET_PLAYER_WANTED_LEVEL

**Hash:** `0xE28E54788CE8F12D` | **JHash:** `0xBDCDD163` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_WANTED_LEVEL(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_WANTED_LEVEL, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### SET_MAX_WANTED_LEVEL

**Hash:** `0xAA5F02DB48D704B9` | **JHash:** `0x665A06F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MAX_WANTED_LEVEL(int maxWantedLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MAX_WANTED_LEVEL, maxWantedLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `maxWantedLevel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_POLICE_RADAR_BLIPS

**Hash:** `0x43286D561B72B8BF` | **JHash:** `0x8E114B10` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POLICE_RADAR_BLIPS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POLICE_RADAR_BLIPS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If toggle is set to false:
 The police won't be shown on the (mini)map

If toggle is set to true:
 The police will be shown on the (mini)map

---

### SET_POLICE_IGNORE_PLAYER

**Hash:** `0x32C62AA929C2DA6A` | **JHash:** `0xE6DE71B7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POLICE_IGNORE_PLAYER(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POLICE_IGNORE_PLAYER, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The player will be ignored by the police if toggle is set to true

---

### IS_PLAYER_PLAYING

**Hash:** `0x5E9564D8246B909A` | **JHash:** `0xE15D777F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_PLAYING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_PLAYING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks whether the specified player has a Ped, the Ped is not dead, is not injured and is not arrested.

---

### SET_EVERYONE_IGNORE_PLAYER

**Hash:** `0x8EEDA153AD141BA4` | **JHash:** `0xC915285E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EVERYONE_IGNORE_PLAYER(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EVERYONE_IGNORE_PLAYER, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALL_RANDOM_PEDS_FLEE

**Hash:** `0x056E0FE8534C2949` | **JHash:** `0x49EAE968` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_RANDOM_PEDS_FLEE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_RANDOM_PEDS_FLEE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALL_RANDOM_PEDS_FLEE_THIS_FRAME

**Hash:** `0x471D2FF42A94B4F2` | **JHash:** `0xBF974891` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_RANDOM_PEDS_FLEE_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_RANDOM_PEDS_FLEE_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE

**Hash:** `0xDE45D1A1EF45EE61` | **JHash:** `0x274631FE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE_THIS_FRAME

**Hash:** `0xC3376F42B1FACCC6` | **JHash:** `0x02DF7AF4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_NEUTRAL_RANDOM_PEDS_FLEE_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** - This is called after SET_ALL_RANDOM_PEDS_FLEE_THIS_FRAME


---

### SET_LAW_PEDS_CAN_ATTACK_NON_WANTED_PLAYER_THIS_FRAME

**Hash:** `0xFAC75988A7D078D3` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_LAW_PEDS_CAN_ATTACK_NON_WANTED_PLAYER_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LAW_PEDS_CAN_ATTACK_NON_WANTED_PLAYER_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_IGNORE_LOW_PRIORITY_SHOCKING_EVENTS

**Hash:** `0x596976B02B6B5700` | **JHash:** `0xA3D675ED` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_IGNORE_LOW_PRIORITY_SHOCKING_EVENTS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IGNORE_LOW_PRIORITY_SHOCKING_EVENTS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_WANTED_LEVEL_MULTIPLIER

**Hash:** `0x020E5F00CDA207BA` | **JHash:** `0x1359292F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WANTED_LEVEL_MULTIPLIER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WANTED_LEVEL_MULTIPLIER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_WANTED_LEVEL_DIFFICULTY

**Hash:** `0x9B0BB33B04405E7A` | **JHash:** `0xB552626C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WANTED_LEVEL_DIFFICULTY(Player player, float difficulty)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WANTED_LEVEL_DIFFICULTY, player, difficulty);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `difficulty` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Max value is 1.0

---

### RESET_WANTED_LEVEL_DIFFICULTY

**Hash:** `0xB9D0DD990DC141DD` | **JHash:** `0xA64C378D` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_WANTED_LEVEL_DIFFICULTY(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_WANTED_LEVEL_DIFFICULTY, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### GET_WANTED_LEVEL_TIME_TO_ESCAPE

**Hash:** `0xA72200F51875FEA4` | **Build:** `2372`

**C++ 原型:**
```cpp
int GET_WANTED_LEVEL_TIME_TO_ESCAPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WANTED_LEVEL_TIME_TO_ESCAPE);
```

**返回值:** `int` → `int`

---

### SET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME

**Hash:** `0x49B856B1360C47C7` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME(Player player, int wantedLevel, int lossTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME, player, wantedLevel, lossTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `wantedLevel` | `int` | `int` | |
| `lossTime` | `int` | `int` | |

**返回值:** `void` → `void`

---

### RESET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME

**Hash:** `0x823EC8E82BA45986` | **Build:** `2060`

**C++ 原型:**
```cpp
void RESET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_WANTED_LEVEL_HIDDEN_ESCAPE_TIME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### START_FIRING_AMNESTY

**Hash:** `0xBF9BD71691857E48` | **JHash:** `0x5F8A22A6` | **Build:** `323`

**C++ 原型:**
```cpp
void START_FIRING_AMNESTY(int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_FIRING_AMNESTY, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REPORT_CRIME

**Hash:** `0xE9B09589827545E7` | **JHash:** `0xD8EB3A44` | **Build:** `323`

**C++ 原型:**
```cpp
void REPORT_CRIME(Player player, int crimeType, int wantedLvlThresh)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPORT_CRIME, player, crimeType, wantedLvlThresh);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `crimeType` | `int` | `int` | |
| `wantedLvlThresh` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** PLAYER::REPORT_CRIME(PLAYER::PLAYER_ID(), 37, PLAYER::GET_WANTED_LEVEL_THRESHOLD(1));

From am_armybase.ysc.c4:

PLAYER::REPORT_CRIME(PLAYER::PLAYER_ID(4), 36, PLAYER::GET_WANTED_LEVEL_THRESHOLD(4));

-----

This was taken from the GTAV.exe v1.334. The function is called sub_140592CE8. For a full decompilation of the function, see here: https://pastebin.com/09qSMsN7 

-----
crimeType:
1: Firearms possession
2: Person running a red light ("5-0-5")
3: Reckless driver
4: Speeding vehicle (a "5-10")
5: Traffic violation (a "5-0-5")
6: Motorcycle rider without a helmet
7: Vehicle theft (a "5-0-3")
8: Grand Theft Auto
9: ???
10: ???
11: Assault on a civilian (a "2-40")
12: Assault on an officer
13: Assault with a deadly weapon (a "2-45")
14: Officer shot (a "2-45")
15: Pedestrian struck by a vehicle
16: Officer struck by a vehicle
17: Helicopter down (an "AC"?)
18: Civilian on fire (a "2-40")
19: Officer set on fire (a "10-99")
20: Car on fire
21: Air unit down (an "AC"?)
22: An explosion (a "9-96")
23: A stabbing (a "2-45") (also something else I couldn't understand)
24: Officer stabbed (also something else I couldn't understand)
25: Attack on a vehicle ("MDV"?)
26: Damage to property
27: Suspect threatening officer with a firearm
28: Shots fired
29: ???
30: ???
31: ???
32: ???
33: ???
34: A "2-45"
35: ???
36: A "9-25"
37: ???
38: ???
39: ???
40: ???
41: ???
42: ???
43: Possible disturbance
44: Civilian in need of assistance
45: ???
46: ???

---

### SUPPRESS_CRIME_THIS_FRAME

**Hash:** `0x9A987297ED8BD838` | **JHash:** `0x59B5C2A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_CRIME_THIS_FRAME(Player player, int crimeType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_CRIME_THIS_FRAME, player, crimeType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `crimeType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** crimeType: see REPORT_CRIME

---

### UPDATE_WANTED_POSITION_THIS_FRAME

**Hash:** `0xBC9490CA15AEA8FB` | **JHash:** `0x6B34A160` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_WANTED_POSITION_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_WANTED_POSITION_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** This native is used in both singleplayer and multiplayer scripts.

Always used like this in scripts
PLAYER::UPDATE_WANTED_POSITION_THIS_FRAME(PLAYER::PLAYER_ID());

---

### SUPPRESS_LOSING_WANTED_LEVEL_IF_HIDDEN_THIS_FRAME

**Hash:** `0x4669B3ED80F24B4E` | **JHash:** `0xB9FB142F` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_LOSING_WANTED_LEVEL_IF_HIDDEN_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_LOSING_WANTED_LEVEL_IF_HIDDEN_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** This has been found in use in the decompiled files.

---

### ALLOW_EVASION_HUD_IF_DISABLING_HIDDEN_EVASION_THIS_FRAME

**Hash:** `0x2F41A3BAE005E5FA` | **Build:** `372`

**C++ 原型:**
```cpp
void ALLOW_EVASION_HUD_IF_DISABLING_HIDDEN_EVASION_THIS_FRAME(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_EVASION_HUD_IF_DISABLING_HIDDEN_EVASION_THIS_FRAME, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### FORCE_START_HIDDEN_EVASION

**Hash:** `0xAD73CE5A09E42D12` | **JHash:** `0x85725848` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_START_HIDDEN_EVASION(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_START_HIDDEN_EVASION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** This has been found in use in the decompiled files.

---

### SUPPRESS_WITNESSES_CALLING_POLICE_THIS_FRAME

**Hash:** `0x36F1B38855F2A8DF` | **JHash:** `0x3A7E5FB6` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_WITNESSES_CALLING_POLICE_THIS_FRAME(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_WITNESSES_CALLING_POLICE_THIS_FRAME, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### REPORT_POLICE_SPOTTED_PLAYER

**Hash:** `0xDC64D2C53493ED12` | **JHash:** `0xD15C4B1C` | **Build:** `323`

**C++ 原型:**
```cpp
void REPORT_POLICE_SPOTTED_PLAYER(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPORT_POLICE_SPOTTED_PLAYER, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_LAW_RESPONSE_DELAY_OVERRIDE

**Hash:** `0xB45EFF719D8427A6` | **JHash:** `0xBF6993C7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LAW_RESPONSE_DELAY_OVERRIDE(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LAW_RESPONSE_DELAY_OVERRIDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** PLAYER::SET_LAW_RESPONSE_DELAY_OVERRIDE(rPtr((&l_122) + 71)); // Found in decompilation

***

In "am_hold_up.ysc" used once:

l_8d._f47 = MISC::GET_RANDOM_FLOAT_IN_RANGE(18.0, 28.0);
PLAYER::SET_LAW_RESPONSE_DELAY_OVERRIDE((l_8d._f47));

---

### RESET_LAW_RESPONSE_DELAY_OVERRIDE

**Hash:** `0x0032A6DBA562C518` | **JHash:** `0x47CAB814` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_LAW_RESPONSE_DELAY_OVERRIDE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_LAW_RESPONSE_DELAY_OVERRIDE);
```

**返回值:** `void` → `void`

---

### CAN_PLAYER_START_MISSION

**Hash:** `0xDE7465A27D403C06` | **JHash:** `0x39E3CB3F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PLAYER_START_MISSION(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PLAYER_START_MISSION, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_READY_FOR_CUTSCENE

**Hash:** `0x908CBECC2CAA3690` | **JHash:** `0xBB77E9CD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_READY_FOR_CUTSCENE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_READY_FOR_CUTSCENE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_TARGETTING_ENTITY

**Hash:** `0x7912F7FC4F6264B6` | **JHash:** `0xF3240B77` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_TARGETTING_ENTITY(Player player, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_TARGETTING_ENTITY, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PLAYER_TARGET_ENTITY

**Hash:** `0x13EDE1A5DBF797C9` | **JHash:** `0xF6AAA2D7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_TARGET_ENTITY(Player player, Entity* entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_TARGET_ENTITY, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Assigns the handle of locked-on melee target to *entity that you pass it.
Returns false if no entity found.

---

### IS_PLAYER_FREE_AIMING

**Hash:** `0x2E397FD2ECD37C87` | **JHash:** `0x1DEC67B7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_FREE_AIMING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_FREE_AIMING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified player is currently aiming freely.

---

### IS_PLAYER_FREE_AIMING_AT_ENTITY

**Hash:** `0x3C06B5C839B38F7B` | **JHash:** `0x7D80EEAA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_FREE_AIMING_AT_ENTITY(Player player, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_FREE_AIMING_AT_ENTITY, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets a value indicating whether the specified player is currently aiming freely at the specified entity.

---

### GET_ENTITY_PLAYER_IS_FREE_AIMING_AT

**Hash:** `0x2975C866E6713290` | **JHash:** `0x8866D9D0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_ENTITY_PLAYER_IS_FREE_AIMING_AT(Player player, Entity* entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ENTITY_PLAYER_IS_FREE_AIMING_AT, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns TRUE if it found an entity in your crosshair within range of your weapon. Assigns the handle of the target to the *entity that you pass it.
Returns false if no entity found.

---

### SET_PLAYER_LOCKON_RANGE_OVERRIDE

**Hash:** `0x29961D490E5814FD` | **JHash:** `0x74D42C03` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_LOCKON_RANGE_OVERRIDE(Player player, float range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_LOCKON_RANGE_OVERRIDE, player, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `range` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Affects the range of auto aim target.

---

### SET_PLAYER_CAN_DO_DRIVE_BY

**Hash:** `0x6E8834B52EC20C77` | **JHash:** `0xF4D99685` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_DO_DRIVE_BY(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_DO_DRIVE_BY, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Set whether this player should be able to do drive-bys.

"A drive-by is when a ped is aiming/shooting from vehicle. This includes middle finger taunts. By setting this value to false I confirm the player is unable to do all that. Tested on tick."


---

### SET_PLAYER_CAN_BE_HASSLED_BY_GANGS

**Hash:** `0xD5E460AD7020A246` | **JHash:** `0x71B305BB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_BE_HASSLED_BY_GANGS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_BE_HASSLED_BY_GANGS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether this player can be hassled by gangs.

---

### SET_PLAYER_CAN_USE_COVER

**Hash:** `0xD465A8599DFF6814` | **JHash:** `0x13CAFAFA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_USE_COVER(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_USE_COVER, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets whether this player can take cover.

---

### GET_MAX_WANTED_LEVEL

**Hash:** `0x462E0DB9B137DC5F` | **JHash:** `0x457F1E44` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_WANTED_LEVEL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_WANTED_LEVEL);
```

**返回值:** `int` → `int`

**说明:** Gets the maximum wanted level the player can get.
Ranges from 0 to 5.

---

### IS_PLAYER_TARGETTING_ANYTHING

**Hash:** `0x78CFE51896B6B8A4` | **JHash:** `0x456DB50D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_TARGETTING_ANYTHING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_TARGETTING_ANYTHING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_SPRINT

**Hash:** `0xA01B8075D8B92DF4` | **JHash:** `0x7DD7900C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SPRINT(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SPRINT, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RESET_PLAYER_STAMINA

**Hash:** `0xA6F312FCCE9C1DFE` | **JHash:** `0xC0445A9C` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PLAYER_STAMINA(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PLAYER_STAMINA, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### RESTORE_PLAYER_STAMINA

**Hash:** `0xA352C1B864CAFD33` | **JHash:** `0x62A93608` | **Build:** `323`

**C++ 原型:**
```cpp
void RESTORE_PLAYER_STAMINA(Player player, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESTORE_PLAYER_STAMINA, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_SPRINT_STAMINA_REMAINING

**Hash:** `0x3F9F16F8E65A7ED7` | **JHash:** `0x47017C90` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PLAYER_SPRINT_STAMINA_REMAINING(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PLAYER_SPRINT_STAMINA_REMAINING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### GET_PLAYER_SPRINT_TIME_REMAINING

**Hash:** `0x1885BC9B108B4C99` | **JHash:** `0x40E80543` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PLAYER_SPRINT_TIME_REMAINING(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PLAYER_SPRINT_TIME_REMAINING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### GET_PLAYER_UNDERWATER_TIME_REMAINING

**Hash:** `0xA1FCF8E6AF40B731` | **JHash:** `0x1317125A` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PLAYER_UNDERWATER_TIME_REMAINING(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PLAYER_UNDERWATER_TIME_REMAINING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### SET_PLAYER_UNDERWATER_BREATH_PERCENT_REMAINING

**Hash:** `0xA0D3E4F7AAFB7E78` | **Build:** `757`

**C++ 原型:**
```cpp
float SET_PLAYER_UNDERWATER_BREATH_PERCENT_REMAINING(Player player, float time)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.SET_PLAYER_UNDERWATER_BREATH_PERCENT_REMAINING, player, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `time` | `float` | `float` | |

**返回值:** `float` → `float`

---

### GET_PLAYER_GROUP

**Hash:** `0x0D127585F77030AF` | **JHash:** `0xA5EDCDE8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_GROUP(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_GROUP, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the group ID the player is member of.

---

### GET_PLAYER_MAX_ARMOUR

**Hash:** `0x92659B4CE1863CB3` | **JHash:** `0x02A50657` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_MAX_ARMOUR(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_MAX_ARMOUR, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### IS_PLAYER_CONTROL_ON

**Hash:** `0x49C32D60007AFA47` | **JHash:** `0x618857F2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_CONTROL_ON(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_CONTROL_ON, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Can the player control himself, used to disable controls for player for things like a cutscene.

---

You can't disable controls with this, use SET_PLAYER_CONTROL(...) for this. 

---

### GET_ARE_CAMERA_CONTROLS_DISABLED

**Hash:** `0x7C814D2FB49F40C0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_ARE_CAMERA_CONTROLS_DISABLED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ARE_CAMERA_CONTROLS_DISABLED);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true when the player is not able to control the cam i.e. when running a benchmark test, switching the player or viewing a cutscene.

Note: I am not 100% sure if the native actually checks if the cam control is disabled but it seems promising.

---

### IS_PLAYER_SCRIPT_CONTROL_ON

**Hash:** `0x8A876A65283DD7D7` | **JHash:** `0x61B00A84` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_SCRIPT_CONTROL_ON(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_SCRIPT_CONTROL_ON, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_CLIMBING

**Hash:** `0x95E8F73DC65EFB9C` | **JHash:** `0x4A9E9AE0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_CLIMBING(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_CLIMBING, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns TRUE if the player ('s ped) is climbing at the moment.

---

### IS_PLAYER_BEING_ARRESTED

**Hash:** `0x388A47C51ABDAC8E` | **JHash:** `0x7F6A60D3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_BEING_ARRESTED(Player player, BOOL atArresting)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_BEING_ARRESTED, player, atArresting);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `atArresting` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Return true while player is being arrested / busted.

If atArresting is set to 1, this function will return 1 when player is being arrested (while player is putting his hand up, but still have control)

If atArresting is set to 0, this function will return 1 only when the busted screen is shown.

---

### RESET_PLAYER_ARREST_STATE

**Hash:** `0x2D03E13C460760D6` | **JHash:** `0x453C7CAB` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PLAYER_ARREST_STATE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PLAYER_ARREST_STATE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### GET_PLAYERS_LAST_VEHICLE

**Hash:** `0xB6997A7EB3F5C8C0` | **JHash:** `0xE2757AC1` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_PLAYERS_LAST_VEHICLE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYERS_LAST_VEHICLE);
```

**返回值:** `Vehicle` → `int`

**说明:** Alternative: GET_VEHICLE_PED_IS_IN(PLAYER_PED_ID(), 1);

---

### GET_PLAYER_INDEX

**Hash:** `0xA5EDC40EF369B48D` | **JHash:** `0x309BBDC1` | **Build:** `323`

**C++ 原型:**
```cpp
Player GET_PLAYER_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_INDEX);
```

**返回值:** `Player` → `int`

**说明:** Returns the same as PLAYER_ID and NETWORK_PLAYER_ID_TO_INT

---

### INT_TO_PLAYERINDEX

**Hash:** `0x41BD2A6B006AF756` | **JHash:** `0x98DD98F1` | **Build:** `323`

**C++ 原型:**
```cpp
Player INT_TO_PLAYERINDEX(int value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.INT_TO_PLAYERINDEX, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `Player` → `int`

**说明:** Simply returns whatever is passed to it (Regardless of whether the handle is valid or not).

---

### INT_TO_PARTICIPANTINDEX

**Hash:** `0x9EC6603812C24710` | **JHash:** `0x98F3B274` | **Build:** `323`

**C++ 原型:**
```cpp
int INT_TO_PARTICIPANTINDEX(int value)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.INT_TO_PARTICIPANTINDEX, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Simply returns whatever is passed to it (Regardless of whether the handle is valid or not).
--------------------------------------------------------
if (NETWORK::NETWORK_IS_PARTICIPANT_ACTIVE(PLAYER::INT_TO_PARTICIPANTINDEX(i)))


---

### GET_TIME_SINCE_PLAYER_HIT_VEHICLE

**Hash:** `0x5D35ECF3A81A0EE0` | **JHash:** `0x6E9B8B9E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_PLAYER_HIT_VEHICLE(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_PLAYER_HIT_VEHICLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### GET_TIME_SINCE_PLAYER_HIT_PED

**Hash:** `0xE36A25322DC35F42` | **JHash:** `0xB6209195` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_PLAYER_HIT_PED(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_PLAYER_HIT_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### GET_TIME_SINCE_PLAYER_DROVE_ON_PAVEMENT

**Hash:** `0xD559D2BE9E37853B` | **JHash:** `0x8836E732` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_PLAYER_DROVE_ON_PAVEMENT(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_PLAYER_DROVE_ON_PAVEMENT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### GET_TIME_SINCE_PLAYER_DROVE_AGAINST_TRAFFIC

**Hash:** `0xDB89591E290D9182` | **JHash:** `0x9F27D00E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_PLAYER_DROVE_AGAINST_TRAFFIC(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_PLAYER_DROVE_AGAINST_TRAFFIC, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### IS_PLAYER_FREE_FOR_AMBIENT_TASK

**Hash:** `0xDCCFD3F106C36AB4` | **JHash:** `0x85C7E232` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_FREE_FOR_AMBIENT_TASK(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_FREE_FOR_AMBIENT_TASK, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLAYER_ID

**Hash:** `0x4F8644AF03D0E0D6` | **JHash:** `0x8AEA886C` | **Build:** `323`

**C++ 原型:**
```cpp
Player PLAYER_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PLAYER_ID);
```

**返回值:** `Player` → `int`

**说明:** This returns YOUR 'identity' as a Player type.

Always returns 0 in story mode.

---

### PLAYER_PED_ID

**Hash:** `0xD80958FC74E988A6` | **JHash:** `0xFA92E226` | **Build:** `323`

**C++ 原型:**
```cpp
Ped PLAYER_PED_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PLAYER_PED_ID);
```

**返回值:** `Ped` → `int`

**说明:** Returns current player ped

---

### NETWORK_PLAYER_ID_TO_INT

**Hash:** `0xEE68096F9F37341E` | **JHash:** `0x8DD5B838` | **Build:** `323`

**C++ 原型:**
```cpp
int NETWORK_PLAYER_ID_TO_INT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.NETWORK_PLAYER_ID_TO_INT);
```

**返回值:** `int` → `int`

**说明:** Does exactly the same thing as PLAYER_ID()

---

### HAS_FORCE_CLEANUP_OCCURRED

**Hash:** `0xC968670BFACE42D9` | **JHash:** `0x4B37333C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_FORCE_CLEANUP_OCCURRED(int cleanupFlags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_FORCE_CLEANUP_OCCURRED, cleanupFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cleanupFlags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FORCE_CLEANUP

**Hash:** `0xBC8983F38F78ED51` | **JHash:** `0xFDAAEA2B` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLEANUP(int cleanupFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLEANUP, cleanupFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cleanupFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** used with 1,2,8,64,128 in the scripts

---

### FORCE_CLEANUP_FOR_ALL_THREADS_WITH_THIS_NAME

**Hash:** `0x4C68DDDDF0097317` | **JHash:** `0x04256C73` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLEANUP_FOR_ALL_THREADS_WITH_THIS_NAME(const char* name, int cleanupFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLEANUP_FOR_ALL_THREADS_WITH_THIS_NAME, name, cleanupFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `cleanupFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** PLAYER::FORCE_CLEANUP_FOR_ALL_THREADS_WITH_THIS_NAME("pb_prostitute", 1); // Found in decompilation

---

### FORCE_CLEANUP_FOR_THREAD_WITH_THIS_ID

**Hash:** `0xF745B37630DF176B` | **JHash:** `0x882D3EB3` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLEANUP_FOR_THREAD_WITH_THIS_ID(int id, int cleanupFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLEANUP_FOR_THREAD_WITH_THIS_ID, id, cleanupFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `cleanupFlags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_CAUSE_OF_MOST_RECENT_FORCE_CLEANUP

**Hash:** `0x9A41CF4674A12272` | **JHash:** `0x39AA9FC8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CAUSE_OF_MOST_RECENT_FORCE_CLEANUP(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CAUSE_OF_MOST_RECENT_FORCE_CLEANUP);
```

**返回值:** `int` → `int`

---

### SET_PLAYER_MAY_ONLY_ENTER_THIS_VEHICLE

**Hash:** `0x8026FF78F208978A` | **JHash:** `0xA454DD29` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MAY_ONLY_ENTER_THIS_VEHICLE(Player player, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MAY_ONLY_ENTER_THIS_VEHICLE, player, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_MAY_NOT_ENTER_ANY_VEHICLE

**Hash:** `0x1DE37BBF9E9CC14A` | **JHash:** `0xAF7AFCC4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MAY_NOT_ENTER_ANY_VEHICLE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MAY_NOT_ENTER_ANY_VEHICLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### GIVE_ACHIEVEMENT_TO_PLAYER

**Hash:** `0xBEC7076D64130195` | **JHash:** `0x822BC992` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GIVE_ACHIEVEMENT_TO_PLAYER(int achievementId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GIVE_ACHIEVEMENT_TO_PLAYER, achievementId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `achievementId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** 1 - Welcome to Los Santos
2 - A Friendship Resurrected
3 - A Fair Day's Pay
4 - The Moment of Truth
5 - To Live or Die in Los Santos
6 - Diamond Hard
7 - Subversive
8 - Blitzed
9 - Small Town, Big Job
10 - The Government Gimps
11 - The Big One!
12 - Solid Gold, Baby!
13 - Career Criminal
14 - San Andreas Sightseer
15 - All's Fare in Love and War
16 - TP Industries Arms Race
17 - Multi-Disciplined
18 - From Beyond the Stars
19 - A Mystery, Solved
20 - Waste Management
21 - Red Mist
22 - Show Off
23 - Kifflom!
24 - Three Man Army
25 - Out of Your Depth
26 - Altruist Acolyte
27 - A Lot of Cheddar
28 - Trading Pure Alpha
29 - Pimp My Sidearm
30 - Wanted: Alive Or Alive
31 - Los Santos Customs
32 - Close Shave
33 - Off the Plane
34 - Three-Bit Gangster
35 - Making Moves
36 - Above the Law
37 - Numero Uno
38 - The Midnight Club
39 - Unnatural Selection
40 - Backseat Driver
41 - Run Like The Wind
42 - Clean Sweep
43 - Decorated
44 - Stick Up Kid
45 - Enjoy Your Stay
46 - Crew Cut
47 - Full Refund
48 - Dialling Digits
49 - American Dream
50 - A New Perspective
51 - Be Prepared
52 - In the Name of Science
53 - Dead Presidents
54 - Parole Day
55 - Shot Caller
56 - Four Way
57 - Live a Little
58 - Can't Touch This
59 - Mastermind
60 - Vinewood Visionary
61 - Majestic
62 - Humans of Los Santos
63 - First Time Director
64 - Animal Lover
65 - Ensemble Piece
66 - Cult Movie
67 - Location Scout
68 - Method Actor
69 - Cryptozoologist
70 - Getting Started
71 - The Data Breaches
72 - The Bogdan Problem
73 - The Doomsday Scenario
74 - A World Worth Saving
75 - Orbital Obliteration
76 - Elitist
77 - Masterminds

---

### SET_ACHIEVEMENT_PROGRESS

**Hash:** `0xC2AFFFDABBDC2C5C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_ACHIEVEMENT_PROGRESS(int achievementId, int progress)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_ACHIEVEMENT_PROGRESS, achievementId, progress);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `achievementId` | `int` | `int` | |
| `progress` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** For Steam.
Does nothing and always returns false in the retail version of the game.

---

### GET_ACHIEVEMENT_PROGRESS

**Hash:** `0x1C186837D0619335` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ACHIEVEMENT_PROGRESS(int achievementId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ACHIEVEMENT_PROGRESS, achievementId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `achievementId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** For Steam.
Always returns 0 in retail version of the game.

---

### HAS_ACHIEVEMENT_BEEN_PASSED

**Hash:** `0x867365E111A3B6EB` | **JHash:** `0x136A5BE9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ACHIEVEMENT_BEEN_PASSED(int achievementId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ACHIEVEMENT_BEEN_PASSED, achievementId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `achievementId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** See GIVE_ACHIEVEMENT_TO_PLAYER

---

### IS_PLAYER_ONLINE

**Hash:** `0xF25D331DC2627BBC` | **JHash:** `0x9FAB6729` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_ONLINE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_ONLINE);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns TRUE if the game is in online mode and FALSE if in offline mode.

This is an alias for NETWORK_IS_SIGNED_ONLINE.

---

### IS_PLAYER_LOGGING_IN_NP

**Hash:** `0x74556E1420867ECA` | **JHash:** `0x8F72FAD0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_LOGGING_IN_NP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_LOGGING_IN_NP);
```

**返回值:** `BOOL` → `bool`

**说明:** this function is hard-coded to always return 0.

---

### DISPLAY_SYSTEM_SIGNIN_UI

**Hash:** `0x94DD7888C10A979E` | **JHash:** `0x4264CED2` | **Build:** `323`

**C++ 原型:**
```cpp
void DISPLAY_SYSTEM_SIGNIN_UI(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISPLAY_SYSTEM_SIGNIN_UI, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Purpose of the BOOL currently unknown.
Both, true and false, work

---

### IS_SYSTEM_UI_BEING_DISPLAYED

**Hash:** `0x5D511E3867C87139` | **JHash:** `0xE495B6DA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SYSTEM_UI_BEING_DISPLAYED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SYSTEM_UI_BEING_DISPLAYED);
```

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_INVINCIBLE

**Hash:** `0x239528EACDC3E7DE` | **JHash:** `0xDFB9A2A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_INVINCIBLE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_INVINCIBLE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Simply sets you as invincible (Health will not deplete).

Use 0x733A643B5B0C53C1 instead if you want Ragdoll enabled, which is equal to:
*(DWORD *)(playerPedAddress + 0x188) |= (1 << 9);

---

### GET_PLAYER_INVINCIBLE

**Hash:** `0xB721981B2B939E07` | **JHash:** `0x680C90EE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_INVINCIBLE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_INVINCIBLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns the Player's Invincible status.

This function will always return false if 0x733A643B5B0C53C1 is used to set the invincibility status. To always get the correct result, use this:

 bool IsPlayerInvincible(Player player)
    {
     auto addr = getScriptHandleBaseAddress(GET_PLAYER_PED(player)); 

        if (addr)
     {
         DWORD flag = *(DWORD *)(addr + 0x188);
            return ((flag & (1 << 8)) != 0) || ((flag & (1 << 9)) != 0);
      }

       return false;
 }



---

### GET_PLAYER_DEBUG_INVINCIBLE

**Hash:** `0xDCC07526B8EC45AF` | **Build:** `1868`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_DEBUG_INVINCIBLE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_DEBUG_INVINCIBLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Always returns false.

---

### SET_PLAYER_INVINCIBLE_BUT_HAS_REACTIONS

**Hash:** `0x6BC97F4F4BB3C04B` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_PLAYER_INVINCIBLE_BUT_HAS_REACTIONS(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_INVINCIBLE_BUT_HAS_REACTIONS, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_CAN_COLLECT_DROPPED_MONEY

**Hash:** `0xCAC57395B151135F` | **JHash:** `0x00563E0D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_COLLECT_DROPPED_MONEY(Player player, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_COLLECT_DROPPED_MONEY, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_PLAYER_HELMET

**Hash:** `0xF3AC26D3CC576528` | **JHash:** `0x6255F3B4` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PLAYER_HELMET(Player player, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PLAYER_HELMET, player, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GIVE_PLAYER_RAGDOLL_CONTROL

**Hash:** `0x3C49C870E66F0A28` | **JHash:** `0xC7B4D7AC` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_PLAYER_RAGDOLL_CONTROL(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_PLAYER_RAGDOLL_CONTROL, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_LOCKON

**Hash:** `0x5C8B2F450EE4328E` | **JHash:** `0x0B270E0F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_LOCKON(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_LOCKON, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example from fm_mission_controler.ysc.c4:

PLAYER::SET_PLAYER_LOCKON(PLAYER::PLAYER_ID(), 1);

All other decompiled scripts using this seem to be using the player id as the first parameter, so I feel the need to confirm it as so.

No need to confirm it says PLAYER_ID() so it uses PLAYER_ID() lol.

---

### SET_PLAYER_TARGETING_MODE

**Hash:** `0xB1906895227793F3` | **JHash:** `0x61CAE253` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_TARGETING_MODE(int targetMode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_TARGETING_MODE, targetMode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `targetMode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets your targeting mode.
0 = Assisted Aim - Full
1 = Assisted Aim - Partial
2 = Free Aim - Assisted
3 = Free Aim

---

### GET_PLAYER_TARGETING_MODE

**Hash:** `0x875BDD898B99C8CE` | **Build:** `3570`

**C++ 原型:**
```cpp
int GET_PLAYER_TARGETING_MODE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_TARGETING_MODE);
```

**返回值:** `int` → `int`

---

### SET_PLAYER_TARGET_LEVEL

**Hash:** `0x5702B917B99DB1CD` | **JHash:** `0x772DA539` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_TARGET_LEVEL(int targetLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_TARGET_LEVEL, targetLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `targetLevel` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_IS_USING_FPS_THIRD_PERSON_COVER

**Hash:** `0xB9CF1F793A9F1BF1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_USING_FPS_THIRD_PERSON_COVER(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_USING_FPS_THIRD_PERSON_COVER);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns profile setting 237.

---

### GET_IS_USING_HOOD_CAMERA

**Hash:** `0xCB645E85E97EA48B` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL GET_IS_USING_HOOD_CAMERA(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_USING_HOOD_CAMERA);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns profile setting 243.

---

### CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_PED

**Hash:** `0xF0B67A4DE6AB5F98` | **JHash:** `0x1D31CBBD` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_PED(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### HAS_PLAYER_DAMAGED_AT_LEAST_ONE_PED

**Hash:** `0x20CE80B0C2BF4ACC` | **JHash:** `0x14F52453` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PLAYER_DAMAGED_AT_LEAST_ONE_PED(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PLAYER_DAMAGED_AT_LEAST_ONE_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED

**Hash:** `0x4AACB96203D11A31` | **JHash:** `0x7E3BFBC5` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_HAS_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### HAS_PLAYER_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED

**Hash:** `0xE4B90F367BD81752` | **JHash:** `0xA3707DFC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PLAYER_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PLAYER_DAMAGED_AT_LEAST_ONE_NON_ANIMAL_PED, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_AIR_DRAG_MULTIPLIER_FOR_PLAYERS_VEHICLE

**Hash:** `0xCA7DC8329F0A1E9E` | **JHash:** `0xF20F72E5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AIR_DRAG_MULTIPLIER_FOR_PLAYERS_VEHICLE(Player player, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AIR_DRAG_MULTIPLIER_FOR_PLAYERS_VEHICLE, player, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This can be between 1.0f - 14.9f 

You can change the max in IDA from 15.0. I say 15.0 as the function blrs if what you input is greater than or equal to 15.0 hence why it's 14.9 max default.



---

### SET_SWIM_MULTIPLIER_FOR_PLAYER

**Hash:** `0xA91C6F0FF7D16A13` | **JHash:** `0xB986FF47` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SWIM_MULTIPLIER_FOR_PLAYER(Player player, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SWIM_MULTIPLIER_FOR_PLAYER, player, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Swim speed multiplier.
Multiplier goes up to 1.49

Just call it one time, it is not required to be called once every tick. - Note copied from below native.

Note: At least the IDA method if you change the max float multiplier from 1.5 it will change it for both this and RUN_SPRINT below. I say 1.5 as the function blrs if what you input is greater than or equal to 1.5 hence why it's 1.49 max default.

---

### SET_RUN_SPRINT_MULTIPLIER_FOR_PLAYER

**Hash:** `0x6DB47AA77FD94E09` | **JHash:** `0x825423C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RUN_SPRINT_MULTIPLIER_FOR_PLAYER(Player player, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RUN_SPRINT_MULTIPLIER_FOR_PLAYER, player, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Multiplier goes up to 1.49 any value above will be completely overruled by the game and the multiplier will not take effect, this can be edited in memory however.

Just call it one time, it is not required to be called once every tick.

Note: At least the IDA method if you change the max float multiplier from 1.5 it will change it for both this and SWIM above. I say 1.5 as the function blrs if what you input is greater than or equal to 1.5 hence why it's 1.49 max default.

---

### GET_TIME_SINCE_LAST_ARREST

**Hash:** `0x5063F92F07C2A316` | **JHash:** `0x62824EF4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_LAST_ARREST(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_LAST_ARREST);
```

**返回值:** `int` → `int`

**说明:** Returns the time since the character was arrested in (ms) milliseconds.

example

var time = Function.call<int>(Hash.GET_TIME_SINCE_LAST_ARREST();

UI.DrawSubtitle(time.ToString());

if player has not been arrested, the int returned will be -1.

---

### GET_TIME_SINCE_LAST_DEATH

**Hash:** `0xC7034807558DDFCA` | **JHash:** `0x24BC5AC0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_TIME_SINCE_LAST_DEATH(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TIME_SINCE_LAST_DEATH);
```

**返回值:** `int` → `int`

**说明:** Returns the time since the character died in (ms) milliseconds.

example

var time = Function.call<int>(Hash.GET_TIME_SINCE_LAST_DEATH();

UI.DrawSubtitle(time.ToString());

if player has not died, the int returned will be -1.

---

### ASSISTED_MOVEMENT_CLOSE_ROUTE

**Hash:** `0xAEBF081FFC0A0E5E` | **JHash:** `0xF23277F3` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_CLOSE_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_CLOSE_ROUTE);
```

**返回值:** `void` → `void`

---

### ASSISTED_MOVEMENT_FLUSH_ROUTE

**Hash:** `0x8621390F0CDCFE1F` | **JHash:** `0xD04568B9` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_FLUSH_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_FLUSH_ROUTE);
```

**返回值:** `void` → `void`

---

### SET_PLAYER_FORCED_AIM

**Hash:** `0x0FEE4F80AC44A726` | **JHash:** `0x94E42E2E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_FORCED_AIM(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_FORCED_AIM, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_FORCED_ZOOM

**Hash:** `0x75E7D505F2B15902` | **JHash:** `0xB0C576CB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_FORCED_ZOOM(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_FORCED_ZOOM, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_FORCE_SKIP_AIM_INTRO

**Hash:** `0x7651BC64AE59E128` | **JHash:** `0x374F42F0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_FORCE_SKIP_AIM_INTRO(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_FORCE_SKIP_AIM_INTRO, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_PLAYER_FIRING

**Hash:** `0x5E6CC07646BBEAB8` | **JHash:** `0x30CB28CB` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PLAYER_FIRING(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PLAYER_FIRING, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Inhibits the player from using any method of combat including melee and firearms.

NOTE: Only disables the firing for one frame

---

### DISABLE_PLAYER_THROW_GRENADE_WHILE_USING_GUN

**Hash:** `0xB885852C39CC265D` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PLAYER_THROW_GRENADE_WHILE_USING_GUN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PLAYER_THROW_GRENADE_WHILE_USING_GUN);
```

**返回值:** `void` → `void`

**说明:** Used only once in R* scripts (freemode.ysc).

---

### SET_DISABLE_AMBIENT_MELEE_MOVE

**Hash:** `0x2E8AABFA40A84F8C` | **JHash:** `0xCCD937E7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_AMBIENT_MELEE_MOVE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_AMBIENT_MELEE_MOVE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_MAX_ARMOUR

**Hash:** `0x77DFCCF5948B8C71` | **JHash:** `0xC6C3C53B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MAX_ARMOUR(Player player, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MAX_ARMOUR, player, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Default is 100. Use player id and not ped id. For instance: PLAYER::SET_PLAYER_MAX_ARMOUR(PLAYER::PLAYER_ID(), 100); // main_persistent.ct4

---

### SPECIAL_ABILITY_ACTIVATE

**Hash:** `0x821FDC827D6F4090` | **Build:** `678`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_ACTIVATE(Player player, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_ACTIVATE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0 in the scripts

---

### SET_SPECIAL_ABILITY_MP

**Hash:** `0xB214D570EAD7F81A` | **Build:** `678`

**C++ 原型:**
```cpp
void SET_SPECIAL_ABILITY_MP(Player player, int p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPECIAL_ABILITY_MP, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_DEACTIVATE_MP

**Hash:** `0x17F7471EACA78290` | **Build:** `678`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_DEACTIVATE_MP(Player player, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_DEACTIVATE_MP, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0 in the scripts

---

### SPECIAL_ABILITY_DEACTIVATE

**Hash:** `0xD6A953C6D1492057` | **JHash:** `0x80C2AB09` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_DEACTIVATE(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_DEACTIVATE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_DEACTIVATE_FAST

**Hash:** `0x9CB5CE07A3968D5A` | **JHash:** `0x0751908A` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_DEACTIVATE_FAST(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_DEACTIVATE_FAST, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_RESET

**Hash:** `0x375F0E738F861A94` | **JHash:** `0xA7D8BCD3` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_RESET(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_RESET, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_CHARGE_ON_MISSION_FAILED

**Hash:** `0xC9A763D8FE87436A` | **JHash:** `0x4136829A` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_ON_MISSION_FAILED(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_ON_MISSION_FAILED, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_CHARGE_SMALL

**Hash:** `0x2E7B9B683481687D` | **JHash:** `0x6F463F56` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_SMALL(Player player, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_SMALL, player, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Every occurrence of p1 & p2 were both true.

---

### SPECIAL_ABILITY_CHARGE_MEDIUM

**Hash:** `0xF113E3AA9BC54613` | **JHash:** `0xAB55D8F3` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_MEDIUM(Player player, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_MEDIUM, player, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Only 1 match. Both p1 & p2 were true.

---

### SPECIAL_ABILITY_CHARGE_LARGE

**Hash:** `0xF733F45FA4497D93` | **JHash:** `0xF440C04D` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_LARGE(Player player, BOOL p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_LARGE, player, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** 2 matches. p1 was always true.

---

### SPECIAL_ABILITY_CHARGE_CONTINUOUS

**Hash:** `0xED481732DFF7E997` | **JHash:** `0x5FEE98A2` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_CONTINUOUS(Player player, Ped p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_CONTINUOUS, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Ped` | `int` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 appears to always be 1 (only comes up twice)

---

### SPECIAL_ABILITY_CHARGE_ABSOLUTE

**Hash:** `0xB7B0870EB531D08D` | **JHash:** `0x72429998` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_ABSOLUTE(Player player, int p1, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_ABSOLUTE, player, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 appears as 5, 10, 15, 25, or 30. p2 is always true.

---

### SPECIAL_ABILITY_CHARGE_NORMALIZED

**Hash:** `0xA0696A65F009EE18` | **JHash:** `0x8C7E68C1` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_CHARGE_NORMALIZED(Player player, float normalizedValue, BOOL p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_CHARGE_NORMALIZED, player, normalizedValue, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `normalizedValue` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** 
normalizedValue is from 0.0 - 1.0
p2 is always 1

---

### SPECIAL_ABILITY_FILL_METER

**Hash:** `0x3DACA8DDC6FD4980` | **JHash:** `0xB71589DA` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_FILL_METER(Player player, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_FILL_METER, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Also known as _RECHARGE_SPECIAL_ABILITY

---

### SPECIAL_ABILITY_DEPLETE_METER

**Hash:** `0x1D506DBBBC51E64B` | **JHash:** `0x9F80F6DF` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_DEPLETE_METER(Player player, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_DEPLETE_METER, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** p1 was always true.

---

### SPECIAL_ABILITY_LOCK

**Hash:** `0x6A09D0D590A47D13` | **JHash:** `0x1B7BB388` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_LOCK(Hash playerModel, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_LOCK, playerModel, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `playerModel` | `Hash` | `uint` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SPECIAL_ABILITY_UNLOCK

**Hash:** `0xF145F3BE2EFA9A3B` | **JHash:** `0x1FDB2919` | **Build:** `323`

**C++ 原型:**
```cpp
void SPECIAL_ABILITY_UNLOCK(Hash playerModel, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SPECIAL_ABILITY_UNLOCK, playerModel, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `playerModel` | `Hash` | `uint` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_SPECIAL_ABILITY_UNLOCKED

**Hash:** `0xC6017F6A6CDFA694` | **JHash:** `0xC9C75E82` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPECIAL_ABILITY_UNLOCKED(Hash playerModel)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPECIAL_ABILITY_UNLOCKED, playerModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `playerModel` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_SPECIAL_ABILITY_ACTIVE

**Hash:** `0x3E5F7FC85D854E15` | **JHash:** `0x1B17E334` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPECIAL_ABILITY_ACTIVE(Player player, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPECIAL_ABILITY_ACTIVE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_SPECIAL_ABILITY_METER_FULL

**Hash:** `0x05A1FE504B7F2587` | **JHash:** `0x2E19D7F6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPECIAL_ABILITY_METER_FULL(Player player, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPECIAL_ABILITY_METER_FULL, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### ENABLE_SPECIAL_ABILITY

**Hash:** `0x181EC197DAEFE121` | **JHash:** `0xC86C1B4E` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_SPECIAL_ABILITY(Player player, BOOL toggle, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_SPECIAL_ABILITY, player, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_SPECIAL_ABILITY_ENABLED

**Hash:** `0xB1D200FE26AEF3CB` | **JHash:** `0xC01238CC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SPECIAL_ABILITY_ENABLED(Player player, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SPECIAL_ABILITY_ENABLED, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_SPECIAL_ABILITY_MULTIPLIER

**Hash:** `0xA49C426ED0CA4AB7` | **JHash:** `0xFF1BC556` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SPECIAL_ABILITY_MULTIPLIER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPECIAL_ABILITY_MULTIPLIER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UPDATE_SPECIAL_ABILITY_FROM_STAT

**Hash:** `0xFFEE8FA29AB9A18E` | **JHash:** `0x5D0FE25B` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_SPECIAL_ABILITY_FROM_STAT(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_SPECIAL_ABILITY_FROM_STAT, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_IS_PLAYER_DRIVING_ON_HIGHWAY

**Hash:** `0x5FC472C501CCADB3` | **JHash:** `0x46E7E31D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PLAYER_DRIVING_ON_HIGHWAY(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PLAYER_DRIVING_ON_HIGHWAY, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Appears once in "re_dealgonewrong"

---

### GET_IS_PLAYER_DRIVING_WRECKLESS

**Hash:** `0xF10B44FD479D69F3` | **JHash:** `0x1E359CC8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PLAYER_DRIVING_WRECKLESS(Player player, int p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PLAYER_DRIVING_WRECKLESS, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Only 1 occurrence. p1 was 2.

---

### GET_IS_MOPPING_AREA_FREE_IN_FRONT_OF_PLAYER

**Hash:** `0xDD2620B7B9D16FF1` | **JHash:** `0x8CB53C9F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_MOPPING_AREA_FREE_IN_FRONT_OF_PLAYER(Player player, float p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_MOPPING_AREA_FREE_IN_FRONT_OF_PLAYER, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** 2 occurrences in agency_heist3a. p1 was 0.7f then 0.4f.

---

### START_PLAYER_TELEPORT

**Hash:** `0xAD15F075A4DA0FDE` | **JHash:** `0xC552E06C` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PLAYER_TELEPORT(Player player, float x, float y, float z, float heading, BOOL p5, BOOL findCollisionLand, BOOL p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PLAYER_TELEPORT, player, x, y, z, heading, p5, findCollisionLand, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |
| `findCollisionLand` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** `findCollisionLand`: This teleports the player to land when set to true and will not consider the Z coordinate parameter provided by you. It will automatically put the Z coordinate so that you don't fall from sky.

---

### UPDATE_PLAYER_TELEPORT

**Hash:** `0xE23D5873C2394C61` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL UPDATE_PLAYER_TELEPORT(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.UPDATE_PLAYER_TELEPORT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STOP_PLAYER_TELEPORT

**Hash:** `0xC449EDED9D73009C` | **JHash:** `0x86AB8DBB` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PLAYER_TELEPORT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PLAYER_TELEPORT);
```

**返回值:** `void` → `void`

**说明:** Disables the player's teleportation

---

### IS_PLAYER_TELEPORT_ACTIVE

**Hash:** `0x02B15662D7F8886F` | **JHash:** `0x3A11D118` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_TELEPORT_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_TELEPORT_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### GET_PLAYER_CURRENT_STEALTH_NOISE

**Hash:** `0x2F395D61F3A1F877` | **JHash:** `0xC3B02362` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PLAYER_CURRENT_STEALTH_NOISE(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PLAYER_CURRENT_STEALTH_NOISE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### SET_PLAYER_HEALTH_RECHARGE_MULTIPLIER

**Hash:** `0x5DB660B38DD98A31` | **JHash:** `0x45514731` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_HEALTH_RECHARGE_MULTIPLIER(Player player, float regenRate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_HEALTH_RECHARGE_MULTIPLIER, player, regenRate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `regenRate` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** `regenRate`: The recharge multiplier, a value between 0.0 and 1.0.
Use 1.0 to reset it back to normal

---

### GET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT

**Hash:** `0x8BC515BAE4AAF8FF` | **Build:** `617`

**C++ 原型:**
```cpp
float GET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT(Player player)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `float` → `float`

---

### SET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT

**Hash:** `0xC388A0F065F5BC34` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT(Player player, float limit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_HEALTH_RECHARGE_MAX_PERCENT, player, limit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `limit` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DISABLE_PLAYER_HEALTH_RECHARGE

**Hash:** `0xBCB06442F7E52666` | **Build:** `2802`

**C++ 原型:**
```cpp
void DISABLE_PLAYER_HEALTH_RECHARGE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PLAYER_HEALTH_RECHARGE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

**说明:** Needs to be called every frame.

---

### SET_PLAYER_FALL_DISTANCE_TO_TRIGGER_RAGDOLL_OVERRIDE

**Hash:** `0xEFD79FA81DFBA9CB` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_PLAYER_FALL_DISTANCE_TO_TRIGGER_RAGDOLL_OVERRIDE(Player player, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_FALL_DISTANCE_TO_TRIGGER_RAGDOLL_OVERRIDE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_WEAPON_DAMAGE_MODIFIER

**Hash:** `0xCE07B9F7817AADA3` | **JHash:** `0xB02C2F39` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WEAPON_DAMAGE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WEAPON_DAMAGE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This modifies the damage value of your weapon. Whether it is a multiplier or base damage is unknown. 

Based on tests, it is unlikely to be a multiplier.

modifier's min value is 0.1

---

### SET_PLAYER_WEAPON_DEFENSE_MODIFIER

**Hash:** `0x2D83BC011CA14A3C` | **JHash:** `0xAE446344` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_WEAPON_DEFENSE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WEAPON_DEFENSE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_WEAPON_MINIGUN_DEFENSE_MODIFIER

**Hash:** `0xBCFDE9EDE4CF27DC` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_PLAYER_WEAPON_MINIGUN_DEFENSE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WEAPON_MINIGUN_DEFENSE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_MELEE_WEAPON_DAMAGE_MODIFIER

**Hash:** `0x4A3DC7ECCC321032` | **JHash:** `0x362E69AD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MELEE_WEAPON_DAMAGE_MODIFIER(Player player, float modifier, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MELEE_WEAPON_DAMAGE_MODIFIER, player, modifier, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_MELEE_WEAPON_DEFENSE_MODIFIER

**Hash:** `0xAE540335B4ABC4E2` | **JHash:** `0x9F3D577F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_MELEE_WEAPON_DEFENSE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MELEE_WEAPON_DEFENSE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_VEHICLE_DAMAGE_MODIFIER

**Hash:** `0xA50E117CDDF82F0C` | **JHash:** `0x823ECA63` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_VEHICLE_DAMAGE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_VEHICLE_DAMAGE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_VEHICLE_DEFENSE_MODIFIER

**Hash:** `0x4C60E6EFDAFF2462` | **JHash:** `0xA16626C7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_VEHICLE_DEFENSE_MODIFIER(Player player, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_VEHICLE_DEFENSE_MODIFIER, player, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** modifier's min value is 0.1

---

### SET_PLAYER_MAX_EXPLOSIVE_DAMAGE

**Hash:** `0x8D768602ADEF2245` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_PLAYER_MAX_EXPLOSIVE_DAMAGE(Player player, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_MAX_EXPLOSIVE_DAMAGE, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_EXPLOSIVE_DAMAGE_MODIFIER

**Hash:** `0xD821056B9ACF8052` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_PLAYER_EXPLOSIVE_DAMAGE_MODIFIER(Player player, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_EXPLOSIVE_DAMAGE_MODIFIER, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_WEAPON_TAKEDOWN_DEFENSE_MODIFIER

**Hash:** `0x31E90B8873A4CD3B` | **Build:** `617`

**C++ 原型:**
```cpp
void SET_PLAYER_WEAPON_TAKEDOWN_DEFENSE_MODIFIER(Player player, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_WEAPON_TAKEDOWN_DEFENSE_MODIFIER, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PARACHUTE_TINT_INDEX

**Hash:** `0xA3D0E54541D9A5E5` | **JHash:** `0x8EA12EDB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_TINT_INDEX(Player player, int tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_TINT_INDEX, player, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `tintIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Tints:
   None = -1,
    Rainbow = 0,
  Red = 1,
  SeasideStripes = 2,
   WidowMaker = 3,
   Patriot = 4,
  Blue = 5,
 Black = 6,
    Hornet = 7,
   AirFocce = 8,
 Desert = 9,
   Shadow = 10,
  HighAltitude = 11,
    Airbone = 12,
 Sunrise = 13,


---

### GET_PLAYER_PARACHUTE_TINT_INDEX

**Hash:** `0x75D3F7A1B0D9B145` | **JHash:** `0x432B0509` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PLAYER_PARACHUTE_TINT_INDEX(Player player, int* tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PLAYER_PARACHUTE_TINT_INDEX, player, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `tintIndex` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Tints:
  None = -1,
    Rainbow = 0,
  Red = 1,
  SeasideStripes = 2,
   WidowMaker = 3,
   Patriot = 4,
  Blue = 5,
 Black = 6,
    Hornet = 7,
   AirFocce = 8,
 Desert = 9,
   Shadow = 10,
  HighAltitude = 11,
    Airbone = 12,
 Sunrise = 13,

---

### SET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX

**Hash:** `0xAF04C87F5DC1DF38` | **JHash:** `0x70689638` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX(Player player, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX, player, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Tints:
   None = -1,
    Rainbow = 0,
  Red = 1,
  SeasideStripes = 2,
   WidowMaker = 3,
   Patriot = 4,
  Blue = 5,
 Black = 6,
    Hornet = 7,
   AirFocce = 8,
 Desert = 9,
   Shadow = 10,
  HighAltitude = 11,
    Airbone = 12,
 Sunrise = 13,

---

### GET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX

**Hash:** `0xD5A016BC3C09CF40` | **JHash:** `0x77B8EF01` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX(Player player, int* index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PLAYER_RESERVE_PARACHUTE_TINT_INDEX, player, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `index` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Tints:
  None = -1,
    Rainbow = 0,
  Red = 1,
  SeasideStripes = 2,
   WidowMaker = 3,
   Patriot = 4,
  Blue = 5,
 Black = 6,
    Hornet = 7,
   AirFocce = 8,
 Desert = 9,
   Shadow = 10,
  HighAltitude = 11,
    Airbone = 12,
 Sunrise = 13,

---

### SET_PLAYER_PARACHUTE_PACK_TINT_INDEX

**Hash:** `0x93B0FB27C9A04060` | **JHash:** `0xD79D5D1B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_PACK_TINT_INDEX(Player player, int tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_PACK_TINT_INDEX, player, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `tintIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** tints 0- 13
0 - unkown
1 - unkown
2 - unkown
3 - unkown
4 - unkown

---

### GET_PLAYER_PARACHUTE_PACK_TINT_INDEX

**Hash:** `0x6E9C742F340CE5A2` | **JHash:** `0x4E418E13` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PLAYER_PARACHUTE_PACK_TINT_INDEX(Player player, int* tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PLAYER_PARACHUTE_PACK_TINT_INDEX, player, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `tintIndex` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_HAS_RESERVE_PARACHUTE

**Hash:** `0x7DDAB28D31FAC363` | **JHash:** `0xA3E4798E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_HAS_RESERVE_PARACHUTE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_HAS_RESERVE_PARACHUTE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_HAS_RESERVE_PARACHUTE

**Hash:** `0x5DDFE2FF727F3CA3` | **JHash:** `0x30DA1DA1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_HAS_RESERVE_PARACHUTE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_HAS_RESERVE_PARACHUTE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_CAN_LEAVE_PARACHUTE_SMOKE_TRAIL

**Hash:** `0xF401B182DBA8AF53` | **JHash:** `0x832DEB7A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_LEAVE_PARACHUTE_SMOKE_TRAIL(Player player, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_LEAVE_PARACHUTE_SMOKE_TRAIL, player, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR

**Hash:** `0x8217FD371A4625CF` | **JHash:** `0x14FE9264` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR(Player player, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR, player, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR

**Hash:** `0xEF56DBABD3CD4887` | **JHash:** `0xF66E5CDD` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR(Player player, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_PLAYER_PARACHUTE_SMOKE_TRAIL_COLOR, player, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PHONE_PALETTE_IDX

**Hash:** `0x11D5F725F0E780E0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PHONE_PALETTE_IDX(Player player, int idx)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PHONE_PALETTE_IDX, player, idx);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `idx` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_NOISE_MULTIPLIER

**Hash:** `0xDB89EF50FF25FCE9` | **JHash:** `0x15786DD1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_NOISE_MULTIPLIER(Player player, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_NOISE_MULTIPLIER, player, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_SNEAKING_NOISE_MULTIPLIER

**Hash:** `0xB2C1A29588A9F47C` | **JHash:** `0x8D2D89C4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SNEAKING_NOISE_MULTIPLIER(Player player, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SNEAKING_NOISE_MULTIPLIER, player, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Values around 1.0f to 2.0f used in game scripts.

---

### CAN_PED_HEAR_PLAYER

**Hash:** `0xF297383AA91DCA29` | **JHash:** `0x1C70B2EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_PED_HEAR_PLAYER(Player player, Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_PED_HEAR_PLAYER, player, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SIMULATE_PLAYER_INPUT_GAIT

**Hash:** `0x477D5D63E63ECA5D` | **JHash:** `0x0D77CC34` | **Build:** `323`

**C++ 原型:**
```cpp
void SIMULATE_PLAYER_INPUT_GAIT(Player player, float amount, int gaitType, float speed, BOOL p4, BOOL p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SIMULATE_PLAYER_INPUT_GAIT, player, amount, gaitType, speed, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `amount` | `float` | `float` | |
| `gaitType` | `int` | `int` | |
| `speed` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This is to make the player walk without accepting input from INPUT.

gaitType is in increments of 100s. 2000, 500, 300, 200, etc.

p4 is always 1 and p5 is always 0.

C# Example :

Function.Call(Hash.SIMULATE_PLAYER_INPUT_GAIT, Game.Player, 1.0f, 100, 1.0f, 1, 0); //Player will go forward for 100ms

---

### RESET_PLAYER_INPUT_GAIT

**Hash:** `0x19531C47A2ABD691` | **JHash:** `0x4A701EE1` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_PLAYER_INPUT_GAIT(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_PLAYER_INPUT_GAIT, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_AUTO_GIVE_PARACHUTE_WHEN_ENTER_PLANE

**Hash:** `0x9F343285A00B4BB6` | **JHash:** `0xA97C2059` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUTO_GIVE_PARACHUTE_WHEN_ENTER_PLANE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUTO_GIVE_PARACHUTE_WHEN_ENTER_PLANE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AUTO_GIVE_SCUBA_GEAR_WHEN_EXIT_VEHICLE

**Hash:** `0xD2B315B6689D537D` | **JHash:** `0xA25D767E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AUTO_GIVE_SCUBA_GEAR_WHEN_EXIT_VEHICLE(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AUTO_GIVE_SCUBA_GEAR_WHEN_EXIT_VEHICLE, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_STEALTH_PERCEPTION_MODIFIER

**Hash:** `0x4E9021C1FCDD507A` | **JHash:** `0x3D26105F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_STEALTH_PERCEPTION_MODIFIER(Player player, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_STEALTH_PERCEPTION_MODIFIER, player, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_REMOTE_PLAYER_IN_NON_CLONED_VEHICLE

**Hash:** `0x690A61A6D13583F6` | **JHash:** `0x1D371529` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REMOTE_PLAYER_IN_NON_CLONED_VEHICLE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REMOTE_PLAYER_IN_NON_CLONED_VEHICLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### INCREASE_PLAYER_JUMP_SUPPRESSION_RANGE

**Hash:** `0x9EDD76E87D5D51BA` | **JHash:** `0xE30A64DC` | **Build:** `323`

**C++ 原型:**
```cpp
void INCREASE_PLAYER_JUMP_SUPPRESSION_RANGE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INCREASE_PLAYER_JUMP_SUPPRESSION_RANGE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_SIMULATE_AIMING

**Hash:** `0xC54C95DA968EC5B5` | **JHash:** `0xF1E0CAFC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SIMULATE_AIMING(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SIMULATE_AIMING, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_CLOTH_PIN_FRAMES

**Hash:** `0x749FADDF97DFE930` | **JHash:** `0xF7A0F00F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CLOTH_PIN_FRAMES(Player player, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CLOTH_PIN_FRAMES, player, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_CLOTH_PACKAGE_INDEX

**Hash:** `0x9F7BBA2EA6372500` | **JHash:** `0xB8209F16` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CLOTH_PACKAGE_INDEX(int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CLOTH_PACKAGE_INDEX, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Every occurrence was either 0 or 2.

---

### SET_PLAYER_CLOTH_LOCK_COUNTER

**Hash:** `0x14D913B777DFF5DA` | **JHash:** `0x8D9FD4D1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_CLOTH_LOCK_COUNTER(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CLOTH_LOCK_COUNTER, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 6 matches across 4 scripts. 5 occurrences were 240. The other was 255.

---

### PLAYER_ATTACH_VIRTUAL_BOUND

**Hash:** `0xED51733DC73AED51` | **JHash:** `0xECD12E60` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYER_ATTACH_VIRTUAL_BOUND(float p0, float p1, float p2, float p3, float p4, float p5, float p6, float p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYER_ATTACH_VIRTUAL_BOUND, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only 1 match. ob_sofa_michael.

PLAYER::PLAYER_ATTACH_VIRTUAL_BOUND(-804.5928f, 173.1801f, 71.68436f, 0f, 0f, 0.590625f, 1f, 0.7f);1.0.335.2, 1.0.350.1/2, 1.0.372.2, 1.0.393.2, 1.0.393.4, 1.0.463.1;

---

### PLAYER_DETACH_VIRTUAL_BOUND

**Hash:** `0x1DD5897E2FA6E7C9` | **JHash:** `0x96100EA4` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYER_DETACH_VIRTUAL_BOUND(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYER_DETACH_VIRTUAL_BOUND);
```

**返回值:** `void` → `void`

**说明:** 1.0.335.2, 1.0.350.1/2, 1.0.372.2, 1.0.393.2, 1.0.393.4, 1.0.463.1;

---

### HAS_PLAYER_BEEN_SPOTTED_IN_STOLEN_VEHICLE

**Hash:** `0xD705740BB0A1CF4C` | **JHash:** `0x4A01B76A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PLAYER_BEEN_SPOTTED_IN_STOLEN_VEHICLE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PLAYER_BEEN_SPOTTED_IN_STOLEN_VEHICLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYER_BATTLE_AWARE

**Hash:** `0x38D28DA81E4E9BF9` | **JHash:** `0x013B4F72` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_BATTLE_AWARE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_BATTLE_AWARE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if an unk value is greater than 0.0f

---

### GET_PLAYER_RECEIVED_BATTLE_EVENT_RECENTLY

**Hash:** `0xBC0753C9CA14B506` | **JHash:** `0x9DF75B2A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_RECEIVED_BATTLE_EVENT_RECENTLY(Player player, int p1, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_RECEIVED_BATTLE_EVENT_RECENTLY, player, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### EXTEND_WORLD_BOUNDARY_FOR_PLAYER

**Hash:** `0x5006D96C995A5827` | **JHash:** `0x64DDB07D` | **Build:** `323`

**C++ 原型:**
```cpp
void EXTEND_WORLD_BOUNDARY_FOR_PLAYER(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXTEND_WORLD_BOUNDARY_FOR_PLAYER, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Appears only 3 times in the scripts, more specifically in michael1.ysc

-
This can be used to prevent dying if you are "out of the world"

---

### RESET_WORLD_BOUNDARY_FOR_PLAYER

**Hash:** `0xDA1DF03D5A315F4E` | **JHash:** `0xA97C2F6C` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_WORLD_BOUNDARY_FOR_PLAYER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_WORLD_BOUNDARY_FOR_PLAYER);
```

**返回值:** `void` → `void`

---

### IS_PLAYER_RIDING_TRAIN

**Hash:** `0x4EC12697209F2196` | **JHash:** `0x9765E71D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_RIDING_TRAIN(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_RIDING_TRAIN, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the player is riding a train.

---

### HAS_PLAYER_LEFT_THE_WORLD

**Hash:** `0xD55DDFB47991A294` | **JHash:** `0xFEA40B6C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PLAYER_LEFT_THE_WORLD(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PLAYER_LEFT_THE_WORLD, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_LEAVE_PED_BEHIND

**Hash:** `0xFF300C7649724A0B` | **JHash:** `0xAD8383FA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_LEAVE_PED_BEHIND(Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_LEAVE_PED_BEHIND, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PARACHUTE_VARIATION_OVERRIDE

**Hash:** `0xD9284A8C0D48352C` | **JHash:** `0x9254249D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_VARIATION_OVERRIDE(Player player, int p1, Any p2, Any p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_VARIATION_OVERRIDE, player, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 was always 5.
p4 was always false.

---

### CLEAR_PLAYER_PARACHUTE_VARIATION_OVERRIDE

**Hash:** `0x0F4CC924CF8C7B21` | **JHash:** `0xFD60F5AB` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_PARACHUTE_VARIATION_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_PARACHUTE_VARIATION_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x977DB4641F6FC3DB` | **JHash:** `0x5D382498` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_MODEL_OVERRIDE(Player player, Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_MODEL_OVERRIDE, player, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x0764486AEDE748DB` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE(Player player, Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE, player, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0xC219887CA3E65C41` | **Build:** `2372`

**C++ 原型:**
```cpp
Hash GET_PLAYER_PARACHUTE_MODEL_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PLAYER_PARACHUTE_MODEL_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x37FAAA68DCA9D08D` | **Build:** `2372`

**C++ 原型:**
```cpp
Hash GET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `Hash` → `uint`

---

### CLEAR_PLAYER_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x8753997EB5F6EE3F` | **JHash:** `0x6FF034BB` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_PARACHUTE_MODEL_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_PARACHUTE_MODEL_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x290D248E25815AE8` | **Build:** `2372`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_RESERVE_PARACHUTE_MODEL_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE

**Hash:** `0xDC80A4C2F18A2B64` | **JHash:** `0xA877FF5E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE(Player player, Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE, player, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### CLEAR_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE

**Hash:** `0x10C54E4389C12B42` | **JHash:** `0xBB62AAC5` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PLAYER_PARACHUTE_PACK_MODEL_OVERRIDE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### DISABLE_PLAYER_VEHICLE_REWARDS

**Hash:** `0xC142BE3BB9CE125F` | **JHash:** `0x8C6E611D` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PLAYER_VEHICLE_REWARDS(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PLAYER_VEHICLE_REWARDS, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_SPECTATED_VEHICLE_RADIO_OVERRIDE

**Hash:** `0x2F7CEB6520288061` | **JHash:** `0x2849D4B2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SPECTATED_VEHICLE_RADIO_OVERRIDE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SPECTATED_VEHICLE_RADIO_OVERRIDE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_BLUETOOTH_STATE

**Hash:** `0x5DC40A8869C22141` | **JHash:** `0x7A6B0EF8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_BLUETOOTH_STATE(Player player, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_BLUETOOTH_STATE, player, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_PLAYER_BLUETOOTH_ENABLE

**Hash:** `0x65FAEE425DE637B0` | **JHash:** `0xEA01BD4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_BLUETOOTH_ENABLE(Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_BLUETOOTH_ENABLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DISABLE_CAMERA_VIEW_MODE_CYCLE

**Hash:** `0x5501B7A5CDB79D37` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_CAMERA_VIEW_MODE_CYCLE(Player player)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_CAMERA_VIEW_MODE_CYCLE, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_FAKE_WANTED_LEVEL

**Hash:** `0x56105E599CAB0EFA` | **JHash:** `0x0098D244` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_FAKE_WANTED_LEVEL(Player player)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_FAKE_WANTED_LEVEL, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |

**返回值:** `int` → `int`

---

### SET_PLAYER_CAN_DAMAGE_PLAYER

**Hash:** `0x55FCC0C390620314` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_PLAYER_CAN_DAMAGE_PLAYER(Player player1, Player player2, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_CAN_DAMAGE_PLAYER, player1, player2, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player1` | `Player` | `int` | |
| `player2` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_APPLY_WAYPOINT_OF_PLAYER

**Hash:** `0x2382AB11450AE7BA` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_APPLY_WAYPOINT_OF_PLAYER(Player player, int hudColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_APPLY_WAYPOINT_OF_PLAYER, player, hudColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `hudColor` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_PLAYER_VEHICLE_WEAPON_TOGGLED_TO_NON_HOMING

**Hash:** `0x6E4361FF3E8CD7CA` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_VEHICLE_WEAPON_TOGGLED_TO_NON_HOMING(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_VEHICLE_WEAPON_TOGGLED_TO_NON_HOMING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLAYER_VEHICLE_WEAPON_TO_NON_HOMING

**Hash:** `0x237440E46D918649` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_PLAYER_VEHICLE_WEAPON_TO_NON_HOMING(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_VEHICLE_WEAPON_TO_NON_HOMING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Unsets playerPed+330 if the current weapon has certain flags.

---

### SET_PLAYER_HOMING_DISABLED_FOR_ALL_VEHICLE_WEAPONS

**Hash:** `0xEE4EBDD2593BA844` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_PLAYER_HOMING_DISABLED_FOR_ALL_VEHICLE_WEAPONS(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_HOMING_DISABLED_FOR_ALL_VEHICLE_WEAPONS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ADD_PLAYER_TARGETABLE_ENTITY

**Hash:** `0x9097EB6D4BB9A12A` | **Build:** `1868`

**C++ 原型:**
```cpp
void ADD_PLAYER_TARGETABLE_ENTITY(Player player, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PLAYER_TARGETABLE_ENTITY, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_PLAYER_TARGETABLE_ENTITY

**Hash:** `0x9F260BFB59ADBCA3` | **Build:** `1868`

**C++ 原型:**
```cpp
void REMOVE_PLAYER_TARGETABLE_ENTITY(Player player, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PLAYER_TARGETABLE_ENTITY, player, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_PREVIOUS_VARIATION_DATA

**Hash:** `0x7BAE68775557AE0B` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_PLAYER_PREVIOUS_VARIATION_DATA(Player player, int p1, int p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_PREVIOUS_VARIATION_DATA, player, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REMOVE_SCRIPT_FIRE_POSITION

**Hash:** `0x7148E0F43D11F0D9` | **Build:** `1604`

**C++ 原型:**
```cpp
void REMOVE_SCRIPT_FIRE_POSITION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SCRIPT_FIRE_POSITION);
```

**返回值:** `void` → `void`

**说明:** Resets values set by SET_SCRIPT_FIRE_POSITION

---

### SET_SCRIPT_FIRE_POSITION

**Hash:** `0x70A382ADEC069DD3` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_SCRIPT_FIRE_POSITION(float coordX, float coordY, float coordZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_FIRE_POSITION, coordX, coordY, coordZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `coordX` | `float` | `float` | |
| `coordY` | `float` | `float` | |
| `coordZ` | `float` | `float` | |

**返回值:** `void` → `void`

---


---

## RECORDING

**包含 17 个 Native 函数**

### REPLAY_START_EVENT

**Hash:** `0x48621C9FCA3EBD28` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_START_EVENT(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_START_EVENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REPLAY_STOP_EVENT

**Hash:** `0x81CBAE94390F9F89` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_STOP_EVENT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_STOP_EVENT);
```

**返回值:** `void` → `void`

---

### REPLAY_CANCEL_EVENT

**Hash:** `0x13B350B8AD0EEE10` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_CANCEL_EVENT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_CANCEL_EVENT);
```

**返回值:** `void` → `void`

---

### REPLAY_RECORD_BACK_FOR_TIME

**Hash:** `0x293220DA1B46CEBC` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_RECORD_BACK_FOR_TIME(float p0, float p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_RECORD_BACK_FOR_TIME, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### REPLAY_CHECK_FOR_EVENT_THIS_FRAME

**Hash:** `0x208784099002BC30` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_CHECK_FOR_EVENT_THIS_FRAME(const char* missionNameLabel, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_CHECK_FOR_EVENT_THIS_FRAME, missionNameLabel, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `missionNameLabel` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** -This function appears to be deprecated/ unused. Tracing the call internally leads to a _nullsub -

first one seems to be a string of a mission name, second one seems to be a bool/toggle

p1 was always 0.


---

### REPLAY_PREVENT_RECORDING_THIS_FRAME

**Hash:** `0xEB2D525B57F42B40` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_PREVENT_RECORDING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_PREVENT_RECORDING_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** This disable the recording feature and has to be called every frame.

---

### REPLAY_RESET_EVENT_INFO

**Hash:** `0xF854439EFBB3B583` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_RESET_EVENT_INFO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_RESET_EVENT_INFO);
```

**返回值:** `void` → `void`

---

### REPLAY_DISABLE_CAMERA_MOVEMENT_THIS_FRAME

**Hash:** `0xAF66DCEE6609B148` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_DISABLE_CAMERA_MOVEMENT_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_DISABLE_CAMERA_MOVEMENT_THIS_FRAME);
```

**返回值:** `void` → `void`

**说明:** This will disable the ability to make camera changes in R* Editor.

---

### RECORD_GREATEST_MOMENT

**Hash:** `0x66972397E0757E7A` | **Build:** `323`

**C++ 原型:**
```cpp
void RECORD_GREATEST_MOMENT(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RECORD_GREATEST_MOMENT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### START_REPLAY_RECORDING

**Hash:** `0xC3AC2FFF9612AC81` | **Build:** `323`

**C++ 原型:**
```cpp
void START_REPLAY_RECORDING(int mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_REPLAY_RECORDING, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `mode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Starts recording a replay.
If mode is 0, turns on action replay.
If mode is 1, starts recording.
If already recording a replay, does nothing.

---

### STOP_REPLAY_RECORDING

**Hash:** `0x071A5197D6AFC8B3` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_REPLAY_RECORDING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_REPLAY_RECORDING);
```

**返回值:** `void` → `void`

**说明:** Stops recording and saves the recorded clip.

---

### CANCEL_REPLAY_RECORDING

**Hash:** `0x88BB3507ED41A240` | **Build:** `323`

**C++ 原型:**
```cpp
void CANCEL_REPLAY_RECORDING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CANCEL_REPLAY_RECORDING);
```

**返回值:** `void` → `void`

**说明:** Stops recording and discards the recorded clip.

---

### SAVE_REPLAY_RECORDING

**Hash:** `0x644546EC5287471B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SAVE_REPLAY_RECORDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVE_REPLAY_RECORDING);
```

**返回值:** `BOOL` → `bool`

---

### IS_REPLAY_RECORDING

**Hash:** `0x1897CA71995A90B4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REPLAY_RECORDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REPLAY_RECORDING);
```

**返回值:** `BOOL` → `bool`

**说明:** Checks if you're recording, returns TRUE when you start recording (F1) or turn on action replay (F2)

mov al, cs:g_bIsRecordingGameplay // byte_141DD0CD0 in b944
retn

---

### IS_REPLAY_INITIALIZED

**Hash:** `0xDF4B952F7D381B95` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REPLAY_INITIALIZED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REPLAY_INITIALIZED);
```

**返回值:** `BOOL` → `bool`

---

### IS_REPLAY_AVAILABLE

**Hash:** `0x4282E08174868BE3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REPLAY_AVAILABLE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REPLAY_AVAILABLE);
```

**返回值:** `BOOL` → `bool`

---

### IS_REPLAY_RECORD_SPACE_AVAILABLE

**Hash:** `0x33D47E85B476ABCD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_REPLAY_RECORD_SPACE_AVAILABLE(BOOL p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_REPLAY_RECORD_SPACE_AVAILABLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---


---

## REPLAY

**包含 6 个 Native 函数**

### REGISTER_EFFECT_FOR_REPLAY_EDITOR

**Hash:** `0x7E2BD3EF6C205F09` | **Build:** `323`

**C++ 原型:**
```cpp
void REGISTER_EFFECT_FOR_REPLAY_EDITOR(const char* p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_EFFECT_FOR_REPLAY_EDITOR, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing (it's a nullsub).

---

### REPLAY_SYSTEM_HAS_REQUESTED_A_SCRIPT_CLEANUP

**Hash:** `0x95AB8B5C992C7B58` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REPLAY_SYSTEM_HAS_REQUESTED_A_SCRIPT_CLEANUP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REPLAY_SYSTEM_HAS_REQUESTED_A_SCRIPT_CLEANUP);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns a bool if interior rendering is disabled, if yes, all "normal" rendered interiors are invisible

---

### SET_SCRIPTS_HAVE_CLEANED_UP_FOR_REPLAY_SYSTEM

**Hash:** `0x5AD3932DAEB1E5D3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPTS_HAVE_CLEANED_UP_FOR_REPLAY_SYSTEM(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPTS_HAVE_CLEANED_UP_FOR_REPLAY_SYSTEM);
```

**返回值:** `void` → `void`

**说明:** Disables some other rendering (internal)

---

### SET_REPLAY_SYSTEM_PAUSED_FOR_SAVE

**Hash:** `0xE058175F8EAFE79A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_REPLAY_SYSTEM_PAUSED_FOR_SAVE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REPLAY_SYSTEM_PAUSED_FOR_SAVE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REPLAY_CONTROL_SHUTDOWN

**Hash:** `0x3353D13F09307691` | **Build:** `323`

**C++ 原型:**
```cpp
void REPLAY_CONTROL_SHUTDOWN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REPLAY_CONTROL_SHUTDOWN);
```

**返回值:** `void` → `void`

**说明:** Sets (almost, not sure) all Rockstar Editor values (bIsRecording etc) to 0.

---

### ACTIVATE_ROCKSTAR_EDITOR

**Hash:** `0x49DA8145672B2725` | **Build:** `323`

**C++ 原型:**
```cpp
void ACTIVATE_ROCKSTAR_EDITOR(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ACTIVATE_ROCKSTAR_EDITOR, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Please note that you will need to call DO_SCREEN_FADE_IN after exiting the Rockstar Editor when you call this.

---


---

## SAVEMIGRATION

**包含 7 个 Native 函数**

### SAVEMIGRATION_IS_MP_ENABLED

**Hash:** `0x84B418E93894AC1C` | **Build:** `2372`

**C++ 原型:**
```cpp
BOOL SAVEMIGRATION_IS_MP_ENABLED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVEMIGRATION_IS_MP_ENABLED);
```

**返回值:** `BOOL` → `bool`

---

### SAVEMIGRATION_MP_REQUEST_ACCOUNTS

**Hash:** `0x85F41F9225D08C72` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL SAVEMIGRATION_MP_REQUEST_ACCOUNTS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVEMIGRATION_MP_REQUEST_ACCOUNTS);
```

**返回值:** `BOOL` → `bool`

---

### SAVEMIGRATION_MP_GET_ACCOUNTS_STATUS

**Hash:** `0xC8CB5999919EA2CA` | **Build:** `2612`

**C++ 原型:**
```cpp
int SAVEMIGRATION_MP_GET_ACCOUNTS_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SAVEMIGRATION_MP_GET_ACCOUNTS_STATUS);
```

**返回值:** `int` → `int`

---

### SAVEMIGRATION_MP_NUM_ACCOUNTS

**Hash:** `0x77A16200E18E0C55` | **Build:** `2612`

**C++ 原型:**
```cpp
int SAVEMIGRATION_MP_NUM_ACCOUNTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SAVEMIGRATION_MP_NUM_ACCOUNTS);
```

**返回值:** `int` → `int`

---

### SAVEMIGRATION_MP_GET_ACCOUNT

**Hash:** `0xFCE2747EEF1D05FC` | **Build:** `2612`

**C++ 原型:**
```cpp
BOOL SAVEMIGRATION_MP_GET_ACCOUNT(int p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVEMIGRATION_MP_GET_ACCOUNT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SAVEMIGRATION_MP_REQUEST_STATUS

**Hash:** `0xE5E9746A66359F9D` | **Build:** `2372`

**C++ 原型:**
```cpp
BOOL SAVEMIGRATION_MP_REQUEST_STATUS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SAVEMIGRATION_MP_REQUEST_STATUS);
```

**返回值:** `BOOL` → `bool`

---

### SAVEMIGRATION_MP_GET_STATUS

**Hash:** `0x690B76BD2763E068` | **Build:** `2372`

**C++ 原型:**
```cpp
int SAVEMIGRATION_MP_GET_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SAVEMIGRATION_MP_GET_STATUS);
```

**返回值:** `int` → `int`

---


---

## SCRIPT

**包含 37 个 Native 函数**

### REQUEST_SCRIPT

**Hash:** `0x6EB5F71AA68F2E8E` | **JHash:** `0xE26B2666` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_SCRIPT(const char* scriptName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_SCRIPT, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_SCRIPT_AS_NO_LONGER_NEEDED

**Hash:** `0xC90D2DCACD56184C` | **JHash:** `0x6FCB7795` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_AS_NO_LONGER_NEEDED(const char* scriptName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_AS_NO_LONGER_NEEDED, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### HAS_SCRIPT_LOADED

**Hash:** `0xE6CC9F3BA0FB9EF1` | **JHash:** `0x5D67F751` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCRIPT_LOADED(const char* scriptName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCRIPT_LOADED, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns if a script has been loaded into the game. Used to see if a script was loaded after requesting.

---

### DOES_SCRIPT_EXIST

**Hash:** `0xFC04745FBE67C19A` | **JHash:** `0xDEAB87AB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCRIPT_EXIST(const char* scriptName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCRIPT_EXIST, scriptName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_SCRIPT_WITH_NAME_HASH

**Hash:** `0xD62A67D26D9653E6` | **JHash:** `0x1C68D9DC` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_SCRIPT_WITH_NAME_HASH(Hash scriptHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_SCRIPT_WITH_NAME_HASH, scriptHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** formerly _REQUEST_STREAMED_SCRIPT

---

### SET_SCRIPT_WITH_NAME_HASH_AS_NO_LONGER_NEEDED

**Hash:** `0xC5BC038960E9DB27` | **JHash:** `0x96C26F66` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_WITH_NAME_HASH_AS_NO_LONGER_NEEDED(Hash scriptHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_WITH_NAME_HASH_AS_NO_LONGER_NEEDED, scriptHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### HAS_SCRIPT_WITH_NAME_HASH_LOADED

**Hash:** `0x5F0F0C783EB16C04` | **JHash:** `0x06674818` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_SCRIPT_WITH_NAME_HASH_LOADED(Hash scriptHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_SCRIPT_WITH_NAME_HASH_LOADED, scriptHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### DOES_SCRIPT_WITH_NAME_HASH_EXIST

**Hash:** `0xF86AA3C56BA31381` | **JHash:** `0x19EAE282` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCRIPT_WITH_NAME_HASH_EXIST(Hash scriptHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCRIPT_WITH_NAME_HASH_EXIST, scriptHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### TERMINATE_THREAD

**Hash:** `0xC8B189ED9138BCD4` | **JHash:** `0x253FD520` | **Build:** `323`

**C++ 原型:**
```cpp
void TERMINATE_THREAD(int threadId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERMINATE_THREAD, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_THREAD_ACTIVE

**Hash:** `0x46E9AE36D8FA6417` | **JHash:** `0x78D7A5A0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THREAD_ACTIVE(int threadId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THREAD_ACTIVE, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_NAME_OF_SCRIPT_WITH_THIS_ID

**Hash:** `0x05A42BA9FC8DA96B` | **JHash:** `0xBE7ACD89` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_NAME_OF_SCRIPT_WITH_THIS_ID(int threadId)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_NAME_OF_SCRIPT_WITH_THIS_ID, threadId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `threadId` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### SCRIPT_THREAD_ITERATOR_RESET

**Hash:** `0xDADFADA5A20143A8` | **JHash:** `0xBB4E2F66` | **Build:** `323`

**C++ 原型:**
```cpp
void SCRIPT_THREAD_ITERATOR_RESET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SCRIPT_THREAD_ITERATOR_RESET);
```

**返回值:** `void` → `void`

**说明:** Starts a new iteration of the current threads.
Call this first, then SCRIPT_THREAD_ITERATOR_GET_NEXT_THREAD_ID (0x30B4FA1C82DD4B9F)

---

### SCRIPT_THREAD_ITERATOR_GET_NEXT_THREAD_ID

**Hash:** `0x30B4FA1C82DD4B9F` | **JHash:** `0x1E28B28F` | **Build:** `323`

**C++ 原型:**
```cpp
int SCRIPT_THREAD_ITERATOR_GET_NEXT_THREAD_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SCRIPT_THREAD_ITERATOR_GET_NEXT_THREAD_ID);
```

**返回值:** `int` → `int`

**说明:** If the function returns 0, the end of the iteration has been reached.

---

### GET_ID_OF_THIS_THREAD

**Hash:** `0xC30338E8088E2E21` | **JHash:** `0xDE524830` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ID_OF_THIS_THREAD(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ID_OF_THIS_THREAD);
```

**返回值:** `int` → `int`

---

### TERMINATE_THIS_THREAD

**Hash:** `0x1090044AD1DA76FA` | **JHash:** `0x3CD9CBB7` | **Build:** `323`

**C++ 原型:**
```cpp
void TERMINATE_THIS_THREAD(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TERMINATE_THIS_THREAD);
```

**返回值:** `void` → `void`

---

### GET_NUMBER_OF_THREADS_RUNNING_THE_SCRIPT_WITH_THIS_HASH

**Hash:** `0x2C83A9DA6BFFC4F9` | **JHash:** `0x029D3841` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_THREADS_RUNNING_THE_SCRIPT_WITH_THIS_HASH(Hash scriptHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_THREADS_RUNNING_THE_SCRIPT_WITH_THIS_HASH, scriptHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Gets the number of instances of the specified script is currently running.

Actually returns numRefs - 1.
if (program)
	v3 = rage::scrProgram::GetNumRefs(program) - 1;
return v3;

---

### GET_THIS_SCRIPT_NAME

**Hash:** `0x442E0A7EDE4A738A` | **JHash:** `0xA40FD5D9` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_THIS_SCRIPT_NAME(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_THIS_SCRIPT_NAME);
```

**返回值:** `const char*` → `string`

---

### GET_HASH_OF_THIS_SCRIPT_NAME

**Hash:** `0x8A1C8B1738FFE87E` | **JHash:** `0x2BEE1F45` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_HASH_OF_THIS_SCRIPT_NAME(void)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_HASH_OF_THIS_SCRIPT_NAME);
```

**返回值:** `Hash` → `uint`

---

### GET_NUMBER_OF_EVENTS

**Hash:** `0x5F92A689A06620AA` | **JHash:** `0xA3525D60` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_EVENTS(int eventGroup)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_EVENTS, eventGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** eventGroup: 0 = SCRIPT_EVENT_QUEUE_AI (CEventGroupScriptAI), 1 = SCRIPT_EVENT_QUEUE_NETWORK (CEventGroupScriptNetwork)

---

### GET_EVENT_EXISTS

**Hash:** `0x936E6168A9BCEDB5` | **JHash:** `0xA1B447B5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_EVENT_EXISTS(int eventGroup, int eventIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_EVENT_EXISTS, eventGroup, eventIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |
| `eventIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** eventGroup: 0 = SCRIPT_EVENT_QUEUE_AI (CEventGroupScriptAI), 1 = SCRIPT_EVENT_QUEUE_NETWORK (CEventGroupScriptNetwork)

---

### GET_EVENT_AT_INDEX

**Hash:** `0xD8F66A3A60C62153` | **JHash:** `0xB49C1442` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_EVENT_AT_INDEX(int eventGroup, int eventIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_EVENT_AT_INDEX, eventGroup, eventIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |
| `eventIndex` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** eventGroup: 0 = SCRIPT_EVENT_QUEUE_AI (CEventGroupScriptAI), 1 = SCRIPT_EVENT_QUEUE_NETWORK (CEventGroupScriptNetwork)

---

### GET_EVENT_DATA

**Hash:** `0x2902843FCD2B2D79` | **JHash:** `0x4280F92F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_EVENT_DATA(int eventGroup, int eventIndex, Any* eventData, int eventDataSize)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_EVENT_DATA, eventGroup, eventIndex, eventData, eventDataSize);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |
| `eventIndex` | `int` | `int` | |
| `eventData` | `Any*` | `object*` | |
| `eventDataSize` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** eventGroup: 0 = SCRIPT_EVENT_QUEUE_AI (CEventGroupScriptAI), 1 = SCRIPT_EVENT_QUEUE_NETWORK (CEventGroupScriptNetwork)

Note: eventDataSize is NOT the size in bytes, it is the size determined by the SIZE_OF operator (RAGE Script operator, not C/C++ sizeof). That is, the size in bytes divided by 8 (script variables are always 8-byte aligned!).

---

### TRIGGER_SCRIPT_EVENT

**Hash:** `0x5AE99C571D5BBE5D` | **JHash:** `0x54763B35` | **Build:** `323`

**C++ 原型:**
```cpp
void TRIGGER_SCRIPT_EVENT(int eventGroup, Any* eventData, int eventDataSize, int playerBits)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRIGGER_SCRIPT_EVENT, eventGroup, eventData, eventDataSize, playerBits);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |
| `eventData` | `Any*` | `object*` | |
| `eventDataSize` | `int` | `int` | |
| `playerBits` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** eventGroup: 0 = SCRIPT_EVENT_QUEUE_AI (CEventGroupScriptAI), 1 = SCRIPT_EVENT_QUEUE_NETWORK (CEventGroupScriptNetwork)

Note: eventDataSize is NOT the size in bytes, it is the size determined by the SIZE_OF operator (RAGE Script operator, not C/C++ sizeof). That is, the size in bytes divided by 8 (script variables are always 8-byte aligned!).

playerBits (also known as playersToBroadcastTo) is a bitset that indicates which players this event should be sent to. In order to send the event to specific players only, use (1 << playerIndex). Set all bits if it should be broadcast to all players.

---

### SHUTDOWN_LOADING_SCREEN

**Hash:** `0x078EBE9809CCD637` | **JHash:** `0xA2826D17` | **Build:** `323`

**C++ 原型:**
```cpp
void SHUTDOWN_LOADING_SCREEN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHUTDOWN_LOADING_SCREEN);
```

**返回值:** `void` → `void`

---

### SET_NO_LOADING_SCREEN

**Hash:** `0x5262CC1995D07E09` | **JHash:** `0xC8055034` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NO_LOADING_SCREEN(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NO_LOADING_SCREEN, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_NO_LOADING_SCREEN

**Hash:** `0x18C1270EA7F199BC` | **JHash:** `0x27512BA0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_NO_LOADING_SCREEN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_NO_LOADING_SCREEN);
```

**返回值:** `BOOL` → `bool`

---

### COMMIT_TO_LOADINGSCREEN_SELCTION

**Hash:** `0xB1577667C3708F9B` | **JHash:** `0xB03BCCDF` | **Build:** `323`

**C++ 原型:**
```cpp
void COMMIT_TO_LOADINGSCREEN_SELCTION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.COMMIT_TO_LOADINGSCREEN_SELCTION);
```

**返回值:** `void` → `void`

---

### BG_IS_EXITFLAG_SET

**Hash:** `0x836B62713E0534CA` | **JHash:** `0x22B9F132` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BG_IS_EXITFLAG_SET(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BG_IS_EXITFLAG_SET);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if bit 0 in GtaThread+0x154 is set.

---

### BG_SET_EXITFLAG_RESPONSE

**Hash:** `0x760910B49D2B98EA` | **JHash:** `0x5C95B670` | **Build:** `323`

**C++ 原型:**
```cpp
void BG_SET_EXITFLAG_RESPONSE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BG_SET_EXITFLAG_RESPONSE);
```

**返回值:** `void` → `void`

**说明:** Sets bit 1 in GtaThread+0x154

---

### BG_START_CONTEXT_HASH

**Hash:** `0x75B18E49607874C7` | **JHash:** `0x67B23E17` | **Build:** `323`

**C++ 原型:**
```cpp
void BG_START_CONTEXT_HASH(Hash contextHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BG_START_CONTEXT_HASH, contextHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Hashed version of BG_START_CONTEXT.

---

### BG_END_CONTEXT_HASH

**Hash:** `0x107E5CC7CA942BC1` | **JHash:** `0x3D981EBF` | **Build:** `323`

**C++ 原型:**
```cpp
void BG_END_CONTEXT_HASH(Hash contextHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BG_END_CONTEXT_HASH, contextHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Hashed version of BG_END_CONTEXT.

---

### BG_START_CONTEXT

**Hash:** `0x9D5A25BADB742ACD` | **JHash:** `0xF3F1D953` | **Build:** `323`

**C++ 原型:**
```cpp
void BG_START_CONTEXT(const char* contextName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BG_START_CONTEXT, contextName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Inserts the given context into the background scripts context map.

---

### BG_END_CONTEXT

**Hash:** `0xDC2BACD920D0A0DD` | **JHash:** `0x11F38FB3` | **Build:** `323`

**C++ 原型:**
```cpp
void BG_END_CONTEXT(const char* contextName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BG_END_CONTEXT, contextName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contextName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Deletes the given context from the background scripts context map.

---

### BG_DOES_LAUNCH_PARAM_EXIST

**Hash:** `0x0F6F1EBBC4E1D5E6` | **JHash:** `0xF8E5F73A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL BG_DOES_LAUNCH_PARAM_EXIST(int scriptIndex, const char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.BG_DOES_LAUNCH_PARAM_EXIST, scriptIndex, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptIndex` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### BG_GET_LAUNCH_PARAM_VALUE

**Hash:** `0x22E21FBCFC88C149` | **JHash:** `0x682CB7E1` | **Build:** `323`

**C++ 原型:**
```cpp
int BG_GET_LAUNCH_PARAM_VALUE(int scriptIndex, const char* p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.BG_GET_LAUNCH_PARAM_VALUE, scriptIndex, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scriptIndex` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### BG_GET_SCRIPT_ID_FROM_NAME_HASH

**Hash:** `0x829CD22E043A2577` | **JHash:** `0xC515E735` | **Build:** `323`

**C++ 原型:**
```cpp
int BG_GET_SCRIPT_ID_FROM_NAME_HASH(Hash p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.BG_GET_SCRIPT_ID_FROM_NAME_HASH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### _SEND_TU_SCRIPT_EVENT_NEW

**Hash:** `0x71A6F836422FDD2B` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SEND_TU_SCRIPT_EVENT_NEW(int eventGroup, Any* eventData, int eventDataSize, int playerBits, Hash eventType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SEND_TU_SCRIPT_EVENT_NEW, eventGroup, eventData, eventDataSize, playerBits, eventType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `eventGroup` | `int` | `int` | |
| `eventData` | `Any*` | `object*` | |
| `eventDataSize` | `int` | `int` | |
| `playerBits` | `int` | `int` | |
| `eventType` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** New variant of SEND_TU_SCRIPT_EVENT that automatically initializes the event data header.
See TRIGGER_SCRIPT_EVENT for more info.

---


---

## SECURITY

**包含 3 个 Native 函数**

### REGISTER_SCRIPT_VARIABLE

**Hash:** `0x40EB1EFD921822BC` | **Build:** `2545`

**C++ 原型:**
```cpp
void REGISTER_SCRIPT_VARIABLE(Any* variable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REGISTER_SCRIPT_VARIABLE, variable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variable` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Registers a protected variable that will be checked for modifications by the anticheat

---

### UNREGISTER_SCRIPT_VARIABLE

**Hash:** `0x340A36A700E99699` | **Build:** `2545`

**C++ 原型:**
```cpp
void UNREGISTER_SCRIPT_VARIABLE(Any* variable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNREGISTER_SCRIPT_VARIABLE, variable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variable` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### FORCE_CHECK_SCRIPT_VARIABLES

**Hash:** `0x8E580AB902917360` | **Build:** `2545`

**C++ 原型:**
```cpp
void FORCE_CHECK_SCRIPT_VARIABLES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CHECK_SCRIPT_VARIABLES);
```

**返回值:** `void` → `void`

---


---

## SHAPETEST

**包含 11 个 Native 函数**

### START_SHAPE_TEST_LOS_PROBE

**Hash:** `0x7EE9F5D83DD4F90E` | **JHash:** `0xEFAF4BA6` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_LOS_PROBE(float x1, float y1, float z1, float x2, float y2, float z2, int flags, Entity entity, int p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_LOS_PROBE, x1, y1, z1, x2, y2, z2, flags, entity, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `p8` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Asynchronously starts a line-of-sight (raycast) world probe shape test.

Use the handle with 0x3D87450E15D98694 or 0x65287525D951F6BE until it returns 0 or 2.

p8 is a bit mask with bits 1, 2 and/or 4, relating to collider types; 4 should usually be used.

---

### START_EXPENSIVE_SYNCHRONOUS_SHAPE_TEST_LOS_PROBE

**Hash:** `0x377906D8A31E5586` | **JHash:** `0x8251485D` | **Build:** `323`

**C++ 原型:**
```cpp
int START_EXPENSIVE_SYNCHRONOUS_SHAPE_TEST_LOS_PROBE(float x1, float y1, float z1, float x2, float y2, float z2, int flags, Entity entity, int p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_EXPENSIVE_SYNCHRONOUS_SHAPE_TEST_LOS_PROBE, x1, y1, z1, x2, y2, z2, flags, entity, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `p8` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Does the same as 0x7EE9F5D83DD4F90E, except blocking until the shape test completes.

---

### START_SHAPE_TEST_BOUNDING_BOX

**Hash:** `0x052837721A854EC7` | **JHash:** `0xCEEAD94B` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_BOUNDING_BOX(Entity entity, int flags1, int flags2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_BOUNDING_BOX, entity, flags1, flags2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `flags1` | `int` | `int` | |
| `flags2` | `int` | `int` | |

**返回值:** `int` → `int`

---

### START_SHAPE_TEST_BOX

**Hash:** `0xFE466162C4401D18` | **JHash:** `0x249BC876` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_BOX(float x, float y, float z, float dimX, float dimY, float dimZ, float rotX, float rotY, float rotZ, Any p9, int flags, Entity entity, Any p12)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_BOX, x, y, z, dimX, dimY, dimZ, rotX, rotY, rotZ, p9, flags, entity, p12);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `dimX` | `float` | `float` | |
| `dimY` | `float` | `float` | |
| `dimZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `p9` | `Any` | `object` | |
| `flags` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `p12` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### START_SHAPE_TEST_BOUND

**Hash:** `0x37181417CE7C8900` | **JHash:** `0x13BC46C0` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_BOUND(Entity entity, int flags1, int flags2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_BOUND, entity, flags1, flags2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `flags1` | `int` | `int` | |
| `flags2` | `int` | `int` | |

**返回值:** `int` → `int`

---

### START_SHAPE_TEST_CAPSULE

**Hash:** `0x28579D1B8F8AAC80` | **JHash:** `0x591EA833` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_CAPSULE(float x1, float y1, float z1, float x2, float y2, float z2, float radius, int flags, Entity entity, int p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_CAPSULE, x1, y1, z1, x2, y2, z2, radius, flags, entity, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `p9` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Raycast from point to point, where the ray has a radius. 

flags:
vehicles=10
peds =12

Iterating through flags yields many ped / vehicle/ object combinations

p9 = 7, but no idea what it does

Entity is an entity to ignore

---

### START_SHAPE_TEST_SWEPT_SPHERE

**Hash:** `0xE6AC6C45FBE83004` | **JHash:** `0x4559460A` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_SWEPT_SPHERE(float x1, float y1, float z1, float x2, float y2, float z2, float radius, int flags, Entity entity, Any p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_SWEPT_SPHERE, x1, y1, z1, x2, y2, z2, radius, flags, entity, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `p9` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### START_SHAPE_TEST_MOUSE_CURSOR_LOS_PROBE

**Hash:** `0xFF6BE494C7987F34` | **Build:** `323`

**C++ 原型:**
```cpp
int START_SHAPE_TEST_MOUSE_CURSOR_LOS_PROBE(Vector3* pVec1, Vector3* pVec2, int flag, Entity entity, int flag2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.START_SHAPE_TEST_MOUSE_CURSOR_LOS_PROBE, pVec1, pVec2, flag, entity, flag2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pVec1` | `Vector3*` | `Vector3*` | |
| `pVec2` | `Vector3*` | `Vector3*` | |
| `flag` | `int` | `int` | |
| `entity` | `Entity` | `int` | |
| `flag2` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns a ShapeTest handle that can be used with GET_SHAPE_TEST_RESULT.

In its only usage in game scripts its called with flag set to 511, entity to player_ped_id and flag2 set to 7

---

### GET_SHAPE_TEST_RESULT

**Hash:** `0x3D87450E15D98694` | **JHash:** `0xF3C2875A` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHAPE_TEST_RESULT(int shapeTestHandle, BOOL* hit, Vector3* endCoords, Vector3* surfaceNormal, Entity* entityHit)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHAPE_TEST_RESULT, shapeTestHandle, hit, endCoords, surfaceNormal, entityHit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shapeTestHandle` | `int` | `int` | |
| `hit` | `BOOL*` | `bool*` | |
| `endCoords` | `Vector3*` | `Vector3*` | |
| `surfaceNormal` | `Vector3*` | `Vector3*` | |
| `entityHit` | `Entity*` | `int*` | |

**返回值:** `int` → `int`

**说明:** Returns the result of a shape test: 0 if the handle is invalid, 1 if the shape test is still pending, or 2 if the shape test has completed, and the handle should be invalidated.

When used with an asynchronous shape test, this native should be looped until returning 0 or 2, after which the handle is invalidated.

---

### GET_SHAPE_TEST_RESULT_INCLUDING_MATERIAL

**Hash:** `0x65287525D951F6BE` | **JHash:** `0x4301E10C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SHAPE_TEST_RESULT_INCLUDING_MATERIAL(int shapeTestHandle, BOOL* hit, Vector3* endCoords, Vector3* surfaceNormal, Hash* materialHash, Entity* entityHit)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SHAPE_TEST_RESULT_INCLUDING_MATERIAL, shapeTestHandle, hit, endCoords, surfaceNormal, materialHash, entityHit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shapeTestHandle` | `int` | `int` | |
| `hit` | `BOOL*` | `bool*` | |
| `endCoords` | `Vector3*` | `Vector3*` | |
| `surfaceNormal` | `Vector3*` | `Vector3*` | |
| `materialHash` | `Hash*` | `uint*` | |
| `entityHit` | `Entity*` | `int*` | |

**返回值:** `int` → `int`

**说明:** Returns the result of a shape test, also returning the material of any touched surface.

When used with an asynchronous shape test, this native should be looped until returning 0 or 2, after which the handle is invalidated.

Unless the return value is 2, the other return values are undefined.

---

### RELEASE_SCRIPT_GUID_FROM_ENTITY

**Hash:** `0x2B3334BCA57CD799` | **JHash:** `0xEC2AAF06` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_SCRIPT_GUID_FROM_ENTITY(Entity entityHit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_SCRIPT_GUID_FROM_ENTITY, entityHit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entityHit` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** Invalidates the entity handle passed by removing the fwScriptGuid from the entity. This should be used when receiving an ambient entity from shape testing natives, but can also be used for other natives returning an 'irrelevant' entity handle.

---


---

## SOCIALCLUB

**包含 84 个 Native 函数**

### SC_INBOX_GET_TOTAL_NUM_MESSAGES

**Hash:** `0x03A93FF1A2CA0864` | **JHash:** `0x6BE5DF29` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_INBOX_GET_TOTAL_NUM_MESSAGES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_INBOX_GET_TOTAL_NUM_MESSAGES);
```

**返回值:** `int` → `int`

---

### SC_INBOX_GET_MESSAGE_TYPE_AT_INDEX

**Hash:** `0xBB8EA16ECBC976C4` | **JHash:** `0x5ECF955D` | **Build:** `323`

**C++ 原型:**
```cpp
Hash SC_INBOX_GET_MESSAGE_TYPE_AT_INDEX(int msgIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.SC_INBOX_GET_MESSAGE_TYPE_AT_INDEX, msgIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `msgIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### SC_INBOX_GET_MESSAGE_IS_READ_AT_INDEX

**Hash:** `0x93028F1DB42BFD08` | **JHash:** `0xD1ED1D48` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_GET_MESSAGE_IS_READ_AT_INDEX(int msgIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_GET_MESSAGE_IS_READ_AT_INDEX, msgIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `msgIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_SET_MESSAGE_AS_READ_AT_INDEX

**Hash:** `0x2C015348CF19CA1D` | **JHash:** `0x19EE0CCB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_SET_MESSAGE_AS_READ_AT_INDEX(int msgIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_SET_MESSAGE_AS_READ_AT_INDEX, msgIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `msgIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_MESSAGE_GET_DATA_INT

**Hash:** `0xA00EFE4082C4056E` | **JHash:** `0x88068C7C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_MESSAGE_GET_DATA_INT(int p0, const char* context, int* out)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_MESSAGE_GET_DATA_INT, p0, context, out);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `context` | `const char*` | `string` | |
| `out` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_MESSAGE_GET_DATA_BOOL

**Hash:** `0xFFE5C16F402D851D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_MESSAGE_GET_DATA_BOOL(int p0, const char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_MESSAGE_GET_DATA_BOOL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_MESSAGE_GET_DATA_STRING

**Hash:** `0x7572EF42FC6A9B6D` | **JHash:** `0x15607620` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_MESSAGE_GET_DATA_STRING(int p0, const char* context, char* out)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_MESSAGE_GET_DATA_STRING, p0, context, out);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `context` | `const char*` | `string` | |
| `out` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_MESSAGE_DO_APPLY

**Hash:** `0x9A2C8064B6C1E41A` | **JHash:** `0xEBE420A4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_MESSAGE_DO_APPLY(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_MESSAGE_DO_APPLY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_MESSAGE_GET_RAW_TYPE_AT_INDEX

**Hash:** `0xF3E31D16CBDCB304` | **JHash:** `0x2C959AF9` | **Build:** `323`

**C++ 原型:**
```cpp
const char* SC_INBOX_MESSAGE_GET_RAW_TYPE_AT_INDEX(int p0)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.SC_INBOX_MESSAGE_GET_RAW_TYPE_AT_INDEX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### SC_INBOX_MESSAGE_PUSH_GAMER_T0_RECIP_LIST

**Hash:** `0xDA024BDBD600F44A` | **JHash:** `0x0B9A3512` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_INBOX_MESSAGE_PUSH_GAMER_T0_RECIP_LIST(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_INBOX_MESSAGE_PUSH_GAMER_T0_RECIP_LIST, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### SC_INBOX_SEND_UGCSTATUPDATE_TO_RECIP_LIST

**Hash:** `0xA68D3D229F4F3B06` | **JHash:** `0x75324674` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_INBOX_SEND_UGCSTATUPDATE_TO_RECIP_LIST(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_INBOX_SEND_UGCSTATUPDATE_TO_RECIP_LIST, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### SC_INBOX_MESSAGE_GET_UGCDATA

**Hash:** `0x69D82604A1A5A254` | **JHash:** `0x88CA3BFC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_MESSAGE_GET_UGCDATA(int p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_MESSAGE_GET_UGCDATA, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SC_INBOX_GET_BOUNTY_DATA_AT_INDEX

**Hash:** `0x87E0052F08BD64E6` | **JHash:** `0xAB3346B5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_INBOX_GET_BOUNTY_DATA_AT_INDEX(int index, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_INBOX_GET_BOUNTY_DATA_AT_INDEX, index, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SC_EMAIL_RETRIEVE_EMAILS

**Hash:** `0x040ADDCBAFA1018A` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_EMAIL_RETRIEVE_EMAILS(int offset, int limit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_EMAIL_RETRIEVE_EMAILS, offset, limit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `offset` | `int` | `int` | |
| `limit` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SC_EMAIL_GET_RETRIEVAL_STATUS

**Hash:** `0x16DA8172459434AA` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_EMAIL_GET_RETRIEVAL_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_EMAIL_GET_RETRIEVAL_STATUS);
```

**返回值:** `int` → `int`

---

### SC_EMAIL_GET_NUM_RETRIEVED_EMAILS

**Hash:** `0x7DB18CA8CAD5B098` | **Build:** `505`

**C++ 原型:**
```cpp
int SC_EMAIL_GET_NUM_RETRIEVED_EMAILS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_EMAIL_GET_NUM_RETRIEVED_EMAILS);
```

**返回值:** `int` → `int`

---

### SC_EMAIL_GET_EMAIL_AT_INDEX

**Hash:** `0x4737980E8A283806` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_EMAIL_GET_EMAIL_AT_INDEX(int p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_EMAIL_GET_EMAIL_AT_INDEX, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### _SC_EMAIL_MARKETING_EMAIL_OPENED

**Hash:** `0x69AA35F3F391CDBA` | **Build:** `3258`

**C++ 原型:**
```cpp
void _SC_EMAIL_MARKETING_EMAIL_OPENED(int index, Hash type)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SC_EMAIL_MARKETING_EMAIL_OPENED, index, type);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `type` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SC_EMAIL_DELETE_EMAILS

**Hash:** `0x44ACA259D67651DB` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_EMAIL_DELETE_EMAILS(Any* p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_EMAIL_DELETE_EMAILS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SC_EMAIL_MESSAGE_PUSH_GAMER_TO_RECIP_LIST

**Hash:** `0x2330C12A7A605D16` | **JHash:** `0x9A703A2B` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_EMAIL_MESSAGE_PUSH_GAMER_TO_RECIP_LIST(Any* gamerHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_EMAIL_MESSAGE_PUSH_GAMER_TO_RECIP_LIST, gamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gamerHandle` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### SC_EMAIL_MESSAGE_CLEAR_RECIP_LIST

**Hash:** `0x55DF6DB45179236E` | **JHash:** `0xD094F11A` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_EMAIL_MESSAGE_CLEAR_RECIP_LIST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_EMAIL_MESSAGE_CLEAR_RECIP_LIST);
```

**返回值:** `void` → `void`

---

### SC_EMAIL_SEND_EMAIL

**Hash:** `0x116FB94DC4B79F17` | **JHash:** `0xAF3C081B` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_EMAIL_SEND_EMAIL(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_EMAIL_SEND_EMAIL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SC_EMAIL_SET_CURRENT_EMAIL_TAG

**Hash:** `0x07DBD622D9533857` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL SC_EMAIL_SET_CURRENT_EMAIL_TAG(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_EMAIL_SET_CURRENT_EMAIL_TAG, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_CACHE_NEW_ROCKSTAR_MSGS

**Hash:** `0xBFA0A56A817C6C7D` | **JHash:** `0x2FB9F53C` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_CACHE_NEW_ROCKSTAR_MSGS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_CACHE_NEW_ROCKSTAR_MSGS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SC_HAS_NEW_ROCKSTAR_MSG

**Hash:** `0xBC1CC91205EC8D6E` | **JHash:** `0x6C5738AB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_HAS_NEW_ROCKSTAR_MSG(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_HAS_NEW_ROCKSTAR_MSG);
```

**返回值:** `BOOL` → `bool`

---

### SC_GET_NEW_ROCKSTAR_MSG

**Hash:** `0xDF649C4E9AFDD788` | **JHash:** `0x468668F0` | **Build:** `323`

**C++ 原型:**
```cpp
const char* SC_GET_NEW_ROCKSTAR_MSG(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.SC_GET_NEW_ROCKSTAR_MSG);
```

**返回值:** `const char*` → `string`

---

### SC_PRESENCE_ATTR_SET_INT

**Hash:** `0x1F1E9682483697C7` | **JHash:** `0x90C74343` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PRESENCE_ATTR_SET_INT(Hash attrHash, int value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PRESENCE_ATTR_SET_INT, attrHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `attrHash` | `Hash` | `uint` | |
| `value` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_PRESENCE_ATTR_SET_FLOAT

**Hash:** `0xC4C4575F62534A24` | **JHash:** `0x94BF0D35` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PRESENCE_ATTR_SET_FLOAT(Hash attrHash, float value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PRESENCE_ATTR_SET_FLOAT, attrHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `attrHash` | `Hash` | `uint` | |
| `value` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SC_PRESENCE_ATTR_SET_STRING

**Hash:** `0x287F1F75D2803595` | **JHash:** `0x3ACE6D6B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PRESENCE_ATTR_SET_STRING(Hash attrHash, const char* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PRESENCE_ATTR_SET_STRING, attrHash, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `attrHash` | `Hash` | `uint` | |
| `value` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_PRESENCE_SET_ACTIVITY_RATING

**Hash:** `0x487912FD248EFDDF` | **JHash:** `0x579B4510` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PRESENCE_SET_ACTIVITY_RATING(Any p0, float p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PRESENCE_SET_ACTIVITY_RATING, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SC_GAMERDATA_GET_INT

**Hash:** `0xC85A7127E7AD02AA` | **JHash:** `0xF8BDA989` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_GAMERDATA_GET_INT(const char* name, int* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_GAMERDATA_GET_INT, name, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `value` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_GAMERDATA_GET_FLOAT

**Hash:** `0xA770C8EEC6FB2AC5` | **JHash:** `0x515AF67C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_GAMERDATA_GET_FLOAT(const char* name, float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_GAMERDATA_GET_FLOAT, name, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### SC_GAMERDATA_GET_BOOL

**Hash:** `0x8416FE4E4629D7D7` | **JHash:** `0xDF45B2A7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_GAMERDATA_GET_BOOL(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_GAMERDATA_GET_BOOL, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_GAMERDATA_GET_STRING

**Hash:** `0x7FFCBFEE44ECFABF` | **JHash:** `0x2AE35169` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_GAMERDATA_GET_STRING(const char* name, char* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_GAMERDATA_GET_STRING, name, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `value` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_GAMERDATA_GET_ACTIVE_XP_BONUS

**Hash:** `0x2D874D4AE612A65F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_GAMERDATA_GET_ACTIVE_XP_BONUS(float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_GAMERDATA_GET_ACTIVE_XP_BONUS, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### SC_PROFANITY_CHECK_STRING

**Hash:** `0x75632C5ECD7ED843` | **JHash:** `0xDF084A6B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_CHECK_STRING(const char* string, int* token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_CHECK_STRING, string, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |
| `token` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Starts a task to check an entered string for profanity on the ROS/Social Club services.

See also: 1753344C770358AE, 82E4A58BABC15AE7.

---

### SC_PROFANITY_CHECK_STRING_UGC

**Hash:** `0xEB2BF817463DFA28` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_CHECK_STRING_UGC(const char* string, int* token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_CHECK_STRING_UGC, string, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `string` | `const char*` | `string` | |
| `token` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_PROFANITY_GET_CHECK_IS_VALID

**Hash:** `0x1753344C770358AE` | **JHash:** `0xFFED3676` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_GET_CHECK_IS_VALID(int token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_GET_CHECK_IS_VALID, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_PROFANITY_GET_CHECK_IS_PENDING

**Hash:** `0x82E4A58BABC15AE7` | **JHash:** `0xA796D7A7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_GET_CHECK_IS_PENDING(int token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_GET_CHECK_IS_PENDING, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_PROFANITY_GET_STRING_PASSED

**Hash:** `0x85535ACF97FC0969` | **JHash:** `0x09497F31` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_GET_STRING_PASSED(int token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_GET_STRING_PASSED, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_PROFANITY_GET_STRING_STATUS

**Hash:** `0x930DE22F07B1CCE3` | **JHash:** `0x4D8A6521` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_PROFANITY_GET_STRING_STATUS(int token)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_PROFANITY_GET_STRING_STATUS, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SC_PROFANITY_GET_PROFANE_WORD

**Hash:** `0x75CC8931A11128C9` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL SC_PROFANITY_GET_PROFANE_WORD(int token, char* outProfaneWord)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PROFANITY_GET_PROFANE_WORD, token, outProfaneWord);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |
| `outProfaneWord` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Retrieves the profane part of the string with max size of 128.
Returns true if profane term was retrieved.

---

### SC_LICENSEPLATE_CHECK_STRING

**Hash:** `0xF6BAAAF762E1BF40` | **JHash:** `0x7AA36406` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_CHECK_STRING(const char* p0, int* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_CHECK_STRING, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_CHECK_IS_VALID

**Hash:** `0xF22CA0FD74B80E7A` | **JHash:** `0xF379DCE4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_GET_CHECK_IS_VALID(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_GET_CHECK_IS_VALID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_CHECK_IS_PENDING

**Hash:** `0x9237E334F6E43156` | **JHash:** `0x65D84665` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_GET_CHECK_IS_PENDING(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_GET_CHECK_IS_PENDING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_COUNT

**Hash:** `0x700569DBA175A77C` | **JHash:** `0x159FE39C` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_LICENSEPLATE_GET_COUNT(int token)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_LICENSEPLATE_GET_COUNT, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SC_LICENSEPLATE_GET_PLATE

**Hash:** `0x1D4446A62D35B0D0` | **JHash:** `0x4CE9BAA7` | **Build:** `323`

**C++ 原型:**
```cpp
const char* SC_LICENSEPLATE_GET_PLATE(int token, int plateIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.SC_LICENSEPLATE_GET_PLATE, token, plateIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |
| `plateIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### SC_LICENSEPLATE_GET_PLATE_DATA

**Hash:** `0x2E89990DDFF670C3` | **JHash:** `0x05303FC8` | **Build:** `323`

**C++ 原型:**
```cpp
const char* SC_LICENSEPLATE_GET_PLATE_DATA(int token, int plateIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.SC_LICENSEPLATE_GET_PLATE_DATA, token, plateIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |
| `plateIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### SC_LICENSEPLATE_SET_PLATE_DATA

**Hash:** `0xD0EE05FE193646EA` | **JHash:** `0x9F98FA5C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_SET_PLATE_DATA(const char* oldPlateText, const char* newPlateText, Any* plateData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_SET_PLATE_DATA, oldPlateText, newPlateText, plateData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `oldPlateText` | `const char*` | `string` | |
| `newPlateText` | `const char*` | `string` | |
| `plateData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_ADD

**Hash:** `0x1989C6E6F67E76A8` | **JHash:** `0xEEDE7FAB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_ADD(const char* plateText, Any* plateData, int* token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_ADD, plateText, plateData, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plateText` | `const char*` | `string` | |
| `plateData` | `Any*` | `object*` | |
| `token` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_ADD_IS_PENDING

**Hash:** `0x07C61676E5BB52CD` | **JHash:** `0xE190E45A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_GET_ADD_IS_PENDING(int token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_GET_ADD_IS_PENDING, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_ADD_STATUS

**Hash:** `0x8147FFF6A718E1AD` | **JHash:** `0x930B3AE5` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_LICENSEPLATE_GET_ADD_STATUS(int token)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_LICENSEPLATE_GET_ADD_STATUS, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SC_LICENSEPLATE_ISVALID

**Hash:** `0x0F73393BAC7E6730` | **JHash:** `0x5298F472` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_ISVALID(const char* plateText, int* token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_ISVALID, plateText, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plateText` | `const char*` | `string` | |
| `token` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_ISVALID_IS_PENDING

**Hash:** `0xD302E99EDF0449CF` | **JHash:** `0x86DAE6D6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_LICENSEPLATE_GET_ISVALID_IS_PENDING(int token)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_LICENSEPLATE_GET_ISVALID_IS_PENDING, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_LICENSEPLATE_GET_ISVALID_STATUS

**Hash:** `0x5C4EBFFA98BDB41C` | **JHash:** `0x9AA2BA3F` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_LICENSEPLATE_GET_ISVALID_STATUS(int token)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_LICENSEPLATE_GET_ISVALID_STATUS, token);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `token` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SC_COMMUNITY_EVENT_IS_ACTIVE

**Hash:** `0xFF8F3A92B75ED67A` | **JHash:** `0xC96456BA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_IS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_IS_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EVENT_ID

**Hash:** `0x4ED9C8D6DA297639` | **JHash:** `0xC5E08B66` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_COMMUNITY_EVENT_GET_EVENT_ID(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_COMMUNITY_EVENT_GET_EVENT_ID);
```

**返回值:** `int` → `int`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT

**Hash:** `0x710BCDA8071EDED1` | **JHash:** `0x04EE74A3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT(const char* p0, int* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT

**Hash:** `0x50A8A36201DBF83E` | **JHash:** `0x8DA93C3D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT(const char* p0, float* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING

**Hash:** `0x9DE5D2F723575ED0` | **JHash:** `0x37DD48B5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING(const char* p0, char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_DISPLAY_NAME

**Hash:** `0xC2C97EA97711D1AE` | **JHash:** `0xF929F61D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_DISPLAY_NAME(char* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_DISPLAY_NAME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_IS_ACTIVE_FOR_TYPE

**Hash:** `0x450819D8CF90C416` | **JHash:** `0x0572EB5E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_IS_ACTIVE_FOR_TYPE(const char* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_IS_ACTIVE_FOR_TYPE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EVENT_ID_FOR_TYPE

**Hash:** `0x4A7D6E727F941747` | **JHash:** `0x8E7AEEB7` | **Build:** `323`

**C++ 原型:**
```cpp
int SC_COMMUNITY_EVENT_GET_EVENT_ID_FOR_TYPE(const char* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SC_COMMUNITY_EVENT_GET_EVENT_ID_FOR_TYPE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_FOR_TYPE

**Hash:** `0xE75A4A2E5E316D86` | **JHash:** `0x28C57FA0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_FOR_TYPE(const char* p0, int* p1, const char* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_FOR_TYPE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int*` | `int*` | |
| `p2` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_FOR_TYPE

**Hash:** `0x2570E26BE63964E3` | **JHash:** `0x59394583` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_FOR_TYPE(const char* p0, float* p1, const char* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_FOR_TYPE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `float*` | `float*` | |
| `p2` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_FOR_TYPE

**Hash:** `0x1D12A56FC95BE92E` | **JHash:** `0x92452C0E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_FOR_TYPE(const char* p0, char* p1, const char* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_FOR_TYPE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `char*` | `string` | |
| `p2` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_FOR_TYPE

**Hash:** `0x33DF47CC0642061B` | **JHash:** `0xBBA8D34D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_FOR_TYPE(char* p0, const char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_FOR_TYPE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `char*` | `string` | |
| `p1` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_IS_ACTIVE_BY_ID

**Hash:** `0xA468E0BE12B12C70` | **JHash:** `0xB21288FA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_IS_ACTIVE_BY_ID(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_IS_ACTIVE_BY_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_BY_ID

**Hash:** `0x8CC469AB4D349B7C` | **JHash:** `0xE778B2A7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_BY_ID(int p0, const char* p1, int* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_INT_BY_ID, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_BY_ID

**Hash:** `0xC5A35C73B68F3C49` | **JHash:** `0x7E24BD6E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_BY_ID(int p0, const char* p1, float* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_FLOAT_BY_ID, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_BY_ID

**Hash:** `0x699E4A5C8C893A18` | **JHash:** `0xCE7D50A8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_BY_ID(int p0, const char* p1, char* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_EXTRA_DATA_STRING_BY_ID, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `const char*` | `string` | |
| `p2` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_BY_ID

**Hash:** `0x19853B5B17D77BCA` | **JHash:** `0xD26CCA46` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_BY_ID(int p0, char* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_COMMUNITY_EVENT_GET_DISPLAY_NAME_BY_ID, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_SHOW

**Hash:** `0x6BFB12CE158E3DD4` | **JHash:** `0x24D84334` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_TRANSITION_NEWS_SHOW(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_TRANSITION_NEWS_SHOW, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_SHOW_TIMED

**Hash:** `0xFE4C1D0D3B9CC17E` | **JHash:** `0x8A023024` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_TRANSITION_NEWS_SHOW_TIMED(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_TRANSITION_NEWS_SHOW_TIMED, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_SHOW_NEXT_ITEM

**Hash:** `0xD8122C407663B995` | **JHash:** `0x1C65B038` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_TRANSITION_NEWS_SHOW_NEXT_ITEM(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_TRANSITION_NEWS_SHOW_NEXT_ITEM);
```

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_HAS_EXTRA_DATA_TU

**Hash:** `0x3001BEF2FECA3680` | **JHash:** `0x4D4C37B3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_TRANSITION_NEWS_HAS_EXTRA_DATA_TU(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_TRANSITION_NEWS_HAS_EXTRA_DATA_TU);
```

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_GET_EXTRA_DATA_INT_TU

**Hash:** `0x92DA6E70EF249BD1` | **JHash:** `0xAED95A6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_TRANSITION_NEWS_GET_EXTRA_DATA_INT_TU(const char* p0, int* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_TRANSITION_NEWS_GET_EXTRA_DATA_INT_TU, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_TRANSITION_NEWS_END

**Hash:** `0x675721C9F644D161` | **JHash:** `0x486867E6` | **Build:** `323`

**C++ 原型:**
```cpp
void SC_TRANSITION_NEWS_END(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_TRANSITION_NEWS_END);
```

**返回值:** `void` → `void`

---

### SC_PAUSE_NEWS_INIT_STARTER_PACK

**Hash:** `0xE4F6E8D07A2F0F51` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL SC_PAUSE_NEWS_INIT_STARTER_PACK(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PAUSE_NEWS_INIT_STARTER_PACK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SC_PAUSE_NEWS_GET_PENDING_STORY

**Hash:** `0x8A4416C0DB05FA66` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL SC_PAUSE_NEWS_GET_PENDING_STORY(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_PAUSE_NEWS_GET_PENDING_STORY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** Fills some 0x30 sized struct

---

### SC_PAUSE_NEWS_SHUTDOWN

**Hash:** `0xEA95C0853A27888E` | **Build:** `1290`

**C++ 原型:**
```cpp
void SC_PAUSE_NEWS_SHUTDOWN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SC_PAUSE_NEWS_SHUTDOWN);
```

**返回值:** `void` → `void`

---

### SC_ACCOUNT_INFO_GET_NICKNAME

**Hash:** `0x198D161F458ECC7F` | **Build:** `323`

**C++ 原型:**
```cpp
const char* SC_ACCOUNT_INFO_GET_NICKNAME(void)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.SC_ACCOUNT_INFO_GET_NICKNAME);
```

**返回值:** `const char*` → `string`

**说明:** Returns the nickname of the logged-in Rockstar Social Club account.

---

### SC_ACHIEVEMENT_INFO_STATUS

**Hash:** `0x225798743970412B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_ACHIEVEMENT_INFO_STATUS(int* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_ACHIEVEMENT_INFO_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SC_HAS_ACHIEVEMENT_BEEN_PASSED

**Hash:** `0x418DC16FAE452C1C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SC_HAS_ACHIEVEMENT_BEEN_PASSED(int achievementId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SC_HAS_ACHIEVEMENT_BEEN_PASSED, achievementId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `achievementId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Same as HAS_ACHIEVEMENT_BEEN_PASSED

---


---

## STATS

**包含 333 个 Native 函数**

### STAT_CLEAR_SLOT_FOR_RELOAD

**Hash:** `0xEB0A72181D4AA4AD` | **JHash:** `0x84BDD475` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_CLEAR_SLOT_FOR_RELOAD(int statSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_CLEAR_SLOT_FOR_RELOAD, statSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:

for (v_2 = 0; v_2 <= 4; v_2 += 1) {
    STATS::STAT_CLEAR_SLOT_FOR_RELOAD(v_2);
}

---

### STAT_LOAD

**Hash:** `0xA651443F437B1CE6` | **JHash:** `0x9E5629F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_LOAD(int statSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_LOAD, statSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SAVE

**Hash:** `0xE07BCA305B82D2FD` | **JHash:** `0xE10A7CA4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SAVE(int p0, BOOL p1, int p2, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SAVE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SET_OPEN_SAVETYPE_IN_JOB

**Hash:** `0x5688585E6D563CD8` | **JHash:** `0xC62406A6` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_SET_OPEN_SAVETYPE_IN_JOB(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_SET_OPEN_SAVETYPE_IN_JOB, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### STAT_LOAD_PENDING

**Hash:** `0xA1750FFAFA181661` | **JHash:** `0x4E9AC983` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_LOAD_PENDING(int statSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_LOAD_PENDING, statSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SAVE_PENDING

**Hash:** `0x7D3A583856F2C5AC` | **JHash:** `0xC3FD3822` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SAVE_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SAVE_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### STAT_SAVE_PENDING_OR_REQUESTED

**Hash:** `0xBBB6AD006F1BBEA3` | **JHash:** `0xA3407CA3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SAVE_PENDING_OR_REQUESTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SAVE_PENDING_OR_REQUESTED);
```

**返回值:** `BOOL` → `bool`

---

### STAT_DELETE_SLOT

**Hash:** `0x49A49BED12794D70` | **JHash:** `0x2F171B94` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_DELETE_SLOT(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_DELETE_SLOT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p0 is characterSlot? seems range from 0 to 2

---

### STAT_SLOT_IS_LOADED

**Hash:** `0x0D0A9F0E7BD91E3C` | **JHash:** `0x7A299C13` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SLOT_IS_LOADED(int statSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SLOT_IS_LOADED, statSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STAT_CLOUD_SLOT_LOAD_FAILED

**Hash:** `0x7F2C4CDF2E82DF4C` | **JHash:** `0x0BF0F4B2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_CLOUD_SLOT_LOAD_FAILED(int p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_CLOUD_SLOT_LOAD_FAILED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### STAT_CLOUD_SLOT_LOAD_FAILED_CODE

**Hash:** `0xE496A53BA5F50A56` | **JHash:** `0xCE6B62B5` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_CLOUD_SLOT_LOAD_FAILED_CODE(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_CLOUD_SLOT_LOAD_FAILED_CODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### STAT_SET_BLOCK_SAVES

**Hash:** `0xF434A10BA01C37D0` | **JHash:** `0xCE7A2411` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_SET_BLOCK_SAVES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_SET_BLOCK_SAVES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### STAT_GET_BLOCK_SAVES

**Hash:** `0x6A7F19756F1A9016` | **Build:** `2060`

**C++ 原型:**
```cpp
BOOL STAT_GET_BLOCK_SAVES(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_BLOCK_SAVES);
```

**返回值:** `BOOL` → `bool`

---

### STAT_CLOUD_SLOT_SAVE_FAILED

**Hash:** `0x7E6946F68A38B74F` | **JHash:** `0x22804C20` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_CLOUD_SLOT_SAVE_FAILED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_CLOUD_SLOT_SAVE_FAILED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### STAT_CLEAR_PENDING_SAVES

**Hash:** `0xA8733668D1047B51` | **JHash:** `0x395D18B1` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_CLEAR_PENDING_SAVES(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_CLEAR_PENDING_SAVES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### STAT_LOAD_DIRTY_READ_DETECTED

**Hash:** `0xECB41AC6AB754401` | **JHash:** `0xED7000C8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_LOAD_DIRTY_READ_DETECTED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_LOAD_DIRTY_READ_DETECTED);
```

**返回值:** `BOOL` → `bool`

---

### STAT_CLEAR_DIRTY_READ_DETECTED

**Hash:** `0x9B4BD21D69B1E609` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_CLEAR_DIRTY_READ_DETECTED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_CLEAR_DIRTY_READ_DETECTED);
```

**返回值:** `void` → `void`

---

### STAT_GET_LOAD_SAFE_TO_PROGRESS_TO_MP_FROM_SP

**Hash:** `0xC0E0D686DDFC6EAE` | **JHash:** `0x099FCC86` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_LOAD_SAFE_TO_PROGRESS_TO_MP_FROM_SP(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_LOAD_SAFE_TO_PROGRESS_TO_MP_FROM_SP);
```

**返回值:** `BOOL` → `bool`

---

### _GET_STAT_HASH_FOR_CHARACTER_STAT

**Hash:** `0xD69CE161FE614531` | **Build:** `2944`

**C++ 原型:**
```cpp
Hash _GET_STAT_HASH_FOR_CHARACTER_STAT(int dataType, int statIndex, int charSlot)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash._GET_STAT_HASH_FOR_CHARACTER_STAT, dataType, statIndex, charSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dataType` | `int` | `int` | |
| `statIndex` | `int` | `int` | |
| `charSlot` | `int` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns stat hash based on dataType, statIndex/statId and characterSlot. Related to CStatsMpCharacterMappingData

---

### STAT_SET_INT

**Hash:** `0xB3271D7AB655B441` | **JHash:** `0xC9CC1C5C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_INT(Hash statName, int value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_INT, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `int` | `int` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:
 STATS::STAT_SET_INT(MISC::GET_HASH_KEY("MPPLY_KILLS_PLAYERS"), 1337, true);

---

### STAT_SET_FLOAT

**Hash:** `0x4851997F37FE9B3C` | **JHash:** `0x6CEA96F2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_FLOAT(Hash statName, float value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_FLOAT, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `float` | `float` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:
 STATS::STAT_SET_FLOAT(MISC::GET_HASH_KEY("MP0_WEAPON_ACCURACY"), 66.6f, true);

---

### STAT_SET_BOOL

**Hash:** `0x4B33C4243DE0C432` | **JHash:** `0x55D79DFB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_BOOL(Hash statName, BOOL value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_BOOL, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `BOOL` | `bool` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Example:
 STATS::STAT_SET_BOOL(MISC::GET_HASH_KEY("MPPLY_MELEECHLENGECOMPLETED"), trur, true);

---

### STAT_SET_GXT_LABEL

**Hash:** `0x17695002FD8B2AE0` | **JHash:** `0xC1224AA7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_GXT_LABEL(Hash statName, const char* value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_GXT_LABEL, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `const char*` | `string` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** The following values have been found in the decompiled scripts:
"RC_ABI1"
"RC_ABI2"
"RC_BA1"
"RC_BA2"
"RC_BA3"
"RC_BA3A"
"RC_BA3C"
"RC_BA4"
"RC_DRE1"
"RC_EPS1"
"RC_EPS2"
"RC_EPS3"
"RC_EPS4"
"RC_EPS5"
"RC_EPS6"
"RC_EPS7"
"RC_EPS8"
"RC_EXT1"
"RC_EXT2"
"RC_EXT3"
"RC_EXT4"
"RC_FAN1"
"RC_FAN2"
"RC_FAN3"
"RC_HAO1"
"RC_HUN1"
"RC_HUN2"
"RC_JOS1"
"RC_JOS2"
"RC_JOS3"
"RC_JOS4"
"RC_MAU1"
"RC_MIN1"
"RC_MIN2"
"RC_MIN3"
"RC_MRS1"
"RC_MRS2"
"RC_NI1"
"RC_NI1A"
"RC_NI1B"
"RC_NI1C"
"RC_NI1D"
"RC_NI2"
"RC_NI3"
"RC_OME1"
"RC_OME2"
"RC_PA1"
"RC_PA2"
"RC_PA3"
"RC_PA3A"
"RC_PA3B"
"RC_PA4"
"RC_RAM1"
"RC_RAM2"
"RC_RAM3"
"RC_RAM4"
"RC_RAM5"
"RC_SAS1"
"RC_TON1"
"RC_TON2"
"RC_TON3"
"RC_TON4"
"RC_TON5"

---

### STAT_SET_DATE

**Hash:** `0x2C29BFB64F4FCBE4` | **JHash:** `0x36BE807B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_DATE(Hash statName, Any* value, int numFields, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_DATE, statName, value, numFields, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `Any*` | `object*` | |
| `numFields` | `int` | `int` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** 'value' is a structure to a structure, 'numFields' is how many fields there are in said structure (usually 7).

The structure looks like this:

int year
int month
int day
int hour
int minute
int second
int millisecond

The decompiled scripts use TIME::GET_POSIX_TIME to fill this structure.

---

### STAT_SET_STRING

**Hash:** `0xA87B2335D12531D7` | **JHash:** `0xB1EF2E21` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_STRING(Hash statName, const char* value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_STRING, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `const char*` | `string` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SET_POS

**Hash:** `0xDB283FDE680FE72E` | **JHash:** `0x1192C9A3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_POS(Hash statName, float x, float y, float z, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_POS, statName, x, y, z, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SET_MASKED_INT

**Hash:** `0x7BBB1B54583ED410` | **JHash:** `0x2CBAA739` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_MASKED_INT(Hash statName, int p1, int p2, int p3, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_MASKED_INT, statName, p1, p2, p3, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SET_USER_ID

**Hash:** `0x8CDDF1E452BABE11` | **JHash:** `0xDBE78ED7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_USER_ID(Hash statName, const char* value, BOOL save)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_USER_ID, statName, value, save);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `const char*` | `string` | |
| `save` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### STAT_SET_CURRENT_POSIX_TIME

**Hash:** `0xC2F84B7F9C4D0C61` | **JHash:** `0xA286F015` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_CURRENT_POSIX_TIME(Hash statName, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_CURRENT_POSIX_TIME, statName, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 always true.

---

### STAT_GET_INT

**Hash:** `0x767FBC2AC802EF3D` | **JHash:** `0x1C6FE43E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_INT(Hash statHash, int* outValue, int p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_INT, statHash, outValue, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `int*` | `int*` | |
| `p2` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 appears to always be -1


---

### STAT_GET_FLOAT

**Hash:** `0xD7AE6C9C9C6AC54C` | **JHash:** `0xFCBDA612` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_FLOAT(Hash statHash, float* outValue, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_FLOAT, statHash, outValue, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `float*` | `float*` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### STAT_GET_BOOL

**Hash:** `0x11B5E6D2AE73F48E` | **JHash:** `0x28A3DD2B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_BOOL(Hash statHash, BOOL* outValue, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_BOOL, statHash, outValue, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `BOOL*` | `bool*` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### STAT_GET_DATE

**Hash:** `0x8B0FACEFC36C824B` | **JHash:** `0xD762D16C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_DATE(Hash statHash, Any* outValue, int numFields, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_DATE, statHash, outValue, numFields, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `Any*` | `object*` | |
| `numFields` | `int` | `int` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p3 is probably characterSlot or BOOL save, always -1

---

### STAT_GET_STRING

**Hash:** `0xE50384ACC2C3DB74` | **JHash:** `0x10CE4BDE` | **Build:** `323`

**C++ 原型:**
```cpp
const char* STAT_GET_STRING(Hash statHash, int p1)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.STAT_GET_STRING, statHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** p1 is always -1 in the script files

---

### STAT_GET_POS

**Hash:** `0x350F82CCB186AA1B` | **JHash:** `0xC846ECCE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_POS(Hash statName, float* outX, float* outY, float* outZ, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_POS, statName, outX, outY, outZ, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `outX` | `float*` | `float*` | |
| `outY` | `float*` | `float*` | |
| `outZ` | `float*` | `float*` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p3 is probably characterSlot or BOOL save, always -1

---

### STAT_GET_MASKED_INT

**Hash:** `0x655185A06D9EEAAB` | **JHash:** `0xE9D9B70F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_GET_MASKED_INT(Hash statHash, int* outValue, int p2, int p3, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_MASKED_INT, statHash, outValue, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `outValue` | `int*` | `int*` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** p4 is probably characterSlot or BOOL save

---

### STAT_GET_USER_ID

**Hash:** `0x2365C388E393BBE2` | **JHash:** `0xE2E8B6BA` | **Build:** `323`

**C++ 原型:**
```cpp
const char* STAT_GET_USER_ID(Hash statHash)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.STAT_GET_USER_ID, statHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |

**返回值:** `const char*` → `string`

**说明:** Returns the rockstar ID (user id) value of a given stat. Returns "STAT_UNKNOWN" if the statHash is invalid or the stat has no userId

---

### STAT_GET_LICENSE_PLATE

**Hash:** `0x5473D4195058B2E4` | **JHash:** `0x1544B29F` | **Build:** `323`

**C++ 原型:**
```cpp
const char* STAT_GET_LICENSE_PLATE(Hash statName)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.STAT_GET_LICENSE_PLATE, statName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |

**返回值:** `const char*` → `string`

---

### STAT_SET_LICENSE_PLATE

**Hash:** `0x69FF13266D7296DA` | **JHash:** `0x3507D253` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SET_LICENSE_PLATE(Hash statName, const char* str)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SET_LICENSE_PLATE, statName, str);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `str` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### STAT_INCREMENT

**Hash:** `0x9B5A68C6489E9909` | **JHash:** `0xDFC5F71E` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_INCREMENT(Hash statName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_INCREMENT, statName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STAT_COMMUNITY_START_SYNCH

**Hash:** `0x5A556B229A169402` | **JHash:** `0x46F21343` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_COMMUNITY_START_SYNCH(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_COMMUNITY_START_SYNCH);
```

**返回值:** `BOOL` → `bool`

---

### STAT_COMMUNITY_SYNCH_IS_PENDING

**Hash:** `0xB1D2BB1E1631F5B1` | **JHash:** `0x02F283CE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_COMMUNITY_SYNCH_IS_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_COMMUNITY_SYNCH_IS_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### STAT_COMMUNITY_GET_HISTORY

**Hash:** `0xBED9F5693F34ED17` | **JHash:** `0xC4110917` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_COMMUNITY_GET_HISTORY(Hash statName, int p1, float* outValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_COMMUNITY_GET_HISTORY, statName, p1, outValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `outValue` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### STAT_RESET_ALL_ONLINE_CHARACTER_STATS

**Hash:** `0x26D7399B9587FE89` | **JHash:** `0x343B27E2` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_RESET_ALL_ONLINE_CHARACTER_STATS(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_RESET_ALL_ONLINE_CHARACTER_STATS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 seems to range from 0 to 7

---

### STAT_LOCAL_RESET_ALL_ONLINE_CHARACTER_STATS

**Hash:** `0xA78B8FA58200DA56` | **JHash:** `0xE3247582` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_LOCAL_RESET_ALL_ONLINE_CHARACTER_STATS(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_LOCAL_RESET_ALL_ONLINE_CHARACTER_STATS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 seems to range from 0 to 7

---

### STAT_GET_NUMBER_OF_DAYS

**Hash:** `0xE0E854F5280FB769` | **JHash:** `0xFD66A429` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_NUMBER_OF_DAYS(Hash statName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_NUMBER_OF_DAYS, statName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### STAT_GET_NUMBER_OF_HOURS

**Hash:** `0xF2D4B2FE415AAFC3` | **JHash:** `0x9B431236` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_NUMBER_OF_HOURS(Hash statName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_NUMBER_OF_HOURS, statName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### STAT_GET_NUMBER_OF_MINUTES

**Hash:** `0x7583B4BE4C5A41B5` | **JHash:** `0x347B4436` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_NUMBER_OF_MINUTES(Hash statName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_NUMBER_OF_MINUTES, statName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### STAT_GET_NUMBER_OF_SECONDS

**Hash:** `0x2CE056FF3723F00B` | **JHash:** `0x2C1D6C31` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_NUMBER_OF_SECONDS(Hash statName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_NUMBER_OF_SECONDS, statName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statName` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### STAT_SET_PROFILE_SETTING_VALUE

**Hash:** `0x68F01422BE1D838F` | **JHash:** `0x24DD4929` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_SET_PROFILE_SETTING_VALUE(int profileSetting, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_SET_PROFILE_SETTING_VALUE, profileSetting, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `profileSetting` | `int` | `int` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Does not take effect immediately, unfortunately.

profileSetting seems to only be 936, 937 and 938 in scripts

---

### STATS_COMPLETED_CHARACTER_CREATION

**Hash:** `0xC01D2470F22CDE5A` | **Build:** `2372`

**C++ 原型:**
```cpp
void STATS_COMPLETED_CHARACTER_CREATION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STATS_COMPLETED_CHARACTER_CREATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### PACKED_STAT_GET_INT_STAT_INDEX

**Hash:** `0x94F12ABF9C79E339` | **JHash:** `0xCA160BCC` | **Build:** `323`

**C++ 原型:**
```cpp
int PACKED_STAT_GET_INT_STAT_INDEX(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.PACKED_STAT_GET_INT_STAT_INDEX, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Needs more research. Possibly used to calculate the "mask" when calling "STAT_SET_MASKED_INT"?

---

### GET_PACKED_INT_STAT_KEY

**Hash:** `0x61E111E323419E07` | **JHash:** `0x1F938864` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PACKED_INT_STAT_KEY(int index, BOOL spStat, BOOL charStat, int character)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PACKED_INT_STAT_KEY, index, spStat, charStat, character);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `spStat` | `BOOL` | `bool` | |
| `charStat` | `BOOL` | `bool` | |
| `character` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_PACKED_TU_INT_STAT_KEY

**Hash:** `0xD16C2AD6B8E32854` | **JHash:** `0xFB93C5A2` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PACKED_TU_INT_STAT_KEY(int index, BOOL spStat, BOOL charStat, int character)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PACKED_TU_INT_STAT_KEY, index, spStat, charStat, character);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `spStat` | `BOOL` | `bool` | |
| `charStat` | `BOOL` | `bool` | |
| `character` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### GET_PACKED_NG_INT_STAT_KEY

**Hash:** `0x2B4CDCA6F07FF3DA` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PACKED_NG_INT_STAT_KEY(int index, BOOL spStat, BOOL charStat, int character, const char* section)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PACKED_NG_INT_STAT_KEY, index, spStat, charStat, character, section);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `spStat` | `BOOL` | `bool` | |
| `charStat` | `BOOL` | `bool` | |
| `character` | `int` | `int` | |
| `section` | `const char*` | `string` | |

**返回值:** `Hash` → `uint`

**说明:** Needs more research. Gets the stat name of a masked int?

section - values used in the decompiled scripts:
"_NGPSTAT_INT"
"_MP_NGPSTAT_INT"
"_MP_LRPSTAT_INT"
"_MP_APAPSTAT_INT"
"_MP_LR2PSTAT_INT"
"_MP_BIKEPSTAT_INT"
"_MP_IMPEXPPSTAT_INT"
"_MP_GUNRPSTAT_INT"
"_NGDLCPSTAT_INT"
"_MP_NGDLCPSTAT_INT"
"_DLCSMUGCHARPSTAT_INT"
"_GANGOPSPSTAT_INT"
"_BUSINESSBATPSTAT_INT"
"_ARENAWARSPSTAT_INT"
"_CASINOPSTAT_INT"
"_CASINOHSTPSTAT_INT"

---

### GET_PACKED_STAT_BOOL_CODE

**Hash:** `0xDA7EBFC49AE3F1B0` | **Build:** `2545`

**C++ 原型:**
```cpp
BOOL GET_PACKED_STAT_BOOL_CODE(int index, int characterSlot)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PACKED_STAT_BOOL_CODE, index, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PACKED_STAT_INT_CODE

**Hash:** `0x0BC900A6FE73770C` | **Build:** `2545`

**C++ 原型:**
```cpp
int GET_PACKED_STAT_INT_CODE(int index, int characterSlot)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PACKED_STAT_INT_CODE, index, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `int` → `int`

---

### SET_PACKED_STAT_BOOL_CODE

**Hash:** `0xDB8A58AEAA67CD07` | **Build:** `2545`

**C++ 原型:**
```cpp
void SET_PACKED_STAT_BOOL_CODE(int index, BOOL value, int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PACKED_STAT_BOOL_CODE, index, value, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `value` | `BOOL` | `bool` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PACKED_STAT_INT_CODE

**Hash:** `0x1581503AE529CD2E` | **Build:** `2545`

**C++ 原型:**
```cpp
void SET_PACKED_STAT_INT_CODE(int index, int value, int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PACKED_STAT_INT_CODE, index, value, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `index` | `int` | `int` | |
| `value` | `int` | `int` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BACKGROUND_SCRIPT_ACTION

**Hash:** `0x5009DFD741329729` | **JHash:** `0x61ECC465` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_BACKGROUND_SCRIPT_ACTION(const char* action, int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BACKGROUND_SCRIPT_ACTION, action, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `action` | `const char*` | `string` | |
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_FLOW_LOW

**Hash:** `0xE6A27CDA42887F93` | **Build:** `2944`

**C++ 原型:**
```cpp
void _PLAYSTATS_FLOW_LOW(float posX, float posY, float posZ, const char* action, BOOL p4, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_FLOW_LOW, posX, posY, posZ, action, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `action` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_FLOW_MEDIUM

**Hash:** `0xC4493521BAA12CCE` | **Build:** `3095`

**C++ 原型:**
```cpp
void _PLAYSTATS_FLOW_MEDIUM(float posX, float posY, float posZ, const char* action, BOOL p4, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_FLOW_MEDIUM, posX, posY, posZ, action, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `action` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_FLOW_HIGH

**Hash:** `0xCFB0E9C3456319EA` | **Build:** `3258`

**C++ 原型:**
```cpp
void _PLAYSTATS_FLOW_HIGH(float posX, float posY, float posZ, const char* action, BOOL p4, int p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_FLOW_HIGH, posX, posY, posZ, action, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `action` | `const char*` | `string` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_NPC_INVITE

**Hash:** `0x93054C88E6AA7C44` | **JHash:** `0x598C06F3` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_NPC_INVITE(const char* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_NPC_INVITE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_AWARD_XP

**Hash:** `0x46F917F6B4128FE4` | **JHash:** `0x8770017B` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_AWARD_XP(int amount, Hash type, Hash category)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_AWARD_XP, amount, type, category);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `type` | `Hash` | `uint` | |
| `category` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RANK_UP

**Hash:** `0xC7F2DE41D102BFB4` | **JHash:** `0x56AFB9F5` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_RANK_UP(int rank)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RANK_UP, rank);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `rank` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_STARTED_SESSION_IN_OFFLINEMODE

**Hash:** `0x098760C7461724CD` | **JHash:** `0x896CDF8D` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_STARTED_SESSION_IN_OFFLINEMODE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_STARTED_SESSION_IN_OFFLINEMODE);
```

**返回值:** `void` → `void`

---

### PLAYSTATS_ACTIVITY_DONE

**Hash:** `0xA071E0ED98F91286` | **JHash:** `0x1A66945F` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_ACTIVITY_DONE(int p0, int activityId, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ACTIVITY_DONE, p0, activityId, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `activityId` | `int` | `int` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_LEAVE_JOB_CHAIN

**Hash:** `0xC5BE134EC7BA96A0` | **JHash:** `0xC960E161` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_LEAVE_JOB_CHAIN(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_LEAVE_JOB_CHAIN, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MISSION_STARTED

**Hash:** `0xC19A2925C34D2231` | **JHash:** `0x3AAB699C` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_MISSION_STARTED(const char* p0, Any p1, Any p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MISSION_STARTED, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MISSION_OVER

**Hash:** `0x7C4BB33A8CED7324` | **JHash:** `0x5B90B5FF` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_MISSION_OVER(const char* p0, Any p1, Any p2, BOOL p3, BOOL p4, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MISSION_OVER, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MISSION_CHECKPOINT

**Hash:** `0xC900596A63978C1D` | **JHash:** `0xCDC52280` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_MISSION_CHECKPOINT(const char* p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MISSION_CHECKPOINT, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RANDOM_MISSION_DONE

**Hash:** `0x71862B1D855F32E1` | **JHash:** `0xAC2C7C63` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_RANDOM_MISSION_DONE(const char* name, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RANDOM_MISSION_DONE, name, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ROS_BET

**Hash:** `0x121FB4DDDC2D5291` | **JHash:** `0x413539BC` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_ROS_BET(int amount, int act, Player player, float cm)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ROS_BET, amount, act, player, cm);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |
| `act` | `int` | `int` | |
| `player` | `Player` | `int` | |
| `cm` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RACE_CHECKPOINT

**Hash:** `0x9C375C315099DDE4` | **JHash:** `0x580D5508` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_RACE_CHECKPOINT(Vehicle p0, Any p1, int p2, int p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RACE_CHECKPOINT, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CREATE_MATCH_HISTORY_ID_2

**Hash:** `0x6DEE77AFF8C21BD1` | **JHash:** `0x489E27E7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PLAYSTATS_CREATE_MATCH_HISTORY_ID_2(int* playerAccountId, int* posixTime)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PLAYSTATS_CREATE_MATCH_HISTORY_ID_2, playerAccountId, posixTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `playerAccountId` | `int*` | `int*` | |
| `posixTime` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### PLAYSTATS_MATCH_STARTED

**Hash:** `0xBC80E22DED931E3D` | **JHash:** `0x2BDE85C1` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_MATCH_STARTED(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MATCH_STARTED, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SHOP_ITEM

**Hash:** `0x176852ACAAC173D1` | **JHash:** `0xA4746384` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_SHOP_ITEM(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SHOP_ITEM, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CRATE_DROP_MISSION_DONE

**Hash:** `0x1CAE5D2E3F9A07F0` | **JHash:** `0x6602CED6` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_CRATE_DROP_MISSION_DONE(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CRATE_DROP_MISSION_DONE, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CRATE_CREATED

**Hash:** `0xAFC7E5E075A96F46` | **JHash:** `0xD003E648` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_CRATE_CREATED(float p0, float p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CRATE_CREATED, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HOLD_UP_MISSION_DONE

**Hash:** `0xCB00196B31C39EB1` | **JHash:** `0x759E0EC9` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_HOLD_UP_MISSION_DONE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HOLD_UP_MISSION_DONE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_IMPORT_EXPORT_MISSION_DONE

**Hash:** `0x2B69F5074C894811` | **JHash:** `0x62073DF7` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_IMPORT_EXPORT_MISSION_DONE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_IMPORT_EXPORT_MISSION_DONE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RACE_TO_POINT_MISSION_DONE

**Hash:** `0xADDD1C754E2E2914` | **JHash:** `0x06CE3692` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_RACE_TO_POINT_MISSION_DONE(int p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RACE_TO_POINT_MISSION_DONE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ACQUIRED_HIDDEN_PACKAGE

**Hash:** `0x79AB33F0FBFAC40C` | **JHash:** `0x8D5C7B37` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_ACQUIRED_HIDDEN_PACKAGE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ACQUIRED_HIDDEN_PACKAGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_WEBSITE_VISITED

**Hash:** `0xDDF24D535060F811` | **JHash:** `0x37D152BB` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_WEBSITE_VISITED(Hash scaleformHash, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_WEBSITE_VISITED, scaleformHash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaleformHash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FRIEND_ACTIVITY

**Hash:** `0x0F71DE29AB2258F1` | **JHash:** `0xD1FA1BDB` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_FRIEND_ACTIVITY(int p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FRIEND_ACTIVITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ODDJOB_DONE

**Hash:** `0x69DEA3E9DB727B4C` | **JHash:** `0xFE14A8EA` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_ODDJOB_DONE(int totalTimeMs, int p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ODDJOB_DONE, totalTimeMs, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `totalTimeMs` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### PLAYSTATS_PROP_CHANGE

**Hash:** `0xBA739D6D5A05D6E7` | **JHash:** `0x25740A1D` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_PROP_CHANGE(Ped p0, int p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_PROP_CHANGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Ped` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CLOTH_CHANGE

**Hash:** `0x34B973047A2268B9` | **JHash:** `0x3AFF9E58` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_CLOTH_CHANGE(Ped p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CLOTH_CHANGE, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_WEAPON_MODE_CHANGE

**Hash:** `0xE95C8A1875A02CA4` | **JHash:** `0x79716890` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_WEAPON_MODE_CHANGE(Hash weaponHash, Hash componentHashTo, Hash componentHashFrom)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_WEAPON_MODE_CHANGE, weaponHash, componentHashTo, componentHashFrom);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `componentHashTo` | `Hash` | `uint` | |
| `componentHashFrom` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** This is a typo made by R*. It's supposed to be called PLAYSTATS_WEAPON_MOD_CHANGE.

---

### PLAYSTATS_CHEAT_APPLIED

**Hash:** `0x6058665D72302D3F` | **JHash:** `0x345166F3` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_CHEAT_APPLIED(const char* cheat)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CHEAT_APPLIED, cheat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cheat` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_JOB_ACTIVITY_END

**Hash:** `0xF8C54A461C3E11DC` | **JHash:** `0x04181752` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_JOB_ACTIVITY_END(Any* p0, Any* p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_JOB_ACTIVITY_END, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_JOB_BEND

**Hash:** `0xF5BB8DAC426A52C0` | **JHash:** `0x40520E70` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_JOB_BEND(Any* p0, Any* p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_JOB_BEND, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_JOB_LTS_END

**Hash:** `0xA736CF7FB7C5BFF4` | **JHash:** `0x31002201` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_JOB_LTS_END(Any* p0, Any* p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_JOB_LTS_END, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_JOB_LTS_ROUND_END

**Hash:** `0x14E0B2D1AD1044E0` | **JHash:** `0xDDD1F1F3` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_JOB_LTS_ROUND_END(Any* p0, Any* p1, Any* p2, Any* p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_JOB_LTS_ROUND_END, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |
| `p3` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_QUICKFIX_TOOL

**Hash:** `0x90D0622866E80445` | **JHash:** `0x66FEB701` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_QUICKFIX_TOOL(int element, const char* item)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_QUICKFIX_TOOL, element, item);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `element` | `int` | `int` | |
| `item` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_IDLE_KICK

**Hash:** `0x5DA3A8DE8CB6226F` | **JHash:** `0x9E2B9522` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_IDLE_KICK(int msStoodIdle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_IDLE_KICK, msStoodIdle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `msStoodIdle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SET_JOIN_TYPE

**Hash:** `0xD1032E482629049E` | **JHash:** `0x1AC97138` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_SET_JOIN_TYPE(int joinType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SET_JOIN_TYPE, joinType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `joinType` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST_SAVE_CHEAT

**Hash:** `0xF4FF020A08BC8863` | **JHash:** `0xB731DF54` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST_SAVE_CHEAT(Hash hash, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST_SAVE_CHEAT, hash, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `hash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_APPEND_DIRECTOR_METRIC

**Hash:** `0x46326E13DA4E0546` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAYSTATS_APPEND_DIRECTOR_METRIC(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_APPEND_DIRECTOR_METRIC, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_AWARD_BAD_SPORT

**Hash:** `0x47B32F5611E6E483` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_AWARD_BAD_SPORT(int id)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_AWARD_BAD_SPORT, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_PEGASUS_AS_PERSONAL_AIRCRAFT

**Hash:** `0x9572BD4DD6B72122` | **Build:** `1180`

**C++ 原型:**
```cpp
void PLAYSTATS_PEGASUS_AS_PERSONAL_AIRCRAFT(Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_PEGASUS_AS_PERSONAL_AIRCRAFT, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_SHOPMENU_NAV

**Hash:** `0xF96E9EA876D9DC92` | **Build:** `2802`

**C++ 原型:**
```cpp
void _PLAYSTATS_SHOPMENU_NAV(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_SHOPMENU_NAV, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_CHALLENGES

**Hash:** `0x6A60E43998228229` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_CHALLENGES(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_CHALLENGES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_VEHICLETARGET

**Hash:** `0xBFAFDB5FAAA5C5AB` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_VEHICLETARGET(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_VEHICLETARGET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_URBANWARFARE

**Hash:** `0x8C9D11605E59D955` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_URBANWARFARE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_URBANWARFARE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_CHECKPOINTCOLLECTION

**Hash:** `0x3DE3AA516FB126A4` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_CHECKPOINTCOLLECTION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_CHECKPOINTCOLLECTION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_ATOB

**Hash:** `0xBAA2F0490E146BE8` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_ATOB(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_ATOB, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_PENNEDIN

**Hash:** `0x1A7CE7CD3E653485` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_PENNEDIN(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_PENNEDIN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_PASSTHEPARCEL

**Hash:** `0x419615486BBF1956` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_PASSTHEPARCEL(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_PASSTHEPARCEL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_HOTPROPERTY

**Hash:** `0x84DFC579C2FC214C` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_HOTPROPERTY(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_HOTPROPERTY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_DEADDROP

**Hash:** `0x0A9C7F36E5D7B683` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_DEADDROP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_DEADDROP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_KINGOFTHECASTLE

**Hash:** `0x164C5FF663790845` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_KINGOFTHECASTLE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_KINGOFTHECASTLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_CRIMINALDAMAGE

**Hash:** `0xEDBF6C9B0D2C65C8` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_CRIMINALDAMAGE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_CRIMINALDAMAGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_COMPETITIVEURBANWARFARE

**Hash:** `0x6551B1F7F6CD46EA` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_COMPETITIVEURBANWARFARE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_COMPETITIVEURBANWARFARE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_EVENT_HUNTBEAST

**Hash:** `0x2CD90358F67D0AA8` | **Build:** `463`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_EVENT_HUNTBEAST(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_EVENT_HUNTBEAST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_PIMENU_HIDE_OPTIONS

**Hash:** `0x203B381133817079` | **Build:** `393`

**C++ 原型:**
```cpp
void PLAYSTATS_PIMENU_HIDE_OPTIONS(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_PIMENU_HIDE_OPTIONS, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_NAMED_USER_CONTENT

**Hash:** `0xAA434D7D0A89A95C` | **Build:** `3258`

**C++ 原型:**
```cpp
void _PLAYSTATS_NAMED_USER_CONTENT(BOOL isBoss, int bossType, int bossId1, int bossId2, int textType, const char* textString, int textSource)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_NAMED_USER_CONTENT, isBoss, bossType, bossId1, bossId2, textType, textString, textSource);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `isBoss` | `BOOL` | `bool` | |
| `bossType` | `int` | `int` | |
| `bossId1` | `int` | `int` | |
| `bossId2` | `int` | `int` | |
| `textType` | `int` | `int` | |
| `textString` | `const char*` | `string` | |
| `textSource` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Known text types: NAME_YACHT, NAME_ORG_OFFICE

---

### LEADERBOARDS_GET_NUMBER_OF_COLUMNS

**Hash:** `0x117B45156D7EFF2E` | **JHash:** `0x0A56EE34` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_GET_NUMBER_OF_COLUMNS(int p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_GET_NUMBER_OF_COLUMNS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_GET_COLUMN_ID

**Hash:** `0xC4B5467A1886EA7E` | **JHash:** `0x3821A334` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_GET_COLUMN_ID(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_GET_COLUMN_ID, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_GET_COLUMN_TYPE

**Hash:** `0xBF4FEF46DB7894D3` | **JHash:** `0x6F2820F4` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_GET_COLUMN_TYPE(int p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_GET_COLUMN_TYPE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_READ_CLEAR_ALL

**Hash:** `0xA34CB6E6F0DF4A0B` | **JHash:** `0x233E058A` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_READ_CLEAR_ALL(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_READ_CLEAR_ALL);
```

**返回值:** `int` → `int`

---

### LEADERBOARDS_READ_CLEAR

**Hash:** `0x7CCE5C737A665701` | **JHash:** `0x7090012F` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_READ_CLEAR(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_READ_CLEAR, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_READ_PENDING

**Hash:** `0xAC392C8483342AC2` | **JHash:** `0xEEB8BF5C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_READ_PENDING(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_READ_PENDING, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS_READ_ANY_PENDING

**Hash:** `0xA31FD15197B192BD` | **JHash:** `0x1789437B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_READ_ANY_PENDING(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_READ_ANY_PENDING);
```

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS_READ_SUCCESSFUL

**Hash:** `0x2FB19228983E832C` | **JHash:** `0x3AC5B2F1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_READ_SUCCESSFUL(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_READ_SUCCESSFUL, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_FRIENDS_BY_ROW

**Hash:** `0x918B101666F9CB83` | **JHash:** `0xBD91B136` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_FRIENDS_BY_ROW(Any* p0, Any* p1, Any p2, BOOL p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_FRIENDS_BY_ROW, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_HANDLE

**Hash:** `0xC30713A383BFBF0E` | **JHash:** `0x6B553408` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_HANDLE(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_HANDLE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_RANK

**Hash:** `0xBA2C7DB0C129449A` | **JHash:** `0x1B03F59F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_RANK(Any* p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_RANK, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_RADIUS

**Hash:** `0x5CE587FB5A42C8C4` | **JHash:** `0xC5B7E685` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_RADIUS(Any* p0, Any p1, Any* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_RADIUS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_SCORE_INT

**Hash:** `0x7EEC7E4F6984A16A` | **JHash:** `0xAC020C18` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_SCORE_INT(Any* p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_SCORE_INT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_SCORE_FLOAT

**Hash:** `0xE662C8B759D08F3C` | **JHash:** `0xC678B29F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_SCORE_FLOAT(Any* p0, float p1, Any p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_SCORE_FLOAT, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `float` | `float` | |
| `p2` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_RANK_PREDICTION

**Hash:** `0xC38DC1E90D22547C` | **JHash:** `0x9BEC3401` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_RANK_PREDICTION(Any* p0, Any* p1, Any* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_RANK_PREDICTION, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_BY_PLAFORM

**Hash:** `0xF1AE5DCDBFCA2721` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_BY_PLAFORM(Any* p0, const char* gamerHandleCsv, const char* platformName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_BY_PLAFORM, p0, gamerHandleCsv, platformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `gamerHandleCsv` | `const char*` | `string` | |
| `platformName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_GET_ROW_DATA_START

**Hash:** `0xA0F93D5465B3094D` | **JHash:** `0xC977D6E2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_GET_ROW_DATA_START(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_GET_ROW_DATA_START, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_GET_ROW_DATA_END

**Hash:** `0x71B008056E5692D6` | **JHash:** `0xF2DB6A82` | **Build:** `323`

**C++ 原型:**
```cpp
void LEADERBOARDS2_READ_GET_ROW_DATA_END(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LEADERBOARDS2_READ_GET_ROW_DATA_END);
```

**返回值:** `void` → `void`

---

### LEADERBOARDS2_READ_GET_ROW_DATA_INFO

**Hash:** `0x34770B9CE0E03B91` | **JHash:** `0x766A74FE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_READ_GET_ROW_DATA_INFO(Any p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_READ_GET_ROW_DATA_INFO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS2_READ_GET_ROW_DATA_INT

**Hash:** `0x88578F6EC36B4A3A` | **JHash:** `0x6B90E730` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS2_READ_GET_ROW_DATA_INT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS2_READ_GET_ROW_DATA_INT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS2_READ_GET_ROW_DATA_FLOAT

**Hash:** `0x38491439B6BA7F7D` | **JHash:** `0x509A286F` | **Build:** `323`

**C++ 原型:**
```cpp
float LEADERBOARDS2_READ_GET_ROW_DATA_FLOAT(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.LEADERBOARDS2_READ_GET_ROW_DATA_FLOAT, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `float` → `float`

---

### LEADERBOARDS2_WRITE_DATA

**Hash:** `0xAE2206545888AE49` | **JHash:** `0x5F9DF634` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_WRITE_DATA(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_WRITE_DATA, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS_WRITE_ADD_COLUMN

**Hash:** `0x0BCA1D2C47B0D269` | **JHash:** `0x7524E27B` | **Build:** `323`

**C++ 原型:**
```cpp
void LEADERBOARDS_WRITE_ADD_COLUMN(Any p0, Any p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LEADERBOARDS_WRITE_ADD_COLUMN, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### LEADERBOARDS_WRITE_ADD_COLUMN_LONG

**Hash:** `0x2E65248609523599` | **JHash:** `0x1C5CCC3A` | **Build:** `323`

**C++ 原型:**
```cpp
void LEADERBOARDS_WRITE_ADD_COLUMN_LONG(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LEADERBOARDS_WRITE_ADD_COLUMN_LONG, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### LEADERBOARDS_CACHE_DATA_ROW

**Hash:** `0xB9BB18E2C40142ED` | **JHash:** `0x44F7D82B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_CACHE_DATA_ROW(Any* p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_CACHE_DATA_ROW, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS_CLEAR_CACHE_DATA

**Hash:** `0xD4B02A6B476E1FDC` | **JHash:** `0x87F498C1` | **Build:** `323`

**C++ 原型:**
```cpp
void LEADERBOARDS_CLEAR_CACHE_DATA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LEADERBOARDS_CLEAR_CACHE_DATA);
```

**返回值:** `void` → `void`

---

### LEADERBOARDS_CLEAR_CACHE_DATA_ID

**Hash:** `0x8EC74CEB042E7CFF` | **JHash:** `0x88AE9667` | **Build:** `323`

**C++ 原型:**
```cpp
void LEADERBOARDS_CLEAR_CACHE_DATA_ID(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LEADERBOARDS_CLEAR_CACHE_DATA_ID, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### LEADERBOARDS_GET_CACHE_EXISTS

**Hash:** `0x9C51349BE6CDFE2C` | **JHash:** `0xFC8A71F3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_GET_CACHE_EXISTS(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_GET_CACHE_EXISTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### LEADERBOARDS_GET_CACHE_TIME

**Hash:** `0xF04C1C27DA35F6C8` | **JHash:** `0xEDF02302` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_GET_CACHE_TIME(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_GET_CACHE_TIME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_GET_CACHE_NUMBER_OF_ROWS

**Hash:** `0x58A651CD201D89AD` | **JHash:** `0xCE7CB520` | **Build:** `323`

**C++ 原型:**
```cpp
int LEADERBOARDS_GET_CACHE_NUMBER_OF_ROWS(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.LEADERBOARDS_GET_CACHE_NUMBER_OF_ROWS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### LEADERBOARDS_GET_CACHE_DATA_ROW

**Hash:** `0x9120E8DBA3D69273` | **JHash:** `0xA11289EC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS_GET_CACHE_DATA_ROW(Any p0, Any p1, Any* p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS_GET_CACHE_DATA_ROW, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### PRESENCE_EVENT_UPDATESTAT_INT

**Hash:** `0x11FF1C80276097ED` | **JHash:** `0x4AC39C6C` | **Build:** `323`

**C++ 原型:**
```cpp
void PRESENCE_EVENT_UPDATESTAT_INT(Hash statHash, int value, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRESENCE_EVENT_UPDATESTAT_INT, statHash, value, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `value` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PRESENCE_EVENT_UPDATESTAT_FLOAT

**Hash:** `0x30A6614C1F7799B8` | **JHash:** `0x3E69E7C3` | **Build:** `323`

**C++ 原型:**
```cpp
void PRESENCE_EVENT_UPDATESTAT_FLOAT(Hash statHash, float value, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRESENCE_EVENT_UPDATESTAT_FLOAT, statHash, value, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `value` | `float` | `float` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PRESENCE_EVENT_UPDATESTAT_INT_WITH_STRING

**Hash:** `0x6483C25849031C4F` | **JHash:** `0x2FFD2FA5` | **Build:** `323`

**C++ 原型:**
```cpp
void PRESENCE_EVENT_UPDATESTAT_INT_WITH_STRING(Hash statHash, int value, int p2, const char* string)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRESENCE_EVENT_UPDATESTAT_INT_WITH_STRING, statHash, value, p2, string);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statHash` | `Hash` | `uint` | |
| `value` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `string` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_HAS_DRIVEN_ALL_VEHICLES

**Hash:** `0x5EAD2BF6484852E4` | **JHash:** `0x23D70C39` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PLAYER_HAS_DRIVEN_ALL_VEHICLES(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PLAYER_HAS_DRIVEN_ALL_VEHICLES);
```

**返回值:** `BOOL` → `bool`

---

### SET_HAS_POSTED_ALL_VEHICLES_DRIVEN

**Hash:** `0xC141B8917E0017EC` | **JHash:** `0x0AD43306` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HAS_POSTED_ALL_VEHICLES_DRIVEN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HAS_POSTED_ALL_VEHICLES_DRIVEN);
```

**返回值:** `void` → `void`

---

### SET_PROFILE_SETTING_PROLOGUE_COMPLETE

**Hash:** `0xB475F27C6A994D65` | **JHash:** `0xC7DE5C30` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PROFILE_SETTING_PROLOGUE_COMPLETE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROFILE_SETTING_PROLOGUE_COMPLETE);
```

**返回值:** `void` → `void`

---

### SET_PROFILE_SETTING_SP_CHOP_MISSION_COMPLETE

**Hash:** `0xC67E2DA1CBE759E2` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_PROFILE_SETTING_SP_CHOP_MISSION_COMPLETE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROFILE_SETTING_SP_CHOP_MISSION_COMPLETE);
```

**返回值:** `void` → `void`

**说明:** Sets profile setting 939

---

### SET_PROFILE_SETTING_CREATOR_RACES_DONE

**Hash:** `0xF1A1803D3476F215` | **JHash:** `0xA3DAC790` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PROFILE_SETTING_CREATOR_RACES_DONE(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROFILE_SETTING_CREATOR_RACES_DONE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 933

---

### SET_PROFILE_SETTING_CREATOR_DM_DONE

**Hash:** `0x38BAAA5DD4C9D19F` | **JHash:** `0x726FAE66` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PROFILE_SETTING_CREATOR_DM_DONE(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROFILE_SETTING_CREATOR_DM_DONE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 934

---

### SET_PROFILE_SETTING_CREATOR_CTF_DONE

**Hash:** `0x55384438FC55AD8E` | **JHash:** `0xF03895A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PROFILE_SETTING_CREATOR_CTF_DONE(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PROFILE_SETTING_CREATOR_CTF_DONE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 935

---

### SET_JOB_ACTIVITY_ID_STARTED

**Hash:** `0x723C1CE13FBFDB67` | **JHash:** `0x4C39CF10` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_JOB_ACTIVITY_ID_STARTED(Any p0, int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_JOB_ACTIVITY_ID_STARTED, p0, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_FREEMODE_PROLOGUE_DONE

**Hash:** `0x0D01D20616FC73FB` | **JHash:** `0x2180AE13` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FREEMODE_PROLOGUE_DONE(Any p0, int characterSlot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FREEMODE_PROLOGUE_DONE, p0, characterSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `characterSlot` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_FREEMODE_STRAND_PROGRESSION_STATUS

**Hash:** `0x79D310A861697CC9` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_FREEMODE_STRAND_PROGRESSION_STATUS(int profileSetting, int settingValue)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FREEMODE_STRAND_PROGRESSION_STATUS, profileSetting, settingValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `profileSetting` | `int` | `int` | |
| `settingValue` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 940 and 941

---

### STAT_NETWORK_INCREMENT_ON_SUICIDE

**Hash:** `0x428EAF89E24F6C36` | **JHash:** `0xEE292B91` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_NETWORK_INCREMENT_ON_SUICIDE(Any p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_NETWORK_INCREMENT_ON_SUICIDE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STAT_SET_CHEAT_IS_ACTIVE

**Hash:** `0x047CBED6F6F8B63C` | **JHash:** `0xA063CABD` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_SET_CHEAT_IS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_SET_CHEAT_IS_ACTIVE);
```

**返回值:** `void` → `void`

---

### LEADERBOARDS2_WRITE_DATA_FOR_EVENT_TYPE

**Hash:** `0xC980E62E33DF1D5C` | **JHash:** `0x62C19A3D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL LEADERBOARDS2_WRITE_DATA_FOR_EVENT_TYPE(Any* p0, Any* p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.LEADERBOARDS2_WRITE_DATA_FOR_EVENT_TYPE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### FORCE_CLOUD_MP_STATS_DOWNLOAD_AND_OVERWRITE_LOCAL_SAVE

**Hash:** `0x6F361B8889A792A3` | **JHash:** `0x3B4EF322` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_CLOUD_MP_STATS_DOWNLOAD_AND_OVERWRITE_LOCAL_SAVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_CLOUD_MP_STATS_DOWNLOAD_AND_OVERWRITE_LOCAL_SAVE);
```

**返回值:** `void` → `void`

---

### STAT_MIGRATE_CLEAR_FOR_RESTART

**Hash:** `0xC847B43F369AC0B5` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_MIGRATE_CLEAR_FOR_RESTART(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_MIGRATE_CLEAR_FOR_RESTART);
```

**返回值:** `void` → `void`

---

### STAT_MIGRATE_SAVEGAME_START

**Hash:** `0xA5C80D8E768A9E66` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_MIGRATE_SAVEGAME_START(const char* platformName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_MIGRATE_SAVEGAME_START, platformName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `platformName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** platformName must be one of the following: ps3, xbox360, ps4, xboxone

---

### STAT_MIGRATE_SAVEGAME_GET_STATUS

**Hash:** `0x9A62EC95AE10E011` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_MIGRATE_SAVEGAME_GET_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_MIGRATE_SAVEGAME_GET_STATUS);
```

**返回值:** `int` → `int`

---

### STAT_MIGRATE_CHECK_ALREADY_DONE

**Hash:** `0x4C89FE2BDEB3F169` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_MIGRATE_CHECK_ALREADY_DONE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_MIGRATE_CHECK_ALREADY_DONE);
```

**返回值:** `BOOL` → `bool`

---

### STAT_MIGRATE_CHECK_START

**Hash:** `0xC6E0E2616A7576BB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_MIGRATE_CHECK_START(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_MIGRATE_CHECK_START);
```

**返回值:** `BOOL` → `bool`

---

### STAT_MIGRATE_CHECK_GET_IS_PLATFORM_AVAILABLE

**Hash:** `0x5BD5F255321C4AAF` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_MIGRATE_CHECK_GET_IS_PLATFORM_AVAILABLE(int p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_MIGRATE_CHECK_GET_IS_PLATFORM_AVAILABLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `int` → `int`

---

### STAT_MIGRATE_CHECK_GET_PLATFORM_STATUS

**Hash:** `0xDEAAF77EB3687E97` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_MIGRATE_CHECK_GET_PLATFORM_STATUS(int p0, Any* p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_MIGRATE_CHECK_GET_PLATFORM_STATUS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### STAT_GET_SAVE_MIGRATION_STATUS

**Hash:** `0x886913BBEACA68C1` | **JHash:** `0xE3F0D62D` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_SAVE_MIGRATION_STATUS(Any* data)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_SAVE_MIGRATION_STATUS, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### STAT_SAVE_MIGRATION_CANCEL_PENDING_OPERATION

**Hash:** `0x4FEF53183C3C6414` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SAVE_MIGRATION_CANCEL_PENDING_OPERATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SAVE_MIGRATION_CANCEL_PENDING_OPERATION);
```

**返回值:** `BOOL` → `bool`

---

### STAT_GET_CANCEL_SAVE_MIGRATION_STATUS

**Hash:** `0x567384DFA67029E6` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_CANCEL_SAVE_MIGRATION_STATUS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_CANCEL_SAVE_MIGRATION_STATUS);
```

**返回值:** `int` → `int`

---

### STAT_SAVE_MIGRATION_CONSUME_CONTENT

**Hash:** `0x3270F67EED31FBC1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_SAVE_MIGRATION_CONSUME_CONTENT(Hash contentId, const char* srcPlatform, const char* srcGamerHandle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_SAVE_MIGRATION_CONSUME_CONTENT, contentId, srcPlatform, srcGamerHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `contentId` | `Hash` | `uint` | |
| `srcPlatform` | `const char*` | `string` | |
| `srcGamerHandle` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### STAT_GET_SAVE_MIGRATION_CONSUME_CONTENT_STATUS

**Hash:** `0xCE5AA445ABA8DEE0` | **Build:** `323`

**C++ 原型:**
```cpp
int STAT_GET_SAVE_MIGRATION_CONSUME_CONTENT_STATUS(int* p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_SAVE_MIGRATION_CONSUME_CONTENT_STATUS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int*` | `int*` | |

**返回值:** `int` → `int`

---

### STAT_ENABLE_STATS_TRACKING

**Hash:** `0x98E2BC1CA26287C3` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_ENABLE_STATS_TRACKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_ENABLE_STATS_TRACKING);
```

**返回值:** `void` → `void`

---

### STAT_DISABLE_STATS_TRACKING

**Hash:** `0x629526ABA383BCAA` | **Build:** `323`

**C++ 原型:**
```cpp
void STAT_DISABLE_STATS_TRACKING(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STAT_DISABLE_STATS_TRACKING);
```

**返回值:** `void` → `void`

**说明:** Prevents updates to CStatsMgr (e.g., STAT_SET_* natives)

---

### STAT_IS_STATS_TRACKING_ENABLED

**Hash:** `0xBE3DB208333D9844` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL STAT_IS_STATS_TRACKING_ENABLED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_IS_STATS_TRACKING_ENABLED);
```

**返回值:** `BOOL` → `bool`

---

### STAT_START_RECORD_STAT

**Hash:** `0x33D72899E24C3365` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL STAT_START_RECORD_STAT(int statType, int valueType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_START_RECORD_STAT, statType, valueType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `statType` | `int` | `int` | |
| `valueType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** enum StatTrackingType
{
	LongestWheelie = 1,
	LongestStoppie = 2,
	NoCrashes = 3,
	HighestSpeed = 4,
	_MostFlips = 5,
	_LongestSpin = 6,
	_HighestJumpReached = 7,
	LongestJump = 8,
	_NearMissesNoCrash = 9,
	LongestFallSurvived = 10,
	LowestParachute = 11,
	ReverseDriving = 12,
	LongestFreefall = 13,
	VehiclesStolen = 14,
	_SomeCFireEventCount = 15,
	_Unk16 = 16,
	_LowFlyingTime = 17,
	LowFlying = 18,
	_InvertedFlyingTime = 19,
	InvertedFlying = 20,
	_PlaneSpinCount = 21,
	MeleeKills = 22, // Players
	_LongestSniperKill = 23,
	SniperSkills = 24, // Players
	DrivebyKills = 25, // Players
	HeadshotKills = 26, // Players
	LongestBail = 27,
	_TotalRammedByCar = 28,
	NearMissesPrecise = 29,
	_FreefallTime = 30,
	Unk31 = 31,
}

enum StatTrackingValueType
{
	Total,
	Max,
	Min
}

---

### STAT_STOP_RECORD_STAT

**Hash:** `0xA761D4AC6115623D` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL STAT_STOP_RECORD_STAT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_STOP_RECORD_STAT);
```

**返回值:** `BOOL` → `bool`

---

### STAT_GET_RECORDED_VALUE

**Hash:** `0xF11F01D98113536A` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL STAT_GET_RECORDED_VALUE(float* value)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_RECORDED_VALUE, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### STAT_IS_RECORDING_STAT

**Hash:** `0x8B9CDBD6C566C38C` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL STAT_IS_RECORDING_STAT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_IS_RECORDING_STAT);
```

**返回值:** `BOOL` → `bool`

---

### STAT_GET_CURRENT_NEAR_MISS_NOCRASH_PRECISE

**Hash:** `0xE8853FBCE7D8D0D6` | **Build:** `463`

**C++ 原型:**
```cpp
int STAT_GET_CURRENT_NEAR_MISS_NOCRASH_PRECISE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STAT_GET_CURRENT_NEAR_MISS_NOCRASH_PRECISE);
```

**返回值:** `int` → `int`

**说明:** Perform the most near misses with other vehicles in a land vehicle without crashing

---

### STAT_GET_CURRENT_REAR_WHEEL_DISTANCE

**Hash:** `0xA943FD1722E11EFD` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_REAR_WHEEL_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_REAR_WHEEL_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Perform the longest wheelie on a motorcycle

---

### STAT_GET_CURRENT_FRONT_WHEEL_DISTANCE

**Hash:** `0x84A810B375E69C0E` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_FRONT_WHEEL_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_FRONT_WHEEL_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Perform the longest stoppie on a motorcycle

---

### STAT_GET_CURRENT_JUMP_DISTANCE

**Hash:** `0x9EC8858184CD253A` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_JUMP_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_JUMP_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Perform the longest jump in a land vehicle

---

### STAT_GET_CURRENT_DRIVE_NOCRASH_DISTANCE

**Hash:** `0xBA9749CC94C1FD85` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_DRIVE_NOCRASH_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_DRIVE_NOCRASH_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Drive the furthest distance in a land vehicle without crashing

---

### STAT_GET_CURRENT_SPEED

**Hash:** `0x55A8BECAF28A4EB7` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_SPEED(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_SPEED);
```

**返回值:** `float` → `float`

**说明:** Achieve the highest speed in a land vehicle

---

### STAT_GET_CURRENT_DRIVING_REVERSE_DISTANCE

**Hash:** `0x32CAC93C9DE73D32` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_DRIVING_REVERSE_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_DRIVING_REVERSE_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Reverse the longest distance without crashing

---

### STAT_GET_CURRENT_SKYDIVING_DISTANCE

**Hash:** `0xAFF47709F1D5DCCE` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CURRENT_SKYDIVING_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CURRENT_SKYDIVING_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Fall the longest distance with a parachute before opening it

---

### STAT_GET_CHALLENGE_FLYING_DIST

**Hash:** `0x6E0A5253375C4584` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_CHALLENGE_FLYING_DIST(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_CHALLENGE_FLYING_DIST);
```

**返回值:** `float` → `float`

**说明:** Fly low to the ground for the longest distance

---

### STAT_GET_FLYING_ALTITUDE

**Hash:** `0x1A8EA222F9C67DBB` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL STAT_GET_FLYING_ALTITUDE(float* outValue)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_GET_FLYING_ALTITUDE, outValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outValue` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### STAT_IS_PLAYER_VEHICLE_ABOVE_OCEAN

**Hash:** `0xF9F2922717B819EC` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL STAT_IS_PLAYER_VEHICLE_ABOVE_OCEAN(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_IS_PLAYER_VEHICLE_ABOVE_OCEAN);
```

**返回值:** `BOOL` → `bool`

**说明:** Or non-flyable area

---

### STAT_GET_VEHICLE_BAIL_DISTANCE

**Hash:** `0x0B8B7F74BF061C6D` | **Build:** `463`

**C++ 原型:**
```cpp
float STAT_GET_VEHICLE_BAIL_DISTANCE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.STAT_GET_VEHICLE_BAIL_DISTANCE);
```

**返回值:** `float` → `float`

**说明:** Travel the furthest distance when bailing from a vehicle

---

### STAT_ROLLBACK_SAVE_MIGRATION

**Hash:** `0xB3DA2606774A8E2D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STAT_ROLLBACK_SAVE_MIGRATION(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STAT_ROLLBACK_SAVE_MIGRATION);
```

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 1.

---

### SET_HAS_SPECIALEDITION_CONTENT

**Hash:** `0xDAC073C7901F9E15` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HAS_SPECIALEDITION_CONTENT(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HAS_SPECIALEDITION_CONTENT, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 866

---

### SET_SAVE_MIGRATION_TRANSACTION_ID_WARNING

**Hash:** `0xF6792800AC95350D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SAVE_MIGRATION_TRANSACTION_ID_WARNING(int transactionId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SAVE_MIGRATION_TRANSACTION_ID_WARNING, transactionId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `transactionId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets profile setting 501

---

### GET_BOSS_GOON_UUID

**Hash:** `0x6BC0ACD0673ACEBE` | **Build:** `573`

**C++ 原型:**
```cpp
void GET_BOSS_GOON_UUID(int characterSlot, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_BOSS_GOON_UUID, characterSlot, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `characterSlot` | `int` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_BOSSONBOSSDEATHMATCH

**Hash:** `0x8D8ADB562F09A245` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_BOSSONBOSSDEATHMATCH(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_BOSSONBOSSDEATHMATCH, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_YATCHATTACK

**Hash:** `0xD1A1EE3B4FA8E760` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_YATCHATTACK(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_YATCHATTACK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_HUNT_THE_BOSS

**Hash:** `0x88087EE1F28024AE` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_HUNT_THE_BOSS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_HUNT_THE_BOSS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_SIGHTSEER

**Hash:** `0xFCC228E07217FCAC` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_SIGHTSEER(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_SIGHTSEER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_ASSAULT

**Hash:** `0x678F86D8FC040BDB` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_ASSAULT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_ASSAULT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_BELLY_OF_THE_BEAST

**Hash:** `0xA6F54BB2FFCA35EA` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_BELLY_OF_THE_BEAST(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_BELLY_OF_THE_BEAST, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_HEAD_HUNTER

**Hash:** `0x5FF2C33B13A02A11` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_HEAD_HUNTER(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_HEAD_HUNTER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_FRAGILE_GOODS

**Hash:** `0x282B6739644F4347` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_FRAGILE_GOODS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_FRAGILE_GOODS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BW_AIR_FREIGHT

**Hash:** `0xF06A6F41CB445443` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BW_AIR_FREIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BW_AIR_FREIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_CAR_JACKING

**Hash:** `0x7B18DA61F6BAE9D5` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_CAR_JACKING(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_CAR_JACKING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_SMASH_AND_GRAB

**Hash:** `0x06EAF70AE066441E` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_SMASH_AND_GRAB(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_SMASH_AND_GRAB, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_PROTECTION_RACKET

**Hash:** `0x14EDA9EE27BD1626` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_PROTECTION_RACKET(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_PROTECTION_RACKET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_MOST_WANTED

**Hash:** `0x930F504203F561C9` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_MOST_WANTED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_MOST_WANTED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_FINDERS_KEEPERS

**Hash:** `0xE3261D791EB44ACB` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_FINDERS_KEEPERS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_FINDERS_KEEPERS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_POINT_TO_POINT

**Hash:** `0x73001E34F85137F8` | **Build:** `573`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_POINT_TO_POINT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_POINT_TO_POINT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_CASHING

**Hash:** `0x53CAE13E9B426993` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_CASHING(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_CASHING, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BC_SALVAGE

**Hash:** `0x7D36291161859389` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BC_SALVAGE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BC_SALVAGE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SPENT_PI_CUSTOM_LOADOUT

**Hash:** `0xBE509B0A3693DE8B` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_SPENT_PI_CUSTOM_LOADOUT(int amount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SPENT_PI_CUSTOM_LOADOUT, amount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `amount` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BUY_CONTRABAND_MISSION

**Hash:** `0xD6781E42755531F7` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_BUY_CONTRABAND_MISSION(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BUY_CONTRABAND_MISSION, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SELL_CONTRABAND_MISSION

**Hash:** `0xC729991A9065376E` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_SELL_CONTRABAND_MISSION(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SELL_CONTRABAND_MISSION, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DEFEND_CONTRABAND_MISSION

**Hash:** `0x2605663BD4F23B5D` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_DEFEND_CONTRABAND_MISSION(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DEFEND_CONTRABAND_MISSION, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RECOVER_CONTRABAND_MISSION

**Hash:** `0x04D90BA8207ADA2D` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_RECOVER_CONTRABAND_MISSION(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RECOVER_CONTRABAND_MISSION, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HIT_CONTRABAND_DESTROY_LIMIT

**Hash:** `0x60EEDC12AF66E846` | **Build:** `757`

**C++ 原型:**
```cpp
void PLAYSTATS_HIT_CONTRABAND_DESTROY_LIMIT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HIT_CONTRABAND_DESTROY_LIMIT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### START_BEING_BOSS

**Hash:** `0x3EBEAC6C3F81F6BD` | **Build:** `573`

**C++ 原型:**
```cpp
void START_BEING_BOSS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_BEING_BOSS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### START_BEING_GOON

**Hash:** `0x96E6D5150DBF1C09` | **Build:** `573`

**C++ 原型:**
```cpp
void START_BEING_GOON(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_BEING_GOON, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### END_BEING_BOSS

**Hash:** `0xA3C53804BDB68ED2` | **Build:** `573`

**C++ 原型:**
```cpp
void END_BEING_BOSS(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_BEING_BOSS, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### END_BEING_GOON

**Hash:** `0x6BCCF9948492FD85` | **Build:** `573`

**C++ 原型:**
```cpp
void END_BEING_GOON(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_BEING_GOON, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### HIRED_LIMO

**Hash:** `0x792271AB35C356A4` | **Build:** `573`

**C++ 原型:**
```cpp
void HIRED_LIMO(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIRED_LIMO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### ORDER_BOSS_VEHICLE

**Hash:** `0xCEA553E35C2246E1` | **Build:** `573`

**C++ 原型:**
```cpp
void ORDER_BOSS_VEHICLE(Any p0, Any p1, Hash vehicleHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ORDER_BOSS_VEHICLE, p0, p1, vehicleHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `vehicleHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### CHANGE_UNIFORM

**Hash:** `0xD1C9B92BDD3F151D` | **Build:** `573`

**C++ 原型:**
```cpp
void CHANGE_UNIFORM(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CHANGE_UNIFORM, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CHANGE_GOON_LOOKING_FOR_WORK

**Hash:** `0x44919CC079BB60BF` | **Build:** `573`

**C++ 原型:**
```cpp
void CHANGE_GOON_LOOKING_FOR_WORK(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CHANGE_GOON_LOOKING_FOR_WORK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SEND_METRIC_GHOSTING_TO_PLAYER

**Hash:** `0x7033EEFD9B28088E` | **Build:** `678`

**C++ 原型:**
```cpp
void SEND_METRIC_GHOSTING_TO_PLAYER(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEND_METRIC_GHOSTING_TO_PLAYER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SEND_METRIC_VIP_POACH

**Hash:** `0xAA525DFF66BB82F5` | **Build:** `757`

**C++ 原型:**
```cpp
void SEND_METRIC_VIP_POACH(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEND_METRIC_VIP_POACH, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SEND_METRIC_PUNISH_BODYGUARD

**Hash:** `0x015B03EE1C43E6EC` | **Build:** `757`

**C++ 原型:**
```cpp
void SEND_METRIC_PUNISH_BODYGUARD(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SEND_METRIC_PUNISH_BODYGUARD, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_START_TRACKING_STUNTS

**Hash:** `0x928DBFB892638EF3` | **Build:** `791`

**C++ 原型:**
```cpp
void PLAYSTATS_START_TRACKING_STUNTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_START_TRACKING_STUNTS);
```

**返回值:** `void` → `void`

**说明:** Allows CEventNetworkStuntPerformed to be triggered.

---

### PLAYSTATS_STOP_TRACKING_STUNTS

**Hash:** `0x8A800DACCC0DA55D` | **Build:** `791`

**C++ 原型:**
```cpp
void PLAYSTATS_STOP_TRACKING_STUNTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_STOP_TRACKING_STUNTS);
```

**返回值:** `void` → `void`

**说明:** Disallows CEventNetworkStuntPerformed to be triggered.

---

### PLAYSTATS_MISSION_ENDED

**Hash:** `0xBF371CD2B64212FD` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_MISSION_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MISSION_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_IMPEXP_MISSION_ENDED

**Hash:** `0x7D8BA05688AD64C7` | **Build:** `944`

**C++ 原型:**
```cpp
void PLAYSTATS_IMPEXP_MISSION_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_IMPEXP_MISSION_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CHANGE_MC_ROLE

**Hash:** `0x0B565B0AAE56A0E8` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_CHANGE_MC_ROLE(Any p0, Any p1, Any p2, Any p3, int role, int p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CHANGE_MC_ROLE, p0, p1, p2, p3, role, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `role` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CHANGE_MC_OUTFIT

**Hash:** `0x28ECB8AC2F607DB2` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_CHANGE_MC_OUTFIT(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CHANGE_MC_OUTFIT, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SWITCH_MC_EMBLEM

**Hash:** `0x0A50D2604E05CB94` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_SWITCH_MC_EMBLEM(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SWITCH_MC_EMBLEM, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MC_REQUEST_BIKE

**Hash:** `0xCC25A4553DFBF9EA` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_MC_REQUEST_BIKE(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MC_REQUEST_BIKE, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MC_KILLED_RIVAL_MC_MEMBER

**Hash:** `0xF534D94DFA2EAD26` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_MC_KILLED_RIVAL_MC_MEMBER(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MC_KILLED_RIVAL_MC_MEMBER, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ABANDONED_MC

**Hash:** `0xD558BEC0BBA7E8D2` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_ABANDONED_MC(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ABANDONED_MC, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_EARNED_MC_POINTS

**Hash:** `0x501478855A6074CE` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_EARNED_MC_POINTS(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_EARNED_MC_POINTS, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MC_FORMATION_ENDS

**Hash:** `0x03C2EEBB04B3FB72` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_MC_FORMATION_ENDS(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MC_FORMATION_ENDS, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MC_CLUBHOUSE_ACTIVITY

**Hash:** `0x8989CBD7B4E82534` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_MC_CLUBHOUSE_ACTIVITY(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MC_CLUBHOUSE_ACTIVITY, p0, p1, p2, p3, p4, p5, p6, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_RIVAL_BEHAVIOR

**Hash:** `0x27AA1C973CACFE63` | **Build:** `944`

**C++ 原型:**
```cpp
void PLAYSTATS_RIVAL_BEHAVIOR(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_RIVAL_BEHAVIOR, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_COPY_RANK_INTO_NEW_SLOT

**Hash:** `0xB7257BA2550EA10A` | **Build:** `877`

**C++ 原型:**
```cpp
void PLAYSTATS_COPY_RANK_INTO_NEW_SLOT(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_COPY_RANK_INTO_NEW_SLOT, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DUPE_DETECTED

**Hash:** `0x848B66100EE33B05` | **Build:** `1011`

**C++ 原型:**
```cpp
void PLAYSTATS_DUPE_DETECTED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DUPE_DETECTED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BAN_ALERT

**Hash:** `0x516FC96EB88EEFE5` | **Build:** `1011`

**C++ 原型:**
```cpp
void PLAYSTATS_BAN_ALERT(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BAN_ALERT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_GUNRUNNING_MISSION_ENDED

**Hash:** `0x0EACDF8487D5155A` | **Build:** `1103`

**C++ 原型:**
```cpp
void PLAYSTATS_GUNRUNNING_MISSION_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_GUNRUNNING_MISSION_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_GUNRUNNING_RND

**Hash:** `0xDAF80797FC534BEC` | **Build:** `1103`

**C++ 原型:**
```cpp
void PLAYSTATS_GUNRUNNING_RND(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_GUNRUNNING_RND, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_BUSINESS_BATTLE_ENDED

**Hash:** `0x316DB59CD14C1774` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_BUSINESS_BATTLE_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_BUSINESS_BATTLE_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_WAREHOUSE_MISSION_ENDED

**Hash:** `0x2D7A9B577E72385E` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_WAREHOUSE_MISSION_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_WAREHOUSE_MISSION_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_NIGHTCLUB_MISSION_ENDED

**Hash:** `0x830C3A44EB3F2CF9` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_NIGHTCLUB_MISSION_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_NIGHTCLUB_MISSION_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DJ_USAGE

**Hash:** `0xB26F670685631727` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_DJ_USAGE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DJ_USAGE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MINIGAME_USAGE

**Hash:** `0xC14BD9F5337219B2` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_MINIGAME_USAGE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MINIGAME_USAGE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_STONE_HATCHET_ENDED

**Hash:** `0x35E39E5570358630` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_STONE_HATCHET_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_STONE_HATCHET_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SMUGGLER_MISSION_ENDED

**Hash:** `0x320C35147D5B5DDD` | **Build:** `1180`

**C++ 原型:**
```cpp
void PLAYSTATS_SMUGGLER_MISSION_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SMUGGLER_MISSION_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_HEIST_PREP_ENDED

**Hash:** `0xD8AFB345A9C5CCBB` | **Build:** `1290`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_HEIST_PREP_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_HEIST_PREP_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_INSTANCED_HEIST_ENDED

**Hash:** `0x1E1497D0D2108115` | **Build:** `1290`

**C++ 原型:**
```cpp
void PLAYSTATS_INSTANCED_HEIST_ENDED(Any* data, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_INSTANCED_HEIST_ENDED, data, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DAR_CHECKPOINT

**Hash:** `0x0BC254FF3A911501` | **Build:** `1290`

**C++ 原型:**
```cpp
void PLAYSTATS_DAR_CHECKPOINT(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DAR_CHECKPOINT, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ENTER_SESSION_PACK

**Hash:** `0x878FF156D36E9956` | **Build:** `1290`

**C++ 原型:**
```cpp
void PLAYSTATS_ENTER_SESSION_PACK(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ENTER_SESSION_PACK, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DRONE_USAGE

**Hash:** `0x66C7BB2416ED3FCE` | **Build:** `1493`

**C++ 原型:**
```cpp
void PLAYSTATS_DRONE_USAGE(int p0, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DRONE_USAGE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SPIN_WHEEL

**Hash:** `0x6731DE84A38BFAD0` | **Build:** `1604`

**C++ 原型:**
```cpp
void PLAYSTATS_SPIN_WHEEL(int p0, int p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SPIN_WHEEL, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ARENA_WARS_SPECTATOR

**Hash:** `0x6F4F599753F8200A` | **Build:** `1604`

**C++ 原型:**
```cpp
void PLAYSTATS_ARENA_WARS_SPECTATOR(int p0, int p1, int p2, int p3, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ARENA_WARS_SPECTATOR, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ARENA_WARS_ENDED

**Hash:** `0xB479D9F0D48A1BC5` | **Build:** `1604`

**C++ 原型:**
```cpp
void PLAYSTATS_ARENA_WARS_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ARENA_WARS_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SWITCH_PASSIVE_MODE

**Hash:** `0x35EEC6C2BC821A71` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_SWITCH_PASSIVE_MODE(BOOL p0, int p1, int p2, int p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SWITCH_PASSIVE_MODE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_COLLECTIBLE_PICKED_UP

**Hash:** `0xCD0A8A9338681CF2` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_COLLECTIBLE_PICKED_UP(int p0, Hash objectHash, Any p2, Any p3, int moneyAmount, int rpAmount, int chipsAmount, Any p7, int p8, Any p9, Any p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_COLLECTIBLE_PICKED_UP, p0, objectHash, p2, p3, moneyAmount, rpAmount, chipsAmount, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `objectHash` | `Hash` | `uint` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `moneyAmount` | `int` | `int` | |
| `rpAmount` | `int` | `int` | |
| `chipsAmount` | `int` | `int` | |
| `p7` | `Any` | `object` | |
| `p8` | `int` | `int` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_STORY_MISSION_ENDED

**Hash:** `0xFCCCAC2BD3C1F180` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_STORY_MISSION_ENDED(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_STORY_MISSION_ENDED, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_CHIP

**Hash:** `0x0999F3F090EC5012` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_CHIP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_CHIP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_ROULETTE

**Hash:** `0x95101C443A84E7F1` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_ROULETTE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_ROULETTE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_BLACKJACK

**Hash:** `0x3EAE97309727E7AD` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_BLACKJACK(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_BLACKJACK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_THREE_CARD_POKER

**Hash:** `0xF740FB339D471C35` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_THREE_CARD_POKER(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_THREE_CARD_POKER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_SLOT_MACHINE

**Hash:** `0xEF5EC67D392B830A` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_SLOT_MACHINE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_SLOT_MACHINE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_INSIDE_TRACK

**Hash:** `0x049F059625058A86` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_INSIDE_TRACK(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_INSIDE_TRACK, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_LUCKY_SEVEN

**Hash:** `0x0C432C1435F5E4FA` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_LUCKY_SEVEN(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_LUCKY_SEVEN, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_ROULETTE_LIGHT

**Hash:** `0x6572ABA3DE1197FC` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_ROULETTE_LIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_ROULETTE_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_BLACKJACK_LIGHT

**Hash:** `0xD5451C7BF151EB6F` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_BLACKJACK_LIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_BLACKJACK_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_THREE_CARD_POKER_LIGHT

**Hash:** `0xC9001364B4388F22` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_THREE_CARD_POKER_LIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_THREE_CARD_POKER_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_SLOT_MACHINE_LIGHT

**Hash:** `0xE60054A0FAE8227F` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_SLOT_MACHINE_LIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_SLOT_MACHINE_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CASINO_INSIDE_TRACK_LIGHT

**Hash:** `0x23A3CBCD50D54E47` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_CASINO_INSIDE_TRACK_LIGHT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CASINO_INSIDE_TRACK_LIGHT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ARCADE_GAME

**Hash:** `0x533A7D1EA58DF958` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_ARCADE_GAME(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ARCADE_GAME, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ARCADE_LOVE_MATCH

**Hash:** `0x4FCDBD3F0A813C25` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_ARCADE_LOVE_MATCH(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ARCADE_LOVE_MATCH, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FREEMODE_CASINO_MISSION_ENDED

**Hash:** `0x1A0D4A6C336B7BC5` | **Build:** `1734`

**C++ 原型:**
```cpp
void PLAYSTATS_FREEMODE_CASINO_MISSION_ENDED(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FREEMODE_CASINO_MISSION_ENDED, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST3_DRONE

**Hash:** `0xDFBD93BF2943E29B` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST3_DRONE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST3_DRONE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST3_HACK

**Hash:** `0x92FC0EEDFAC04A14` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST3_HACK(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST3_HACK, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_NPC_PHONE

**Hash:** `0x0077F15613D36993` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_NPC_PHONE(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_NPC_PHONE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ARCADE_CABINET

**Hash:** `0xF9096193DF1F99D4` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_ARCADE_CABINET(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ARCADE_CABINET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST3_FINALE

**Hash:** `0x2E0259BABC27A327` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST3_FINALE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST3_FINALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST3_PREP

**Hash:** `0x53C31853EC9531FF` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST3_PREP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST3_PREP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MASTER_CONTROL

**Hash:** `0x810B5FCC52EC7FF0` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_MASTER_CONTROL(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MASTER_CONTROL, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_QUIT_MODE

**Hash:** `0x5BF29846C6527C54` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_QUIT_MODE(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_QUIT_MODE, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_MISSION_VOTE

**Hash:** `0xC03FAB2C2F92289B` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_MISSION_VOTE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_MISSION_VOTE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_NJVS_VOTE

**Hash:** `0x5CDAED54B34B0ED0` | **Build:** `1868`

**C++ 原型:**
```cpp
void PLAYSTATS_NJVS_VOTE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_NJVS_VOTE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_DEATH_INFO

**Hash:** `0x01D8B04D02F1217F` | **Build:** `3570`

**C++ 原型:**
```cpp
void _PLAYSTATS_DEATH_INFO(Ped victimPed, Ped killerPed, int mentalState, BOOL revengeKill, int victimKvK, int killerKvK)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_DEATH_INFO, victimPed, killerPed, mentalState, revengeKill, victimKvK, killerKvK);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `victimPed` | `Ped` | `int` | |
| `killerPed` | `Ped` | `int` | |
| `mentalState` | `int` | `int` | |
| `revengeKill` | `BOOL` | `bool` | |
| `victimKvK` | `int` | `int` | |
| `killerKvK` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FM_MISSION_END

**Hash:** `0x46A70777BE6CEAB9` | **Build:** `2545`

**C++ 原型:**
```cpp
void PLAYSTATS_FM_MISSION_END(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FM_MISSION_END, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST4_PREP

**Hash:** `0xDFCDB14317A9B361` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST4_PREP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST4_PREP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST4_FINALE

**Hash:** `0xC1E963C58664B556` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST4_FINALE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST4_FINALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HEIST4_HACK

**Hash:** `0x2FA3173480008493` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_HEIST4_HACK(Any p0, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HEIST4_HACK, p0, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_SUB_WEAP

**Hash:** `0xD4367D310F079DB0` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_SUB_WEAP(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_SUB_WEAP, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_FAST_TRVL

**Hash:** `0x4DC416F246A41FC8` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_FAST_TRVL(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8, Any p9, Any p10)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_FAST_TRVL, p0, p1, p2, p3, p4, p5, p6, p7, p8, p9, p10);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |
| `p10` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HUB_ENTRY

**Hash:** `0x2818FF6638CB09DE` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_HUB_ENTRY(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HUB_ENTRY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_DJ_MISSION_ENDED

**Hash:** `0xD6CA58B3B53A0F22` | **Build:** `2189`

**C++ 原型:**
```cpp
void PLAYSTATS_DJ_MISSION_ENDED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_DJ_MISSION_ENDED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ROBBERY_PREP

**Hash:** `0x1A67DFBF1F5C3835` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_ROBBERY_PREP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ROBBERY_PREP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_ROBBERY_FINALE

**Hash:** `0xBBA55BE9AAAABF44` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_ROBBERY_FINALE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_ROBBERY_FINALE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_EXTRA_EVENT

**Hash:** `0xFA5B74BAB8A7EF99` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_EXTRA_EVENT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_EXTRA_EVENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CARCLUB_POINTS

**Hash:** `0xFF14D6FEEC507BBE` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_CARCLUB_POINTS(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CARCLUB_POINTS, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CARCLUB_CHALLENGE

**Hash:** `0x1187CB58D7F3BED7` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_CARCLUB_CHALLENGE(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CARCLUB_CHALLENGE, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_CARCLUB_PRIZE

**Hash:** `0x69C922B677621428` | **Build:** `2372`

**C++ 原型:**
```cpp
void PLAYSTATS_CARCLUB_PRIZE(int p0, Hash vehicleModel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_CARCLUB_PRIZE, p0, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_AWARD_NAV

**Hash:** `0x70F52471E758EBAE` | **Build:** `2545`

**C++ 原型:**
```cpp
void PLAYSTATS_AWARD_NAV(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_AWARD_NAV, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_INIT_MULTIPLAYER

**Hash:** `0x7E5EED10B11CEDBA` | **Build:** `3407`

**C++ 原型:**
```cpp
void _PLAYSTATS_INIT_MULTIPLAYER(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_INIT_MULTIPLAYER, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_INST_MISSION_END

**Hash:** `0xFEA3F7E83C0610FA` | **Build:** `2545`

**C++ 原型:**
```cpp
void PLAYSTATS_INST_MISSION_END(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_INST_MISSION_END, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_HUB_EXIT

**Hash:** `0x5A46ACE5C4661132` | **Build:** `2545`

**C++ 原型:**
```cpp
void PLAYSTATS_HUB_EXIT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_HUB_EXIT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_VEH_DEL

**Hash:** `0x10A691F5756416D0` | **Build:** `2944`

**C++ 原型:**
```cpp
void PLAYSTATS_VEH_DEL(int bossId1, int bossId2, int bossType, int vehicleID, int reason)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_VEH_DEL, bossId1, bossId2, bossType, vehicleID, reason);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `bossId1` | `int` | `int` | |
| `bossId2` | `int` | `int` | |
| `bossType` | `int` | `int` | |
| `vehicleID` | `int` | `int` | |
| `reason` | `int` | `int` | |

**返回值:** `void` → `void`

---

### PLAYSTATS_INVENTORY

**Hash:** `0x887DAD63CF5B7908` | **Build:** `2699`

**C++ 原型:**
```cpp
void PLAYSTATS_INVENTORY(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAYSTATS_INVENTORY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_ACID_MISSION_END

**Hash:** `0x8A23D1324F6B2BAC` | **Build:** `2802`

**C++ 原型:**
```cpp
void _PLAYSTATS_ACID_MISSION_END(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_ACID_MISSION_END, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_ACID_RND

**Hash:** `0xCEACCF0550FDC5BA` | **Build:** `2802`

**C++ 原型:**
```cpp
void _PLAYSTATS_ACID_RND(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_ACID_RND, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_IDLE

**Hash:** `0xEC9553A178E8F1D1` | **Build:** `2802`

**C++ 原型:**
```cpp
void _PLAYSTATS_IDLE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_IDLE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_PLAYER_STYLE

**Hash:** `0x48FAC5DC7AC6EA99` | **Build:** `2802`

**C++ 原型:**
```cpp
void _PLAYSTATS_PLAYER_STYLE(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_PLAYER_STYLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_RANDOM_EVENT

**Hash:** `0x7EA06F970F999394` | **Build:** `2944`

**C++ 原型:**
```cpp
void _PLAYSTATS_RANDOM_EVENT(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_RANDOM_EVENT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_ALERT

**Hash:** `0x5649CA22AF74E019` | **Build:** `3095`

**C++ 原型:**
```cpp
void _PLAYSTATS_ALERT(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_ALERT, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_ATTRITION_STAGE_END

**Hash:** `0xBD642335A732F1A8` | **Build:** `2944`

**C++ 原型:**
```cpp
void _PLAYSTATS_ATTRITION_STAGE_END(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_ATTRITION_STAGE_END, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_SHOWROOM_NAV

**Hash:** `0x961D4157B9B428DB` | **Build:** `2944`

**C++ 原型:**
```cpp
void _PLAYSTATS_SHOWROOM_NAV(Any p0, Any p1, Hash entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_SHOWROOM_NAV, p0, p1, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `entity` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_SHOWROOM_OVERVIEW

**Hash:** `0x151D6C04C9E2742F` | **Build:** `2944`

**C++ 原型:**
```cpp
void _PLAYSTATS_SHOWROOM_OVERVIEW(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_SHOWROOM_OVERVIEW, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

**说明:** Data struct contains various tunables related to test drives at Simeons Showroom or Luxury Showcase.

---

### _PLAYSTATS_PIMENU_NAV

**Hash:** `0xE6D323A5E9EFFB76` | **Build:** `3258`

**C++ 原型:**
```cpp
void _PLAYSTATS_PIMENU_NAV(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_PIMENU_NAV, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_RECOVER_VEHICLE

**Hash:** `0xBEB0D930B3CCE4D5` | **Build:** `3258`

**C++ 原型:**
```cpp
void _PLAYSTATS_RECOVER_VEHICLE(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_RECOVER_VEHICLE, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_SCRIPT_EVENT_FPOM

**Hash:** `0x574A7808450E141C` | **Build:** `3570`

**C++ 原型:**
```cpp
void _PLAYSTATS_SCRIPT_EVENT_FPOM(Any* data)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_SCRIPT_EVENT_FPOM, data);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `data` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_LOBBY_EXIT

**Hash:** `0x4649D80CE1A35BA1` | **Build:** `3717`

**C++ 原型:**
```cpp
void _PLAYSTATS_LOBBY_EXIT(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_LOBBY_EXIT, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_LOBBY_STARTED

**Hash:** `0xDC00BB9C27347A8F` | **Build:** `3717`

**C++ 原型:**
```cpp
void _PLAYSTATS_LOBBY_STARTED(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_LOBBY_STARTED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---

### _PLAYSTATS_CREATOR_END

**Hash:** `0x72289B8C78B02962` | **Build:** `3717`

**C++ 原型:**
```cpp
void _PLAYSTATS_CREATOR_END(Any* p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._PLAYSTATS_CREATOR_END, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any*` | `object*` | |

**返回值:** `void` → `void`

---


---

## STREAMING

**包含 123 个 Native 函数**

### LOAD_ALL_OBJECTS_NOW

**Hash:** `0xBD6E84632DD4CB3F` | **JHash:** `0xC9DBDA90` | **Build:** `323`

**C++ 原型:**
```cpp
void LOAD_ALL_OBJECTS_NOW(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOAD_ALL_OBJECTS_NOW);
```

**返回值:** `void` → `void`

---

### LOAD_SCENE

**Hash:** `0x4448EB75B4904BDB` | **JHash:** `0xB72403F5` | **Build:** `323`

**C++ 原型:**
```cpp
void LOAD_SCENE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOAD_SCENE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### NETWORK_UPDATE_LOAD_SCENE

**Hash:** `0xC4582015556D1C46` | **JHash:** `0xC76E023C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NETWORK_UPDATE_LOAD_SCENE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NETWORK_UPDATE_LOAD_SCENE);
```

**返回值:** `BOOL` → `bool`

---

### IS_NETWORK_LOADING_SCENE

**Hash:** `0x41CA5A33160EA4AB` | **JHash:** `0x6DCFC021` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NETWORK_LOADING_SCENE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NETWORK_LOADING_SCENE);
```

**返回值:** `BOOL` → `bool`

---

### SET_INTERIOR_ACTIVE

**Hash:** `0xE37B76C387BE28ED` | **JHash:** `0xE1013910` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_INTERIOR_ACTIVE(int interiorID, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INTERIOR_ACTIVE, interiorID, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interiorID` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_MODEL

**Hash:** `0x963D27A58DF860AC` | **JHash:** `0xFFF1B500` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_MODEL(Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_MODEL, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Request a model to be loaded into memory.


---

### REQUEST_MENU_PED_MODEL

**Hash:** `0xA0261AEF7ACFC51E` | **JHash:** `0x48CEB6B4` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_MENU_PED_MODEL(Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_MENU_PED_MODEL, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### HAS_MODEL_LOADED

**Hash:** `0x98A4EB5D89A0C952` | **JHash:** `0x62BFDB37` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_MODEL_LOADED(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_MODEL_LOADED, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the specified model has loaded into memory.

---

### REQUEST_MODELS_IN_ROOM

**Hash:** `0x8A7A40100EDFEC58` | **JHash:** `0x939243FB` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_MODELS_IN_ROOM(Interior interior, const char* roomName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_MODELS_IN_ROOM, interior, roomName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `interior` | `Interior` | `int` | |
| `roomName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** STREAMING::REQUEST_MODELS_IN_ROOM(l_13BC, "V_FIB01_cur_elev");
STREAMING::REQUEST_MODELS_IN_ROOM(l_13BC, "limbo");
STREAMING::REQUEST_MODELS_IN_ROOM(l_13BB, "V_Office_gnd_lifts");
STREAMING::REQUEST_MODELS_IN_ROOM(l_13BB, "limbo");
STREAMING::REQUEST_MODELS_IN_ROOM(l_13BC, "v_fib01_jan_elev");
STREAMING::REQUEST_MODELS_IN_ROOM(l_13BC, "limbo");

---

### SET_MODEL_AS_NO_LONGER_NEEDED

**Hash:** `0xE532F5D78798DAAB` | **JHash:** `0xAE0F069E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MODEL_AS_NO_LONGER_NEEDED(Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MODEL_AS_NO_LONGER_NEEDED, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Unloads model from memory

---

### IS_MODEL_IN_CDIMAGE

**Hash:** `0x35B9E0803292B641` | **JHash:** `0x1094782F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MODEL_IN_CDIMAGE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MODEL_IN_CDIMAGE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Check if model is in cdimage(rpf)

---

### IS_MODEL_VALID

**Hash:** `0xC0296A2EDF545E92` | **JHash:** `0xAF8F8E9D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MODEL_VALID(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MODEL_VALID, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified model exists in the game.

---

### IS_MODEL_A_PED

**Hash:** `0x75816577FEA6DAD5` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL IS_MODEL_A_PED(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MODEL_A_PED, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_MODEL_A_VEHICLE

**Hash:** `0x19AAC8F07BFEC53E` | **JHash:** `0xFFFC85D4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MODEL_A_VEHICLE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MODEL_A_VEHICLE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified model represents a vehicle.

---

### REQUEST_COLLISION_AT_COORD

**Hash:** `0x07503F7948F491A7` | **JHash:** `0xCD9805E7` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_COLLISION_AT_COORD(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_COLLISION_AT_COORD, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### REQUEST_COLLISION_FOR_MODEL

**Hash:** `0x923CB32A3B874FCB` | **JHash:** `0x3930C042` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_COLLISION_FOR_MODEL(Hash model)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_COLLISION_FOR_MODEL, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### HAS_COLLISION_FOR_MODEL_LOADED

**Hash:** `0x22CCA434E368F03A` | **JHash:** `0x41A094F8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_COLLISION_FOR_MODEL_LOADED(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_COLLISION_FOR_MODEL_LOADED, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_ADDITIONAL_COLLISION_AT_COORD

**Hash:** `0xC9156DC11411A9EA` | **JHash:** `0xC2CC1DF2` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ADDITIONAL_COLLISION_AT_COORD(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ADDITIONAL_COLLISION_AT_COORD, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Alias of REQUEST_COLLISION_AT_COORD.

---

### DOES_ANIM_DICT_EXIST

**Hash:** `0x2DA49C3B79856961` | **JHash:** `0xCD31C872` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_ANIM_DICT_EXIST(const char* animDict)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_ANIM_DICT_EXIST, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### REQUEST_ANIM_DICT

**Hash:** `0xD3BD40951412FEF6` | **JHash:** `0xDCA96950` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ANIM_DICT(const char* animDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ANIM_DICT, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### HAS_ANIM_DICT_LOADED

**Hash:** `0xD031A9162D01088C` | **JHash:** `0x05E6763C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ANIM_DICT_LOADED(const char* animDict)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ANIM_DICT_LOADED, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### REMOVE_ANIM_DICT

**Hash:** `0xF66A602F829E2A06` | **JHash:** `0x0AE050B5` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ANIM_DICT(const char* animDict)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ANIM_DICT, animDict);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animDict` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### REQUEST_ANIM_SET

**Hash:** `0x6EA47DAE7FAD0EED` | **JHash:** `0x2988B3FC` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_ANIM_SET(const char* animSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_ANIM_SET, animSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Starts loading the specified animation set. An animation set provides movement animations for a ped. See SET_PED_MOVEMENT_CLIPSET.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### HAS_ANIM_SET_LOADED

**Hash:** `0xC4EA073D86FB29B0` | **JHash:** `0x4FFF397D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ANIM_SET_LOADED(const char* animSet)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ANIM_SET_LOADED, animSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animSet` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets whether the specified animation set has finished loading. An animation set provides movement animations for a ped. See SET_PED_MOVEMENT_CLIPSET.

Animation set and clip set are synonymous.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### REMOVE_ANIM_SET

**Hash:** `0x16350528F93024B3` | **JHash:** `0xD04A817A` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ANIM_SET(const char* animSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ANIM_SET, animSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `animSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Unloads the specified animation set. An animation set provides movement animations for a ped. See SET_PED_MOVEMENT_CLIPSET.

Animation set and clip set are synonymous.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### REQUEST_CLIP_SET

**Hash:** `0xD2A71E1A77418A49` | **JHash:** `0x546C627A` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_CLIP_SET(const char* clipSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_CLIP_SET, clipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clipSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### HAS_CLIP_SET_LOADED

**Hash:** `0x318234F4F3738AF3` | **JHash:** `0x230D5455` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_CLIP_SET_LOADED(const char* clipSet)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_CLIP_SET_LOADED, clipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clipSet` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Alias for HAS_ANIM_SET_LOADED.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### REMOVE_CLIP_SET

**Hash:** `0x01F73A131C18CD94` | **JHash:** `0x1E21F7AA` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_CLIP_SET(const char* clipSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_CLIP_SET, clipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `clipSet` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Alias for REMOVE_ANIM_SET.

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

Full list of movement clipsets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/movementClipsetsCompact.json

---

### REQUEST_IPL

**Hash:** `0x41B4893843BBDB74` | **JHash:** `0x3B70D1DB` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_IPL(const char* iplName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_IPL, iplName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `iplName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Exemple: REQUEST_IPL("TrevorsTrailerTrash");

Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### REMOVE_IPL

**Hash:** `0xEE6C5AD3ECE0A82D` | **JHash:** `0xDF7CBD36` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_IPL(const char* iplName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_IPL, iplName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `iplName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Removes an IPL from the map.

Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

Example:
C#:
Function.Call(Hash.REMOVE_IPL, "trevorstrailertidy");

C++:
STREAMING::REMOVE_IPL("trevorstrailertidy");

iplName = Name of IPL you want to remove.

---

### IS_IPL_ACTIVE

**Hash:** `0x88A741E44A2B3495` | **JHash:** `0xB2C33714` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_IPL_ACTIVE(const char* iplName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_IPL_ACTIVE, iplName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `iplName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of IPLs and interior entity sets by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/ipls.json

---

### SET_STREAMING

**Hash:** `0x6E0C692677008888` | **JHash:** `0x27EF6CB2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_STREAMING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_STREAMING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOAD_GLOBAL_WATER_FILE

**Hash:** `0x7E3F55ED251B76D3` | **Build:** `2189`

**C++ 原型:**
```cpp
void LOAD_GLOBAL_WATER_FILE(int waterType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOAD_GLOBAL_WATER_FILE, waterType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `waterType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 0 - default
1 - HeistIsland

---

### GET_GLOBAL_WATER_FILE

**Hash:** `0xF741BD853611592D` | **Build:** `2189`

**C++ 原型:**
```cpp
int GET_GLOBAL_WATER_FILE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_GLOBAL_WATER_FILE);
```

**返回值:** `int` → `int`

---

### SET_GAME_PAUSES_FOR_STREAMING

**Hash:** `0x717CD6E6FAEBBEDC` | **JHash:** `0x9211A28A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GAME_PAUSES_FOR_STREAMING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GAME_PAUSES_FOR_STREAMING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_REDUCE_PED_MODEL_BUDGET

**Hash:** `0x77B5F9A36BF96710` | **JHash:** `0xAFCB2B86` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_REDUCE_PED_MODEL_BUDGET(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REDUCE_PED_MODEL_BUDGET, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_REDUCE_VEHICLE_MODEL_BUDGET

**Hash:** `0x80C527893080CCF3` | **JHash:** `0xCDB4FB7E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_REDUCE_VEHICLE_MODEL_BUDGET(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REDUCE_VEHICLE_MODEL_BUDGET, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DITCH_POLICE_MODELS

**Hash:** `0x42CBE54462D92634` | **JHash:** `0x3EA7FCE4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DITCH_POLICE_MODELS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DITCH_POLICE_MODELS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This is a NOP function. It does nothing at all.

---

### GET_NUMBER_OF_STREAMING_REQUESTS

**Hash:** `0x4060057271CEBC89` | **JHash:** `0xC2EE9A02` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_STREAMING_REQUESTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_STREAMING_REQUESTS);
```

**返回值:** `int` → `int`

---

### REQUEST_PTFX_ASSET

**Hash:** `0x944955FB2A3935C8` | **JHash:** `0x2C649263` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_PTFX_ASSET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_PTFX_ASSET);
```

**返回值:** `void` → `void`

**说明:** maps script name (thread + 0xD0) by lookup via scriptfx.dat - does nothing when script name is empty

---

### HAS_PTFX_ASSET_LOADED

**Hash:** `0xCA7D9B86ECA7481B` | **JHash:** `0x3EFF96BE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PTFX_ASSET_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PTFX_ASSET_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### REMOVE_PTFX_ASSET

**Hash:** `0x88C6814073DD4A73` | **JHash:** `0xC10F178C` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PTFX_ASSET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PTFX_ASSET);
```

**返回值:** `void` → `void`

---

### REQUEST_NAMED_PTFX_ASSET

**Hash:** `0xB80D8756B4668AB6` | **JHash:** `0xCFEA19A9` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_NAMED_PTFX_ASSET(const char* fxName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_NAMED_PTFX_ASSET, fxName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fxName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:**  From the b678d decompiled scripts:

 STREAMING::REQUEST_NAMED_PTFX_ASSET("core_snow");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("fm_mission_controler");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("proj_xmas_firework");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_apartment_mp");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_biolab_heist");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_indep_fireworks");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_indep_parachute");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_indep_wheelsmoke");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_mp_cig_plane");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_mp_creator");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_mp_tankbattle");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_ornate_heist");
 STREAMING::REQUEST_NAMED_PTFX_ASSET("scr_prison_break_heist_station");

---

### HAS_NAMED_PTFX_ASSET_LOADED

**Hash:** `0x8702416E512EC454` | **JHash:** `0x9ACC6446` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_NAMED_PTFX_ASSET_LOADED(const char* fxName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_NAMED_PTFX_ASSET_LOADED, fxName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fxName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_NAMED_PTFX_ASSET

**Hash:** `0x5F61EBBE1A00F96D` | **JHash:** `0xC44762A1` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_NAMED_PTFX_ASSET(const char* fxName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_NAMED_PTFX_ASSET, fxName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `fxName` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_POPULATION_BUDGET

**Hash:** `0xCB9E1EB3BE2AF4E9` | **JHash:** `0x1D56993C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_POPULATION_BUDGET(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_POPULATION_BUDGET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_POPULATION_BUDGET

**Hash:** `0x8C95333CFC3340F3` | **JHash:** `0xD2D026CD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_POPULATION_BUDGET(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_POPULATION_BUDGET, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Control how many new (ambient?) peds will spawn in the game world.
Range for p0 seems to be 0-3, where 0 is none and 3 is the normal level.

---

### CLEAR_FOCUS

**Hash:** `0x31B73D1EA9F01DA2` | **JHash:** `0x34D91E7A` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_FOCUS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_FOCUS);
```

**返回值:** `void` → `void`

---

### SET_FOCUS_POS_AND_VEL

**Hash:** `0xBB7454BAFF08FE25` | **JHash:** `0x14680A60` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOCUS_POS_AND_VEL(float x, float y, float z, float offsetX, float offsetY, float offsetZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOCUS_POS_AND_VEL, x, y, z, offsetX, offsetY, offsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Override the area where the camera will render the terrain.
p3, p4 and p5 are usually set to 0.0


---

### SET_FOCUS_ENTITY

**Hash:** `0x198F77705FA0931D` | **JHash:** `0x18DB04AC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FOCUS_ENTITY(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FOCUS_ENTITY, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** It seems to make the entity's coords mark the point from which LOD-distances are measured. In my testing, setting a vehicle as the focus entity and moving that vehicle more than 300 distance units away from the player will make the level of detail around the player go down drastically (shadows disappear, textures go extremely low res, etc). The player seems to be the default focus entity.

---

### IS_ENTITY_FOCUS

**Hash:** `0x2DDFF3FB9075D747` | **JHash:** `0xB456D707` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_FOCUS(Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_FOCUS, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_RESTORE_FOCUS_ENTITY

**Hash:** `0x0811381EF5062FEC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RESTORE_FOCUS_ENTITY(Entity p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RESTORE_FOCUS_ENTITY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_MAPDATACULLBOX_ENABLED

**Hash:** `0xAF12610C644A35C9` | **JHash:** `0x403CD434` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MAPDATACULLBOX_ENABLED(const char* name, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MAPDATACULLBOX_ENABLED, name, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Possible p0 values:

"prologue"
"Prologue_Main"

---

### SET_ALL_MAPDATA_CULLED

**Hash:** `0x4E52E752C76E7E7A` | **JHash:** `0xA07BAEB9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_MAPDATA_CULLED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_MAPDATA_CULLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

---

### STREAMVOL_CREATE_SPHERE

**Hash:** `0x219C7B8D53E429FD` | **JHash:** `0x10B6AB36` | **Build:** `323`

**C++ 原型:**
```cpp
int STREAMVOL_CREATE_SPHERE(float x, float y, float z, float rad, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STREAMVOL_CREATE_SPHERE, x, y, z, rad, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rad` | `float` | `float` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** Always returns zero.

---

### STREAMVOL_CREATE_FRUSTUM

**Hash:** `0x1F3F018BC3AFA77C` | **JHash:** `0x72344191` | **Build:** `323`

**C++ 原型:**
```cpp
int STREAMVOL_CREATE_FRUSTUM(float p0, float p1, float p2, float p3, float p4, float p5, float p6, Any p7, Any p8)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STREAMVOL_CREATE_FRUSTUM, p0, p1, p2, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** Always returns zero.

---

### STREAMVOL_CREATE_LINE

**Hash:** `0x0AD9710CEE2F590F` | **JHash:** `0xC0157255` | **Build:** `323`

**C++ 原型:**
```cpp
int STREAMVOL_CREATE_LINE(float p0, float p1, float p2, float p3, float p4, float p5, Any p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.STREAMVOL_CREATE_LINE, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `Any` | `object` | |

**返回值:** `int` → `int`

**说明:** Always returns zero.

---

### STREAMVOL_DELETE

**Hash:** `0x1EE7D8DF4425F053` | **JHash:** `0xE80F8ABE` | **Build:** `323`

**C++ 原型:**
```cpp
void STREAMVOL_DELETE(Any unused)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STREAMVOL_DELETE, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `unused` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### STREAMVOL_HAS_LOADED

**Hash:** `0x7D41E9D2D17C5B2D` | **JHash:** `0x1B3521F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STREAMVOL_HAS_LOADED(Any unused)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STREAMVOL_HAS_LOADED, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `unused` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### STREAMVOL_IS_VALID

**Hash:** `0x07C313F94746702C` | **JHash:** `0x42CFE9C0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL STREAMVOL_IS_VALID(Any unused)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.STREAMVOL_IS_VALID, unused);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `unused` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### IS_STREAMVOL_ACTIVE

**Hash:** `0xBC9823AB80A3DCAC` | **JHash:** `0x56253356` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_STREAMVOL_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_STREAMVOL_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### NEW_LOAD_SCENE_START

**Hash:** `0x212A8D0D2BABFAC2` | **JHash:** `0xDF9C38B6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NEW_LOAD_SCENE_START(float posX, float posY, float posZ, float offsetX, float offsetY, float offsetZ, float radius, int p7)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NEW_LOAD_SCENE_START, posX, posY, posZ, offsetX, offsetY, offsetZ, radius, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p7` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** `radius` value is usually between `3f` and `7000f` in original 1868 scripts.
`p7` is 0, 1, 2, 3 or 4 used in decompiled scripts, 0 is by far the most common.
Returns True if success, used only 7 times in decompiled scripts of 1868

---

### NEW_LOAD_SCENE_START_SPHERE

**Hash:** `0xACCFB4ACF53551B0` | **JHash:** `0xFA037FEB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL NEW_LOAD_SCENE_START_SPHERE(float x, float y, float z, float radius, Any p4)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.NEW_LOAD_SCENE_START_SPHERE, x, y, z, radius, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### NEW_LOAD_SCENE_STOP

**Hash:** `0xC197616D221FF4A4` | **JHash:** `0x7C05B1F6` | **Build:** `323`

**C++ 原型:**
```cpp
void NEW_LOAD_SCENE_STOP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NEW_LOAD_SCENE_STOP);
```

**返回值:** `void` → `void`

---

### IS_NEW_LOAD_SCENE_ACTIVE

**Hash:** `0xA41A05B6CB741B85` | **JHash:** `0xAD234B7F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NEW_LOAD_SCENE_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NEW_LOAD_SCENE_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### IS_NEW_LOAD_SCENE_LOADED

**Hash:** `0x01B8247A7A8B9AD1` | **JHash:** `0x3ECD839F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_NEW_LOAD_SCENE_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NEW_LOAD_SCENE_LOADED);
```

**返回值:** `BOOL` → `bool`

---

### IS_SAFE_TO_START_PLAYER_SWITCH

**Hash:** `0x71E7B2E657449AAD` | **JHash:** `0xEAA51103` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SAFE_TO_START_PLAYER_SWITCH(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SAFE_TO_START_PLAYER_SWITCH);
```

**返回值:** `BOOL` → `bool`

---

### START_PLAYER_SWITCH

**Hash:** `0xFAA23F2CBA159D67` | **JHash:** `0x0829E975` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PLAYER_SWITCH(Ped from, Ped to, int flags, int switchType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PLAYER_SWITCH, from, to, flags, switchType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `from` | `Ped` | `int` | |
| `to` | `Ped` | `int` | |
| `flags` | `int` | `int` | |
| `switchType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** // this enum comes directly from R* so don't edit this
enum ePlayerSwitchTypes
{
  SWITCH_TYPE_AUTO,
 SWITCH_TYPE_LONG,
 SWITCH_TYPE_MEDIUM,
   SWITCH_TYPE_SHORT
};

Use GET_IDEAL_PLAYER_SWITCH_TYPE for the best switch type.

----------------------------------------------------

Examples from the decompiled scripts:

STREAMING::START_PLAYER_SWITCH(l_832._f3, PLAYER::PLAYER_PED_ID(), 0, 3);
STREAMING::START_PLAYER_SWITCH(l_832._f3, PLAYER::PLAYER_PED_ID(), 2050, 3);
STREAMING::START_PLAYER_SWITCH(PLAYER::PLAYER_PED_ID(), l_832._f3, 1024, 3);
STREAMING::START_PLAYER_SWITCH(g_141F27, PLAYER::PLAYER_PED_ID(), 513, v_14);

Note: DO NOT, use SWITCH_TYPE_LONG with flag 513. It leaves you stuck in the clouds. You'll have to call STOP_PLAYER_SWITCH() to return to your ped.

Flag 8 w/ SWITCH_TYPE_LONG will zoom out 3 steps, then zoom in 2/3 steps and stop on the 3rd and just hang there.
Flag 8 w/ SWITCH_TYPE_MEDIUM will zoom out 1 step, and just hang there.

---

### STOP_PLAYER_SWITCH

**Hash:** `0x95C0A5BBDC189AA1` | **JHash:** `0x2832C010` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PLAYER_SWITCH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PLAYER_SWITCH);
```

**返回值:** `void` → `void`

---

### IS_PLAYER_SWITCH_IN_PROGRESS

**Hash:** `0xD9D2CFFF49FAB35F` | **JHash:** `0x56135ACC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYER_SWITCH_IN_PROGRESS(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYER_SWITCH_IN_PROGRESS);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the player is currently switching, false otherwise.
(When the camera is in the sky moving from Trevor to Franklin for example)

---

### GET_PLAYER_SWITCH_TYPE

**Hash:** `0xB3C94A90D9FC9E62` | **JHash:** `0x280DC015` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SWITCH_TYPE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SWITCH_TYPE);
```

**返回值:** `int` → `int`

---

### GET_IDEAL_PLAYER_SWITCH_TYPE

**Hash:** `0xB5D7B26B45720E05` | **JHash:** `0xD5A450F1` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_IDEAL_PLAYER_SWITCH_TYPE(float x1, float y1, float z1, float x2, float y2, float z2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_IDEAL_PLAYER_SWITCH_TYPE, x1, y1, z1, x2, y2, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `int` → `int`

**说明:** x1, y1, z1 -- Coords of your ped model
x2, y2, z2 -- Coords of the ped you want to switch to

---

### GET_PLAYER_SWITCH_STATE

**Hash:** `0x470555300D10B2A5` | **JHash:** `0x39A0E1F2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SWITCH_STATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SWITCH_STATE);
```

**返回值:** `int` → `int`

---

### GET_PLAYER_SHORT_SWITCH_STATE

**Hash:** `0x20F898A5D9782800` | **JHash:** `0x9B7BA38F` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SHORT_SWITCH_STATE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SHORT_SWITCH_STATE);
```

**返回值:** `int` → `int`

---

### SET_PLAYER_SHORT_SWITCH_STYLE

**Hash:** `0x5F2013F8BC24EE69` | **JHash:** `0xF0BD420D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SHORT_SWITCH_STYLE(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SHORT_SWITCH_STYLE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_PLAYER_SWITCH_JUMP_CUT_INDEX

**Hash:** `0x78C0D93253149435` | **JHash:** `0x02BA7AC2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SWITCH_JUMP_CUT_INDEX(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SWITCH_JUMP_CUT_INDEX);
```

**返回值:** `int` → `int`

---

### SET_PLAYER_SWITCH_OUTRO

**Hash:** `0xC208B673CE446B61` | **JHash:** `0x47352E14` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SWITCH_OUTRO(float cameraCoordX, float cameraCoordY, float cameraCoordZ, float camRotationX, float camRotationY, float camRotationZ, float camFov, float camFarClip, int rotationOrder)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SWITCH_OUTRO, cameraCoordX, cameraCoordY, cameraCoordZ, camRotationX, camRotationY, camRotationZ, camFov, camFarClip, rotationOrder);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cameraCoordX` | `float` | `float` | |
| `cameraCoordY` | `float` | `float` | |
| `cameraCoordZ` | `float` | `float` | |
| `camRotationX` | `float` | `float` | |
| `camRotationY` | `float` | `float` | |
| `camRotationZ` | `float` | `float` | |
| `camFov` | `float` | `float` | |
| `camFarClip` | `float` | `float` | |
| `rotationOrder` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYER_SWITCH_ESTABLISHING_SHOT

**Hash:** `0x0FDE9DBFC0A6BC65` | **JHash:** `0x279077B0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYER_SWITCH_ESTABLISHING_SHOT(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_SWITCH_ESTABLISHING_SHOT, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** All names can be found in playerswitchestablishingshots.meta

---

### ALLOW_PLAYER_SWITCH_PAN

**Hash:** `0x43D1680C6D19A8E9` | **JHash:** `0x55CB21F9` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_PLAYER_SWITCH_PAN(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PLAYER_SWITCH_PAN);
```

**返回值:** `void` → `void`

---

### ALLOW_PLAYER_SWITCH_OUTRO

**Hash:** `0x74DE2E8739086740` | **JHash:** `0x1084F2F4` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_PLAYER_SWITCH_OUTRO(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PLAYER_SWITCH_OUTRO);
```

**返回值:** `void` → `void`

---

### ALLOW_PLAYER_SWITCH_ASCENT

**Hash:** `0x8E2A065ABDAE6994` | **JHash:** `0x5B1E995D` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_PLAYER_SWITCH_ASCENT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PLAYER_SWITCH_ASCENT);
```

**返回值:** `void` → `void`

---

### ALLOW_PLAYER_SWITCH_DESCENT

**Hash:** `0xAD5FDF34B81BFE79` | **JHash:** `0x4B4B9A13` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_PLAYER_SWITCH_DESCENT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_PLAYER_SWITCH_DESCENT);
```

**返回值:** `void` → `void`

---

### IS_SWITCH_READY_FOR_DESCENT

**Hash:** `0xDFA80CB25D0A19B3` | **JHash:** `0x408F7148` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SWITCH_READY_FOR_DESCENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SWITCH_READY_FOR_DESCENT);
```

**返回值:** `BOOL` → `bool`

---

### ENABLE_SWITCH_PAUSE_BEFORE_DESCENT

**Hash:** `0xD4793DFF3AF2ABCD` | **JHash:** `0x4062FF73` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_SWITCH_PAUSE_BEFORE_DESCENT(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_SWITCH_PAUSE_BEFORE_DESCENT);
```

**返回值:** `void` → `void`

---

### DISABLE_SWITCH_OUTRO_FX

**Hash:** `0xBD605B8E0E18B3BB` | **JHash:** `0x59CC312D` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_SWITCH_OUTRO_FX(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_SWITCH_OUTRO_FX);
```

**返回值:** `void` → `void`

---

### SWITCH_TO_MULTI_FIRSTPART

**Hash:** `0xAAB3200ED59016BC` | **JHash:** `0xFB4D062D` | **Build:** `323`

**C++ 原型:**
```cpp
void SWITCH_TO_MULTI_FIRSTPART(Ped ped, int flags, int switchType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SWITCH_TO_MULTI_FIRSTPART, ped, flags, switchType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flags` | `int` | `int` | |
| `switchType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** doesn't act normally when used on mount chilliad
Flags is a bitflag:
2^n - Enabled Functionality:
0 - Skip camera rotate up
3 - Wait for SET_PLAYER_SWITCH_ESTABLISHING_SHOT / hang at last step. You will still need to run 0x74DE2E8739086740 to exit "properly" and then STOP_PLAYER_SWITCH
6 - Invert Switch Direction (false = out, true = in)
8 - Hang above ped

switchType: 0 - 3
0: 1 step towards ped
1: 3 steps out from ped
2: 1 step out from ped
3: 1 step towards ped

---

### SWITCH_TO_MULTI_SECONDPART

**Hash:** `0xD8295AF639FD9CB8` | **JHash:** `0x2349373B` | **Build:** `323`

**C++ 原型:**
```cpp
void SWITCH_TO_MULTI_SECONDPART(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SWITCH_TO_MULTI_SECONDPART, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_SWITCH_TO_MULTI_FIRSTPART_FINISHED

**Hash:** `0x933BBEEB8C61B5F4` | **JHash:** `0x74C16879` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SWITCH_TO_MULTI_FIRSTPART_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SWITCH_TO_MULTI_FIRSTPART_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### GET_PLAYER_SWITCH_INTERP_OUT_DURATION

**Hash:** `0x08C2D6C52A3104BB` | **JHash:** `0x569847E3` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SWITCH_INTERP_OUT_DURATION(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SWITCH_INTERP_OUT_DURATION);
```

**返回值:** `int` → `int`

---

### GET_PLAYER_SWITCH_INTERP_OUT_CURRENT_TIME

**Hash:** `0x5B48A06DD0E792A5` | **JHash:** `0xC7A3D279` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PLAYER_SWITCH_INTERP_OUT_CURRENT_TIME(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PLAYER_SWITCH_INTERP_OUT_CURRENT_TIME);
```

**返回值:** `int` → `int`

---

### IS_SWITCH_SKIPPING_DESCENT

**Hash:** `0x5B74EA8CFD5E3E7E` | **JHash:** `0x90F64284` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SWITCH_SKIPPING_DESCENT(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SWITCH_SKIPPING_DESCENT);
```

**返回值:** `BOOL` → `bool`

---

### SET_SCENE_STREAMING_TRACKS_CAM_POS_THIS_FRAME

**Hash:** `0x1E9057A74FD73E23` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENE_STREAMING_TRACKS_CAM_POS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENE_STREAMING_TRACKS_CAM_POS_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### GET_LODSCALE

**Hash:** `0x0C15B0E443B2349D` | **JHash:** `0x7154B6FD` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_LODSCALE(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_LODSCALE);
```

**返回值:** `float` → `float`

---

### OVERRIDE_LODSCALE_THIS_FRAME

**Hash:** `0xA76359FC80B2438E` | **JHash:** `0xE5612C1A` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_LODSCALE_THIS_FRAME(float scaling)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_LODSCALE_THIS_FRAME, scaling);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scaling` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This allows you to override "extended distance scaling" setting. Needs to be called each frame.
Max scaling seems to be 200.0, normal is 1.0
See https://gfycat.com/DetailedHauntingIncatern

---

### REMAP_LODSCALE_RANGE_THIS_FRAME

**Hash:** `0xBED8CA5FF5E04113` | **JHash:** `0x9CD6A451` | **Build:** `323`

**C++ 原型:**
```cpp
void REMAP_LODSCALE_RANGE_THIS_FRAME(float p0, float p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMAP_LODSCALE_RANGE_THIS_FRAME, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SUPPRESS_HD_MAP_STREAMING_THIS_FRAME

**Hash:** `0x472397322E92A856` | **JHash:** `0x4267DA87` | **Build:** `323`

**C++ 原型:**
```cpp
void SUPPRESS_HD_MAP_STREAMING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_HD_MAP_STREAMING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### SET_RENDER_HD_ONLY

**Hash:** `0x40AEFD1A244741F2` | **JHash:** `0x9FA4AF99` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RENDER_HD_ONLY(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RENDER_HD_ONLY, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_ALLOW_TIME_BASED_FADING_THIS_FRAME

**Hash:** `0x03F1A106BDA7DD3E` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_ALLOW_TIME_BASED_FADING_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_ALLOW_TIME_BASED_FADING_THIS_FRAME);
```

**返回值:** `void` → `void`

---

### IPL_GROUP_SWAP_START

**Hash:** `0x95A7DABDDBB78AE7` | **JHash:** `0x9EF0A9CF` | **Build:** `323`

**C++ 原型:**
```cpp
void IPL_GROUP_SWAP_START(const char* iplName1, const char* iplName2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.IPL_GROUP_SWAP_START, iplName1, iplName2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `iplName1` | `const char*` | `string` | |
| `iplName2` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### IPL_GROUP_SWAP_CANCEL

**Hash:** `0x63EB2B972A218CAC` | **JHash:** `0xF2CDD6A8` | **Build:** `323`

**C++ 原型:**
```cpp
void IPL_GROUP_SWAP_CANCEL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.IPL_GROUP_SWAP_CANCEL);
```

**返回值:** `void` → `void`

---

### IPL_GROUP_SWAP_IS_READY

**Hash:** `0xFB199266061F820A` | **JHash:** `0x17B0A1CD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IPL_GROUP_SWAP_IS_READY(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IPL_GROUP_SWAP_IS_READY);
```

**返回值:** `BOOL` → `bool`

---

### IPL_GROUP_SWAP_FINISH

**Hash:** `0xF4A0DADB70F57FA6` | **JHash:** `0x3DA7AA5D` | **Build:** `323`

**C++ 原型:**
```cpp
void IPL_GROUP_SWAP_FINISH(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.IPL_GROUP_SWAP_FINISH);
```

**返回值:** `void` → `void`

---

### IPL_GROUP_SWAP_IS_ACTIVE

**Hash:** `0x5068F488DDB54DD8` | **JHash:** `0xDAB4BAC0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IPL_GROUP_SWAP_IS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IPL_GROUP_SWAP_IS_ACTIVE);
```

**返回值:** `BOOL` → `bool`

---

### PREFETCH_SRL

**Hash:** `0x3D245789CE12982C` | **JHash:** `0x37BE2FBB` | **Build:** `323`

**C++ 原型:**
```cpp
void PREFETCH_SRL(const char* srl)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PREFETCH_SRL, srl);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `srl` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** This native is used to attribute the SRL that BEGIN_SRL is going to load. This is usually used for 'in-game' cinematics (not cutscenes but camera stuff) instead of SET_FOCUS_POS_AND_VEL because it loads a specific area of the map which is pretty useful when the camera moves from distant areas.
For instance, GTA:O opening cutscene.
https://pastebin.com/2EeKVeLA : a list of SRL found in srllist.meta
https://pastebin.com/zd9XYUWY here is the content of a SRL file opened with codewalker.

---

### IS_SRL_LOADED

**Hash:** `0xD0263801A4C5B0BB` | **JHash:** `0x670FA2A6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SRL_LOADED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SRL_LOADED);
```

**返回值:** `BOOL` → `bool`

**说明:** Returns true when the srl from BEGIN_SRL is loaded.

---

### BEGIN_SRL

**Hash:** `0x9BADDC94EF83B823` | **JHash:** `0x24F49427` | **Build:** `323`

**C++ 原型:**
```cpp
void BEGIN_SRL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BEGIN_SRL);
```

**返回值:** `void` → `void`

---

### END_SRL

**Hash:** `0x0A41540E63C9EE17` | **JHash:** `0x1977C56A` | **Build:** `323`

**C++ 原型:**
```cpp
void END_SRL(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.END_SRL);
```

**返回值:** `void` → `void`

**说明:** Clear the current srl and stop rendering the area selected by PREFETCH_SRL and started with BEGIN_SRL.

---

### SET_SRL_TIME

**Hash:** `0xA74A541C6884E7B8` | **JHash:** `0x30F8A487` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SRL_TIME(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SRL_TIME, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_SRL_POST_CUTSCENE_CAMERA

**Hash:** `0xEF39EE20C537E98C` | **JHash:** `0x814D0752` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SRL_POST_CUTSCENE_CAMERA(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SRL_POST_CUTSCENE_CAMERA, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_SRL_READAHEAD_TIMES

**Hash:** `0xBEB2D9A1D9A8F55A` | **JHash:** `0x62F02485` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SRL_READAHEAD_TIMES(Any p0, Any p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SRL_READAHEAD_TIMES, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_SRL_LONG_JUMP_MODE

**Hash:** `0x20C6C7E4EB082A7F` | **JHash:** `0xA6459CAA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SRL_LONG_JUMP_MODE(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SRL_LONG_JUMP_MODE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SRL_FORCE_PRESTREAM

**Hash:** `0xF8155A7F03DDFC8E` | **JHash:** `0xF8F515E4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SRL_FORCE_PRESTREAM(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SRL_FORCE_PRESTREAM, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_HD_AREA

**Hash:** `0xB85F26619073E775` | **JHash:** `0x80BAA035` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HD_AREA(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HD_AREA, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_HD_AREA

**Hash:** `0xCE58B1CFB9290813` | **JHash:** `0x7CAC6FA0` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_HD_AREA(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_HD_AREA);
```

**返回值:** `void` → `void`

---

### INIT_CREATOR_BUDGET

**Hash:** `0xB5A4DB34FE89B88A` | **JHash:** `0xE243B2AF` | **Build:** `323`

**C++ 原型:**
```cpp
void INIT_CREATOR_BUDGET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INIT_CREATOR_BUDGET);
```

**返回值:** `void` → `void`

---

### SHUTDOWN_CREATOR_BUDGET

**Hash:** `0xCCE26000E9A6FAD7` | **JHash:** `0x897A510F` | **Build:** `323`

**C++ 原型:**
```cpp
void SHUTDOWN_CREATOR_BUDGET(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SHUTDOWN_CREATOR_BUDGET);
```

**返回值:** `void` → `void`

---

### ADD_MODEL_TO_CREATOR_BUDGET

**Hash:** `0x0BC3144DEB678666` | **JHash:** `0xC0E83320` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ADD_MODEL_TO_CREATOR_BUDGET(Hash modelHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ADD_MODEL_TO_CREATOR_BUDGET, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_MODEL_FROM_CREATOR_BUDGET

**Hash:** `0xF086AD9354FAC3A3` | **JHash:** `0x1C576388` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_MODEL_FROM_CREATOR_BUDGET(Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_MODEL_FROM_CREATOR_BUDGET, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### GET_USED_CREATOR_BUDGET

**Hash:** `0x3D3D8B3BE5A83D35` | **JHash:** `0x3E9C4CBE` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_USED_CREATOR_BUDGET(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_USED_CREATOR_BUDGET);
```

**返回值:** `float` → `float`

**说明:** 0.0 = no memory used
1.0 = all memory used

Maximum model memory (as defined in common\data\missioncreatordata.meta) is 100 MiB

---

### _GET_MODEL_ADDITIONAL_COST

**Hash:** `0x0F7082C60FB479EF` | **Build:** `3717`

**C++ 原型:**
```cpp
float _GET_MODEL_ADDITIONAL_COST(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_MODEL_ADDITIONAL_COST, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

---

### _GET_TOTAL_MODEL_COST

**Hash:** `0x4A91423C04BAADA1` | **Build:** `3717`

**C++ 原型:**
```cpp
float _GET_TOTAL_MODEL_COST(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_TOTAL_MODEL_COST, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

---

### SET_ISLAND_ENABLED

**Hash:** `0x9A9D1BA639675CF1` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_ISLAND_ENABLED(const char* name, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ISLAND_ENABLED, name, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables the specified island. For more information, see islandhopper.meta

---

### _SET_SPHERICAL_STREAM_DISTANT_HILODS_THIS_FRAME

**Hash:** `0x68F1C25420D5F6AA` | **Build:** `3717`

**C++ 原型:**
```cpp
void _SET_SPHERICAL_STREAM_DISTANT_HILODS_THIS_FRAME(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_SPHERICAL_STREAM_DISTANT_HILODS_THIS_FRAME);
```

**返回值:** `void` → `void`

---


---

## TASK

**包含 313 个 Native 函数**

### TASK_PAUSE

**Hash:** `0xE73A266DB0CA9042` | **JHash:** `0x17A64668` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PAUSE(Ped ped, int ms)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PAUSE, ped, ms);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ms` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Stand still (?)

---

### TASK_STAND_STILL

**Hash:** `0x919BE13EED931959` | **JHash:** `0x6F80965D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_STAND_STILL(Ped ped, int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_STAND_STILL, ped, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the specified ped stand still for (time) milliseconds.

---

### TASK_JUMP

**Hash:** `0x0AE4086104E067B1` | **JHash:** `0x0356E3CE` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_JUMP(Ped ped, BOOL usePlayerLaunchForce, BOOL doSuperJump, BOOL useFullSuperJumpForce)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_JUMP, ped, usePlayerLaunchForce, doSuperJump, useFullSuperJumpForce);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `usePlayerLaunchForce` | `BOOL` | `bool` | |
| `doSuperJump` | `BOOL` | `bool` | |
| `useFullSuperJumpForce` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Definition is wrong. This has 4 parameters (Not sure when they were added. v350 has 2, v678 has 4).

v350: Ped ped, bool unused
v678: Ped ped, bool unused, bool flag1, bool flag2

flag1 = super jump, flag2 = do nothing if flag1 is false and doubles super jump height if flag1 is true.

---

### TASK_COWER

**Hash:** `0x3EB1FE9E8E908E15` | **JHash:** `0x9CF1C19B` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COWER(Ped ped, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COWER, ped, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_HANDS_UP

**Hash:** `0xF2EAB31979A7F910` | **JHash:** `0x8DCC19C5` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_HANDS_UP(Ped ped, int duration, Ped facingPed, int timeToFacePed, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_HANDS_UP, ped, duration, facingPed, timeToFacePed, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `duration` | `int` | `int` | |
| `facingPed` | `Ped` | `int` | |
| `timeToFacePed` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** In the scripts, p3 was always -1.

p3 seems to be duration or timeout of turn animation.
Also facingPed can be 0 or -1 so ped will just raise hands up.

---

### UPDATE_TASK_HANDS_UP_DURATION

**Hash:** `0xA98FCAFD7893C834` | **JHash:** `0x3AA39BE9` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_TASK_HANDS_UP_DURATION(Ped ped, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_TASK_HANDS_UP_DURATION, ped, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_OPEN_VEHICLE_DOOR

**Hash:** `0x965791A9A488A062` | **JHash:** `0x8EE06BF4` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_OPEN_VEHICLE_DOOR(Ped ped, Vehicle vehicle, int timeOut, int seat, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_OPEN_VEHICLE_DOOR, ped, vehicle, timeOut, seat, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `timeOut` | `int` | `int` | |
| `seat` | `int` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The given ped will try to open the nearest door to 'seat'.
Example: telling the ped to open the door for the driver seat does not necessarily mean it will open the driver door, it may choose to open the passenger door instead if that one is closer.

---

### TASK_ENTER_VEHICLE

**Hash:** `0xC20E50AA46D09CA8` | **JHash:** `0xB8689B4E` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_ENTER_VEHICLE(Ped ped, Vehicle vehicle, int timeout, int seat, float speed, int flag, const char* overrideEntryClipsetName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_ENTER_VEHICLE, ped, vehicle, timeout, seat, speed, flag, overrideEntryClipsetName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `timeout` | `int` | `int` | |
| `seat` | `int` | `int` | |
| `speed` | `float` | `float` | |
| `flag` | `int` | `int` | |
| `overrideEntryClipsetName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** speed 1.0 = walk, 2.0 = run
p5 1 = normal, 3 = teleport to vehicle, 16 = teleport directly into vehicle
p6 is always 0

Usage of seat 
-1 = driver
0 = passenger
1 = left back seat
2 = right back seat
3 = outside left
4 = outside right

---

### TASK_LEAVE_VEHICLE

**Hash:** `0xD3DBCE61A490BE02` | **JHash:** `0x7B1141C6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_LEAVE_VEHICLE(Ped ped, Vehicle vehicle, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_LEAVE_VEHICLE, ped, vehicle, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Flags from decompiled scripts:
0 = normal exit and closes door.
1 = normal exit and closes door.
16 = teleports outside, door kept closed.
64 = normal exit and closes door, maybe a bit slower animation than 0.
256 = normal exit but does not close the door.
4160 = ped is throwing himself out, even when the vehicle is still.
262144 = ped moves to passenger seat first, then exits normally

Others to be tried out: 320, 512, 131072.

---

### TASK_GET_OFF_BOAT

**Hash:** `0x9C00E77AF14B2DFF` | **JHash:** `0x4293601F` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GET_OFF_BOAT(Ped ped, Vehicle boat)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GET_OFF_BOAT, ped, boat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `boat` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### TASK_SKY_DIVE

**Hash:** `0x601736CFE536B0A0` | **JHash:** `0xD3874AFA` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SKY_DIVE(Ped ped, BOOL instant)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SKY_DIVE, ped, instant);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `instant` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_PARACHUTE

**Hash:** `0xD2F1C53C97EE81AB` | **JHash:** `0xEC3060A2` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PARACHUTE(Ped ped, BOOL giveParachuteItem, BOOL instant)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PARACHUTE, ped, giveParachuteItem, instant);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `giveParachuteItem` | `BOOL` | `bool` | |
| `instant` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Second parameter is unused.

second parameter was for jetpack in the early stages of gta and the hard coded code is now removed

---

### TASK_PARACHUTE_TO_TARGET

**Hash:** `0xB33E291AFA6BD03A` | **JHash:** `0xE0104D6C` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PARACHUTE_TO_TARGET(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PARACHUTE_TO_TARGET, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** makes ped parachute to coords x y z. Works well with PATHFIND::GET_SAFE_COORD_FOR_PED

---

### SET_PARACHUTE_TASK_TARGET

**Hash:** `0xC313379AF0FCEDA7` | **JHash:** `0x6ED3AD81` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARACHUTE_TASK_TARGET(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARACHUTE_TASK_TARGET, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARACHUTE_TASK_THRUST

**Hash:** `0x0729BAC1B8C64317` | **JHash:** `0xD07C8AAA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARACHUTE_TASK_THRUST(Ped ped, float thrust)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARACHUTE_TASK_THRUST, ped, thrust);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `thrust` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_RAPPEL_FROM_HELI

**Hash:** `0x09693B0312F91649` | **JHash:** `0x2C7ADB93` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_RAPPEL_FROM_HELI(Ped ped, float minHeightAboveGround)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_RAPPEL_FROM_HELI, ped, minHeightAboveGround);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `minHeightAboveGround` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** minHeightAboveGround: the minimum height above ground the heli must be at before the ped can start rappelling

Only appears twice in the scripts.

TASK::TASK_RAPPEL_FROM_HELI(PLAYER::PLAYER_PED_ID(), 10.0f);
TASK::TASK_RAPPEL_FROM_HELI(a_0, 10.0f);

---

### TASK_VEHICLE_DRIVE_TO_COORD

**Hash:** `0xE2A2AA2F659D77A7` | **JHash:** `0xE4AC0387` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_DRIVE_TO_COORD(Ped ped, Vehicle vehicle, float x, float y, float z, float speed, Any p6, Hash vehicleModel, int drivingMode, float stopRange, float straightLineDistance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_DRIVE_TO_COORD, ped, vehicle, x, y, z, speed, p6, vehicleModel, drivingMode, stopRange, straightLineDistance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `p6` | `Any` | `object` | |
| `vehicleModel` | `Hash` | `uint` | |
| `drivingMode` | `int` | `int` | |
| `stopRange` | `float` | `float` | |
| `straightLineDistance` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** info about driving modes: https://gtaforums.com/topic/822314-guide-driving-styles/

---

### TASK_VEHICLE_DRIVE_TO_COORD_LONGRANGE

**Hash:** `0x158BB33F920D360C` | **JHash:** `0x1490182A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_DRIVE_TO_COORD_LONGRANGE(Ped ped, Vehicle vehicle, float x, float y, float z, float speed, int driveMode, float stopRange)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_DRIVE_TO_COORD_LONGRANGE, ped, vehicle, x, y, z, speed, driveMode, stopRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `driveMode` | `int` | `int` | |
| `stopRange` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_DRIVE_WANDER

**Hash:** `0x480142959D337D00` | **JHash:** `0x36EC0EB0` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_DRIVE_WANDER(Ped ped, Vehicle vehicle, float speed, int drivingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_DRIVE_WANDER, ped, vehicle, speed, drivingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_FOLLOW_TO_OFFSET_OF_ENTITY

**Hash:** `0x304AE42E357B8C7E` | **JHash:** `0x2DF5A6AC` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FOLLOW_TO_OFFSET_OF_ENTITY(Ped ped, Entity entity, float offsetX, float offsetY, float offsetZ, float movementSpeed, int timeout, float stoppingRange, BOOL persistFollowing)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FOLLOW_TO_OFFSET_OF_ENTITY, ped, entity, offsetX, offsetY, offsetZ, movementSpeed, timeout, stoppingRange, persistFollowing);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `movementSpeed` | `float` | `float` | |
| `timeout` | `int` | `int` | |
| `stoppingRange` | `float` | `float` | |
| `persistFollowing` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p6 always -1
p7 always 10.0
p8 always 1

---

### TASK_GO_STRAIGHT_TO_COORD

**Hash:** `0xD76B57B44F1E6F8B` | **JHash:** `0x80A9E7A7` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_STRAIGHT_TO_COORD(Ped ped, float x, float y, float z, float speed, int timeout, float targetHeading, float distanceToSlide)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_STRAIGHT_TO_COORD, ped, x, y, z, speed, timeout, targetHeading, distanceToSlide);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `timeout` | `int` | `int` | |
| `targetHeading` | `float` | `float` | |
| `distanceToSlide` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_GO_STRAIGHT_TO_COORD_RELATIVE_TO_ENTITY

**Hash:** `0x61E360B7E040D12E` | **JHash:** `0xD26CAC68` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_STRAIGHT_TO_COORD_RELATIVE_TO_ENTITY(Ped ped, Entity entity, float x, float y, float z, float moveBlendRatio, int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_STRAIGHT_TO_COORD_RELATIVE_TO_ENTITY, ped, entity, x, y, z, moveBlendRatio, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_ACHIEVE_HEADING

**Hash:** `0x93B93A37987F1F3D` | **JHash:** `0x0A0E9B42` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_ACHIEVE_HEADING(Ped ped, float heading, int timeout)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_ACHIEVE_HEADING, ped, heading, timeout);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `heading` | `float` | `float` | |
| `timeout` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the specified ped achieve the specified heading.

pedHandle: The handle of the ped to assign the task to.
heading: The desired heading.
timeout: The time, in milliseconds, to allow the task to complete. If the task times out, it is cancelled, and the ped will stay at the heading it managed to reach in the time.

---

### TASK_FLUSH_ROUTE

**Hash:** `0x841142A1376E9006` | **JHash:** `0x34219154` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FLUSH_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FLUSH_ROUTE);
```

**返回值:** `void` → `void`

**说明:** MulleKD19: Clears the current point route. Call this before TASK_EXTEND_ROUTE and TASK_FOLLOW_POINT_ROUTE.

---

### TASK_EXTEND_ROUTE

**Hash:** `0x1E7889778264843A` | **JHash:** `0x43271F69` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_EXTEND_ROUTE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_EXTEND_ROUTE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** MulleKD19: Adds a new point to the current point route. Call TASK_FLUSH_ROUTE before the first call to this. Call TASK_FOLLOW_POINT_ROUTE to make the Ped go the route.

A maximum of 8 points can be added.

---

### TASK_FOLLOW_POINT_ROUTE

**Hash:** `0x595583281858626E` | **JHash:** `0xB837C816` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FOLLOW_POINT_ROUTE(Ped ped, float speed, int mode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FOLLOW_POINT_ROUTE, ped, speed, mode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speed` | `float` | `float` | |
| `mode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** MulleKD19: Makes the ped go on the created point route.

ped: The ped to give the task to.
speed: The speed to move at in m/s.
int: Unknown. Can be 0, 1, 2 or 3.

Example:
TASK_FLUSH_ROUTE();
TASK_EXTEND_ROUTE(0f, 0f, 70f);
TASK_EXTEND_ROUTE(10f, 0f, 70f);
TASK_EXTEND_ROUTE(10f, 10f, 70f);
TASK_FOLLOW_POINT_ROUTE(GET_PLAYER_PED(), 1f, 0);

---

### TASK_GO_TO_ENTITY

**Hash:** `0x6A071245EB0D1882` | **JHash:** `0x374827C2` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_ENTITY(Entity entity, Entity target, int duration, float distance, float moveBlendRatio, float slowDownDistance, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_ENTITY, entity, target, duration, distance, moveBlendRatio, slowDownDistance, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `target` | `Entity` | `int` | |
| `duration` | `int` | `int` | |
| `distance` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `slowDownDistance` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** The entity will move towards the target until time is over (duration) or get in target's range (distance). p5 and p6 are unknown, but you could leave p5 = 1073741824 or 100 or even 0 (didn't see any difference but on the decompiled scripts, they use 1073741824 mostly) and p6 = 0

Note: I've only tested it on entity -> ped and target -> vehicle. It could work differently on other entities, didn't try it yet.

Example: TASK::TASK_GO_TO_ENTITY(pedHandle, vehicleHandle, 5000, 4.0, 100, 1073741824, 0)

Ped will run towards the vehicle for 5 seconds and stop when time is over or when he gets 4 meters(?) around the vehicle (with duration = -1, the task duration will be ignored).

enum EGOTO_ENTITY_SCRIPT_FLAGS
{
	EGOTO_ENTITY_NEVER_SLOW_FOR_PATH_LENGTH = 0x01,
};

---

### TASK_SMART_FLEE_COORD

**Hash:** `0x94587F17E9C365D5` | **JHash:** `0xB2E686FC` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SMART_FLEE_COORD(Ped ped, float x, float y, float z, float distance, int time, BOOL preferPavements, BOOL quitIfOutOfRange)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SMART_FLEE_COORD, ped, x, y, z, distance, time, preferPavements, quitIfOutOfRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `distance` | `float` | `float` | |
| `time` | `int` | `int` | |
| `preferPavements` | `BOOL` | `bool` | |
| `quitIfOutOfRange` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the specified ped flee the specified distance from the specified position.

---

### TASK_SMART_FLEE_PED

**Hash:** `0x22B0D0E37CCB840D` | **JHash:** `0xE52EB560` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SMART_FLEE_PED(Ped ped, Ped fleeTarget, float safeDistance, int fleeTime, BOOL preferPavements, BOOL updateToNearestHatedPed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SMART_FLEE_PED, ped, fleeTarget, safeDistance, fleeTime, preferPavements, updateToNearestHatedPed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `fleeTarget` | `Ped` | `int` | |
| `safeDistance` | `float` | `float` | |
| `fleeTime` | `int` | `int` | |
| `preferPavements` | `BOOL` | `bool` | |
| `updateToNearestHatedPed` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes a ped run away from another ped (fleeTarget).

distance = ped will flee this distance.
fleeTime = ped will flee for this amount of time, set to "-1" to flee forever

---

### TASK_REACT_AND_FLEE_PED

**Hash:** `0x72C896464915D1B1` | **JHash:** `0x8A632BD8` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_REACT_AND_FLEE_PED(Ped ped, Ped fleeTarget)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_REACT_AND_FLEE_PED, ped, fleeTarget);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `fleeTarget` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_SHOCKING_EVENT_REACT

**Hash:** `0x452419CBD838065B` | **JHash:** `0x9BD00ACF` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SHOCKING_EVENT_REACT(Ped ped, int eventHandle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SHOCKING_EVENT_REACT, ped, eventHandle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `eventHandle` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_WANDER_IN_AREA

**Hash:** `0xE054346CA3A0F315` | **JHash:** `0xC6981FB9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_WANDER_IN_AREA(Ped ped, float x, float y, float z, float radius, float minimalLength, float timeBetweenWalks)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WANDER_IN_AREA, ped, x, y, z, radius, minimalLength, timeBetweenWalks);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `minimalLength` | `float` | `float` | |
| `timeBetweenWalks` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_WANDER_STANDARD

**Hash:** `0xBB9CE077274F6A1B` | **JHash:** `0xAF59151A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_WANDER_STANDARD(Ped ped, float heading, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WANDER_STANDARD, ped, heading, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `heading` | `float` | `float` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes ped walk around the area.

set p1 to 10.0f and p2 to 10 if you want the ped to walk anywhere without a duration.

---

### TASK_WANDER_SPECIFIC

**Hash:** `0x6919A2F136426098` | **Build:** `1868`

**C++ 原型:**
```cpp
void TASK_WANDER_SPECIFIC(Ped ped, const char* conditionalAnimGroupStr, const char* conditionalAnimStr, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WANDER_SPECIFIC, ped, conditionalAnimGroupStr, conditionalAnimStr, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `conditionalAnimGroupStr` | `const char*` | `string` | |
| `conditionalAnimStr` | `const char*` | `string` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_PARK

**Hash:** `0x0F3E34E968EA374E` | **JHash:** `0x5C85FF90` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_PARK(Ped ped, Vehicle vehicle, float x, float y, float z, float heading, int mode, float radius, BOOL keepEngineOn)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_PARK, ped, vehicle, x, y, z, heading, mode, radius, keepEngineOn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `mode` | `int` | `int` | |
| `radius` | `float` | `float` | |
| `keepEngineOn` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Modes:
0 - ignore heading
1 - park forward
2 - park backwards

Depending on the angle of approach, the vehicle can park at the specified heading or at its exact opposite (-180) angle.

Radius seems to define how close the vehicle has to be -after parking- to the position for this task considered completed. If the value is too small, the vehicle will try to park again until it's exactly where it should be. 20.0 Works well but lower values don't, like the radius is measured in centimeters or something.

---

### TASK_STEALTH_KILL

**Hash:** `0xAA5DC05579D60BD9` | **JHash:** `0x0D64C2FA` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_STEALTH_KILL(Ped killer, Ped target, Hash stealthKillActionResultHash, float desiredMoveBlendRatio, int stealthFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_STEALTH_KILL, killer, target, stealthKillActionResultHash, desiredMoveBlendRatio, stealthFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `killer` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `stealthKillActionResultHash` | `Hash` | `uint` | |
| `desiredMoveBlendRatio` | `float` | `float` | |
| `stealthFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** known "killTypes" are: "AR_stealth_kill_knife" and "AR_stealth_kill_a".

---

### TASK_PLANT_BOMB

**Hash:** `0x965FEC691D55E9BF` | **JHash:** `0x33457535` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLANT_BOMB(Ped ped, float x, float y, float z, float heading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANT_BOMB, ped, x, y, z, heading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_SHARK_CIRCLE_COORD

**Hash:** `0x60A19CF85FF4CEFA` | **Build:** `3407`

**C++ 原型:**
```cpp
void TASK_SHARK_CIRCLE_COORD(Ped ped, float x, float y, float z, float moveBlendRatio, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SHARK_CIRCLE_COORD, ped, x, y, z, moveBlendRatio, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_FOLLOW_NAV_MESH_TO_COORD

**Hash:** `0x15D3A79D4E44B913` | **JHash:** `0xFE4A10D9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FOLLOW_NAV_MESH_TO_COORD(Ped ped, float x, float y, float z, float moveBlendRatio, int time, float targetRadius, int flags, float targetHeading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FOLLOW_NAV_MESH_TO_COORD, ped, x, y, z, moveBlendRatio, time, targetRadius, flags, targetHeading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `time` | `int` | `int` | |
| `targetRadius` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `targetHeading` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** If no timeout, set timeout to -1.

---

### TASK_FOLLOW_NAV_MESH_TO_COORD_ADVANCED

**Hash:** `0x17F58B88D085DBAC` | **JHash:** `0x6BF6E296` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FOLLOW_NAV_MESH_TO_COORD_ADVANCED(Ped ped, float x, float y, float z, float moveBlendRatio, int time, float targetRadius, int flags, float slideToCoordHeading, float maxSlopeNavigable, float clampMaxSearchDistance, float targetHeading)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FOLLOW_NAV_MESH_TO_COORD_ADVANCED, ped, x, y, z, moveBlendRatio, time, targetRadius, flags, slideToCoordHeading, maxSlopeNavigable, clampMaxSearchDistance, targetHeading);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `time` | `int` | `int` | |
| `targetRadius` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `slideToCoordHeading` | `float` | `float` | |
| `maxSlopeNavigable` | `float` | `float` | |
| `clampMaxSearchDistance` | `float` | `float` | |
| `targetHeading` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_CAN_USE_CLIMBOVERS

**Hash:** `0x8E06A6FE76C9EFF4` | **JHash:** `0xB7B7D442` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_CAN_USE_CLIMBOVERS(Ped ped, BOOL Toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_CAN_USE_CLIMBOVERS, ped, Toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `Toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_CAN_USE_LADDERS

**Hash:** `0x77A5B103C87F476E` | **JHash:** `0x53A879EE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_CAN_USE_LADDERS(Ped ped, BOOL Toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_CAN_USE_LADDERS, ped, Toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `Toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_CAN_DROP_FROM_HEIGHT

**Hash:** `0xE361C5C71C431A4F` | **JHash:** `0x394B7AC9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_CAN_DROP_FROM_HEIGHT(Ped ped, BOOL Toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_CAN_DROP_FROM_HEIGHT, ped, Toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `Toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_CLIMB_COST_MODIFIER

**Hash:** `0x88E32DB8C1A4AA4B` | **JHash:** `0x55E06443` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_CLIMB_COST_MODIFIER(Ped ped, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_CLIMB_COST_MODIFIER, ped, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Default modifier is 1.0, minimum is 0.0 and maximum is 10.0.

---

### SET_PED_PATH_MAY_ENTER_WATER

**Hash:** `0xF35425A4204367EC` | **JHash:** `0x9C606EE3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_MAY_ENTER_WATER(Ped ped, BOOL mayEnterWater)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_MAY_ENTER_WATER, ped, mayEnterWater);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `mayEnterWater` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_PREFER_TO_AVOID_WATER

**Hash:** `0x38FE1EC73743793C` | **JHash:** `0x0EA39A29` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_PREFER_TO_AVOID_WATER(Ped ped, BOOL avoidWater)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_PREFER_TO_AVOID_WATER, ped, avoidWater);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `avoidWater` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_PATH_AVOID_FIRE

**Hash:** `0x4455517B28441E60` | **JHash:** `0xDCC5B934` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_PATH_AVOID_FIRE(Ped ped, BOOL avoidFire)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_PATH_AVOID_FIRE, ped, avoidFire);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `avoidFire` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_GLOBAL_MIN_BIRD_FLIGHT_HEIGHT

**Hash:** `0x6C6B148586F934F7` | **JHash:** `0x2AFB14B8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GLOBAL_MIN_BIRD_FLIGHT_HEIGHT(float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GLOBAL_MIN_BIRD_FLIGHT_HEIGHT, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Needs to be looped! And yes, it does work and is not a hash collision.
Birds will try to reach the given height.

---

### GET_NAVMESH_ROUTE_DISTANCE_REMAINING

**Hash:** `0xC6F5C0BCDC74D62D` | **JHash:** `0xD9281778` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NAVMESH_ROUTE_DISTANCE_REMAINING(Ped ped, float* distanceRemaining, BOOL* isPathReady)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NAVMESH_ROUTE_DISTANCE_REMAINING, ped, distanceRemaining, isPathReady);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `distanceRemaining` | `float*` | `float*` | |
| `isPathReady` | `BOOL*` | `bool*` | |

**返回值:** `int` → `int`

**说明:** Looks like the last parameter returns true if the path has been calculated, while the first returns the remaining distance to the end of the path.
Return value of native is the same as GET_NAVMESH_ROUTE_RESULT
Looks like the native returns an int for the path's state:
1 - ???
2 - ???
3 - Finished Generating 

---

### GET_NAVMESH_ROUTE_RESULT

**Hash:** `0x632E831F382A0FA8` | **JHash:** `0x96491602` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NAVMESH_ROUTE_RESULT(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NAVMESH_ROUTE_RESULT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** See GET_NAVMESH_ROUTE_DISTANCE_REMAINING for more details.

---

### IS_CONTROLLED_VEHICLE_UNABLE_TO_GET_TO_ROAD

**Hash:** `0x3E38E28A1D80DDF6` | **JHash:** `0x54856309` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_CONTROLLED_VEHICLE_UNABLE_TO_GET_TO_ROAD(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_CONTROLLED_VEHICLE_UNABLE_TO_GET_TO_ROAD, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### TASK_GO_TO_COORD_ANY_MEANS

**Hash:** `0x5BC448CB78FA3E88` | **JHash:** `0xF91DF93B` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_ANY_MEANS(Ped ped, float x, float y, float z, float moveBlendRatio, Vehicle vehicle, BOOL useLongRangeVehiclePathing, int drivingFlags, float maxRangeToShootTargets)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_ANY_MEANS, ped, x, y, z, moveBlendRatio, vehicle, useLongRangeVehiclePathing, drivingFlags, maxRangeToShootTargets);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `vehicle` | `Vehicle` | `int` | |
| `useLongRangeVehiclePathing` | `BOOL` | `bool` | |
| `drivingFlags` | `int` | `int` | |
| `maxRangeToShootTargets` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** example from fm_mission_controller

TASK::TASK_GO_TO_COORD_ANY_MEANS(l_649, sub_f7e86(-1, 0), 1.0, 0, 0, 786603, 0xbf800000);
 

---

### TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS

**Hash:** `0x1DD45F9ECFDB1BC9` | **JHash:** `0x094B75EF` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS(Ped ped, float x, float y, float z, float moveBlendRatio, Vehicle vehicle, BOOL useLongRangeVehiclePathing, int drivingFlags, float maxRangeToShootTargets, float extraVehToTargetDistToPreferVehicle, float driveStraightLineDistance, int extraFlags, float warpTimerMS)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS, ped, x, y, z, moveBlendRatio, vehicle, useLongRangeVehiclePathing, drivingFlags, maxRangeToShootTargets, extraVehToTargetDistToPreferVehicle, driveStraightLineDistance, extraFlags, warpTimerMS);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `vehicle` | `Vehicle` | `int` | |
| `useLongRangeVehiclePathing` | `BOOL` | `bool` | |
| `drivingFlags` | `int` | `int` | |
| `maxRangeToShootTargets` | `float` | `float` | |
| `extraVehToTargetDistToPreferVehicle` | `float` | `float` | |
| `driveStraightLineDistance` | `float` | `float` | |
| `extraFlags` | `int` | `int` | |
| `warpTimerMS` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS_WITH_CRUISE_SPEED

**Hash:** `0xB8ECD61F531A7B02` | **JHash:** `0x86DC03F9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS_WITH_CRUISE_SPEED(Ped ped, float x, float y, float z, float moveBlendRatio, Vehicle vehicle, BOOL useLongRangeVehiclePathing, int drivingFlags, float maxRangeToShootTargets, float extraVehToTargetDistToPreferVehicle, float driveStraightLineDistance, int extraFlags, float cruiseSpeed, float targetArriveDist)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_ANY_MEANS_EXTRA_PARAMS_WITH_CRUISE_SPEED, ped, x, y, z, moveBlendRatio, vehicle, useLongRangeVehiclePathing, drivingFlags, maxRangeToShootTargets, extraVehToTargetDistToPreferVehicle, driveStraightLineDistance, extraFlags, cruiseSpeed, targetArriveDist);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `vehicle` | `Vehicle` | `int` | |
| `useLongRangeVehiclePathing` | `BOOL` | `bool` | |
| `drivingFlags` | `int` | `int` | |
| `maxRangeToShootTargets` | `float` | `float` | |
| `extraVehToTargetDistToPreferVehicle` | `float` | `float` | |
| `driveStraightLineDistance` | `float` | `float` | |
| `extraFlags` | `int` | `int` | |
| `cruiseSpeed` | `float` | `float` | |
| `targetArriveDist` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_PLAY_ANIM

**Hash:** `0xEA47FE3719165B94` | **JHash:** `0x5AB552C6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLAY_ANIM(Ped ped, const char* animDictionary, const char* animationName, float blendInSpeed, float blendOutSpeed, int duration, int flag, float playbackRate, BOOL lockX, BOOL lockY, BOOL lockZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLAY_ANIM, ped, animDictionary, animationName, blendInSpeed, blendOutSpeed, duration, flag, playbackRate, lockX, lockY, lockZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animationName` | `const char*` | `string` | |
| `blendInSpeed` | `float` | `float` | |
| `blendOutSpeed` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `flag` | `int` | `int` | |
| `playbackRate` | `float` | `float` | |
| `lockX` | `BOOL` | `bool` | |
| `lockY` | `BOOL` | `bool` | |
| `lockZ` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

float speed > normal speed is 8.0f
----------------------

float speedMultiplier > multiply the playback speed
----------------------

int duration: time in millisecond
----------------------
-1 _ _ _ _ _ _ _> Default (see flag)
0 _ _ _ _ _ _ _ > Not play at all
Small value _ _ > Slow down animation speed
Other _ _ _ _ _ > freeze player control until specific time (ms) has 
_ _ _ _ _ _ _ _ _ passed. (No effect if flag is set to be 
_ _ _ _ _ _ _ _ _ controllable.)

int flag:
----------------------
enum eAnimationFlags
{
 ANIM_FLAG_NORMAL = 0,
   ANIM_FLAG_REPEAT = 1,
   ANIM_FLAG_STOP_LAST_FRAME = 2,
   ANIM_FLAG_UPPERBODY = 16,
   ANIM_FLAG_ENABLE_PLAYER_CONTROL = 32,
   ANIM_FLAG_CANCELABLE = 120,
};
Odd number : loop infinitely
Even number : Freeze at last frame
Multiple of 4: Freeze at last frame but controllable

01 to 15 > Full body
10 to 31 > Upper body
32 to 47 > Full body > Controllable
48 to 63 > Upper body > Controllable
...
001 to 255 > Normal
256 to 511 > Garbled
...

playbackRate:

values are between 0.0 and 1.0


lockX:  

0 in most cases 1 for rcmepsilonism8 and rcmpaparazzo_3
> 1 for mini@sprunk
 

lockY:

0 in most cases 
1 for missfam5_yoga, missfra1mcs_2_crew_react


lockZ: 

    0 for single player 
    Can be 1 but only for MP 

---

### TASK_PLAY_ANIM_ADVANCED

**Hash:** `0x83CDB10EA29B370B` | **JHash:** `0x3DDEB0E6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLAY_ANIM_ADVANCED(Ped ped, const char* animDict, const char* animName, float posX, float posY, float posZ, float rotX, float rotY, float rotZ, float animEnterSpeed, float animExitSpeed, int duration, Any flag, float animTime, int rotOrder, int ikFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLAY_ANIM_ADVANCED, ped, animDict, animName, posX, posY, posZ, rotX, rotY, rotZ, animEnterSpeed, animExitSpeed, duration, flag, animTime, rotOrder, ikFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `animEnterSpeed` | `float` | `float` | |
| `animExitSpeed` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `flag` | `Any` | `object` | |
| `animTime` | `float` | `float` | |
| `rotOrder` | `int` | `int` | |
| `ikFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** It's similar to TASK_PLAY_ANIM, except the first 6 floats let you specify the initial position and rotation of the task. (Ped gets teleported to the position).

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### STOP_ANIM_TASK

**Hash:** `0x97FF36A1D40EA00A` | **JHash:** `0x2B520A57` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ANIM_TASK(Entity entity, const char* animDictionary, const char* animationName, float blendDelta)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ANIM_TASK, entity, animDictionary, animationName, blendDelta);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animationName` | `const char*` | `string` | |
| `blendDelta` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### TASK_SCRIPTED_ANIMATION

**Hash:** `0x126EF75F1E17ABE5` | **JHash:** `0xFC2DCF47` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SCRIPTED_ANIMATION(Ped ped, int* priorityLowData, int* priorityMidData, int* priorityHighData, float blendInDelta, float blendOutDelta)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SCRIPTED_ANIMATION, ped, priorityLowData, priorityMidData, priorityHighData, blendInDelta, blendOutDelta);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `priorityLowData` | `int*` | `int*` | |
| `priorityMidData` | `int*` | `int*` | |
| `priorityHighData` | `int*` | `int*` | |
| `blendInDelta` | `float` | `float` | |
| `blendOutDelta` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** From fm_mission_controller.c:
reserve_network_mission_objects(get_num_reserved_mission_objects(0) + 1);
           vVar28 = {0.094f, 0.02f, -0.005f};
            vVar29 = {-92.24f, 63.64f, 150.24f};
          func_253(&uVar30, joaat("prop_ld_case_01"), Global_1592429.imm_34757[iParam1 <268>], 1, 1, 0, 1);
         set_entity_lod_dist(net_to_ent(uVar30), 500);
         attach_entity_to_entity(net_to_ent(uVar30), iParam0, get_ped_bone_index(iParam0, 28422), vVar28, vVar29, 1, 0, 0, 0, 2, 1);
           Var31.imm_4 = 1065353216;
         Var31.imm_5 = 1065353216;
         Var31.imm_9 = 1065353216;
         Var31.imm_10 = 1065353216;
            Var31.imm_14 = 1065353216;
            Var31.imm_15 = 1065353216;
            Var31.imm_17 = 1040187392;
            Var31.imm_18 = 1040187392;
            Var31.imm_19 = -1;
            Var32.imm_4 = 1065353216;
         Var32.imm_5 = 1065353216;
         Var32.imm_9 = 1065353216;
         Var32.imm_10 = 1065353216;
            Var32.imm_14 = 1065353216;
            Var32.imm_15 = 1065353216;
            Var32.imm_17 = 1040187392;
            Var32.imm_18 = 1040187392;
            Var32.imm_19 = -1;
            Var31 = 1;
            Var31.imm_1 = "weapons@misc@jerrycan@mp_male";
          Var31.imm_2 = "idle";
           Var31.imm_20 = 1048633;
           Var31.imm_4 = 0.5f;
           Var31.imm_16 = get_hash_key("BONEMASK_ARMONLY_R");
          task_scripted_animation(iParam0, &Var31, &Var32, &Var32, 0f, 0.25f);
          set_model_as_no_longer_needed(joaat("prop_ld_case_01"));
            remove_anim_dict("anim@heists@biolab@");

---

### PLAY_ENTITY_SCRIPTED_ANIM

**Hash:** `0x77A1EEC547E7FCF1` | **JHash:** `0x02F72AE5` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_ENTITY_SCRIPTED_ANIM(Entity entity, int* priorityLowData, int* priorityMidData, int* priorityHighData, float blendInDelta, float blendOutDelta)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_ENTITY_SCRIPTED_ANIM, entity, priorityLowData, priorityMidData, priorityHighData, blendInDelta, blendOutDelta);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `priorityLowData` | `int*` | `int*` | |
| `priorityMidData` | `int*` | `int*` | |
| `priorityHighData` | `int*` | `int*` | |
| `blendInDelta` | `float` | `float` | |
| `blendOutDelta` | `float` | `float` | |

**返回值:** `void` → `void`

---

### STOP_ANIM_PLAYBACK

**Hash:** `0xEE08C992D238C5D1` | **JHash:** `0xE5F16398` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ANIM_PLAYBACK(Entity entity, int priority, BOOL secondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ANIM_PLAYBACK, entity, priority, secondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `priority` | `int` | `int` | |
| `secondary` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Looks like p1 may be a flag, still need to do some research, though.

---

### SET_ANIM_WEIGHT

**Hash:** `0x207F1A47C0342F48` | **JHash:** `0x17229D98` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ANIM_WEIGHT(Entity entity, float weight, int priority, int index, BOOL secondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ANIM_WEIGHT, entity, weight, priority, index, secondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `weight` | `float` | `float` | |
| `priority` | `int` | `int` | |
| `index` | `int` | `int` | |
| `secondary` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ANIM_PHASE

**Hash:** `0xDDF3CB5A0A4C0B49` | **JHash:** `0xB621E7E4` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_ANIM_PHASE(Entity entity, float phase, int priority, BOOL secondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ANIM_PHASE, entity, phase, priority, secondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `phase` | `float` | `float` | |
| `priority` | `int` | `int` | |
| `secondary` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ANIM_RATE

**Hash:** `0x032D49C5E359C847` | **JHash:** `0x6DB46584` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ANIM_RATE(Entity entity, float rate, int priority, BOOL secondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ANIM_RATE, entity, rate, priority, secondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `rate` | `float` | `float` | |
| `priority` | `int` | `int` | |
| `secondary` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ANIM_LOOPED

**Hash:** `0x70033C3CC29A1FF4` | **JHash:** `0x095D61A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ANIM_LOOPED(Entity entity, BOOL looped, int priority, BOOL secondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ANIM_LOOPED, entity, looped, priority, secondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `looped` | `BOOL` | `bool` | |
| `priority` | `int` | `int` | |
| `secondary` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_PLAY_PHONE_GESTURE_ANIMATION

**Hash:** `0x8FBB6758B3B3E9EC` | **JHash:** `0x1582162C` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLAY_PHONE_GESTURE_ANIMATION(Ped ped, const char* animDict, const char* animation, const char* boneMaskType, float blendInDuration, float blendOutDuration, BOOL isLooping, BOOL holdLastFrame)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLAY_PHONE_GESTURE_ANIMATION, ped, animDict, animation, boneMaskType, blendInDuration, blendOutDuration, isLooping, holdLastFrame);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animation` | `const char*` | `string` | |
| `boneMaskType` | `const char*` | `string` | |
| `blendInDuration` | `float` | `float` | |
| `blendOutDuration` | `float` | `float` | |
| `isLooping` | `BOOL` | `bool` | |
| `holdLastFrame` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Example from the scripts:
TASK::TASK_PLAY_PHONE_GESTURE_ANIMATION(PLAYER::PLAYER_PED_ID(), v_3, v_2, v_4, 0.25, 0.25, 0, 0);

=========================================================
^^ No offense, but Idk how that would really help anyone.

As for the animDict & animation, they're both store in a global in all 5 scripts. So if anyone would be so kind as to read that global and comment what strings they use. Thanks.

Known boneMaskTypes'
"BONEMASK_HEADONLY"
"BONEMASK_HEAD_NECK_AND_ARMS"
"BONEMASK_HEAD_NECK_AND_L_ARM"
"BONEMASK_HEAD_NECK_AND_R_ARM"

p4 known args - 0.0f, 0.5f, 0.25f
p5 known args - 0.0f, 0.25f
p6 known args - 1 if a global if check is passed.
p7 known args - 1 if a global if check is passed.

The values found above, I found within the 5 scripts this is ever called in. (fmmc_launcher, fm_deathmatch_controller, fm_impromptu_dm_controller, fm_mission_controller, and freemode).
=========================================================

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### TASK_STOP_PHONE_GESTURE_ANIMATION

**Hash:** `0x3FA00D4F4641BFAE` | **JHash:** `0x5A32D4B4` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_STOP_PHONE_GESTURE_ANIMATION(Ped ped, float blendOutOverride)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_STOP_PHONE_GESTURE_ANIMATION, ped, blendOutOverride);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `blendOutOverride` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_PLAYING_PHONE_GESTURE_ANIM

**Hash:** `0xB8EBB1E9D3588C10` | **JHash:** `0x500B6805` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYING_PHONE_GESTURE_ANIM(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYING_PHONE_GESTURE_ANIM, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PHONE_GESTURE_ANIM_CURRENT_TIME

**Hash:** `0x47619ABE8B268C60` | **JHash:** `0x7B72AFD1` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PHONE_GESTURE_ANIM_CURRENT_TIME(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PHONE_GESTURE_ANIM_CURRENT_TIME, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### GET_PHONE_GESTURE_ANIM_TOTAL_TIME

**Hash:** `0x1EE0F68A7C25DEC6` | **JHash:** `0xEF8C3959` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PHONE_GESTURE_ANIM_TOTAL_TIME(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PHONE_GESTURE_ANIM_TOTAL_TIME, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### TASK_VEHICLE_PLAY_ANIM

**Hash:** `0x69F5C3BD0F3EBD89` | **JHash:** `0x2B28F598` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_PLAY_ANIM(Vehicle vehicle, const char* animationSet, const char* animationName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_PLAY_ANIM, vehicle, animationSet, animationName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `animationSet` | `const char*` | `string` | |
| `animationName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Most probably plays a specific animation on vehicle. For example getting chop out of van etc...

Here's how its used - 

TASK::TASK_VEHICLE_PLAY_ANIM(l_325, "rcmnigel1b", "idle_speedo");

TASK::TASK_VEHICLE_PLAY_ANIM(l_556[0/*1*/], "missfra0_chop_drhome", "InCar_GetOutofBack_Speedo");

FYI : Speedo is the name of van in which chop was put in the mission.

---

### TASK_LOOK_AT_COORD

**Hash:** `0x6FA46612594F7973` | **JHash:** `0x7B784DD8` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_LOOK_AT_COORD(Entity entity, float x, float y, float z, int duration, int flags, int priority)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_LOOK_AT_COORD, entity, x, y, z, duration, flags, priority);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `flags` | `int` | `int` | |
| `priority` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eScriptLookatFlags
{
	SLF_SLOW_TURN_RATE            = 1,    // turn the head toward the target slowly
	SLF_FAST_TURN_RATE            = 2,    // turn the head toward the target quickly
	SLF_EXTEND_YAW_LIMIT        = 4,    // wide yaw head limits
	SLF_EXTEND_PITCH_LIMIT        = 8,    // wide pitch head limit
	SLF_WIDEST_YAW_LIMIT        = 16,   // widest yaw head limit
	SLF_WIDEST_PITCH_LIMIT        = 32,   // widest pitch head limit
	SLF_NARROW_YAW_LIMIT        = 64,   // narrow yaw head limits
	SLF_NARROW_PITCH_LIMIT        = 128,  // narrow pitch head limit
	SLF_NARROWEST_YAW_LIMIT        = 256,  // narrowest yaw head limit
	SLF_NARROWEST_PITCH_LIMIT    = 512,  // narrowest pitch head limit
	SLF_USE_TORSO                = 1024, // use the torso aswell as the neck and head (currently disabled)
	SLF_WHILE_NOT_IN_FOV        = 2048, // keep tracking the target even if they are not in the hard coded FOV
	SLF_USE_CAMERA_FOCUS        = 4096, // use the camera as the target
	SLF_USE_EYES_ONLY            = 8192, // only track the target with the eyes  
	SLF_USE_LOOK_DIR            = 16384, // use information in look dir DOF
	SLF_FROM_SCRIPT                = 32768, // internal use only
	SLF_USE_REF_DIR_ABSOLUTE    = 65536  // use absolute reference direction mode for solver
};

---

### TASK_LOOK_AT_ENTITY

**Hash:** `0x69F4BE8C8CC4796C` | **JHash:** `0x991D6619` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_LOOK_AT_ENTITY(Ped ped, Entity lookAt, int duration, int flags, int priority)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_LOOK_AT_ENTITY, ped, lookAt, duration, flags, priority);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `lookAt` | `Entity` | `int` | |
| `duration` | `int` | `int` | |
| `flags` | `int` | `int` | |
| `priority` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** For flags, please refer to TASK_LOOK_AT_COORD.

---

### TASK_CLEAR_LOOK_AT

**Hash:** `0x0F804F1DB19B9689` | **JHash:** `0x60EB4054` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_CLEAR_LOOK_AT(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_CLEAR_LOOK_AT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### OPEN_SEQUENCE_TASK

**Hash:** `0xE8854A4326B9E12B` | **JHash:** `0xABA6923E` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_SEQUENCE_TASK(int* taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_SEQUENCE_TASK, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `taskSequenceId` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### CLOSE_SEQUENCE_TASK

**Hash:** `0x39E72BC99E6360CB` | **JHash:** `0x1A7CEBD0` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_SEQUENCE_TASK(int taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_SEQUENCE_TASK, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `taskSequenceId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_PERFORM_SEQUENCE

**Hash:** `0x5ABA3986D90D8A3B` | **JHash:** `0x4D9FBD11` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PERFORM_SEQUENCE(Ped ped, int taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PERFORM_SEQUENCE, ped, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `taskSequenceId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_PERFORM_SEQUENCE_LOCALLY

**Hash:** `0x8C33220C8D78CA0D` | **Build:** `944`

**C++ 原型:**
```cpp
void TASK_PERFORM_SEQUENCE_LOCALLY(Ped ped, int taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PERFORM_SEQUENCE_LOCALLY, ped, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `taskSequenceId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_SEQUENCE_TASK

**Hash:** `0x3841422E9C488D8C` | **JHash:** `0x47ED03CE` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_SEQUENCE_TASK(int* taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_SEQUENCE_TASK, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `taskSequenceId` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_SEQUENCE_TO_REPEAT

**Hash:** `0x58C70CF3A41E4AE7` | **JHash:** `0xCDDF1508` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SEQUENCE_TO_REPEAT(int taskSequenceId, BOOL repeat)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SEQUENCE_TO_REPEAT, taskSequenceId, repeat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `taskSequenceId` | `int` | `int` | |
| `repeat` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_SEQUENCE_PREVENT_MIGRATION

**Hash:** `0xF5D1F489147CB683` | **Build:** `3570`

**C++ 原型:**
```cpp
void SET_SEQUENCE_PREVENT_MIGRATION(int taskSequenceId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SEQUENCE_PREVENT_MIGRATION, taskSequenceId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `taskSequenceId` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_SEQUENCE_PROGRESS

**Hash:** `0x00A9010CFE1E3533` | **JHash:** `0xA3419909` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SEQUENCE_PROGRESS(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SEQUENCE_PROGRESS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

**说明:** returned values:
0 to 7 = task that's currently in progress, 0 meaning the first one.
-1 no task sequence in progress.

---

### GET_IS_TASK_ACTIVE

**Hash:** `0xB0760331C7AA4155` | **JHash:** `0x86FDDF55` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_TASK_ACTIVE(Ped ped, int taskIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_TASK_ACTIVE, ped, taskIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `taskIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Task index enum: https://alloc8or.re/gta5/doc/enums/eTaskTypeIndex.txt

---

### GET_SCRIPT_TASK_STATUS

**Hash:** `0x77F1BEB8863288D5` | **JHash:** `0xB2477B23` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_SCRIPT_TASK_STATUS(Ped ped, Hash taskHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SCRIPT_TASK_STATUS, ped, taskHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `taskHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Gets the status of a script-assigned task.
taskHash: https://alloc8or.re/gta5/doc/enums/eScriptTaskHash.txt

---

### GET_ACTIVE_VEHICLE_MISSION_TYPE

**Hash:** `0x534AEBA6E5ED4CAB` | **JHash:** `0xAFA914EF` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ACTIVE_VEHICLE_MISSION_TYPE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ACTIVE_VEHICLE_MISSION_TYPE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** https://alloc8or.re/gta5/doc/enums/eVehicleMissionType.txt

---

### TASK_LEAVE_ANY_VEHICLE

**Hash:** `0x504D54DF3F6F2247` | **JHash:** `0xDBDD79FA` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_LEAVE_ANY_VEHICLE(Ped ped, int delayTime, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_LEAVE_ANY_VEHICLE, ped, delayTime, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `delayTime` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Flags are the same flags used in TASK_LEAVE_VEHICLE

---

### TASK_AIM_GUN_SCRIPTED

**Hash:** `0x7A192BE16D373D00` | **JHash:** `0x9D296BCD` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_AIM_GUN_SCRIPTED(Ped ped, Hash scriptTask, BOOL disableBlockingClip, BOOL instantBlendToAim)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_AIM_GUN_SCRIPTED, ped, scriptTask, disableBlockingClip, instantBlendToAim);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `scriptTask` | `Hash` | `uint` | |
| `disableBlockingClip` | `BOOL` | `bool` | |
| `instantBlendToAim` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_AIM_GUN_SCRIPTED_WITH_TARGET

**Hash:** `0x8605AF0DE8B3A5AC` | **JHash:** `0xFD517CE3` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_AIM_GUN_SCRIPTED_WITH_TARGET(Ped ped, Ped target, float x, float y, float z, int gunTaskType, BOOL disableBlockingClip, BOOL forceAim)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_AIM_GUN_SCRIPTED_WITH_TARGET, ped, target, x, y, z, gunTaskType, disableBlockingClip, forceAim);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `gunTaskType` | `int` | `int` | |
| `disableBlockingClip` | `BOOL` | `bool` | |
| `forceAim` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### UPDATE_TASK_AIM_GUN_SCRIPTED_TARGET

**Hash:** `0x9724FB59A3E72AD0` | **JHash:** `0x67E73525` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_TASK_AIM_GUN_SCRIPTED_TARGET(Ped ped, Ped target, float x, float y, float z, BOOL disableBlockingClip)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_TASK_AIM_GUN_SCRIPTED_TARGET, ped, target, x, y, z, disableBlockingClip);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `disableBlockingClip` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_CLIP_SET_FOR_SCRIPTED_GUN_TASK

**Hash:** `0x3A8CADC7D37AACC5` | **JHash:** `0x249EB4EB` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_CLIP_SET_FOR_SCRIPTED_GUN_TASK(int gunTaskType)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_CLIP_SET_FOR_SCRIPTED_GUN_TASK, gunTaskType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `gunTaskType` | `int` | `int` | |

**返回值:** `const char*` → `string`

---

### TASK_AIM_GUN_AT_ENTITY

**Hash:** `0x9B53BB6E8943AF53` | **JHash:** `0xBE32B3B6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_AIM_GUN_AT_ENTITY(Ped ped, Entity entity, int duration, BOOL instantBlendToAim)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_AIM_GUN_AT_ENTITY, ped, entity, duration, instantBlendToAim);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `duration` | `int` | `int` | |
| `instantBlendToAim` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** duration: the amount of time in milliseconds to do the task.  -1 will keep the task going until either another task is applied, or CLEAR_ALL_TASKS() is called with the ped

---

### TASK_TURN_PED_TO_FACE_ENTITY

**Hash:** `0x5AD23D40115353AC` | **JHash:** `0x3C37C767` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_TURN_PED_TO_FACE_ENTITY(Ped ped, Entity entity, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_TURN_PED_TO_FACE_ENTITY, ped, entity, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** duration: the amount of time in milliseconds to do the task. -1 will keep the task going until either another task is applied, or CLEAR_ALL_TASKS() is called with the ped

---

### TASK_AIM_GUN_AT_COORD

**Hash:** `0x6671F3EEC681BDA1` | **JHash:** `0xFBF44AD3` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_AIM_GUN_AT_COORD(Ped ped, float x, float y, float z, int time, BOOL instantBlendToAim, BOOL playAnimIntro)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_AIM_GUN_AT_COORD, ped, x, y, z, time, instantBlendToAim, playAnimIntro);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `time` | `int` | `int` | |
| `instantBlendToAim` | `BOOL` | `bool` | |
| `playAnimIntro` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_SHOOT_AT_COORD

**Hash:** `0x46A6CC01E0826106` | **JHash:** `0x601C22E3` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SHOOT_AT_COORD(Ped ped, float x, float y, float z, int duration, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SHOOT_AT_COORD, ped, x, y, z, duration, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Firing Pattern Hash Information: https://pastebin.com/Px036isB

---

### TASK_SHUFFLE_TO_NEXT_VEHICLE_SEAT

**Hash:** `0x7AA80209BDA643EB` | **JHash:** `0xBEAF8F67` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SHUFFLE_TO_NEXT_VEHICLE_SEAT(Ped ped, Vehicle vehicle, BOOL useAlternateShuffle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SHUFFLE_TO_NEXT_VEHICLE_SEAT, ped, vehicle, useAlternateShuffle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `useAlternateShuffle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the specified ped shuffle to the next vehicle seat.
The ped MUST be in a vehicle and the vehicle parameter MUST be the ped's current vehicle.

---

### CLEAR_PED_TASKS

**Hash:** `0xE1EF3C1216AFF2CD` | **JHash:** `0xDE3316AB` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_TASKS(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_TASKS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_SCRIPT_TASK_IF_RUNNING_THREAT_RESPONSE_NON_TEMP_TASK

**Hash:** `0xF6DC48E56BE1243A` | **Build:** `3407`

**C++ 原型:**
```cpp
void CLEAR_PED_SCRIPT_TASK_IF_RUNNING_THREAT_RESPONSE_NON_TEMP_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_SCRIPT_TASK_IF_RUNNING_THREAT_RESPONSE_NON_TEMP_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PED_SECONDARY_TASK

**Hash:** `0x176CECF6F920D707` | **JHash:** `0xA635F451` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_SECONDARY_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_SECONDARY_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_EVERYONE_LEAVE_VEHICLE

**Hash:** `0x7F93691AB4B92272` | **JHash:** `0xC1971F30` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_EVERYONE_LEAVE_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_EVERYONE_LEAVE_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### TASK_GOTO_ENTITY_OFFSET

**Hash:** `0xE39B4FF4FDEBDE27` | **JHash:** `0x1A17A85E` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GOTO_ENTITY_OFFSET(Ped ped, Entity entity, int time, float seekRadius, float seekAngleDeg, float moveBlendRatio, int gotoEntityOffsetFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GOTO_ENTITY_OFFSET, ped, entity, time, seekRadius, seekAngleDeg, moveBlendRatio, gotoEntityOffsetFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `time` | `int` | `int` | |
| `seekRadius` | `float` | `float` | |
| `seekAngleDeg` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `gotoEntityOffsetFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum ESEEK_ENTITY_OFFSET_FLAGS
{
	ESEEK_OFFSET_ORIENTATES_WITH_ENTITY = 0x01,
	ESEEK_KEEP_TO_PAVEMENTS = 0x02
};

---

### TASK_GOTO_ENTITY_OFFSET_XY

**Hash:** `0x338E7EF52B6095A9` | **JHash:** `0xBC1E3D0A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GOTO_ENTITY_OFFSET_XY(Ped ped, Entity entity, int duration, float targetRadius, float offsetX, float offsetY, float moveBlendRatio, int gotoEntityOffsetFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GOTO_ENTITY_OFFSET_XY, ped, entity, duration, targetRadius, offsetX, offsetY, moveBlendRatio, gotoEntityOffsetFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `duration` | `int` | `int` | |
| `targetRadius` | `float` | `float` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `gotoEntityOffsetFlags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_TURN_PED_TO_FACE_COORD

**Hash:** `0x1DDA930A0AC38571` | **JHash:** `0x30463D73` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_TURN_PED_TO_FACE_COORD(Ped ped, float x, float y, float z, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_TURN_PED_TO_FACE_COORD, ped, x, y, z, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** duration in milliseconds

---

### TASK_VEHICLE_TEMP_ACTION

**Hash:** `0xC429DCEEB339E129` | **JHash:** `0x0679DFB8` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_TEMP_ACTION(Ped driver, Vehicle vehicle, int action, int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_TEMP_ACTION, driver, vehicle, action, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `action` | `int` | `int` | |
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** '1 - brake
'3 - brake + reverse
'4 - turn left 90 + braking
'5 - turn right 90 + braking
'6 - brake strong (handbrake?) until time ends
'7 - turn left + accelerate
'8 - turn right + accelerate
'9 - weak acceleration
'10 - turn left + restore wheel pos to center in the end
'11 - turn right + restore wheel pos to center in the end
'13 - turn left + go reverse
'14 - turn left + go reverse
'16 - crash the game after like 2 seconds :)
'17 - keep actual state, game crashed after few tries
'18 - game crash
'19 - strong brake + turn left/right
'20 - weak brake + turn left then turn right
'21 - weak brake + turn right then turn left
'22 - brake + reverse
'23 - accelerate fast
'24 - brake
'25 - brake turning left then when almost stopping it turns left more
'26 - brake turning right then when almost stopping it turns right more
'27 - brake until car stop or until time ends
'28 - brake + strong reverse acceleration
'30 - performs a burnout (brake until stop + brake and accelerate)
'31 - accelerate + handbrake
'32 - accelerate very strong

Seems to be this:
Works on NPCs, but overrides their current task. If inside a task sequence (and not being the last task), "time" will work, otherwise the task will be performed forever until tasked with something else

---

### TASK_VEHICLE_MISSION

**Hash:** `0x659427E0EF36BCDE` | **JHash:** `0x20609E56` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_MISSION(Ped driver, Vehicle vehicle, Vehicle vehicleTarget, int missionType, float cruiseSpeed, int drivingStyle, float targetReached, float straightLineDistance, BOOL DriveAgainstTraffic)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_MISSION, driver, vehicle, vehicleTarget, missionType, cruiseSpeed, drivingStyle, targetReached, straightLineDistance, DriveAgainstTraffic);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `vehicleTarget` | `Vehicle` | `int` | |
| `missionType` | `int` | `int` | |
| `cruiseSpeed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `targetReached` | `float` | `float` | |
| `straightLineDistance` | `float` | `float` | |
| `DriveAgainstTraffic` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** missionType: https://alloc8or.re/gta5/doc/enums/eVehicleMissionType.txt

---

### TASK_VEHICLE_MISSION_PED_TARGET

**Hash:** `0x9454528DF15D657A` | **JHash:** `0xC81C4677` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_MISSION_PED_TARGET(Ped ped, Vehicle vehicle, Ped pedTarget, int missionType, float maxSpeed, int drivingStyle, float minDistance, float straightLineDistance, BOOL DriveAgainstTraffic)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_MISSION_PED_TARGET, ped, vehicle, pedTarget, missionType, maxSpeed, drivingStyle, minDistance, straightLineDistance, DriveAgainstTraffic);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `pedTarget` | `Ped` | `int` | |
| `missionType` | `int` | `int` | |
| `maxSpeed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `minDistance` | `float` | `float` | |
| `straightLineDistance` | `float` | `float` | |
| `DriveAgainstTraffic` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** See TASK_VEHICLE_MISSION

---

### TASK_VEHICLE_MISSION_COORS_TARGET

**Hash:** `0xF0AF20AA7731F8C3` | **JHash:** `0x6719C109` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_MISSION_COORS_TARGET(Ped ped, Vehicle vehicle, float x, float y, float z, int mission, float cruiseSpeed, int drivingStyle, float targetReached, float straightLineDistance, BOOL DriveAgainstTraffic)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_MISSION_COORS_TARGET, ped, vehicle, x, y, z, mission, cruiseSpeed, drivingStyle, targetReached, straightLineDistance, DriveAgainstTraffic);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `mission` | `int` | `int` | |
| `cruiseSpeed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `targetReached` | `float` | `float` | |
| `straightLineDistance` | `float` | `float` | |
| `DriveAgainstTraffic` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** See TASK_VEHICLE_MISSION

---

### TASK_VEHICLE_ESCORT

**Hash:** `0x0FA6E4B75F302400` | **JHash:** `0x9FDCB250` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_ESCORT(Ped ped, Vehicle vehicle, Vehicle targetVehicle, int mode, float speed, int drivingStyle, float minDistance, int minHeightAboveTerrain, float noRoadsDistance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_ESCORT, ped, vehicle, targetVehicle, mode, speed, drivingStyle, minDistance, minHeightAboveTerrain, noRoadsDistance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `mode` | `int` | `int` | |
| `speed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `minDistance` | `float` | `float` | |
| `minHeightAboveTerrain` | `int` | `int` | |
| `noRoadsDistance` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Makes a ped follow the targetVehicle with <minDistance> in between.

note: minDistance is ignored if drivingstyle is avoiding traffic, but Rushed is fine.

Mode: The mode defines the relative position to the targetVehicle. The ped will try to position its vehicle there.
-1 = behind
0 = ahead
1 = left
2 = right
3 = back left
4 = back right

if the target is closer than noRoadsDistance, the driver will ignore pathing/roads and follow you directly.

Driving Styles guide: gtaforums.com/topic/822314-guide-driving-styles/

---

### TASK_VEHICLE_FOLLOW

**Hash:** `0xFC545A9F0626E3B6` | **JHash:** `0xA8B917D7` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_FOLLOW(Ped driver, Vehicle vehicle, Entity targetEntity, float speed, int drivingStyle, int minDistance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_FOLLOW, driver, vehicle, targetEntity, speed, drivingStyle, minDistance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `targetEntity` | `Entity` | `int` | |
| `speed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `minDistance` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes a ped in a vehicle follow an entity (ped, vehicle, etc.)

drivingStyle: http://gtaforums.com/topic/822314-guide-driving-styles/

---

### TASK_VEHICLE_CHASE

**Hash:** `0x3C08A8E30363B353` | **JHash:** `0x55634798` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_CHASE(Ped driver, Entity targetEnt)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_CHASE, driver, targetEnt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `targetEnt` | `Entity` | `int` | |

**返回值:** `void` → `void`

**说明:** chases targetEnt fast and aggressively
--
Makes ped (needs to be in vehicle) chase targetEnt.

---

### TASK_VEHICLE_HELI_PROTECT

**Hash:** `0x1E09C32048FEFD1C` | **JHash:** `0x0CB415EE` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_HELI_PROTECT(Ped pilot, Vehicle vehicle, Entity entityToFollow, float targetSpeed, int drivingFlags, float radius, int altitude, int heliFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_HELI_PROTECT, pilot, vehicle, entityToFollow, targetSpeed, drivingFlags, radius, altitude, heliFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `entityToFollow` | `Entity` | `int` | |
| `targetSpeed` | `float` | `float` | |
| `drivingFlags` | `int` | `int` | |
| `radius` | `float` | `float` | |
| `altitude` | `int` | `int` | |
| `heliFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** pilot, vehicle and altitude are rather self-explanatory.

p4: is unused variable in the function.

entityToFollow: you can provide a Vehicle entity or a Ped entity, the heli will protect them.

'targetSpeed':  The pilot will dip the nose AS MUCH AS POSSIBLE so as to reach this value AS FAST AS POSSIBLE.  As such, you'll want to modulate it as opposed to calling it via a hard-wired, constant #.

'radius' isn't just "stop within radius of X of target" like with ground vehicles.  In this case, the pilot will fly an entire circle around 'radius' and continue to do so.

NOT CONFIRMED:  p7 appears to be a FlyingStyle enum.  Still investigating it as of this writing, but playing around with values here appears to result in different -behavior- as opposed to offsetting coordinates, altitude, target speed, etc.

NOTE: If the pilot finds enemies, it will engage them until it kills them, but will return to protect the ped/vehicle given shortly thereafter.

---

### SET_TASK_VEHICLE_CHASE_BEHAVIOR_FLAG

**Hash:** `0xCC665AAC360D31E7` | **JHash:** `0x2A83083F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TASK_VEHICLE_CHASE_BEHAVIOR_FLAG(Ped ped, int flag, BOOL set)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_VEHICLE_CHASE_BEHAVIOR_FLAG, ped, flag, set);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `flag` | `int` | `int` | |
| `set` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Flag 8: Medium-aggressive boxing tactic with a bit of PIT
Flag 1: Aggressive ramming of suspect
Flag 2: Ram attempts
Flag 32: Stay back from suspect, no tactical contact. Convoy-like.
Flag 16: Ramming, seems to be slightly less aggressive than 1-2.

---

### SET_TASK_VEHICLE_CHASE_IDEAL_PURSUIT_DISTANCE

**Hash:** `0x639B642FACBE4EDD` | **JHash:** `0x04FD3EE7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TASK_VEHICLE_CHASE_IDEAL_PURSUIT_DISTANCE(Ped ped, float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_VEHICLE_CHASE_IDEAL_PURSUIT_DISTANCE, ped, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_HELI_CHASE

**Hash:** `0xAC83B1DB38D0ADA0` | **JHash:** `0xAC290A21` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_HELI_CHASE(Ped pilot, Entity entityToFollow, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_HELI_CHASE, pilot, entityToFollow, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `entityToFollow` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Ped pilot should be in a heli.
EntityToFollow can be a vehicle or Ped.

x,y,z appear to be how close to the EntityToFollow the heli should be. Scripts use 0.0, 0.0, 80.0. Then the heli tries to position itself 80 units above the EntityToFollow. If you reduce it to -5.0, it tries to go below (if the EntityToFollow is a heli or plane)


NOTE: If the pilot finds enemies, it will engage them, then remain there idle, not continuing to chase the Entity given.

---

### TASK_PLANE_CHASE

**Hash:** `0x2D2386F273FF7A25` | **JHash:** `0x12FA1C28` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLANE_CHASE(Ped pilot, Entity entityToFollow, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANE_CHASE, pilot, entityToFollow, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `entityToFollow` | `Entity` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_PLANE_LAND

**Hash:** `0xBF19721FA34D32C0` | **JHash:** `0x5F7E23EA` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLANE_LAND(Ped pilot, Vehicle plane, float runwayStartX, float runwayStartY, float runwayStartZ, float runwayEndX, float runwayEndY, float runwayEndZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANE_LAND, pilot, plane, runwayStartX, runwayStartY, runwayStartZ, runwayEndX, runwayEndY, runwayEndZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `plane` | `Vehicle` | `int` | |
| `runwayStartX` | `float` | `float` | |
| `runwayStartY` | `float` | `float` | |
| `runwayStartZ` | `float` | `float` | |
| `runwayEndX` | `float` | `float` | |
| `runwayEndY` | `float` | `float` | |
| `runwayEndZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### CLEAR_DEFAULT_PRIMARY_TASK

**Hash:** `0x6100B3CEFD43452E` | **Build:** `2189`

**C++ 原型:**
```cpp
void CLEAR_DEFAULT_PRIMARY_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_DEFAULT_PRIMARY_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_PRIMARY_VEHICLE_TASK

**Hash:** `0xDBBC7A2432524127` | **Build:** `1290`

**C++ 原型:**
```cpp
void CLEAR_PRIMARY_VEHICLE_TASK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PRIMARY_VEHICLE_TASK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** This native is very useful when switching the player to a ped inside a vehicle that has a task assigned prior to the player switch.
It is necessary to clear the ped's tasks AND call this native with the vehicle the player is switching into in order to allow the player to control the vehicle after the player switches.

---

### CLEAR_VEHICLE_CRASH_TASK

**Hash:** `0x53DDC75BC3AC0A90` | **Build:** `1290`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_CRASH_TASK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_CRASH_TASK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### TASK_PLANE_GOTO_PRECISE_VTOL

**Hash:** `0xF7F9DCCA89E7505B` | **Build:** `1290`

**C++ 原型:**
```cpp
void TASK_PLANE_GOTO_PRECISE_VTOL(Ped ped, Vehicle vehicle, float x, float y, float z, int flightHeight, int minHeightAboveTerrain, BOOL useDesiredOrientation, float desiredOrientation, BOOL autopilot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANE_GOTO_PRECISE_VTOL, ped, vehicle, x, y, z, flightHeight, minHeightAboveTerrain, useDesiredOrientation, desiredOrientation, autopilot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `flightHeight` | `int` | `int` | |
| `minHeightAboveTerrain` | `int` | `int` | |
| `useDesiredOrientation` | `BOOL` | `bool` | |
| `desiredOrientation` | `float` | `float` | |
| `autopilot` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_SUBMARINE_GOTO_AND_STOP

**Hash:** `0xC22B40579A498CA4` | **Build:** `2189`

**C++ 原型:**
```cpp
void TASK_SUBMARINE_GOTO_AND_STOP(Ped ped, Vehicle submarine, float x, float y, float z, BOOL autopilot)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SUBMARINE_GOTO_AND_STOP, ped, submarine, x, y, z, autopilot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `submarine` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `autopilot` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used in am_vehicle_spawn.ysc and am_mp_submarine.ysc.

p0 is always 0, p5 is always 1

p1 is the vehicle handle of the submarine. Submarine must have a driver, but the ped handle is not passed to the native.

Speed can be set by calling SET_DRIVE_TASK_CRUISE_SPEED after

---

### TASK_HELI_MISSION

**Hash:** `0xDAD029E187A2BEB4` | **JHash:** `0x0C143E97` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_HELI_MISSION(Ped pilot, Vehicle aircraft, Vehicle targetVehicle, Ped targetPed, float destinationX, float destinationY, float destinationZ, int missionFlag, float maxSpeed, float radius, float targetHeading, int maxHeight, int minHeight, float slowDownDistance, int behaviorFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_HELI_MISSION, pilot, aircraft, targetVehicle, targetPed, destinationX, destinationY, destinationZ, missionFlag, maxSpeed, radius, targetHeading, maxHeight, minHeight, slowDownDistance, behaviorFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `aircraft` | `Vehicle` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `destinationX` | `float` | `float` | |
| `destinationY` | `float` | `float` | |
| `destinationZ` | `float` | `float` | |
| `missionFlag` | `int` | `int` | |
| `maxSpeed` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `targetHeading` | `float` | `float` | |
| `maxHeight` | `int` | `int` | |
| `minHeight` | `int` | `int` | |
| `slowDownDistance` | `float` | `float` | |
| `behaviorFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Must have targetVehicle, targetPed, OR destination X/Y/Z set
Will follow targeted vehicle/ped, or fly to destination
Set whichever is not being used to 0


Mission mode type:
 - 4, 7: Forces heli to snap to the heading if set, flies to destination or tracks specified entity (mode 4 only works for coordinates, 7 works for coordinates OR ped/vehicle)
 - 6: Attacks the target ped/vehicle with mounted weapons. If radius is set, will maintain that distance from target.
 - 8: Makes the heli flee from the ped/vehicle/coordinate
 - 9: Circles around target ped/vehicle, snaps to angle if set. Behavior flag (last parameter) of 2048 switches from counter-clockwise to clockwise circling. Does not work with coordinate destination.
 - 10, 11: Follows ped/vehicle target and imitates target heading. Only works with ped/vehicle target, not coord target
 - 19: Heli lands at specified coordinate, ignores heading (lands facing whatever direction it is facing when the task is started)
 - 20: Makes the heli land when near target ped. It won't resume chasing.
 - 21: Emulates a helicopter crash
 - 23: makes the heli circle erratically around ped


Heli will fly at maxSpeed (up to actual maximum speed defined by the model's handling config)
You can use SET_DRIVE_TASK_CRUISE_SPEED to modulate the speed based on distance to the target without having to re-invoke the task native. Setting to 8.0 when close to the destination results in a much smoother approach.

If minHeight and maxHeight are set, heli will fly between those specified elevations, relative to ground level and any obstructions/buildings below. You can specify -1 for either if you only want to specify one. Usually it is easiest to leave maxHeight at -1, and specify a reasonable minHeight to ensure clearance over any obstacles. Note this MUST be passed as an INT, not a FLOAT. 

Radius affects how closely the heli will follow tracked ped/vehicle, and when circling (mission type 9) sets the radius (in meters) that it will circle the target from

Heading is -1.0 for default behavior, which will point the nose of the helicopter towards the destination. Set a heading and the heli will lock to that direction when near its destination/target, but may still turn towards the destination when flying at higher speed from a further distance.

Behavior Flags is a bitwise value that modifies the AI behavior. Not clear what all flags do, but here are some guesses/notes:
   1: Forces heading to face E
   2: Unknown
   4: Tight circles around coordinate destination
   8: Unknown
  16: Circles around coordinate destination facing towards destination
  32: Flys to normally, then lands at coordinate destination and stays on the ground (using mission type 4)
  64: Ignores obstacles when flying, will follow at specified minHeight above ground level but will not avoid buildings, vehicles, etc.
 128: Unknown
 256: Unknown
 512: Unknown
1024: Unknown 
2048: Reverses direction of circling (mission type 9) to clockwise
4096: Hugs closer to the ground, maintains minHeight from ground generally, but barely clears buildings and dips down more between buildings instead of taking a more efficient/safe route
8192: Unknown

Unk3 is a float value, you may see -1082130432 for this value in decompiled native scripts, this is the equivalent to -1.0f. Seems to affect acceleration/aggressiveness, but not sure exactly how it works. Higher value seems to result in lower acceleration/less aggressive flying. Almost always -1.0 in native scripts, occasionally 20.0 or 50.0. Setting to 400.0 seems to work well for making the pilot not overshoot the destination when using coordinate destination.

Notes updated by PNWParksFan, May 2021


---

### TASK_HELI_ESCORT_HELI

**Hash:** `0xB385523325077210` | **Build:** `1290`

**C++ 原型:**
```cpp
void TASK_HELI_ESCORT_HELI(Ped pilot, Vehicle heli1, Vehicle heli2, float offsetX, float offsetY, float offsetZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_HELI_ESCORT_HELI, pilot, heli1, heli2, offsetX, offsetY, offsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `heli1` | `Vehicle` | `int` | |
| `heli2` | `Vehicle` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_PLANE_MISSION

**Hash:** `0x23703CD154E83B88` | **JHash:** `0x1D007E65` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PLANE_MISSION(Ped pilot, Vehicle aircraft, Vehicle targetVehicle, Ped targetPed, float destinationX, float destinationY, float destinationZ, int missionFlag, float angularDrag, float targetReached, float targetHeading, float maxZ, float minZ, BOOL precise)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANE_MISSION, pilot, aircraft, targetVehicle, targetPed, destinationX, destinationY, destinationZ, missionFlag, angularDrag, targetReached, targetHeading, maxZ, minZ, precise);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `aircraft` | `Vehicle` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `destinationX` | `float` | `float` | |
| `destinationY` | `float` | `float` | |
| `destinationZ` | `float` | `float` | |
| `missionFlag` | `int` | `int` | |
| `angularDrag` | `float` | `float` | |
| `targetReached` | `float` | `float` | |
| `targetHeading` | `float` | `float` | |
| `maxZ` | `float` | `float` | |
| `minZ` | `float` | `float` | |
| `precise` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** EXAMPLE USAGE:

Fly around target (Precautiously, keeps high altitude):
Function.Call(Hash.TASK_PLANE_MISSION, pilot, selectedAirplane, 0, 0, Target.X, Target.Y, Target.Z, 4, 100f, 0f, 90f, 0, 200f);

Fly around target (Dangerously, keeps VERY low altitude):
Function.Call(Hash.TASK_PLANE_MISSION, pilot, selectedAirplane, 0, 0, Target.X, Target.Y, Target.Z, 4, 100f, 0f, 90f, 0, -500f);

Fly directly into target:
Function.Call(Hash.TASK_PLANE_MISSION, pilot, selectedAirplane, 0, 0, Target.X, Target.Y, Target.Z, 4, 100f, 0f, 90f, 0, -5000f);

EXPANDED INFORMATION FOR ADVANCED USAGE (custom pilot)

'physicsSpeed': (THIS IS NOT YOUR ORDINARY SPEED PARAMETER: READ!!)
Think of this -first- as a radius value, not a true speed value.  The ACTUAL effective speed of the plane will be that of the maximum speed permissible to successfully fly in a -circle- with a radius of 'physicsSpeed'.  This also means that the plane must complete a circle before it can begin its "bombing run", its straight line pass towards the target.  p9 appears to influence the angle at which a "bombing run" begins, although I can't confirm yet.

VERY IMPORTANT: A "bombing run" will only occur if a plane can successfully determine a possible navigable route (the slower the value of 'physicsSpeed', the more precise the pilot can be due to less influence of physics on flightpath).  Otherwise, the pilot will continue to patrol around Destination (be it a dynamic Entity position vector or a fixed world coordinate vector.)

0 = Plane's physics are almost entirely frozen, plane appears to "orbit" around precise destination point
1-299 = Blend of "frozen, small radius" vs. normal vs. "accelerated, hyperfast, large radius"
300+ =  Vehicle behaves entirely like a normal gameplay plane.

'patrolBlend' (The lower the value, the more the Destination is treated as a "fly AT" rather than a "fly AROUND point".)

Scenario: Destination is an Entity on ground level, wide open field
-5000 = Pilot kamikazes directly into Entity
-1000 = Pilot flies extremely low -around- Entity, very prone to crashing
-200 = Pilot flies lower than average around Entity.
0 = Pilot flies around Entity, normal altitude
200 = Pilot flies an extra eighty units or so higher than 0 while flying around Destination (this doesn't seem to correlate directly into distance units.)

-- Valid mission types found in the exe: --

0 = None
1 = Unk
2 = CTaskVehicleRam
3 = CTaskVehicleBlock
4 = CTaskVehicleGoToPlane
5 = CTaskVehicleStop
6 = CTaskVehicleAttack
7 = CTaskVehicleFollow
8 = CTaskVehicleFleeAirborne
9= CTaskVehicleCircle
10 = CTaskVehicleEscort
15 = CTaskVehicleFollowRecording
16 = CTaskVehiclePoliceBehaviour
17 = CTaskVehicleCrash

---

### TASK_PLANE_TAXI

**Hash:** `0x92C360B5F15D2302` | **Build:** `1103`

**C++ 原型:**
```cpp
void TASK_PLANE_TAXI(Ped pilot, Vehicle aircraft, float x, float y, float z, float cruiseSpeed, float targetReached)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PLANE_TAXI, pilot, aircraft, x, y, z, cruiseSpeed, targetReached);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pilot` | `Ped` | `int` | |
| `aircraft` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `cruiseSpeed` | `float` | `float` | |
| `targetReached` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_BOAT_MISSION

**Hash:** `0x15C86013127CE63F` | **JHash:** `0x5865B031` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_BOAT_MISSION(Ped pedDriver, Vehicle vehicle, Vehicle targetVehicle, Ped targetPed, float x, float y, float z, int mission, float maxSpeed, int drivingStyle, float targetReached, Any boatFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_BOAT_MISSION, pedDriver, vehicle, targetVehicle, targetPed, x, y, z, mission, maxSpeed, drivingStyle, targetReached, boatFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedDriver` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `mission` | `int` | `int` | |
| `maxSpeed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |
| `targetReached` | `float` | `float` | |
| `boatFlags` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** You need to call PED::SET_BLOCKING_OF_NON_TEMPORARY_EVENTS after TASK_BOAT_MISSION in order for the task to execute.

Working example
float vehicleMaxSpeed = VEHICLE::GET_VEHICLE_ESTIMATED_MAX_SPEED(ENTITY::GET_ENTITY_MODEL(pedVehicle));
TASK::TASK_BOAT_MISSION(pedDriver, pedVehicle, 0, 0, waypointCoord.x, waypointCoord.y, waypointCoord.z, 4, vehicleMaxSpeed, 786469, -1.0, 7);
PED::SET_BLOCKING_OF_NON_TEMPORARY_EVENTS(pedDriver, 1);

P8 appears to be driving style flag - see gtaforums.com/topic/822314-guide-driving-styles/ for documentation

---

### TASK_DRIVE_BY

**Hash:** `0x2F8AF0E82773A171` | **JHash:** `0x2B84D1C4` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_DRIVE_BY(Ped driverPed, Ped targetPed, Vehicle targetVehicle, float targetX, float targetY, float targetZ, float distanceToShoot, int pedAccuracy, BOOL pushUnderneathDrivingTaskIfDriving, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_DRIVE_BY, driverPed, targetPed, targetVehicle, targetX, targetY, targetZ, distanceToShoot, pedAccuracy, pushUnderneathDrivingTaskIfDriving, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driverPed` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `targetX` | `float` | `float` | |
| `targetY` | `float` | `float` | |
| `targetZ` | `float` | `float` | |
| `distanceToShoot` | `float` | `float` | |
| `pedAccuracy` | `int` | `int` | |
| `pushUnderneathDrivingTaskIfDriving` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Example:

TASK::TASK_DRIVE_BY(l_467[1/*22*/], PLAYER::PLAYER_PED_ID(), 0, 0.0, 0.0, 2.0, 300.0, 100, 0, ${firing_pattern_burst_fire_driveby});




Needs working example. Doesn't seem to do anything.

I marked p2 as targetVehicle as all these shooting related tasks seem to have that in common.
I marked p6 as distanceToShoot as if you think of GTA's Logic with the native SET_VEHICLE_SHOOT natives, it won't shoot till it gets within a certain distance of the target.
I marked p7 as pedAccuracy as it seems it's mostly 100 (Completely Accurate), 75, 90, etc. Although this could be the ammo count within the gun, but I highly doubt it. I will change this comment once I find out if it's ammo count or not.

---

### SET_DRIVEBY_TASK_TARGET

**Hash:** `0xE5B302114D8162EE` | **JHash:** `0xDA6A6FC1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVEBY_TASK_TARGET(Ped shootingPed, Ped targetPed, Vehicle targetVehicle, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVEBY_TASK_TARGET, shootingPed, targetPed, targetVehicle, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shootingPed` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** For p1 & p2 (Ped, Vehicle). I could be wrong, as the only time this native is called in scripts is once and both are 0, but I assume this native will work like SET_MOUNTED_WEAPON_TARGET in which has the same exact amount of parameters and the 1st and last 3 parameters are right and the same for both natives.

---

### CLEAR_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK

**Hash:** `0xC35B5CDB2824CF69` | **JHash:** `0x9B76F7E6` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK

**Hash:** `0x8785E6E40C7A8818` | **JHash:** `0xB23F46E6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_DRIVEBY_TASK_UNDERNEATH_DRIVING_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CONTROL_MOUNTED_WEAPON

**Hash:** `0xDCFE42068FE0135A` | **JHash:** `0x500D9244` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CONTROL_MOUNTED_WEAPON(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CONTROL_MOUNTED_WEAPON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Forces the ped to use the mounted weapon.
Returns false if task is not possible.

---

### SET_MOUNTED_WEAPON_TARGET

**Hash:** `0xCCD892192C6D2BB9` | **JHash:** `0x98713C68` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MOUNTED_WEAPON_TARGET(Ped shootingPed, Ped targetPed, Vehicle targetVehicle, float x, float y, float z, int taskMode, BOOL ignoreTargetVehDeadCheck)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MOUNTED_WEAPON_TARGET, shootingPed, targetPed, targetVehicle, x, y, z, taskMode, ignoreTargetVehDeadCheck);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `shootingPed` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `taskMode` | `int` | `int` | |
| `ignoreTargetVehDeadCheck` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Note: Look in decompiled scripts and the times that p1 and p2 aren't 0. They are filled with vars. If you look through out that script what other natives those vars are used in, you can tell p1 is a ped and p2 is a vehicle. Which most likely means if you want the mounted weapon to target a ped set targetVehicle to 0 or vice-versa.

---

### IS_MOUNTED_WEAPON_TASK_UNDERNEATH_DRIVING_TASK

**Hash:** `0xA320EF046186FA3B` | **JHash:** `0x291E938C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOUNTED_WEAPON_TASK_UNDERNEATH_DRIVING_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOUNTED_WEAPON_TASK_UNDERNEATH_DRIVING_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### TASK_USE_MOBILE_PHONE

**Hash:** `0xBD2A8EC3AF4DE7DB` | **JHash:** `0x225A38C8` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_MOBILE_PHONE(Ped ped, BOOL usePhone, int desiredPhoneMode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_MOBILE_PHONE, ped, usePhone, desiredPhoneMode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `usePhone` | `BOOL` | `bool` | |
| `desiredPhoneMode` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Actually has 3 params, not 2.

p0: Ped
p1: int (or bool?)
p2: int

---

### TASK_USE_MOBILE_PHONE_TIMED

**Hash:** `0x5EE02954A14C69DB` | **JHash:** `0xC99C19F5` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_MOBILE_PHONE_TIMED(Ped ped, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_MOBILE_PHONE_TIMED, ped, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_CHAT_TO_PED

**Hash:** `0x8C338E0263E4FD19` | **JHash:** `0xA2BE1821` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_CHAT_TO_PED(Ped ped, Ped target, int flags, float goToLocationX, float goToLocationY, float goToLocationZ, float headingDegs, float idleTime)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_CHAT_TO_PED, ped, target, flags, goToLocationX, goToLocationY, goToLocationZ, headingDegs, idleTime);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `flags` | `int` | `int` | |
| `goToLocationX` | `float` | `float` | |
| `goToLocationY` | `float` | `float` | |
| `goToLocationZ` | `float` | `float` | |
| `headingDegs` | `float` | `float` | |
| `idleTime` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p2 tend to be 16, 17 or 1
p3 to p7 tend to be 0.0

---

### TASK_WARP_PED_INTO_VEHICLE

**Hash:** `0x9A7D091411C5F684` | **JHash:** `0x65D4A35D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_WARP_PED_INTO_VEHICLE(Ped ped, Vehicle vehicle, int seat)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WARP_PED_INTO_VEHICLE, ped, vehicle, seat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Seat Numbers
-------------------------------
Driver = -1
Any = -2
Left-Rear = 1
Right-Front = 0
Right-Rear = 2
Extra seats = 3-14(This may differ from vehicle type e.g. Firetruck Rear Stand, Ambulance Rear)

---

### TASK_SHOOT_AT_ENTITY

**Hash:** `0x08DA95E8298AE772` | **JHash:** `0xAC0631C9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SHOOT_AT_ENTITY(Entity entity, Entity target, int duration, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SHOOT_AT_ENTITY, entity, target, duration, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `target` | `Entity` | `int` | |
| `duration` | `int` | `int` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** //this part of the code is to determine at which entity the player is aiming, for example if you want to create a mod where you give orders to peds
Entity aimedentity;
Player player = PLAYER::PLAYER_ID();
PLAYER::_GET_AIMED_ENTITY(player, &aimedentity);

//bg is an array of peds
TASK::TASK_SHOOT_AT_ENTITY(bg[i], aimedentity, 5000, MISC::GET_HASH_KEY("FIRING_PATTERN_FULL_AUTO"));

in practical usage, getting the entity the player is aiming at and then task the peds to shoot at the entity, at a button press event would be better.

Firing Pattern Hash Information: https://pastebin.com/Px036isB

---

### TASK_CLIMB

**Hash:** `0x89D9FCC2435112F1` | **JHash:** `0x90847790` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_CLIMB(Ped ped, BOOL usePlayerLaunchForce)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_CLIMB, ped, usePlayerLaunchForce);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `usePlayerLaunchForce` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Climbs or vaults the nearest thing.
usePlayerLaunchForce is unused.

---

### TASK_CLIMB_LADDER

**Hash:** `0xB6C987F9285A3814` | **JHash:** `0x35BB4EE0` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_CLIMB_LADDER(Ped ped, BOOL fast)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_CLIMB_LADDER, ped, fast);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `fast` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_RAPPEL_DOWN_WALL_USING_CLIPSET_OVERRIDE

**Hash:** `0xEAF66ACDDC794793` | **Build:** `1868`

**C++ 原型:**
```cpp
void TASK_RAPPEL_DOWN_WALL_USING_CLIPSET_OVERRIDE(Ped ped, float x1, float y1, float z1, float x2, float y2, float z2, float minZ, int ropeHandle, const char* clipSet, Any p10, Any p11)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_RAPPEL_DOWN_WALL_USING_CLIPSET_OVERRIDE, ped, x1, y1, z1, x2, y2, z2, minZ, ropeHandle, clipSet, p10, p11);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `minZ` | `float` | `float` | |
| `ropeHandle` | `int` | `int` | |
| `clipSet` | `const char*` | `string` | |
| `p10` | `Any` | `object` | |
| `p11` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Attaches a ped to a rope and allows player control to rappel down a wall. Disables all collisions while on the rope.
p10: Usually 1 in the scripts, clipSet: Clipset to use for the task, minZ: Minimum Z that the player can descend to, ropeHandle: Rope to attach this task to created with ADD_ROPE

---

### GET_TASK_RAPPEL_DOWN_WALL_STATE

**Hash:** `0x9D252648778160DF` | **Build:** `1868`

**C++ 原型:**
```cpp
int GET_TASK_RAPPEL_DOWN_WALL_STATE(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TASK_RAPPEL_DOWN_WALL_STATE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### CLEAR_PED_TASKS_IMMEDIATELY

**Hash:** `0xAAA34F8A7CB32098` | **JHash:** `0xBC045625` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_TASKS_IMMEDIATELY(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_TASKS_IMMEDIATELY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Immediately stops the pedestrian from whatever it's doing. They stop fighting, animations, etc. they forget what they were doing.

---

### TASK_PERFORM_SEQUENCE_FROM_PROGRESS

**Hash:** `0x89221B16730234F0` | **JHash:** `0xFA60601B` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PERFORM_SEQUENCE_FROM_PROGRESS(Ped ped, int taskIndex, int progress1, int progress2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PERFORM_SEQUENCE_FROM_PROGRESS, ped, taskIndex, progress1, progress2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `taskIndex` | `int` | `int` | |
| `progress1` | `int` | `int` | |
| `progress2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_NEXT_DESIRED_MOVE_STATE

**Hash:** `0xF1B9F16E89E2C93A` | **JHash:** `0x4E937D57` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NEXT_DESIRED_MOVE_STATE(float nextMoveState)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NEXT_DESIRED_MOVE_STATE, nextMoveState);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nextMoveState` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native does absolutely nothing, just a nullsub

R* Comment:
SET_NEXT_DESIRED_MOVE_STATE - Function is deprecated - do not use anymore

---

### SET_PED_DESIRED_MOVE_BLEND_RATIO

**Hash:** `0x1E982AC8716912C5` | **JHash:** `0xC65FC712` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DESIRED_MOVE_BLEND_RATIO(Ped ped, float newMoveBlendRatio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DESIRED_MOVE_BLEND_RATIO, ped, newMoveBlendRatio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `newMoveBlendRatio` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PED_DESIRED_MOVE_BLEND_RATIO

**Hash:** `0x8517D4A6CA8513ED` | **JHash:** `0x5FEFAB72` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PED_DESIRED_MOVE_BLEND_RATIO(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PED_DESIRED_MOVE_BLEND_RATIO, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### TASK_GOTO_ENTITY_AIMING

**Hash:** `0xA9DA48FAB8A76C12` | **JHash:** `0xF1C493CF` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GOTO_ENTITY_AIMING(Ped ped, Entity target, float distanceToStopAt, float StartAimingDist)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GOTO_ENTITY_AIMING, ped, target, distanceToStopAt, StartAimingDist);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Entity` | `int` | |
| `distanceToStopAt` | `float` | `float` | |
| `StartAimingDist` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** eg

 TASK::TASK_GOTO_ENTITY_AIMING(v_2, PLAYER::PLAYER_PED_ID(), 5.0, 25.0);

ped = Ped you want to perform this task.
target = the Entity they should aim at.
distanceToStopAt = distance from the target, where the ped should stop to aim.
StartAimingDist = distance where the ped should start to aim.

---

### TASK_SET_DECISION_MAKER

**Hash:** `0xEB8517DDA73720DA` | **JHash:** `0x830AD50C` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SET_DECISION_MAKER(Ped ped, Hash decisionMakerId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SET_DECISION_MAKER, ped, decisionMakerId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `decisionMakerId` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** p1 is always GET_HASH_KEY("empty") in scripts, for the rare times this is used

---

### TASK_SET_SPHERE_DEFENSIVE_AREA

**Hash:** `0x933C06518B52A9A4` | **JHash:** `0x9F3C5D6A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SET_SPHERE_DEFENSIVE_AREA(Ped ped, float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SET_SPHERE_DEFENSIVE_AREA, ped, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_CLEAR_DEFENSIVE_AREA

**Hash:** `0x95A6C46A31D1917D` | **JHash:** `0x7A05BF0D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_CLEAR_DEFENSIVE_AREA(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_CLEAR_DEFENSIVE_AREA, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_PED_SLIDE_TO_COORD

**Hash:** `0xD04FE6765D990A06` | **JHash:** `0x225380EF` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PED_SLIDE_TO_COORD(Ped ped, float x, float y, float z, float heading, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PED_SLIDE_TO_COORD, ped, x, y, z, heading, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_PED_SLIDE_TO_COORD_HDG_RATE

**Hash:** `0x5A4A6A6D3DC64F52` | **JHash:** `0x38A995C1` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PED_SLIDE_TO_COORD_HDG_RATE(Ped ped, float x, float y, float z, float heading, float speed, float headingChangeRate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PED_SLIDE_TO_COORD_HDG_RATE, ped, x, y, z, heading, speed, headingChangeRate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `headingChangeRate` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ADD_COVER_POINT

**Hash:** `0xD5C12A75C7B9497F` | **JHash:** `0xA0AF0B98` | **Build:** `323`

**C++ 原型:**
```cpp
ScrHandle ADD_COVER_POINT(float x, float y, float z, float direction, int usage, int height, int arc, BOOL isPriority)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_COVER_POINT, x, y, z, direction, usage, height, arc, isPriority);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `direction` | `float` | `float` | |
| `usage` | `int` | `int` | |
| `height` | `int` | `int` | |
| `arc` | `int` | `int` | |
| `isPriority` | `BOOL` | `bool` | |

**返回值:** `ScrHandle` → `int`

---

### REMOVE_COVER_POINT

**Hash:** `0xAE287C923D891715` | **JHash:** `0x0776888B` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_COVER_POINT(ScrHandle coverpoint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_COVER_POINT, coverpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `coverpoint` | `ScrHandle` | `int` | |

**返回值:** `void` → `void`

---

### DOES_SCRIPTED_COVER_POINT_EXIST_AT_COORDS

**Hash:** `0xA98B8E3C088E5A31` | **JHash:** `0x29F97A71` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCRIPTED_COVER_POINT_EXIST_AT_COORDS(float x, float y, float z)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCRIPTED_COVER_POINT_EXIST_AT_COORDS, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if there is a cover point at position

---

### GET_SCRIPTED_COVER_POINT_COORDS

**Hash:** `0x594A1028FC2A3E85` | **JHash:** `0xC6B6CCC1` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_SCRIPTED_COVER_POINT_COORDS(ScrHandle coverpoint)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_SCRIPTED_COVER_POINT_COORDS, coverpoint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `coverpoint` | `ScrHandle` | `int` | |

**返回值:** `Vector3` → `Vector3`

---

### ADD_SCRIPTED_COVER_AREA

**Hash:** `0x28B7B9BFDAF274AA` | **Build:** `2545`

**C++ 原型:**
```cpp
void ADD_SCRIPTED_COVER_AREA(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_SCRIPTED_COVER_AREA, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_COMBAT_PED

**Hash:** `0xF166E48407BAC484` | **JHash:** `0xCB0D8932` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COMBAT_PED(Ped ped, Ped targetPed, int combatFlags, int threatResponseFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COMBAT_PED, ped, targetPed, combatFlags, threatResponseFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `targetPed` | `Ped` | `int` | |
| `combatFlags` | `int` | `int` | |
| `threatResponseFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the specified ped attack the target ped.
p2 should be 0
p3 should be 16

---

### TASK_COMBAT_PED_TIMED

**Hash:** `0x944F30DCB7096BDE` | **JHash:** `0xF5CA2A45` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COMBAT_PED_TIMED(Ped ped, Ped target, int time, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COMBAT_PED_TIMED, ped, target, time, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `time` | `int` | `int` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_SEEK_COVER_FROM_POS

**Hash:** `0x75AC2B60386D89F2` | **JHash:** `0x83F18EE9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SEEK_COVER_FROM_POS(Ped ped, float x, float y, float z, int duration, BOOL allowPeekingAndFiring)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SEEK_COVER_FROM_POS, ped, x, y, z, duration, allowPeekingAndFiring);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `allowPeekingAndFiring` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_SEEK_COVER_FROM_PED

**Hash:** `0x84D32B3BEC531324` | **JHash:** `0xC1EC907E` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SEEK_COVER_FROM_PED(Ped ped, Ped target, int duration, BOOL allowPeekingAndFiring)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SEEK_COVER_FROM_PED, ped, target, duration, allowPeekingAndFiring);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `duration` | `int` | `int` | |
| `allowPeekingAndFiring` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_SEEK_COVER_TO_COVER_POINT

**Hash:** `0xD43D95C7A869447F` | **JHash:** `0x3D026B29` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SEEK_COVER_TO_COVER_POINT(Ped ped, ScrHandle coverpoint, float x, float y, float z, int time, BOOL allowPeekingAndFiring)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SEEK_COVER_TO_COVER_POINT, ped, coverpoint, x, y, z, time, allowPeekingAndFiring);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `coverpoint` | `ScrHandle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `time` | `int` | `int` | |
| `allowPeekingAndFiring` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p5 is always -1

---

### TASK_SEEK_COVER_TO_COORDS

**Hash:** `0x39246A6958EF072C` | **JHash:** `0xFFFE754E` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SEEK_COVER_TO_COORDS(Ped ped, float x1, float y1, float z1, float x2, float y2, float z2, int timeout, BOOL shortRoute)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SEEK_COVER_TO_COORDS, ped, x1, y1, z1, x2, y2, z2, timeout, shortRoute);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `timeout` | `int` | `int` | |
| `shortRoute` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p8 causes the ped to take the shortest route to the cover position. It may have something to do with navmesh or pathfinding mechanics.

from michael2:
TASK::TASK_SEEK_COVER_TO_COORDS(ped, 967.5164794921875, -2121.603515625, 30.479299545288086, 978.94677734375, -2125.84130859375, 29.4752, -1, 1);


appears to be shorter variation
from michael3:
TASK::TASK_SEEK_COVER_TO_COORDS(ped, -2231.011474609375, 263.6326599121094, 173.60195922851562, -1, 0);

---

### TASK_PUT_PED_DIRECTLY_INTO_COVER

**Hash:** `0x4172393E6BE1FECE` | **JHash:** `0xC9F00E68` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PUT_PED_DIRECTLY_INTO_COVER(Ped ped, float x, float y, float z, int time, BOOL allowPeekingAndFiring, float blendInDuration, BOOL forceInitialFacingDirection, BOOL forceFaceLeft, int identifier, BOOL doEntry)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PUT_PED_DIRECTLY_INTO_COVER, ped, x, y, z, time, allowPeekingAndFiring, blendInDuration, forceInitialFacingDirection, forceFaceLeft, identifier, doEntry);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `time` | `int` | `int` | |
| `allowPeekingAndFiring` | `BOOL` | `bool` | |
| `blendInDuration` | `float` | `float` | |
| `forceInitialFacingDirection` | `BOOL` | `bool` | |
| `forceFaceLeft` | `BOOL` | `bool` | |
| `identifier` | `int` | `int` | |
| `doEntry` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_WARP_PED_DIRECTLY_INTO_COVER

**Hash:** `0x6E01E9E8D89F8276` | **Build:** `2545`

**C++ 原型:**
```cpp
void TASK_WARP_PED_DIRECTLY_INTO_COVER(Ped ped, int time, BOOL allowPeekingAndFiring, BOOL forceInitialFacingDirection, BOOL forceFaceLeft, int identifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WARP_PED_DIRECTLY_INTO_COVER, ped, time, allowPeekingAndFiring, forceInitialFacingDirection, forceFaceLeft, identifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `time` | `int` | `int` | |
| `allowPeekingAndFiring` | `BOOL` | `bool` | |
| `forceInitialFacingDirection` | `BOOL` | `bool` | |
| `forceFaceLeft` | `BOOL` | `bool` | |
| `identifier` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_EXIT_COVER

**Hash:** `0x79B258E397854D29` | **JHash:** `0xC829FAC9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_EXIT_COVER(Ped ped, int exitType, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_EXIT_COVER, ped, exitType, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `exitType` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p1 is 1, 2, or 3 in scripts

---

### TASK_PUT_PED_DIRECTLY_INTO_MELEE

**Hash:** `0x1C6CD14A876FFE39` | **JHash:** `0x79E1D27D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PUT_PED_DIRECTLY_INTO_MELEE(Ped ped, Ped meleeTarget, float blendInDuration, float timeInMelee, float strafePhaseSync, int aiCombatFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PUT_PED_DIRECTLY_INTO_MELEE, ped, meleeTarget, blendInDuration, timeInMelee, strafePhaseSync, aiCombatFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `meleeTarget` | `Ped` | `int` | |
| `blendInDuration` | `float` | `float` | |
| `timeInMelee` | `float` | `float` | |
| `strafePhaseSync` | `float` | `float` | |
| `aiCombatFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** from armenian3.c4

TASK::TASK_PUT_PED_DIRECTLY_INTO_MELEE(PlayerPed, armenianPed, 0.0, -1.0, 0.0, 0);


---

### TASK_TOGGLE_DUCK

**Hash:** `0xAC96609B9995EDF8` | **JHash:** `0x61CFBCBF` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_TOGGLE_DUCK(Ped ped, int toggleType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_TOGGLE_DUCK, ped, toggleType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggleType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** used in sequence task

both parameters seems to be always 0

---

### TASK_GUARD_CURRENT_POSITION

**Hash:** `0x4A58A47A72E3FCB4` | **JHash:** `0x2FB099E9` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GUARD_CURRENT_POSITION(Ped ped, float maxPatrolProximity, float defensiveAreaRadius, BOOL setDefensiveArea)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GUARD_CURRENT_POSITION, ped, maxPatrolProximity, defensiveAreaRadius, setDefensiveArea);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `maxPatrolProximity` | `float` | `float` | |
| `defensiveAreaRadius` | `float` | `float` | |
| `setDefensiveArea` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** From re_prisonvanbreak:

TASK::TASK_GUARD_CURRENT_POSITION(l_DD, 35.0, 35.0, 1);

---

### TASK_GUARD_ASSIGNED_DEFENSIVE_AREA

**Hash:** `0xD2A207EEBDF9889B` | **JHash:** `0x7AF0133D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GUARD_ASSIGNED_DEFENSIVE_AREA(Ped ped, float x, float y, float z, float heading, float maxPatrolProximity, int timer)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GUARD_ASSIGNED_DEFENSIVE_AREA, ped, x, y, z, heading, maxPatrolProximity, timer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `maxPatrolProximity` | `float` | `float` | |
| `timer` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_GUARD_SPHERE_DEFENSIVE_AREA

**Hash:** `0xC946FE14BE0EB5E2` | **JHash:** `0x86B76CB7` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GUARD_SPHERE_DEFENSIVE_AREA(Ped ped, float defendPositionX, float defendPositionY, float defendPositionZ, float heading, float maxPatrolProximity, int time, float x, float y, float z, float defensiveAreaRadius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GUARD_SPHERE_DEFENSIVE_AREA, ped, defendPositionX, defendPositionY, defendPositionZ, heading, maxPatrolProximity, time, x, y, z, defensiveAreaRadius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `defendPositionX` | `float` | `float` | |
| `defendPositionY` | `float` | `float` | |
| `defendPositionZ` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `maxPatrolProximity` | `float` | `float` | |
| `time` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `defensiveAreaRadius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_STAND_GUARD

**Hash:** `0xAE032F8BBA959E90` | **JHash:** `0xD130F636` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_STAND_GUARD(Ped ped, float x, float y, float z, float heading, const char* scenarioName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_STAND_GUARD, ped, x, y, z, heading, scenarioName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `scenarioName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** scenarioName example: "WORLD_HUMAN_GUARD_STAND"

---

### SET_DRIVE_TASK_CRUISE_SPEED

**Hash:** `0x5C9B84BD7D31D908` | **JHash:** `0x3CEC07B1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVE_TASK_CRUISE_SPEED(Ped driver, float cruiseSpeed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVE_TASK_CRUISE_SPEED, driver, cruiseSpeed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `cruiseSpeed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_DRIVE_TASK_MAX_CRUISE_SPEED

**Hash:** `0x404A5AA9B9F0B746` | **JHash:** `0x7FDF6131` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVE_TASK_MAX_CRUISE_SPEED(Ped ped, float speed, BOOL updateBaseTask)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVE_TASK_MAX_CRUISE_SPEED, ped, speed, updateBaseTask);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `speed` | `float` | `float` | |
| `updateBaseTask` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DRIVE_TASK_DRIVING_STYLE

**Hash:** `0xDACE1BE37D88AF67` | **JHash:** `0x59C5FAD7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DRIVE_TASK_DRIVING_STYLE(Ped ped, int drivingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIVE_TASK_DRIVING_STYLE, ped, drivingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `drivingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** This native is used to set the driving style for specific ped.

Driving styles id seems to be:
786468
262144
786469

http://gtaforums.com/topic/822314-guide-driving-styles/

---

### ADD_COVER_BLOCKING_AREA

**Hash:** `0x45C597097DD7CB81` | **JHash:** `0x3536946F` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_COVER_BLOCKING_AREA(float startX, float startY, float startZ, float endX, float endY, float endZ, BOOL blockObjects, BOOL blockVehicles, BOOL blockMap, BOOL blockPlayer)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_COVER_BLOCKING_AREA, startX, startY, startZ, endX, endY, endZ, blockObjects, blockVehicles, blockMap, blockPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startX` | `float` | `float` | |
| `startY` | `float` | `float` | |
| `startZ` | `float` | `float` | |
| `endX` | `float` | `float` | |
| `endY` | `float` | `float` | |
| `endZ` | `float` | `float` | |
| `blockObjects` | `BOOL` | `bool` | |
| `blockVehicles` | `BOOL` | `bool` | |
| `blockMap` | `BOOL` | `bool` | |
| `blockPlayer` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_ALL_COVER_BLOCKING_AREAS

**Hash:** `0xDB6708C0B46F56D8` | **JHash:** `0xCF9221A7` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_COVER_BLOCKING_AREAS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_COVER_BLOCKING_AREAS);
```

**返回值:** `void` → `void`

---

### REMOVE_COVER_BLOCKING_AREAS_AT_POSITION

**Hash:** `0xFA83CA6776038F64` | **Build:** `1493`

**C++ 原型:**
```cpp
void REMOVE_COVER_BLOCKING_AREAS_AT_POSITION(float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_COVER_BLOCKING_AREAS_AT_POSITION, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### REMOVE_SPECIFIC_COVER_BLOCKING_AREAS

**Hash:** `0x1F351CF1C6475734` | **Build:** `505`

**C++ 原型:**
```cpp
void REMOVE_SPECIFIC_COVER_BLOCKING_AREAS(float startX, float startY, float startZ, float endX, float endY, float endZ, BOOL blockObjects, BOOL blockVehicles, BOOL blockMap, BOOL blockPlayer)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_SPECIFIC_COVER_BLOCKING_AREAS, startX, startY, startZ, endX, endY, endZ, blockObjects, blockVehicles, blockMap, blockPlayer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `startX` | `float` | `float` | |
| `startY` | `float` | `float` | |
| `startZ` | `float` | `float` | |
| `endX` | `float` | `float` | |
| `endY` | `float` | `float` | |
| `endZ` | `float` | `float` | |
| `blockObjects` | `BOOL` | `bool` | |
| `blockVehicles` | `BOOL` | `bool` | |
| `blockMap` | `BOOL` | `bool` | |
| `blockPlayer` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_START_SCENARIO_IN_PLACE

**Hash:** `0x142A02425FF02BD9` | **JHash:** `0xE50D6DDE` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_START_SCENARIO_IN_PLACE(Ped ped, const char* scenarioName, int unkDelay, BOOL playEnterAnim)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_START_SCENARIO_IN_PLACE, ped, scenarioName, unkDelay, playEnterAnim);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `scenarioName` | `const char*` | `string` | |
| `unkDelay` | `int` | `int` | |
| `playEnterAnim` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Plays a scenario on a Ped at their current location.

unkDelay - Usually 0 or -1, doesn't seem to have any effect. Might be a delay between sequences.
playEnterAnim - Plays the "Enter" anim if true, otherwise plays the "Exit" anim. Scenarios that don't have any "Enter" anims won't play if this is set to true.

----

From "am_hold_up.ysc.c4" at line 339:

TASK::TASK_START_SCENARIO_IN_PLACE(NETWORK::NET_TO_PED(l_8D._f4), sub_adf(), 0, 1);

I'm unsure of what the last two parameters are, however sub_adf() randomly returns 1 of 3 scenarios, those being:
WORLD_HUMAN_SMOKING
WORLD_HUMAN_HANG_OUT_STREET
WORLD_HUMAN_STAND_MOBILE

This makes sense, as these are what I commonly see when going by a liquor store.
-------------------------
List of scenarioNames: https://pastebin.com/6mrYTdQv
(^ Thank you so fucking much for this)

Also these:
WORLD_FISH_FLEE
DRIVE
WORLD_HUMAN_HIKER
WORLD_VEHICLE_ATTRACTOR
WORLD_VEHICLE_BICYCLE_MOUNTAIN
WORLD_VEHICLE_BIKE_OFF_ROAD_RACE
WORLD_VEHICLE_BIKER
WORLD_VEHICLE_CONSTRUCTION_PASSENGERS
WORLD_VEHICLE_CONSTRUCTION_SOLO
WORLD_VEHICLE_DRIVE_PASSENGERS
WORLD_VEHICLE_DRIVE_SOLO
WORLD_VEHICLE_EMPTY
WORLD_VEHICLE_PARK_PARALLEL
WORLD_VEHICLE_PARK_PERPENDICULAR_NOSE_IN
WORLD_VEHICLE_POLICE_BIKE
WORLD_VEHICLE_POLICE_CAR
WORLD_VEHICLE_POLICE_NEXT_TO_CAR
WORLD_VEHICLE_SALTON_DIRT_BIKE
WORLD_VEHICLE_TRUCK_LOGS

Full list of ped scenarios by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenariosCompact.json

---

### TASK_START_SCENARIO_AT_POSITION

**Hash:** `0xFA4EFC79F69D4F07` | **JHash:** `0xAA2C4AC2` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_START_SCENARIO_AT_POSITION(Ped ped, const char* scenarioName, float x, float y, float z, float heading, int duration, BOOL sittingScenario, BOOL teleport)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_START_SCENARIO_AT_POSITION, ped, scenarioName, x, y, z, heading, duration, sittingScenario, teleport);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `scenarioName` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `sittingScenario` | `BOOL` | `bool` | |
| `teleport` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of ped scenarios by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenariosCompact.json

Also a few more listed at TASK::TASK_START_SCENARIO_IN_PLACE just above.
---------------
The first parameter in every scenario has always been a Ped of some sort. The second like TASK_START_SCENARIO_IN_PLACE is the name of the scenario. 

The next 4 parameters were harder to decipher. After viewing "hairdo_shop_mp.ysc.c4", and being confused from seeing the case in other scripts, they passed the first three of the arguments as one array from a function, and it looked like it was obviously x, y, and z.

I haven't seen the sixth parameter go to or over 360, making me believe that it is rotation, but I really can't confirm anything.

I have no idea what the last 3 parameters are, but I'll try to find out.

-going on the last 3 parameters, they appear to always be "0, 0, 1"

p6 -1 also used in scrips

p7 used for sitting scenarios

p8 teleports ped to position

---

### TASK_USE_NEAREST_SCENARIO_TO_COORD

**Hash:** `0x277F471BA9DB000B` | **JHash:** `0x9C50FBF0` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_NEAREST_SCENARIO_TO_COORD(Ped ped, float x, float y, float z, float distance, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_NEAREST_SCENARIO_TO_COORD, ped, x, y, z, distance, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `distance` | `float` | `float` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Updated variables

An alternative to TASK::TASK_USE_NEAREST_SCENARIO_TO_COORD_WARP. Makes the ped walk to the scenario instead.

---

### TASK_USE_NEAREST_SCENARIO_TO_COORD_WARP

**Hash:** `0x58E2E0F23F6B76C3` | **JHash:** `0x1BE9D65C` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_NEAREST_SCENARIO_TO_COORD_WARP(Ped ped, float x, float y, float z, float radius, int timeToLeave)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_NEAREST_SCENARIO_TO_COORD_WARP, ped, x, y, z, radius, timeToLeave);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `timeToLeave` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD

**Hash:** `0x9FDA1B3D7E7028B3` | **JHash:** `0xE32FFB22` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD(Ped ped, float x, float y, float z, float maxRange, int timeToLeave)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD, ped, x, y, z, maxRange, timeToLeave);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `maxRange` | `float` | `float` | |
| `timeToLeave` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p5 is always 0 in scripts

---

### TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD_WARP

**Hash:** `0x97A28E63F0BA5631` | **JHash:** `0xBAB4C0AE` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD_WARP(Ped ped, float x, float y, float z, float radius, int timeToLeave)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_USE_NEAREST_SCENARIO_CHAIN_TO_COORD_WARP, ped, x, y, z, radius, timeToLeave);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `timeToLeave` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p5 is always -1 or 0 in scripts

---

### DOES_SCENARIO_EXIST_IN_AREA

**Hash:** `0x5A59271FFADD33C1` | **JHash:** `0xFA7F5047` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCENARIO_EXIST_IN_AREA(float x, float y, float z, float radius, BOOL mustBeFree)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCENARIO_EXIST_IN_AREA, x, y, z, radius, mustBeFree);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `mustBeFree` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### DOES_SCENARIO_OF_TYPE_EXIST_IN_AREA

**Hash:** `0x0A9D0C2A3BBC86C1` | **JHash:** `0x0FB138A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCENARIO_OF_TYPE_EXIST_IN_AREA(float x, float y, float z, const char* scenarioName, float radius, BOOL mustBeFree)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCENARIO_OF_TYPE_EXIST_IN_AREA, x, y, z, scenarioName, radius, mustBeFree);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `scenarioName` | `const char*` | `string` | |
| `radius` | `float` | `float` | |
| `mustBeFree` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_SCENARIO_OCCUPIED

**Hash:** `0x788756D73AC2E07C` | **JHash:** `0x697FC008` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCENARIO_OCCUPIED(float x, float y, float z, float maxRange, BOOL onlyUsersActuallyAtScenario)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCENARIO_OCCUPIED, x, y, z, maxRange, onlyUsersActuallyAtScenario);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `maxRange` | `float` | `float` | |
| `onlyUsersActuallyAtScenario` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### PED_HAS_USE_SCENARIO_TASK

**Hash:** `0x295E3CCEC879CCD7` | **JHash:** `0x9BE9C691` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL PED_HAS_USE_SCENARIO_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.PED_HAS_USE_SCENARIO_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### PLAY_ANIM_ON_RUNNING_SCENARIO

**Hash:** `0x748040460F8DF5DC` | **JHash:** `0x1984A5D1` | **Build:** `323`

**C++ 原型:**
```cpp
void PLAY_ANIM_ON_RUNNING_SCENARIO(Ped ped, const char* animDict, const char* animName)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PLAY_ANIM_ON_RUNNING_SCENARIO, ped, animDict, animName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `animName` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### DOES_SCENARIO_GROUP_EXIST

**Hash:** `0xF9034C136C9E00D3` | **JHash:** `0x5F072EB9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCENARIO_GROUP_EXIST(const char* scenarioGroup)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCENARIO_GROUP_EXIST, scenarioGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioGroup` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of scenario groups used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenarioGroupNames.json
Occurrences in the b617d scripts:

"ARMY_GUARD",
"ARMY_HELI",
"Cinema_Downtown",
"Cinema_Morningwood",
"Cinema_Textile",
"City_Banks",
"Countryside_Banks",
"DEALERSHIP",
"GRAPESEED_PLANES",
"KORTZ_SECURITY",
"LOST_BIKERS",
"LSA_Planes",
"LSA_Planes",
"MP_POLICE",
"Observatory_Bikers", 
"POLICE_POUND1",
"POLICE_POUND2",
"POLICE_POUND3",
"POLICE_POUND4",
"POLICE_POUND5"
"QUARRY",
"SANDY_PLANES",
"SCRAP_SECURITY",
"SEW_MACHINE",
"SOLOMON_GATE",
"Triathlon_1_Start", 
"Triathlon_2_Start", 
"Triathlon_3_Start"

Sometimes used with IS_SCENARIO_GROUP_ENABLED:
if (TASK::DOES_SCENARIO_GROUP_EXIST("Observatory_Bikers") && (!TASK::IS_SCENARIO_GROUP_ENABLED("Observatory_Bikers"))) {
else if (TASK::IS_SCENARIO_GROUP_ENABLED("BLIMP")) {


---

### IS_SCENARIO_GROUP_ENABLED

**Hash:** `0x367A09DED4E05B99` | **JHash:** `0x90991122` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCENARIO_GROUP_ENABLED(const char* scenarioGroup)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCENARIO_GROUP_ENABLED, scenarioGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioGroup` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of scenario groups used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenarioGroupNames.json
Occurrences in the b617d scripts: 

 "ARMY_GUARD",
 "ARMY_HELI",
 "BLIMP",
 "Cinema_Downtown",
 "Cinema_Morningwood",
 "Cinema_Textile",
 "City_Banks",
 "Countryside_Banks",
 "DEALERSHIP",
 "KORTZ_SECURITY",
 "LSA_Planes",
 "MP_POLICE",
 "Observatory_Bikers",
 "POLICE_POUND1",
 "POLICE_POUND2",
 "POLICE_POUND3",
 "POLICE_POUND4",
 "POLICE_POUND5",
 "Rampage1",
 "SANDY_PLANES",
 "SCRAP_SECURITY",
 "SEW_MACHINE",
 "SOLOMON_GATE"

Sometimes used with DOES_SCENARIO_GROUP_EXIST:
if (TASK::DOES_SCENARIO_GROUP_EXIST("Observatory_Bikers") &&   (!TASK::IS_SCENARIO_GROUP_ENABLED("Observatory_Bikers"))) {
else if (TASK::IS_SCENARIO_GROUP_ENABLED("BLIMP")) {

---

### SET_SCENARIO_GROUP_ENABLED

**Hash:** `0x02C8E5B49848664E` | **JHash:** `0x116997B1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENARIO_GROUP_ENABLED(const char* scenarioGroup, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENARIO_GROUP_ENABLED, scenarioGroup, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioGroup` | `const char*` | `string` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of scenario groups used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenarioGroupNames.json
Occurrences in the b617d scripts: https://pastebin.com/Tvg2PRHU

---

### RESET_SCENARIO_GROUPS_ENABLED

**Hash:** `0xDD902D0349AFAD3A` | **JHash:** `0xBF55025D` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_SCENARIO_GROUPS_ENABLED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_SCENARIO_GROUPS_ENABLED);
```

**返回值:** `void` → `void`

---

### SET_EXCLUSIVE_SCENARIO_GROUP

**Hash:** `0x535E97E1F7FC0C6A` | **JHash:** `0x59DB8F26` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EXCLUSIVE_SCENARIO_GROUP(const char* scenarioGroup)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EXCLUSIVE_SCENARIO_GROUP, scenarioGroup);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioGroup` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of scenario groups used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenarioGroupNames.json
Groups found in the scripts used with this native:

"AMMUNATION",
"QUARRY",
"Triathlon_1",
"Triathlon_2",
"Triathlon_3"

---

### RESET_EXCLUSIVE_SCENARIO_GROUP

**Hash:** `0x4202BBCB8684563D` | **JHash:** `0x17F9DFE8` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_EXCLUSIVE_SCENARIO_GROUP(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_EXCLUSIVE_SCENARIO_GROUP);
```

**返回值:** `void` → `void`

---

### IS_SCENARIO_TYPE_ENABLED

**Hash:** `0x3A815DB3EA088722` | **JHash:** `0xAE37E969` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SCENARIO_TYPE_ENABLED(const char* scenarioType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SCENARIO_TYPE_ENABLED, scenarioType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioType` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of scenario types used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenariosCompact.json
Occurrences in the b617d scripts:
"PROP_HUMAN_SEAT_CHAIR",
"WORLD_HUMAN_DRINKING",
"WORLD_HUMAN_HANG_OUT_STREET",
"WORLD_HUMAN_SMOKING",
"WORLD_MOUNTAIN_LION_WANDER",
"WORLD_HUMAN_DRINKING"

Sometimes used together with MISC::IS_STRING_NULL_OR_EMPTY in the scripts.

scenarioType could be the same as scenarioName, used in for example TASK::TASK_START_SCENARIO_AT_POSITION.


---

### SET_SCENARIO_TYPE_ENABLED

**Hash:** `0xEB47EC4E34FB7EE1` | **JHash:** `0xDB18E5DE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCENARIO_TYPE_ENABLED(const char* scenarioType, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCENARIO_TYPE_ENABLED, scenarioType, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scenarioType` | `const char*` | `string` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of scenario types used in scripts by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/scenariosCompact.json
seems to enable/disable specific scenario-types from happening in the game world.

Here are some scenario types from the scripts:
"WORLD_MOUNTAIN_LION_REST"                                             
"WORLD_MOUNTAIN_LION_WANDER"                                            
"DRIVE"                                                                  
"WORLD_VEHICLE_POLICE_BIKE"                                             
"WORLD_VEHICLE_POLICE_CAR"                                             
"WORLD_VEHICLE_POLICE_NEXT_TO_CAR"                                        
"WORLD_VEHICLE_DRIVE_SOLO"                                                 
"WORLD_VEHICLE_BIKER"                                                      
"WORLD_VEHICLE_DRIVE_PASSENGERS"                                           
"WORLD_VEHICLE_SALTON_DIRT_BIKE"                                           
"WORLD_VEHICLE_BICYCLE_MOUNTAIN"                                           
"PROP_HUMAN_SEAT_CHAIR"                                             
"WORLD_VEHICLE_ATTRACTOR"                                             
"WORLD_HUMAN_LEANING"                                                 
"WORLD_HUMAN_HANG_OUT_STREET"                                        
"WORLD_HUMAN_DRINKING"                                                
"WORLD_HUMAN_SMOKING"                                                
"WORLD_HUMAN_GUARD_STAND"                                            
"WORLD_HUMAN_CLIPBOARD"                                              
"WORLD_HUMAN_HIKER"                                                  
"WORLD_VEHICLE_EMPTY"                                                      
"WORLD_VEHICLE_BIKE_OFF_ROAD_RACE"                                      
"WORLD_HUMAN_PAPARAZZI"                                               
"WORLD_VEHICLE_PARK_PERPENDICULAR_NOSE_IN"                            
"WORLD_VEHICLE_PARK_PARALLEL"                                              
"WORLD_VEHICLE_CONSTRUCTION_SOLO"                               
"WORLD_VEHICLE_CONSTRUCTION_PASSENGERS"                                                                    
"WORLD_VEHICLE_TRUCK_LOGS"

scenarioType could be the same as scenarioName, used in for example TASK::TASK_START_SCENARIO_AT_POSITION.

---

### RESET_SCENARIO_TYPES_ENABLED

**Hash:** `0x0D40EE2A7F2B2D6D` | **JHash:** `0xF58FDEB4` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_SCENARIO_TYPES_ENABLED(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_SCENARIO_TYPES_ENABLED);
```

**返回值:** `void` → `void`

---

### IS_PED_ACTIVE_IN_SCENARIO

**Hash:** `0xAA135F9482C82CC3` | **JHash:** `0x05038F1A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ACTIVE_IN_SCENARIO(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ACTIVE_IN_SCENARIO, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_PLAYING_BASE_CLIP_IN_SCENARIO

**Hash:** `0x621C6E4729388E41` | **JHash:** `0xA0DA6B1D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_PLAYING_BASE_CLIP_IN_SCENARIO(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_PLAYING_BASE_CLIP_IN_SCENARIO, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Used only once (am_mp_property_int)

ped was PLAYER_PED_ID()

Related to CTaskAmbientClips.

---

### SET_PED_CAN_PLAY_AMBIENT_IDLES

**Hash:** `0x8FD89A6240813FD0` | **JHash:** `0xB5AD044E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CAN_PLAY_AMBIENT_IDLES(Ped ped, BOOL blockIdleClips, BOOL removeIdleClipIfPlaying)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CAN_PLAY_AMBIENT_IDLES, ped, blockIdleClips, removeIdleClipIfPlaying);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `blockIdleClips` | `BOOL` | `bool` | |
| `removeIdleClipIfPlaying` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Appears only in fm_mission_controller and used only 3 times.

ped was always PLAYER_PED_ID()
p1 was always true
p2 was always true

---

### TASK_COMBAT_HATED_TARGETS_IN_AREA

**Hash:** `0x4CF5F55DAC3280A0` | **JHash:** `0xDF099E18` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COMBAT_HATED_TARGETS_IN_AREA(Ped ped, float x, float y, float z, float radius, int combatFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COMBAT_HATED_TARGETS_IN_AREA, ped, x, y, z, radius, combatFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `combatFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Despite its name, it only attacks ONE hated target. The one closest to the specified position.

---

### TASK_COMBAT_HATED_TARGETS_AROUND_PED

**Hash:** `0x7BF835BB9E2698C8` | **JHash:** `0x2E7064E4` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COMBAT_HATED_TARGETS_AROUND_PED(Ped ped, float radius, int combatFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COMBAT_HATED_TARGETS_AROUND_PED, ped, radius, combatFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `radius` | `float` | `float` | |
| `combatFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Despite its name, it only attacks ONE hated target. The one closest hated target.

p2 seems to be always 0

---

### TASK_COMBAT_HATED_TARGETS_AROUND_PED_TIMED

**Hash:** `0x2BBA30B854534A0C` | **JHash:** `0xF127AD6A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_COMBAT_HATED_TARGETS_AROUND_PED_TIMED(Ped ped, float radius, int time, int combatFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_COMBAT_HATED_TARGETS_AROUND_PED_TIMED, ped, radius, time, combatFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `radius` | `float` | `float` | |
| `time` | `int` | `int` | |
| `combatFlags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_THROW_PROJECTILE

**Hash:** `0x7285951DBF6B5A51` | **JHash:** `0xF65C20A7` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_THROW_PROJECTILE(Ped ped, float x, float y, float z, int ignoreCollisionEntityIndex, BOOL createInvincibleProjectile)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_THROW_PROJECTILE, ped, x, y, z, ignoreCollisionEntityIndex, createInvincibleProjectile);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `ignoreCollisionEntityIndex` | `int` | `int` | |
| `createInvincibleProjectile` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** In every case of this native, I've only seen the first parameter passed as 0, although I believe it's a Ped after seeing tasks around it using 0. That's because it's used in a Sequence Task.

The last 3 parameters are definitely coordinates after seeing them passed in other scripts, and even being used straight from the player's coordinates.
---
It seems that - in the decompiled scripts - this native was used on a ped who was in a vehicle to throw a projectile out the window at the player. This is something any ped will naturally do if they have a throwable and they are doing driveby-combat (although not very accurately).
It is possible, however, that this is how SWAT throws smoke grenades at the player when in cover.
----------------------------------------------------
The first comment is right it definately is the ped as if you look in script finale_heist2b.c line 59628 in Xbox Scripts atleast you will see task_throw_projectile and the first param is Local_559[2 <14>] if you look above it a little bit line 59622 give_weapon_to_ped uses the same exact param Local_559[2 <14>] and we all know the first param of that native is ped. So it guaranteed has to be ped. 0 just may mean to use your ped by default for some reason.

---

### TASK_SWAP_WEAPON

**Hash:** `0xA21C51255B205245` | **JHash:** `0xDAF4F8FC` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SWAP_WEAPON(Ped ped, BOOL drawWeapon)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SWAP_WEAPON, ped, drawWeapon);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `drawWeapon` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### TASK_RELOAD_WEAPON

**Hash:** `0x62D2916F56B9CD2D` | **JHash:** `0xCA6E91FD` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_RELOAD_WEAPON(Ped ped, BOOL drawWeapon)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_RELOAD_WEAPON, ped, drawWeapon);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `drawWeapon` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** The 2nd param (drawWeapon) is not implemented.

-----------------------------------------------------------------------

The only occurrence I found in a R* script ("assassin_construction.ysc.c4"):

            if (((v_3 < v_4) && (TASK::GET_SCRIPT_TASK_STATUS(PLAYER::PLAYER_PED_ID(), 0x6a67a5cc) != 1)) && (v_5 > v_3)) {
                TASK::TASK_RELOAD_WEAPON(PLAYER::PLAYER_PED_ID(), 1);
            }

---

### IS_PED_GETTING_UP

**Hash:** `0x2A74E1D5F2F00EEC` | **JHash:** `0x320813E6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_GETTING_UP(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_GETTING_UP, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### TASK_WRITHE

**Hash:** `0xCDDC2B77CE54AC6E` | **JHash:** `0x0FDC54FC` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_WRITHE(Ped ped, Ped target, int minFireLoops, int startState, BOOL forceShootOnGround, int shootFromGroundTimer)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_WRITHE, ped, target, minFireLoops, startState, forceShootOnGround, shootFromGroundTimer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `minFireLoops` | `int` | `int` | |
| `startState` | `int` | `int` | |
| `forceShootOnGround` | `BOOL` | `bool` | |
| `shootFromGroundTimer` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** EX: Function.Call(Ped1, Ped2, Time, 0);

The last parameter is always 0 for some reason I do not know. The first parameter is the pedestrian who will writhe to the pedestrian in the other parameter. The third paremeter is how long until the Writhe task ends. When the task ends, the ped will die. If set to -1, he will not die automatically, and the task will continue until something causes it to end. This can be being touched by an entity, being shot, explosion, going into ragdoll, having task cleared. Anything that ends the current task will kill the ped at this point.



Third parameter does not appear to be time. The last parameter is not implemented (It's not used, regardless of value).

---

### IS_PED_IN_WRITHE

**Hash:** `0xDEB6D52126E7D640` | **JHash:** `0x09E61921` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_IN_WRITHE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_IN_WRITHE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This native checks if a ped is on the ground, in pain from a (gunshot) wound.
Returns `true` if the ped is in writhe, `false` otherwise.

---

### OPEN_PATROL_ROUTE

**Hash:** `0xA36BFB5EE89F3D82` | **JHash:** `0xF33F83CA` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_PATROL_ROUTE(const char* patrolRoute)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_PATROL_ROUTE, patrolRoute);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `patrolRoute` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:**  patrolRoutes found in the b617d scripts:
 "miss_Ass0",
 "miss_Ass1",
 "miss_Ass2",
 "miss_Ass3",
 "miss_Ass4",
 "miss_Ass5",
 "miss_Ass6",
 "MISS_PATROL_6",
 "MISS_PATROL_7",
 "MISS_PATROL_8",
 "MISS_PATROL_9",
 "miss_Tower_01",
 "miss_Tower_02",
 "miss_Tower_03",
 "miss_Tower_04",
 "miss_Tower_05",
 "miss_Tower_06",
 "miss_Tower_07",
 "miss_Tower_08",
 "miss_Tower_10"

---

### CLOSE_PATROL_ROUTE

**Hash:** `0xB043ECA801B8CBC1` | **JHash:** `0x67305E59` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_PATROL_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_PATROL_ROUTE);
```

**返回值:** `void` → `void`

---

### ADD_PATROL_ROUTE_NODE

**Hash:** `0x8EDF950167586B7C` | **JHash:** `0x21B48F10` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PATROL_ROUTE_NODE(int nodeId, const char* nodeType, float posX, float posY, float posZ, float headingX, float headingY, float headingZ, int duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PATROL_ROUTE_NODE, nodeId, nodeType, posX, posY, posZ, headingX, headingY, headingZ, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeId` | `int` | `int` | |
| `nodeType` | `const char*` | `string` | |
| `posX` | `float` | `float` | |
| `posY` | `float` | `float` | |
| `posZ` | `float` | `float` | |
| `headingX` | `float` | `float` | |
| `headingY` | `float` | `float` | |
| `headingZ` | `float` | `float` | |
| `duration` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example: 
TASK::ADD_PATROL_ROUTE_NODE(2, "WORLD_HUMAN_GUARD_STAND", -193.4915, -2378.864990234375, 10.9719, -193.4915, -2378.864990234375, 10.9719, 3000);

p0 is between 0 and 4 in the scripts.

p1 is "WORLD_HUMAN_GUARD_STAND" or "StandGuard".

p2, p3 and p4 is only one parameter sometimes in the scripts. Most likely a Vector3 hence p2, p3 and p4 are coordinates. 
Examples: 
TASK::ADD_PATROL_ROUTE_NODE(1, "WORLD_HUMAN_GUARD_STAND", l_739[7/*3*/], 0.0, 0.0, 0.0, 0);

TASK::ADD_PATROL_ROUTE_NODE(1, "WORLD_HUMAN_GUARD_STAND", l_B0[17/*44*/]._f3, l_B0[17/*44*/]._f3, 2000);

p5, p6 and p7 are for example set to: 1599.0406494140625, 2713.392578125, 44.4309.

p8 is an int, often random set to for example: MISC::GET_RANDOM_INT_IN_RANGE(5000, 10000).

---

### ADD_PATROL_ROUTE_LINK

**Hash:** `0x23083260DEC3A551` | **JHash:** `0xD8761BB3` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_PATROL_ROUTE_LINK(int nodeId1, int nodeId2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PATROL_ROUTE_LINK, nodeId1, nodeId2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `nodeId1` | `int` | `int` | |
| `nodeId2` | `int` | `int` | |

**返回值:** `void` → `void`

---

### CREATE_PATROL_ROUTE

**Hash:** `0xAF8A443CCC8018DC` | **JHash:** `0x0A6C7864` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_PATROL_ROUTE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_PATROL_ROUTE);
```

**返回值:** `void` → `void`

---

### DELETE_PATROL_ROUTE

**Hash:** `0x7767DD9D65E91319` | **JHash:** `0x2A4E6706` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_PATROL_ROUTE(const char* patrolRoute)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_PATROL_ROUTE, patrolRoute);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `patrolRoute` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** From the b617d scripts:

TASK::DELETE_PATROL_ROUTE("miss_merc0");
TASK::DELETE_PATROL_ROUTE("miss_merc1");
TASK::DELETE_PATROL_ROUTE("miss_merc2");
TASK::DELETE_PATROL_ROUTE("miss_dock");

---

### GET_PATROL_TASK_INFO

**Hash:** `0x52F734CEBE20DFBA` | **Build:** `2545`

**C++ 原型:**
```cpp
BOOL GET_PATROL_TASK_INFO(Ped ped, int* timeLeftAtNode, int* nodeId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PATROL_TASK_INFO, ped, timeLeftAtNode, nodeId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `timeLeftAtNode` | `int*` | `int*` | |
| `nodeId` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### TASK_PATROL

**Hash:** `0xBDA5DF49D080FE4E` | **JHash:** `0xB92E5AF6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_PATROL(Ped ped, const char* patrolRouteName, int alertState, BOOL canChatToPeds, BOOL useHeadLookAt)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_PATROL, ped, patrolRouteName, alertState, canChatToPeds, useHeadLookAt);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `patrolRouteName` | `const char*` | `string` | |
| `alertState` | `int` | `int` | |
| `canChatToPeds` | `BOOL` | `bool` | |
| `useHeadLookAt` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** After looking at some scripts the second parameter seems to be an id of some kind. Here are some I found from some R* scripts:

"miss_Tower_01" (this went from 01 - 10)
"miss_Ass0" (0, 4, 6, 3)
"MISS_PATROL_8"

I think they're patrol routes, but I'm not sure. And I believe the 3rd parameter is a BOOL, but I can't confirm other than only seeing 0 and 1 being passed.


As far as I can see the patrol routes names such as "miss_Ass0" have been defined earlier in the scripts. This leads me to believe we can defined our own new patrol routes by following the same approach. 
From the scripts

    TASK::OPEN_PATROL_ROUTE("miss_Ass0");
    TASK::ADD_PATROL_ROUTE_NODE(0, "WORLD_HUMAN_GUARD_STAND", l_738[0/*3*/], -139.4076690673828, -993.4732055664062, 26.2754, MISC::GET_RANDOM_INT_IN_RANGE(5000, 10000));
    TASK::ADD_PATROL_ROUTE_NODE(1, "WORLD_HUMAN_GUARD_STAND", l_738[1/*3*/], -116.1391830444336, -987.4984130859375, 26.38541030883789, MISC::GET_RANDOM_INT_IN_RANGE(5000, 10000));
    TASK::ADD_PATROL_ROUTE_NODE(2, "WORLD_HUMAN_GUARD_STAND", l_738[2/*3*/], -128.46847534179688, -979.0340576171875, 26.2754, MISC::GET_RANDOM_INT_IN_RANGE(5000, 10000));
    TASK::ADD_PATROL_ROUTE_LINK(0, 1);
    TASK::ADD_PATROL_ROUTE_LINK(1, 2);
    TASK::ADD_PATROL_ROUTE_LINK(2, 0);
    TASK::CLOSE_PATROL_ROUTE();
    TASK::CREATE_PATROL_ROUTE();



---

### TASK_STAY_IN_COVER

**Hash:** `0xE5DA8615A6180789` | **JHash:** `0xA27A9413` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_STAY_IN_COVER(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_STAY_IN_COVER, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the ped run to take cover

---

### ADD_VEHICLE_SUBTASK_ATTACK_COORD

**Hash:** `0x5CF0D8F9BBA0DD75` | **JHash:** `0x50779A2C` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_VEHICLE_SUBTASK_ATTACK_COORD(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VEHICLE_SUBTASK_ATTACK_COORD, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** x, y, z: offset in world coords from some entity.

---

### ADD_VEHICLE_SUBTASK_ATTACK_PED

**Hash:** `0x85F462BADC7DA47F` | **JHash:** `0x80461113` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_VEHICLE_SUBTASK_ATTACK_PED(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VEHICLE_SUBTASK_ATTACK_PED, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_SHOOT_AT_PED

**Hash:** `0x10AB107B887214D8` | **JHash:** `0x59677BA0` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_SHOOT_AT_PED(Ped ped, Ped target, float fireTolerance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_SHOOT_AT_PED, ped, target, fireTolerance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `fireTolerance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_AIM_AT_PED

**Hash:** `0xE41885592B08B097` | **JHash:** `0x920AE6DB` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_AIM_AT_PED(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_AIM_AT_PED, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_SHOOT_AT_COORD

**Hash:** `0x5190796ED39C9B6D` | **JHash:** `0xA7AAA4D6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_SHOOT_AT_COORD(Ped ped, float x, float y, float z, float fireTolerance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_SHOOT_AT_COORD, ped, x, y, z, fireTolerance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `fireTolerance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_AIM_AT_COORD

**Hash:** `0x447C1E9EF844BC0F` | **JHash:** `0x010F47CE` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_AIM_AT_COORD(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_AIM_AT_COORD, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_GOTO_NAVMESH

**Hash:** `0x195AEEB13CEFE2EE` | **JHash:** `0x55CF3BCD` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_GOTO_NAVMESH(Ped ped, Vehicle vehicle, float x, float y, float z, float speed, int behaviorFlag, float stoppingRange)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_GOTO_NAVMESH, ped, vehicle, x, y, z, speed, behaviorFlag, stoppingRange);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `behaviorFlag` | `int` | `int` | |
| `stoppingRange` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Differs from TASK_VEHICLE_DRIVE_TO_COORDS in that it will pick the shortest possible road route without taking one-way streets and other "road laws" into consideration.

WARNING:
A behaviorFlag value of 0 will result in a clunky, stupid driver!

Recommended settings:
speed = 30.0f,
behaviorFlag = 156, 
stoppingRange = 5.0f;

If you simply want to have your driver move to a fixed location, call it only once, or, when necessary in the event of interruption. 

If using this to continually follow a Ped who is on foot:  You will need to run this in a tick loop.  Call it in with the Ped's updated coordinates every 20 ticks or so and you will have one hell of a smart, fast-reacting NPC driver -- provided he doesn't get stuck.  If your update frequency is too fast, the Ped may not have enough time to figure his way out of being stuck, and thus, remain stuck.  One way around this would be to implement an "anti-stuck" mechanism, which allows the driver to realize he's stuck, temporarily pause the tick, unstuck, then resume the tick.

EDIT:  This is being discussed in more detail at http://gtaforums.com/topic/818504-any-idea-on-how-to-make-peds-clever-and-insanely-fast-c/  

---

### TASK_GO_TO_COORD_WHILE_AIMING_AT_COORD

**Hash:** `0x11315AB3385B8AC0` | **JHash:** `0x1552DC91` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_WHILE_AIMING_AT_COORD(Ped ped, float x, float y, float z, float aimAtX, float aimAtY, float aimAtZ, float moveBlendRatio, BOOL shoot, float targetRadius, float slowDistance, BOOL useNavMesh, int navFlags, BOOL instantBlendToAim, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_WHILE_AIMING_AT_COORD, ped, x, y, z, aimAtX, aimAtY, aimAtZ, moveBlendRatio, shoot, targetRadius, slowDistance, useNavMesh, navFlags, instantBlendToAim, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `aimAtX` | `float` | `float` | |
| `aimAtY` | `float` | `float` | |
| `aimAtZ` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `shoot` | `BOOL` | `bool` | |
| `targetRadius` | `float` | `float` | |
| `slowDistance` | `float` | `float` | |
| `useNavMesh` | `BOOL` | `bool` | |
| `navFlags` | `int` | `int` | |
| `instantBlendToAim` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** movement_speed: mostly 2f, but also 1/1.2f, etc.
p8: always false
p9: 2f
p10: 0.5f
p11: true
p12: 0 / 512 / 513, etc.
p13: 0
firing_pattern: ${firing_pattern_full_auto}, 0xC6EE6B4C

---

### TASK_GO_TO_COORD_WHILE_AIMING_AT_ENTITY

**Hash:** `0xB2A16444EAD9AE47` | **JHash:** `0x9BD52ABD` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_WHILE_AIMING_AT_ENTITY(Ped ped, float x, float y, float z, Entity aimAtID, float moveBlendRatio, BOOL shoot, float targetRadius, float slowDistance, BOOL useNavMesh, int navFlags, BOOL instantBlendToAim, Hash firingPattern, int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_WHILE_AIMING_AT_ENTITY, ped, x, y, z, aimAtID, moveBlendRatio, shoot, targetRadius, slowDistance, useNavMesh, navFlags, instantBlendToAim, firingPattern, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `aimAtID` | `Entity` | `int` | |
| `moveBlendRatio` | `float` | `float` | |
| `shoot` | `BOOL` | `bool` | |
| `targetRadius` | `float` | `float` | |
| `slowDistance` | `float` | `float` | |
| `useNavMesh` | `BOOL` | `bool` | |
| `navFlags` | `int` | `int` | |
| `instantBlendToAim` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TASK_GO_TO_COORD_AND_AIM_AT_HATED_ENTITIES_NEAR_COORD

**Hash:** `0xA55547801EB331FC` | **JHash:** `0x3F91358E` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_COORD_AND_AIM_AT_HATED_ENTITIES_NEAR_COORD(Ped pedHandle, float goToLocationX, float goToLocationY, float goToLocationZ, float focusLocationX, float focusLocationY, float focusLocationZ, float speed, BOOL shootAtEnemies, float distanceToStopAt, float noRoadsDistance, BOOL useNavMesh, int navFlags, int taskFlags, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_COORD_AND_AIM_AT_HATED_ENTITIES_NEAR_COORD, pedHandle, goToLocationX, goToLocationY, goToLocationZ, focusLocationX, focusLocationY, focusLocationZ, speed, shootAtEnemies, distanceToStopAt, noRoadsDistance, useNavMesh, navFlags, taskFlags, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `pedHandle` | `Ped` | `int` | |
| `goToLocationX` | `float` | `float` | |
| `goToLocationY` | `float` | `float` | |
| `goToLocationZ` | `float` | `float` | |
| `focusLocationX` | `float` | `float` | |
| `focusLocationY` | `float` | `float` | |
| `focusLocationZ` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `shootAtEnemies` | `BOOL` | `bool` | |
| `distanceToStopAt` | `float` | `float` | |
| `noRoadsDistance` | `float` | `float` | |
| `useNavMesh` | `BOOL` | `bool` | |
| `navFlags` | `int` | `int` | |
| `taskFlags` | `int` | `int` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** The ped will walk or run towards goToLocation, aiming towards goToLocation or focusLocation (depending on the aimingFlag) and shooting if shootAtEnemies = true to any enemy in his path.

If the ped is closer than noRoadsDistance, the ped will ignore pathing/navmesh and go towards goToLocation directly. This could cause the ped to get stuck behind tall walls if the goToLocation is on the other side. To avoid this, use 0.0f and the ped will always use pathing/navmesh to reach his destination.

If the speed is set to 0.0f, the ped will just stand there while aiming, if set to 1.0f he will walk while aiming, 2.0f will run while aiming.

The ped will stop aiming when he is closer than distanceToStopAt to goToLocation.

I still can't figure out what unkTrue is used for. I don't notice any difference if I set it to false but in the decompiled scripts is always true.

I think that unkFlag, like the driving styles, could be a flag that "work as a list of 32 bits converted to a decimal integer. Each bit acts as a flag, and enables or disables a function". What leads me to this conclusion is the fact that in the decompiled scripts, unkFlag takes values like: 0, 1, 5 (101 in binary) and 4097 (4096 + 1 or 1000000000001 in binary). For now, I don't know what behavior enable or disable this possible flag so I leave it at 0.

Note: After some testing, using unkFlag = 16 (0x10) enables the use of sidewalks while moving towards goToLocation.

The aimingFlag takes 2 values: 0 to aim at the focusLocation, 1 to aim at where the ped is heading (goToLocation).

Example:

enum AimFlag
{
   AimAtFocusLocation,
   AimAtGoToLocation
};

Vector3 goToLocation1 = { 996.2867f, 0, -2143.044f, 0, 28.4763f, 0 }; // remember the padding.

Vector3 goToLocation2 = { 990.2867f, 0, -2140.044f, 0, 28.4763f, 0 }; // remember the padding.

Vector3 focusLocation = { 994.3478f, 0, -2136.118f, 0, 29.2463f, 0 }; // the coord z should be a little higher, around +1.0f to avoid aiming at the ground

// 1st example
TASK::TASK_GO_TO_COORD_AND_AIM_AT_HATED_ENTITIES_NEAR_COORD(pedHandle, goToLocation1.x, goToLocation1.y, goToLocation1.z, focusLocation.x, focusLocation.y, focusLocation.z, 2.0f /*run*/, true /*shoot*/, 3.0f /*stop at*/, 0.0f /*noRoadsDistance*/, true /*always true*/, 0 /*possible flag*/, AimFlag::AimAtGoToLocation, -957453492 /*FullAuto pattern*/);

// 2nd example
TASK::TASK_GO_TO_COORD_AND_AIM_AT_HATED_ENTITIES_NEAR_COORD(pedHandle, goToLocation2.x, goToLocation2.y, goToLocation2.z, focusLocation.x, focusLocation.y, focusLocation.z, 1.0f /*walk*/, false /*don't shoot*/, 3.0f /*stop at*/, 0.0f /*noRoadsDistance*/, true /*always true*/, 0 /*possible flag*/, AimFlag::AimAtFocusLocation, -957453492 /*FullAuto pattern*/);


1st example: The ped (pedhandle) will run towards goToLocation1. While running and aiming towards goToLocation1, the ped will shoot on sight to any enemy in his path, using "FullAuto" firing pattern. The ped will stop once he is closer than distanceToStopAt to goToLocation1.

2nd example: The ped will walk towards goToLocation2. This time, while walking towards goToLocation2 and aiming at focusLocation, the ped will point his weapon on sight to any enemy in his path without shooting. The ped will stop once he is closer than distanceToStopAt to goToLocation2.

---

### TASK_GO_TO_ENTITY_WHILE_AIMING_AT_COORD

**Hash:** `0x04701832B739DCE5` | **JHash:** `0xD896CD82` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_ENTITY_WHILE_AIMING_AT_COORD(Ped ped, Entity entity, float aimX, float aimY, float aimZ, float moveBlendRatio, BOOL shoot, float targetRadius, float slowDistance, BOOL useNavMesh, BOOL instantBlendToAim, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_ENTITY_WHILE_AIMING_AT_COORD, ped, entity, aimX, aimY, aimZ, moveBlendRatio, shoot, targetRadius, slowDistance, useNavMesh, instantBlendToAim, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `aimX` | `float` | `float` | |
| `aimY` | `float` | `float` | |
| `aimZ` | `float` | `float` | |
| `moveBlendRatio` | `float` | `float` | |
| `shoot` | `BOOL` | `bool` | |
| `targetRadius` | `float` | `float` | |
| `slowDistance` | `float` | `float` | |
| `useNavMesh` | `BOOL` | `bool` | |
| `instantBlendToAim` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### TASK_GO_TO_ENTITY_WHILE_AIMING_AT_ENTITY

**Hash:** `0x97465886D35210E9` | **JHash:** `0x68E36B7A` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_GO_TO_ENTITY_WHILE_AIMING_AT_ENTITY(Ped ped, Entity entityToWalkTo, Entity entityToAimAt, float speed, BOOL shootatEntity, float targetRadius, float slowDistance, BOOL useNavMesh, BOOL instantBlendToAim, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_GO_TO_ENTITY_WHILE_AIMING_AT_ENTITY, ped, entityToWalkTo, entityToAimAt, speed, shootatEntity, targetRadius, slowDistance, useNavMesh, instantBlendToAim, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entityToWalkTo` | `Entity` | `int` | |
| `entityToAimAt` | `Entity` | `int` | |
| `speed` | `float` | `float` | |
| `shootatEntity` | `BOOL` | `bool` | |
| `targetRadius` | `float` | `float` | |
| `slowDistance` | `float` | `float` | |
| `useNavMesh` | `BOOL` | `bool` | |
| `instantBlendToAim` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** shootatEntity:
If true, peds will shoot at Entity till it is dead.
If false, peds will just walk till they reach the entity and will cease shooting.

---

### SET_HIGH_FALL_TASK

**Hash:** `0x8C825BDC7741D37C` | **JHash:** `0xBBB26172` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HIGH_FALL_TASK(Ped ped, int minTime, int maxTime, int entryType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HIGH_FALL_TASK, ped, minTime, maxTime, entryType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `minTime` | `int` | `int` | |
| `maxTime` | `int` | `int` | |
| `entryType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Makes the ped ragdoll like when falling from a great height

---

### REQUEST_WAYPOINT_RECORDING

**Hash:** `0x9EEFB62EB27B5792` | **JHash:** `0xAFABFB5D` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_WAYPOINT_RECORDING(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_WAYPOINT_RECORDING, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json
For a full list of the points, see here: goo.gl/wIH0vn

Max number of loaded recordings is 32.

---

### GET_IS_WAYPOINT_RECORDING_LOADED

**Hash:** `0xCB4E8BE8A0063C5D` | **JHash:** `0x87125F5D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_WAYPOINT_RECORDING_LOADED(const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_WAYPOINT_RECORDING_LOADED, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json

---

### REMOVE_WAYPOINT_RECORDING

**Hash:** `0xFF1B8B4AA1C25DC8` | **JHash:** `0x624530B0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WAYPOINT_RECORDING(const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WAYPOINT_RECORDING, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json

---

### WAYPOINT_RECORDING_GET_NUM_POINTS

**Hash:** `0x5343532C01A07234` | **JHash:** `0xF5F9B71E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAYPOINT_RECORDING_GET_NUM_POINTS(const char* name, int* points)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAYPOINT_RECORDING_GET_NUM_POINTS, name, points);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `points` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json
For a full list of the points, see here: goo.gl/wIH0vn

---

### WAYPOINT_RECORDING_GET_COORD

**Hash:** `0x2FB897405C90B361` | **JHash:** `0x19266913` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAYPOINT_RECORDING_GET_COORD(const char* name, int point, Vector3* coord)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAYPOINT_RECORDING_GET_COORD, name, point, coord);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `point` | `int` | `int` | |
| `coord` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json
For a full list of the points, see here: goo.gl/wIH0vn

---

### WAYPOINT_RECORDING_GET_SPEED_AT_POINT

**Hash:** `0x005622AEBC33ACA9` | **JHash:** `0xC765633A` | **Build:** `323`

**C++ 原型:**
```cpp
float WAYPOINT_RECORDING_GET_SPEED_AT_POINT(const char* name, int point)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.WAYPOINT_RECORDING_GET_SPEED_AT_POINT, name, point);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `point` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json

---

### WAYPOINT_RECORDING_GET_CLOSEST_WAYPOINT

**Hash:** `0xB629A298081F876F` | **JHash:** `0xC4CD35AF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAYPOINT_RECORDING_GET_CLOSEST_WAYPOINT(const char* name, float x, float y, float z, int* point)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAYPOINT_RECORDING_GET_CLOSEST_WAYPOINT, name, x, y, z, point);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `point` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json
For a full list of the points, see here: goo.gl/wIH0vn

---

### TASK_FOLLOW_WAYPOINT_RECORDING

**Hash:** `0x0759591819534F7B` | **JHash:** `0xADF9904D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FOLLOW_WAYPOINT_RECORDING(Ped ped, const char* name, int p2, int p3, int p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FOLLOW_WAYPOINT_RECORDING, ped, name, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `const char*` | `string` | |
| `p2` | `int` | `int` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_PED

**Hash:** `0xE03B3F2D3DC59B64` | **JHash:** `0x85B7725F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_PED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PED_WAYPOINT_PROGRESS

**Hash:** `0x2720AAA75001E094` | **JHash:** `0x3595B104` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_WAYPOINT_PROGRESS(Ped ped)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_WAYPOINT_PROGRESS, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `int` → `int`

---

### SET_PED_WAYPOINT_PROGRESS

**Hash:** `0x686ECCD99D4E61BB` | **Build:** `3570`

**C++ 原型:**
```cpp
void SET_PED_WAYPOINT_PROGRESS(Ped ped, int progress)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WAYPOINT_PROGRESS, ped, progress);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `progress` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_PED_WAYPOINT_DISTANCE

**Hash:** `0xE6A877C64CAF1BC5` | **JHash:** `0x084B35B0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_PED_WAYPOINT_DISTANCE(Any p0)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_PED_WAYPOINT_DISTANCE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `float` → `float`

---

### SET_PED_WAYPOINT_ROUTE_OFFSET

**Hash:** `0xED98E10B0AFCE4B4` | **JHash:** `0xF867F747` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_PED_WAYPOINT_ROUTE_OFFSET(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PED_WAYPOINT_ROUTE_OFFSET, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### GET_WAYPOINT_DISTANCE_ALONG_ROUTE

**Hash:** `0xA5B769058763E497` | **JHash:** `0xE8422AC4` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_WAYPOINT_DISTANCE_ALONG_ROUTE(const char* name, int point)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_WAYPOINT_DISTANCE_ALONG_ROUTE, name, point);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `point` | `int` | `int` | |

**返回值:** `float` → `float`

---

### WAYPOINT_PLAYBACK_GET_IS_PAUSED

**Hash:** `0x701375A7D43F01CB` | **JHash:** `0xA6BB5717` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL WAYPOINT_PLAYBACK_GET_IS_PAUSED(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.WAYPOINT_PLAYBACK_GET_IS_PAUSED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### WAYPOINT_PLAYBACK_PAUSE

**Hash:** `0x0F342546AA06FED5` | **JHash:** `0xFE39ECF8` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_PAUSE(Any p0, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_PAUSE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_RESUME

**Hash:** `0x244F70C84C547D2D` | **JHash:** `0x50F392EF` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_RESUME(Any p0, BOOL p1, Any p2, Any p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_RESUME, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_OVERRIDE_SPEED

**Hash:** `0x7D7D2B47FA788E85` | **JHash:** `0x23E6BA96` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_OVERRIDE_SPEED(Any p0, float p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_OVERRIDE_SPEED, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED

**Hash:** `0x6599D834B12D0800` | **JHash:** `0x1BBB2CAC` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### USE_WAYPOINT_RECORDING_AS_ASSISTED_MOVEMENT_ROUTE

**Hash:** `0x5A353B8E6B1095B5` | **JHash:** `0x4DFD5FEC` | **Build:** `323`

**C++ 原型:**
```cpp
void USE_WAYPOINT_RECORDING_AS_ASSISTED_MOVEMENT_ROUTE(const char* name, BOOL p1, float p2, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.USE_WAYPOINT_RECORDING_AS_ASSISTED_MOVEMENT_ROUTE, name, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_START_AIMING_AT_PED

**Hash:** `0x20E330937C399D29` | **JHash:** `0x75E60CF6` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_START_AIMING_AT_PED(Ped ped, Ped target, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_START_AIMING_AT_PED, ped, target, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_START_AIMING_AT_COORD

**Hash:** `0x8968400D900ED8B3` | **JHash:** `0xF120A34E` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_START_AIMING_AT_COORD(Ped ped, float x, float y, float z, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_START_AIMING_AT_COORD, ped, x, y, z, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_START_SHOOTING_AT_PED

**Hash:** `0xE70BA7B90F8390DC` | **JHash:** `0xBD5F0EB8` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_START_SHOOTING_AT_PED(Ped ped, Ped ped2, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_START_SHOOTING_AT_PED, ped, ped2, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ped2` | `Ped` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_START_SHOOTING_AT_COORD

**Hash:** `0x057A25CFCC9DB671` | **JHash:** `0xCDDB44D5` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_START_SHOOTING_AT_COORD(Ped ped, float x, float y, float z, BOOL p4, Hash firingPattern)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_START_SHOOTING_AT_COORD, ped, x, y, z, p4, firingPattern);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `BOOL` | `bool` | |
| `firingPattern` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### WAYPOINT_PLAYBACK_STOP_AIMING_OR_SHOOTING

**Hash:** `0x47EFA040EBB8E2EA` | **JHash:** `0x6D7CF40C` | **Build:** `323`

**C++ 原型:**
```cpp
void WAYPOINT_PLAYBACK_STOP_AIMING_OR_SHOOTING(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.WAYPOINT_PLAYBACK_STOP_AIMING_OR_SHOOTING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### ASSISTED_MOVEMENT_REQUEST_ROUTE

**Hash:** `0x817268968605947A` | **JHash:** `0x48262EDA` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_REQUEST_ROUTE(const char* route)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_REQUEST_ROUTE, route);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `route` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Routes: "1_FIBStairs", "2_FIBStairs", "3_FIBStairs", "4_FIBStairs", "5_FIBStairs", "5_TowardsFire", "6a_FIBStairs", "7_FIBStairs", "8_FIBStairs", "Aprtmnt_1", "AssAfterLift", "ATM_1", "coroner2", "coroner_stairs", "f5_jimmy1", "fame1", "family5b", "family5c", "Family5d", "family5d", "FIB_Glass1", "FIB_Glass2", "FIB_Glass3", "finaBroute1A", "finalb1st", "finalB1sta", "finalbround", "finalbroute2", "Hairdresser1", "jan_foyet_ft_door", "Jo_3", "Lemar1", "Lemar2", "mansion_1", "Mansion_1", "pols_1", "pols_2", "pols_3", "pols_4", "pols_5", "pols_6", "pols_7", "pols_8", "Pro_S1", "Pro_S1a", "Pro_S2", "Towards_case", "trev_steps", "tunrs1", "tunrs2", "tunrs3", "Wave01457s"

---

### ASSISTED_MOVEMENT_REMOVE_ROUTE

**Hash:** `0x3548536485DD792B` | **JHash:** `0xB3CEC06F` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_REMOVE_ROUTE(const char* route)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_REMOVE_ROUTE, route);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `route` | `const char*` | `string` | |

**返回值:** `void` → `void`

---

### ASSISTED_MOVEMENT_IS_ROUTE_LOADED

**Hash:** `0x60F9A4393A21F741` | **JHash:** `0x79B067AF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ASSISTED_MOVEMENT_IS_ROUTE_LOADED(const char* route)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ASSISTED_MOVEMENT_IS_ROUTE_LOADED, route);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `route` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### ASSISTED_MOVEMENT_SET_ROUTE_PROPERTIES

**Hash:** `0xD5002D78B7162E1B` | **JHash:** `0x01CAAFCC` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_SET_ROUTE_PROPERTIES(const char* route, int props)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_SET_ROUTE_PROPERTIES, route, props);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `route` | `const char*` | `string` | |
| `props` | `int` | `int` | |

**返回值:** `void` → `void`

---

### ASSISTED_MOVEMENT_OVERRIDE_LOAD_DISTANCE_THIS_FRAME

**Hash:** `0x13945951E16EF912` | **JHash:** `0x8FB923EC` | **Build:** `323`

**C++ 原型:**
```cpp
void ASSISTED_MOVEMENT_OVERRIDE_LOAD_DISTANCE_THIS_FRAME(float dist)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ASSISTED_MOVEMENT_OVERRIDE_LOAD_DISTANCE_THIS_FRAME, dist);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `dist` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_VEHICLE_FOLLOW_WAYPOINT_RECORDING

**Hash:** `0x3123FAA6DB1CF7ED` | **JHash:** `0x959818B6` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_VEHICLE_FOLLOW_WAYPOINT_RECORDING(Ped ped, Vehicle vehicle, const char* WPRecording, int p3, int p4, int p5, int p6, float p7, BOOL p8, float p9)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_VEHICLE_FOLLOW_WAYPOINT_RECORDING, ped, vehicle, WPRecording, p3, p4, p5, p6, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `WPRecording` | `const char*` | `string` | |
| `p3` | `int` | `int` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `int` | `int` | |
| `p7` | `float` | `float` | |
| `p8` | `BOOL` | `bool` | |
| `p9` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 

p2 = Waypoint recording string (found in update\update.rpf\x64\levels\gta5\waypointrec.rpf
p3 = 786468
p4 = 0
p5 = 16
p6 = -1 (angle?)
p7/8/9 = usually v3.zero
p10 = bool (repeat?)
p11 = 1073741824

Full list of waypoint recordings by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/waypointRecordings.json

---

### IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_VEHICLE

**Hash:** `0xF5134943EA29868C` | **JHash:** `0x80DD15DB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WAYPOINT_PLAYBACK_GOING_ON_FOR_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_VEHICLE_WAYPOINT_PROGRESS

**Hash:** `0x9824CFF8FC66E159` | **JHash:** `0xD3CCF64E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_WAYPOINT_PROGRESS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_WAYPOINT_PROGRESS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_WAYPOINT_TARGET_POINT

**Hash:** `0x416B62AC8B9E5BBD` | **JHash:** `0x81049608` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_WAYPOINT_TARGET_POINT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_WAYPOINT_TARGET_POINT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### VEHICLE_WAYPOINT_PLAYBACK_PAUSE

**Hash:** `0x8A4E6AC373666BC5` | **JHash:** `0x7C00B415` | **Build:** `323`

**C++ 原型:**
```cpp
void VEHICLE_WAYPOINT_PLAYBACK_PAUSE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_WAYPOINT_PLAYBACK_PAUSE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### VEHICLE_WAYPOINT_PLAYBACK_GET_IS_PAUSED

**Hash:** `0xE435D3539EFDCD1B` | **Build:** `3570`

**C++ 原型:**
```cpp
BOOL VEHICLE_WAYPOINT_PLAYBACK_GET_IS_PAUSED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.VEHICLE_WAYPOINT_PLAYBACK_GET_IS_PAUSED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### VEHICLE_WAYPOINT_PLAYBACK_RESUME

**Hash:** `0xDC04FCAA7839D492` | **JHash:** `0xBEB14C82` | **Build:** `323`

**C++ 原型:**
```cpp
void VEHICLE_WAYPOINT_PLAYBACK_RESUME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_WAYPOINT_PLAYBACK_RESUME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### VEHICLE_WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED

**Hash:** `0x5CEB25A7D2848963` | **JHash:** `0x923C3AA4` | **Build:** `323`

**C++ 原型:**
```cpp
void VEHICLE_WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_WAYPOINT_PLAYBACK_USE_DEFAULT_SPEED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### VEHICLE_WAYPOINT_PLAYBACK_OVERRIDE_SPEED

**Hash:** `0x121F0593E0A431D7` | **JHash:** `0xBE1E7BB4` | **Build:** `323`

**C++ 原型:**
```cpp
void VEHICLE_WAYPOINT_PLAYBACK_OVERRIDE_SPEED(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_WAYPOINT_PLAYBACK_OVERRIDE_SPEED, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_SET_BLOCKING_OF_NON_TEMPORARY_EVENTS

**Hash:** `0x90D2156198831D69` | **JHash:** `0x1B54FB6B` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SET_BLOCKING_OF_NON_TEMPORARY_EVENTS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SET_BLOCKING_OF_NON_TEMPORARY_EVENTS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** I cant believe I have to define this, this is one of the best natives.

It makes the ped ignore basically all shocking events around it. Occasionally the ped may comment or gesture, but other than that they just continue their daily activities. This includes shooting and wounding the ped. And - most importantly - they do not flee.

Since it is a task, every time the native is called the ped will stop for a moment. 

---

### TASK_FORCE_MOTION_STATE

**Hash:** `0x4F056E1AFFEF17AB` | **JHash:** `0xCAD2EF77` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_FORCE_MOTION_STATE(Ped ped, Hash state, BOOL forceRestart)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_FORCE_MOTION_STATE, ped, state, forceRestart);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `state` | `Hash` | `uint` | |
| `forceRestart` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p2 always false

[30/03/2017] ins1de :

See FORCE_PED_MOTION_STATE

---

### TASK_MOVE_NETWORK_BY_NAME

**Hash:** `0x2D537BA194896636` | **JHash:** `0x6F5D215F` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_MOVE_NETWORK_BY_NAME(Ped ped, const char* task, float multiplier, BOOL allowOverrideCloneUpdate, const char* animDict, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_MOVE_NETWORK_BY_NAME, ped, task, multiplier, allowOverrideCloneUpdate, animDict, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `task` | `const char*` | `string` | |
| `multiplier` | `float` | `float` | |
| `allowOverrideCloneUpdate` | `BOOL` | `bool` | |
| `animDict` | `const char*` | `string` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example:
TASK::TASK_MOVE_NETWORK_BY_NAME(PLAYER::PLAYER_PED_ID(), "arm_wrestling_sweep_paired_a_rev3", 0.0f, true, "mini@arm_wrestling", 0);

---

### TASK_MOVE_NETWORK_ADVANCED_BY_NAME

**Hash:** `0xD5B35BEA41919ACB` | **JHash:** `0x71A5C5DB` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_MOVE_NETWORK_ADVANCED_BY_NAME(Ped ped, const char* network, float x, float y, float z, float rotX, float rotY, float rotZ, int rotOrder, float blendDuration, BOOL allowOverrideCloneUpdate, const char* animDict, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_MOVE_NETWORK_ADVANCED_BY_NAME, ped, network, x, y, z, rotX, rotY, rotZ, rotOrder, blendDuration, allowOverrideCloneUpdate, animDict, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `network` | `const char*` | `string` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `rotOrder` | `int` | `int` | |
| `blendDuration` | `float` | `float` | |
| `allowOverrideCloneUpdate` | `BOOL` | `bool` | |
| `animDict` | `const char*` | `string` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Example:
TASK::TASK_MOVE_NETWORK_ADVANCED_BY_NAME(PLAYER::PLAYER_PED_ID(), "minigame_tattoo_michael_parts", 324.13f, 181.29f, 102.6f, 0.0f, 0.0f, 22.32f, 2, 0, false, 0, 0);

---

### TASK_MOVE_NETWORK_BY_NAME_WITH_INIT_PARAMS

**Hash:** `0x3D45B0B355C5E0C9` | **Build:** `1493`

**C++ 原型:**
```cpp
void TASK_MOVE_NETWORK_BY_NAME_WITH_INIT_PARAMS(Ped ped, const char* network, int* initialParameters, float blendDuration, BOOL allowOverrideCloneUpdate, const char* animDict, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_MOVE_NETWORK_BY_NAME_WITH_INIT_PARAMS, ped, network, initialParameters, blendDuration, allowOverrideCloneUpdate, animDict, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `network` | `const char*` | `string` | |
| `initialParameters` | `int*` | `int*` | |
| `blendDuration` | `float` | `float` | |
| `allowOverrideCloneUpdate` | `BOOL` | `bool` | |
| `animDict` | `const char*` | `string` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Used only once in the scripts (am_mp_nightclub)

---

### TASK_MOVE_NETWORK_ADVANCED_BY_NAME_WITH_INIT_PARAMS

**Hash:** `0x29682E2CCF21E9B5` | **Build:** `1868`

**C++ 原型:**
```cpp
void TASK_MOVE_NETWORK_ADVANCED_BY_NAME_WITH_INIT_PARAMS(Ped ped, const char* network, int* initialParameters, float x, float y, float z, float rotX, float rotY, float rotZ, int rotOrder, float blendDuration, BOOL allowOverrideCloneUpdate, const char* dictionary, int flags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_MOVE_NETWORK_ADVANCED_BY_NAME_WITH_INIT_PARAMS, ped, network, initialParameters, x, y, z, rotX, rotY, rotZ, rotOrder, blendDuration, allowOverrideCloneUpdate, dictionary, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `network` | `const char*` | `string` | |
| `initialParameters` | `int*` | `int*` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `rotOrder` | `int` | `int` | |
| `blendDuration` | `float` | `float` | |
| `allowOverrideCloneUpdate` | `BOOL` | `bool` | |
| `dictionary` | `const char*` | `string` | |
| `flags` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_TASK_MOVE_NETWORK_ACTIVE

**Hash:** `0x921CE12C489C4C41` | **JHash:** `0x902656EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TASK_MOVE_NETWORK_ACTIVE(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TASK_MOVE_NETWORK_ACTIVE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_TASK_MOVE_NETWORK_READY_FOR_TRANSITION

**Hash:** `0x30ED88D5E0C56A37` | **JHash:** `0x92FDBAE6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TASK_MOVE_NETWORK_READY_FOR_TRANSITION(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TASK_MOVE_NETWORK_READY_FOR_TRANSITION, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_TASK_MOVE_NETWORK_STATE_TRANSITION

**Hash:** `0xD01015C7316AE176` | **JHash:** `0x885724DE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REQUEST_TASK_MOVE_NETWORK_STATE_TRANSITION(Ped ped, const char* name)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REQUEST_TASK_MOVE_NETWORK_STATE_TRANSITION, ped, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SET_EXPECTED_CLONE_NEXT_TASK_MOVE_NETWORK_STATE

**Hash:** `0xAB13A5565480B6D9` | **JHash:** `0x1E58A7AD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_EXPECTED_CLONE_NEXT_TASK_MOVE_NETWORK_STATE(Ped ped, const char* state)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_EXPECTED_CLONE_NEXT_TASK_MOVE_NETWORK_STATE, ped, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `state` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** Used only once in the scripts (fm_mission_controller) like so:

TASK::SET_EXPECTED_CLONE_NEXT_TASK_MOVE_NETWORK_STATE(iLocal_3160, "Cutting");

---

### GET_TASK_MOVE_NETWORK_STATE

**Hash:** `0x717E4D1F2048376D` | **JHash:** `0x96C0277B` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_TASK_MOVE_NETWORK_STATE(Ped ped)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_TASK_MOVE_NETWORK_STATE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `const char*` → `string`

---

### SET_TASK_MOVE_NETWORK_ANIM_SET

**Hash:** `0x8423541E8B3A1589` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_TASK_MOVE_NETWORK_ANIM_SET(Ped ped, Hash clipSet, Hash variableClipSet)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_MOVE_NETWORK_ANIM_SET, ped, clipSet, variableClipSet);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `clipSet` | `Hash` | `uint` | |
| `variableClipSet` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT

**Hash:** `0xD5BB4025AE449A4E` | **JHash:** `0xA79BE783` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT(Ped ped, const char* signalName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT, ped, signalName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** signalName - "Phase", "Wobble", "x_axis","y_axis","introphase","speed".
p2 - From what i can see it goes up to 1f (maybe).

Example: TASK::SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT(PLAYER::PLAYER_PED_ID(), "Phase", 0.5);

---

### SET_TASK_MOVE_NETWORK_SIGNAL_LOCAL_FLOAT

**Hash:** `0x373EF409B82697A3` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_TASK_MOVE_NETWORK_SIGNAL_LOCAL_FLOAT(Ped ped, const char* signalName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_MOVE_NETWORK_SIGNAL_LOCAL_FLOAT, ped, signalName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT_LERP_RATE

**Hash:** `0x8634CEF2522D987B` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT_LERP_RATE(Ped ped, const char* signalName, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_MOVE_NETWORK_SIGNAL_FLOAT_LERP_RATE, ped, signalName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TASK_MOVE_NETWORK_SIGNAL_BOOL

**Hash:** `0xB0A6CFD2C69C1088` | **JHash:** `0xF3538041` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TASK_MOVE_NETWORK_SIGNAL_BOOL(Ped ped, const char* signalName, BOOL value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_MOVE_NETWORK_SIGNAL_BOOL, ped, signalName, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |
| `value` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_TASK_MOVE_NETWORK_SIGNAL_FLOAT

**Hash:** `0x44AB0B3AFECCE242` | **Build:** `1493`

**C++ 原型:**
```cpp
float GET_TASK_MOVE_NETWORK_SIGNAL_FLOAT(Ped ped, const char* signalName)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TASK_MOVE_NETWORK_SIGNAL_FLOAT, ped, signalName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |

**返回值:** `float` → `float`

---

### GET_TASK_MOVE_NETWORK_SIGNAL_BOOL

**Hash:** `0xA7FFBA498E4AAF67` | **JHash:** `0x1EBB6F3D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TASK_MOVE_NETWORK_SIGNAL_BOOL(Ped ped, const char* signalName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TASK_MOVE_NETWORK_SIGNAL_BOOL, ped, signalName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `signalName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### GET_TASK_MOVE_NETWORK_EVENT

**Hash:** `0xB4F47213DF45A64C` | **JHash:** `0x72FA5EF2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_TASK_MOVE_NETWORK_EVENT(Ped ped, const char* eventName)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_TASK_MOVE_NETWORK_EVENT, ped, eventName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `eventName` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

---

### SET_TASK_MOVE_NETWORK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED

**Hash:** `0x0FFB3C758E8C07B9` | **Build:** `2060`

**C++ 原型:**
```cpp
BOOL SET_TASK_MOVE_NETWORK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED(Ped ped, BOOL enable)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_TASK_MOVE_NETWORK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED, ped, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Doesn't actually return anything.

---

### _SET_SCRIPT_TASK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED

**Hash:** `0x32F6EEF031F943DC` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_SCRIPT_TASK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED(Ped ped, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_SCRIPT_TASK_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED, ped, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_AMBIENT_PED_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED

**Hash:** `0x0EFE4834A2F40563` | **Build:** `3570`

**C++ 原型:**
```cpp
void _SET_AMBIENT_PED_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED(Ped ped, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_AMBIENT_PED_ENABLE_COLLISION_ON_NETWORK_CLONE_WHEN_FIXED, ped, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_MOVE_BLEND_RATIO_STILL

**Hash:** `0x349CE7B56DAFD95C` | **JHash:** `0xE9DAF877` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOVE_BLEND_RATIO_STILL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOVE_BLEND_RATIO_STILL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_MOVE_BLEND_RATIO_WALKING

**Hash:** `0xF133BBBE91E1691F` | **JHash:** `0xD21639A8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOVE_BLEND_RATIO_WALKING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOVE_BLEND_RATIO_WALKING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_MOVE_BLEND_RATIO_RUNNING

**Hash:** `0xD4D8636C0199A939` | **JHash:** `0xE76A2353` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOVE_BLEND_RATIO_RUNNING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOVE_BLEND_RATIO_RUNNING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_MOVE_BLEND_RATIO_SPRINTING

**Hash:** `0x24A2AD74FA9814E2` | **JHash:** `0xDD616893` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_MOVE_BLEND_RATIO_SPRINTING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MOVE_BLEND_RATIO_SPRINTING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_STILL

**Hash:** `0xAC29253EEF8F0180` | **JHash:** `0x09E3418D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_STILL(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_STILL, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_WALKING

**Hash:** `0xDE4C184B2B9B071A` | **JHash:** `0x4B865C4A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_WALKING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_WALKING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_RUNNING

**Hash:** `0xC5286FFC176F28A2` | **JHash:** `0xE9A5578F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RUNNING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RUNNING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_SPRINTING

**Hash:** `0x57E457CD2C0FC168` | **JHash:** `0x4F3E0633` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_SPRINTING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_SPRINTING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_STRAFING

**Hash:** `0xE45B7F222DE47E09` | **JHash:** `0xEFEED13C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_STRAFING(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_STRAFING, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### TASK_SYNCHRONIZED_SCENE

**Hash:** `0xEEA929141F699854` | **JHash:** `0x4F217E7B` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SYNCHRONIZED_SCENE(Ped ped, int scene, const char* animDictionary, const char* animationName, float blendIn, float blendOut, int flags, int ragdollBlockingFlags, float moverBlendDelta, int ikFlags)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SYNCHRONIZED_SCENE, ped, scene, animDictionary, animationName, blendIn, blendOut, flags, ragdollBlockingFlags, moverBlendDelta, ikFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `scene` | `int` | `int` | |
| `animDictionary` | `const char*` | `string` | |
| `animationName` | `const char*` | `string` | |
| `blendIn` | `float` | `float` | |
| `blendOut` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `ragdollBlockingFlags` | `int` | `int` | |
| `moverBlendDelta` | `float` | `float` | |
| `ikFlags` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:**  TASK::TASK_SYNCHRONIZED_SCENE(ped, scene, "creatures@rottweiler@in_vehicle@std_car", "get_in", 1000.0, -8.0, 4, 0, 0x447a0000, 0);

Full list of animation dictionaries and anims by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/animDictsCompact.json

---

### TASK_AGITATED_ACTION_CONFRONT_RESPONSE

**Hash:** `0x19D1B791CB3670FE` | **Build:** `877`

**C++ 原型:**
```cpp
void TASK_AGITATED_ACTION_CONFRONT_RESPONSE(Ped ped, Ped ped2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_AGITATED_ACTION_CONFRONT_RESPONSE, ped, ped2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ped2` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### TASK_SWEEP_AIM_ENTITY

**Hash:** `0x2047C02158D6405A` | **JHash:** `0x4D210467` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SWEEP_AIM_ENTITY(Ped ped, const char* animDict, const char* lowAnimName, const char* medAnimName, const char* hiAnimName, int runtime, Entity targetEntity, float turnRate, float blendInDuration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SWEEP_AIM_ENTITY, ped, animDict, lowAnimName, medAnimName, hiAnimName, runtime, targetEntity, turnRate, blendInDuration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `lowAnimName` | `const char*` | `string` | |
| `medAnimName` | `const char*` | `string` | |
| `hiAnimName` | `const char*` | `string` | |
| `runtime` | `int` | `int` | |
| `targetEntity` | `Entity` | `int` | |
| `turnRate` | `float` | `float` | |
| `blendInDuration` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This function is called on peds in vehicles.

anim: animation name
p2, p3, p4: "sweep_low", "sweep_med" or "sweep_high"
p5: no idea what it does but is usually -1

---

### UPDATE_TASK_SWEEP_AIM_ENTITY

**Hash:** `0xE4973DBDBE6E44B3` | **JHash:** `0xF65F0F4F` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_TASK_SWEEP_AIM_ENTITY(Ped ped, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_TASK_SWEEP_AIM_ENTITY, ped, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### TASK_SWEEP_AIM_POSITION

**Hash:** `0x7AFE8FDC10BC07D2` | **JHash:** `0x1683FE66` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_SWEEP_AIM_POSITION(Ped ped, const char* animDict, const char* lowAnimName, const char* medAnimName, const char* hiAnimName, int runtime, float x, float y, float z, float turnRate, float blendInDuration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_SWEEP_AIM_POSITION, ped, animDict, lowAnimName, medAnimName, hiAnimName, runtime, x, y, z, turnRate, blendInDuration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animDict` | `const char*` | `string` | |
| `lowAnimName` | `const char*` | `string` | |
| `medAnimName` | `const char*` | `string` | |
| `hiAnimName` | `const char*` | `string` | |
| `runtime` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `turnRate` | `float` | `float` | |
| `blendInDuration` | `float` | `float` | |

**返回值:** `void` → `void`

---

### UPDATE_TASK_SWEEP_AIM_POSITION

**Hash:** `0xBB106883F5201FC4` | **JHash:** `0x6345EC80` | **Build:** `323`

**C++ 原型:**
```cpp
void UPDATE_TASK_SWEEP_AIM_POSITION(Ped ped, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UPDATE_TASK_SWEEP_AIM_POSITION, ped, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### TASK_ARREST_PED

**Hash:** `0xF3B9A78A178572B1` | **JHash:** `0xBC0F153D` | **Build:** `323`

**C++ 原型:**
```cpp
void TASK_ARREST_PED(Ped ped, Ped target)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TASK_ARREST_PED, ped, target);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `target` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Example from "me_amanda1.ysc.c4":
TASK::TASK_ARREST_PED(l_19F /* This is a Ped */ , PLAYER::PLAYER_PED_ID());

Example from "armenian1.ysc.c4":
if (!PED::IS_PED_INJURED(l_B18[0/*1*/])) {
    TASK::TASK_ARREST_PED(l_B18[0/*1*/], PLAYER::PLAYER_PED_ID());
}

I would love to have time to experiment to see if a player Ped can arrest another Ped. Might make for a good cop mod.


Looks like only the player can be arrested this way. Peds react and try to arrest you if you task them, but the player charater doesn't do anything if tasked to arrest another ped.

---

### IS_PED_RUNNING_ARREST_TASK

**Hash:** `0x3DC52677769B4AE0` | **JHash:** `0x6942DB7A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_RUNNING_ARREST_TASK(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_RUNNING_ARREST_TASK, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PED_BEING_ARRESTED

**Hash:** `0x90A09F3A45FED688` | **JHash:** `0x5FF6C2ED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_BEING_ARRESTED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_BEING_ARRESTED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This function is hard-coded to always return 0.

---

### UNCUFF_PED

**Hash:** `0x67406F2C8F87FC4F` | **JHash:** `0xA23A1D61` | **Build:** `323`

**C++ 原型:**
```cpp
void UNCUFF_PED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNCUFF_PED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_CUFFED

**Hash:** `0x74E559B3BC910685` | **JHash:** `0x511CE741` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_CUFFED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_CUFFED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---


---

## VEHICLE

**包含 779 个 Native 函数**

### CREATE_VEHICLE

**Hash:** `0xAF35D0D2583051B0` | **JHash:** `0xDD75460A` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle CREATE_VEHICLE(Hash modelHash, float x, float y, float z, float heading, BOOL isNetwork, BOOL bScriptHostVeh, BOOL p7)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_VEHICLE, modelHash, x, y, z, heading, isNetwork, bScriptHostVeh, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `isNetwork` | `BOOL` | `bool` | |
| `bScriptHostVeh` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `Vehicle` → `int`

**说明:** p7 when set to true allows you to spawn vehicles under -100 z.
Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### DELETE_VEHICLE

**Hash:** `0xEA386986E786A54F` | **JHash:** `0x9803AF60` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_VEHICLE(Vehicle* vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Deletes a vehicle.
The vehicle must be a mission entity to delete, so call this before deleting: SET_ENTITY_AS_MISSION_ENTITY(vehicle, true, true);

eg how to use:
SET_ENTITY_AS_MISSION_ENTITY(vehicle, true, true);
DELETE_VEHICLE(&vehicle);

Deletes the specified vehicle, then sets the handle pointed to by the pointer to NULL.

---

### SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON

**Hash:** `0x7D6F9A3EF26136A0` | **JHash:** `0xBB54ECCA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON(Vehicle vehicle, BOOL toggle, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON, vehicle, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _GET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED

**Hash:** `0x613A431BCD5A22EB` | **Build:** `3717`

**C++ 原型:**
```cpp
BOOL _GET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._GET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED

**Hash:** `0x1DDA078D12879EEE` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED(Vehicle vehicle, BOOL canBeLockedOn, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ALLOW_HOMING_MISSLE_LOCKON_SYNCED, vehicle, canBeLockedOn, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `canBeLockedOn` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_ALLOW_NO_PASSENGERS_LOCKON

**Hash:** `0x5D14D4154BFE7B2C` | **JHash:** `0x8BAAC437` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ALLOW_NO_PASSENGERS_LOCKON(Vehicle veh, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ALLOW_NO_PASSENGERS_LOCKON, veh, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `veh` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the vehicle accept no passengers.

---

### GET_VEHICLE_HOMING_LOCKON_STATE

**Hash:** `0xE6B0E8CFC3633BF0` | **JHash:** `0xFBDE9FD8` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_HOMING_LOCKON_STATE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_HOMING_LOCKON_STATE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns a value depending on the lock-on state of vehicle weapons.
0: not locked on
1: locking on
2: locked on

---

### GET_VEHICLE_HOMING_LOCKEDONTO_STATE

**Hash:** `0x6EAAEFC76ACC311F` | **Build:** `757`

**C++ 原型:**
```cpp
int GET_VEHICLE_HOMING_LOCKEDONTO_STATE(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_HOMING_LOCKEDONTO_STATE, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### SET_VEHICLE_HOMING_LOCKEDONTO_STATE

**Hash:** `0x407DC5E97DB1A4D3` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_VEHICLE_HOMING_LOCKEDONTO_STATE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HOMING_LOCKEDONTO_STATE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_MODEL

**Hash:** `0x423E8DE37D934D89` | **JHash:** `0x013B10B6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_MODEL(Vehicle vehicle, Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_MODEL, vehicle, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### DOES_SCRIPT_VEHICLE_GENERATOR_EXIST

**Hash:** `0xF6086BC836400876` | **JHash:** `0xF6BDDA30` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_SCRIPT_VEHICLE_GENERATOR_EXIST(int vehicleGenerator)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_SCRIPT_VEHICLE_GENERATOR_EXIST, vehicleGenerator);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleGenerator` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CREATE_SCRIPT_VEHICLE_GENERATOR

**Hash:** `0x9DEF883114668116` | **JHash:** `0x25A9A261` | **Build:** `323`

**C++ 原型:**
```cpp
int CREATE_SCRIPT_VEHICLE_GENERATOR(float x, float y, float z, float heading, float p4, float p5, Hash modelHash, int p7, int p8, int p9, int p10, BOOL p11, BOOL p12, BOOL p13, BOOL p14, BOOL p15, int p16)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_SCRIPT_VEHICLE_GENERATOR, x, y, z, heading, p4, p5, modelHash, p7, p8, p9, p10, p11, p12, p13, p14, p15, p16);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `heading` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `p7` | `int` | `int` | |
| `p8` | `int` | `int` | |
| `p9` | `int` | `int` | |
| `p10` | `int` | `int` | |
| `p11` | `BOOL` | `bool` | |
| `p12` | `BOOL` | `bool` | |
| `p13` | `BOOL` | `bool` | |
| `p14` | `BOOL` | `bool` | |
| `p15` | `BOOL` | `bool` | |
| `p16` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Creates a script vehicle generator at the given coordinates. Most parameters after the model hash are unknown.

Parameters:
x/y/z - Generator position
heading - Generator heading
p4 - Unknown (always 5.0)
p5 - Unknown (always 3.0)
modelHash - Vehicle model hash
p7/8/9/10 - Unknown (always -1)
p11 - Unknown (usually TRUE, only one instance of FALSE)
p12/13 - Unknown (always FALSE)
p14 - Unknown (usally FALSE, only two instances of TRUE)
p15 - Unknown (always TRUE)
p16 - Unknown (always -1)

Vector3 coords = GET_ENTITY_COORDS(PLAYER_PED_ID(), 0);    CREATE_SCRIPT_VEHICLE_GENERATOR(coords.x, coords.y, coords.z, 1.0f, 5.0f, 3.0f, GET_HASH_KEY("adder"), -1. -1, -1, -1, -1, true, false, false, false, true, -1);

---

### DELETE_SCRIPT_VEHICLE_GENERATOR

**Hash:** `0x22102C9ABFCF125D` | **JHash:** `0xE4328E3F` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_SCRIPT_VEHICLE_GENERATOR(int vehicleGenerator)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_SCRIPT_VEHICLE_GENERATOR, vehicleGenerator);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleGenerator` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_SCRIPT_VEHICLE_GENERATOR

**Hash:** `0xD9D620E0AC6DC4B0` | **JHash:** `0x40D73747` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SCRIPT_VEHICLE_GENERATOR(int vehicleGenerator, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_VEHICLE_GENERATOR, vehicleGenerator, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleGenerator` | `int` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only called once in the decompiled scripts. Presumably activates the specified generator.

---

### SET_ALL_VEHICLE_GENERATORS_ACTIVE_IN_AREA

**Hash:** `0xC12321827687FE4D` | **JHash:** `0xB4E0E69A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_VEHICLE_GENERATORS_ACTIVE_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, BOOL toggle, BOOL p7)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_VEHICLE_GENERATORS_ACTIVE_IN_AREA, x1, y1, z1, x2, y2, z2, toggle, p7);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `toggle` | `BOOL` | `bool` | |
| `p7` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When p6 is true, vehicle generators are active.
p7 seems to always be true in story mode scripts, but it's sometimes false in online scripts.

---

### SET_ALL_VEHICLE_GENERATORS_ACTIVE

**Hash:** `0x34AD89078831A4BC` | **JHash:** `0xAB1FDD76` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_VEHICLE_GENERATORS_ACTIVE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_VEHICLE_GENERATORS_ACTIVE);
```

**返回值:** `void` → `void`

---

### SET_ALL_LOW_PRIORITY_VEHICLE_GENERATORS_ACTIVE

**Hash:** `0x608207E7A8FB787C` | **JHash:** `0x87F767F2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALL_LOW_PRIORITY_VEHICLE_GENERATORS_ACTIVE(BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALL_LOW_PRIORITY_VEHICLE_GENERATORS_ACTIVE, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_GENERATOR_AREA_OF_INTEREST

**Hash:** `0x9A75585FB2E54FAD` | **JHash:** `0x935A95DA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_GENERATOR_AREA_OF_INTEREST(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_GENERATOR_AREA_OF_INTEREST, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Related to car generators & CPlayerSwitchMgrLong

---

### CLEAR_VEHICLE_GENERATOR_AREA_OF_INTEREST

**Hash:** `0x0A436B8643716D14` | **JHash:** `0x6C73E45A` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_GENERATOR_AREA_OF_INTEREST(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_GENERATOR_AREA_OF_INTEREST);
```

**返回值:** `void` → `void`

---

### SET_VEHICLE_ON_GROUND_PROPERLY

**Hash:** `0x49733E92263139D1` | **JHash:** `0xE14FDBA6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_VEHICLE_ON_GROUND_PROPERLY(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_VEHICLE_ON_GROUND_PROPERLY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Sets a vehicle on the ground on all wheels.  Returns whether or not the operation was successful.

sfink: This has an additional param(Vehicle vehicle, float p1) which is always set to 5.0f in the b944 scripts.

---

### SET_VEHICLE_USE_CUTSCENE_WHEEL_COMPRESSION

**Hash:** `0xE023E8AC4EF7C117` | **JHash:** `0xA0909ADB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_VEHICLE_USE_CUTSCENE_WHEEL_COMPRESSION(Vehicle p0, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_VEHICLE_USE_CUTSCENE_WHEEL_COMPRESSION, p0, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_STUCK_ON_ROOF

**Hash:** `0xB497F06B288DCFDF` | **JHash:** `0x18D07C6C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_STUCK_ON_ROOF(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_STUCK_ON_ROOF, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ADD_VEHICLE_UPSIDEDOWN_CHECK

**Hash:** `0xB72E26D81006005B` | **JHash:** `0x3A13D384` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_VEHICLE_UPSIDEDOWN_CHECK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VEHICLE_UPSIDEDOWN_CHECK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### REMOVE_VEHICLE_UPSIDEDOWN_CHECK

**Hash:** `0xC53EB42A499A7E90` | **JHash:** `0xF390BA1B` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_UPSIDEDOWN_CHECK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_UPSIDEDOWN_CHECK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_STOPPED

**Hash:** `0x5721B434AD84D57A` | **JHash:** `0x655F072C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_STOPPED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_STOPPED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle's current speed is less than, or equal to 0.0025f.

For some vehicles it returns true if the current speed is <= 0.00039999999.

---

### GET_VEHICLE_NUMBER_OF_PASSENGERS

**Hash:** `0x24CB2137731FFE89` | **JHash:** `0x1EF20849` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_NUMBER_OF_PASSENGERS(Vehicle vehicle, BOOL includeDriver, BOOL includeDeadOccupants)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_NUMBER_OF_PASSENGERS, vehicle, includeDriver, includeDeadOccupants);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `includeDriver` | `BOOL` | `bool` | |
| `includeDeadOccupants` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** Gets the number of passengers.

This native was modified in b2545 to take two additional parameters, allowing you to include the driver or exclude dead passengers.

To keep it working like before b2545, set includeDriver to false and includeDeadOccupants to true.

---

### GET_VEHICLE_MAX_NUMBER_OF_PASSENGERS

**Hash:** `0xA7C4F2C6E744A550` | **JHash:** `0x0A2FC08C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MAX_NUMBER_OF_PASSENGERS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MAX_NUMBER_OF_PASSENGERS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_MODEL_NUMBER_OF_SEATS

**Hash:** `0x2AD93716F184EDA4` | **JHash:** `0x838F7BF7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MODEL_NUMBER_OF_SEATS(Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MODEL_NUMBER_OF_SEATS, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns max number of passengers (including the driver) for the specified vehicle model.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### IS_SEAT_WARP_ONLY

**Hash:** `0xF7F203E31F96F6A1` | **JHash:** `0x769E5CF2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_SEAT_WARP_ONLY(Vehicle vehicle, int seatIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_SEAT_WARP_ONLY, vehicle, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_TURRET_SEAT

**Hash:** `0xE33FFA906CE74880` | **JHash:** `0x7C43D32A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TURRET_SEAT(Vehicle vehicle, int seatIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TURRET_SEAT, vehicle, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DOES_VEHICLE_ALLOW_RAPPEL

**Hash:** `0x4E417C547182C84D` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_ALLOW_RAPPEL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_ALLOW_RAPPEL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle has the FLAG_ALLOWS_RAPPEL flag set.

---

### SET_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME

**Hash:** `0x245A6883D966D537` | **JHash:** `0xF4187E51` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Use this native inside a looped function.
Values:
- `0.0` = no vehicles on streets
- `1.0` = normal vehicles on streets

---

### SET_RANDOM_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME

**Hash:** `0xB3B3359379FE77D3` | **JHash:** `0x543F712B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PARKED_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME

**Hash:** `0xEAE6DCC7EEE3DB1D` | **JHash:** `0xDD46CEBE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PARKED_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PARKED_VEHICLE_DENSITY_MULTIPLIER_THIS_FRAME, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_RANDOM_TRAINS_THIS_FRAME

**Hash:** `0xD4B8E3D1917BC86B` | **JHash:** `0x09462665` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_RANDOM_TRAINS_THIS_FRAME(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_RANDOM_TRAINS_THIS_FRAME, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AMBIENT_VEHICLE_RANGE_MULTIPLIER_THIS_FRAME

**Hash:** `0x90B6DA738A9A25DA` | **JHash:** `0xDAE2A2BE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_VEHICLE_RANGE_MULTIPLIER_THIS_FRAME(float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_VEHICLE_RANGE_MULTIPLIER_THIS_FRAME, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_FAR_DRAW_VEHICLES

**Hash:** `0x26324F33423F3CC3` | **JHash:** `0x9F019C49` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FAR_DRAW_VEHICLES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FAR_DRAW_VEHICLES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_NUMBER_OF_PARKED_VEHICLES

**Hash:** `0xCAA15F13EBD417FF` | **JHash:** `0x206A58E8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_NUMBER_OF_PARKED_VEHICLES(int value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NUMBER_OF_PARKED_VEHICLES, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `value` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DOORS_LOCKED

**Hash:** `0xB664292EAECF7FA6` | **JHash:** `0x4CDD35D0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED(Vehicle vehicle, int doorLockStatus)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED, vehicle, doorLockStatus);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorLockStatus` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum eVehicleLockState
{
	VEHICLELOCK_NONE,
	VEHICLELOCK_UNLOCKED,
	VEHICLELOCK_LOCKED,
	VEHICLELOCK_LOCKOUT_PLAYER_ONLY,
	VEHICLELOCK_LOCKED_PLAYER_INSIDE,
	VEHICLELOCK_LOCKED_INITIALLY,
	VEHICLELOCK_FORCE_SHUT_DOORS,
	VEHICLELOCK_LOCKED_BUT_CAN_BE_DAMAGED,
	VEHICLELOCK_LOCKED_BUT_BOOT_UNLOCKED,
	VEHICLELOCK_LOCKED_NO_PASSENGERS,
	VEHICLELOCK_CANNOT_ENTER	
};

---

### SET_VEHICLE_INDIVIDUAL_DOORS_LOCKED

**Hash:** `0xBE70724027F85BCD` | **JHash:** `0xD61D182D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_INDIVIDUAL_DOORS_LOCKED(Vehicle vehicle, int doorId, int doorLockStatus)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_INDIVIDUAL_DOORS_LOCKED, vehicle, doorId, doorLockStatus);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `doorLockStatus` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_HAS_MUTED_SIRENS

**Hash:** `0xD8050E0EB60CF274` | **JHash:** `0xC54156A9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HAS_MUTED_SIRENS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HAS_MUTED_SIRENS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If set to true, prevents vehicle sirens from having sound, leaving only the lights.

---

### SET_VEHICLE_DOORS_LOCKED_FOR_PLAYER

**Hash:** `0x517AAF684BB50CD1` | **JHash:** `0x49829236` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED_FOR_PLAYER(Vehicle vehicle, Player player, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED_FOR_PLAYER, vehicle, player, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `player` | `Player` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_DOORS_LOCKED_FOR_PLAYER

**Hash:** `0xF6AF6CB341349015` | **JHash:** `0x1DC50247` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_DOORS_LOCKED_FOR_PLAYER(Vehicle vehicle, Player player)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_DOORS_LOCKED_FOR_PLAYER, vehicle, player);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `player` | `Player` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_DOORS_LOCKED_FOR_ALL_PLAYERS

**Hash:** `0xA2F80B8D040727CC` | **JHash:** `0x891BA8A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED_FOR_ALL_PLAYERS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED_FOR_ALL_PLAYERS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** After some analysis, I've decided that these are what the parameters are.

We can see this being used in R* scripts such as "am_mp_property_int.ysc.c4":
l_11A1 = PED::GET_VEHICLE_PED_IS_IN(PLAYER::PLAYER_PED_ID(), 1);
...
VEHICLE::SET_VEHICLE_DOORS_LOCKED_FOR_ALL_PLAYERS(l_11A1, 1);

---

### SET_VEHICLE_DOORS_LOCKED_FOR_NON_SCRIPT_PLAYERS

**Hash:** `0x9737A37136F07E75` | **JHash:** `0xE4EF6514` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED_FOR_NON_SCRIPT_PLAYERS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED_FOR_NON_SCRIPT_PLAYERS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DOORS_LOCKED_FOR_TEAM

**Hash:** `0xB81F6D4A8F5EEBA8` | **JHash:** `0x4F85E783` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED_FOR_TEAM(Vehicle vehicle, int team, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED_FOR_TEAM, vehicle, team, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `team` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DOORS_LOCKED_FOR_ALL_TEAMS

**Hash:** `0x203B527D1B77904C` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_LOCKED_FOR_ALL_TEAMS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_LOCKED_FOR_ALL_TEAMS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DONT_TERMINATE_TASK_WHEN_ACHIEVED

**Hash:** `0x76D26A22750E849E` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_DONT_TERMINATE_TASK_WHEN_ACHIEVED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DONT_TERMINATE_TASK_WHEN_ACHIEVED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### _SET_VEHICLE_MAX_LAUNCH_ENGINE_REVS

**Hash:** `0x5AE614ECA5FDD423` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_VEHICLE_MAX_LAUNCH_ENGINE_REVS(Vehicle vehicle, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_VEHICLE_MAX_LAUNCH_ENGINE_REVS, vehicle, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 0.0f = engine rev minimum
1.0f = engine rev limit

---

### _GET_VEHICLE_THROTTLE

**Hash:** `0x92D96892FC06AF22` | **Build:** `3095`

**C++ 原型:**
```cpp
float _GET_VEHICLE_THROTTLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_VEHICLE_THROTTLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### EXPLODE_VEHICLE

**Hash:** `0xBA71116ADF5B514C` | **JHash:** `0xBEDEACEB` | **Build:** `323`

**C++ 原型:**
```cpp
void EXPLODE_VEHICLE(Vehicle vehicle, BOOL isAudible, BOOL isInvisible)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXPLODE_VEHICLE, vehicle, isAudible, isInvisible);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `isAudible` | `BOOL` | `bool` | |
| `isInvisible` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Explodes a selected vehicle.

Vehicle vehicle = Vehicle you want to explode.
BOOL isAudible = If explosion makes a sound.
BOOL isInvisible = If the explosion is invisible or not.

First BOOL does not give any visual explosion, the vehicle just falls apart completely but slowly and starts to burn.

---

### SET_VEHICLE_OUT_OF_CONTROL

**Hash:** `0xF19D095E42D430CC` | **JHash:** `0x3764D734` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_OUT_OF_CONTROL(Vehicle vehicle, BOOL killDriver, BOOL explodeOnImpact)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_OUT_OF_CONTROL, vehicle, killDriver, explodeOnImpact);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `killDriver` | `BOOL` | `bool` | |
| `explodeOnImpact` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Tested on the player's current vehicle. Unless you kill the driver, the vehicle doesn't loose control, however, if enabled, explodeOnImpact is still active. The moment you crash, boom.

---

### SET_VEHICLE_TIMED_EXPLOSION

**Hash:** `0x2E0A74E1002380B1` | **JHash:** `0xDB8CB8E2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TIMED_EXPLOSION(Vehicle vehicle, Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TIMED_EXPLOSION, vehicle, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADD_VEHICLE_PHONE_EXPLOSIVE_DEVICE

**Hash:** `0x99AD4CCCB128CBC9` | **JHash:** `0x811373DE` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_VEHICLE_PHONE_EXPLOSIVE_DEVICE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VEHICLE_PHONE_EXPLOSIVE_DEVICE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### CLEAR_VEHICLE_PHONE_EXPLOSIVE_DEVICE

**Hash:** `0xAA3F739ABDDCF21F` | **Build:** `463`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_PHONE_EXPLOSIVE_DEVICE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_PHONE_EXPLOSIVE_DEVICE);
```

**返回值:** `void` → `void`

---

### HAS_VEHICLE_PHONE_EXPLOSIVE_DEVICE

**Hash:** `0x6ADAABD3068C5235` | **JHash:** `0xA4E69134` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_VEHICLE_PHONE_EXPLOSIVE_DEVICE(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VEHICLE_PHONE_EXPLOSIVE_DEVICE);
```

**返回值:** `BOOL` → `bool`

---

### DETONATE_VEHICLE_PHONE_EXPLOSIVE_DEVICE

**Hash:** `0xEF49CF0270307CBE` | **JHash:** `0x65255524` | **Build:** `323`

**C++ 原型:**
```cpp
void DETONATE_VEHICLE_PHONE_EXPLOSIVE_DEVICE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETONATE_VEHICLE_PHONE_EXPLOSIVE_DEVICE);
```

**返回值:** `void` → `void`

---

### HAVE_VEHICLE_REAR_DOORS_BEEN_BLOWN_OPEN_BY_STICKYBOMB

**Hash:** `0x6B407F2525E93644` | **Build:** `2944`

**C++ 原型:**
```cpp
BOOL HAVE_VEHICLE_REAR_DOORS_BEEN_BLOWN_OPEN_BY_STICKYBOMB(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_VEHICLE_REAR_DOORS_BEEN_BLOWN_OPEN_BY_STICKYBOMB, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_TAXI_LIGHTS

**Hash:** `0x598803E85E8448D9` | **JHash:** `0x68639D85` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TAXI_LIGHTS(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TAXI_LIGHTS, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This is not tested - it's just an assumption.
- Nac

Doesn't seem to work.  I'll try with an int instead. --JT

Read the scripts, im dumpass. 

                            if (!VEHICLE::IS_TAXI_LIGHT_ON(l_115)) {
                                VEHICLE::SET_TAXI_LIGHTS(l_115, 1);
                            }

---

### IS_TAXI_LIGHT_ON

**Hash:** `0x7504C0F113AB50FC` | **JHash:** `0x6FC4924A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TAXI_LIGHT_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TAXI_LIGHT_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_IN_GARAGE_AREA

**Hash:** `0xCEE4490CD57BB3C2` | **JHash:** `0xA90EC257` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_IN_GARAGE_AREA(const char* garageName, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_IN_GARAGE_AREA, garageName, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `garageName` | `const char*` | `string` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** garageName example "Michael - Beverly Hills"

Full list of garages by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/garages.json

---

### SET_VEHICLE_COLOURS

**Hash:** `0x4F1D4BE3A7F24601` | **JHash:** `0x57F24253` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_COLOURS(Vehicle vehicle, int colorPrimary, int colorSecondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_COLOURS, vehicle, colorPrimary, colorSecondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `colorPrimary` | `int` | `int` | |
| `colorSecondary` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** colorPrimary & colorSecondary are the paint index for the vehicle.
For a list of valid paint indexes, view: https://pastebin.com/pwHci0xK
-------------------------------------------------------------------------
Note: minimum color index is 0, maximum color index is (numColorIndices - 1)

Full list of vehicle colors by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleColors.json

---

### SET_VEHICLE_FULLBEAM

**Hash:** `0x8B7FD87F0DDB421E` | **JHash:** `0x9C49CC15` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FULLBEAM(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FULLBEAM, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** It switch to highbeam when p1 is set to true.

---

### SET_VEHICLE_IS_RACING

**Hash:** `0x07116E24E9D1929D` | **JHash:** `0xA59E3DCD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_IS_RACING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IS_RACING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 (toggle) was always 1 (true) except in one case in the b678 scripts.

---

### SET_VEHICLE_CUSTOM_PRIMARY_COLOUR

**Hash:** `0x7141766F91D15BEA` | **JHash:** `0x8DF9F9BC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CUSTOM_PRIMARY_COLOUR(Vehicle vehicle, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CUSTOM_PRIMARY_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1, p2, p3 are RGB values for color (255,0,0 for Red, ect)

---

### GET_VEHICLE_CUSTOM_PRIMARY_COLOUR

**Hash:** `0xB64CF2CCA9D95F52` | **JHash:** `0x1C2B9FEF` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_CUSTOM_PRIMARY_COLOUR(Vehicle vehicle, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_CUSTOM_PRIMARY_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### CLEAR_VEHICLE_CUSTOM_PRIMARY_COLOUR

**Hash:** `0x55E1D2758F34E437` | **JHash:** `0x51E1E33D` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_CUSTOM_PRIMARY_COLOUR(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_CUSTOM_PRIMARY_COLOUR, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_IS_VEHICLE_PRIMARY_COLOUR_CUSTOM

**Hash:** `0xF095C0405307B21B` | **JHash:** `0xD7EC8760` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_VEHICLE_PRIMARY_COLOUR_CUSTOM(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_VEHICLE_PRIMARY_COLOUR_CUSTOM, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_CUSTOM_SECONDARY_COLOUR

**Hash:** `0x36CED73BFED89754` | **JHash:** `0x9D77259E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CUSTOM_SECONDARY_COLOUR(Vehicle vehicle, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CUSTOM_SECONDARY_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1, p2, p3 are RGB values for color (255,0,0 for Red, ect)

---

### GET_VEHICLE_CUSTOM_SECONDARY_COLOUR

**Hash:** `0x8389CD56CA8072DC` | **JHash:** `0x3FF247A2` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_CUSTOM_SECONDARY_COLOUR(Vehicle vehicle, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_CUSTOM_SECONDARY_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### CLEAR_VEHICLE_CUSTOM_SECONDARY_COLOUR

**Hash:** `0x5FFBDEEC3E8E2009` | **JHash:** `0x7CE00B29` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_CUSTOM_SECONDARY_COLOUR(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_CUSTOM_SECONDARY_COLOUR, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_IS_VEHICLE_SECONDARY_COLOUR_CUSTOM

**Hash:** `0x910A32E7AAD2656C` | **JHash:** `0x288AD228` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_VEHICLE_SECONDARY_COLOUR_CUSTOM(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_VEHICLE_SECONDARY_COLOUR_CUSTOM, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Check if Vehicle Secondary is avaliable for customize

---

### SET_VEHICLE_ENVEFF_SCALE

**Hash:** `0x3AFDC536C3D01674` | **JHash:** `0x8332730C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ENVEFF_SCALE(Vehicle vehicle, float fade)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ENVEFF_SCALE, vehicle, fade);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `fade` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** The parameter fade is a value from 0-1, where 0 is fresh paint.

---

### GET_VEHICLE_ENVEFF_SCALE

**Hash:** `0xA82819CAC9C4C403` | **JHash:** `0xD5F1EEE1` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_ENVEFF_SCALE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_ENVEFF_SCALE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** The result is a value from 0-1, where 0 is fresh paint.

---

### SET_CAN_RESPRAY_VEHICLE

**Hash:** `0x52BBA29D5EC69356` | **JHash:** `0x37677590` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_RESPRAY_VEHICLE(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_RESPRAY_VEHICLE, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Hardcoded to not work in multiplayer.

---

### SET_GOON_BOSS_VEHICLE

**Hash:** `0xAB31EF4DE6800CE9` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_GOON_BOSS_VEHICLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GOON_BOSS_VEHICLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used for GTAO CEO/Associate spawned vehicles.

---

### SET_OPEN_REAR_DOORS_ON_EXPLOSION

**Hash:** `0x1B212B26DD3C04DF` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_OPEN_REAR_DOORS_ON_EXPLOSION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OPEN_REAR_DOORS_ON_EXPLOSION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_SUBMARINE_SURFACE_MODE

**Hash:** `0x33506883545AC0DF` | **JHash:** `0x54E9EE75` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_SUBMARINE_SURFACE_MODE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_SUBMARINE_SURFACE_MODE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### FORCE_SUBMARINE_NEURTAL_BUOYANCY

**Hash:** `0xC67DB108A9ADE3BE` | **Build:** `2189`

**C++ 原型:**
```cpp
void FORCE_SUBMARINE_NEURTAL_BUOYANCY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_SUBMARINE_NEURTAL_BUOYANCY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_SUBMARINE_CRUSH_DEPTHS

**Hash:** `0xC59872A5134879C7` | **JHash:** `0x4A46E814` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SUBMARINE_CRUSH_DEPTHS(Vehicle vehicle, BOOL p1, float depth1, float depth2, float depth3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SUBMARINE_CRUSH_DEPTHS, vehicle, p1, depth1, depth2, depth3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `depth1` | `float` | `float` | |
| `depth2` | `float` | `float` | |
| `depth3` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_SUBMARINE_IS_UNDER_DESIGN_DEPTH

**Hash:** `0x3E71D0B300B7AA79` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL GET_SUBMARINE_IS_UNDER_DESIGN_DEPTH(Vehicle submarine)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_SUBMARINE_IS_UNDER_DESIGN_DEPTH, submarine);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `submarine` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_SUBMARINE_NUMBER_OF_AIR_LEAKS

**Hash:** `0x093D6DDCA5B8FBAE` | **Build:** `2189`

**C++ 原型:**
```cpp
int GET_SUBMARINE_NUMBER_OF_AIR_LEAKS(Vehicle submarine)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_SUBMARINE_NUMBER_OF_AIR_LEAKS, submarine);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `submarine` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SET_BOAT_IGNORE_LAND_PROBES

**Hash:** `0xED5EDE9E676643C9` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_BOAT_IGNORE_LAND_PROBES(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_IGNORE_LAND_PROBES, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### _SET_BOUNDS_AFFECT_WATER_PROBES

**Hash:** `0x85FC953F6C6CBDE1` | **Build:** `2802`

**C++ 原型:**
```cpp
void _SET_BOUNDS_AFFECT_WATER_PROBES(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_BOUNDS_AFFECT_WATER_PROBES, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Use the vehicle bounds (instead of viewport) when deciding if a vehicle is sufficiently above the water (waterheight.dat), bypassing wave simulation checks

---

### SET_BOAT_ANCHOR

**Hash:** `0x75DBEC174AEEAD10` | **JHash:** `0xA3906284` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BOAT_ANCHOR(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_ANCHOR, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CAN_ANCHOR_BOAT_HERE

**Hash:** `0x26C10ECBDA5D043B` | **JHash:** `0xE97A4F5E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_ANCHOR_BOAT_HERE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_ANCHOR_BOAT_HERE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CAN_ANCHOR_BOAT_HERE_IGNORE_PLAYERS

**Hash:** `0x24F4121D07579880` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL CAN_ANCHOR_BOAT_HERE_IGNORE_PLAYERS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_ANCHOR_BOAT_HERE_IGNORE_PLAYERS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_BOAT_REMAINS_ANCHORED_WHILE_PLAYER_IS_DRIVER

**Hash:** `0xE3EBAAE484798530` | **JHash:** `0x0ED84792` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BOAT_REMAINS_ANCHORED_WHILE_PLAYER_IS_DRIVER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_REMAINS_ANCHORED_WHILE_PLAYER_IS_DRIVER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_FORCE_LOW_LOD_ANCHOR_MODE

**Hash:** `0xB28B1FE5BFADD7F5` | **JHash:** `0xA739012A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_LOW_LOD_ANCHOR_MODE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_LOW_LOD_ANCHOR_MODE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** No observed effect.

---

### SET_BOAT_LOW_LOD_ANCHOR_DISTANCE

**Hash:** `0xE842A9398079BD82` | **JHash:** `0x66FA450C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BOAT_LOW_LOD_ANCHOR_DISTANCE(Vehicle vehicle, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_LOW_LOD_ANCHOR_DISTANCE, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_BOAT_ANCHORED

**Hash:** `0xB0AD1238A709B1A2` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL IS_BOAT_ANCHORED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BOAT_ANCHORED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_BOAT_SINKS_WHEN_WRECKED

**Hash:** `0x8F719973E1445BA2` | **JHash:** `0x35614622` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BOAT_SINKS_WHEN_WRECKED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_SINKS_WHEN_WRECKED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BOAT_WRECKED

**Hash:** `0xBD32E46AA95C1DD2` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_BOAT_WRECKED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_WRECKED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_SIREN

**Hash:** `0xF4924635A19EB37D` | **JHash:** `0x4AC1EFC7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_SIREN(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_SIREN, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Activate siren on vehicle (Only works if the vehicle has a siren).

---

### IS_VEHICLE_SIREN_ON

**Hash:** `0x4C9BF537BE2634B2` | **JHash:** `0x25EB5873` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_SIREN_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_SIREN_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_SIREN_AUDIO_ON

**Hash:** `0xB5CC40FBCB586380` | **JHash:** `0xC9458688` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_SIREN_AUDIO_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_SIREN_AUDIO_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_STRONG

**Hash:** `0x3E8C8727991A8A0B` | **JHash:** `0xC758D19F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STRONG(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STRONG, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If set to true, vehicle will not take crash damage, but is still susceptible to damage from bullets and explosives

---

### REMOVE_VEHICLE_STUCK_CHECK

**Hash:** `0x8386BFB614D06749` | **JHash:** `0x81594917` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_STUCK_CHECK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_STUCK_CHECK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_COLOURS

**Hash:** `0xA19435F193E081AC` | **JHash:** `0x40D82D88` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_COLOURS(Vehicle vehicle, int* colorPrimary, int* colorSecondary)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_COLOURS, vehicle, colorPrimary, colorSecondary);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `colorPrimary` | `int*` | `int*` | |
| `colorSecondary` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_SEAT_FREE

**Hash:** `0x22AC59A870E6A669` | **JHash:** `0xDAF42B02` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_SEAT_FREE(Vehicle vehicle, int seatIndex, BOOL isTaskRunning)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_SEAT_FREE, vehicle, seatIndex, isTaskRunning);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |
| `isTaskRunning` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Check if a vehicle seat is free.

seatIndex  = -1 being the driver seat.
Use GET_VEHICLE_MAX_NUMBER_OF_PASSENGERS(vehicle) - 1 for last seat index.
isTaskRunning = on true the function returns already false while a task on the target seat is running (TASK_ENTER_VEHICLE/TASK_SHUFFLE_TO_NEXT_VEHICLE_SEAT) - on false only when a ped is finally sitting in the seat.

---

### GET_PED_IN_VEHICLE_SEAT

**Hash:** `0xBB40DD2270B65366` | **JHash:** `0x388FDE9A` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PED_IN_VEHICLE_SEAT(Vehicle vehicle, int seatIndex, BOOL p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_IN_VEHICLE_SEAT, vehicle, seatIndex, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `Ped` → `int`

**说明:** If there is no ped in the seat, and the game considers the vehicle as ambient population, this will create a random occupant ped in the seat, which may be cleaned up by the game fairly soon if not marked as script-owned mission entity.

Seat indexes:
-1 = Driver
0 = Front Right Passenger
1 = Back Left Passenger
2 = Back Right Passenger
3 = Further Back Left Passenger (vehicles > 4 seats)
4 = Further Back Right Passenger (vehicles > 4 seats)
etc.

If p2 is true it uses a different GetOccupant function.

---

### GET_LAST_PED_IN_VEHICLE_SEAT

**Hash:** `0x83F969AA1EE2A664` | **JHash:** `0xF7C6792D` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_LAST_PED_IN_VEHICLE_SEAT(Vehicle vehicle, int seatIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LAST_PED_IN_VEHICLE_SEAT, vehicle, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |

**返回值:** `Ped` → `int`

---

### GET_VEHICLE_LIGHTS_STATE

**Hash:** `0xB91B4C20085BD12F` | **JHash:** `0x7C278621` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_LIGHTS_STATE(Vehicle vehicle, BOOL* lightsOn, BOOL* highbeamsOn)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_LIGHTS_STATE, vehicle, lightsOn, highbeamsOn);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `lightsOn` | `BOOL*` | `bool*` | |
| `highbeamsOn` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_TYRE_BURST

**Hash:** `0xBA291848A0815CA9` | **JHash:** `0x48C80210` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_TYRE_BURST(Vehicle vehicle, int wheelID, BOOL completely)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_TYRE_BURST, vehicle, wheelID, completely);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelID` | `int` | `int` | |
| `completely` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** wheelID used for 4 wheelers seem to be (0, 1, 4, 5)
completely - is to check if tire completely gone from rim.

'0 = wheel_lf / bike, plane or jet front
'1 = wheel_rf
'2 = wheel_lm / in 6 wheels trailer, plane or jet is first one on left
'3 = wheel_rm / in 6 wheels trailer, plane or jet is first one on right
'4 = wheel_lr / bike rear / in 6 wheels trailer, plane or jet is last one on left
'5 = wheel_rr / in 6 wheels trailer, plane or jet is last one on right
'45 = 6 wheels trailer mid wheel left
'47 = 6 wheels trailer mid wheel right

---

### SET_VEHICLE_FORWARD_SPEED

**Hash:** `0xAB54A438726D25D5` | **JHash:** `0x69880D14` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FORWARD_SPEED(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FORWARD_SPEED, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** SCALE: Setting the speed to 30 would result in a speed of roughly 60mph, according to speedometer.

Speed is in meters per second
You can convert meters/s to mph here:
http://www.calculateme.com/Speed/MetersperSecond/ToMilesperHour.htm

---

### SET_VEHICLE_FORWARD_SPEED_XY

**Hash:** `0x6501129C9E0FFA05` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_VEHICLE_FORWARD_SPEED_XY(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FORWARD_SPEED_XY, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Seems to be identical to SET_VEHICLE_FORWARD_SPEED

---

### BRING_VEHICLE_TO_HALT

**Hash:** `0x260BE8F09E326A20` | **JHash:** `0xCBC7D3C8` | **Build:** `323`

**C++ 原型:**
```cpp
void BRING_VEHICLE_TO_HALT(Vehicle vehicle, float distance, int duration, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.BRING_VEHICLE_TO_HALT, vehicle, distance, duration, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `distance` | `float` | `float` | |
| `duration` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This native makes the vehicle stop immediately, as happens when we enter a MP garage.

. distance defines how far it will travel until stopping. Garage doors use 3.0.

. If killEngine is set to 1, you cannot resume driving the vehicle once it stops. This looks like is a bitmapped integer.

---

### SET_VEHICLE_STEER_FOR_BUILDINGS

**Hash:** `0xDCE97BDF8A0EABC8` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_STEER_FOR_BUILDINGS(Vehicle vehicle, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STEER_FOR_BUILDINGS, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAUSES_SWERVING

**Hash:** `0x9849DE24FCF23CCC` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAUSES_SWERVING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAUSES_SWERVING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_IGNORE_PLANES_SMALL_PITCH_CHANGE

**Hash:** `0x8664170EF165C4A6` | **Build:** `2189`

**C++ 原型:**
```cpp
void SET_IGNORE_PLANES_SMALL_PITCH_CHANGE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_IGNORE_PLANES_SMALL_PITCH_CHANGE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### STOP_BRINGING_VEHICLE_TO_HALT

**Hash:** `0x7C06330BFDDA182E` | **Build:** `1103`

**C++ 原型:**
```cpp
void STOP_BRINGING_VEHICLE_TO_HALT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_BRINGING_VEHICLE_TO_HALT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Stops CTaskBringVehicleToHalt

---

### IS_VEHICLE_BEING_BROUGHT_TO_HALT

**Hash:** `0xC69BB1D832A710EF` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_BEING_BROUGHT_TO_HALT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_BEING_BROUGHT_TO_HALT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if vehicle is halted by BRING_VEHICLE_TO_HALT

---

### LOWER_FORKLIFT_FORKS

**Hash:** `0x923A293361DF44E5` | **Build:** `2802`

**C++ 原型:**
```cpp
void LOWER_FORKLIFT_FORKS(Vehicle forklift)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOWER_FORKLIFT_FORKS, forklift);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `forklift` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_FORKLIFT_FORK_HEIGHT

**Hash:** `0x37EBBF3117BD6A25` | **JHash:** `0x943A6CFC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORKLIFT_FORK_HEIGHT(Vehicle vehicle, float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORKLIFT_FORK_HEIGHT, vehicle, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 0.0 = Lowest 1.0 = Highest. This is best to be used if you wanna pick-up a car since un-realistically on GTA V forklifts can't pick up much of anything due to vehicle mass. If you put this under a car then set it above 0.0 to a 'lifted-value' it will raise the car with no issue lol

---

### IS_ENTITY_ATTACHED_TO_HANDLER_FRAME

**Hash:** `0x57715966069157AD` | **JHash:** `0x7FB25568` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTITY_ATTACHED_TO_HANDLER_FRAME(Vehicle vehicle, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTITY_ATTACHED_TO_HANDLER_FRAME, vehicle, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ANY_ENTITY_ATTACHED_TO_HANDLER_FRAME

**Hash:** `0x62CA17B74C435651` | **JHash:** `0x593143B9` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_ENTITY_ATTACHED_TO_HANDLER_FRAME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_ENTITY_ATTACHED_TO_HANDLER_FRAME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### FIND_HANDLER_VEHICLE_CONTAINER_IS_ATTACHED_TO

**Hash:** `0x375E7FC44F21C8AB` | **JHash:** `0x70DD5E25` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle FIND_HANDLER_VEHICLE_CONTAINER_IS_ATTACHED_TO(Entity entity)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.FIND_HANDLER_VEHICLE_CONTAINER_IS_ATTACHED_TO, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Finds the vehicle that is carrying this entity with a handler frame.
The model of the entity must be prop_contr_03b_ld or the function will return 0.

---

### IS_HANDLER_FRAME_LINED_UP_WITH_CONTAINER

**Hash:** `0x89D630CF5EA96D23` | **JHash:** `0xFBF5536A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HANDLER_FRAME_LINED_UP_WITH_CONTAINER(Vehicle vehicle, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HANDLER_FRAME_LINED_UP_WITH_CONTAINER, vehicle, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ATTACH_CONTAINER_TO_HANDLER_FRAME_WHEN_LINED_UP

**Hash:** `0x6A98C2ECF57FA5D4` | **JHash:** `0x20AB5783` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_CONTAINER_TO_HANDLER_FRAME_WHEN_LINED_UP(Vehicle vehicle, Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_CONTAINER_TO_HANDLER_FRAME_WHEN_LINED_UP, vehicle, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### DETACH_CONTAINER_FROM_HANDLER_FRAME

**Hash:** `0x7C0043FDFF6436BC` | **JHash:** `0x0F11D01F` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_CONTAINER_FROM_HANDLER_FRAME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_CONTAINER_FROM_HANDLER_FRAME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DISABLE_HEIGHT_MAP_AVOIDANCE

**Hash:** `0x8AA9180DE2FEDD45` | **JHash:** `0xAE040377` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DISABLE_HEIGHT_MAP_AVOIDANCE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DISABLE_HEIGHT_MAP_AVOIDANCE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_PLANE_AVOIDS_OTHERS

**Hash:** `0xBAFB99B304BC52A7` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_PLANE_AVOIDS_OTHERS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_PLANE_AVOIDS_OTHERS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BOAT_DISABLE_AVOIDANCE

**Hash:** `0x0A6A279F3AA4FD70` | **JHash:** `0x4C0E4031` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BOAT_DISABLE_AVOIDANCE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BOAT_DISABLE_AVOIDANCE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_HELI_LANDING_AREA_BLOCKED

**Hash:** `0x634148744F385576` | **JHash:** `0x6346B7CC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HELI_LANDING_AREA_BLOCKED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HELI_LANDING_AREA_BLOCKED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_SHORT_SLOWDOWN_FOR_LANDING

**Hash:** `0x107A473D7A6647A9` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_SHORT_SLOWDOWN_FOR_LANDING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SHORT_SLOWDOWN_FOR_LANDING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Used on helicopters and blimps during the CTaskVehicleLand task. Sets a value on the task to 10f

---

### SET_HELI_TURBULENCE_SCALAR

**Hash:** `0xE6F13851780394DA` | **JHash:** `0xCCB41A55` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HELI_TURBULENCE_SCALAR(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_TURBULENCE_SCALAR, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CAR_BOOT_OPEN

**Hash:** `0xFC40CBF7B90CA77C` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_CAR_BOOT_OPEN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAR_BOOT_OPEN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Initially used in Max Payne 3, that's why we know the name.

---

### SET_VEHICLE_TYRE_BURST

**Hash:** `0xEC6A202EE4960385` | **JHash:** `0x89D28068` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TYRE_BURST(Vehicle vehicle, int index, BOOL onRim, float p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TYRE_BURST, vehicle, index, onRim, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |
| `onRim` | `BOOL` | `bool` | |
| `p3` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** "To burst tyres VEHICLE::SET_VEHICLE_TYRE_BURST(vehicle, 0, true, 1000.0)
to burst all tyres type it 8 times where p1 = 0 to 7.

p3 seems to be how much damage it has taken. 0 doesn't deflate them, 1000 completely deflates them.

'0 = wheel_lf / bike, plane or jet front
'1 = wheel_rf
'2 = wheel_lm / in 6 wheels trailer, plane or jet is first one on left
'3 = wheel_rm / in 6 wheels trailer, plane or jet is first one on right
'4 = wheel_lr / bike rear / in 6 wheels trailer, plane or jet is last one on left
'5 = wheel_rr / in 6 wheels trailer, plane or jet is last one on right
'45 = 6 wheels trailer mid wheel left
'47 = 6 wheels trailer mid wheel right

---

### SET_VEHICLE_DOORS_SHUT

**Hash:** `0x781B3D62BB013EF5` | **JHash:** `0xBB1FF6E7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOORS_SHUT(Vehicle vehicle, BOOL closeInstantly)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOORS_SHUT, vehicle, closeInstantly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `closeInstantly` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Closes all doors of a vehicle:

---

### SET_VEHICLE_TYRES_CAN_BURST

**Hash:** `0xEB9DC3C7D8596C46` | **JHash:** `0xA198DB54` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TYRES_CAN_BURST(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TYRES_CAN_BURST, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Allows you to toggle bulletproof tires.

---

### GET_VEHICLE_TYRES_CAN_BURST

**Hash:** `0x678B9BB8C3F58FEB` | **JHash:** `0x4D76CD2F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_TYRES_CAN_BURST(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_TYRES_CAN_BURST, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_WHEELS_CAN_BREAK

**Hash:** `0x29B18B4FD460CA8F` | **JHash:** `0x829ED654` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WHEELS_CAN_BREAK(Vehicle vehicle, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WHEELS_CAN_BREAK, vehicle, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DOOR_OPEN

**Hash:** `0x7C65DAC73C35C862` | **JHash:** `0xBB75D38B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_OPEN(Vehicle vehicle, int doorId, BOOL loose, BOOL openInstantly)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_OPEN, vehicle, doorId, loose, openInstantly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `loose` | `BOOL` | `bool` | |
| `openInstantly` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_DOOR_AUTO_LOCK

**Hash:** `0x3B458DDB57038F08` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_AUTO_LOCK(Vehicle vehicle, int doorId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_AUTO_LOCK, vehicle, doorId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

Usually used alongside other vehicle door natives.

---

### SET_FLEEING_VEHICLES_USE_SWITCHED_OFF_NODES

**Hash:** `0xA247F9EF01D8082E` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_FLEEING_VEHICLES_USE_SWITCHED_OFF_NODES(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLEEING_VEHICLES_USE_SWITCHED_OFF_NODES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### REMOVE_VEHICLE_WINDOW

**Hash:** `0xA711568EEDB43069` | **JHash:** `0xBB8104A3` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_WINDOW(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_WINDOW, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** windowIndex:
0 = Front Right Window
1 = Front Left Window
2 = Back Right Window
3 = Back Left Window
4 = Unknown
5 = Unknown
6 = Windscreen
7 = Rear Windscreen

---

### ROLL_DOWN_WINDOWS

**Hash:** `0x85796B0549DDE156` | **JHash:** `0x51A16DC6` | **Build:** `323`

**C++ 原型:**
```cpp
void ROLL_DOWN_WINDOWS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROLL_DOWN_WINDOWS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Roll down all the windows of the vehicle passed through the first parameter.

---

### ROLL_DOWN_WINDOW

**Hash:** `0x7AD9E6CE657D69E3` | **JHash:** `0xF840134C` | **Build:** `323`

**C++ 原型:**
```cpp
void ROLL_DOWN_WINDOW(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROLL_DOWN_WINDOW, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** windowIndex:
0 = Front Left Window
1 = Front Right Window
2 = Rear Left Window
3 = Rear Right Window
4 = Front Windscreen
5 = Rear Windscreen
6 = Mid Left
7 = Mid Right
8 = Invalid

---

### ROLL_UP_WINDOW

**Hash:** `0x602E548F46E24D59` | **JHash:** `0x83B7E06A` | **Build:** `323`

**C++ 原型:**
```cpp
void ROLL_UP_WINDOW(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ROLL_UP_WINDOW, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** windowIndex:
0 = Front Left Window
1 = Front Right Window
2 = Rear Left Window
3 = Rear Right Window
4 = Front Windscreen
5 = Rear Windscreen
6 = Mid Left
7 = Mid Right
8 = Invalid

---

### SMASH_VEHICLE_WINDOW

**Hash:** `0x9E5B5E4D2CCD2259` | **JHash:** `0xDDD9A8C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SMASH_VEHICLE_WINDOW(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SMASH_VEHICLE_WINDOW, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** windowIndex:
0 = Front Left Window
1 = Front Right Window
2 = Rear Left Window
3 = Rear Right Window
4 = Front Windscreen
5 = Rear Windscreen
6 = Mid Left
7 = Mid Right
8 = Invalid

---

### FIX_VEHICLE_WINDOW

**Hash:** `0x772282EBEB95E682` | **JHash:** `0x6B8E990D` | **Build:** `323`

**C++ 原型:**
```cpp
void FIX_VEHICLE_WINDOW(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FIX_VEHICLE_WINDOW, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** windowIndex:
0 = Front Left Window
1 = Front Right Window
2 = Rear Left Window
3 = Rear Right Window
4 = Front Windscreen
5 = Rear Windscreen
6 = Mid Left
7 = Mid Right
8 = Invalid

Additional information: FIX_VEHICLE_WINDOW (0x140D0BB88) references an array of bone vehicle indices (0x141D4B3E0) { 2Ah, 2Bh, 2Ch, 2Dh, 2Eh, 2Fh, 28h, 29h } that correspond to: window_lf, window_rf, window_lr, window_rr, window_lm, window_rm, windscreen, windscreen_r. This array is used by most vehwindow natives.

Also, this function is coded to not work on vehicles of type: CBike, Bmx, CBoat, CTrain, and CSubmarine.

---

### POP_OUT_VEHICLE_WINDSCREEN

**Hash:** `0x6D645D59FB5F5AD3` | **JHash:** `0xCC95C96B` | **Build:** `323`

**C++ 原型:**
```cpp
void POP_OUT_VEHICLE_WINDSCREEN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POP_OUT_VEHICLE_WINDSCREEN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Detaches the vehicle's windscreen.
For further information, see : gtaforums.com/topic/859570-glass/#entry1068894566

---

### POP_OFF_VEHICLE_ROOF_WITH_IMPULSE

**Hash:** `0xE38CB9D7D39FDBCC` | **JHash:** `0xFDA7B6CA` | **Build:** `323`

**C++ 原型:**
```cpp
void POP_OFF_VEHICLE_ROOF_WITH_IMPULSE(Vehicle vehicle, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.POP_OFF_VEHICLE_ROOF_WITH_IMPULSE, vehicle, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Pops off the "roof" bone in the direction of the specified offset from the vehicle.

---

### SET_VEHICLE_LIGHTS

**Hash:** `0x34E710FF01247C5A` | **JHash:** `0xE8930226` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_LIGHTS(Vehicle vehicle, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LIGHTS, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** set's if the vehicle has lights or not.
not an on off toggle.
p1 = 0 ;vehicle normal lights, off then lowbeams, then highbeams
p1 = 1 ;vehicle doesn't have lights, always off
p1 = 2 ;vehicle has always on lights
p1 = 3 ;or even larger like 4,5,... normal lights like =1
note1: when using =2 on day it's lowbeam,highbeam
but at night it's lowbeam,lowbeam,highbeam
note2: when using =0 it's affected by day or night for highbeams don't exist in daytime.

---

### SET_VEHICLE_USE_PLAYER_LIGHT_SETTINGS

**Hash:** `0xC45C27EF50F36ADC` | **JHash:** `0x4221E435` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_USE_PLAYER_LIGHT_SETTINGS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USE_PLAYER_LIGHT_SETTINGS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_HEADLIGHT_SHADOWS

**Hash:** `0x1FD09E7390A74D54` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HEADLIGHT_SHADOWS(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HEADLIGHT_SHADOWS, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p1 can be either 0, 1 or 2.

Determines how vehicle lights behave when toggled.

0 = Default (Lights can be toggled between off, normal and high beams)
1 = Lights Disabled (Lights are fully disabled, cannot be toggled)
2 = Always On (Lights can be toggled between normal and high beams)

---

### SET_VEHICLE_ALARM

**Hash:** `0xCDE5E70C1DDB954C` | **JHash:** `0x24877D84` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ALARM(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ALARM, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_VEHICLE_ALARM

**Hash:** `0xB8FF7AB45305C345` | **JHash:** `0x5B451FF7` | **Build:** `323`

**C++ 原型:**
```cpp
void START_VEHICLE_ALARM(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_VEHICLE_ALARM, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_ALARM_ACTIVATED

**Hash:** `0x4319E335B71FFF34` | **JHash:** `0xF2630A4C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_ALARM_ACTIVATED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_ALARM_ACTIVATED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_INTERIORLIGHT

**Hash:** `0xBC2042F090AF6AD3` | **JHash:** `0x9AD1FE1E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_INTERIORLIGHT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_INTERIORLIGHT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_FORCE_INTERIORLIGHT

**Hash:** `0x8821196D91FA2DE5` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_VEHICLE_FORCE_INTERIORLIGHT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FORCE_INTERIORLIGHT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets some bit of vehicle

---

### SET_VEHICLE_LIGHT_MULTIPLIER

**Hash:** `0xB385454F8791F57C` | **JHash:** `0x48039D6A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_LIGHT_MULTIPLIER(Vehicle vehicle, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LIGHT_MULTIPLIER, vehicle, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** multiplier = brightness of head lights.
this value isn't capped afaik.

multiplier = 0.0 no lights
multiplier = 1.0 default game value


---

### ATTACH_VEHICLE_TO_TRAILER

**Hash:** `0x3C7D42D58F770B54` | **JHash:** `0x2133977F` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_VEHICLE_TO_TRAILER(Vehicle vehicle, Vehicle trailer, float radius)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_VEHICLE_TO_TRAILER, vehicle, trailer, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `trailer` | `Vehicle` | `int` | |
| `radius` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ATTACH_VEHICLE_ON_TO_TRAILER

**Hash:** `0x16B5E274BDE402F8` | **JHash:** `0x12AC1A16` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_VEHICLE_ON_TO_TRAILER(Vehicle vehicle, Vehicle trailer, float offsetX, float offsetY, float offsetZ, float coordsX, float coordsY, float coordsZ, float rotationX, float rotationY, float rotationZ, float disableCollisions)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_VEHICLE_ON_TO_TRAILER, vehicle, trailer, offsetX, offsetY, offsetZ, coordsX, coordsY, coordsZ, rotationX, rotationY, rotationZ, disableCollisions);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `trailer` | `Vehicle` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |
| `coordsX` | `float` | `float` | |
| `coordsY` | `float` | `float` | |
| `coordsZ` | `float` | `float` | |
| `rotationX` | `float` | `float` | |
| `rotationY` | `float` | `float` | |
| `rotationZ` | `float` | `float` | |
| `disableCollisions` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This is the proper way of attaching vehicles to the car carrier, it's what Rockstar uses. Video Demo: https://www.youtube.com/watch?v=2lVEIzf7bgo

---

### STABILISE_ENTITY_ATTACHED_TO_HELI

**Hash:** `0x374706271354CB18` | **JHash:** `0x40C4763F` | **Build:** `323`

**C++ 原型:**
```cpp
void STABILISE_ENTITY_ATTACHED_TO_HELI(Vehicle vehicle, Entity entity, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STABILISE_ENTITY_ATTACHED_TO_HELI, vehicle, entity, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `entity` | `Entity` | `int` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DETACH_VEHICLE_FROM_TRAILER

**Hash:** `0x90532EDF0D2BDD86` | **JHash:** `0xB5DBF91D` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_VEHICLE_FROM_TRAILER(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_VEHICLE_FROM_TRAILER, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_ATTACHED_TO_TRAILER

**Hash:** `0xE7CF3C4F9F489F0C` | **JHash:** `0xE142BBCC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_ATTACHED_TO_TRAILER(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_ATTACHED_TO_TRAILER, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_TRAILER_INVERSE_MASS_SCALE

**Hash:** `0x2A8F319B392E7B3F` | **JHash:** `0xE74E85CE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRAILER_INVERSE_MASS_SCALE(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAILER_INVERSE_MASS_SCALE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TRAILER_LEGS_RAISED

**Hash:** `0x95CF53B3D687F9FA` | **JHash:** `0x06C47A6F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRAILER_LEGS_RAISED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAILER_LEGS_RAISED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** in the decompiled scripts, seems to be always called on the vehicle right after being attached to a trailer.

---

### SET_TRAILER_LEGS_LOWERED

**Hash:** `0x878C75C09FBDB942` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_TRAILER_LEGS_LOWERED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAILER_LEGS_LOWERED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TYRE_FIXED

**Hash:** `0x6E13FC662B882D1D` | **JHash:** `0xA42EFA6B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TYRE_FIXED(Vehicle vehicle, int tyreIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TYRE_FIXED, vehicle, tyreIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `tyreIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** tyreIndex = 0 to 4 on normal vehicles

'0 = wheel_lf / bike, plane or jet front
'1 = wheel_rf
'2 = wheel_lm / in 6 wheels trailer, plane or jet is first one on left
'3 = wheel_rm / in 6 wheels trailer, plane or jet is first one on right
'4 = wheel_lr / bike rear / in 6 wheels trailer, plane or jet is last one on left
'5 = wheel_rr / in 6 wheels trailer, plane or jet is last one on right
'45 = 6 wheels trailer mid wheel left
'47 = 6 wheels trailer mid wheel right

---

### SET_VEHICLE_NUMBER_PLATE_TEXT

**Hash:** `0x95A88F0B409CDA47` | **JHash:** `0x400F9556` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NUMBER_PLATE_TEXT(Vehicle vehicle, const char* plateText)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NUMBER_PLATE_TEXT, vehicle, plateText);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `plateText` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Sets a vehicle's license plate text.  8 chars maximum.

Example:
Ped playerPed = PLAYER::PLAYER_PED_ID();
Vehicle veh = PED::GET_VEHICLE_PED_IS_USING(playerPed);
char *plateText = "KING";
VEHICLE::SET_VEHICLE_NUMBER_PLATE_TEXT(veh, plateText);

---

### GET_VEHICLE_NUMBER_PLATE_TEXT

**Hash:** `0x7CE1CCB9B293020E` | **JHash:** `0xE8522D58` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_VEHICLE_NUMBER_PLATE_TEXT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_VEHICLE_NUMBER_PLATE_TEXT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns the license plate text from a vehicle. 8 chars maximum.

---

### GET_NUMBER_OF_VEHICLE_NUMBER_PLATES

**Hash:** `0x4C4D6B2644F458CB` | **JHash:** `0xD24BC1AE` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_VEHICLE_NUMBER_PLATES(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_VEHICLE_NUMBER_PLATES);
```

**返回值:** `int` → `int`

**说明:** Returns the number of *types* of licence plates, enumerated below in SET_VEHICLE_NUMBER_PLATE_TEXT_INDEX.

---

### SET_VEHICLE_NUMBER_PLATE_TEXT_INDEX

**Hash:** `0x9088EB5A43FFB0A1` | **JHash:** `0xA1A1890E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NUMBER_PLATE_TEXT_INDEX(Vehicle vehicle, int plateIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NUMBER_PLATE_TEXT_INDEX, vehicle, plateIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `plateIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Plates:
Blue/White - 0
Yellow/black - 1
Yellow/Blue - 2
Blue/White2 - 3
Blue/White3 - 4
Yankton - 5

---

### GET_VEHICLE_NUMBER_PLATE_TEXT_INDEX

**Hash:** `0xF11BC2DD9A3E7195` | **JHash:** `0x499747B6` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_NUMBER_PLATE_TEXT_INDEX(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_NUMBER_PLATE_TEXT_INDEX, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the PlateType of a vehicle
      Blue_on_White_1 = 3,
      Blue_on_White_2 = 0,
      Blue_on_White_3 = 4,
      Yellow_on_Blue = 2,
       Yellow_on_Black = 1,
      North_Yankton = 5,

---

### SET_RANDOM_TRAINS

**Hash:** `0x80D9F74197EA47D9` | **JHash:** `0xD461CA7F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_TRAINS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_TRAINS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CREATE_MISSION_TRAIN

**Hash:** `0x63C6CCA8E68AE8C8` | **JHash:** `0xD4C2EAFD` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle CREATE_MISSION_TRAIN(int variation, float x, float y, float z, BOOL direction, Any p5, Any p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_MISSION_TRAIN, variation, x, y, z, direction, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `variation` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `direction` | `BOOL` | `bool` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |

**返回值:** `Vehicle` → `int`

**说明:** Train models HAVE TO be loaded (requested) before you use this.
For variation 15 - request:

freight
freightcar
freightgrain
freightcont1
freightcont2
freighttrailer



---

### SWITCH_TRAIN_TRACK

**Hash:** `0xFD813BB7DB977F20` | **JHash:** `0x68BFDD61` | **Build:** `323`

**C++ 原型:**
```cpp
void SWITCH_TRAIN_TRACK(int trackId, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SWITCH_TRAIN_TRACK, trackId, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `trackId` | `int` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles whether ambient trains can spawn on the specified track or not

`trackId` is the internal id of the train track to switch.
`state` is whether ambient trains can spawn or not

trackIds
0 (`trains1.dat`) Main track around SA
1 (`trains2.dat`) Davis Quartz Quarry branch
2 (`trains3.dat`) Second track alongside live track along Roy Lewenstein Blv.
3 (`trains4.dat`) Metro track circuit
4 (`trains5.dat`) Branch in Mirror Park Railyard
5 (`trains6.dat`) Branch in Mirror Park Railyard
6 (`trains7.dat`) LS branch to Mirror Park Railyard
7 (`trains8.dat`) Overground part of metro track along Forum Dr.
8 (`trains9.dat`) Branch to Mirror Park Railyard
9 (`trains10.dat`) Yankton train
10 (`trains11.dat`) Part of metro track near mission row
11 (`trains12.dat`) Yankton prologue mission train
Full list of all train tracks + track nodes by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/traintracks.json

---

### SET_TRAIN_TRACK_SPAWN_FREQUENCY

**Hash:** `0x21973BBF8D17EDFA` | **JHash:** `0xD5774FB7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRAIN_TRACK_SPAWN_FREQUENCY(int trackIndex, int frequency)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAIN_TRACK_SPAWN_FREQUENCY, trackIndex, frequency);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `trackIndex` | `int` | `int` | |
| `frequency` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Only called once inside main_persitant with the parameters p0 = 0, p1 = 120000

trackIndex: 0 - 26
Full list of all train tracks + track nodes by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/traintracks.json

---

### ALLOW_TRAIN_TO_BE_REMOVED_BY_POPULATION

**Hash:** `0x2310A8F9421EBF43` | **Build:** `2372`

**C++ 原型:**
```cpp
void ALLOW_TRAIN_TO_BE_REMOVED_BY_POPULATION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_TRAIN_TO_BE_REMOVED_BY_POPULATION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DELETE_ALL_TRAINS

**Hash:** `0x736A718577F39C7D` | **JHash:** `0x83DE7ABF` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_ALL_TRAINS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_ALL_TRAINS);
```

**返回值:** `void` → `void`

---

### SET_TRAIN_SPEED

**Hash:** `0xAA0BC91BE0B796E3` | **JHash:** `0xDFC35E4D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRAIN_SPEED(Vehicle train, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAIN_SPEED, train, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_TRAIN_CRUISE_SPEED

**Hash:** `0x16469284DB8C62B5` | **JHash:** `0xB507F51D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TRAIN_CRUISE_SPEED(Vehicle train, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAIN_CRUISE_SPEED, train, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_RANDOM_BOATS

**Hash:** `0x84436EC293B1415F` | **JHash:** `0xB505BD89` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RANDOM_BOATS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_BOATS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_RANDOM_BOATS_MP

**Hash:** `0xDA5E12F728DB30CA` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_RANDOM_BOATS_MP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RANDOM_BOATS_MP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_GARBAGE_TRUCKS

**Hash:** `0x2AFD795EEAC8D30D` | **JHash:** `0xD9ABB0FF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GARBAGE_TRUCKS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GARBAGE_TRUCKS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DOES_VEHICLE_HAVE_STUCK_VEHICLE_CHECK

**Hash:** `0x57E4C39DE5EE8470` | **JHash:** `0x5D91D9AC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_HAVE_STUCK_VEHICLE_CHECK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_HAVE_STUCK_VEHICLE_CHECK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Maximum amount of vehicles with vehicle stuck check appears to be 16.

---

### GET_VEHICLE_RECORDING_ID

**Hash:** `0x21543C612379DB3C` | **JHash:** `0x328D601D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_RECORDING_ID(int recording, const char* script)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_RECORDING_ID, recording, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** See REQUEST_VEHICLE_RECORDING

---

### REQUEST_VEHICLE_RECORDING

**Hash:** `0xAF514CABE74CBF15` | **JHash:** `0x91AFEFD9` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_VEHICLE_RECORDING(int recording, const char* script)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_VEHICLE_RECORDING, recording, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** Request the vehicle recording defined by the lowercase format string "%s%03d.yvr". For example, REQUEST_VEHICLE_RECORDING(1, "FBIs1UBER") corresponds to fbis1uber001.yvr.
For all vehicle recording/playback natives, "script" is a common prefix that usually corresponds to the script/mission the recording is used in, "recording" is its int suffix, and "id" (e.g., in native GET_TOTAL_DURATION_OF_VEHICLE_RECORDING_ID) corresponds to a unique identifier within the recording streaming module.
Note that only 24 recordings (hardcoded in multiple places) can ever active at a given time before clobbering begins.

---

### HAS_VEHICLE_RECORDING_BEEN_LOADED

**Hash:** `0x300D614A4C785FC4` | **JHash:** `0xF52CD7F5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_VEHICLE_RECORDING_BEEN_LOADED(int recording, const char* script)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VEHICLE_RECORDING_BEEN_LOADED, recording, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |

**返回值:** `BOOL` → `bool`

**说明:** See REQUEST_VEHICLE_RECORDING

---

### REMOVE_VEHICLE_RECORDING

**Hash:** `0xF1160ACCF98A3FC8` | **JHash:** `0xD3C05B00` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_RECORDING(int recording, const char* script)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_RECORDING, recording, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** See REQUEST_VEHICLE_RECORDING

---

### GET_POSITION_OF_VEHICLE_RECORDING_ID_AT_TIME

**Hash:** `0x92523B76657A517D` | **JHash:** `0xF31973BB` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_POSITION_OF_VEHICLE_RECORDING_ID_AT_TIME(int id, float time)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_POSITION_OF_VEHICLE_RECORDING_ID_AT_TIME, id, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `time` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_POSITION_OF_VEHICLE_RECORDING_AT_TIME

**Hash:** `0xD242728AA6F0FBA2` | **JHash:** `0x7178558D` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_POSITION_OF_VEHICLE_RECORDING_AT_TIME(int recording, float time, const char* script)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_POSITION_OF_VEHICLE_RECORDING_AT_TIME, recording, time, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `time` | `float` | `float` | |
| `script` | `const char*` | `string` | |

**返回值:** `Vector3` → `Vector3`

**说明:** This native does no interpolation between pathpoints. The same position will be returned for all times up to the next pathpoint in the recording.

See REQUEST_VEHICLE_RECORDING

---

### GET_ROTATION_OF_VEHICLE_RECORDING_ID_AT_TIME

**Hash:** `0xF0F2103EFAF8CBA7` | **JHash:** `0x4D1C15C2` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ROTATION_OF_VEHICLE_RECORDING_ID_AT_TIME(int id, float time)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ROTATION_OF_VEHICLE_RECORDING_ID_AT_TIME, id, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |
| `time` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

---

### GET_ROTATION_OF_VEHICLE_RECORDING_AT_TIME

**Hash:** `0x2058206FBE79A8AD` | **JHash:** `0xD96DEC68` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ROTATION_OF_VEHICLE_RECORDING_AT_TIME(int recording, float time, const char* script)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ROTATION_OF_VEHICLE_RECORDING_AT_TIME, recording, time, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `time` | `float` | `float` | |
| `script` | `const char*` | `string` | |

**返回值:** `Vector3` → `Vector3`

**说明:** This native does no interpolation between pathpoints. The same rotation will be returned for all times up to the next pathpoint in the recording.

See REQUEST_VEHICLE_RECORDING

---

### GET_TOTAL_DURATION_OF_VEHICLE_RECORDING_ID

**Hash:** `0x102D125411A7B6E6` | **JHash:** `0x7116785E` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_TOTAL_DURATION_OF_VEHICLE_RECORDING_ID(int id)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TOTAL_DURATION_OF_VEHICLE_RECORDING_ID, id);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `id` | `int` | `int` | |

**返回值:** `float` → `float`

---

### GET_TOTAL_DURATION_OF_VEHICLE_RECORDING

**Hash:** `0x0E48D1C262390950` | **JHash:** `0x5B35EEB7` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_TOTAL_DURATION_OF_VEHICLE_RECORDING(int recording, const char* script)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TOTAL_DURATION_OF_VEHICLE_RECORDING, recording, script);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |

**返回值:** `float` → `float`

**说明:** See REQUEST_VEHICLE_RECORDING

---

### GET_POSITION_IN_RECORDING

**Hash:** `0x2DACD605FC681475` | **JHash:** `0x7DCD644C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_POSITION_IN_RECORDING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_POSITION_IN_RECORDING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Distance traveled in the vehicles current recording.

---

### GET_TIME_POSITION_IN_RECORDING

**Hash:** `0x5746F3A7AB7FE544` | **JHash:** `0xF8C3E4A2` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_TIME_POSITION_IN_RECORDING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TIME_POSITION_IN_RECORDING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Can be used with GET_TOTAL_DURATION_OF_VEHICLE_RECORDING{_ID} to compute a percentage.

---

### START_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0x3F878F92B3A7A071` | **JHash:** `0xCF614CA8` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle, int recording, const char* script, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PLAYBACK_RECORDED_VEHICLE, vehicle, recording, script, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p3 is some flag related to 'trailers' (invokes CVehicle::GetTrailer).

See REQUEST_VEHICLE_RECORDING

---

### START_PLAYBACK_RECORDED_VEHICLE_WITH_FLAGS

**Hash:** `0x7D80FD645D4DA346` | **JHash:** `0x4E721AD2` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PLAYBACK_RECORDED_VEHICLE_WITH_FLAGS(Vehicle vehicle, int recording, const char* script, int flags, int time, int drivingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PLAYBACK_RECORDED_VEHICLE_WITH_FLAGS, vehicle, recording, script, flags, time, drivingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |
| `flags` | `int` | `int` | |
| `time` | `int` | `int` | |
| `drivingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** flags requires further research, e.g., 0x4/0x8 are related to the AI driving task and 0x20 is internally set and interacts with dynamic entity components.

time, often zero and capped at 500, is related to SET_PLAYBACK_TO_USE_AI_TRY_TO_REVERT_BACK_LATER

---

### FORCE_PLAYBACK_RECORDED_VEHICLE_UPDATE

**Hash:** `0x1F2E4E06DEA8992B` | **JHash:** `0x01B91CD0` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_PLAYBACK_RECORDED_VEHICLE_UPDATE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_PLAYBACK_RECORDED_VEHICLE_UPDATE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Often called after START_PLAYBACK_RECORDED_VEHICLE and SKIP_TIME_IN_PLAYBACK_RECORDED_VEHICLE; similar in use to FORCE_ENTITY_AI_AND_ANIMATION_UPDATE.

---

### STOP_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0x54833611C17ABDEA` | **JHash:** `0xAE99C57C` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_PLAYBACK_RECORDED_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### PAUSE_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0x632A689BF42301B1` | **JHash:** `0xCCF54912` | **Build:** `323`

**C++ 原型:**
```cpp
void PAUSE_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PAUSE_PLAYBACK_RECORDED_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### UNPAUSE_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0x8879EE09268305D5` | **JHash:** `0x59060F75` | **Build:** `323`

**C++ 原型:**
```cpp
void UNPAUSE_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.UNPAUSE_PLAYBACK_RECORDED_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_PLAYBACK_GOING_ON_FOR_VEHICLE

**Hash:** `0x1C8A4C2C19E68EEC` | **JHash:** `0x61F7650D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYBACK_GOING_ON_FOR_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYBACK_GOING_ON_FOR_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_PLAYBACK_USING_AI_GOING_ON_FOR_VEHICLE

**Hash:** `0xAEA8FD591FAD4106` | **JHash:** `0x63022C58` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLAYBACK_USING_AI_GOING_ON_FOR_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLAYBACK_USING_AI_GOING_ON_FOR_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_CURRENT_PLAYBACK_FOR_VEHICLE

**Hash:** `0x42BC05C27A946054` | **JHash:** `0xA3F44390` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CURRENT_PLAYBACK_FOR_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_PLAYBACK_FOR_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SKIP_TO_END_AND_STOP_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0xAB8E2EDA0C0A5883` | **JHash:** `0x8DEA18C8` | **Build:** `323`

**C++ 原型:**
```cpp
void SKIP_TO_END_AND_STOP_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SKIP_TO_END_AND_STOP_PLAYBACK_RECORDED_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_PLAYBACK_SPEED

**Hash:** `0x6683AB880E427778` | **JHash:** `0x684E26E4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYBACK_SPEED(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYBACK_SPEED, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### START_PLAYBACK_RECORDED_VEHICLE_USING_AI

**Hash:** `0x29DE5FA52D00428C` | **JHash:** `0x8DE8E24E` | **Build:** `323`

**C++ 原型:**
```cpp
void START_PLAYBACK_RECORDED_VEHICLE_USING_AI(Vehicle vehicle, int recording, const char* script, float speed, int drivingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_PLAYBACK_RECORDED_VEHICLE_USING_AI, vehicle, recording, script, speed, drivingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `recording` | `int` | `int` | |
| `script` | `const char*` | `string` | |
| `speed` | `float` | `float` | |
| `drivingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** AI abides by the provided driving style (e.g., stopping at red lights or waiting behind traffic) while executing the specificed vehicle recording.

FORCE_PLAYBACK_RECORDED_VEHICLE_UPDATE is a related native that deals with the AI physics for such recordings.

---

### SKIP_TIME_IN_PLAYBACK_RECORDED_VEHICLE

**Hash:** `0x9438F7AD68771A20` | **JHash:** `0xCF3EFA4B` | **Build:** `323`

**C++ 原型:**
```cpp
void SKIP_TIME_IN_PLAYBACK_RECORDED_VEHICLE(Vehicle vehicle, float time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SKIP_TIME_IN_PLAYBACK_RECORDED_VEHICLE, vehicle, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `time` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** SET_TIME_POSITION_IN_RECORDING can be emulated by: desired_time - GET_TIME_POSITION_IN_RECORDING(vehicle)

---

### SET_PLAYBACK_TO_USE_AI

**Hash:** `0xA549C3B37EA28131` | **JHash:** `0xB536CCD7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYBACK_TO_USE_AI(Vehicle vehicle, int drivingStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYBACK_TO_USE_AI, vehicle, drivingStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `drivingStyle` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Identical to SET_PLAYBACK_TO_USE_AI_TRY_TO_REVERT_BACK_LATER with 0 as arguments for p1 and p3.

---

### SET_PLAYBACK_TO_USE_AI_TRY_TO_REVERT_BACK_LATER

**Hash:** `0x6E63860BBB190730` | **JHash:** `0x0C8ABAA4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYBACK_TO_USE_AI_TRY_TO_REVERT_BACK_LATER(Vehicle vehicle, int time, int drivingStyle, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYBACK_TO_USE_AI_TRY_TO_REVERT_BACK_LATER, vehicle, time, drivingStyle, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `time` | `int` | `int` | |
| `drivingStyle` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Time is number of milliseconds before reverting, zero for indefinitely.

---

### SET_ADDITIONAL_ROTATION_FOR_RECORDED_VEHICLE_PLAYBACK

**Hash:** `0x5845066D8A1EA7F7` | **JHash:** `0x943A58EB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ADDITIONAL_ROTATION_FOR_RECORDED_VEHICLE_PLAYBACK(Vehicle vehicle, float x, float y, float z, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ADDITIONAL_ROTATION_FOR_RECORDED_VEHICLE_PLAYBACK, vehicle, x, y, z, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK

**Hash:** `0x796A877E459B99EA` | **JHash:** `0x5C9F477C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK(Vehicle vehicle, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK, vehicle, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GLOBAL_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK

**Hash:** `0xFAF2A78061FD9EF4` | **JHash:** `0xCD83C393` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_GLOBAL_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK(Vehicle vehicle, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GLOBAL_POSITION_OFFSET_FOR_RECORDED_VEHICLE_PLAYBACK, vehicle, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_SHOULD_LERP_FROM_AI_TO_FULL_RECORDING

**Hash:** `0x063AE2B2CC273588` | **JHash:** `0x2EF8435C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_SHOULD_LERP_FROM_AI_TO_FULL_RECORDING(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SHOULD_LERP_FROM_AI_TO_FULL_RECORDING, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** A vehicle recording playback flag only used in jewelry_heist

---

### EXPLODE_VEHICLE_IN_CUTSCENE

**Hash:** `0x786A4EB67B01BF0B` | **JHash:** `0xA85207B5` | **Build:** `323`

**C++ 原型:**
```cpp
void EXPLODE_VEHICLE_IN_CUTSCENE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXPLODE_VEHICLE_IN_CUTSCENE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ADD_VEHICLE_STUCK_CHECK_WITH_WARP

**Hash:** `0x2FA9923062DD396C` | **JHash:** `0xC8B789AD` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_VEHICLE_STUCK_CHECK_WITH_WARP(Any p0, float p1, Any p2, BOOL p3, BOOL p4, BOOL p5, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_VEHICLE_STUCK_CHECK_WITH_WARP, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |
| `p2` | `Any` | `object` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |
| `p5` | `BOOL` | `bool` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_MODEL_IS_SUPPRESSED

**Hash:** `0x0FC2D89AC25A5814` | **JHash:** `0x42A08C9B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MODEL_IS_SUPPRESSED(Hash model, BOOL suppressed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MODEL_IS_SUPPRESSED, model, suppressed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |
| `suppressed` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the vehicle stop spawning naturally in traffic. Here's an essential example:

VEHICLE::SET_VEHICLE_MODEL_IS_SUPPRESSED(MISC::GET_HASH_KEY("taco"), true);

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_RANDOM_VEHICLE_IN_SPHERE

**Hash:** `0x386F6CE5BAF6091C` | **JHash:** `0x57216D03` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_RANDOM_VEHICLE_IN_SPHERE(float x, float y, float z, float radius, Hash modelHash, int flags)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_VEHICLE_IN_SPHERE, x, y, z, radius, modelHash, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `flags` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Gets a random vehicle in a sphere at the specified position, of the specified radius.

x: The X-component of the position of the sphere.
y: The Y-component of the position of the sphere.
z: The Z-component of the position of the sphere.
radius: The radius of the sphere. Max is 9999.9004.
modelHash: The vehicle model to limit the selection to. Pass 0 for any model.
flags: The bitwise flags that modifies the behaviour of this function.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_RANDOM_VEHICLE_FRONT_BUMPER_IN_SPHERE

**Hash:** `0xC5574E0AEB86BA68` | **JHash:** `0xDCADEB66` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_RANDOM_VEHICLE_FRONT_BUMPER_IN_SPHERE(float p0, float p1, float p2, float p3, int p4, int p5, int p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_VEHICLE_FRONT_BUMPER_IN_SPHERE, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

---

### GET_RANDOM_VEHICLE_BACK_BUMPER_IN_SPHERE

**Hash:** `0xB50807EABE20A8DC` | **JHash:** `0xD6343F6B` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_RANDOM_VEHICLE_BACK_BUMPER_IN_SPHERE(float p0, float p1, float p2, float p3, int p4, int p5, int p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_RANDOM_VEHICLE_BACK_BUMPER_IN_SPHERE, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `int` | `int` | |
| `p5` | `int` | `int` | |
| `p6` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

---

### GET_CLOSEST_VEHICLE

**Hash:** `0xF73EB622C4F1689B` | **JHash:** `0xD7E26B2C` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_CLOSEST_VEHICLE(float x, float y, float z, float radius, Hash modelHash, int flags)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CLOSEST_VEHICLE, x, y, z, radius, modelHash, flags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `modelHash` | `Hash` | `uint` | |
| `flags` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Example usage
VEHICLE::GET_CLOSEST_VEHICLE(x, y, z, radius, hash, unknown leave at 70) 

x, y, z: Position to get closest vehicle to.
radius: Max radius to get a vehicle.
modelHash: Limit to vehicles with this model. 0 for any.
flags: The bitwise flags altering the function's behaviour.

Does not return police cars or helicopters.

It seems to return police cars for me, does not seem to return helicopters, planes or boats for some reason

Only returns non police cars and motorbikes with the flag set to 70 and modelHash to 0. ModelHash seems to always be 0 when not a modelHash in the scripts, as stated above. 

These flags were found in the b617d scripts: 0,2,4,6,7,23,127,260,2146,2175,12294,16384,16386,20503,32768,67590,67711,98309,100359.
Converted to binary, each bit probably represents a flag as explained regarding another native here: gtaforums.com/topic/822314-guide-driving-styles

Conversion of found flags to binary: https://pastebin.com/kghNFkRi

At exactly 16384 which is 0100000000000000 in binary and 4000 in hexadecimal only planes are returned. 

It's probably more convenient to use worldGetAllVehicles(int *arr, int arrSize) and check the shortest distance yourself and sort if you want by checking the vehicle type with for example VEHICLE::IS_THIS_MODEL_A_BOAT

-------------------------------------------------------------------------

Conclusion: This native is not worth trying to use. Use something like this instead: https://pastebin.com/xiFdXa7h

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_TRAIN_CARRIAGE

**Hash:** `0x08AAFD0814722BC3` | **JHash:** `0x2544E7A6` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_TRAIN_CARRIAGE(Vehicle train, int trailerNumber)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_TRAIN_CARRIAGE, train, trailerNumber);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `trailerNumber` | `int` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Corrected p1. it's basically the 'carriage/trailer number'. So if the train has 3 trailers you'd call the native once with a var or 3 times with 1, 2, 3.

---

### _SET_MAKE_TRAIN_SCAN_FOR_BLOCKING_ENTITIES

**Hash:** `0x559B6073DB7FFFF9` | **Build:** `3717`

**C++ 原型:**
```cpp
void _SET_MAKE_TRAIN_SCAN_FOR_BLOCKING_ENTITIES(Vehicle train, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_MAKE_TRAIN_SCAN_FOR_BLOCKING_ENTITIES, train, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _GET_MISSION_TRAIN_CONFIG_INDEX_BY_NAME

**Hash:** `0x7DFAD92A34F09C00` | **Build:** `3717`

**C++ 原型:**
```cpp
int _GET_MISSION_TRAIN_CONFIG_INDEX_BY_NAME(const char* name)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_MISSION_TRAIN_CONFIG_INDEX_BY_NAME, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `name` | `const char*` | `string` | |

**返回值:** `int` → `int`

---

### IS_MISSION_TRAIN

**Hash:** `0xAD464F2E18836BFC` | **Build:** `2372`

**C++ 原型:**
```cpp
BOOL IS_MISSION_TRAIN(Vehicle train)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_MISSION_TRAIN, train);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DELETE_MISSION_TRAIN

**Hash:** `0x5B76B14AE875C795` | **JHash:** `0x86C9497D` | **Build:** `323`

**C++ 原型:**
```cpp
void DELETE_MISSION_TRAIN(Vehicle* train)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DELETE_MISSION_TRAIN, train);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_MISSION_TRAIN_AS_NO_LONGER_NEEDED

**Hash:** `0xBBE7648349B49BE8` | **JHash:** `0x19808560` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MISSION_TRAIN_AS_NO_LONGER_NEEDED(Vehicle* train, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MISSION_TRAIN_AS_NO_LONGER_NEEDED, train, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle*` | `int*` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1 is always 0

---

### SET_MISSION_TRAIN_COORDS

**Hash:** `0x591CA673AA6AB736` | **JHash:** `0xD6D70803` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_MISSION_TRAIN_COORDS(Vehicle train, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_MISSION_TRAIN_COORDS, train, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### IS_THIS_MODEL_A_BOAT

**Hash:** `0x45A9187928F4B9E3` | **JHash:** `0x10F6085C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_BOAT(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_BOAT, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_JETSKI

**Hash:** `0x9537097412CF75FE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_JETSKI(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_JETSKI, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if model is a boat, then checks for FLAG_IS_JETSKI.

---

### IS_THIS_MODEL_A_PLANE

**Hash:** `0xA0948AB42D7BA0DE` | **JHash:** `0x3B3907BB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_PLANE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_PLANE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_HELI

**Hash:** `0xDCE4334788AF94EA` | **JHash:** `0x8AF7F568` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_HELI(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_HELI, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_CAR

**Hash:** `0x7F6DB52EEFC96DF8` | **JHash:** `0x60E4C22F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_CAR(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_CAR, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** To check if the model is an amphibious car, use IS_THIS_MODEL_AN_AMPHIBIOUS_CAR.

---

### IS_THIS_MODEL_A_TRAIN

**Hash:** `0xAB935175B22E822B` | **JHash:** `0xF87DCFFD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_TRAIN(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_TRAIN, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_BIKE

**Hash:** `0xB50C0B0CEDC6CE84` | **JHash:** `0x7E702CDD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_BIKE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_BIKE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_BICYCLE

**Hash:** `0xBF94DD42F63BDED2` | **JHash:** `0x328E6FF5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_BICYCLE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_BICYCLE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_A_QUADBIKE

**Hash:** `0x39DAC362EE65FA28` | **JHash:** `0xC1625277` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_A_QUADBIKE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_A_QUADBIKE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_AN_AMPHIBIOUS_CAR

**Hash:** `0x633F6F44A537EBB6` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_AN_AMPHIBIOUS_CAR(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_AN_AMPHIBIOUS_CAR, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### IS_THIS_MODEL_AN_AMPHIBIOUS_QUADBIKE

**Hash:** `0xA1A9FC1C76A6730D` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL IS_THIS_MODEL_AN_AMPHIBIOUS_QUADBIKE(Hash model)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_THIS_MODEL_AN_AMPHIBIOUS_QUADBIKE, model);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `model` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### SET_HELI_BLADES_FULL_SPEED

**Hash:** `0xA178472EBB8AE60D` | **JHash:** `0x033A9408` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HELI_BLADES_FULL_SPEED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_BLADES_FULL_SPEED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Equivalent of SET_HELI_BLADES_SPEED(vehicleHandle, 1.0f);

---

### SET_HELI_BLADES_SPEED

**Hash:** `0xFD280B4D7F3ABC4D` | **JHash:** `0x5C7D4EA9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HELI_BLADES_SPEED(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_BLADES_SPEED, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the speed of the helicopter blades in percentage of the full speed.

vehicleHandle: The helicopter.
speed: The speed in percentage, 0.0f being 0% and 1.0f being 100%.

---

### FORCE_SUB_THROTTLE_FOR_TIME

**Hash:** `0x99CAD8E7AFDB60FA` | **JHash:** `0x1128A45B` | **Build:** `323`

**C++ 原型:**
```cpp
void FORCE_SUB_THROTTLE_FOR_TIME(Vehicle vehicle, float p1, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FORCE_SUB_THROTTLE_FOR_TIME, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_BE_TARGETTED

**Hash:** `0x3750146A28097A82` | **JHash:** `0x64B70B1D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_BE_TARGETTED(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_BE_TARGETTED, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** This has not yet been tested - it's just an assumption of what the types could be.

---

### SET_DONT_ALLOW_PLAYER_TO_ENTER_VEHICLE_IF_LOCKED_FOR_PLAYER

**Hash:** `0xDBC631F109350B8C` | **JHash:** `0x486C1280` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DONT_ALLOW_PLAYER_TO_ENTER_VEHICLE_IF_LOCKED_FOR_PLAYER(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DONT_ALLOW_PLAYER_TO_ENTER_VEHICLE_IF_LOCKED_FOR_PLAYER, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_BE_VISIBLY_DAMAGED

**Hash:** `0x4C7028F78FFD3681` | **JHash:** `0xC5D94017` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_BE_VISIBLY_DAMAGED(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_BE_VISIBLY_DAMAGED, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_HAS_UNBREAKABLE_LIGHTS

**Hash:** `0x1AA8A837D2169D94` | **JHash:** `0x009AB49E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HAS_UNBREAKABLE_LIGHTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HAS_UNBREAKABLE_LIGHTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_RESPECTS_LOCKS_WHEN_HAS_DRIVER

**Hash:** `0x2311DD7159F00582` | **JHash:** `0x758C5E2E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_RESPECTS_LOCKS_WHEN_HAS_DRIVER(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_RESPECTS_LOCKS_WHEN_HAS_DRIVER, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_EJECT_PASSENGERS_IF_LOCKED

**Hash:** `0x065D03A9D6B2C6B5` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_EJECT_PASSENGERS_IF_LOCKED(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_EJECT_PASSENGERS_IF_LOCKED, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_DIRT_LEVEL

**Hash:** `0x8F17BC8BA08DA62B` | **JHash:** `0xFD15C065` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_DIRT_LEVEL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_DIRT_LEVEL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Dirt level does not become greater than 15.0

---

### SET_VEHICLE_DIRT_LEVEL

**Hash:** `0x79D3B596FE44EE8B` | **JHash:** `0x2B39128B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DIRT_LEVEL(Vehicle vehicle, float dirtLevel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DIRT_LEVEL, vehicle, dirtLevel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `dirtLevel` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** You can't use values greater than 15.0
Also, R* does (float)(rand() % 15) to get a random dirt level when generating a vehicle.

---

### GET_DOES_VEHICLE_HAVE_DAMAGE_DECALS

**Hash:** `0xBCDC5017D3CE1E9E` | **JHash:** `0xDAC523BC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_DOES_VEHICLE_HAVE_DAMAGE_DECALS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DOES_VEHICLE_HAVE_DAMAGE_DECALS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Appears to return true if the vehicle has any damage, including cosmetically.

---

### IS_VEHICLE_DOOR_FULLY_OPEN

**Hash:** `0x3E933CFF7B111C22` | **JHash:** `0xC2385B6F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_DOOR_FULLY_OPEN(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_DOOR_FULLY_OPEN, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_ENGINE_ON

**Hash:** `0x2497C4717C8B881E` | **JHash:** `0x7FBC86F1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ENGINE_ON(Vehicle vehicle, BOOL value, BOOL instantly, BOOL disableAutoStart)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ENGINE_ON, vehicle, value, instantly, disableAutoStart);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `BOOL` | `bool` | |
| `instantly` | `BOOL` | `bool` | |
| `disableAutoStart` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Starts or stops the engine on the specified vehicle.

vehicle: The vehicle to start or stop the engine on.
value: true to turn the vehicle on; false to turn it off.
instantly: if true, the vehicle will be set to the state immediately; otherwise, the current driver will physically turn on or off the engine.
disableAutoStart: If true, the system will prevent the engine from starting when the player got into it.

---

### SET_VEHICLE_UNDRIVEABLE

**Hash:** `0x8ABA6AF54B942B95` | **JHash:** `0x48D02A4E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_UNDRIVEABLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_UNDRIVEABLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_PROVIDES_COVER

**Hash:** `0x5AFEEDD9BB2899D7` | **JHash:** `0xEFC01CA9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_PROVIDES_COVER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_PROVIDES_COVER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DOOR_CONTROL

**Hash:** `0xF2BFA0430F0A0FCB` | **JHash:** `0x572DD360` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_CONTROL(Vehicle vehicle, int doorId, int speed, float angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_CONTROL, vehicle, doorId, speed, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `speed` | `int` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_DOOR_LATCHED

**Hash:** `0xA5A9653A8D2CAF48` | **JHash:** `0x4EB7BBFC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_LATCHED(Vehicle vehicle, int doorId, BOOL p2, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_LATCHED, vehicle, doorId, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### GET_VEHICLE_DOOR_ANGLE_RATIO

**Hash:** `0xFE3F9C29F7B32BD5` | **JHash:** `0x0E399C26` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_DOOR_ANGLE_RATIO(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_DOOR_ANGLE_RATIO, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### GET_PED_USING_VEHICLE_DOOR

**Hash:** `0x218297BF0CFD853B` | **JHash:** `0x0630101F` | **Build:** `323`

**C++ 原型:**
```cpp
Ped GET_PED_USING_VEHICLE_DOOR(Vehicle vehicle, int doord)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_USING_VEHICLE_DOOR, vehicle, doord);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doord` | `int` | `int` | |

**返回值:** `Ped` → `int`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_DOOR_SHUT

**Hash:** `0x93D9BD300D7789E5` | **JHash:** `0x142606BD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_SHUT(Vehicle vehicle, int doorId, BOOL closeInstantly)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_SHUT, vehicle, doorId, closeInstantly);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `closeInstantly` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** enum eDoorId
{
	VEH_EXT_DOOR_INVALID_ID = -1,
	VEH_EXT_DOOR_DSIDE_F,
	VEH_EXT_DOOR_DSIDE_R,
	VEH_EXT_DOOR_PSIDE_F,
	VEH_EXT_DOOR_PSIDE_R,
	VEH_EXT_BONNET,
	VEH_EXT_BOOT
};

---

### SET_VEHICLE_DOOR_BROKEN

**Hash:** `0xD4D4F6A4AB575A33` | **JHash:** `0x8147FEA7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DOOR_BROKEN(Vehicle vehicle, int doorId, BOOL deleteDoor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DOOR_BROKEN, vehicle, doorId, deleteDoor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `deleteDoor` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_VEHICLE_CAN_BREAK

**Hash:** `0x59BF8C3D52C92F66` | **JHash:** `0x90A810D1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_BREAK(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_BREAK, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DOES_VEHICLE_HAVE_ROOF

**Hash:** `0x8AC862B0B32C5B80` | **JHash:** `0xDB817403` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_HAVE_ROOF(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_HAVE_ROOF, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_REMOVE_AGGRESSIVE_CARJACK_MISSION

**Hash:** `0xC4B3347BD68BD609` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_VEHICLE_REMOVE_AGGRESSIVE_CARJACK_MISSION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_REMOVE_AGGRESSIVE_CARJACK_MISSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_AVOID_PLAYER_VEHICLE_RIOT_VAN_MISSION

**Hash:** `0xD3301660A57C9272` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_VEHICLE_AVOID_PLAYER_VEHICLE_RIOT_VAN_MISSION(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_AVOID_PLAYER_VEHICLE_RIOT_VAN_MISSION, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CARJACK_MISSION_REMOVAL_PARAMETERS

**Hash:** `0xB9562064627FF9DB` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_CARJACK_MISSION_REMOVAL_PARAMETERS(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARJACK_MISSION_REMOVAL_PARAMETERS, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### IS_BIG_VEHICLE

**Hash:** `0x9F243D3919F442FE` | **JHash:** `0x9CDBA8DE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_BIG_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_BIG_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if MF_IS_BIG (strModelFlags 0x8) handling model flag is set.

---

### GET_NUMBER_OF_VEHICLE_COLOURS

**Hash:** `0x3B963160CD65D41E` | **JHash:** `0xF2442EE2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_VEHICLE_COLOURS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_VEHICLE_COLOURS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the total amount of color combinations found in the vehicle's carvariations.meta entry.

---

### SET_VEHICLE_COLOUR_COMBINATION

**Hash:** `0x33E8CD3322E2FE31` | **JHash:** `0xA557AEAD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_COLOUR_COMBINATION(Vehicle vehicle, int colorCombination)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_COLOUR_COMBINATION, vehicle, colorCombination);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `colorCombination` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the selected vehicle's colors to the specified index of the color combination found in the vehicle's carvariations.meta entry.

---

### GET_VEHICLE_COLOUR_COMBINATION

**Hash:** `0x6A842D197F845D56` | **JHash:** `0x77AC1B4C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_COLOUR_COMBINATION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_COLOUR_COMBINATION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the index of the color combination found in the vehicle's carvariations.meta entry.

---

### SET_VEHICLE_XENON_LIGHT_COLOR_INDEX

**Hash:** `0xE41033B25D003A07` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_XENON_LIGHT_COLOR_INDEX(Vehicle vehicle, int colorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_XENON_LIGHT_COLOR_INDEX, vehicle, colorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `colorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** `color`: is the paint index for the vehicle.
Paint index goes from 0 to 12.
Be aware that it only works on xenon lights. Example: https://i.imgur.com/yV3cpG9.png
Full list of all vehicle xenon lights by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleColors.json

---

### GET_VEHICLE_XENON_LIGHT_COLOR_INDEX

**Hash:** `0x3DFF319A831E0CDB` | **Build:** `1604`

**C++ 原型:**
```cpp
int GET_VEHICLE_XENON_LIGHT_COLOR_INDEX(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_XENON_LIGHT_COLOR_INDEX, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns the headlight color index from the vehicle. Value between 0, 12.
Use SET_VEHICLE_XENON_LIGHT_COLOR_INDEX to set the headlights color for the vehicle.
Must enable xenon headlights before it'll take affect.

Returns an int, value between 0-12 or 255 if no color is set.

---

### SET_VEHICLE_IS_CONSIDERED_BY_PLAYER

**Hash:** `0x31B927BBC44156CD` | **JHash:** `0x14413319` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_IS_CONSIDERED_BY_PLAYER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IS_CONSIDERED_BY_PLAYER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Setting this to false, makes the specified vehicle to where if you press Y your character doesn't even attempt the animation to enter the vehicle. Hence it's not considered aka ignored.

---

### SET_VEHICLE_WILL_FORCE_OTHER_VEHICLES_TO_STOP

**Hash:** `0xBE5C1255A1830FF5` | **JHash:** `0xA6D8D7A5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WILL_FORCE_OTHER_VEHICLES_TO_STOP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WILL_FORCE_OTHER_VEHICLES_TO_STOP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_ACT_AS_IF_HAS_SIREN_ON

**Hash:** `0x9BECD4B9FEF3F8A6` | **JHash:** `0xACAB8FF3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ACT_AS_IF_HAS_SIREN_ON(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ACT_AS_IF_HAS_SIREN_ON, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_USE_MORE_RESTRICTIVE_SPAWN_CHECKS

**Hash:** `0x88BC673CA9E0AE99` | **JHash:** `0xF0E5C41D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_USE_MORE_RESTRICTIVE_SPAWN_CHECKS(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USE_MORE_RESTRICTIVE_SPAWN_CHECKS, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_MAY_BE_USED_BY_GOTO_POINT_ANY_MEANS

**Hash:** `0xE851E480B814D4BA` | **JHash:** `0x2F98B4B7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MAY_BE_USED_BY_GOTO_POINT_ANY_MEANS(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MAY_BE_USED_BY_GOTO_POINT_ANY_MEANS, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_RANDOM_VEHICLE_MODEL_IN_MEMORY

**Hash:** `0x055BF0AC0C34F4FD` | **JHash:** `0xE2C45631` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_RANDOM_VEHICLE_MODEL_IN_MEMORY(BOOL p0, Hash* modelHash, int* successIndicator)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_RANDOM_VEHICLE_MODEL_IN_MEMORY, p0, modelHash, successIndicator);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |
| `modelHash` | `Hash*` | `uint*` | |
| `successIndicator` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Not present in the retail version! It's just a nullsub.

p0 always true (except in one case)
successIndicator: 0 if success, -1 if failed

---

### GET_VEHICLE_DOOR_LOCK_STATUS

**Hash:** `0x25BC98A59C2EA962` | **JHash:** `0x0D72CEF2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_DOOR_LOCK_STATUS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_DOOR_LOCK_STATUS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** enum VehicleLockStatus = {
    None = 0,
    Unlocked = 1,
    Locked = 2,
    LockedForPlayer = 3,
    StickPlayerInside = 4, -- Doesn't allow players to exit the vehicle with the exit vehicle key.
    CanBeBrokenInto = 7, -- Can be broken into the car. If the glass is broken, the value will be set to 1
    CanBeBrokenIntoPersist = 8, -- Can be broken into persist
    CannotBeTriedToEnter = 10, -- Cannot be tried to enter (Nothing happens when you press the vehicle enter key).
}

---

### GET_VEHICLE_INDIVIDUAL_DOOR_LOCK_STATUS

**Hash:** `0xCA4AC3EAAE46EC7B` | **Build:** `1103`

**C++ 原型:**
```cpp
int GET_VEHICLE_INDIVIDUAL_DOOR_LOCK_STATUS(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_INDIVIDUAL_DOOR_LOCK_STATUS, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns vehicle door lock state previously set with SET_VEHICLE_INDIVIDUAL_DOORS_LOCKED

---

### IS_VEHICLE_DOOR_DAMAGED

**Hash:** `0xB8E181E559464527` | **JHash:** `0x4999E3C3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_DOOR_DAMAGED(Vehicle veh, int doorID)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_DOOR_DAMAGED, veh, doorID);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `veh` | `Vehicle` | `int` | |
| `doorID` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** doorID starts at 0, not seeming to skip any numbers. Four door vehicles intuitively range from 0 to 3.

---

### SET_DOOR_ALLOWED_TO_BE_BROKEN_OFF

**Hash:** `0x2FA133A4A9D37ED8` | **JHash:** `0x065B92B3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DOOR_ALLOWED_TO_BE_BROKEN_OFF(Vehicle vehicle, int doorId, BOOL isBreakable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DOOR_ALLOWED_TO_BE_BROKEN_OFF, vehicle, doorId, isBreakable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |
| `isBreakable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### IS_VEHICLE_BUMPER_BOUNCING

**Hash:** `0x27B926779DEB502D` | **JHash:** `0xB3A2CC4F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_BUMPER_BOUNCING(Vehicle vehicle, BOOL frontBumper)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_BUMPER_BOUNCING, vehicle, frontBumper);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `frontBumper` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_BUMPER_BROKEN_OFF

**Hash:** `0x468056A6BB6F3846` | **JHash:** `0xAF25C027` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_BUMPER_BROKEN_OFF(Vehicle vehicle, BOOL frontBumper)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_BUMPER_BROKEN_OFF, vehicle, frontBumper);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `frontBumper` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### IS_COP_VEHICLE_IN_AREA_3D

**Hash:** `0x7EEF65D5F153E26A` | **JHash:** `0xFB16C6D1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_COP_VEHICLE_IN_AREA_3D(float x1, float x2, float y1, float y2, float z1, float z2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_COP_VEHICLE_IN_AREA_3D, x1, x2, y1, y2, z1, z2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `z2` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

**说明:** Usage:

public bool isCopInRange(Vector3 Location, float Range)
        {
            return Function.Call<bool>(Hash.IS_COP_PED_IN_AREA_3D, Location.X - Range, Location.Y - Range, Location.Z - Range, Location.X + Range, Location.Y + Range, Location.Z + Range);
        }

---

### IS_VEHICLE_ON_ALL_WHEELS

**Hash:** `0xB104CD1BABF302E2` | **JHash:** `0x10089F8E` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_ON_ALL_WHEELS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_ON_ALL_WHEELS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:**  Public Function isVehicleOnAllWheels(vh As Vehicle) As Boolean
        Return Native.Function.Call(Of Boolean)(Hash.IS_VEHICLE_ON_ALL_WHEELS, vh)
    End Function


---

### GET_VEHICLE_MODEL_VALUE

**Hash:** `0x5873C14A52D74236` | **JHash:** `0x58FEFC3D` | **Build:** `463`

**C++ 原型:**
```cpp
int GET_VEHICLE_MODEL_VALUE(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MODEL_VALUE, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns `nMonetaryValue` from handling.meta for specific model.

---

### GET_VEHICLE_LAYOUT_HASH

**Hash:** `0x28D37D4F71AC5C58` | **JHash:** `0xE0B35187` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_VEHICLE_LAYOUT_HASH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_VEHICLE_LAYOUT_HASH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_IN_VEHICLE_CLIPSET_HASH_FOR_SEAT

**Hash:** `0xA01BC64DD4BFBBAC` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_IN_VEHICLE_CLIPSET_HASH_FOR_SEAT(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_IN_VEHICLE_CLIPSET_HASH_FOR_SEAT, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `Hash` → `uint`

---

### SET_RENDER_TRAIN_AS_DERAILED

**Hash:** `0x317B11A312DF5534` | **JHash:** `0x899D9092` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_RENDER_TRAIN_AS_DERAILED(Vehicle train, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_RENDER_TRAIN_AS_DERAILED, train, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `train` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Makes the train all jumbled up and derailed as it moves on the tracks (though that wont stop it from its normal operations)

---

### SET_VEHICLE_EXTRA_COLOURS

**Hash:** `0x2036F561ADD12E33` | **JHash:** `0x515DB2A0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXTRA_COLOURS(Vehicle vehicle, int pearlescentColor, int wheelColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXTRA_COLOURS, vehicle, pearlescentColor, wheelColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `pearlescentColor` | `int` | `int` | |
| `wheelColor` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** They use the same color indexs as SET_VEHICLE_COLOURS.

---

### GET_VEHICLE_EXTRA_COLOURS

**Hash:** `0x3BC4245933A166F7` | **JHash:** `0x80E4659B` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_EXTRA_COLOURS(Vehicle vehicle, int* pearlescentColor, int* wheelColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_EXTRA_COLOURS, vehicle, pearlescentColor, wheelColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `pearlescentColor` | `int*` | `int*` | |
| `wheelColor` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_EXTRA_COLOUR_5

**Hash:** `0xF40DD601A65F7F19` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXTRA_COLOUR_5(Vehicle vehicle, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXTRA_COLOUR_5, vehicle, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_EXTRA_COLOUR_5

**Hash:** `0x7D1464D472D32136` | **Build:** `505`

**C++ 原型:**
```cpp
void GET_VEHICLE_EXTRA_COLOUR_5(Vehicle vehicle, int* color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_EXTRA_COLOUR_5, vehicle, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `color` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_EXTRA_COLOUR_6

**Hash:** `0x6089CDF6A57F326C` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXTRA_COLOUR_6(Vehicle vehicle, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXTRA_COLOUR_6, vehicle, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_EXTRA_COLOUR_6

**Hash:** `0xB7635E80A5C31BFF` | **Build:** `505`

**C++ 原型:**
```cpp
void GET_VEHICLE_EXTRA_COLOUR_6(Vehicle vehicle, int* color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_EXTRA_COLOUR_6, vehicle, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `color` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### STOP_ALL_GARAGE_ACTIVITY

**Hash:** `0x0F87E938BDF29D66` | **JHash:** `0x17A0BCE5` | **Build:** `323`

**C++ 原型:**
```cpp
void STOP_ALL_GARAGE_ACTIVITY(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.STOP_ALL_GARAGE_ACTIVITY);
```

**返回值:** `void` → `void`

---

### SET_VEHICLE_FIXED

**Hash:** `0x115722B1B9C14C1C` | **JHash:** `0x17469AA1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FIXED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FIXED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** This fixes a vehicle.
If the vehicle's engine's broken then you cannot fix it with this native.

---

### SET_VEHICLE_DEFORMATION_FIXED

**Hash:** `0x953DA1E1B12C0491` | **JHash:** `0xDD2920C8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DEFORMATION_FIXED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DEFORMATION_FIXED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** This fixes the deformation of a vehicle but the vehicle health doesn't improve

---

### SET_VEHICLE_CAN_ENGINE_MISSFIRE

**Hash:** `0x206BC5DC9D1AC70A` | **JHash:** `0x8EACBD13` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_ENGINE_MISSFIRE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_ENGINE_MISSFIRE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_LEAK_OIL

**Hash:** `0x51BB2D88D31A914B` | **JHash:** `0x88F0F7E7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_LEAK_OIL(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_LEAK_OIL, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_LEAK_PETROL

**Hash:** `0x192547247864DFDD` | **JHash:** `0x90D6EE57` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_LEAK_PETROL(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_LEAK_PETROL, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_VEHICLE_PETROL_TANK_FIRES

**Hash:** `0x465BF26AB9684352` | **JHash:** `0xC40192B5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_VEHICLE_PETROL_TANK_FIRES(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_VEHICLE_PETROL_TANK_FIRES, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_VEHICLE_PETROL_TANK_DAMAGE

**Hash:** `0x37C8252A7C92D017` | **JHash:** `0xAD3E05F2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_VEHICLE_PETROL_TANK_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_VEHICLE_PETROL_TANK_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_VEHICLE_ENGINE_FIRES

**Hash:** `0x91A0BD635321F145` | **JHash:** `0x1784BA1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_VEHICLE_ENGINE_FIRES(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_VEHICLE_ENGINE_FIRES, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_LIMIT_SPEED_WHEN_PLAYER_INACTIVE

**Hash:** `0xC50CE861B55EAB8B` | **JHash:** `0x40C323AE` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_LIMIT_SPEED_WHEN_PLAYER_INACTIVE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LIMIT_SPEED_WHEN_PLAYER_INACTIVE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_STOP_INSTANTLY_WHEN_PLAYER_INACTIVE

**Hash:** `0x6EBFB22D646FFC18` | **JHash:** `0x847F1304` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STOP_INSTANTLY_WHEN_PLAYER_INACTIVE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STOP_INSTANTLY_WHEN_PLAYER_INACTIVE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** sfink: sets bit in vehicle's structure, used by maintransition, fm_mission_controller, mission_race and a couple of other scripts. see dissassembly: 
CVehicle *__fastcall sub_140CDAA10(signed int a1, char a2)
{
    CVehicle *result; // rax@1

    result = EntityAsCVehicle(a1);
    if ( result )
    {
        result->field_886 &= 0xEFu;
        result->field_886 |= 16 * (a2 & 1);
    }
    return result;
}

---

### SET_DISABLE_PRETEND_OCCUPANTS

**Hash:** `0x25367DE49D64CF16` | **JHash:** `0xCBD98BA1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_PRETEND_OCCUPANTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_PRETEND_OCCUPANTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REMOVE_VEHICLES_FROM_GENERATORS_IN_AREA

**Hash:** `0x46A1E1A299EC4BBA` | **JHash:** `0x42CC15E0` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLES_FROM_GENERATORS_IN_AREA(float x1, float y1, float z1, float x2, float y2, float z2, Any p6)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLES_FROM_GENERATORS_IN_AREA, x1, y1, z1, x2, y2, z2, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `p6` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_STEER_BIAS

**Hash:** `0x42A8EC77D5150CBE` | **JHash:** `0x7357C1EB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STEER_BIAS(Vehicle vehicle, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STEER_BIAS, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Locks the vehicle's steering to the desired angle, explained below.

Requires to be called onTick. Steering is unlocked the moment the function stops being called on the vehicle.

Steer bias:
-1.0 = full right
0.0 = centered steering
1.0 = full left

---

### IS_VEHICLE_EXTRA_TURNED_ON

**Hash:** `0xD2E6822DBFD6C8BD` | **JHash:** `0x042098B5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_EXTRA_TURNED_ON(Vehicle vehicle, int extraId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_EXTRA_TURNED_ON, vehicle, extraId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `extraId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_EXTRA

**Hash:** `0x7EE3A3C5E4A40CC9` | **JHash:** `0x642D065C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXTRA(Vehicle vehicle, int extraId, BOOL disable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXTRA, vehicle, extraId, disable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `extraId` | `int` | `int` | |
| `disable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Available extraIds are 1-14, however none of the vehicles have extras above 12.

---

### DOES_EXTRA_EXIST

**Hash:** `0x1262D55792428154` | **JHash:** `0x409411CC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_EXTRA_EXIST(Vehicle vehicle, int extraId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_EXTRA_EXIST, vehicle, extraId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `extraId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks via CVehicleModelInfo

---

### IS_EXTRA_BROKEN_OFF

**Hash:** `0x534E36D4DB9ECC5D` | **Build:** `1493`

**C++ 原型:**
```cpp
BOOL IS_EXTRA_BROKEN_OFF(Vehicle vehicle, int extraId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_EXTRA_BROKEN_OFF, vehicle, extraId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `extraId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if specified extra part is broken off. It only works for extras that can break off during collisions, non-breakable extras always return false. Also returns true if the breakable extra is toggled off through script.

---

### SET_CONVERTIBLE_ROOF

**Hash:** `0xF39C4F538B5124C2` | **JHash:** `0xC87B6A51` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONVERTIBLE_ROOF(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONVERTIBLE_ROOF, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOWER_CONVERTIBLE_ROOF

**Hash:** `0xDED51F703D0FA83D` | **JHash:** `0xC5F72EAE` | **Build:** `323`

**C++ 原型:**
```cpp
void LOWER_CONVERTIBLE_ROOF(Vehicle vehicle, BOOL instantlyLower)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOWER_CONVERTIBLE_ROOF, vehicle, instantlyLower);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `instantlyLower` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### RAISE_CONVERTIBLE_ROOF

**Hash:** `0x8F5FB35D7E88FC70` | **JHash:** `0xA4E4CBA3` | **Build:** `323`

**C++ 原型:**
```cpp
void RAISE_CONVERTIBLE_ROOF(Vehicle vehicle, BOOL instantlyRaise)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RAISE_CONVERTIBLE_ROOF, vehicle, instantlyRaise);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `instantlyRaise` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_CONVERTIBLE_ROOF_STATE

**Hash:** `0xF8C397922FC03F41` | **JHash:** `0x1B09714D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_CONVERTIBLE_ROOF_STATE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CONVERTIBLE_ROOF_STATE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** 0 -> up
1 -> lowering down
2 -> down
3 -> raising up

---

### IS_VEHICLE_A_CONVERTIBLE

**Hash:** `0x52F357A30698BCCE` | **JHash:** `0x6EF54490` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_A_CONVERTIBLE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_A_CONVERTIBLE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle has a convertible roof.

p1 is false almost always. However, in launcher_carwash/carwash1/carwash2 scripts, p1 is true and is accompanied by DOES_VEHICLE_HAVE_ROOF. If p1 is true, it seems that every single vehicle will return true irrespective of being a convertible.

---

### TRANSFORM_TO_SUBMARINE

**Hash:** `0xBE4C854FFDB6EEBE` | **JHash:** `0xCAFE5FE0` | **Build:** `1365`

**C++ 原型:**
```cpp
BOOL TRANSFORM_TO_SUBMARINE(Vehicle vehicle, BOOL noAnimation)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TRANSFORM_TO_SUBMARINE, vehicle, noAnimation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `noAnimation` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Transforms the `stormberg`/`toreador` to its "submarine" variant. If the vehicle is already in that state then the vehicle transformation audio will still play, but the vehicle won't change at all.

---

### TRANSFORM_TO_CAR

**Hash:** `0x2A69FFD1B42BFF9E` | **JHash:** `0xD8B90941` | **Build:** `1290`

**C++ 原型:**
```cpp
void TRANSFORM_TO_CAR(Vehicle vehicle, BOOL noAnimation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRANSFORM_TO_CAR, vehicle, noAnimation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `noAnimation` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Transforms the `stormberg`/`toreador` to its "road vehicle" variant. If the vehicle is already in that state then the vehicle transformation audio will still play, but the vehicle won't change at all.

---

### IS_VEHICLE_IN_SUBMARINE_MODE

**Hash:** `0xA77DC70BD689A1E5` | **JHash:** `0xE2FF06DB` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_IN_SUBMARINE_MODE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_IN_SUBMARINE_MODE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_STOPPED_AT_TRAFFIC_LIGHTS

**Hash:** `0x2959F696AE390A99` | **JHash:** `0x69200FA4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_STOPPED_AT_TRAFFIC_LIGHTS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_STOPPED_AT_TRAFFIC_LIGHTS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_DAMAGE

**Hash:** `0xA1DD317EA8FD4F29` | **JHash:** `0x21B458B2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DAMAGE(Vehicle vehicle, float xOffset, float yOffset, float zOffset, float damage, float radius, BOOL focusOnModel)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DAMAGE, vehicle, xOffset, yOffset, zOffset, damage, radius, focusOnModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `damage` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `focusOnModel` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Apply damage to vehicle at a location. Location is relative to vehicle model (not world).

Radius of effect damage applied in a sphere at impact location
When `focusOnModel` set to `true`, the damage sphere will travel towards the vehicle from the given point, thus guaranteeing an impact

---

### SET_VEHICLE_OCCUPANTS_TAKE_EXPLOSIVE_DAMAGE

**Hash:** `0x35BB21DE06784373` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_VEHICLE_OCCUPANTS_TAKE_EXPLOSIVE_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_OCCUPANTS_TAKE_EXPLOSIVE_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_ENGINE_HEALTH

**Hash:** `0xC45D23BAF168AAB8` | **JHash:** `0x8880038A` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_ENGINE_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_ENGINE_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Returns 1000.0 if the function is unable to get the address of the specified vehicle or if it's not a vehicle.

Minimum: -4000
Maximum: 1000

-4000: Engine is destroyed
0 and below: Engine catches fire and health rapidly declines
300: Engine is smoking and losing functionality
1000: Engine is perfect

---

### SET_VEHICLE_ENGINE_HEALTH

**Hash:** `0x45F6D8EEF34ABEF1` | **JHash:** `0x1B760FB5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ENGINE_HEALTH(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ENGINE_HEALTH, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 1000 is max health
Begins leaking gas at around 650 health
Minimum: -4000
Maximum: 1000

-4000: Engine is destroyed
0 and below: Engine catches fire and health rapidly declines
300: Engine is smoking and losing functionality
1000: Engine is perfect

---

### SET_PLANE_ENGINE_HEALTH

**Hash:** `0x2A86A0475B6A1434` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_PLANE_ENGINE_HEALTH(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLANE_ENGINE_HEALTH, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Works just like SET_VEHICLE_ENGINE_HEALTH, but only for planes.

---

### GET_VEHICLE_PETROL_TANK_HEALTH

**Hash:** `0x7D5DABE888D2D074` | **JHash:** `0xE41595CE` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_PETROL_TANK_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_PETROL_TANK_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** 1000 is max health
Begins leaking gas at around 650 health
-999.90002441406 appears to be minimum health, although nothing special occurs

---

### SET_VEHICLE_PETROL_TANK_HEALTH

**Hash:** `0x70DB57649FA8D0D8` | **JHash:** `0x660A3692` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_PETROL_TANK_HEALTH(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_PETROL_TANK_HEALTH, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** 1000 is max health
Begins leaking gas at around 650 health
-999.90002441406 appears to be minimum health, although nothing special occurs

---

### IS_VEHICLE_STUCK_TIMER_UP

**Hash:** `0x679BE1DAF71DA874` | **JHash:** `0x2FCF58C1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_STUCK_TIMER_UP(Vehicle vehicle, int p1, int ms)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_STUCK_TIMER_UP, vehicle, p1, ms);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |
| `ms` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 can be anywhere from 0 to 3 in the scripts.
p2 being how long in milliseconds the vehicle has been stuck

---

### RESET_VEHICLE_STUCK_TIMER

**Hash:** `0xD7591B0065AFAA7A` | **JHash:** `0xEF2A6016` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_VEHICLE_STUCK_TIMER(Vehicle vehicle, int nullAttributes)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_VEHICLE_STUCK_TIMER, vehicle, nullAttributes);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `nullAttributes` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** The inner function has a switch on the second parameter. It's the stuck timer index.

Here's some pseudo code I wrote for the inner function:
void __fastcall NATIVE_RESET_VEHICLE_STUCK_TIMER_INNER(CUnknown* unknownClassInVehicle, int timerIndex)
{
 switch (timerIndex)
   {
 case 0:
       unknownClassInVehicle->FirstStuckTimer = (WORD)0u;
 case 1:
       unknownClassInVehicle->SecondStuckTimer = (WORD)0u;
    case 2:
       unknownClassInVehicle->ThirdStuckTimer = (WORD)0u;
 case 3:
       unknownClassInVehicle->FourthStuckTimer = (WORD)0u;
    case 4:
       unknownClassInVehicle->FirstStuckTimer = (WORD)0u;
     unknownClassInVehicle->SecondStuckTimer = (WORD)0u;
        unknownClassInVehicle->ThirdStuckTimer = (WORD)0u;
     unknownClassInVehicle->FourthStuckTimer = (WORD)0u;
        break;
    };
}

---

### IS_VEHICLE_DRIVEABLE

**Hash:** `0x4C241E39B23DF959` | **JHash:** `0x41A7267A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_DRIVEABLE(Vehicle vehicle, BOOL isOnFireCheck)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_DRIVEABLE, vehicle, isOnFireCheck);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `isOnFireCheck` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p1 is always 0 in the scripts.

p1 = check if vehicle is on fire

---

### SET_VEHICLE_HAS_BEEN_OWNED_BY_PLAYER

**Hash:** `0x2B5F9D2AF1F1722D` | **JHash:** `0xB4D3DBFB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HAS_BEEN_OWNED_BY_PLAYER(Vehicle vehicle, BOOL owned)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HAS_BEEN_OWNED_BY_PLAYER, vehicle, owned);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `owned` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_NEEDS_TO_BE_HOTWIRED

**Hash:** `0xFBA550EA44404EE6` | **JHash:** `0xD8260751` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NEEDS_TO_BE_HOTWIRED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NEEDS_TO_BE_HOTWIRED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_BLIP_THROTTLE_RANDOMLY

**Hash:** `0x9F3F689B814F2599` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BLIP_THROTTLE_RANDOMLY(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BLIP_THROTTLE_RANDOMLY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_POLICE_FOCUS_WILL_TRACK_VEHICLE

**Hash:** `0x4E74E62E0A97E901` | **JHash:** `0x5690F6C3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_POLICE_FOCUS_WILL_TRACK_VEHICLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_POLICE_FOCUS_WILL_TRACK_VEHICLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### START_VEHICLE_HORN

**Hash:** `0x9C8C6504B5B63D2C` | **JHash:** `0x0DF5ADB3` | **Build:** `323`

**C++ 原型:**
```cpp
void START_VEHICLE_HORN(Vehicle vehicle, int duration, Hash mode, BOOL forever)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.START_VEHICLE_HORN, vehicle, duration, mode, forever);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `duration` | `int` | `int` | |
| `mode` | `Hash` | `uint` | |
| `forever` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sounds the horn for the specified vehicle.

vehicle: The vehicle to activate the horn for.
mode: The hash of "NORMAL" or "HELDDOWN". Can be 0.
duration: The duration to sound the horn, in milliseconds.

Note: If a player is in the vehicle, it will only sound briefly.

---

### SET_VEHICLE_IN_CAR_MOD_SHOP

**Hash:** `0x9D44FCCE98450843` | **JHash:** `0x968E5770` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_IN_CAR_MOD_SHOP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IN_CAR_MOD_SHOP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If set to TRUE, it seems to suppress door noises and doesn't allow the horn to be continuous.

---

### SET_VEHICLE_HAS_STRONG_AXLES

**Hash:** `0x92F0CF722BC4202F` | **JHash:** `0x0D1CBC65` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HAS_STRONG_AXLES(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HAS_STRONG_AXLES, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** if true, axles won't bend.

---

### GET_DISPLAY_NAME_FROM_VEHICLE_MODEL

**Hash:** `0xB215AAC32D25D019` | **JHash:** `0xEC86DF39` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_DISPLAY_NAME_FROM_VEHICLE_MODEL(Hash modelHash)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_DISPLAY_NAME_FROM_VEHICLE_MODEL, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `const char*` → `string`

**说明:** Returns model name of vehicle in all caps. Needs to be displayed through localizing text natives to get proper display name.
-----------------------------------------------------------------------------------------------------------------------------------------
While often the case, this does not simply return the model name of the vehicle (which could be hashed to return the model hash). Variations of the same vehicle may also use the same display name.
-----------------------------------------------------------------------------------------------------------------------------------------

Returns "CARNOTFOUND" if the hash doesn't match a vehicle hash.

Using HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION, you can get the localized name.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_MAKE_NAME_FROM_VEHICLE_MODEL

**Hash:** `0xF7AF4F159FF99F97` | **Build:** `1868`

**C++ 原型:**
```cpp
const char* GET_MAKE_NAME_FROM_VEHICLE_MODEL(Hash modelHash)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_MAKE_NAME_FROM_VEHICLE_MODEL, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `const char*` → `string`

**说明:** Will return a vehicle's manufacturer display label.
Returns "CARNOTFOUND" if the hash doesn't match a vehicle hash.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_DEFORMATION_AT_POS

**Hash:** `0x4EC6CFBC7B2E9536` | **JHash:** `0xABF02075` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_VEHICLE_DEFORMATION_AT_POS(Vehicle vehicle, float offsetX, float offsetY, float offsetZ)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_VEHICLE_DEFORMATION_AT_POS, vehicle, offsetX, offsetY, offsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `offsetX` | `float` | `float` | |
| `offsetY` | `float` | `float` | |
| `offsetZ` | `float` | `float` | |

**返回值:** `Vector3` → `Vector3`

**说明:** The only example I can find of this function in the scripts, is this:

struct _s = VEHICLE::GET_VEHICLE_DEFORMATION_AT_POS(rPtr((A_0) + 4), 1.21f, 6.15f, 0.3f);

-----------------------------------------------------------------------------------------------------------------------------------------
PC scripts:

v_5/*{3}*/ = VEHICLE::GET_VEHICLE_DEFORMATION_AT_POS(a_0._f1, 1.21, 6.15, 0.3);

---

### SET_VEHICLE_LIVERY

**Hash:** `0x60BF608F1B8CD1B6` | **JHash:** `0x7AD87059` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_LIVERY(Vehicle vehicle, int livery)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LIVERY, vehicle, livery);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `livery` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_LIVERY

**Hash:** `0x2BB9230590DA5E8A` | **JHash:** `0xEC82A51D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_LIVERY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_LIVERY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** -1 = no livery

---

### GET_VEHICLE_LIVERY_COUNT

**Hash:** `0x87B63E25A529D526` | **JHash:** `0xFB0CA947` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_LIVERY_COUNT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_LIVERY_COUNT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns -1 if the vehicle has no livery

---

### SET_VEHICLE_LIVERY2

**Hash:** `0xA6D3A8750DC73270` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_VEHICLE_LIVERY2(Vehicle vehicle, int livery)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LIVERY2, vehicle, livery);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `livery` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Used to set the secondary livery (the roof on Tornado Custom being one such example.)

Livery value is dependent on the amount of liveries present in the vehicle's texture dictionary, for Tornado Custom this would be 0-6.

---

### GET_VEHICLE_LIVERY2

**Hash:** `0x60190048C0764A26` | **Build:** `505`

**C++ 原型:**
```cpp
int GET_VEHICLE_LIVERY2(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_LIVERY2, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns index of the current vehicle's secondary livery. A getter for SET_VEHICLE_LIVERY2.

---

### GET_VEHICLE_LIVERY2_COUNT

**Hash:** `0x5ECB40269053C0D4` | **Build:** `505`

**C++ 原型:**
```cpp
int GET_VEHICLE_LIVERY2_COUNT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_LIVERY2_COUNT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns a number of available secondary liveries, or -1 if vehicle has no secondary liveries available.

---

### IS_VEHICLE_WINDOW_INTACT

**Hash:** `0x46E571A0E20D01F1` | **JHash:** `0xAC4EF23D` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_WINDOW_INTACT(Vehicle vehicle, int windowIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_WINDOW_INTACT, vehicle, windowIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `windowIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This will return false if the window is broken, or rolled down.
Window indexes:
0 = Front Right Window
1 = Front Left Window
2 = Back Right Window
3 = Back Left Window


Those numbers go on for vehicles that have more than 4 doors with windows.

---

### ARE_ALL_VEHICLE_WINDOWS_INTACT

**Hash:** `0x11D862A3E977A9EF` | **JHash:** `0xBB619744` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ALL_VEHICLE_WINDOWS_INTACT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ALL_VEHICLE_WINDOWS_INTACT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Appears to return false if any window is broken.

---

### ARE_ANY_VEHICLE_SEATS_FREE

**Hash:** `0x2D34FC3BC4ADB780` | **JHash:** `0x648E685A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_ANY_VEHICLE_SEATS_FREE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_ANY_VEHICLE_SEATS_FREE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns false if every seat is occupied.

---

### RESET_VEHICLE_WHEELS

**Hash:** `0x21D2E5662C1F6FED` | **JHash:** `0xD5FFE779` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_VEHICLE_WHEELS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_VEHICLE_WHEELS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_HELI_PART_BROKEN

**Hash:** `0xBC74B4BE25EB6C8A` | **JHash:** `0xF4E4C439` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_HELI_PART_BROKEN(Vehicle vehicle, BOOL p1, BOOL p2, BOOL p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_HELI_PART_BROKEN, vehicle, p1, p2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### GET_HELI_MAIN_ROTOR_HEALTH

**Hash:** `0xE4CB7541F413D2C5` | **JHash:** `0xF01E2AAB` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_HELI_MAIN_ROTOR_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_HELI_MAIN_ROTOR_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Max 1000.
At 0 the main rotor will stall.

---

### GET_HELI_TAIL_ROTOR_HEALTH

**Hash:** `0xAE8CE82A4219AC8C` | **JHash:** `0xA41BC13D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_HELI_TAIL_ROTOR_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_HELI_TAIL_ROTOR_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Max 1000.
At 0 the tail rotor will stall.

---

### GET_HELI_TAIL_BOOM_HEALTH

**Hash:** `0xAC51915D27E4A5F7` | **JHash:** `0x8A68388F` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_HELI_TAIL_BOOM_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_HELI_TAIL_BOOM_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Max 1000.
At -100 both helicopter rotors will stall.

---

### SET_HELI_MAIN_ROTOR_HEALTH

**Hash:** `0x4056EA1105F5ABD7` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_HELI_MAIN_ROTOR_HEALTH(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_MAIN_ROTOR_HEALTH, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_HELI_TAIL_ROTOR_HEALTH

**Hash:** `0xFE205F38AAA58E5B` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_HELI_TAIL_ROTOR_HEALTH(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_TAIL_ROTOR_HEALTH, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_HELI_TAIL_BOOM_CAN_BREAK_OFF

**Hash:** `0x3EC8BF18AA453FE9` | **JHash:** `0x2916D69B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_HELI_TAIL_BOOM_CAN_BREAK_OFF(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_HELI_TAIL_BOOM_CAN_BREAK_OFF, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_NAME_DEBUG

**Hash:** `0xBFDF984E2C22B94F` | **JHash:** `0xA712FF5C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NAME_DEBUG(Vehicle vehicle, const char* name)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NAME_DEBUG, vehicle, name);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `name` | `const char*` | `string` | |

**返回值:** `void` → `void`

**说明:** NOTE: Debugging functions are not present in the retail version of the game.

---

### SET_VEHICLE_EXPLODES_ON_HIGH_EXPLOSION_DAMAGE

**Hash:** `0x71B0892EC081D60A` | **JHash:** `0x38CC692B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXPLODES_ON_HIGH_EXPLOSION_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXPLODES_ON_HIGH_EXPLOSION_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a vehicle to be strongly resistant to explosions. p0 is the vehicle; set p1 to false to toggle the effect on/off.

---

### SET_VEHICLE_EXPLODES_ON_EXPLOSION_DAMAGE_AT_ZERO_BODY_HEALTH

**Hash:** `0xD565F438137F0E10` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXPLODES_ON_EXPLOSION_DAMAGE_AT_ZERO_BODY_HEALTH(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXPLODES_ON_EXPLOSION_DAMAGE_AT_ZERO_BODY_HEALTH, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ALLOW_VEHICLE_EXPLODES_ON_CONTACT

**Hash:** `0x3441CAD2F2231923` | **JHash:** `0xC306A9A3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ALLOW_VEHICLE_EXPLODES_ON_CONTACT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_VEHICLE_EXPLODES_ON_CONTACT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DISABLE_TOWING

**Hash:** `0x2B6747FAA9DB9D6B` | **JHash:** `0x95A9ACCB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DISABLE_TOWING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DISABLE_TOWING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_HAS_LANDING_GEAR

**Hash:** `0xE43701C36CAFF1A4` | **Build:** `1180`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_HAS_LANDING_GEAR(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_HAS_LANDING_GEAR, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CONTROL_LANDING_GEAR

**Hash:** `0xCFC8BE9A5E1FE575` | **JHash:** `0x24F873FB` | **Build:** `323`

**C++ 原型:**
```cpp
void CONTROL_LANDING_GEAR(Vehicle vehicle, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CONTROL_LANDING_GEAR, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Works for vehicles with a retractable landing gear

Landing gear states:

0: Deployed
1: Closing
2: Opening
3: Retracted

---

### GET_LANDING_GEAR_STATE

**Hash:** `0x9B0F3DCA3DB0F4CD` | **JHash:** `0xA6F02670` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_LANDING_GEAR_STATE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LANDING_GEAR_STATE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Landing gear states:

0: Deployed
1: Closing (Retracting)
2: (Landing gear state 2 is never used.)
3: Opening (Deploying)
4: Retracted

Returns the current state of the vehicles landing gear.

---

### IS_ANY_VEHICLE_NEAR_POINT

**Hash:** `0x61E1DD6125A3EEE6` | **JHash:** `0x2867A834` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_VEHICLE_NEAR_POINT(float x, float y, float z, float radius)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_VEHICLE_NEAR_POINT, x, y, z, radius);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_VEHICLE_HIGH_DETAIL_MODEL

**Hash:** `0xA6E9FDCB2C76785E` | **JHash:** `0x9DA21956` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_VEHICLE_HIGH_DETAIL_MODEL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_VEHICLE_HIGH_DETAIL_MODEL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### _GET_VEHICLE_MODEL_NUM_DRIVE_GEARS

**Hash:** `0x61F02E4E9A7A61EA` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_VEHICLE_MODEL_NUM_DRIVE_GEARS(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_MODEL_NUM_DRIVE_GEARS, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### _GET_VEHICLE_MAX_DRIVE_GEAR_COUNT

**Hash:** `0x24910C3D66BA770D` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_VEHICLE_MAX_DRIVE_GEAR_COUNT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_MAX_DRIVE_GEAR_COUNT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### _GET_IS_VEHICLE_ELECTRIC

**Hash:** `0x1FCB07FE230B6639` | **Build:** `3258`

**C++ 原型:**
```cpp
BOOL _GET_IS_VEHICLE_ELECTRIC(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._GET_IS_VEHICLE_ELECTRIC, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### _GET_VEHICLE_DRIVETRAIN_TYPE

**Hash:** `0x1423725069EE1D14` | **Build:** `3258`

**C++ 原型:**
```cpp
int _GET_VEHICLE_DRIVETRAIN_TYPE(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_DRIVETRAIN_TYPE, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns the vehicle's drivetrain type.

enum eVehicleDrivetrainType
{
	VehicleDrivetrainType_INVALID,
	VehicleDrivetrainType_FWD,
	VehicleDrivetrainType_RWD,
	VehicleDrivetrainType_AWD
};

_GET_VEHICLE_DE* - _GET_VEHICLE_DI*

---

### _GET_VEHICLE_CURRENT_DRIVE_GEAR

**Hash:** `0x56185A25D45A0DCD` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_VEHICLE_CURRENT_DRIVE_GEAR(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_CURRENT_DRIVE_GEAR, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### _GET_VEHICLE_CURRENT_REV_RATIO

**Hash:** `0xF9DDA40BC293A61E` | **Build:** `3095`

**C++ 原型:**
```cpp
float _GET_VEHICLE_CURRENT_REV_RATIO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_VEHICLE_CURRENT_REV_RATIO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### REMOVE_VEHICLE_HIGH_DETAIL_MODEL

**Hash:** `0x00689CDE5F7C6787` | **JHash:** `0x382BE070` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_HIGH_DETAIL_MODEL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_HIGH_DETAIL_MODEL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_HIGH_DETAIL

**Hash:** `0x1F25887F3C104278` | **JHash:** `0x55D41928` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_HIGH_DETAIL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_HIGH_DETAIL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_VEHICLE_ASSET

**Hash:** `0x81A15811460FAB3A` | **JHash:** `0x902B4F06` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_VEHICLE_ASSET(Hash vehicleHash, int vehicleAsset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_VEHICLE_ASSET, vehicleHash, vehicleAsset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleHash` | `Hash` | `uint` | |
| `vehicleAsset` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** REQUEST_VEHICLE_ASSET(GET_HASH_KEY(cargobob3), 3);

vehicle found that have asset's:
cargobob3
submersible
blazer

---

### HAS_VEHICLE_ASSET_LOADED

**Hash:** `0x1BBE0523B8DB9A21` | **JHash:** `0x8DAAC3CB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_VEHICLE_ASSET_LOADED(int vehicleAsset)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VEHICLE_ASSET_LOADED, vehicleAsset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleAsset` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_VEHICLE_ASSET

**Hash:** `0xACE699C71AB9DEB5` | **JHash:** `0x9620E9C6` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_ASSET(int vehicleAsset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_ASSET, vehicleAsset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleAsset` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TOW_TRUCK_ARM_POSITION

**Hash:** `0xFE54B92A344583CA` | **JHash:** `0x88236E22` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TOW_TRUCK_ARM_POSITION(Vehicle vehicle, float position)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TOW_TRUCK_ARM_POSITION, vehicle, position);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `position` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets how much the crane on the tow truck is raised, where 0.0 is fully lowered and 1.0 is fully raised.

---

### _SET_ATTACHED_VEHICLE_TO_TOW_TRUCK_ARM

**Hash:** `0x48BD57D0DD17786A` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_ATTACHED_VEHICLE_TO_TOW_TRUCK_ARM(Vehicle towTruck, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_ATTACHED_VEHICLE_TO_TOW_TRUCK_ARM, towTruck, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `towTruck` | `Vehicle` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### ATTACH_VEHICLE_TO_TOW_TRUCK

**Hash:** `0x29A16F8D621C4508` | **JHash:** `0x8151571A` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_VEHICLE_TO_TOW_TRUCK(Vehicle towTruck, Vehicle vehicle, BOOL rear, float hookOffsetX, float hookOffsetY, float hookOffsetZ)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_VEHICLE_TO_TOW_TRUCK, towTruck, vehicle, rear, hookOffsetX, hookOffsetY, hookOffsetZ);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `towTruck` | `Vehicle` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `rear` | `BOOL` | `bool` | |
| `hookOffsetX` | `float` | `float` | |
| `hookOffsetY` | `float` | `float` | |
| `hookOffsetZ` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** HookOffset defines where the hook is attached. leave at 0 for default attachment.

---

### DETACH_VEHICLE_FROM_TOW_TRUCK

**Hash:** `0xC2DB6B6708350ED8` | **JHash:** `0xC666CF33` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_VEHICLE_FROM_TOW_TRUCK(Vehicle towTruck, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_VEHICLE_FROM_TOW_TRUCK, towTruck, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `towTruck` | `Vehicle` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** First two parameters swapped. Scripts verify that towTruck is the first parameter, not the second.

---

### DETACH_VEHICLE_FROM_ANY_TOW_TRUCK

**Hash:** `0xD0E9CE05A1E68CD8` | **JHash:** `0x3BF93651` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DETACH_VEHICLE_FROM_ANY_TOW_TRUCK(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DETACH_VEHICLE_FROM_ANY_TOW_TRUCK, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_ATTACHED_TO_TOW_TRUCK

**Hash:** `0x146DF9EC4C4B9FD4` | **JHash:** `0x9699CFDC` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_ATTACHED_TO_TOW_TRUCK(Vehicle towTruck, Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_ATTACHED_TO_TOW_TRUCK, towTruck, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `towTruck` | `Vehicle` | `int` | |
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Scripts verify that towTruck is the first parameter, not the second.

---

### GET_ENTITY_ATTACHED_TO_TOW_TRUCK

**Hash:** `0xEFEA18DCF10F8F75` | **JHash:** `0x11EC7844` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_ENTITY_ATTACHED_TO_TOW_TRUCK(Vehicle towTruck)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_ATTACHED_TO_TOW_TRUCK, towTruck);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `towTruck` | `Vehicle` | `int` | |

**返回值:** `Entity` → `int`

---

### SET_VEHICLE_AUTOMATICALLY_ATTACHES

**Hash:** `0x8BA6F76BC53A1493` | **JHash:** `0x4273A8D3` | **Build:** `323`

**C++ 原型:**
```cpp
Entity SET_VEHICLE_AUTOMATICALLY_ATTACHES(Vehicle vehicle, BOOL p1, Any p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_VEHICLE_AUTOMATICALLY_ATTACHES, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `Any` | `object` | |

**返回值:** `Entity` → `int`

---

### SET_VEHICLE_BULLDOZER_ARM_POSITION

**Hash:** `0xF8EBCCC96ADB9FB7` | **JHash:** `0xED23C8A3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BULLDOZER_ARM_POSITION(Vehicle vehicle, float position, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BULLDOZER_ARM_POSITION, vehicle, position, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `position` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the arm position of a bulldozer. Position must be a value between 0.0 and 1.0. Ignored when `p2` is set to false, instead incrementing arm position by 0.1 (or 10%).

---

### SET_VEHICLE_TANK_TURRET_POSITION

**Hash:** `0x56B94C6D7127DFBA` | **JHash:** `0xB1A52EF7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TANK_TURRET_POSITION(Vehicle vehicle, float position, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TANK_TURRET_POSITION, vehicle, position, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `position` | `float` | `float` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TURRET_TARGET

**Hash:** `0x0581730AB9380412` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_TURRET_TARGET(Vehicle vehicle, BOOL p1, float x, float y, float z, BOOL p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TURRET_TARGET, vehicle, p1, x, y, z, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TANK_STATIONARY

**Hash:** `0x737E398138550FFF` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_VEHICLE_TANK_STATIONARY(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TANK_STATIONARY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TURRET_SPEED_THIS_FRAME

**Hash:** `0x1093408B4B9D1146` | **JHash:** `0xF30C566F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TURRET_SPEED_THIS_FRAME(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TURRET_SPEED_THIS_FRAME, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DISABLE_VEHICLE_TURRET_MOVEMENT_THIS_FRAME

**Hash:** `0x32CAEDF24A583345` | **Build:** `463`

**C++ 原型:**
```cpp
void DISABLE_VEHICLE_TURRET_MOVEMENT_THIS_FRAME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_VEHICLE_TURRET_MOVEMENT_THIS_FRAME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_FLIGHT_NOZZLE_POSITION

**Hash:** `0x30D779DE7C4F6DD3` | **JHash:** `0xA7DF64D7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FLIGHT_NOZZLE_POSITION(Vehicle vehicle, float angleRatio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FLIGHT_NOZZLE_POSITION, vehicle, angleRatio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `angleRatio` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_FLIGHT_NOZZLE_POSITION_IMMEDIATE

**Hash:** `0x9AA47FFF660CB932` | **JHash:** `0xDD7936F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FLIGHT_NOZZLE_POSITION_IMMEDIATE(Vehicle vehicle, float angle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FLIGHT_NOZZLE_POSITION_IMMEDIATE, vehicle, angle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `angle` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_FLIGHT_NOZZLE_POSITION

**Hash:** `0xDA62027C8BDB326E` | **JHash:** `0xAD40AD55` | **Build:** `1180`

**C++ 原型:**
```cpp
float GET_VEHICLE_FLIGHT_NOZZLE_POSITION(Vehicle plane)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_FLIGHT_NOZZLE_POSITION, plane);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### SET_DISABLE_VERTICAL_FLIGHT_MODE_TRANSITION

**Hash:** `0xCE2B43770B655F8F` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_DISABLE_VERTICAL_FLIGHT_MODE_TRANSITION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_VERTICAL_FLIGHT_MODE_TRANSITION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** True stops vtols from switching modes. Doesn't stop the sound though.

---

### GENERATE_VEHICLE_CREATION_POS_FROM_PATHS

**Hash:** `0xA4822F1CF23F4810` | **JHash:** `0x34E02FCD` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GENERATE_VEHICLE_CREATION_POS_FROM_PATHS(Vector3* outVec, Any p1, Vector3* outVec1, Any p3, Any p4, Any p5, Any p6, Any p7, Any p8)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GENERATE_VEHICLE_CREATION_POS_FROM_PATHS, outVec, p1, outVec1, p3, p4, p5, p6, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `outVec` | `Vector3*` | `Vector3*` | |
| `p1` | `Any` | `object` | |
| `outVec1` | `Vector3*` | `Vector3*` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |
| `p6` | `Any` | `object` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_BURNOUT

**Hash:** `0xFB8794444A7D60FB` | **JHash:** `0x9B6EF0EA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BURNOUT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BURNOUT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** On accelerating, spins the driven wheels with the others braked, so you don't go anywhere.

---

### IS_VEHICLE_IN_BURNOUT

**Hash:** `0x1297A88E081430EB` | **JHash:** `0x6632BC12` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_IN_BURNOUT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_IN_BURNOUT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether the specified vehicle is currently in a burnout.


vb.net
Public Function isVehicleInBurnout(vh As Vehicle) As Boolean
        Return Native.Function.Call(Of Boolean)(Hash.IS_VEHICLE_IN_BURNOUT, vh)
    End Function

---

### SET_VEHICLE_REDUCE_GRIP

**Hash:** `0x222FF6A823D122E2` | **JHash:** `0x90D3A0D9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_REDUCE_GRIP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_REDUCE_GRIP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Reduces grip significantly so it's hard to go anywhere.

---

### SET_VEHICLE_REDUCE_GRIP_LEVEL

**Hash:** `0x6DEE944E1EE90CFB` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_REDUCE_GRIP_LEVEL(Vehicle vehicle, int val)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_REDUCE_GRIP_LEVEL, vehicle, val);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `val` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** val is 0-3
Often used in conjunction with: SET_VEHICLE_REDUCE_GRIP

---

### SET_VEHICLE_INDICATOR_LIGHTS

**Hash:** `0xB5D45264751B7DF0` | **JHash:** `0xA6073B5D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_INDICATOR_LIGHTS(Vehicle vehicle, int turnSignal, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_INDICATOR_LIGHTS, vehicle, turnSignal, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `turnSignal` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the turn signal enabled for a vehicle.
Set turnSignal to 1 for left light, 0 for right light.

---

### SET_VEHICLE_BRAKE_LIGHTS

**Hash:** `0x92B35082E0B42F66` | **JHash:** `0x6D9BA11E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BRAKE_LIGHTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BRAKE_LIGHTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TAIL_LIGHTS

**Hash:** `0x5815BD2763178DF4` | **Build:** `2802`

**C++ 原型:**
```cpp
void SET_VEHICLE_TAIL_LIGHTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TAIL_LIGHTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_HANDBRAKE

**Hash:** `0x684785568EF26A22` | **JHash:** `0xBA729A25` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HANDBRAKE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HANDBRAKE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_BRAKE

**Hash:** `0xE4E2FD323574965C` | **JHash:** `0x5A36BC37` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_VEHICLE_BRAKE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BRAKE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### INSTANTLY_FILL_VEHICLE_POPULATION

**Hash:** `0x48ADC8A773564670` | **JHash:** `0x37BC6ACB` | **Build:** `323`

**C++ 原型:**
```cpp
void INSTANTLY_FILL_VEHICLE_POPULATION(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.INSTANTLY_FILL_VEHICLE_POPULATION);
```

**返回值:** `void` → `void`

---

### HAS_INSTANT_FILL_VEHICLE_POPULATION_FINISHED

**Hash:** `0x91D6DD290888CBAB` | **JHash:** `0x71D898EF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_INSTANT_FILL_VEHICLE_POPULATION_FINISHED(void)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_INSTANT_FILL_VEHICLE_POPULATION_FINISHED);
```

**返回值:** `BOOL` → `bool`

---

### NETWORK_ENABLE_EMPTY_CROWDING_VEHICLES_REMOVAL

**Hash:** `0x51DB102F4A3BA5E0` | **JHash:** `0x0B0523B0` | **Build:** `323`

**C++ 原型:**
```cpp
void NETWORK_ENABLE_EMPTY_CROWDING_VEHICLES_REMOVAL(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_ENABLE_EMPTY_CROWDING_VEHICLES_REMOVAL, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### NETWORK_CAP_EMPTY_CROWDING_VEHICLES_REMOVAL

**Hash:** `0xA4A9A4C40E615885` | **Build:** `1604`

**C++ 原型:**
```cpp
void NETWORK_CAP_EMPTY_CROWDING_VEHICLES_REMOVAL(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_CAP_EMPTY_CROWDING_VEHICLES_REMOVAL, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Default:1000||This sets a value which is used when NETWORK_ENABLE_EMPTY_CROWDING_VEHICLES_REMOVAL(true) is called each frame.

---

### GET_VEHICLE_TRAILER_VEHICLE

**Hash:** `0x1CDD6BADC297830D` | **JHash:** `0xAE84D758` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_TRAILER_VEHICLE(Vehicle vehicle, Vehicle* trailer)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_TRAILER_VEHICLE, vehicle, trailer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `trailer` | `Vehicle*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Gets the trailer of a vehicle and puts it into the trailer parameter.

---

### _GET_VEHICLE_TRAILER_PARENT_VEHICLE

**Hash:** `0x80D9D32636369C92` | **Build:** `2802`

**C++ 原型:**
```cpp
Vehicle _GET_VEHICLE_TRAILER_PARENT_VEHICLE(Vehicle trailer)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_TRAILER_PARENT_VEHICLE, trailer);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `trailer` | `Vehicle` | `int` | |

**返回值:** `Vehicle` → `int`

---

### SET_VEHICLE_USES_LARGE_REAR_RAMP

**Hash:** `0xCAC66558B944DA67` | **JHash:** `0x0B200CE2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_USES_LARGE_REAR_RAMP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USES_LARGE_REAR_RAMP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** vehicle must be a plane

---

### SET_VEHICLE_RUDDER_BROKEN

**Hash:** `0x09606148B6C71DEF` | **JHash:** `0x3FAC3CD4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_RUDDER_BROKEN(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_RUDDER_BROKEN, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TAIL_BROKEN

**Hash:** `0x6CB5B84B78AC55FE` | **Build:** `3717`

**C++ 原型:**
```cpp
void SET_VEHICLE_TAIL_BROKEN(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TAIL_BROKEN, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CONVERTIBLE_ROOF_LATCH_STATE

**Hash:** `0x1A78AD3D8240536F` | **JHash:** `0x0858678C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CONVERTIBLE_ROOF_LATCH_STATE(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CONVERTIBLE_ROOF_LATCH_STATE, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_ESTIMATED_MAX_SPEED

**Hash:** `0x53AF99BAA671CA47` | **JHash:** `0x7D1A0616` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_ESTIMATED_MAX_SPEED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_ESTIMATED_MAX_SPEED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_MAX_BRAKING

**Hash:** `0xAD7E85FC227197C4` | **JHash:** `0x03B926F6` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MAX_BRAKING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MAX_BRAKING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_MAX_TRACTION

**Hash:** `0xA132FB5370554DB0` | **JHash:** `0x7E5A1587` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MAX_TRACTION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MAX_TRACTION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_ACCELERATION

**Hash:** `0x5DD35C8D074E57AE` | **JHash:** `0x00478321` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_ACCELERATION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_ACCELERATION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** static - max acceleration

---

### GET_VEHICLE_MODEL_ESTIMATED_MAX_SPEED

**Hash:** `0xF417C2502FFFED43` | **JHash:** `0x8F291C4A` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_ESTIMATED_MAX_SPEED(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_ESTIMATED_MAX_SPEED, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Returns max speed (without mods) of the specified vehicle model in m/s.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_MODEL_MAX_BRAKING

**Hash:** `0xDC53FD41B4ED944C` | **JHash:** `0x7EF02883` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_MAX_BRAKING(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_MAX_BRAKING, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Returns max braking of the specified vehicle model.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_MODEL_MAX_BRAKING_MAX_MODS

**Hash:** `0xBFBA3BA79CFF7EBF` | **JHash:** `0xF3A7293F` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_MAX_BRAKING_MAX_MODS(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_MAX_BRAKING_MAX_MODS, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_MODEL_MAX_TRACTION

**Hash:** `0x539DE94D44FDFD0D` | **JHash:** `0x7F985597` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_MAX_TRACTION(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_MAX_TRACTION, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Returns max traction of the specified vehicle model.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_MODEL_ACCELERATION

**Hash:** `0x8C044C5C84505B6A` | **JHash:** `0x29CB3537` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_ACCELERATION(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_ACCELERATION, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Returns the acceleration of the specified model.

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_MODEL_ACCELERATION_MAX_MODS

**Hash:** `0x53409B5163D5B846` | **JHash:** `0x37FBA7BC` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_MODEL_ACCELERATION_MAX_MODS(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_MODEL_ACCELERATION_MAX_MODS, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** 9.8 * thrust if air vehicle, else 0.38 + drive force?

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_FLYING_VEHICLE_MODEL_AGILITY

**Hash:** `0xC6AD107DDC9054CC` | **JHash:** `0x95BB67EB` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FLYING_VEHICLE_MODEL_AGILITY(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FLYING_VEHICLE_MODEL_AGILITY, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_BOAT_VEHICLE_MODEL_AGILITY

**Hash:** `0x5AA3F878A178C4FC` | **JHash:** `0x87C5D271` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_BOAT_VEHICLE_MODEL_AGILITY(Hash modelHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_BOAT_VEHICLE_MODEL_AGILITY, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### GET_VEHICLE_CLASS_ESTIMATED_MAX_SPEED

**Hash:** `0x00C09F246ABEDD82` | **JHash:** `0xCE67162C` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CLASS_ESTIMATED_MAX_SPEED(int vehicleClass)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CLASS_ESTIMATED_MAX_SPEED, vehicleClass);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleClass` | `int` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_CLASS_MAX_TRACTION

**Hash:** `0xDBC86D85C5059461` | **JHash:** `0x5B4FDC16` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CLASS_MAX_TRACTION(int vehicleClass)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CLASS_MAX_TRACTION, vehicleClass);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleClass` | `int` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_CLASS_MAX_AGILITY

**Hash:** `0x4F930AD022D6DE3B` | **JHash:** `0x45F2BD83` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CLASS_MAX_AGILITY(int vehicleClass)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CLASS_MAX_AGILITY, vehicleClass);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleClass` | `int` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_CLASS_MAX_ACCELERATION

**Hash:** `0x2F83E7E45D9EA7AE` | **JHash:** `0x3E220A9B` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CLASS_MAX_ACCELERATION(int vehicleClass)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CLASS_MAX_ACCELERATION, vehicleClass);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleClass` | `int` | `int` | |

**返回值:** `float` → `float`

---

### GET_VEHICLE_CLASS_MAX_BRAKING

**Hash:** `0x4BF54C16EC8FEC03` | **JHash:** `0xD08CC1A5` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CLASS_MAX_BRAKING(int vehicleClass)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CLASS_MAX_BRAKING, vehicleClass);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleClass` | `int` | `int` | |

**返回值:** `float` → `float`

---

### ADD_ROAD_NODE_SPEED_ZONE

**Hash:** `0x2CE544C68FB812A0` | **JHash:** `0xD6685803` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_ROAD_NODE_SPEED_ZONE(float x, float y, float z, float radius, float speed, BOOL p5)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_ROAD_NODE_SPEED_ZONE, x, y, z, radius, speed, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `speed` | `float` | `float` | |
| `p5` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

---

### REMOVE_ROAD_NODE_SPEED_ZONE

**Hash:** `0x1033371FC8E842A7` | **JHash:** `0x0C0332A6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REMOVE_ROAD_NODE_SPEED_ZONE(int speedzone)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REMOVE_ROAD_NODE_SPEED_ZONE, speedzone);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `speedzone` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### OPEN_BOMB_BAY_DOORS

**Hash:** `0x87E7F24270732CB1` | **JHash:** `0x6574041D` | **Build:** `323`

**C++ 原型:**
```cpp
void OPEN_BOMB_BAY_DOORS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OPEN_BOMB_BAY_DOORS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### CLOSE_BOMB_BAY_DOORS

**Hash:** `0x3556041742A0DC74` | **JHash:** `0xF8EC5751` | **Build:** `323`

**C++ 原型:**
```cpp
void CLOSE_BOMB_BAY_DOORS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLOSE_BOMB_BAY_DOORS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_ARE_BOMB_BAY_DOORS_OPEN

**Hash:** `0xD0917A423314BBA8` | **Build:** `1180`

**C++ 原型:**
```cpp
BOOL GET_ARE_BOMB_BAY_DOORS_OPEN(Vehicle aircraft)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_ARE_BOMB_BAY_DOORS_OPEN, aircraft);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `aircraft` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true when the bomb bay doors of this plane are open. False if they're closed.

---

### IS_VEHICLE_SEARCHLIGHT_ON

**Hash:** `0xC0F97FCE55094987` | **JHash:** `0xADAF3513` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_SEARCHLIGHT_ON(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_SEARCHLIGHT_ON, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Possibly: Returns whether the searchlight (found on police vehicles) is toggled on.

@Author Nac

---

### SET_VEHICLE_SEARCHLIGHT

**Hash:** `0x14E85C5EE7A4D542` | **JHash:** `0xE2C0DD8A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_SEARCHLIGHT(Vehicle heli, BOOL toggle, BOOL canBeUsedByAI)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_SEARCHLIGHT, heli, toggle, canBeUsedByAI);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `heli` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `canBeUsedByAI` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only works during nighttime.

---

### DOES_VEHICLE_HAVE_SEARCHLIGHT

**Hash:** `0x99015ED7DBEA5113` | **Build:** `2189`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_HAVE_SEARCHLIGHT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_HAVE_SEARCHLIGHT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ENTRY_POINT_FOR_SEAT_CLEAR

**Hash:** `0x639431E895B9AA57` | **JHash:** `0xAB0E79EB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ENTRY_POINT_FOR_SEAT_CLEAR(Ped ped, Vehicle vehicle, int seatIndex, BOOL side, BOOL onEnter)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ENTRY_POINT_FOR_SEAT_CLEAR, ped, vehicle, seatIndex, side, onEnter);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |
| `side` | `BOOL` | `bool` | |
| `onEnter` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** Check if a vehicle seat is accessible. If you park your vehicle near a wall and the ped cannot enter/exit this side, the return value toggles from true (not blocked) to false (blocked).

seatIndex  = -1 being the driver seat.
Use GET_VEHICLE_MAX_NUMBER_OF_PASSENGERS(vehicle) - 1 for last seat index.
side = only relevant for bikes/motorcycles to check if the left (false)/right (true) side is blocked.
onEnter = check if you can enter (true) or exit (false) a vehicle.

---

### GET_ENTRY_POINT_POSITION

**Hash:** `0xC0572928C0ABFDA3` | **Build:** `944`

**C++ 原型:**
```cpp
Vector3 GET_ENTRY_POINT_POSITION(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ENTRY_POINT_POSITION, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### CAN_SHUFFLE_SEAT

**Hash:** `0x30785D90C956BF35` | **JHash:** `0xB3EB01ED` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_SHUFFLE_SEAT(Vehicle vehicle, int seatIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_SHUFFLE_SEAT, vehicle, seatIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seatIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_NUM_MOD_KITS

**Hash:** `0x33F2E3FE70EAAE1D` | **JHash:** `0xE4903AA0` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_MOD_KITS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_MOD_KITS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SET_VEHICLE_MOD_KIT

**Hash:** `0x1F2AA07F00B3217A` | **JHash:** `0xB8132158` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MOD_KIT(Vehicle vehicle, int modKit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MOD_KIT, vehicle, modKit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modKit` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Set modKit to 0 if you plan to call SET_VEHICLE_MOD. That's what the game does. Most body modifications through SET_VEHICLE_MOD will not take effect until this is set to 0.

Full list of vehicle mod kits and mods by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleModKits.json

---

### GET_VEHICLE_MOD_KIT

**Hash:** `0x6325D1A044AE510D` | **JHash:** `0x9FE60927` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MOD_KIT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MOD_KIT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_MOD_KIT_TYPE

**Hash:** `0xFC058F5121E54C32` | **JHash:** `0xE5F76765` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MOD_KIT_TYPE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MOD_KIT_TYPE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_WHEEL_TYPE

**Hash:** `0xB3ED1BFB4BE636DC` | **JHash:** `0xDA58D7AE` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_WHEEL_TYPE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_WHEEL_TYPE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns an int

Wheel Types:
0: Sport
1: Muscle
2: Lowrider
3: SUV
4: Offroad
5: Tuner
6: Bike Wheels
7: High End
8: Benny's Originals
9: Benny's Bespoke
10: Racing
11: Street
12: Track

Tested in Los Santos Customs

---

### SET_VEHICLE_WHEEL_TYPE

**Hash:** `0x487EB21CC7295BA1` | **JHash:** `0x64BDAAAD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WHEEL_TYPE(Vehicle vehicle, int WheelType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WHEEL_TYPE, vehicle, WheelType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `WheelType` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** 0: Sport
1: Muscle
2: Lowrider
3: SUV
4: Offroad
5: Tuner
6: Bike Wheels
7: High End
8: Benny's Originals
9: Benny's Bespoke
10: Racing
11: Street
12: Track

---

### GET_NUM_MOD_COLORS

**Hash:** `0xA551BE18C11A476D` | **JHash:** `0x73722CD9` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_MOD_COLORS(int paintType, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_MOD_COLORS, paintType, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `paintType` | `int` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** paintType:
0: Normal
1: Metallic
2: Pearl
3: Matte
4: Metal
5: Chrome
6: Chameleon

---

### SET_VEHICLE_MOD_COLOR_1

**Hash:** `0x43FEB945EE7F85B8` | **JHash:** `0xCBE9A54D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MOD_COLOR_1(Vehicle vehicle, int paintType, int color, int pearlescentColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MOD_COLOR_1, vehicle, paintType, color, pearlescentColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `paintType` | `int` | `int` | |
| `color` | `int` | `int` | |
| `pearlescentColor` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** paintType:
0: Normal
1: Metallic
2: Pearl
3: Matte
4: Metal
5: Chrome
6: Chameleon

color: number of the color.

p3 seems to always be 0.

Full list of vehicle colors and vehicle plates by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleColors.json

---

### SET_VEHICLE_MOD_COLOR_2

**Hash:** `0x816562BADFDEC83E` | **JHash:** `0xC32613C2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MOD_COLOR_2(Vehicle vehicle, int paintType, int color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MOD_COLOR_2, vehicle, paintType, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `paintType` | `int` | `int` | |
| `color` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Changes the secondary paint type and color
paintType:
0: Normal
1: Metallic
2: Pearl
3: Matte
4: Metal
5: Chrome
6: Chameleon

color: number of the color

Full list of vehicle colors and vehicle plates by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleColors.json

---

### GET_VEHICLE_MOD_COLOR_1

**Hash:** `0xE8D65CA700C9A693` | **JHash:** `0xE625510A` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_MOD_COLOR_1(Vehicle vehicle, int* paintType, int* color, int* pearlescentColor)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_MOD_COLOR_1, vehicle, paintType, color, pearlescentColor);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `paintType` | `int*` | `int*` | |
| `color` | `int*` | `int*` | |
| `pearlescentColor` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_MOD_COLOR_2

**Hash:** `0x81592BE4E3878728` | **JHash:** `0x9B76BB8E` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_MOD_COLOR_2(Vehicle vehicle, int* paintType, int* color)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_MOD_COLOR_2, vehicle, paintType, color);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `paintType` | `int*` | `int*` | |
| `color` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_MOD_COLOR_1_NAME

**Hash:** `0xB45085B721EFD38C` | **JHash:** `0x9A0840FD` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_VEHICLE_MOD_COLOR_1_NAME(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_VEHICLE_MOD_COLOR_1_NAME, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `const char*` → `string`

**说明:** Returns a string which is the codename of the vehicle's currently selected primary color

p1 is always 0

---

### GET_VEHICLE_MOD_COLOR_2_NAME

**Hash:** `0x4967A516ED23A5A1` | **JHash:** `0x9BDC0B49` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_VEHICLE_MOD_COLOR_2_NAME(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_VEHICLE_MOD_COLOR_2_NAME, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns a string which is the codename of the vehicle's currently selected secondary color

---

### HAVE_VEHICLE_MODS_STREAMED_IN

**Hash:** `0x9A83F5F9963775EF` | **JHash:** `0x112D637A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAVE_VEHICLE_MODS_STREAMED_IN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAVE_VEHICLE_MODS_STREAMED_IN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_MOD_GEN9_EXCLUSIVE

**Hash:** `0x00834EAC4A96E010` | **Build:** `2699`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_MOD_GEN9_EXCLUSIVE(Vehicle vehicle, int modType, int modIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_MOD_GEN9_EXCLUSIVE, vehicle, modType, modIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modIndex` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true for any mod part listed in GEN9_EXCLUSIVE_ASSETS_VEHICLES_FILE.

---

### SET_VEHICLE_MOD

**Hash:** `0x6AF0636DDEDCB6DD` | **JHash:** `0xB52E5ED5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_MOD(Vehicle vehicle, int modType, int modIndex, BOOL customTires)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MOD, vehicle, modType, modIndex, customTires);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modIndex` | `int` | `int` | |
| `customTires` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** In b944, there are 50 (0 - 49) mod types.

Sets the vehicle mod.
The vehicle must have a mod kit first.

Any out of range ModIndex is stock.

#Mod Type
Spoilers - 0
Front Bumper - 1
Rear Bumper - 2
Side Skirt - 3
Exhaust - 4
Frame - 5
Grille - 6
Hood - 7
Fender - 8
Right Fender - 9
Roof - 10
Engine - 11
Brakes - 12
Transmission - 13
Horns - 14 (modIndex from 0 to 51)
Suspension - 15
Armor - 16
Front Wheels - 23
Back Wheels - 24 //only for motocycles
Plate holders - 25
Trim Design - 27
Ornaments - 28
Dial Design - 30
Steering Wheel - 33
Shifter Leavers - 34
Plaques - 35
Hydraulics - 38
Livery - 48

ENUMS: https://pastebin.com/QzEAn02v

---

### GET_VEHICLE_MOD

**Hash:** `0x772960298DA26FDB` | **JHash:** `0xDC520069` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MOD(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MOD, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** In b944, there are 50 (0 - 49) mod types. See SET_VEHICLE_MOD for the list.

Returns -1 if the vehicle mod is stock

---

### GET_VEHICLE_MOD_VARIATION

**Hash:** `0xB3924ECD70E095DC` | **JHash:** `0xC1B92003` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MOD_VARIATION(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MOD_VARIATION, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Only used for wheels(ModType = 23/24) Returns true if the wheels are custom wheels

---

### GET_NUM_VEHICLE_MODS

**Hash:** `0xE38E9162A2500646` | **JHash:** `0x8A814FF9` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_VEHICLE_MODS(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_VEHICLE_MODS, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns how many possible mods a vehicle has for a given mod type

---

### REMOVE_VEHICLE_MOD

**Hash:** `0x92D619E420858204` | **JHash:** `0x9CC80A43` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_MOD(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_MOD, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `void` → `void`

---

### TOGGLE_VEHICLE_MOD

**Hash:** `0x2A1F4F37F95BAD08` | **JHash:** `0xD095F811` | **Build:** `323`

**C++ 原型:**
```cpp
void TOGGLE_VEHICLE_MOD(Vehicle vehicle, int modType, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TOGGLE_VEHICLE_MOD, vehicle, modType, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles:
UNK17 - 17
Turbo - 18
UNK19 - 19
Tire Smoke - 20
UNK21 - 21
Xenon Headlights - 22

---

### IS_TOGGLE_MOD_ON

**Hash:** `0x84B233A8C8FC8AE7` | **JHash:** `0xF0E1689F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_TOGGLE_MOD_ON(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_TOGGLE_MOD_ON, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_MOD_TEXT_LABEL

**Hash:** `0x8935624F8C5592CC` | **JHash:** `0x0BA39CA7` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_MOD_TEXT_LABEL(Vehicle vehicle, int modType, int modValue)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_MOD_TEXT_LABEL, vehicle, modType, modValue);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modValue` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns the text label of a mod type for a given vehicle

Use GET_FILENAME_FOR_AUDIO_CONVERSATION to get the part name in the game's language

---

### GET_MOD_SLOT_NAME

**Hash:** `0x51F0FEB9F6AE98C0` | **JHash:** `0x5E113483` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_MOD_SLOT_NAME(Vehicle vehicle, int modType)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_MOD_SLOT_NAME, vehicle, modType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns the name for the type of vehicle mod(Armour, engine etc)


---

### GET_LIVERY_NAME

**Hash:** `0xB4C7A93837C91A1F` | **JHash:** `0xED80B5BE` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_LIVERY_NAME(Vehicle vehicle, int liveryIndex)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_LIVERY_NAME, vehicle, liveryIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `liveryIndex` | `int` | `int` | |

**返回值:** `const char*` → `string`

**说明:** Returns the text label of the vehicle's liveryIndex, as specified by the liveryNames section of the vehicle's modkit data in the carcols file.

example 

int count = VEHICLE::GET_VEHICLE_LIVERY_COUNT(veh);
for (int i = 0; i < count; i++)  
  {
     const char* LiveryName = VEHICLE::GET_LIVERY_NAME(veh, i);
  }


this example will work fine to fetch all names 
for example for Sanchez we get 

SANC_LV1
SANC_LV2
SANC_LV3
SANC_LV4
SANC_LV5


Use GET_FILENAME_FOR_AUDIO_CONVERSATION, to get the localized livery name.

Full list of vehicle mod kits and mods by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleModKits.json

---

### GET_VEHICLE_MOD_MODIFIER_VALUE

**Hash:** `0x90A38E9838E0A8C1` | **JHash:** `0x73AE5505` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_MOD_MODIFIER_VALUE(Vehicle vehicle, int modType, int modIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_MOD_MODIFIER_VALUE, vehicle, modType, modIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modIndex` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_MOD_IDENTIFIER_HASH

**Hash:** `0x4593CF82AA179706` | **JHash:** `0x94850968` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_VEHICLE_MOD_IDENTIFIER_HASH(Vehicle vehicle, int modType, int modIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_VEHICLE_MOD_IDENTIFIER_HASH, vehicle, modType, modIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Can be used for IS_DLC_VEHICLE_MOD and GET_DLC_VEHICLE_MOD_LOCK_HASH

---

### PRELOAD_VEHICLE_MOD

**Hash:** `0x758F49C24925568A` | **JHash:** `0x6EA5F4A8` | **Build:** `323`

**C++ 原型:**
```cpp
void PRELOAD_VEHICLE_MOD(Vehicle vehicle, int modType, int modIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.PRELOAD_VEHICLE_MOD, vehicle, modType, modIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modType` | `int` | `int` | |
| `modIndex` | `int` | `int` | |

**返回值:** `void` → `void`

---

### HAS_PRELOAD_MODS_FINISHED

**Hash:** `0x06F43E5175EB6D96` | **JHash:** `0xA8A0D246` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PRELOAD_MODS_FINISHED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PRELOAD_MODS_FINISHED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### RELEASE_PRELOAD_MODS

**Hash:** `0x445D79F995508307` | **JHash:** `0xD442521F` | **Build:** `323`

**C++ 原型:**
```cpp
void RELEASE_PRELOAD_MODS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RELEASE_PRELOAD_MODS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_TYRE_SMOKE_COLOR

**Hash:** `0xB5BA80F839791C0F` | **JHash:** `0x3EDEC0DB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_TYRE_SMOKE_COLOR(Vehicle vehicle, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_TYRE_SMOKE_COLOR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the tire smoke's color of this vehicle.

vehicle: The vehicle that is the target of this method.
r: The red level in the RGB color code.
g: The green level in the RGB color code.
b: The blue level in the RGB color code.

Note: setting r,g,b to 0 will give the car the "Patriot" tire smoke.

---

### GET_VEHICLE_TYRE_SMOKE_COLOR

**Hash:** `0xB635392A4938B3C3` | **JHash:** `0x75280015` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_TYRE_SMOKE_COLOR(Vehicle vehicle, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_TYRE_SMOKE_COLOR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_WINDOW_TINT

**Hash:** `0x57C51E6BAD752696` | **JHash:** `0x497C8787` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WINDOW_TINT(Vehicle vehicle, int tint)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WINDOW_TINT, vehicle, tint);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `tint` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** enum WindowTints
{
 WINDOWTINT_NONE,
  WINDOWTINT_PURE_BLACK,
    WINDOWTINT_DARKSMOKE,
 WINDOWTINT_LIGHTSMOKE,
    WINDOWTINT_STOCK,
 WINDOWTINT_LIMO,
  WINDOWTINT_GREEN
};
Full list of all vehicle window tints by DurtyFree https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicleColors.json

---

### GET_VEHICLE_WINDOW_TINT

**Hash:** `0x0EE21293DAD47C95` | **JHash:** `0x13D53892` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_WINDOW_TINT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_WINDOW_TINT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_NUM_VEHICLE_WINDOW_TINTS

**Hash:** `0x9D1224004B3A6707` | **JHash:** `0x625C7B66` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_NUM_VEHICLE_WINDOW_TINTS(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUM_VEHICLE_WINDOW_TINTS);
```

**返回值:** `int` → `int`

---

### GET_VEHICLE_COLOR

**Hash:** `0xF3CC740D36221548` | **JHash:** `0x03BC8F1B` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_COLOR(Vehicle vehicle, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_COLOR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** What's this for? Primary and Secondary RGB have their own natives and this one doesn't seem specific.

---

### GET_VEHICLE_COLOURS_WHICH_CAN_BE_SET

**Hash:** `0xEEBFC7A7EFDC35B4` | **JHash:** `0x749DEEA2` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_COLOURS_WHICH_CAN_BE_SET(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_COLOURS_WHICH_CAN_BE_SET, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Some kind of flags.

---

### GET_VEHICLE_CAUSE_OF_DESTRUCTION

**Hash:** `0xE495D1EF4C91FD20` | **JHash:** `0x7F8C20DD` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_VEHICLE_CAUSE_OF_DESTRUCTION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_VEHICLE_CAUSE_OF_DESTRUCTION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** iVar3 = get_vehicle_cause_of_destruction(uLocal_248[iVar2]);
if (iVar3 == joaat("weapon_stickybomb"))
{
	func_171(726);
	iLocal_260 = 1;
}

---

### OVERRIDE_PLANE_DAMAGE_THREHSOLD

**Hash:** `0x5EE5632F47AE9695` | **Build:** `463`

**C++ 原型:**
```cpp
void OVERRIDE_PLANE_DAMAGE_THREHSOLD(Vehicle vehicle, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_PLANE_DAMAGE_THREHSOLD, vehicle, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Used for helis.

---

### _SET_TRANSMISSION_REDUCED_GEAR_RATIO

**Hash:** `0x337EF33DA3DDB990` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_TRANSMISSION_REDUCED_GEAR_RATIO(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_TRANSMISSION_REDUCED_GEAR_RATIO, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _GET_VEHICLE_DESIRED_DRIVE_GEAR

**Hash:** `0xFD8CE53356B5D745` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_VEHICLE_DESIRED_DRIVE_GEAR(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_DESIRED_DRIVE_GEAR, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### GET_IS_LEFT_VEHICLE_HEADLIGHT_DAMAGED

**Hash:** `0x5EF77C9ADD3B11A3` | **JHash:** `0xA0777943` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_LEFT_VEHICLE_HEADLIGHT_DAMAGED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_LEFT_VEHICLE_HEADLIGHT_DAMAGED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** From the driver's perspective, is the left headlight broken.

---

### GET_IS_RIGHT_VEHICLE_HEADLIGHT_DAMAGED

**Hash:** `0xA7ECB73355EB2F20` | **JHash:** `0xF178390B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_RIGHT_VEHICLE_HEADLIGHT_DAMAGED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_RIGHT_VEHICLE_HEADLIGHT_DAMAGED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** From the driver's perspective, is the right headlight broken.

---

### GET_BOTH_VEHICLE_HEADLIGHTS_DAMAGED

**Hash:** `0xEC69ADF931AAE0C3` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_BOTH_VEHICLE_HEADLIGHTS_DAMAGED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_BOTH_VEHICLE_HEADLIGHTS_DAMAGED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true when both headlights are broken. This does not include extralights.

---

### MODIFY_VEHICLE_TOP_SPEED

**Hash:** `0x93A3996368C94158` | **JHash:** `0xE943B09C` | **Build:** `323`

**C++ 原型:**
```cpp
void MODIFY_VEHICLE_TOP_SPEED(Vehicle vehicle, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MODIFY_VEHICLE_TOP_SPEED, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_MAX_SPEED

**Hash:** `0xBAA045B4E42F3C06` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_MAX_SPEED(Vehicle vehicle, float speed)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_MAX_SPEED, vehicle, speed);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `speed` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** To reset the max speed, set the `speed` value to `0.0` or lower.

---

### SET_VEHICLE_STAYS_FROZEN_WHEN_CLEANED_UP

**Hash:** `0x1CF38D529D7441D9` | **JHash:** `0xDF594D8D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STAYS_FROZEN_WHEN_CLEANED_UP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STAYS_FROZEN_WHEN_CLEANED_UP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Has something to do with trains. Always precedes SET_MISSION_TRAIN_AS_NO_LONGER_NEEDED.
May be true that it can be used with trains not sure, but not specifically for trains. Go find Xbox360 decompiled scripts and search for 'func_1333' in freemode.c it isn't used just for trains. Thanks for the info tho.

---

### SET_VEHICLE_ACT_AS_IF_HIGH_SPEED_FOR_FRAG_SMASHING

**Hash:** `0x1F9FB66F3A3842D2` | **JHash:** `0x4D840FC4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ACT_AS_IF_HIGH_SPEED_FOR_FRAG_SMASHING(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ACT_AS_IF_HIGH_SPEED_FOR_FRAG_SMASHING, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PEDS_CAN_FALL_OFF_THIS_VEHICLE_FROM_LARGE_FALL_DAMAGE

**Hash:** `0x59C3757B3B7408E8` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_PEDS_CAN_FALL_OFF_THIS_VEHICLE_FROM_LARGE_FALL_DAMAGE(Vehicle vehicle, BOOL toggle, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PEDS_CAN_FALL_OFF_THIS_VEHICLE_FROM_LARGE_FALL_DAMAGE, vehicle, toggle, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets some bit and float of vehicle. float is >= 0

---

### ADD_VEHICLE_COMBAT_ANGLED_AVOIDANCE_AREA

**Hash:** `0x54B0F614960F4A5F` | **JHash:** `0x5AB26C2B` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_VEHICLE_COMBAT_ANGLED_AVOIDANCE_AREA(float p0, float p1, float p2, float p3, float p4, float p5, float p6)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_VEHICLE_COMBAT_ANGLED_AVOIDANCE_AREA, p0, p1, p2, p3, p4, p5, p6);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |

**返回值:** `int` → `int`

---

### REMOVE_VEHICLE_COMBAT_AVOIDANCE_AREA

**Hash:** `0xE30524E1871F481D` | **JHash:** `0xEF05F807` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_VEHICLE_COMBAT_AVOIDANCE_AREA(int p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_VEHICLE_COMBAT_AVOIDANCE_AREA, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `int` | `int` | |

**返回值:** `void` → `void`

---

### IS_ANY_PED_RAPPELLING_FROM_HELI

**Hash:** `0x291E373D483E7EE7` | **JHash:** `0xD656E7E5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_ANY_PED_RAPPELLING_FROM_HELI(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ANY_PED_RAPPELLING_FROM_HELI, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_CHEAT_POWER_INCREASE

**Hash:** `0xB59E4BD37AE292DB` | **JHash:** `0x642DA5AA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CHEAT_POWER_INCREASE(Vehicle vehicle, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CHEAT_POWER_INCREASE, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** <1.0 - Decreased torque
=1.0 - Default torque
>1.0 - Increased torque

Negative values will cause the vehicle to go backwards instead of forwards while accelerating.

value - is between 0.2 and 1.8 in the decompiled scripts. 

This needs to be called every frame to take effect.

---

### SET_VEHICLE_INFLUENCES_WANTED_LEVEL

**Hash:** `0x0AD9E8F87FF7C16F` | **JHash:** `0x04F5546C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_INFLUENCES_WANTED_LEVEL(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_INFLUENCES_WANTED_LEVEL, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_IS_WANTED

**Hash:** `0xF7EC25A3EBEEC726` | **JHash:** `0xDAA388E8` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_IS_WANTED(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IS_WANTED, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the wanted state of this vehicle.

---

### SWING_BOAT_BOOM_TO_RATIO

**Hash:** `0xF488C566413B4232` | **JHash:** `0xA25CCB8C` | **Build:** `323`

**C++ 原型:**
```cpp
void SWING_BOAT_BOOM_TO_RATIO(Vehicle vehicle, float ratio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SWING_BOAT_BOOM_TO_RATIO, vehicle, ratio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ratio` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the boat boom position for the `TR3` trailer.
Ratio value is between `0.0` and `1.0`, where `0.0` is 90 degrees to the left of the boat, and `1.0` is just slightly to the right/back of the boat.
To get the current boom position ratio, use GET_BOAT_BOOM_POSITION_RATIO

---

### SWING_BOAT_BOOM_FREELY

**Hash:** `0xC1F981A6F74F0C23` | **JHash:** `0x00966934` | **Build:** `323`

**C++ 原型:**
```cpp
void SWING_BOAT_BOOM_FREELY(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SWING_BOAT_BOOM_FREELY, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Same call as ALLOW_BOAT_BOOM_TO_ANIMATE

---

### ALLOW_BOAT_BOOM_TO_ANIMATE

**Hash:** `0x0F3B4D4E43177236` | **JHash:** `0x113DF5FD` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_BOAT_BOOM_TO_ANIMATE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_BOAT_BOOM_TO_ANIMATE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_BOAT_BOOM_POSITION_RATIO

**Hash:** `0x6636C535F6CC2725` | **JHash:** `0x7C8D6464` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_BOAT_BOOM_POSITION_RATIO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_BOAT_BOOM_POSITION_RATIO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### DISABLE_PLANE_AILERON

**Hash:** `0x23428FC53C60919C` | **JHash:** `0x7E84C45C` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_PLANE_AILERON(Vehicle vehicle, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_PLANE_AILERON, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_IS_VEHICLE_ENGINE_RUNNING

**Hash:** `0xAE31E7DF9B5B132E` | **JHash:** `0x7DC6D022` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_VEHICLE_ENGINE_RUNNING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_VEHICLE_ENGINE_RUNNING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true when in a vehicle, false whilst entering/exiting.

---

### SET_VEHICLE_USE_ALTERNATE_HANDLING

**Hash:** `0x1D97D1E3A70A649F` | **JHash:** `0xA03E42DF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_USE_ALTERNATE_HANDLING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USE_ALTERNATE_HANDLING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_BIKE_ON_STAND

**Hash:** `0x9CFA4896C3A53CBB` | **JHash:** `0x15D40761` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_BIKE_ON_STAND(Vehicle vehicle, float x, float y)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BIKE_ON_STAND, vehicle, x, y);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Only works on bikes, both X and Y work in the -1 - 1 range.

X forces the bike to turn left or right (-1, 1)
Y forces the bike to lean to the left or to the right (-1, 1)

Example with X -1/Y 1
http://i.imgur.com/TgIuAPJ.jpg

---

### SET_VEHICLE_NOT_STEALABLE_AMBIENTLY

**Hash:** `0xAB04325045427AAE` | **JHash:** `0x1984F88D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NOT_STEALABLE_AMBIENTLY(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NOT_STEALABLE_AMBIENTLY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### LOCK_DOORS_WHEN_NO_LONGER_NEEDED

**Hash:** `0xCFD778E7904C255E` | **JHash:** `0x3FBE904F` | **Build:** `323`

**C++ 原型:**
```cpp
void LOCK_DOORS_WHEN_NO_LONGER_NEEDED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.LOCK_DOORS_WHEN_NO_LONGER_NEEDED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_LAST_DRIVEN_VEHICLE

**Hash:** `0xACFB2463CC22BED2` | **JHash:** `0xD1B71A25` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LAST_DRIVEN_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LAST_DRIVEN_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_LAST_DRIVEN_VEHICLE

**Hash:** `0xB2D06FAEDE65B577` | **JHash:** `0xFEB0C0C8` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_LAST_DRIVEN_VEHICLE(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LAST_DRIVEN_VEHICLE);
```

**返回值:** `Vehicle` → `int`

---

### CLEAR_LAST_DRIVEN_VEHICLE

**Hash:** `0xE01903C47C7AC89E` | **JHash:** `0x07186AD9` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_LAST_DRIVEN_VEHICLE(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_LAST_DRIVEN_VEHICLE);
```

**返回值:** `void` → `void`

---

### SET_VEHICLE_HAS_BEEN_DRIVEN_FLAG

**Hash:** `0x02398B627547189C` | **JHash:** `0x08CD58F9` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HAS_BEEN_DRIVEN_FLAG(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HAS_BEEN_DRIVEN_FLAG, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TASK_VEHICLE_GOTO_PLANE_MIN_HEIGHT_ABOVE_TERRAIN

**Hash:** `0xB893215D8D4C015B` | **JHash:** `0x8C4B63E2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_TASK_VEHICLE_GOTO_PLANE_MIN_HEIGHT_ABOVE_TERRAIN(Vehicle plane, int height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TASK_VEHICLE_GOTO_PLANE_MIN_HEIGHT_ABOVE_TERRAIN, plane, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |
| `height` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_LOD_MULTIPLIER

**Hash:** `0x93AE6A61BE015BF1` | **JHash:** `0x569E5AE3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_LOD_MULTIPLIER(Vehicle vehicle, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_LOD_MULTIPLIER, vehicle, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_SAVE_IN_GARAGE

**Hash:** `0x428BACCDF5E26EAD` | **JHash:** `0x1604C2F5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_SAVE_IN_GARAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_SAVE_IN_GARAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_NUM_OF_BROKEN_OFF_PARTS

**Hash:** `0x42A4BEB35D372407` | **JHash:** `0x8CDB0C09` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_NUM_OF_BROKEN_OFF_PARTS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_NUM_OF_BROKEN_OFF_PARTS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Also includes some "turnOffBones" when vehicle mods are installed.

---

### GET_VEHICLE_NUM_OF_BROKEN_LOOSEN_PARTS

**Hash:** `0x2C8CBFE1EA5FC631` | **JHash:** `0xABC99E21` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_NUM_OF_BROKEN_LOOSEN_PARTS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_NUM_OF_BROKEN_LOOSEN_PARTS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SET_FORCE_VEHICLE_ENGINE_DAMAGE_BY_BULLET

**Hash:** `0x4D9D109F63FEE1D4` | **JHash:** `0x900C878C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_VEHICLE_ENGINE_DAMAGE_BY_BULLET(Any p0, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_VEHICLE_ENGINE_DAMAGE_BY_BULLET, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_GENERATES_ENGINE_SHOCKING_EVENTS

**Hash:** `0x279D50DE5652D935` | **JHash:** `0xB3200F72` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_GENERATES_ENGINE_SHOCKING_EVENTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_GENERATES_ENGINE_SHOCKING_EVENTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Allows creation of CEventShockingPlaneFlyby, CEventShockingHelicopterOverhead, and other(?) Shocking events

---

### COPY_VEHICLE_DAMAGES

**Hash:** `0xE44A982368A4AF23` | **JHash:** `0xBAE491C7` | **Build:** `323`

**C++ 原型:**
```cpp
void COPY_VEHICLE_DAMAGES(Vehicle sourceVehicle, Vehicle targetVehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.COPY_VEHICLE_DAMAGES, sourceVehicle, targetVehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `sourceVehicle` | `Vehicle` | `int` | |
| `targetVehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Copies sourceVehicle's damage (broken bumpers, broken lights, etc.) to targetVehicle.

---

### DISABLE_VEHICLE_EXPLOSION_BREAK_OFF_PARTS

**Hash:** `0xF25E02CB9C5818F8` | **JHash:** `0xF0E59BC1` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_VEHICLE_EXPLOSION_BREAK_OFF_PARTS(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_VEHICLE_EXPLOSION_BREAK_OFF_PARTS);
```

**返回值:** `void` → `void`

---

### SET_LIGHTS_CUTOFF_DISTANCE_TWEAK

**Hash:** `0xBC3CCA5844452B06` | **JHash:** `0x929801C6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_LIGHTS_CUTOFF_DISTANCE_TWEAK(float distance)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_LIGHTS_CUTOFF_DISTANCE_TWEAK, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_SHOOT_AT_TARGET

**Hash:** `0x74CD9A9327A282EA` | **JHash:** `0x2343FFDF` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_SHOOT_AT_TARGET(Ped driver, Entity entity, float xTarget, float yTarget, float zTarget)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_SHOOT_AT_TARGET, driver, entity, xTarget, yTarget, zTarget);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `entity` | `Entity` | `int` | |
| `xTarget` | `float` | `float` | |
| `yTarget` | `float` | `float` | |
| `zTarget` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Commands the driver of an armed vehicle (p0) to shoot its weapon at a target (p1). p3, p4 and p5 are the coordinates of the target. Example:

WEAPON::SET_CURRENT_PED_VEHICLE_WEAPON(pilot,MISC::GET_HASH_KEY("VEHICLE_WEAPON_PLANE_ROCKET"));                        VEHICLE::SET_VEHICLE_SHOOT_AT_TARGET(pilot, target, targPos.x, targPos.y, targPos.z);

---

### GET_VEHICLE_LOCK_ON_TARGET

**Hash:** `0x8F5EBAB1F260CFCE` | **JHash:** `0x4A557117` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_LOCK_ON_TARGET(Vehicle vehicle, Entity* entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_LOCK_ON_TARGET, vehicle, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `entity` | `Entity*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### SET_FORCE_HD_VEHICLE

**Hash:** `0x97CE68CB032583F0` | **JHash:** `0xE0FC6A32` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_FORCE_HD_VEHICLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_HD_VEHICLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CUSTOM_PATH_NODE_STREAMING_RADIUS

**Hash:** `0x182F266C2D9E2BEB` | **JHash:** `0x7D0DE7EA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CUSTOM_PATH_NODE_STREAMING_RADIUS(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CUSTOM_PATH_NODE_STREAMING_RADIUS, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_PLATE_TYPE

**Hash:** `0x9CCC9525BF2408E0` | **JHash:** `0x65CA9286` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_PLATE_TYPE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_PLATE_TYPE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### TRACK_VEHICLE_VISIBILITY

**Hash:** `0x64473AEFDCF47DCA` | **JHash:** `0x78122DC1` | **Build:** `323`

**C++ 原型:**
```cpp
void TRACK_VEHICLE_VISIBILITY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.TRACK_VEHICLE_VISIBILITY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** in script hook .net 

Vehicle v = ...;
Function.Call(Hash.TRACK_VEHICLE_VISIBILITY, v.Handle);

---

### IS_VEHICLE_VISIBLE

**Hash:** `0xAA0A52D24FB98293` | **JHash:** `0x7E0D6056` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_VISIBLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_VISIBLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** must be called after TRACK_VEHICLE_VISIBILITY 

it's not instant so probabilly must pass an 'update' to see correct result.

---

### SET_VEHICLE_GRAVITY

**Hash:** `0x89F149B6131E57DA` | **JHash:** `0x07B2A6DC` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_GRAVITY(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_GRAVITY, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ENABLE_VEHICLE_SLIPSTREAMING

**Hash:** `0xE6C0C80B8C867537` | **JHash:** `0xD2B8ACBD` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ENABLE_VEHICLE_SLIPSTREAMING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ENABLE_VEHICLE_SLIPSTREAMING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enable/Disables global slipstream physics

---

### SET_VEHICLE_SLIPSTREAMING_SHOULD_TIME_OUT

**Hash:** `0xF051D9BFB6BA39C0` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_VEHICLE_SLIPSTREAMING_SHOULD_TIME_OUT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_SLIPSTREAMING_SHOULD_TIME_OUT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_CURRENT_TIME_IN_SLIP_STREAM

**Hash:** `0x36492C2F0D134C56` | **JHash:** `0xA4A75FCF` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_CURRENT_TIME_IN_SLIP_STREAM(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_CURRENT_TIME_IN_SLIP_STREAM, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Returns a float value between 0.0 and 3.0 related to its slipstream draft (boost/speedup).

---

### IS_VEHICLE_PRODUCING_SLIP_STREAM

**Hash:** `0x48C633E94A8142A7` | **Build:** `877`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_PRODUCING_SLIP_STREAM(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_PRODUCING_SLIP_STREAM, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle is being slipstreamed by another vehicle

---

### SET_VEHICLE_INACTIVE_DURING_PLAYBACK

**Hash:** `0x06582AFF74894C75` | **JHash:** `0x50F89338` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_INACTIVE_DURING_PLAYBACK(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_INACTIVE_DURING_PLAYBACK, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_ACTIVE_DURING_PLAYBACK

**Hash:** `0xDFFCEF48E511DB48` | **JHash:** `0xEB7D7C27` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ACTIVE_DURING_PLAYBACK(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ACTIVE_DURING_PLAYBACK, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_SPRAYABLE

**Hash:** `0x8D474C8FAEFF6CDE` | **JHash:** `0x5EB00A6A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_SPRAYABLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_SPRAYABLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns false if the vehicle has the FLAG_NO_RESPRAY flag set.

---

### SET_VEHICLE_ENGINE_CAN_DEGRADE

**Hash:** `0x983765856F2564F9` | **JHash:** `0x081DAC12` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ENGINE_CAN_DEGRADE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ENGINE_CAN_DEGRADE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_VEHCILE_DYNAMIC_AMBIENT_SCALES

**Hash:** `0xF0E4BA16D1DB546C` | **JHash:** `0x5BD8D82D` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_VEHCILE_DYNAMIC_AMBIENT_SCALES(Vehicle vehicle, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_VEHCILE_DYNAMIC_AMBIENT_SCALES, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Adds some kind of shadow to the vehicle.

p1 and p2 use values from 0-255 and both make the shadow darker the lower the value is. -1 disables the effect.

---

### ENABLE_VEHICLE_DYNAMIC_AMBIENT_SCALES

**Hash:** `0xF87D9F2301F7D206` | **JHash:** `0x450AD03A` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_VEHICLE_DYNAMIC_AMBIENT_SCALES(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_VEHICLE_DYNAMIC_AMBIENT_SCALES, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Remove the weird shadow applied by DISABLE_VEHCILE_DYNAMIC_AMBIENT_SCALES.

---

### IS_PLANE_LANDING_GEAR_INTACT

**Hash:** `0x4198AB0022B15F87` | **JHash:** `0xBD085DCA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PLANE_LANDING_GEAR_INTACT(Vehicle plane)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PLANE_LANDING_GEAR_INTACT, plane);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ARE_PLANE_PROPELLERS_INTACT

**Hash:** `0x755D6D5267CBBD7E` | **JHash:** `0xABBDD5C6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_PLANE_PROPELLERS_INTACT(Vehicle plane)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_PLANE_PROPELLERS_INTACT, plane);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_PLANE_PROPELLER_HEALTH

**Hash:** `0x4C815EB175086F84` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL SET_PLANE_PROPELLER_HEALTH(Vehicle plane, float health)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_PLANE_PROPELLER_HEALTH, plane, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_CAN_DEFORM_WHEELS

**Hash:** `0x0CDDA42F9E360CA6` | **JHash:** `0x9B581DE7` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_DEFORM_WHEELS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_DEFORM_WHEELS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_STOLEN

**Hash:** `0x4AF9BD80EEBEB453` | **JHash:** `0x20B61DDE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_STOLEN(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_STOLEN, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Only returns true if the vehicle was marked as stolen with SET_VEHICLE_IS_STOLEN.

---

### SET_VEHICLE_IS_STOLEN

**Hash:** `0x67B2C79AA7FF5738` | **JHash:** `0x70912E42` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_IS_STOLEN(Vehicle vehicle, BOOL isStolen)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IS_STOLEN, vehicle, isStolen);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `isStolen` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PLANE_TURBULENCE_MULTIPLIER

**Hash:** `0xAD2D28A1AFDFF131` | **JHash:** `0xED159AE6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLANE_TURBULENCE_MULTIPLIER(Vehicle vehicle, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLANE_TURBULENCE_MULTIPLIER, vehicle, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** This native sets the turbulence multiplier. It only works for planes.
0.0 = no turbulence at all.
1.0 = heavy turbulence.
Works by just calling it once, does not need to be called every tick.

---

### ARE_WINGS_OF_PLANE_INTACT

**Hash:** `0x5991A01434CE9677` | **JHash:** `0xAF8CB3DF` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_WINGS_OF_PLANE_INTACT(Vehicle plane)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_WINGS_OF_PLANE_INTACT, plane);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `plane` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### ALLOW_AMBIENT_VEHICLES_TO_AVOID_ADVERSE_CONDITIONS

**Hash:** `0xB264C4D2F2B0A78B` | **JHash:** `0x45F72495` | **Build:** `323`

**C++ 原型:**
```cpp
void ALLOW_AMBIENT_VEHICLES_TO_AVOID_ADVERSE_CONDITIONS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ALLOW_AMBIENT_VEHICLES_TO_AVOID_ADVERSE_CONDITIONS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** This native doesn't seem to do anything, might be a debug-only native.

Confirmed, it is a debug native.

---

### DETACH_VEHICLE_FROM_CARGOBOB

**Hash:** `0x0E21D3DF1051399D` | **JHash:** `0x83D3D331` | **Build:** `323`

**C++ 原型:**
```cpp
void DETACH_VEHICLE_FROM_CARGOBOB(Vehicle vehicle, Vehicle cargobob)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DETACH_VEHICLE_FROM_CARGOBOB, vehicle, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### DETACH_VEHICLE_FROM_ANY_CARGOBOB

**Hash:** `0xADF7BE450512C12F` | **JHash:** `0x50E0EABE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DETACH_VEHICLE_FROM_ANY_CARGOBOB(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DETACH_VEHICLE_FROM_ANY_CARGOBOB, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### DETACH_ENTITY_FROM_CARGOBOB

**Hash:** `0xAF03011701811146` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL DETACH_ENTITY_FROM_CARGOBOB(Vehicle cargobob, Entity entity)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DETACH_ENTITY_FROM_CARGOBOB, cargobob, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `entity` | `Entity` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_VEHICLE_ATTACHED_TO_CARGOBOB

**Hash:** `0xD40148F22E81A1D9` | **JHash:** `0x5DEEC76C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_ATTACHED_TO_CARGOBOB(Vehicle cargobob, Vehicle vehicleAttached)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_ATTACHED_TO_CARGOBOB, cargobob, vehicleAttached);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `vehicleAttached` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_VEHICLE_ATTACHED_TO_CARGOBOB

**Hash:** `0x873B82D42AC2B9E5` | **JHash:** `0x301A1D24` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle GET_VEHICLE_ATTACHED_TO_CARGOBOB(Vehicle cargobob)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_ATTACHED_TO_CARGOBOB, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Returns attached vehicle (Vehicle in parameter must be cargobob)

---

### GET_ENTITY_ATTACHED_TO_CARGOBOB

**Hash:** `0x99093F60746708CA` | **Build:** `1103`

**C++ 原型:**
```cpp
Entity GET_ENTITY_ATTACHED_TO_CARGOBOB(Any p0)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ENTITY_ATTACHED_TO_CARGOBOB, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `Entity` → `int`

---

### ATTACH_VEHICLE_TO_CARGOBOB

**Hash:** `0x4127F1D84E347769` | **JHash:** `0x607DC9D5` | **Build:** `323`

**C++ 原型:**
```cpp
void ATTACH_VEHICLE_TO_CARGOBOB(Vehicle cargobob, Vehicle vehicle, int p2, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_VEHICLE_TO_CARGOBOB, cargobob, vehicle, p2, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `p2` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### ATTACH_ENTITY_TO_CARGOBOB

**Hash:** `0xA1DD82F3CCF9A01E` | **JHash:** `0xAEB29F98` | **Build:** `944`

**C++ 原型:**
```cpp
void ATTACH_ENTITY_TO_CARGOBOB(Any p0, Any p1, Any p2, Any p3, Any p4, Any p5)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ATTACH_ENTITY_TO_CARGOBOB, p0, p1, p2, p3, p4, p5);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |
| `p5` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_FORCE_DONT_DETACH_VEHICLE

**Hash:** `0x571FEB383F629926` | **JHash:** `0x49949FDA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_FORCE_DONT_DETACH_VEHICLE(Vehicle cargobob, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_FORCE_DONT_DETACH_VEHICLE, cargobob, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Stops cargobob from being able to detach the attached vehicle.

---

### SET_CARGOBOB_EXCLUDE_FROM_PICKUP_ENTITY

**Hash:** `0x1F34B0626C594380` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_CARGOBOB_EXCLUDE_FROM_PICKUP_ENTITY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_EXCLUDE_FROM_PICKUP_ENTITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### CAN_CARGOBOB_PICK_UP_ENTITY

**Hash:** `0x2C1D8B3B19E517CC` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL CAN_CARGOBOB_PICK_UP_ENTITY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_CARGOBOB_PICK_UP_ENTITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### GET_ATTACHED_PICK_UP_HOOK_POSITION

**Hash:** `0xCBDB9B923CACC92D` | **JHash:** `0xE6633DCF` | **Build:** `323`

**C++ 原型:**
```cpp
Vector3 GET_ATTACHED_PICK_UP_HOOK_POSITION(Vehicle cargobob)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.GET_ATTACHED_PICK_UP_HOOK_POSITION, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Gets the position of the cargobob hook, in world coords.

---

### DOES_CARGOBOB_HAVE_PICK_UP_ROPE

**Hash:** `0x1821D91AD4B56108` | **JHash:** `0xAF769B81` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_CARGOBOB_HAVE_PICK_UP_ROPE(Vehicle cargobob)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CARGOBOB_HAVE_PICK_UP_ROPE, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true only when the hook is active, will return false if the magnet is active

---

### CREATE_PICK_UP_ROPE_FOR_CARGOBOB

**Hash:** `0x7BEB0C7A235F6F3B` | **JHash:** `0x4D3C9A99` | **Build:** `323`

**C++ 原型:**
```cpp
void CREATE_PICK_UP_ROPE_FOR_CARGOBOB(Vehicle cargobob, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CREATE_PICK_UP_ROPE_FOR_CARGOBOB, cargobob, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Drops the Hook/Magnet on a cargobob

state
enum eCargobobHook
{
  CARGOBOB_HOOK = 0,
    CARGOBOB_MAGNET = 1,
};

---

### REMOVE_PICK_UP_ROPE_FOR_CARGOBOB

**Hash:** `0x9768CF648F54C804` | **JHash:** `0xA8211EE9` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_PICK_UP_ROPE_FOR_CARGOBOB(Vehicle cargobob)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_PICK_UP_ROPE_FOR_CARGOBOB, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Retracts the hook on the cargobob.

Note: after you retract it the natives for dropping the hook no longer work

---

### SET_PICKUP_ROPE_LENGTH_FOR_CARGOBOB

**Hash:** `0x877C1EAEAC531023` | **JHash:** `0x3A8AB081` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PICKUP_ROPE_LENGTH_FOR_CARGOBOB(Vehicle cargobob, float length1, float length2, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_ROPE_LENGTH_FOR_CARGOBOB, cargobob, length1, length2, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `length1` | `float` | `float` | |
| `length2` | `float` | `float` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** min: 1.9f, max: 100.0f

---

### SET_PICKUP_ROPE_LENGTH_WITHOUT_CREATING_ROPE_FOR_CARGOBOB

**Hash:** `0xC0ED6438E6D39BA8` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_PICKUP_ROPE_LENGTH_WITHOUT_CREATING_ROPE_FOR_CARGOBOB(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_ROPE_LENGTH_WITHOUT_CREATING_ROPE_FOR_CARGOBOB, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_ROPE_DAMPING_MULTIPLIER

**Hash:** `0xCF1182F682F65307` | **JHash:** `0x7D927E1B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_ROPE_DAMPING_MULTIPLIER(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_ROPE_DAMPING_MULTIPLIER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_ROPE_TYPE

**Hash:** `0x0D5F65A8F4EBDAB5` | **JHash:** `0xF258ADA1` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_ROPE_TYPE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_ROPE_TYPE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### DOES_CARGOBOB_HAVE_PICKUP_MAGNET

**Hash:** `0x6E08BF5B3722BAC9` | **JHash:** `0x4778CA0A` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_CARGOBOB_HAVE_PICKUP_MAGNET(Vehicle cargobob)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_CARGOBOB_HAVE_PICKUP_MAGNET, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true only when the magnet is active, will return false if the hook is active

---

### SET_CARGOBOB_PICKUP_MAGNET_ACTIVE

**Hash:** `0x9A665550F8DA349B` | **JHash:** `0xF57066DA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_ACTIVE(Vehicle cargobob, BOOL isActive)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_ACTIVE, cargobob, isActive);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `isActive` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Won't attract or magnetize to any helicopters or planes of course, but that's common sense.

---

### SET_CARGOBOB_PICKUP_MAGNET_STRENGTH

**Hash:** `0xBCBFCD9D1DAC19E2` | **JHash:** `0xCC3CF97D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_STRENGTH(Vehicle cargobob, float strength)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_STRENGTH, cargobob, strength);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `strength` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_FALLOFF

**Hash:** `0xA17BAD153B51547E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_FALLOFF(Vehicle cargobob, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_FALLOFF, cargobob, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_REDUCED_STRENGTH

**Hash:** `0x66979ACF5102FD2F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_REDUCED_STRENGTH(Vehicle cargobob, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_REDUCED_STRENGTH, cargobob, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_REDUCED_FALLOFF

**Hash:** `0x6D8EAC07506291FB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_REDUCED_FALLOFF(Vehicle cargobob, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_REDUCED_FALLOFF, cargobob, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_PULL_STRENGTH

**Hash:** `0xED8286F71A819BAA` | **JHash:** `0x8191BC1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_PULL_STRENGTH(Vehicle cargobob, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_PULL_STRENGTH, cargobob, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `cargobob` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_PULL_ROPE_LENGTH

**Hash:** `0x685D5561680D088B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_PULL_ROPE_LENGTH(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_PULL_ROPE_LENGTH, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_SET_TARGETED_MODE

**Hash:** `0xE301BD63E9E13CF0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_SET_TARGETED_MODE(Vehicle vehicle, Vehicle cargobob)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_SET_TARGETED_MODE, vehicle, cargobob);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `cargobob` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_SET_AMBIENT_MODE

**Hash:** `0x9BDDC73CC6A115D4` | **JHash:** `0x50CDB295` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_SET_AMBIENT_MODE(Vehicle vehicle, BOOL p1, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_SET_AMBIENT_MODE, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_CARGOBOB_PICKUP_MAGNET_ENSURE_PICKUP_ENTITY_UPRIGHT

**Hash:** `0x56EB5E94318D3FB6` | **JHash:** `0x30F43FE3` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CARGOBOB_PICKUP_MAGNET_ENSURE_PICKUP_ENTITY_UPRIGHT(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_PICKUP_MAGNET_ENSURE_PICKUP_ENTITY_UPRIGHT, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DOES_VEHICLE_HAVE_WEAPONS

**Hash:** `0x25ECB9F8017D98E0` | **JHash:** `0xB2E1E1FB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_VEHICLE_HAVE_WEAPONS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_VEHICLE_HAVE_WEAPONS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_WILL_TELL_OTHERS_TO_HURRY

**Hash:** `0x2C4A1590ABF43E8B` | **JHash:** `0x2EC19A8B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WILL_TELL_OTHERS_TO_HURRY(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WILL_TELL_OTHERS_TO_HURRY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### DISABLE_VEHICLE_WEAPON

**Hash:** `0xF4FC6A6F67D8D856` | **JHash:** `0xA688B7D1` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_VEHICLE_WEAPON(BOOL disabled, Hash weaponHash, Vehicle vehicle, Ped owner)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_VEHICLE_WEAPON, disabled, weaponHash, vehicle, owner);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `disabled` | `BOOL` | `bool` | |
| `weaponHash` | `Hash` | `uint` | |
| `vehicle` | `Vehicle` | `int` | |
| `owner` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree (Search for VEHICLE_*): https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### IS_VEHICLE_WEAPON_DISABLED

**Hash:** `0x563B65A643ED072E` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_WEAPON_DISABLED(Hash weaponHash, Vehicle vehicle, Ped owner)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_WEAPON_DISABLED, weaponHash, vehicle, owner);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `vehicle` | `Vehicle` | `int` | |
| `owner` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree (Search for VEHICLE_*): https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_VEHICLE_USED_FOR_PILOT_SCHOOL

**Hash:** `0xE05DD0E9707003A3` | **JHash:** `0x123E5B90` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_USED_FOR_PILOT_SCHOOL(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USED_FOR_PILOT_SCHOOL, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_ACTIVE_FOR_PED_NAVIGATION

**Hash:** `0x21115BCD6E44656A` | **JHash:** `0xEBC225C1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_ACTIVE_FOR_PED_NAVIGATION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_ACTIVE_FOR_PED_NAVIGATION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_CLASS

**Hash:** `0x29439776AAA00A62` | **JHash:** `0xC025338E` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_CLASS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_CLASS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Returns an int

Vehicle Classes:
0: Compacts
1: Sedans
2: SUVs
3: Coupes
4: Muscle
5: Sports Classics
6: Sports
7: Super
8: Motorcycles
9: Off-road
10: Industrial
11: Utility
12: Vans
13: Cycles
14: Boats
15: Helicopters
16: Planes
17: Service
18: Emergency
19: Military
20: Commercial
21: Trains

char buffer[128];
std::sprintf(buffer, "VEH_CLASS_%i", VEHICLE::GET_VEHICLE_CLASS(vehicle));

const char* className = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(buffer);

---

### GET_VEHICLE_CLASS_FROM_NAME

**Hash:** `0xDEDF1C8BD47C2200` | **JHash:** `0xEA469980` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_VEHICLE_CLASS_FROM_NAME(Hash modelHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_CLASS_FROM_NAME, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `modelHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** char buffer[128];
std::sprintf(buffer, "VEH_CLASS_%i", VEHICLE::GET_VEHICLE_CLASS_FROM_NAME (hash));

const char* className = HUD::GET_FILENAME_FOR_AUDIO_CONVERSATION(buffer);

Full list of vehicles by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/vehicles.json

---

### SET_PLAYERS_LAST_VEHICLE

**Hash:** `0xBCDF8BAF56C87B6A` | **JHash:** `0xDE86447D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLAYERS_LAST_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYERS_LAST_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_CAN_BE_USED_BY_FLEEING_PEDS

**Hash:** `0x300504B23BD3B711` | **JHash:** `0x5130DB1E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CAN_BE_USED_BY_FLEEING_PEDS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CAN_BE_USED_BY_FLEEING_PEDS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_AIRCRAFT_PILOT_SKILL_NOISE_SCALAR

**Hash:** `0xE5810AC70602F2F5` | **JHash:** `0xB6BE07E0` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AIRCRAFT_PILOT_SKILL_NOISE_SCALAR(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AIRCRAFT_PILOT_SKILL_NOISE_SCALAR, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DROPS_MONEY_WHEN_BLOWN_UP

**Hash:** `0x068F64F2470F9656` | **JHash:** `0x4BB5605D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_DROPS_MONEY_WHEN_BLOWN_UP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DROPS_MONEY_WHEN_BLOWN_UP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Money pickups are created around cars when they explode. Only works when the vehicle model is a car. A single pickup is between 1 and 18 dollars in size. All car models seem to give the same amount of money.

youtu.be/3arlUxzHl5Y 
i.imgur.com/WrNpYFs.jpg

---

### SET_VEHICLE_KEEP_ENGINE_ON_WHEN_ABANDONED

**Hash:** `0xB8FBC8B1330CA9B4` | **JHash:** `0x51E0064F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_KEEP_ENGINE_ON_WHEN_ABANDONED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_KEEP_ENGINE_ON_WHEN_ABANDONED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_IMPATIENCE_TIMER

**Hash:** `0x6A973569BA094650` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_IMPATIENCE_TIMER(Vehicle vehicle, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_IMPATIENCE_TIMER, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Seems to copy some values in vehicle

---

### SET_VEHICLE_HANDLING_OVERRIDE

**Hash:** `0x10655FAB9915623D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_HANDLING_OVERRIDE(Vehicle vehicle, Hash hash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_HANDLING_OVERRIDE, vehicle, hash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `hash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Use the "AIHandling" string found in handling.meta

---

### SET_VEHICLE_EXTENDED_REMOVAL_RANGE

**Hash:** `0x79DF7E806202CE01` | **JHash:** `0xAEF9611C` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXTENDED_REMOVAL_RANGE(Vehicle vehicle, int range)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXTENDED_REMOVAL_RANGE, vehicle, range);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `range` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Max value is 32767

---

### SET_VEHICLE_STEERING_BIAS_SCALAR

**Hash:** `0x9007A2F21DC108D4` | **JHash:** `0x585E49B6` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_STEERING_BIAS_SCALAR(Any p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_STEERING_BIAS_SCALAR, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_HELI_CONTROL_LAGGING_RATE_SCALAR

**Hash:** `0x6E0859B530A365CC` | **JHash:** `0x6E67FD35` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_HELI_CONTROL_LAGGING_RATE_SCALAR(Vehicle helicopter, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_CONTROL_LAGGING_RATE_SCALAR, helicopter, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `helicopter` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** value between 0.0 and 1.0

---

### SET_VEHICLE_FRICTION_OVERRIDE

**Hash:** `0x1837AF7C627009BA` | **JHash:** `0x32AFD42E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FRICTION_OVERRIDE(Vehicle vehicle, float friction)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FRICTION_OVERRIDE, vehicle, friction);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `friction` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Seems to be related to the metal parts, not tyres (like i was expecting lol)

---

### SET_VEHICLE_WHEELS_CAN_BREAK_OFF_WHEN_BLOW_UP

**Hash:** `0xA37B9A517B133349` | **JHash:** `0x670913A4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_WHEELS_CAN_BREAK_OFF_WHEN_BLOW_UP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WHEELS_CAN_BREAK_OFF_WHEN_BLOW_UP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### ARE_PLANE_CONTROL_PANELS_INTACT

**Hash:** `0xF78F94D60248C737` | **JHash:** `0x3B51B348` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL ARE_PLANE_CONTROL_PANELS_INTACT(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_PLANE_CONTROL_PANELS_INTACT, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_CEILING_HEIGHT

**Hash:** `0xA46413066687A328` | **JHash:** `0x98A10A86` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_CEILING_HEIGHT(Vehicle vehicle, float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_CEILING_HEIGHT, vehicle, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_NO_EXPLOSION_DAMAGE_FROM_DRIVER

**Hash:** `0x5E569EC46EC21CAE` | **JHash:** `0xBC649C49` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NO_EXPLOSION_DAMAGE_FROM_DRIVER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NO_EXPLOSION_DAMAGE_FROM_DRIVER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### CLEAR_VEHICLE_ROUTE_HISTORY

**Hash:** `0x6D6AF961B72728AE` | **JHash:** `0x8DD9AA0C` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_ROUTE_HISTORY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_ROUTE_HISTORY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### DOES_VEHICLE_EXIST_WITH_DECORATOR

**Hash:** `0x956B409B984D9BF7` | **JHash:** `0x39E68EDD` | **Build:** `323`

**C++ 原型:**
```cpp
Vehicle DOES_VEHICLE_EXIST_WITH_DECORATOR(const char* decorator)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.DOES_VEHICLE_EXIST_WITH_DECORATOR, decorator);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `decorator` | `const char*` | `string` | |

**返回值:** `Vehicle` → `int`

---

### SET_VEHICLE_AI_CAN_USE_EXCLUSIVE_SEATS

**Hash:** `0x41062318F23ED854` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_AI_CAN_USE_EXCLUSIVE_SEATS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_AI_CAN_USE_EXCLUSIVE_SEATS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used to be incorrectly named SET_VEHICLE_EXCLUSIVE_DRIVER
Toggles a flag related to SET_VEHICLE_EXCLUSIVE_DRIVER, however, doesn't enable that feature (or trigger script events related to it).

---

### SET_VEHICLE_EXCLUSIVE_DRIVER

**Hash:** `0xB5C51B5502E85E83` | **JHash:** `0xAA8BD440` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_EXCLUSIVE_DRIVER(Vehicle vehicle, Ped ped, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_EXCLUSIVE_DRIVER, vehicle, ped, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ped` | `Ped` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** index: 0 - 1

Used to be incorrectly named _SET_VEHICLE_EXCLUSIVE_DRIVER_2

---

### IS_PED_EXCLUSIVE_DRIVER_OF_VEHICLE

**Hash:** `0xB09D25E77C33EB3F` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL IS_PED_EXCLUSIVE_DRIVER_OF_VEHICLE(Ped ped, Vehicle vehicle, int* outIndex)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_EXCLUSIVE_DRIVER_OF_VEHICLE, ped, vehicle, outIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `outIndex` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### DISABLE_INDIVIDUAL_PLANE_PROPELLER

**Hash:** `0x500873A45724C863` | **JHash:** `0x004926A3` | **Build:** `323`

**C++ 原型:**
```cpp
void DISABLE_INDIVIDUAL_PLANE_PROPELLER(Vehicle vehicle, int propeller)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.DISABLE_INDIVIDUAL_PLANE_PROPELLER, vehicle, propeller);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `propeller` | `int` | `int` | |

**返回值:** `void` → `void`

---

### _ENABLE_INDIVIDUAL_PLANE_PROPELLER

**Hash:** `0xDC05D2777F855F44` | **Build:** `3407`

**C++ 原型:**
```cpp
void _ENABLE_INDIVIDUAL_PLANE_PROPELLER(Vehicle vehicle, int propeller)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._ENABLE_INDIVIDUAL_PLANE_PROPELLER, vehicle, propeller);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `propeller` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_FORCE_AFTERBURNER

**Hash:** `0xB055A34527CB8FD7` | **JHash:** `0xC195803B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_FORCE_AFTERBURNER(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_FORCE_AFTERBURNER, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DONT_PROCESS_VEHICLE_GLASS

**Hash:** `0x1087BC8EC540DAEB` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_DONT_PROCESS_VEHICLE_GLASS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DONT_PROCESS_VEHICLE_GLASS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** R* used it to "remove" vehicle windows when "nightshark" had some mod, which adding some kind of armored windows. When enabled, you can't break vehicles glass. All your bullets wiil shoot through glass. You also will not able to break the glass with any other way (hitting and etc)

---

### SET_DISABLE_WANTED_CONES_RESPONSE

**Hash:** `0x4AD280EB48B2D8E6` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_DISABLE_WANTED_CONES_RESPONSE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_WANTED_CONES_RESPONSE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_USE_DESIRED_Z_CRUISE_SPEED_FOR_LANDING

**Hash:** `0xB68CFAF83A02768D` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_USE_DESIRED_Z_CRUISE_SPEED_FOR_LANDING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_DESIRED_Z_CRUISE_SPEED_FOR_LANDING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_ARRIVE_DISTANCE_OVERRIDE_FOR_VEHICLE_PERSUIT_ATTACK

**Hash:** `0x0205F5365292D2EB` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_ARRIVE_DISTANCE_OVERRIDE_FOR_VEHICLE_PERSUIT_ATTACK(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ARRIVE_DISTANCE_OVERRIDE_FOR_VEHICLE_PERSUIT_ATTACK, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_READY_FOR_CLEANUP

**Hash:** `0xCF9159024555488C` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_VEHICLE_READY_FOR_CLEANUP(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_READY_FOR_CLEANUP, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_DISTANT_CARS_ENABLED

**Hash:** `0xF796359A959DF65D` | **JHash:** `0xB5CC548B` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISTANT_CARS_ENABLED(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISTANT_CARS_ENABLED, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles to render distant vehicles. They may not be vehicles but images to look like vehicles.

---

### SET_VEHICLE_NEON_COLOUR

**Hash:** `0x8E0A582209A62695` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NEON_COLOUR(Vehicle vehicle, int r, int g, int b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NEON_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int` | `int` | |
| `g` | `int` | `int` | |
| `b` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the color of the neon lights of the specified vehicle.

---

### SET_VEHICLE_NEON_INDEX_COLOUR

**Hash:** `0xB93B2867F7B479D1` | **Build:** `1493`

**C++ 原型:**
```cpp
void SET_VEHICLE_NEON_INDEX_COLOUR(Vehicle vehicle, int index)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NEON_INDEX_COLOUR, vehicle, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Index references CVehicleModelColor

---

### GET_VEHICLE_NEON_COLOUR

**Hash:** `0x7619EEE8C886757F` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_NEON_COLOUR(Vehicle vehicle, int* r, int* g, int* b)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_NEON_COLOUR, vehicle, r, g, b);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `r` | `int*` | `int*` | |
| `g` | `int*` | `int*` | |
| `b` | `int*` | `int*` | |

**返回值:** `void` → `void`

**说明:** Gets the color of the neon lights of the specified vehicle.

See SET_VEHICLE_NEON_COLOUR (0x8E0A582209A62695) for more information

---

### SET_VEHICLE_NEON_ENABLED

**Hash:** `0x2AA720E4287BF269` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_NEON_ENABLED(Vehicle vehicle, int index, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_NEON_ENABLED, vehicle, index, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets the neon lights of the specified vehicle on/off.

Indices:
0 = Left
1 = Right
2 = Front
3 = Back

---

### GET_VEHICLE_NEON_ENABLED

**Hash:** `0x8C4B92553E4766A5` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_NEON_ENABLED(Vehicle vehicle, int index)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_NEON_ENABLED, vehicle, index);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** indices:
0 = Left
1 = Right
2 = Front
3 = Back

---

### SET_AMBIENT_VEHICLE_NEON_ENABLED

**Hash:** `0x35E0654F4BAD7971` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_AMBIENT_VEHICLE_NEON_ENABLED(BOOL p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AMBIENT_VEHICLE_NEON_ENABLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SUPPRESS_NEONS_ON_VEHICLE

**Hash:** `0x83F813570FF519DE` | **Build:** `573`

**C++ 原型:**
```cpp
void SUPPRESS_NEONS_ON_VEHICLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SUPPRESS_NEONS_ON_VEHICLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_SUPERDUMMY

**Hash:** `0xB088E9A47AE6EDD5` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DISABLE_SUPERDUMMY(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_SUPERDUMMY, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### REQUEST_VEHICLE_DIAL

**Hash:** `0xDBA3C090E3D74690` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_VEHICLE_DIAL(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_VEHICLE_DIAL, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_BODY_HEALTH

**Hash:** `0xF271147EB7B40F12` | **JHash:** `0x2B2FCC28` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_BODY_HEALTH(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_BODY_HEALTH, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Seems related to vehicle health, like the one in IV.
Max 1000, min 0.
Vehicle does not necessarily explode or become undrivable at 0.

---

### SET_VEHICLE_BODY_HEALTH

**Hash:** `0xB77D05AC8C78AADB` | **JHash:** `0x920C2517` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BODY_HEALTH(Vehicle vehicle, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BODY_HEALTH, vehicle, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** p2 often set to 1000.0 in the decompiled scripts.

---

### GET_VEHICLE_SIZE

**Hash:** `0xDF7E3EEB29642C38` | **Build:** `323`

**C++ 原型:**
```cpp
void GET_VEHICLE_SIZE(Vehicle vehicle, Vector3* out1, Vector3* out2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GET_VEHICLE_SIZE, vehicle, out1, out2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `out1` | `Vector3*` | `Vector3*` | |
| `out2` | `Vector3*` | `Vector3*` | |

**返回值:** `void` → `void`

**说明:** Outputs 2 Vector3's.
Scripts check if out2.x - out1.x > someshit.x
Could be suspension related, as in max suspension height and min suspension height, considering the natives location.

---

### GET_FAKE_SUSPENSION_LOWERING_AMOUNT

**Hash:** `0x53952FD2BAA19F17` | **JHash:** `0xB73A1486` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_FAKE_SUSPENSION_LOWERING_AMOUNT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_FAKE_SUSPENSION_LOWERING_AMOUNT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

**说明:** Gets the height of the vehicle's suspension.
The higher the value the lower the suspension. Each 0.002 corresponds with one more level lowered.
0.000 is the stock suspension.
0.008 is Ultra Suspension.

---

### SET_CAR_HIGH_SPEED_BUMP_SEVERITY_MULTIPLIER

**Hash:** `0x84FD40F56075E816` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAR_HIGH_SPEED_BUMP_SEVERITY_MULTIPLIER(float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAR_HIGH_SPEED_BUMP_SEVERITY_MULTIPLIER, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_NUMBER_OF_VEHICLE_DOORS

**Hash:** `0x92922A607497B14D` | **Build:** `463`

**C++ 原型:**
```cpp
int GET_NUMBER_OF_VEHICLE_DOORS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_NUMBER_OF_VEHICLE_DOORS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

---

### SET_HYDRAULICS_CONTROL

**Hash:** `0x28B18377EB6E25F6` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_HYDRAULICS_CONTROL(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HYDRAULICS_CONTROL, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If false, lowers hydraulics (if raised) and disables hydraulics controls. If true, raises hydraulics and enables hydraulics controls.

Only used once in each carmod script, on a car that does not have hydraulics to begin with.

---

### SET_CAN_ADJUST_GROUND_CLEARANCE

**Hash:** `0xA7DCDF4DED40A8F4` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CAN_ADJUST_GROUND_CLEARANCE(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_ADJUST_GROUND_CLEARANCE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_HEALTH_PERCENTAGE

**Hash:** `0xB8EF61207C2393A9` | **JHash:** `0x196BA503` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_VEHICLE_HEALTH_PERCENTAGE(Vehicle vehicle, float maxEngineHealth, float maxPetrolTankHealth, float maxBodyHealth, float maxMainRotorHealth, float maxTailRotorHealth, float maxUnkHealth)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_VEHICLE_HEALTH_PERCENTAGE, vehicle, maxEngineHealth, maxPetrolTankHealth, maxBodyHealth, maxMainRotorHealth, maxTailRotorHealth, maxUnkHealth);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `maxEngineHealth` | `float` | `float` | |
| `maxPetrolTankHealth` | `float` | `float` | |
| `maxBodyHealth` | `float` | `float` | |
| `maxMainRotorHealth` | `float` | `float` | |
| `maxTailRotorHealth` | `float` | `float` | |
| `maxUnkHealth` | `float` | `float` | |

**返回值:** `float` → `float`

**说明:** 0 min 100 max
starts at 100
Seams to have health zones
Front of vehicle when damaged goes from 100-50 and stops at 50.
Rear can be damaged from 100-0
Only tested with two cars.

any idea how this differs from the first one?

--
May return the vehicle health on a scale of 0.0 - 100.0 (needs to be confirmed)

example:

v_F = ENTITY::GET_ENTITY_MODEL(v_3);
if (((v_F == ${tanker}) || (v_F == ${armytanker})) || (v_F == ${tanker2})) {
    if (VEHICLE::GET_VEHICLE_HEALTH_PERCENTAGE(v_3) <= 1.0) {
        NETWORK::NETWORK_EXPLODE_VEHICLE(v_3, 1, 1, -1);
    }
}

---

### GET_VEHICLE_IS_MERCENARY

**Hash:** `0xD4C4642CB7F50B5D` | **JHash:** `0x575504DE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_IS_MERCENARY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_IS_MERCENARY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_BROKEN_PARTS_DONT_AFFECT_AI_HANDLING

**Hash:** `0xC361AA040D6637A8` | **JHash:** `0x819CD954` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_BROKEN_PARTS_DONT_AFFECT_AI_HANDLING(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BROKEN_PARTS_DONT_AFFECT_AI_HANDLING, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_KERS_ALLOWED

**Hash:** `0x99C82F8A139F3E4E` | **JHash:** `0x71CDD52F` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_VEHICLE_KERS_ALLOWED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_KERS_ALLOWED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_VEHICLE_HAS_KERS

**Hash:** `0x50634E348C8D44EF` | **JHash:** `0x0761E635` | **Build:** `372`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_HAS_KERS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_HAS_KERS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle has a HF_HAS_KERS (strHandlingFlags 0x4) handing flag set, for instance the lectro/vindicator bikes or the open wheelers.

---

### SET_PLANE_RESIST_TO_EXPLOSION

**Hash:** `0xE16142B94664DEFD` | **JHash:** `0xFBBA699A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PLANE_RESIST_TO_EXPLOSION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLANE_RESIST_TO_EXPLOSION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_HELI_RESIST_TO_EXPLOSION

**Hash:** `0x8074CC1886802912` | **Build:** `2545`

**C++ 原型:**
```cpp
void SET_HELI_RESIST_TO_EXPLOSION(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_RESIST_TO_EXPLOSION, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_BMX_EXTRA_TRICK_FORCES

**Hash:** `0x26D99D5A82FD18E8` | **Build:** `463`

**C++ 原型:**
```cpp
void SET_DISABLE_BMX_EXTRA_TRICK_FORCES(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_BMX_EXTRA_TRICK_FORCES, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_HYDRAULIC_SUSPENSION_RAISE_FACTOR

**Hash:** `0x84EA99C62CB3EF0C` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_HYDRAULIC_SUSPENSION_RAISE_FACTOR(Vehicle vehicle, int wheelId, float value)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HYDRAULIC_SUSPENSION_RAISE_FACTOR, vehicle, wheelId, value);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelId` | `int` | `int` | |
| `value` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Works only on vehicles that support hydraulics.

---

### GET_HYDRAULIC_SUSPENSION_RAISE_FACTOR

**Hash:** `0x0BB5CBDDD0F25AE3` | **Build:** `2372`

**C++ 原型:**
```cpp
float GET_HYDRAULIC_SUSPENSION_RAISE_FACTOR(Vehicle vehicle, int wheelId)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_HYDRAULIC_SUSPENSION_RAISE_FACTOR, vehicle, wheelId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelId` | `int` | `int` | |

**返回值:** `float` → `float`

---

### SET_CAN_USE_HYDRAULICS

**Hash:** `0x1201E8A3290A3B98` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_CAN_USE_HYDRAULICS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_USE_HYDRAULICS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_HYDRAULIC_VEHICLE_STATE

**Hash:** `0x8EA86DF356801C7D` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_HYDRAULIC_VEHICLE_STATE(Vehicle vehicle, int state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HYDRAULIC_VEHICLE_STATE, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** States:
4 = raise
5 = lower
6 = jump

---

### SET_HYDRAULIC_WHEEL_STATE

**Hash:** `0xC24075310A8B9CD1` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_HYDRAULIC_WHEEL_STATE(Vehicle vehicle, int wheelId, int state, float value, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HYDRAULIC_WHEEL_STATE, vehicle, wheelId, state, value, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelId` | `int` | `int` | |
| `state` | `int` | `int` | |
| `value` | `float` | `float` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

**说明:** Sets vehicle wheel hydraulic states transition. Known states:
0 - reset
1 - raise wheel (uses value arg, works just like _SET_VEHICLE_HYDRAULIC_WHEEL_VALUE)
2 - jump using wheel

---

### HAS_VEHICLE_PETROLTANK_SET_ON_FIRE_BY_ENTITY

**Hash:** `0x5BA68A0840D546AC` | **Build:** `463`

**C++ 原型:**
```cpp
BOOL HAS_VEHICLE_PETROLTANK_SET_ON_FIRE_BY_ENTITY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VEHICLE_PETROLTANK_SET_ON_FIRE_BY_ENTITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_VEHICLE_PETROLTANK_FIRE_CULPRIT

**Hash:** `0x4419966C9936071A` | **Build:** `463`

**C++ 原型:**
```cpp
void CLEAR_VEHICLE_PETROLTANK_FIRE_CULPRIT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_VEHICLE_PETROLTANK_FIRE_CULPRIT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_BOBBLEHEAD_VELOCITY

**Hash:** `0x870B8B7A766615C8` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_VEHICLE_BOBBLEHEAD_VELOCITY(float x, float y, float p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BOBBLEHEAD_VELOCITY, x, y, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `p2` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Controls how fast bobbleheads and tsurikawas move on each axis.

p2 is probably z, but changing that value didn't seem to have a noticeable effect.

---

### GET_VEHICLE_IS_DUMMY

**Hash:** `0x8533CAFDE1F0F336` | **Build:** `505`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_IS_DUMMY(Any p0)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_IS_DUMMY, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_DAMAGE_SCALE

**Hash:** `0x4E20D2A627011E8E` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL SET_VEHICLE_DAMAGE_SCALE(Vehicle vehicle, float p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_VEHICLE_DAMAGE_SCALE, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### _SET_VEHICLE_EXPLOSIVE_DAMAGE_SCALE

**Hash:** `0x84D7FFD223CAAFFD` | **Build:** `3407`

**C++ 原型:**
```cpp
Any _SET_VEHICLE_EXPLOSIVE_DAMAGE_SCALE(Vehicle vehicle, float scale)
```

**C# 调用格式:**
```csharp
object result = Function.Call<object>(Hash._SET_VEHICLE_EXPLOSIVE_DAMAGE_SCALE, vehicle, scale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `scale` | `float` | `float` | |

**返回值:** `Any` → `object`

**说明:** Does not actually return anything.

---

### SET_VEHICLE_WEAPON_DAMAGE_SCALE

**Hash:** `0x45A561A9421AB6AD` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL SET_VEHICLE_WEAPON_DAMAGE_SCALE(Vehicle vehicle, float multiplier)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_VEHICLE_WEAPON_DAMAGE_SCALE, vehicle, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_DISABLE_DAMAGE_WITH_PICKED_UP_ENTITY

**Hash:** `0xD4196117AF7BB974` | **Build:** `757`

**C++ 原型:**
```cpp
BOOL SET_DISABLE_DAMAGE_WITH_PICKED_UP_ENTITY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_DISABLE_DAMAGE_WITH_PICKED_UP_ENTITY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

---

### SET_VEHICLE_USES_MP_PLAYER_DAMAGE_MULTIPLIER

**Hash:** `0xBB2333BB87DDD87F` | **Build:** `757`

**C++ 原型:**
```cpp
void SET_VEHICLE_USES_MP_PLAYER_DAMAGE_MULTIPLIER(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USES_MP_PLAYER_DAMAGE_MULTIPLIER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_BIKE_EASY_TO_LAND

**Hash:** `0x73561D4425A021A2` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_BIKE_EASY_TO_LAND(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_BIKE_EASY_TO_LAND, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When enabled, the player won't fall off the bike when landing from large heights.

---

### SET_INVERT_VEHICLE_CONTROLS

**Hash:** `0x5B91B229243351A8` | **Build:** `791`

**C++ 原型:**
```cpp
void SET_INVERT_VEHICLE_CONTROLS(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INVERT_VEHICLE_CONTROLS, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Inverts vehicle's controls. So INPUT_VEH_ACCELERATE will be INPUT_VEH_BRAKE and vise versa (same for A/D controls)
Doesn't work for planes/helis.

---

### SET_SPEED_BOOST_EFFECT_DISABLED

**Hash:** `0x7BBE7FF626A591FE` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_SPEED_BOOST_EFFECT_DISABLED(BOOL disabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPEED_BOOST_EFFECT_DISABLED, disabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `disabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables the screen effects and sound effects when driving over a speed boost pad.

---

### SET_SLOW_DOWN_EFFECT_DISABLED

**Hash:** `0x65B080555EA48149` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_SLOW_DOWN_EFFECT_DISABLED(BOOL disabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SLOW_DOWN_EFFECT_DISABLED, disabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `disabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables the screen effects and sound effects when driving over a slowdown pad.

---

### SET_FORMATION_LEADER

**Hash:** `0x428AD3E26C8D9EB0` | **Build:** `877`

**C++ 原型:**
```cpp
void SET_FORMATION_LEADER(Vehicle vehicle, float x, float y, float z, float p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORMATION_LEADER, vehicle, x, y, z, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `p4` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_FORMATION_LEADER

**Hash:** `0xE2F53F172B45EDE1` | **Build:** `877`

**C++ 原型:**
```cpp
void RESET_FORMATION_LEADER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_FORMATION_LEADER);
```

**返回值:** `void` → `void`

**说明:** Resets the effect of SET_FORMATION_LEADER

---

### GET_IS_BOAT_CAPSIZED

**Hash:** `0xBA91D045575699AD` | **Build:** `877`

**C++ 原型:**
```cpp
BOOL GET_IS_BOAT_CAPSIZED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_BOAT_CAPSIZED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ALLOW_RAMMING_SOOP_OR_RAMP

**Hash:** `0x80E3357FDEF45C21` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_ALLOW_RAMMING_SOOP_OR_RAMP(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ALLOW_RAMMING_SOOP_OR_RAMP, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_SCRIPT_RAMP_IMPULSE_SCALE

**Hash:** `0xEFC13B1CE30D755D` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_SCRIPT_RAMP_IMPULSE_SCALE(Vehicle vehicle, float impulseScale)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_RAMP_IMPULSE_SCALE, vehicle, impulseScale);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `impulseScale` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_IS_DOOR_VALID

**Hash:** `0x645F4B6E8499F632` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_IS_DOOR_VALID(Vehicle vehicle, int doorId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_DOOR_VALID, vehicle, doorId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `doorId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** doorId: see SET_VEHICLE_DOOR_SHUT

---

### SET_SCRIPT_ROCKET_BOOST_RECHARGE_TIME

**Hash:** `0xE00F2AB100B76E89` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_SCRIPT_ROCKET_BOOST_RECHARGE_TIME(Vehicle vehicle, float seconds)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SCRIPT_ROCKET_BOOST_RECHARGE_TIME, vehicle, seconds);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seconds` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_HAS_ROCKET_BOOST

**Hash:** `0x36D782F68B309BDA` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_HAS_ROCKET_BOOST(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_HAS_ROCKET_BOOST, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### IS_ROCKET_BOOST_ACTIVE

**Hash:** `0x3D34E80EED4AE3BE` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL IS_ROCKET_BOOST_ACTIVE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_ROCKET_BOOST_ACTIVE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_ROCKET_BOOST_ACTIVE

**Hash:** `0x81E1552E35DC3839` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_ROCKET_BOOST_ACTIVE(Vehicle vehicle, BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROCKET_BOOST_ACTIVE, vehicle, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_HAS_RETRACTABLE_WHEELS

**Hash:** `0xDCA174A42133F08C` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_HAS_RETRACTABLE_WHEELS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_HAS_RETRACTABLE_WHEELS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_IS_WHEELS_RETRACTED

**Hash:** `0x1DA0DA9CB3F0C8BF` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_IS_WHEELS_RETRACTED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_WHEELS_RETRACTED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_WHEELS_EXTENDED_INSTANTLY

**Hash:** `0xF660602546D27BA8` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_WHEELS_EXTENDED_INSTANTLY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WHEELS_EXTENDED_INSTANTLY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_WHEELS_RETRACTED_INSTANTLY

**Hash:** `0x5335BE58C083E74E` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_WHEELS_RETRACTED_INSTANTLY(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WHEELS_RETRACTED_INSTANTLY, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_CAR_HAS_JUMP

**Hash:** `0x9078C0C5EF8C19E9` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_CAR_HAS_JUMP(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CAR_HAS_JUMP, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true if the vehicle has the FLAG_JUMPING_CAR flag set.

---

### SET_USE_HIGHER_CAR_JUMP

**Hash:** `0xF06A16CA55D138D8` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_USE_HIGHER_CAR_JUMP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_HIGHER_CAR_JUMP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Allows vehicles with the FLAG_JUMPING_CAR flag to jump higher (i.e. Ruiner 2000).

---

### SET_CLEAR_FREEZE_WAITING_ON_COLLISION_ONCE_PLAYER_ENTERS

**Hash:** `0xB2E0C0D6922D31F2` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_CLEAR_FREEZE_WAITING_ON_COLLISION_ONCE_PLAYER_ENTERS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CLEAR_FREEZE_WAITING_ON_COLLISION_ONCE_PLAYER_ENTERS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_WEAPON_RESTRICTED_AMMO

**Hash:** `0x44CD1F493DB2A0A6` | **Build:** `944`

**C++ 原型:**
```cpp
void SET_VEHICLE_WEAPON_RESTRICTED_AMMO(Vehicle vehicle, int weaponIndex, int capacity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WEAPON_RESTRICTED_AMMO, vehicle, weaponIndex, capacity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `weaponIndex` | `int` | `int` | |
| `capacity` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Set vehicle's primary mounted weapon 2 ammo. For example, use it on APC.
For example, you can "remove" any vehicle weapon from any vehicle.
ammoAmount -1 = infinite ammo (default value for any spawned vehicle tho)

---

### GET_VEHICLE_WEAPON_RESTRICTED_AMMO

**Hash:** `0x8181CE2F25CB9BB7` | **Build:** `1011`

**C++ 原型:**
```cpp
int GET_VEHICLE_WEAPON_RESTRICTED_AMMO(Vehicle vehicle, int weaponIndex)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_WEAPON_RESTRICTED_AMMO, vehicle, weaponIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `weaponIndex` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_VEHICLE_HAS_PARACHUTE

**Hash:** `0xBC9CFF381338CB4F` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_HAS_PARACHUTE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_HAS_PARACHUTE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_VEHICLE_CAN_DEPLOY_PARACHUTE

**Hash:** `0xA916396DF4154EE3` | **Build:** `944`

**C++ 原型:**
```cpp
BOOL GET_VEHICLE_CAN_DEPLOY_PARACHUTE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_VEHICLE_CAN_DEPLOY_PARACHUTE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### VEHICLE_START_PARACHUTING

**Hash:** `0x0BFFB028B3DD0A97` | **Build:** `944`

**C++ 原型:**
```cpp
void VEHICLE_START_PARACHUTING(Vehicle vehicle, BOOL active)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_START_PARACHUTING, vehicle, active);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `active` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_VEHICLE_PARACHUTE_DEPLOYED

**Hash:** `0x3DE51E9C80B116CF` | **Build:** `1011`

**C++ 原型:**
```cpp
BOOL IS_VEHICLE_PARACHUTE_DEPLOYED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_VEHICLE_PARACHUTE_DEPLOYED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### VEHICLE_SET_RAMP_AND_RAMMING_CARS_TAKE_DAMAGE

**Hash:** `0x28D034A93FE31BF5` | **Build:** `944`

**C++ 原型:**
```cpp
void VEHICLE_SET_RAMP_AND_RAMMING_CARS_TAKE_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_RAMP_AND_RAMMING_CARS_TAKE_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### VEHICLE_SET_ENABLE_RAMP_CAR_SIDE_IMPULSE

**Hash:** `0x1BBAC99C0BC53656` | **Build:** `944`

**C++ 原型:**
```cpp
void VEHICLE_SET_ENABLE_RAMP_CAR_SIDE_IMPULSE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_ENABLE_RAMP_CAR_SIDE_IMPULSE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### VEHICLE_SET_ENABLE_NORMALISE_RAMP_CAR_VERTICAL_VELOCTIY

**Hash:** `0x756AE6E962168A04` | **Build:** `944`

**C++ 原型:**
```cpp
void VEHICLE_SET_ENABLE_NORMALISE_RAMP_CAR_VERTICAL_VELOCTIY(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_ENABLE_NORMALISE_RAMP_CAR_VERTICAL_VELOCTIY, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### VEHICLE_SET_JET_WASH_FORCE_ENABLED

**Hash:** `0x9D30687C57BAA0BB` | **Build:** `1011`

**C++ 原型:**
```cpp
void VEHICLE_SET_JET_WASH_FORCE_ENABLED(Any p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_JET_WASH_FORCE_ENABLED, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_WEAPON_CAN_TARGET_OBJECTS

**Hash:** `0x86B4B6212CB8B627` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_VEHICLE_WEAPON_CAN_TARGET_OBJECTS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_WEAPON_CAN_TARGET_OBJECTS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_USE_BOOST_BUTTON_FOR_WHEEL_RETRACT

**Hash:** `0x41290B40FA63E6DA` | **Build:** `1011`

**C++ 原型:**
```cpp
void SET_VEHICLE_USE_BOOST_BUTTON_FOR_WHEEL_RETRACT(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_USE_BOOST_BUTTON_FOR_WHEEL_RETRACT, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Used for blazer5. Changes the quadbike-jetski transformation input from raise/lower convertible roof (hold H by default) to horn (press E by default.)

---

### _SET_VEHICLE_USE_HORN_BUTTON_FOR_NITROUS

**Hash:** `0x1980F68872CC2C3D` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_VEHICLE_USE_HORN_BUTTON_FOR_NITROUS(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_VEHICLE_USE_HORN_BUTTON_FOR_NITROUS, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### VEHICLE_SET_PARACHUTE_MODEL_OVERRIDE

**Hash:** `0x4D610C6B56031351` | **Build:** `1011`

**C++ 原型:**
```cpp
void VEHICLE_SET_PARACHUTE_MODEL_OVERRIDE(Vehicle vehicle, Hash modelHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_PARACHUTE_MODEL_OVERRIDE, vehicle, modelHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modelHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Parachute models:
- sr_prop_specraces_para_s_01
- imp_prop_impexp_para_s (SecuroServ; Default)
Plus, many more props can be used as vehicle parachutes, like umbrellas (prop_beach_parasol_03), and unlike SET_PLAYER_PARACHUTE_MODEL_OVERRIDE, you won't get stuck mid-air when using an umbrella.

---

### VEHICLE_SET_PARACHUTE_MODEL_TINT_INDEX

**Hash:** `0xA74AD2439468C883` | **Build:** `1011`

**C++ 原型:**
```cpp
void VEHICLE_SET_PARACHUTE_MODEL_TINT_INDEX(Vehicle vehicle, int textureVariation)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.VEHICLE_SET_PARACHUTE_MODEL_TINT_INDEX, vehicle, textureVariation);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `textureVariation` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Variations available for the generic parachute (sr_prop_specraces_para_s_01):
- 0: Rainbow
- 1: Red
- 2: White, blue, yellow
- 3: Black, red, white
- 4: Red, white, blue
- 5: Blue
- 6: Black
- 7: Black, yellow

---

### VEHICLE_SET_OVERRIDE_EXTENABLE_SIDE_RATIO

**Hash:** `0x0419B167EE128F33` | **Build:** `1103`

**C++ 原型:**
```cpp
int VEHICLE_SET_OVERRIDE_EXTENABLE_SIDE_RATIO(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.VEHICLE_SET_OVERRIDE_EXTENABLE_SIDE_RATIO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### VEHICLE_SET_EXTENABLE_SIDE_TARGET_RATIO

**Hash:** `0xF3B0E0AED097A3F5` | **Build:** `1103`

**C++ 原型:**
```cpp
int VEHICLE_SET_EXTENABLE_SIDE_TARGET_RATIO(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.VEHICLE_SET_EXTENABLE_SIDE_TARGET_RATIO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### VEHICLE_SET_OVERRIDE_SIDE_RATIO

**Hash:** `0xD3E51C0AB8C26EEE` | **Build:** `1103`

**C++ 原型:**
```cpp
int VEHICLE_SET_OVERRIDE_SIDE_RATIO(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.VEHICLE_SET_OVERRIDE_SIDE_RATIO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `int` → `int`

---

### GET_ALL_VEHICLES

**Hash:** `0x9B8E1BF04B51F2E8` | **Build:** `1103`

**C++ 原型:**
```cpp
int GET_ALL_VEHICLES(Any* vehsStruct)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ALL_VEHICLES, vehsStruct);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehsStruct` | `Any*` | `object*` | |

**返回值:** `int` → `int`

---

### SET_CARGOBOB_EXTA_PICKUP_RANGE

**Hash:** `0x72BECCF4B829522E` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_CARGOBOB_EXTA_PICKUP_RANGE(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CARGOBOB_EXTA_PICKUP_RANGE, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_OVERRIDE_VEHICLE_DOOR_TORQUE

**Hash:** `0x66E3AAFACE2D1EB8` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_OVERRIDE_VEHICLE_DOOR_TORQUE(Any p0, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OVERRIDE_VEHICLE_DOOR_TORQUE, p0, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_WHEELIE_ENABLED

**Hash:** `0x1312DDD8385AEE4E` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_WHEELIE_ENABLED(Vehicle vehicle, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WHEELIE_ENABLED, vehicle, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables/disables the ability to wheelie on motorcycles.

---

### SET_DISABLE_HELI_EXPLODE_FROM_BODY_DAMAGE

**Hash:** `0xEDBC8405B3895CC9` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_DISABLE_HELI_EXPLODE_FROM_BODY_DAMAGE(Vehicle vehicle, BOOL disable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_HELI_EXPLODE_FROM_BODY_DAMAGE, vehicle, disable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `disable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Prevents the vehicle from exploding when taking body damage. Only works for helis and planes.

---

### SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_ON_COLLISION

**Hash:** `0x26E13D440E7F6064` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_ON_COLLISION(Vehicle vehicle, BOOL disable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_ON_COLLISION, vehicle, disable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `disable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Prevents the vehicle from exploding when taking body damage if it was caused by a collision. Only works for planes.

---

### _SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_RECEIVED_BY_AI_VEHICLE

**Hash:** `0xB0B7DF5CB876FF5E` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_RECEIVED_BY_AI_VEHICLE(Vehicle vehicle, BOOL disable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_DISABLE_EXPLODE_FROM_BODY_DAMAGE_RECEIVED_BY_AI_VEHICLE, vehicle, disable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `disable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Prevents the vehicle from exploding when taking body damage if the inflictor is an AI-controlled vehicle. Only works for planes.

---

### SET_TRAILER_ATTACHMENT_ENABLED

**Hash:** `0x2FA2494B47FDD009` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_TRAILER_ATTACHMENT_ENABLED(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRAILER_ATTACHMENT_ENABLED, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_ROCKET_BOOST_FILL

**Hash:** `0xFEB2DDED3509562E` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_ROCKET_BOOST_FILL(Vehicle vehicle, float percentage)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ROCKET_BOOST_FILL, vehicle, percentage);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `percentage` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_GLIDER_ACTIVE

**Hash:** `0x544996C0081ABDEB` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_GLIDER_ACTIVE(Vehicle vehicle, BOOL state)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GLIDER_ACTIVE, vehicle, state);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `state` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Set state to true to extend the wings, false to retract them.

---

### SET_SHOULD_RESET_TURRET_IN_SCRIPTED_CAMERAS

**Hash:** `0x78CEEE41F49F421F` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_SHOULD_RESET_TURRET_IN_SCRIPTED_CAMERAS(Vehicle vehicle, BOOL shouldReset)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SHOULD_RESET_TURRET_IN_SCRIPTED_CAMERAS, vehicle, shouldReset);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `shouldReset` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Resets the vehicle's turret to its default position in scripted cameras. Doesn't seem to affect turrets that are occupied by a ped.

---

### SET_VEHICLE_DISABLE_COLLISION_UPON_CREATION

**Hash:** `0xAF60E6A2936F982A` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_VEHICLE_DISABLE_COLLISION_UPON_CREATION(Vehicle vehicle, BOOL disable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DISABLE_COLLISION_UPON_CREATION, vehicle, disable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `disable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_GROUND_EFFECT_REDUCES_DRAG

**Hash:** `0x430A7631A84C9BE7` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_GROUND_EFFECT_REDUCES_DRAG(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_GROUND_EFFECT_REDUCES_DRAG, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_MAP_COLLISION

**Hash:** `0x75627043C6AA90AD` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_DISABLE_MAP_COLLISION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_MAP_COLLISION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

**说明:** Disables collision for this vehicle (maybe it also supports other entities, not sure).
Only world/building/fixed world objects will have their collisions disabled, props, peds, or any other entity still collides with the vehicle.
Example: https://streamable.com/6n45d5
Not sure if there is a native (and if so, which one) that resets the collisions.

---

### SET_DISABLE_PED_STAND_ON_TOP

**Hash:** `0x8235F1BEAD557629` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_DISABLE_PED_STAND_ON_TOP(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_PED_STAND_ON_TOP, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_VEHICLE_DAMAGE_SCALES

**Hash:** `0x9640E30A7F395E4B` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_VEHICLE_DAMAGE_SCALES(Vehicle vehicle, Any p1, Any p2, Any p3, Any p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DAMAGE_SCALES, vehicle, p1, p2, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |
| `p3` | `Any` | `object` | |
| `p4` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PLANE_SECTION_DAMAGE_SCALE

**Hash:** `0x0BBB9A7A8FFE931B` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_PLANE_SECTION_DAMAGE_SCALE(Vehicle vehicle, Any p1, Any p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLANE_SECTION_DAMAGE_SCALE, vehicle, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `Any` | `object` | |
| `p2` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### SET_PLANE_CONTROL_SECTIONS_SHOULD_BREAK_OFF_FROM_EXPLOSIONS

**Hash:** `0xDD8A2D3337F04196` | **Build:** `3407`

**C++ 原型:**
```cpp
void SET_PLANE_CONTROL_SECTIONS_SHOULD_BREAK_OFF_FROM_EXPLOSIONS(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLANE_CONTROL_SECTIONS_SHOULD_BREAK_OFF_FROM_EXPLOSIONS, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_HELI_CAN_PICKUP_ENTITY_THAT_HAS_PICK_UP_DISABLED

**Hash:** `0x94A68DA412C4007D` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_HELI_CAN_PICKUP_ENTITY_THAT_HAS_PICK_UP_DISABLED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_CAN_PICKUP_ENTITY_THAT_HAS_PICK_UP_DISABLED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Stops the cargobob from being able to attach any vehicle

---

### SET_VEHICLE_BOMB_AMMO

**Hash:** `0xF4B2ED59DEB5D774` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_VEHICLE_BOMB_AMMO(Vehicle vehicle, int bombCount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_BOMB_AMMO, vehicle, bombCount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `bombCount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Sets the amount of bombs that this vehicle has. As far as I know, this does _not_ impact vehicle weapons or the ammo of those weapons in any way, it is just a way to keep track of the amount of bombs in a specific plane.

---

### GET_VEHICLE_BOMB_AMMO

**Hash:** `0xEA12BD130D7569A1` | **Build:** `1180`

**C++ 原型:**
```cpp
int GET_VEHICLE_BOMB_AMMO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_BOMB_AMMO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Gets the amount of bombs that this vehicle has. As far as I know, this does _not_ impact vehicle weapons or the ammo of those weapons in any way, it is just a way to keep track of the amount of bombs in a specific plane.

---

### SET_VEHICLE_COUNTERMEASURE_AMMO

**Hash:** `0x9BDA23BF666F0855` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_VEHICLE_COUNTERMEASURE_AMMO(Vehicle vehicle, int counterMeasureCount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_COUNTERMEASURE_AMMO, vehicle, counterMeasureCount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `counterMeasureCount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Similar to SET_VEHICLE_BOMB_AMMO, this sets the amount of countermeasures that are present on this vehicle.
Use GET_VEHICLE_BOMB_AMMO to get the current amount.

---

### GET_VEHICLE_COUNTERMEASURE_AMMO

**Hash:** `0xF846AA63DF56B804` | **Build:** `1180`

**C++ 原型:**
```cpp
int GET_VEHICLE_COUNTERMEASURE_AMMO(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_VEHICLE_COUNTERMEASURE_AMMO, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `int` → `int`

**说明:** Similar to `GET_VEHICLE_BOMB_AMMO`, this gets the amount of countermeasures that are present on this vehicle.
Use SET_VEHICLE_COUNTERMEASURE_AMMO to set the current amount.

---

### SET_HELI_COMBAT_OFFSET

**Hash:** `0x0A3F820A9A9A9AC5` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_HELI_COMBAT_OFFSET(Vehicle vehicle, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HELI_COMBAT_OFFSET, vehicle, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_CAN_VEHICLE_BE_PLACED_HERE

**Hash:** `0x51F30DB60626A20E` | **Build:** `1180`

**C++ 原型:**
```cpp
BOOL GET_CAN_VEHICLE_BE_PLACED_HERE(Vehicle vehicle, float x, float y, float z, float rotX, float rotY, float rotZ, int p7, Any p8)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CAN_VEHICLE_BE_PLACED_HERE, vehicle, x, y, z, rotX, rotY, rotZ, p7, p8);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `rotX` | `float` | `float` | |
| `rotY` | `float` | `float` | |
| `rotZ` | `float` | `float` | |
| `p7` | `int` | `int` | |
| `p8` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** Used in decompiled scripts in combination with GET_VEHICLE_SIZE
p7 is usually 2
p8 is usually 1

---

### SET_DISABLE_AUTOMATIC_CRASH_TASK

**Hash:** `0x97841634EF7DF1D6` | **Build:** `1180`

**C++ 原型:**
```cpp
void SET_DISABLE_AUTOMATIC_CRASH_TASK(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_AUTOMATIC_CRASH_TASK, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets a flag on heli and another vehicle type.

---

### SET_SPECIAL_FLIGHT_MODE_RATIO

**Hash:** `0xD138FA15C9776837` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_SPECIAL_FLIGHT_MODE_RATIO(Vehicle vehicle, float ratio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPECIAL_FLIGHT_MODE_RATIO, vehicle, ratio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ratio` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Used in conjunction with SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO, in Rockstar's scripts. Using this will instantly transform the vehicle into hover mode starting from the given ratio (ranging from 0.0 to 1.0, values greater than 1.0 will put the vehicle into a glitched state.) If this is not used alongside SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO, the vehicle will automatically transform back into car mode.

Usable only with the deluxo and other vehicles with deluxo-like hover mode toggle, modded or otherwise. Does nothing when used on oppressor2.

Example:
Ped playerPed = PLAYER::PLAYER_PED_ID();
Vehicle veh = PED::GET_VEHICLE_PED_IS_USING(playerPed);
VEHICLE::SET_SPECIAL_FLIGHT_MODE_RATIO(veh, 0.7f);
VEHICLE::SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO(veh, 1.0f);

---

### SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO

**Hash:** `0x438B3D7CA026FE91` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO(Vehicle vehicle, float targetRatio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO, vehicle, targetRatio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `targetRatio` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Used in conjunction with SET_SPECIAL_FLIGHT_MODE_RATIO, in Rockstar's scripts. The vehicle will transform into the given targetRatio, starting from the vehicle's current hover mode transform ratio (which can also be manually set by SET_SPECIAL_FLIGHT_MODE_RATIO,) i.e. setting targetRatio to 0.0 while the vehicle is in hover mode will transform the vehicle into car mode, likewise setting targetRatio to 1.0 while the vehicle is in car mode will transform the vehicle into hover mode, and if the current transform ratio is set to 0.7 while targetRatio is 1.0 the vehicle will transform into hover mode starting from being already partially transformed.

targetRatio is recommended to always be 0.0 or 1.0, otherwise the vehicle will transform into a glitched state.

Usable only with the deluxo and other vehicles with deluxo-like hover mode toggle, modded or otherwise. Does nothing when used on oppressor2.

Example:
Ped playerPed = PLAYER::PLAYER_PED_ID();
Vehicle veh = PED::GET_VEHICLE_PED_IS_USING(playerPed);
VEHICLE::SET_SPECIAL_FLIGHT_MODE_RATIO(veh, 0.7f);
VEHICLE::SET_SPECIAL_FLIGHT_MODE_TARGET_RATIO(veh, 1.0f);

---

### SET_SPECIAL_FLIGHT_MODE_ALLOWED

**Hash:** `0xF1211889DF15A763` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_SPECIAL_FLIGHT_MODE_ALLOWED(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_SPECIAL_FLIGHT_MODE_ALLOWED, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** It will override the ability to transform deluxo. For oppressor it will work just like SET_DISABLE_HOVER_MODE_FLIGHT

---

### SET_DISABLE_HOVER_MODE_FLIGHT

**Hash:** `0x2D55FE374D5FDB91` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_DISABLE_HOVER_MODE_FLIGHT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_HOVER_MODE_FLIGHT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables "wings" for some flying vehicles. Works only for oppressor _2_ and deluxo.
For deluxo it just immediately removes vehicle's "wings" and you will be not able to fly up.
For oppressor 2 it will remove wings right after you land. And you will not able to fly up anymore too.
But for opressor 2 you still can fly if you somehow get back in the air.

---

### GET_OUTRIGGERS_DEPLOYED

**Hash:** `0x3A9128352EAC9E85` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL GET_OUTRIGGERS_DEPLOYED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_OUTRIGGERS_DEPLOYED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if Chernobog's stabilizers are deployed or not.
These are the metal supports that allow it to fire.
This native only applies to the Chernobog.

---

### FIND_SPAWN_COORDINATES_FOR_HELI

**Hash:** `0x8DC9675797123522` | **Build:** `1290`

**C++ 原型:**
```cpp
Vector3 FIND_SPAWN_COORDINATES_FOR_HELI(Ped ped)
```

**C# 调用格式:**
```csharp
Vector3 result = Function.Call<Vector3>(Hash.FIND_SPAWN_COORDINATES_FOR_HELI, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Vector3` → `Vector3`

**说明:** Native is significantly more complicated than simply generating a random vector & length.
The 'point' is either 400.0 or 250.0 units away from the Ped's current coordinates; and paths into functions like rage::grcViewport___IsSphereVisible

---

### SET_DEPLOY_FOLDING_WINGS

**Hash:** `0xB251E0B33E58B424` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_DEPLOY_FOLDING_WINGS(Vehicle vehicle, BOOL deploy, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DEPLOY_FOLDING_WINGS, vehicle, deploy, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `deploy` | `BOOL` | `bool` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only used with the "akula" and "annihilator2" in the decompiled native scripts.

---

### ARE_FOLDING_WINGS_DEPLOYED

**Hash:** `0xAEF12960FA943792` | **Build:** `1290`

**C++ 原型:**
```cpp
BOOL ARE_FOLDING_WINGS_DEPLOYED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.ARE_FOLDING_WINGS_DEPLOYED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Only used with the "akula" and "annihilator2" in the decompiled native scripts.

---

### _SET_DEPLOY_MISSILE_BAYS

**Hash:** `0x0C02468829E4AA65` | **Build:** `2944`

**C++ 原型:**
```cpp
void _SET_DEPLOY_MISSILE_BAYS(Vehicle vehicle, BOOL deploy)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_DEPLOY_MISSILE_BAYS, vehicle, deploy);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `deploy` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _ARE_MISSILE_BAYS_DEPLOYED

**Hash:** `0xEA4743874D515F13` | **Build:** `2944`

**C++ 原型:**
```cpp
BOOL _ARE_MISSILE_BAYS_DEPLOYED(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._ARE_MISSILE_BAYS_DEPLOYED, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_DIP_STRAIGHT_DOWN_WHEN_CRASHING_PLANE

**Hash:** `0xAA653AE61924B0A0` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_DIP_STRAIGHT_DOWN_WHEN_CRASHING_PLANE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DIP_STRAIGHT_DOWN_WHEN_CRASHING_PLANE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_TURRET_HIDDEN

**Hash:** `0xC60060EB0D8AC7B1` | **Build:** `1290`

**C++ 原型:**
```cpp
void SET_TURRET_HIDDEN(Vehicle vehicle, int index, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TURRET_HIDDEN, vehicle, index, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Toggles specific flag on turret

---

### SET_HOVER_MODE_WING_RATIO

**Hash:** `0x70A252F60A3E036B` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_HOVER_MODE_WING_RATIO(Vehicle vehicle, float ratio)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_HOVER_MODE_WING_RATIO, vehicle, ratio);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `ratio` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_TURRET_MOVEMENT

**Hash:** `0xE615BB7A7752C76A` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_DISABLE_TURRET_MOVEMENT(Vehicle vehicle, int turretId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_TURRET_MOVEMENT, vehicle, turretId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `turretId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Disables turret movement when called in a loop. You can still fire and aim. You cannot shoot backwards though.

---

### SET_FORCE_FIX_LINK_MATRICES

**Hash:** `0x887FA38787DE8C72` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_FORCE_FIX_LINK_MATRICES(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FORCE_FIX_LINK_MATRICES, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_TRANSFORM_RATE_FOR_ANIMATION

**Hash:** `0x498218259FB7C72D` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_TRANSFORM_RATE_FOR_ANIMATION(Vehicle vehicle, float transformRate)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRANSFORM_RATE_FOR_ANIMATION, vehicle, transformRate);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `transformRate` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Affects the playback speed of the submarine car conversion animations. Does not affect hardcoded animations such as the wheels being retracted.

---

### SET_TRANSFORM_TO_SUBMARINE_USES_ALTERNATE_INPUT

**Hash:** `0x41B9FB92EDED32A6` | **Build:** `1365`

**C++ 原型:**
```cpp
void SET_TRANSFORM_TO_SUBMARINE_USES_ALTERNATE_INPUT(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TRANSFORM_TO_SUBMARINE_USES_ALTERNATE_INPUT, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** When set to true, the key to transform a car to submarine mode changes from raise/lower convertible roof (hold H by default) to special vehicle transform (press X by default.)

---

### SET_VEHICLE_COMBAT_MODE

**Hash:** `0x36DE109527A2C0C4` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_COMBAT_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_COMBAT_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing. It's a nullsub.

---

### SET_VEHICLE_DETONATION_MODE

**Hash:** `0x82E0AC411E41A5B4` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_DETONATION_MODE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_DETONATION_MODE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing. It's a nullsub.

---

### SET_VEHICLE_SHUNT_ON_STICK

**Hash:** `0x99A05839C46CE316` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_VEHICLE_SHUNT_ON_STICK(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_VEHICLE_SHUNT_ON_STICK, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Does nothing. It's a nullsub.

---

### _IS_VEHICLE_ON_BOOST_PAD

**Hash:** `0x71C6302CBCA6CA35` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _IS_VEHICLE_ON_BOOST_PAD(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._IS_VEHICLE_ON_BOOST_PAD, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_IS_VEHICLE_SHUNTING

**Hash:** `0xA2459F72C14E2E8D` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_IS_VEHICLE_SHUNTING(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_VEHICLE_SHUNTING, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_HAS_VEHICLE_BEEN_HIT_BY_SHUNT

**Hash:** `0xE8718FAF591FD224` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_HAS_VEHICLE_BEEN_HIT_BY_SHUNT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_HAS_VEHICLE_BEEN_HIT_BY_SHUNT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_LAST_SHUNT_VEHICLE

**Hash:** `0x04F2FA6E234162F7` | **Build:** `1604`

**C++ 原型:**
```cpp
Vehicle GET_LAST_SHUNT_VEHICLE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_LAST_SHUNT_VEHICLE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `Vehicle` → `int`

**说明:** Returns last vehicle that was rammed by the given vehicle using the shunt boost.

---

### SET_DISABLE_VEHICLE_EXPLOSIONS_DAMAGE

**Hash:** `0x143921E45EC44D62` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_DISABLE_VEHICLE_EXPLOSIONS_DAMAGE(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_VEHICLE_EXPLOSIONS_DAMAGE, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_OVERRIDE_NITROUS_LEVEL

**Hash:** `0xC8E9B6B71B8E660D` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_OVERRIDE_NITROUS_LEVEL(Vehicle vehicle, BOOL toggle, float level, float power, float rechargeTime, BOOL disableSound)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_OVERRIDE_NITROUS_LEVEL, vehicle, toggle, level, power, rechargeTime, disableSound);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `level` | `float` | `float` | |
| `power` | `float` | `float` | |
| `rechargeTime` | `float` | `float` | |
| `disableSound` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_NITROUS_IS_VISIBLE

**Hash:** `0x465EEA70AF251045` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_NITROUS_IS_VISIBLE(Vehicle vehicle, BOOL enabled)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_NITROUS_IS_VISIBLE, vehicle, enabled);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `enabled` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### _SET_OVERRIDE_TRACTION_LOSS_MULTIPLIER

**Hash:** `0xAFD262ACCA64479A` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_OVERRIDE_TRACTION_LOSS_MULTIPLIER(Vehicle vehicle, float modifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_OVERRIDE_TRACTION_LOSS_MULTIPLIER, vehicle, modifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `modifier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### _SET_DRIFT_SLIP_ANGLE_LIMITS

**Hash:** `0xDAF4C98C18AC6F06` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_DRIFT_SLIP_ANGLE_LIMITS(Vehicle vehicle, float durationScalar, float amplitudeScalar, float slipAngleLimit)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_DRIFT_SLIP_ANGLE_LIMITS, vehicle, durationScalar, amplitudeScalar, slipAngleLimit);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `durationScalar` | `float` | `float` | |
| `amplitudeScalar` | `float` | `float` | |
| `slipAngleLimit` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** First two floats relate to rumble, the last is a threshold

---

### _SET_MINIMUM_TIME_BETWEEN_GEAR_SHIFTS

**Hash:** `0x16CFBC5E7EB32861` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_MINIMUM_TIME_BETWEEN_GEAR_SHIFTS(Vehicle vehicle, int time)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_MINIMUM_TIME_BETWEEN_GEAR_SHIFTS, vehicle, time);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `time` | `int` | `int` | |

**返回值:** `void` → `void`

---

### FULLY_CHARGE_NITROUS

**Hash:** `0x1A2BCC8C636F9226` | **Build:** `3095`

**C++ 原型:**
```cpp
void FULLY_CHARGE_NITROUS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FULLY_CHARGE_NITROUS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### _SET_REMAINING_NITROUS_DURATION

**Hash:** `0x1AD0F63A94E10EFF` | **Build:** `3407`

**C++ 原型:**
```cpp
void _SET_REMAINING_NITROUS_DURATION(Vehicle vehicle, float duration)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_REMAINING_NITROUS_DURATION, vehicle, duration);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `duration` | `float` | `float` | |

**返回值:** `void` → `void`

---

### _GET_REMAINING_NITROUS_DURATION

**Hash:** `0xBEC4B8653462450E` | **Build:** `3095`

**C++ 原型:**
```cpp
float _GET_REMAINING_NITROUS_DURATION(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_REMAINING_NITROUS_DURATION, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `float` → `float`

---

### IS_NITROUS_ACTIVE

**Hash:** `0x491E822B2C464FE4` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL IS_NITROUS_ACTIVE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_NITROUS_ACTIVE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### CLEAR_NITROUS

**Hash:** `0xC889AE921400E1ED` | **Build:** `3095`

**C++ 原型:**
```cpp
void CLEAR_NITROUS(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_NITROUS, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### SET_NITROUS_IS_ACTIVE

**Hash:** `0x9E566EA551F4F1A6` | **Build:** `3407`

**C++ 原型:**
```cpp
void SET_NITROUS_IS_ACTIVE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_NITROUS_IS_ACTIVE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_INCREASE_WHEEL_CRUSH_DAMAGE

**Hash:** `0x2970EAA18FD5E42F` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_INCREASE_WHEEL_CRUSH_DAMAGE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_INCREASE_WHEEL_CRUSH_DAMAGE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_DISABLE_WEAPON_BLADE_FORCES

**Hash:** `0x211E95CE9903940C` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_DISABLE_WEAPON_BLADE_FORCES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_WEAPON_BLADE_FORCES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Sets some global vehicle related bool

---

### SET_USE_DOUBLE_CLICK_FOR_CAR_JUMP

**Hash:** `0x5BBCF35BF6E456F7` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_USE_DOUBLE_CLICK_FOR_CAR_JUMP(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_USE_DOUBLE_CLICK_FOR_CAR_JUMP, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Changes the car jump control to require a double-tap to activate.

---

### GET_DOES_VEHICLE_HAVE_TOMBSTONE

**Hash:** `0x71AFB258CCED3A27` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_DOES_VEHICLE_HAVE_TOMBSTONE(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DOES_VEHICLE_HAVE_TOMBSTONE, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns true only if the "tombstone" bone is attached to the vehicle, irrespective of "FLAG_HAS_TOMBSTONE" being present or not. Detaching the tombstone will return false.

---

### HIDE_TOMBSTONE

**Hash:** `0xAE71FB656C600587` | **Build:** `1604`

**C++ 原型:**
```cpp
void HIDE_TOMBSTONE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_TOMBSTONE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables detachable bumber from domnator4, dominator5, dominator6, see https://gfycat.com/SecondUnluckyGosling

---

### APPLY_EMP_EFFECT

**Hash:** `0x249249D74F813EB2` | **Build:** `3095`

**C++ 原型:**
```cpp
void APPLY_EMP_EFFECT(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.APPLY_EMP_EFFECT, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `void` → `void`

---

### GET_IS_VEHICLE_DISABLED_BY_EMP

**Hash:** `0x0506ED94363AD905` | **Build:** `1604`

**C++ 原型:**
```cpp
BOOL GET_IS_VEHICLE_DISABLED_BY_EMP(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_VEHICLE_DISABLED_BY_EMP, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns whether this vehicle is currently disabled by an EMP mine.

---

### SET_DISABLE_RETRACTING_WEAPON_BLADES

**Hash:** `0x8F0D5BA1C2CC91D7` | **Build:** `1604`

**C++ 原型:**
```cpp
void SET_DISABLE_RETRACTING_WEAPON_BLADES(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DISABLE_RETRACTING_WEAPON_BLADES, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_TYRE_HEALTH

**Hash:** `0x55EAB010FAEE9380` | **Build:** `1868`

**C++ 原型:**
```cpp
float GET_TYRE_HEALTH(Vehicle vehicle, int wheelIndex)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TYRE_HEALTH, vehicle, wheelIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### SET_TYRE_HEALTH

**Hash:** `0x74C68EF97645E79D` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_TYRE_HEALTH(Vehicle vehicle, int wheelIndex, float health)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TYRE_HEALTH, vehicle, wheelIndex, health);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |
| `health` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** SET_TYRE_WEAR_RATE must be active, otherwise values set to <1000.0f will default to 350.0f

Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### GET_TYRE_WEAR_RATE

**Hash:** `0x6E387895952F4F71` | **Build:** `2060`

**C++ 原型:**
```cpp
float GET_TYRE_WEAR_RATE(Vehicle vehicle, int wheelIndex)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_TYRE_WEAR_RATE, vehicle, wheelIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |

**返回值:** `float` → `float`

**说明:** Returns the multiplier value from SET_TYRE_WEAR_RATE

Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### SET_TYRE_WEAR_RATE

**Hash:** `0x01894E2EDE923CA2` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_TYRE_WEAR_RATE(Vehicle vehicle, int wheelIndex, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TYRE_WEAR_RATE, vehicle, wheelIndex, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Needs to be run for tire wear to work. Multiplier affects the downforce and how fast the tires will wear out, higher values essentially make the vehicle slower on straights and its tires will wear down quicker when cornering. Value must be >0f.
Default value in Rockstar's Open Wheel Race JSON's ("owrtws", "owrtwm", "owrtwh") is 1.0

Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### SET_TYRE_WEAR_RATE_SCALE

**Hash:** `0x392183BB9EA57697` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_TYRE_WEAR_RATE_SCALE(Vehicle vehicle, int wheelIndex, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TYRE_WEAR_RATE_SCALE, vehicle, wheelIndex, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Controls how fast the tires wear out.

Default values from Rockstar's Open Wheel Race JSON's:
"owrtss" (Soft): 2.2
"owrtsm" (Medium): 1.7
"owrtsh" (Hard): 1.2

Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### SET_TYRE_MAXIMUM_GRIP_DIFFERENCE_DUE_TO_WEAR_RATE

**Hash:** `0xC970D0E0FC31D768` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_TYRE_MAXIMUM_GRIP_DIFFERENCE_DUE_TO_WEAR_RATE(Vehicle vehicle, int wheelIndex, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_TYRE_MAXIMUM_GRIP_DIFFERENCE_DUE_TO_WEAR_RATE, vehicle, wheelIndex, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `wheelIndex` | `int` | `int` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Controls how much traction the wheel loses.

Default values from Rockstar's Open Wheel Race JSON's:
"owrtds" (Soft): 0.05
"owrtdm" (Medium): 0.45
"owrtdh" (Hard): 0.8

Usable wheels:
0: wheel_lf
1: wheel_rf
2: wheel_lm1
3: wheel_rm1
4: wheel_lr
5: wheel_rr

---

### SET_AIRCRAFT_IGNORE_HIGHTMAP_OPTIMISATION

**Hash:** `0xF8B49F5BA7F850E7` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_AIRCRAFT_IGNORE_HIGHTMAP_OPTIMISATION(Vehicle vehicle, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_AIRCRAFT_IGNORE_HIGHTMAP_OPTIMISATION, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_REDUCED_SUSPENSION_FORCE

**Hash:** `0x3A375167F5782A65` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_REDUCED_SUSPENSION_FORCE(Vehicle vehicle, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_REDUCED_SUSPENSION_FORCE, vehicle, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Lowers the vehicle's stance. Only works for vehicles that have strAdvancedFlags 0x8000 and 0x4000000 set.

---

### SET_DRIFT_TYRES

**Hash:** `0x5AC79C98C5C17F05` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_DRIFT_TYRES(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DRIFT_TYRES, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_DRIFT_TYRES_SET

**Hash:** `0x2F5A72430E78C8D3` | **Build:** `2372`

**C++ 原型:**
```cpp
BOOL GET_DRIFT_TYRES_SET(Vehicle vehicle)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_DRIFT_TYRES_SET, vehicle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |

**返回值:** `BOOL` → `bool`

---

### NETWORK_USE_HIGH_PRECISION_TRAIN_BLENDING

**Hash:** `0xEC0C1D4922AF9754` | **Build:** `2372`

**C++ 原型:**
```cpp
void NETWORK_USE_HIGH_PRECISION_TRAIN_BLENDING(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.NETWORK_USE_HIGH_PRECISION_TRAIN_BLENDING, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Implemented only for trains.

---

### SET_CHECK_FOR_ENOUGH_ROOM_FOR_PED

**Hash:** `0xEF9D388F8D377F44` | **Build:** `2699`

**C++ 原型:**
```cpp
void SET_CHECK_FOR_ENOUGH_ROOM_FOR_PED(Vehicle vehicle, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CHECK_FOR_ENOUGH_ROOM_FOR_PED, vehicle, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Only used in R* Script fm_content_cargo

---

### _SET_ALLOW_COLLISION_WHEN_IN_VEHICLE

**Hash:** `0x27D27223E8EF22ED` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_ALLOW_COLLISION_WHEN_IN_VEHICLE(Vehicle vehicle, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_ALLOW_COLLISION_WHEN_IN_VEHICLE, vehicle, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** _SET_ALLOW_R* - _SET_ALLOW_V*

---

### _IS_VEHICLE_GEN9_EXCLUSIVE_MODEL

**Hash:** `0x6638C0F19DE692FE` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _IS_VEHICLE_GEN9_EXCLUSIVE_MODEL(Hash vehicleModel)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._IS_VEHICLE_GEN9_EXCLUSIVE_MODEL, vehicleModel);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicleModel` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### _GET_VEHICLE_MAX_EXHAUST_BONE_COUNT

**Hash:** `0x3EE18B00CD86C54F` | **Build:** `3095`

**C++ 原型:**
```cpp
int _GET_VEHICLE_MAX_EXHAUST_BONE_COUNT(void)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_VEHICLE_MAX_EXHAUST_BONE_COUNT);
```

**返回值:** `int` → `int`

---

### _GET_VEHICLE_EXHAUST_BONE

**Hash:** `0xE728F090D538CB18` | **Build:** `3095`

**C++ 原型:**
```cpp
BOOL _GET_VEHICLE_EXHAUST_BONE(Vehicle vehicle, int index, int* boneIndex, BOOL* axisX)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._GET_VEHICLE_EXHAUST_BONE, vehicle, index, boneIndex, axisX);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `index` | `int` | `int` | |
| `boneIndex` | `int*` | `int*` | |
| `axisX` | `BOOL*` | `bool*` | |

**返回值:** `BOOL` → `bool`

---


---

## WATER

**包含 12 个 Native 函数**

### GET_WATER_HEIGHT

**Hash:** `0xF6829842C06AE524` | **JHash:** `0xD864E17C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_WATER_HEIGHT(float x, float y, float z, float* height)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_WATER_HEIGHT, x, y, z, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `height` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

**说明:** This function set height to the value of z-axis of the water surface.

This function works with sea and lake. However it does not work with shallow rivers (e.g. raton canyon will return -100000.0f)

note: seems to return true when you are in water

---

### GET_WATER_HEIGHT_NO_WAVES

**Hash:** `0x8EE6B53CE13A9794` | **JHash:** `0x262017F8` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_WATER_HEIGHT_NO_WAVES(float x, float y, float z, float* height)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_WATER_HEIGHT_NO_WAVES, x, y, z, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `height` | `float*` | `float*` | |

**返回值:** `BOOL` → `bool`

---

### TEST_PROBE_AGAINST_WATER

**Hash:** `0xFFA5D878809819DB` | **JHash:** `0xAA4AE00C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL TEST_PROBE_AGAINST_WATER(float x1, float y1, float z1, float x2, float y2, float z2, Vector3* result)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.TEST_PROBE_AGAINST_WATER, x1, y1, z1, x2, y2, z2, result);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `result` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

---

### TEST_PROBE_AGAINST_ALL_WATER

**Hash:** `0x8974647ED222EA5F` | **JHash:** `0x4A962D55` | **Build:** `323`

**C++ 原型:**
```cpp
int TEST_PROBE_AGAINST_ALL_WATER(float x1, float y1, float z1, float x2, float y2, float z2, int flags, float* waterHeight)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TEST_PROBE_AGAINST_ALL_WATER, x1, y1, z1, x2, y2, z2, flags, waterHeight);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x1` | `float` | `float` | |
| `y1` | `float` | `float` | |
| `z1` | `float` | `float` | |
| `x2` | `float` | `float` | |
| `y2` | `float` | `float` | |
| `z2` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `waterHeight` | `float*` | `float*` | |

**返回值:** `int` → `int`

**说明:** enum eScriptWaterTestResult
{
	SCRIPT_WATER_TEST_RESULT_NONE,
	SCRIPT_WATER_TEST_RESULT_WATER,
	SCRIPT_WATER_TEST_RESULT_BLOCKED,
};

---

### TEST_VERTICAL_PROBE_AGAINST_ALL_WATER

**Hash:** `0x2B3451FA1E3142E2` | **JHash:** `0x4C71D143` | **Build:** `323`

**C++ 原型:**
```cpp
int TEST_VERTICAL_PROBE_AGAINST_ALL_WATER(float x, float y, float z, int flags, float* waterHeight)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.TEST_VERTICAL_PROBE_AGAINST_ALL_WATER, x, y, z, flags, waterHeight);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `flags` | `int` | `int` | |
| `waterHeight` | `float*` | `float*` | |

**返回值:** `int` → `int`

**说明:** See TEST_PROBE_AGAINST_ALL_WATER.

---

### MODIFY_WATER

**Hash:** `0xC443FD757C3BA637` | **JHash:** `0xC49E005A` | **Build:** `323`

**C++ 原型:**
```cpp
void MODIFY_WATER(float x, float y, float radius, float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.MODIFY_WATER, x, y, radius, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets the water height for a given position and radius.


---

### ADD_EXTRA_CALMING_QUAD

**Hash:** `0xFDBF4CDBC07E1706` | **JHash:** `0xA9419B6D` | **Build:** `323`

**C++ 原型:**
```cpp
int ADD_EXTRA_CALMING_QUAD(float xLow, float yLow, float xHigh, float yHigh, float height)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.ADD_EXTRA_CALMING_QUAD, xLow, yLow, xHigh, yHigh, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `xLow` | `float` | `float` | |
| `yLow` | `float` | `float` | |
| `xHigh` | `float` | `float` | |
| `yHigh` | `float` | `float` | |
| `height` | `float` | `float` | |

**返回值:** `int` → `int`

---

### REMOVE_EXTRA_CALMING_QUAD

**Hash:** `0xB1252E3E59A82AAF` | **JHash:** `0x45268B6F` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_EXTRA_CALMING_QUAD(int calmingQuad)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_EXTRA_CALMING_QUAD, calmingQuad);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `calmingQuad` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** p0 is the handle returned from ADD_EXTRA_CALMING_QUAD

---

### SET_DEEP_OCEAN_SCALER

**Hash:** `0xB96B00E976BE977F` | **JHash:** `0x53B694B1` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_DEEP_OCEAN_SCALER(float intensity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_DEEP_OCEAN_SCALER, intensity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `intensity` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Sets a value that determines how aggressive the ocean waves will be. Values of 2.0 or more make for very aggressive waves like you see during a thunderstorm.

Works only ~200 meters around the player.

---

### GET_DEEP_OCEAN_SCALER

**Hash:** `0x2B2A2CC86778B619` | **JHash:** `0xBD0A67FB` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_DEEP_OCEAN_SCALER(void)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_DEEP_OCEAN_SCALER);
```

**返回值:** `float` → `float`

**说明:** Gets the aggressiveness factor of the ocean waves.

---

### SET_CALMED_WAVE_HEIGHT_SCALER

**Hash:** `0x547237AA71AB44DE` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_CALMED_WAVE_HEIGHT_SCALER(float height)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CALMED_WAVE_HEIGHT_SCALER, height);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `height` | `float` | `float` | |

**返回值:** `void` → `void`

---

### RESET_DEEP_OCEAN_SCALER

**Hash:** `0x5E5E99285AE812DB` | **JHash:** `0x4AD23212` | **Build:** `323`

**C++ 原型:**
```cpp
void RESET_DEEP_OCEAN_SCALER(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.RESET_DEEP_OCEAN_SCALER);
```

**返回值:** `void` → `void`

**说明:** Sets the waves intensity back to original (1.0 in most cases).

---


---

## WEAPON

**包含 123 个 Native 函数**

### ENABLE_LASER_SIGHT_RENDERING

**Hash:** `0xC8B46D7727D864AA` | **JHash:** `0xE3438955` | **Build:** `323`

**C++ 原型:**
```cpp
void ENABLE_LASER_SIGHT_RENDERING(BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ENABLE_LASER_SIGHT_RENDERING, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables laser sight on any weapon.

It doesn't work. Neither on tick nor OnKeyDown

---

### GET_WEAPON_COMPONENT_TYPE_MODEL

**Hash:** `0x0DB57B41EC1DB083` | **JHash:** `0x324FA47A` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_WEAPON_COMPONENT_TYPE_MODEL(Hash componentHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPON_COMPONENT_TYPE_MODEL, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

---

### GET_WEAPONTYPE_MODEL

**Hash:** `0xF46CDC33180FDA94` | **JHash:** `0x44E1C269` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_WEAPONTYPE_MODEL(Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPONTYPE_MODEL, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the model of any weapon.

Can also take an ammo hash?
sub_6663a(&l_115B, WEAPON::GET_WEAPONTYPE_MODEL(${ammo_rpg}));

---

### GET_WEAPONTYPE_SLOT

**Hash:** `0x4215460B9B8B7FA0` | **JHash:** `0x2E3759AF` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_WEAPONTYPE_SLOT(Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPONTYPE_SLOT, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

---

### GET_WEAPONTYPE_GROUP

**Hash:** `0xC3287EE3050FB74C` | **JHash:** `0x5F2DE833` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_WEAPONTYPE_GROUP(Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPONTYPE_GROUP, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

---

### GET_WEAPON_COMPONENT_VARIANT_EXTRA_COUNT

**Hash:** `0x6558AC7C17BFEF58` | **Build:** `372`

**C++ 原型:**
```cpp
int GET_WEAPON_COMPONENT_VARIANT_EXTRA_COUNT(Hash componentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_COMPONENT_VARIANT_EXTRA_COUNT, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns the amount of extra components the specified component has.
Returns -1 if the component isn't of type CWeaponComponentVariantModel.

---

### GET_WEAPON_COMPONENT_VARIANT_EXTRA_MODEL

**Hash:** `0x4D1CB8DC40208A17` | **Build:** `372`

**C++ 原型:**
```cpp
Hash GET_WEAPON_COMPONENT_VARIANT_EXTRA_MODEL(Hash componentHash, int extraComponentIndex)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_WEAPON_COMPONENT_VARIANT_EXTRA_MODEL, componentHash, extraComponentIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `extraComponentIndex` | `int` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the model hash of the extra component at specified index.

---

### SET_CURRENT_PED_WEAPON

**Hash:** `0xADF692B254977C0C` | **JHash:** `0xB8278882` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_CURRENT_PED_WEAPON(Ped ped, Hash weaponHash, BOOL bForceInHand)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CURRENT_PED_WEAPON, ped, weaponHash, bForceInHand);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `bForceInHand` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_CURRENT_PED_WEAPON

**Hash:** `0x3A87E44BB9A01D54` | **JHash:** `0xB0237302` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CURRENT_PED_WEAPON(Ped ped, Hash* weaponHash, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CURRENT_PED_WEAPON, ped, weaponHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** The return value seems to indicate returns true if the hash of the weapon object weapon equals the weapon hash.
p2 seems to be 1 most of the time.





p2 is not implemented

disassembly said that?


---

### GET_CURRENT_PED_WEAPON_ENTITY_INDEX

**Hash:** `0x3B390A939AF0B5FC` | **JHash:** `0x5D73CD20` | **Build:** `323`

**C++ 原型:**
```cpp
Entity GET_CURRENT_PED_WEAPON_ENTITY_INDEX(Ped ped, Any p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_CURRENT_PED_WEAPON_ENTITY_INDEX, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `Any` | `object` | |

**返回值:** `Entity` → `int`

---

### GET_BEST_PED_WEAPON

**Hash:** `0x8483E98E8B888AE2` | **JHash:** `0xB998D444` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_BEST_PED_WEAPON(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_BEST_PED_WEAPON, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Hash` → `uint`

**说明:** p1 is always 0 in the scripts.

---

### SET_CURRENT_PED_VEHICLE_WEAPON

**Hash:** `0x75C55983C2C39DAA` | **JHash:** `0x8E6F2AF1` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_CURRENT_PED_VEHICLE_WEAPON(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_CURRENT_PED_VEHICLE_WEAPON, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree (Search for VEHICLE_*): https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_CURRENT_PED_VEHICLE_WEAPON

**Hash:** `0x1017582BCD3832DC` | **JHash:** `0xF26C5D65` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_CURRENT_PED_VEHICLE_WEAPON(Ped ped, Hash* weaponHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_CURRENT_PED_VEHICLE_WEAPON, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash*` | `uint*` | |

**返回值:** `BOOL` → `bool`

**说明:** Example in VB

    Public Shared Function GetVehicleCurrentWeapon(Ped As Ped) As Integer
        Dim arg As New OutputArgument()
        Native.Function.Call(Hash.GET_CURRENT_PED_VEHICLE_WEAPON, Ped, arg)
        Return arg.GetResult(Of Integer)()
    End Function

Usage:
If GetVehicleCurrentWeapon(Game.Player.Character) = -821520672 Then ...Do something
Note: -821520672 = VEHICLE_WEAPON_PLANE_ROCKET

---

### SET_PED_CYCLE_VEHICLE_WEAPONS_ONLY

**Hash:** `0x50276EF8172F5F12` | **Build:** `1734`

**C++ 原型:**
```cpp
void SET_PED_CYCLE_VEHICLE_WEAPONS_ONLY(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CYCLE_VEHICLE_WEAPONS_ONLY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### IS_PED_ARMED

**Hash:** `0x475768A975D5AD17` | **JHash:** `0x0BFC892C` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_ARMED(Ped ped, int typeFlags)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_ARMED, ped, typeFlags);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `typeFlags` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Checks if the ped is currently equipped with a weapon matching a bit specified using a bitwise-or in typeFlags.

Type flag bit values:
1 = Melee weapons
2 = Explosive weapons
4 = Any other weapons

Not specifying any bit will lead to the native *always* returning 'false', and for example specifying '4 | 2' will check for any weapon except fists and melee weapons.
7 returns true if you are equipped with any weapon except your fists.
6 returns true if you are equipped with any weapon except melee weapons.
5 returns true if you are equipped with any weapon except the Explosives weapon group.
4 returns true if you are equipped with any weapon except Explosives weapon group AND melee weapons.
3 returns true if you are equipped with either Explosives or Melee weapons (the exact opposite of 4).
2 returns true only if you are equipped with any weapon from the Explosives weapon group.
1 returns true only if you are equipped with any Melee weapon.
0 never returns true.

Note: When I say "Explosives weapon group", it does not include the Jerry can and Fire Extinguisher.

---

### IS_WEAPON_VALID

**Hash:** `0x937C71165CF334B3` | **JHash:** `0x38CA2954` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_WEAPON_VALID(Hash weaponHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_WEAPON_VALID, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### HAS_PED_GOT_WEAPON

**Hash:** `0x8DECB02F88F428BC` | **JHash:** `0x43D2FA82` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_GOT_WEAPON(Ped ped, Hash weaponHash, BOOL p2)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_GOT_WEAPON, ped, weaponHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `BOOL` → `bool`

**说明:** p2 should be FALSE, otherwise it seems to always return FALSE

Bool does not check if the weapon is current equipped, unfortunately.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### IS_PED_WEAPON_READY_TO_SHOOT

**Hash:** `0xB80CA294F2F26749` | **JHash:** `0x02A32CB0` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_WEAPON_READY_TO_SHOOT(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_WEAPON_READY_TO_SHOOT, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### GET_PED_WEAPONTYPE_IN_SLOT

**Hash:** `0xEFFED78E9011134D` | **JHash:** `0x9BC64E16` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PED_WEAPONTYPE_IN_SLOT(Ped ped, Hash weaponSlot)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_WEAPONTYPE_IN_SLOT, ped, weaponSlot);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponSlot` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

---

### GET_AMMO_IN_PED_WEAPON

**Hash:** `0x015A522136D7F951` | **JHash:** `0x0C755733` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_AMMO_IN_PED_WEAPON(Ped ped, Hash weaponhash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_AMMO_IN_PED_WEAPON, ped, weaponhash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponhash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** WEAPON::GET_AMMO_IN_PED_WEAPON(PLAYER::PLAYER_PED_ID(), a_0)

From decompiled scripts
Returns total ammo in weapon

GTALua Example :
natives.WEAPON.GET_AMMO_IN_PED_WEAPON(plyPed, WeaponHash)
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### ADD_AMMO_TO_PED

**Hash:** `0x78F0424C34306220` | **JHash:** `0x7F0580C7` | **Build:** `323`

**C++ 原型:**
```cpp
void ADD_AMMO_TO_PED(Ped ped, Hash weaponHash, int ammo)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_AMMO_TO_PED, ped, weaponHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammo` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_PED_AMMO

**Hash:** `0x14E56BC5B5DB6A19` | **JHash:** `0xBF90DF1A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AMMO(Ped ped, Hash weaponHash, int ammo, BOOL p3)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AMMO, ped, weaponHash, ammo, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammo` | `int` | `int` | |
| `p3` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_PED_INFINITE_AMMO

**Hash:** `0x3EDCB0505123623B` | **JHash:** `0x9CB8D278` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INFINITE_AMMO(Ped ped, BOOL toggle, Hash weaponHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INFINITE_AMMO, ped, toggle, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_PED_INFINITE_AMMO_CLIP

**Hash:** `0x183DADC6AA953186` | **JHash:** `0x5A5E3B67` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_INFINITE_AMMO_CLIP(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_INFINITE_AMMO_CLIP, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### SET_PED_STUN_GUN_FINITE_AMMO

**Hash:** `0x24C024BA8379A70A` | **Build:** `1868`

**C++ 原型:**
```cpp
void SET_PED_STUN_GUN_FINITE_AMMO(Any p0, Any p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_STUN_GUN_FINITE_AMMO, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Any` | `object` | |
| `p1` | `Any` | `object` | |

**返回值:** `void` → `void`

---

### GIVE_WEAPON_TO_PED

**Hash:** `0xBF0FD6E56C964FCB` | **JHash:** `0xC4D88A85` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_WEAPON_TO_PED(Ped ped, Hash weaponHash, int ammoCount, BOOL isHidden, BOOL bForceInHand)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_WEAPON_TO_PED, ped, weaponHash, ammoCount, isHidden, bForceInHand);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammoCount` | `int` | `int` | |
| `isHidden` | `BOOL` | `bool` | |
| `bForceInHand` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GIVE_DELAYED_WEAPON_TO_PED

**Hash:** `0xB282DC6EBD803C75` | **JHash:** `0x5868D20D` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_DELAYED_WEAPON_TO_PED(Ped ped, Hash weaponHash, int ammoCount, BOOL bForceInHand)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_DELAYED_WEAPON_TO_PED, ped, weaponHash, ammoCount, bForceInHand);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammoCount` | `int` | `int` | |
| `bForceInHand` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Gives a weapon to PED with a delay, example:

WEAPON::GIVE_DELAYED_WEAPON_TO_PED(PED::PLAYER_PED_ID(), MISC::GET_HASH_KEY("WEAPON_PISTOL"), 1000, false)
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### REMOVE_ALL_PED_WEAPONS

**Hash:** `0xF25DF915FA38C5F3` | **JHash:** `0xA44CE817` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_PED_WEAPONS(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_PED_WEAPONS, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** setting the last params to false it does that same so I would suggest its not a toggle

---

### REMOVE_WEAPON_FROM_PED

**Hash:** `0x4899CB088EDF59B8` | **JHash:** `0x9C37F220` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WEAPON_FROM_PED(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WEAPON_FROM_PED, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** This native removes a specified weapon from your selected ped.

Example:
C#:
Function.Call(Hash.REMOVE_WEAPON_FROM_PED, Game.Player.Character, 0x99B507EA);

C++:
WEAPON::REMOVE_WEAPON_FROM_PED(PLAYER::PLAYER_PED_ID(), 0x99B507EA);

The code above removes the knife from the player.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### HIDE_PED_WEAPON_FOR_SCRIPTED_CUTSCENE

**Hash:** `0x6F6981D2253C208F` | **JHash:** `0x00CFD6E9` | **Build:** `323`

**C++ 原型:**
```cpp
void HIDE_PED_WEAPON_FOR_SCRIPTED_CUTSCENE(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.HIDE_PED_WEAPON_FOR_SCRIPTED_CUTSCENE, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Hides the players weapon during a cutscene.

---

### SET_PED_CURRENT_WEAPON_VISIBLE

**Hash:** `0x0725A4CCFDED9A70` | **JHash:** `0x00BECD77` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CURRENT_WEAPON_VISIBLE(Ped ped, BOOL visible, BOOL deselectWeapon, BOOL p3, BOOL p4)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CURRENT_WEAPON_VISIBLE, ped, visible, deselectWeapon, p3, p4);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `visible` | `BOOL` | `bool` | |
| `deselectWeapon` | `BOOL` | `bool` | |
| `p3` | `BOOL` | `bool` | |
| `p4` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Has 5 parameters since latest patches.

---

### SET_PED_DROPS_WEAPONS_WHEN_DEAD

**Hash:** `0x476AE72C1D19D1A8` | **JHash:** `0x8A444056` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DROPS_WEAPONS_WHEN_DEAD(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DROPS_WEAPONS_WHEN_DEAD, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### HAS_PED_BEEN_DAMAGED_BY_WEAPON

**Hash:** `0x2D343D2219CD027A` | **JHash:** `0xCDFBBCC6` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_BEEN_DAMAGED_BY_WEAPON(Ped ped, Hash weaponHash, int weaponType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_BEEN_DAMAGED_BY_WEAPON, ped, weaponHash, weaponType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `weaponType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** It determines what weapons caused damage:

If you want to define only a specific weapon, second parameter=weapon hash code, third parameter=0
If you want to define any melee weapon, second parameter=0, third parameter=1.
If you want to identify any weapon (firearms, melee, rockets, etc.), second parameter=0, third parameter=2.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### CLEAR_PED_LAST_WEAPON_DAMAGE

**Hash:** `0x0E98F88A24C5F4B8` | **JHash:** `0x52C68832` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_PED_LAST_WEAPON_DAMAGE(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_PED_LAST_WEAPON_DAMAGE, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

**说明:** Does NOT seem to work with HAS_PED_BEEN_DAMAGED_BY_WEAPON. Use CLEAR_ENTITY_LAST_WEAPON_DAMAGE and HAS_ENTITY_BEEN_DAMAGED_BY_WEAPON instead.

---

### HAS_ENTITY_BEEN_DAMAGED_BY_WEAPON

**Hash:** `0x131D401334815E94` | **JHash:** `0x6DAABB39` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_ENTITY_BEEN_DAMAGED_BY_WEAPON(Entity entity, Hash weaponHash, int weaponType)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_ENTITY_BEEN_DAMAGED_BY_WEAPON, entity, weaponHash, weaponType);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `weaponType` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** It determines what weapons caused damage:

If you want to define only a specific weapon, second parameter=weapon hash code, third parameter=0
If you want to define any melee weapon, second parameter=0, third parameter=1.
If you want to identify any weapon (firearms, melee, rockets, etc.), second parameter=0, third parameter=2.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### CLEAR_ENTITY_LAST_WEAPON_DAMAGE

**Hash:** `0xAC678E40BE7C74D2` | **JHash:** `0xCEC2732B` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_ENTITY_LAST_WEAPON_DAMAGE(Entity entity)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_ENTITY_LAST_WEAPON_DAMAGE, entity);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `entity` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_DROPS_WEAPON

**Hash:** `0x6B7513D9966FBEC0` | **JHash:** `0x3D3329FA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DROPS_WEAPON(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DROPS_WEAPON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### SET_PED_DROPS_INVENTORY_WEAPON

**Hash:** `0x208A1888007FC0E6` | **JHash:** `0x81FFB874` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_DROPS_INVENTORY_WEAPON(Ped ped, Hash weaponHash, float xOffset, float yOffset, float zOffset, int ammoCount)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_DROPS_INVENTORY_WEAPON, ped, weaponHash, xOffset, yOffset, zOffset, ammoCount);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `xOffset` | `float` | `float` | |
| `yOffset` | `float` | `float` | |
| `zOffset` | `float` | `float` | |
| `ammoCount` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_MAX_AMMO_IN_CLIP

**Hash:** `0xA38DCFFCEA8962FA` | **JHash:** `0x6961E2A4` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_MAX_AMMO_IN_CLIP(Ped ped, Hash weaponHash, BOOL p2)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_MAX_AMMO_IN_CLIP, ped, weaponHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `int` → `int`

**说明:** p2 is mostly 1 in the scripts.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### _GET_TIME_BEFORE_VEHICLE_WEAPON_RELOAD_FINISHES

**Hash:** `0xC8C6F4B1CDEB40EF` | **Build:** `3407`

**C++ 原型:**
```cpp
int _GET_TIME_BEFORE_VEHICLE_WEAPON_RELOAD_FINISHES(Vehicle vehicle, int seat)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash._GET_TIME_BEFORE_VEHICLE_WEAPON_RELOAD_FINISHES, vehicle, seat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |

**返回值:** `int` → `int`

---

### _HAS_WEAPON_RELOADING_IN_VEHICLE

**Hash:** `0x8062F07153F4446F` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _HAS_WEAPON_RELOADING_IN_VEHICLE(Vehicle vehicle, int seat)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._HAS_WEAPON_RELOADING_IN_VEHICLE, vehicle, seat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### _GET_VEHICLE_WEAPON_RELOAD_TIME

**Hash:** `0xD0AD348FFD7A6868` | **Build:** `3407`

**C++ 原型:**
```cpp
float _GET_VEHICLE_WEAPON_RELOAD_TIME(Vehicle vehicle, int seat)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash._GET_VEHICLE_WEAPON_RELOAD_TIME, vehicle, seat);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |

**返回值:** `float` → `float`

---

### _GET_AMMO_IN_VEHICLE_WEAPON_CLIP

**Hash:** `0x2857938C5D407AFA` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _GET_AMMO_IN_VEHICLE_WEAPON_CLIP(Vehicle vehicle, int seat, int* ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._GET_AMMO_IN_VEHICLE_WEAPON_CLIP, vehicle, seat, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |
| `ammo` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### GET_AMMO_IN_CLIP

**Hash:** `0x2E1202248937775C` | **JHash:** `0x73C100C3` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_AMMO_IN_CLIP(Ped ped, Hash weaponHash, int* ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_AMMO_IN_CLIP, ped, weaponHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammo` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### _SET_AMMO_IN_VEHICLE_WEAPON_CLIP

**Hash:** `0x873906720EE842C3` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _SET_AMMO_IN_VEHICLE_WEAPON_CLIP(Vehicle vehicle, int seat, int ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._SET_AMMO_IN_VEHICLE_WEAPON_CLIP, vehicle, seat, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |
| `ammo` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_AMMO_IN_CLIP

**Hash:** `0xDCD2A934D65CB497` | **JHash:** `0xA54B0B10` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_AMMO_IN_CLIP(Ped ped, Hash weaponHash, int ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_AMMO_IN_CLIP, ped, weaponHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammo` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_MAX_AMMO

**Hash:** `0xDC16122C7A20C933` | **JHash:** `0x0B294796` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_MAX_AMMO(Ped ped, Hash weaponHash, int* ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MAX_AMMO, ped, weaponHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `ammo` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_MAX_AMMO_BY_TYPE

**Hash:** `0x585847C5E4E11709` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL GET_MAX_AMMO_BY_TYPE(Ped ped, Hash ammoTypeHash, int* ammo)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_MAX_AMMO_BY_TYPE, ped, ammoTypeHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ammoTypeHash` | `Hash` | `uint` | |
| `ammo` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

**说明:** Returns the max ammo for an ammo type. Ammo types: https://gist.github.com/root-cause/faf41f59f7a6d818b7db0b839bd147c1

---

### ADD_PED_AMMO_BY_TYPE

**Hash:** `0x2472622CE1F2D45F` | **Build:** `1103`

**C++ 原型:**
```cpp
void ADD_PED_AMMO_BY_TYPE(Ped ped, Hash ammoTypeHash, int ammo)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.ADD_PED_AMMO_BY_TYPE, ped, ammoTypeHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ammoTypeHash` | `Hash` | `uint` | |
| `ammo` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Ammo types: https://gist.github.com/root-cause/faf41f59f7a6d818b7db0b839bd147c1

---

### SET_PED_AMMO_BY_TYPE

**Hash:** `0x5FD1E1F011E76D7E` | **JHash:** `0x311C52BB` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AMMO_BY_TYPE(Ped ped, Hash ammoTypeHash, int ammo)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AMMO_BY_TYPE, ped, ammoTypeHash, ammo);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ammoTypeHash` | `Hash` | `uint` | |
| `ammo` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Ammo types: https://gist.github.com/root-cause/faf41f59f7a6d818b7db0b839bd147c1

---

### GET_PED_AMMO_BY_TYPE

**Hash:** `0x39D22031557946C1` | **JHash:** `0x54077C4D` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_AMMO_BY_TYPE(Ped ped, Hash ammoTypeHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_AMMO_BY_TYPE, ped, ammoTypeHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `ammoTypeHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### SET_PED_AMMO_TO_DROP

**Hash:** `0xA4EFEF9440A5B0EF` | **JHash:** `0x2386A307` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_AMMO_TO_DROP(Ped ped, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_AMMO_TO_DROP, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### SET_PICKUP_AMMO_AMOUNT_SCALER

**Hash:** `0xE620FD3512A04F18` | **JHash:** `0xD6460EA2` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PICKUP_AMMO_AMOUNT_SCALER(float p0)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PICKUP_AMMO_AMOUNT_SCALER, p0);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |

**返回值:** `void` → `void`

---

### GET_PED_AMMO_TYPE_FROM_WEAPON

**Hash:** `0x7FEAD38B326B9F74` | **JHash:** `0x09337863` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_PED_AMMO_TYPE_FROM_WEAPON(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_AMMO_TYPE_FROM_WEAPON, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the current ammo type of the specified ped's specified weapon.
MkII magazines will change the return value, like Pistol MkII returning AMMO_PISTOL without any components and returning AMMO_PISTOL_TRACER after Tracer Rounds component is attached.
Use 0xF489B44DD5AF4BD9 if you always want AMMO_PISTOL.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_PED_ORIGINAL_AMMO_TYPE_FROM_WEAPON

**Hash:** `0xF489B44DD5AF4BD9` | **Build:** `1103`

**C++ 原型:**
```cpp
Hash GET_PED_ORIGINAL_AMMO_TYPE_FROM_WEAPON(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_PED_ORIGINAL_AMMO_TYPE_FROM_WEAPON, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the base/default ammo type of the specified ped's specified weapon.
Use GET_PED_AMMO_TYPE_FROM_WEAPON if you want current ammo type (like AMMO_MG_INCENDIARY/AMMO_MG_TRACER while using MkII magazines) and use this if you want base ammo type. (AMMO_MG)
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_PED_LAST_WEAPON_IMPACT_COORD

**Hash:** `0x6C4D0409BA1A2BC2` | **JHash:** `0x9B266079` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_PED_LAST_WEAPON_IMPACT_COORD(Ped ped, Vector3* coords)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_PED_LAST_WEAPON_IMPACT_COORD, ped, coords);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `coords` | `Vector3*` | `Vector3*` | |

**返回值:** `BOOL` → `bool`

**说明:** Pass ped. Pass address of Vector3.
The coord will be put into the Vector3.
The return will determine whether there was a coord found or not.

---

### SET_PED_GADGET

**Hash:** `0xD0D7B1E680ED4A1A` | **JHash:** `0x8A256D0A` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_GADGET(Ped ped, Hash gadgetHash, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_GADGET, ped, gadgetHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `gadgetHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** p1/gadgetHash was always 0xFBAB5776 ("GADGET_PARACHUTE").
p2 is always true.

---

### GET_IS_PED_GADGET_EQUIPPED

**Hash:** `0xF731332072F5156C` | **JHash:** `0x8DDD0B5B` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_IS_PED_GADGET_EQUIPPED(Ped ped, Hash gadgetHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_IS_PED_GADGET_EQUIPPED, ped, gadgetHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `gadgetHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** gadgetHash - was always 0xFBAB5776 ("GADGET_PARACHUTE").

---

### GET_SELECTED_PED_WEAPON

**Hash:** `0x0A6DB4965674D243` | **JHash:** `0xD240123E` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_SELECTED_PED_WEAPON(Ped ped)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_SELECTED_PED_WEAPON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `Hash` → `uint`

**说明:** Returns the hash of the weapon. 

            var num7 = WEAPON::GET_SELECTED_PED_WEAPON(num4);
            sub_27D3(num7);
            switch (num7)
            {
                case 0x24B17070:

Also see WEAPON::GET_CURRENT_PED_WEAPON. Difference?

-------------------------------------------------------------------------

The difference is that GET_SELECTED_PED_WEAPON simply returns the ped's current weapon hash but GET_CURRENT_PED_WEAPON also checks the weapon object and returns true if the hash of the weapon object equals the weapon hash
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### EXPLODE_PROJECTILES

**Hash:** `0xFC4BD125DE7611E4` | **JHash:** `0x35A0B955` | **Build:** `323`

**C++ 原型:**
```cpp
void EXPLODE_PROJECTILES(Ped ped, Hash weaponHash, BOOL p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.EXPLODE_PROJECTILES, ped, weaponHash, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `p2` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:**              WEAPON::EXPLODE_PROJECTILES(PLAYER::PLAYER_PED_ID(), func_221(0x00000003), 0x00000001);

---

### REMOVE_ALL_PROJECTILES_OF_TYPE

**Hash:** `0xFC52E0F37E446528` | **JHash:** `0xA5F89919` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_ALL_PROJECTILES_OF_TYPE(Hash weaponHash, BOOL explode)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_PROJECTILES_OF_TYPE, weaponHash, explode);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `explode` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** If `explode` true, then removal is done through exploding the projectile. Basically the same as EXPLODE_PROJECTILES but without defining the owner ped.

---

### GET_LOCKON_DISTANCE_OF_CURRENT_PED_WEAPON

**Hash:** `0x840F03E9041E2C9C` | **JHash:** `0x3612110D` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_LOCKON_DISTANCE_OF_CURRENT_PED_WEAPON(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_LOCKON_DISTANCE_OF_CURRENT_PED_WEAPON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### GET_MAX_RANGE_OF_CURRENT_PED_WEAPON

**Hash:** `0x814C9D19DFD69679` | **JHash:** `0xB2B2BBAA` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_MAX_RANGE_OF_CURRENT_PED_WEAPON(Ped ped)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_MAX_RANGE_OF_CURRENT_PED_WEAPON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `float` → `float`

---

### HAS_VEHICLE_GOT_PROJECTILE_ATTACHED

**Hash:** `0x717C8481234E3B88` | **JHash:** `0xA57E2E80` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_VEHICLE_GOT_PROJECTILE_ATTACHED(Ped driver, Vehicle vehicle, Hash weaponHash, Any p3)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_VEHICLE_GOT_PROJECTILE_ATTACHED, driver, vehicle, weaponHash, p3);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `driver` | `Ped` | `int` | |
| `vehicle` | `Vehicle` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `p3` | `Any` | `object` | |

**返回值:** `BOOL` → `bool`

**说明:** Fourth Parameter = unsure, almost always -1

---

### GIVE_WEAPON_COMPONENT_TO_PED

**Hash:** `0xD966D51AA5B28BB9` | **JHash:** `0x3E1E286D` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_WEAPON_COMPONENT_TO_PED(Ped ped, Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_WEAPON_COMPONENT_TO_PED, ped, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons & components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### REMOVE_WEAPON_COMPONENT_FROM_PED

**Hash:** `0x1E8BE90C74FB4C09` | **JHash:** `0x412AA00D` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WEAPON_COMPONENT_FROM_PED(Ped ped, Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WEAPON_COMPONENT_FROM_PED, ped, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons & components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### HAS_PED_GOT_WEAPON_COMPONENT

**Hash:** `0xC593212475FAE340` | **JHash:** `0xDC0FC145` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_PED_GOT_WEAPON_COMPONENT(Ped ped, Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_PED_GOT_WEAPON_COMPONENT, ped, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons & components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### IS_PED_WEAPON_COMPONENT_ACTIVE

**Hash:** `0x0D78DE0572D3969E` | **JHash:** `0x7565FB19` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_WEAPON_COMPONENT_ACTIVE(Ped ped, Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_WEAPON_COMPONENT_ACTIVE, ped, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons & components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### REFILL_AMMO_INSTANTLY

**Hash:** `0x8C0D57EA686FAD87` | **JHash:** `0x82EEAF0F` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL REFILL_AMMO_INSTANTLY(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REFILL_AMMO_INSTANTLY, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### MAKE_PED_RELOAD

**Hash:** `0x20AE33F3AC9C0033` | **JHash:** `0x515292C2` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL MAKE_PED_RELOAD(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.MAKE_PED_RELOAD, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** Forces a ped to reload only if they are able to; if they have a full magazine, they will not reload.

---

### _TRIGGER_VEHICLE_WEAPON_RELOAD

**Hash:** `0x5B1513F27F279A44` | **Build:** `3407`

**C++ 原型:**
```cpp
BOOL _TRIGGER_VEHICLE_WEAPON_RELOAD(Vehicle vehicle, int seat, Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash._TRIGGER_VEHICLE_WEAPON_RELOAD, vehicle, seat, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `vehicle` | `Vehicle` | `int` | |
| `seat` | `int` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REQUEST_WEAPON_ASSET

**Hash:** `0x5443438F033E29C3` | **JHash:** `0x65D139A5` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_WEAPON_ASSET(Hash weaponHash, int p1, int p2)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_WEAPON_ASSET, weaponHash, p1, p2);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `p1` | `int` | `int` | |
| `p2` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Nearly every instance of p1 I found was 31. Nearly every instance of p2 I found was 0.

REQUEST_WEAPON_ASSET(iLocal_1888, 31, 26);

---

### HAS_WEAPON_ASSET_LOADED

**Hash:** `0x36E353271F0E90EE` | **JHash:** `0x1891D5BB` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_WEAPON_ASSET_LOADED(Hash weaponHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_WEAPON_ASSET_LOADED, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_WEAPON_ASSET

**Hash:** `0xAA08EF13F341C8FC` | **JHash:** `0x2C0DFE3C` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WEAPON_ASSET(Hash weaponHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WEAPON_ASSET, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

---

### CREATE_WEAPON_OBJECT

**Hash:** `0x9541D3CF0D398F36` | **JHash:** `0x62F5987F` | **Build:** `323`

**C++ 原型:**
```cpp
Object CREATE_WEAPON_OBJECT(Hash weaponHash, int ammoCount, float x, float y, float z, BOOL showWorldModel, float scale, Any p7, Any p8, Any p9)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_WEAPON_OBJECT, weaponHash, ammoCount, x, y, z, showWorldModel, scale, p7, p8, p9);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `ammoCount` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `showWorldModel` | `BOOL` | `bool` | |
| `scale` | `float` | `float` | |
| `p7` | `Any` | `object` | |
| `p8` | `Any` | `object` | |
| `p9` | `Any` | `object` | |

**返回值:** `Object` → `int`

**说明:** Now has 8 params.

---

### GIVE_WEAPON_COMPONENT_TO_WEAPON_OBJECT

**Hash:** `0x33E179436C0B31DB` | **JHash:** `0xF7612A37` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_WEAPON_COMPONENT_TO_WEAPON_OBJECT(Object weaponObject, Hash componentHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_WEAPON_COMPONENT_TO_WEAPON_OBJECT, weaponObject, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Object` | `int` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** componentHash:
(use WEAPON::GET_WEAPON_COMPONENT_TYPE_MODEL() to get hash value)
${component_at_ar_flsh}, ${component_at_ar_supp}, ${component_at_pi_flsh}, ${component_at_scope_large}, ${component_at_ar_supp_02}

---

### REMOVE_WEAPON_COMPONENT_FROM_WEAPON_OBJECT

**Hash:** `0xF7D82B0D66777611` | **JHash:** `0xA6E7ED3C` | **Build:** `323`

**C++ 原型:**
```cpp
void REMOVE_WEAPON_COMPONENT_FROM_WEAPON_OBJECT(Object object, Hash componentHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_WEAPON_COMPONENT_FROM_WEAPON_OBJECT, object, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `object` | `Object` | `int` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** see DOES_WEAPON_TAKE_WEAPON_COMPONENT for full list of weapons & components

---

### HAS_WEAPON_GOT_WEAPON_COMPONENT

**Hash:** `0x76A18844E743BF91` | **JHash:** `0x1D368510` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL HAS_WEAPON_GOT_WEAPON_COMPONENT(Object weapon, Hash componentHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.HAS_WEAPON_GOT_WEAPON_COMPONENT, weapon, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weapon` | `Object` | `int` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** see DOES_WEAPON_TAKE_WEAPON_COMPONENT for full list of weapons & components

---

### GIVE_WEAPON_OBJECT_TO_PED

**Hash:** `0xB1FA61371AF7C4B7` | **JHash:** `0x639AF3EF` | **Build:** `323`

**C++ 原型:**
```cpp
void GIVE_WEAPON_OBJECT_TO_PED(Object weaponObject, Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_WEAPON_OBJECT_TO_PED, weaponObject, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Object` | `int` | |
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### DOES_WEAPON_TAKE_WEAPON_COMPONENT

**Hash:** `0x5CEE3DF569CECAB0` | **JHash:** `0xB1817BAA` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL DOES_WEAPON_TAKE_WEAPON_COMPONENT(Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_WEAPON_TAKE_WEAPON_COMPONENT, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** Full list of weapons & components by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_OBJECT_FROM_PED

**Hash:** `0xCAE1DC9A0E22A16D` | **JHash:** `0xDF939A38` | **Build:** `323`

**C++ 原型:**
```cpp
Object GET_WEAPON_OBJECT_FROM_PED(Ped ped, BOOL p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_OBJECT_FROM_PED, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `BOOL` | `bool` | |

**返回值:** `Object` → `int`

**说明:** Drops the current weapon and returns the object

Unknown behavior when unarmed.

---

### GIVE_LOADOUT_TO_PED

**Hash:** `0x68F8BE6AF5CDF8A6` | **Build:** `505`

**C++ 原型:**
```cpp
void GIVE_LOADOUT_TO_PED(Ped ped, Hash loadoutHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.GIVE_LOADOUT_TO_PED, ped, loadoutHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `loadoutHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Gives the specified loadout to the specified ped. 
Loadouts are defined in common.rpf\data\ai\loadouts.meta

---

### SET_PED_WEAPON_TINT_INDEX

**Hash:** `0x50969B9B89ED5738` | **JHash:** `0xEB2A7B23` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_WEAPON_TINT_INDEX(Ped ped, Hash weaponHash, int tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WEAPON_TINT_INDEX, ped, weaponHash, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `tintIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** tintIndex can be the following:

0 - Normal
1 - Green
2 - Gold
3 - Pink
4 - Army
5 - LSPD
6 - Orange
7 - Platinum
Full list of weapons, components & tint indexes by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_PED_WEAPON_TINT_INDEX

**Hash:** `0x2B9EEDC07BD06B9F` | **JHash:** `0x3F9C90A7` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_PED_WEAPON_TINT_INDEX(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_WEAPON_TINT_INDEX, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Full list of weapons, components & tint indexes by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_WEAPON_OBJECT_TINT_INDEX

**Hash:** `0xF827589017D4E4A9` | **JHash:** `0x44ACC1DA` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEAPON_OBJECT_TINT_INDEX(Object weapon, int tintIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_OBJECT_TINT_INDEX, weapon, tintIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weapon` | `Object` | `int` | |
| `tintIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Full list of weapons, components & tint indexes by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_OBJECT_TINT_INDEX

**Hash:** `0xCD183314F7CD2E57` | **JHash:** `0xD91D9576` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WEAPON_OBJECT_TINT_INDEX(Object weapon)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_OBJECT_TINT_INDEX, weapon);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weapon` | `Object` | `int` | |

**返回值:** `int` → `int`

---

### GET_WEAPON_TINT_COUNT

**Hash:** `0x5DCF6C5CAB2E9BF7` | **JHash:** `0x99E4EAAB` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WEAPON_TINT_COUNT(Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_TINT_COUNT, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_PED_WEAPON_COMPONENT_TINT_INDEX

**Hash:** `0x9FE5633880ECD8ED` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_PED_WEAPON_COMPONENT_TINT_INDEX(Ped ped, Hash weaponHash, Hash camoComponentHash, int colorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_WEAPON_COMPONENT_TINT_INDEX, ped, weaponHash, camoComponentHash, colorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `camoComponentHash` | `Hash` | `uint` | |
| `colorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Colors:
0 = Gray
1 = Dark Gray
2 = Black
3 = White
4 = Blue
5 = Cyan
6 = Aqua
7 = Cool Blue
8 = Dark Blue
9 = Royal Blue
10 = Plum
11 = Dark Purple
12 = Purple
13 = Red
14 = Wine Red
15 = Magenta
16 = Pink
17 = Salmon
18 = Hot Pink
19 = Rust Orange
20 = Brown
21 = Earth
22 = Orange
23 = Light Orange
24 = Dark Yellow
25 = Yellow
26 = Light Brown
27 = Lime Green
28 = Olive
29 = Moss
30 = Turquoise
31 = Dark Green
Full list of weapons, components, tint indexes & weapon liveries by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_PED_WEAPON_COMPONENT_TINT_INDEX

**Hash:** `0xF0A60040BE558F2D` | **Build:** `1103`

**C++ 原型:**
```cpp
int GET_PED_WEAPON_COMPONENT_TINT_INDEX(Ped ped, Hash weaponHash, Hash camoComponentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_WEAPON_COMPONENT_TINT_INDEX, ped, weaponHash, camoComponentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `camoComponentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns -1 if camoComponentHash is invalid/not attached to the weapon.
Full list of weapons, components, tint indexes & weapon liveries by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_WEAPON_OBJECT_COMPONENT_TINT_INDEX

**Hash:** `0x5DA825A85D0EA6E6` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_WEAPON_OBJECT_COMPONENT_TINT_INDEX(Object weaponObject, Hash camoComponentHash, int colorIndex)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_OBJECT_COMPONENT_TINT_INDEX, weaponObject, camoComponentHash, colorIndex);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Object` | `int` | |
| `camoComponentHash` | `Hash` | `uint` | |
| `colorIndex` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Colors:
0 = Gray
1 = Dark Gray
2 = Black
3 = White
4 = Blue
5 = Cyan
6 = Aqua
7 = Cool Blue
8 = Dark Blue
9 = Royal Blue
10 = Plum
11 = Dark Purple
12 = Purple
13 = Red
14 = Wine Red
15 = Magenta
16 = Pink
17 = Salmon
18 = Hot Pink
19 = Rust Orange
20 = Brown
21 = Earth
22 = Orange
23 = Light Orange
24 = Dark Yellow
25 = Yellow
26 = Light Brown
27 = Lime Green
28 = Olive
29 = Moss
30 = Turquoise
31 = Dark Green
Full list of weapons, components, tint indexes & weapon liveries by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_OBJECT_COMPONENT_TINT_INDEX

**Hash:** `0xB3EA4FEABF41464B` | **Build:** `1103`

**C++ 原型:**
```cpp
int GET_WEAPON_OBJECT_COMPONENT_TINT_INDEX(Object weaponObject, Hash camoComponentHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_OBJECT_COMPONENT_TINT_INDEX, weaponObject, camoComponentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Object` | `int` | |
| `camoComponentHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Returns -1 if camoComponentHash is invalid/not attached to the weapon object.
Full list of weapons, components, tint indexes & weapon liveries by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_PED_WEAPON_CAMO_INDEX

**Hash:** `0xA2C9AC24B4061285` | **Build:** `1103`

**C++ 原型:**
```cpp
int GET_PED_WEAPON_CAMO_INDEX(Ped ped, Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_PED_WEAPON_CAMO_INDEX, ped, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

---

### SET_WEAPON_OBJECT_CAMO_INDEX

**Hash:** `0x977CA98939E82E4B` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_WEAPON_OBJECT_CAMO_INDEX(Object weaponObject, int p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_OBJECT_CAMO_INDEX, weaponObject, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Object` | `int` | |
| `p1` | `int` | `int` | |

**返回值:** `void` → `void`

---

### GET_WEAPON_HUD_STATS

**Hash:** `0xD92C739EE34C9EBA` | **JHash:** `0xA9AD3D98` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_WEAPON_HUD_STATS(Hash weaponHash, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_WEAPON_HUD_STATS, weaponHash, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

**说明:** struct WeaponHudStatsData
{
    BYTE hudDamage; // 0x0000
    char _0x0001[0x7]; // 0x0001
    BYTE hudSpeed; // 0x0008
    char _0x0009[0x7]; // 0x0009
    BYTE hudCapacity; // 0x0010
    char _0x0011[0x7]; // 0x0011
    BYTE hudAccuracy; // 0x0018
    char _0x0019[0x7]; // 0x0019
    BYTE hudRange; // 0x0020
};

Usage:

WeaponHudStatsData data;
if (GET_WEAPON_HUD_STATS(weaponHash, (int *)&data))
{
    // BYTE damagePercentage = data.hudDamage and so on
}
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_COMPONENT_HUD_STATS

**Hash:** `0xB3CAF387AE12E9F8` | **JHash:** `0xBB5498F4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL GET_WEAPON_COMPONENT_HUD_STATS(Hash componentHash, Any* outData)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.GET_WEAPON_COMPONENT_HUD_STATS, componentHash, outData);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `componentHash` | `Hash` | `uint` | |
| `outData` | `Any*` | `object*` | |

**返回值:** `BOOL` → `bool`

---

### GET_WEAPON_DAMAGE

**Hash:** `0x3133B907D8B32053` | **JHash:** `0xE81649C0` | **Build:** `323`

**C++ 原型:**
```cpp
float GET_WEAPON_DAMAGE(Hash weaponHash, Hash componentHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_WEAPON_DAMAGE, weaponHash, componentHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `componentHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** This native does not return damages of weapons from the melee and explosive group.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_CLIP_SIZE

**Hash:** `0x583BE370B1EC6EB4` | **JHash:** `0x8D515E66` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WEAPON_CLIP_SIZE(Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_CLIP_SIZE, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** // Returns the size of the default weapon component clip.

Use it like this:

char cClipSize[32];
Hash cur;
if (WEAPON::GET_CURRENT_PED_WEAPON(playerPed, &cur, 1))
{
    if (WEAPON::IS_WEAPON_VALID(cur))
    {
        int iClipSize = WEAPON::GET_WEAPON_CLIP_SIZE(cur);
        sprintf_s(cClipSize, "ClipSize: %.d", iClipSize);
        vDrawString(cClipSize, 0.5f, 0.5f);
    }
}
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### GET_WEAPON_TIME_BETWEEN_SHOTS

**Hash:** `0x065D2AACAD8CF7A4` | **Build:** `1290`

**C++ 原型:**
```cpp
float GET_WEAPON_TIME_BETWEEN_SHOTS(Hash weaponHash)
```

**C# 调用格式:**
```csharp
float result = Function.Call<float>(Hash.GET_WEAPON_TIME_BETWEEN_SHOTS, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `float` → `float`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_PED_CHANCE_OF_FIRING_BLANKS

**Hash:** `0x8378627201D5497D` | **JHash:** `0xB4F44C6E` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_PED_CHANCE_OF_FIRING_BLANKS(Ped ped, float xBias, float yBias)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PED_CHANCE_OF_FIRING_BLANKS, ped, xBias, yBias);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `xBias` | `float` | `float` | |
| `yBias` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_PED_SHOOT_ORDNANCE_WEAPON

**Hash:** `0xB4C8D77C80C0421E` | **JHash:** `0xEC2E5304` | **Build:** `323`

**C++ 原型:**
```cpp
Object SET_PED_SHOOT_ORDNANCE_WEAPON(Ped ped, float p1)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.SET_PED_SHOOT_ORDNANCE_WEAPON, ped, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `p1` | `float` | `float` | |

**返回值:** `Object` → `int`

**说明:** Returns handle of the projectile.

---

### REQUEST_WEAPON_HIGH_DETAIL_MODEL

**Hash:** `0x48164DBB970AC3F0` | **JHash:** `0xE3BD00F9` | **Build:** `323`

**C++ 原型:**
```cpp
void REQUEST_WEAPON_HIGH_DETAIL_MODEL(Entity weaponObject)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REQUEST_WEAPON_HIGH_DETAIL_MODEL, weaponObject);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponObject` | `Entity` | `int` | |

**返回值:** `void` → `void`

---

### _SET_WEAPON_PED_DAMAGE_MODIFIER

**Hash:** `0x1091922715B68DF0` | **Build:** `3095`

**C++ 原型:**
```cpp
void _SET_WEAPON_PED_DAMAGE_MODIFIER(Hash weapon, float damageModifier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash._SET_WEAPON_PED_DAMAGE_MODIFIER, weapon, damageModifier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weapon` | `Hash` | `uint` | |
| `damageModifier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_WEAPON_DAMAGE_MODIFIER

**Hash:** `0x4757F00BC6323CFE` | **Build:** `505`

**C++ 原型:**
```cpp
void SET_WEAPON_DAMAGE_MODIFIER(Hash weaponHash, float damageMultiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_DAMAGE_MODIFIER, weaponHash, damageMultiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `damageMultiplier` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** Changes the weapon damage output by the given multiplier value. Must be run every frame.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_WEAPON_AOE_MODIFIER

**Hash:** `0x4AE5AC8B852D642C` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_WEAPON_AOE_MODIFIER(Hash weaponHash, float multiplier)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_AOE_MODIFIER, weaponHash, multiplier);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |
| `multiplier` | `float` | `float` | |

**返回值:** `void` → `void`

---

### SET_WEAPON_EFFECT_DURATION_MODIFIER

**Hash:** `0xE6D2CEDD370FF98E` | **Build:** `2372`

**C++ 原型:**
```cpp
void SET_WEAPON_EFFECT_DURATION_MODIFIER(Hash p0, float p1)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_EFFECT_DURATION_MODIFIER, p0, p1);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `Hash` | `uint` | |
| `p1` | `float` | `float` | |

**返回值:** `void` → `void`

**说明:** ex, WEAPON::SET_WEAPON_EFFECT_DURATION_MODIFIER(joaat("vehicle_weapon_mine_slick"), 1.0);

---

### IS_PED_CURRENT_WEAPON_SILENCED

**Hash:** `0x65F0C5AE05943EC7` | **JHash:** `0xBAF7BFBE` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_PED_CURRENT_WEAPON_SILENCED(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_PED_CURRENT_WEAPON_SILENCED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

**说明:** This native returns a true or false value.

Ped ped = The ped whose weapon you want to check.

---

### IS_FLASH_LIGHT_ON

**Hash:** `0x4B7620C47217126C` | **JHash:** `0x76876154` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL IS_FLASH_LIGHT_ON(Ped ped)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_FLASH_LIGHT_ON, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_FLASH_LIGHT_FADE_DISTANCE

**Hash:** `0xCEA66DAD478CD39B` | **JHash:** `0xB0127EA7` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL SET_FLASH_LIGHT_FADE_DISTANCE(float distance)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.SET_FLASH_LIGHT_FADE_DISTANCE, distance);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `distance` | `float` | `float` | |

**返回值:** `BOOL` → `bool`

---

### SET_FLASH_LIGHT_ACTIVE_HISTORY

**Hash:** `0x988DB6FE9B3AC000` | **Build:** `2060`

**C++ 原型:**
```cpp
void SET_FLASH_LIGHT_ACTIVE_HISTORY(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_FLASH_LIGHT_ACTIVE_HISTORY, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Enables/disables flashlight on ped's weapon.

---

### SET_WEAPON_ANIMATION_OVERRIDE

**Hash:** `0x1055AC3A667F09D9` | **JHash:** `0xA5DF7484` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_WEAPON_ANIMATION_OVERRIDE(Ped ped, Hash animStyle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_WEAPON_ANIMATION_OVERRIDE, ped, animStyle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `animStyle` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Changes the selected ped aiming animation style. 
Note : You must use GET_HASH_KEY!

Strings to use with GET_HASH_KEY :

    "Ballistic",
    "Default",
  "Fat",
  "Female",
   "FirstPerson",
  "FirstPersonAiming",
    "FirstPersonFranklin",
  "FirstPersonFranklinAiming",
    "FirstPersonFranklinRNG",
   "FirstPersonFranklinScope",
 "FirstPersonMPFemale",
  "FirstPersonMichael",
   "FirstPersonMichaelAiming",
 "FirstPersonMichaelRNG",
    "FirstPersonMichaelScope",
  "FirstPersonRNG",
   "FirstPersonScope",
 "FirstPersonTrevor",
    "FirstPersonTrevorAiming",
  "FirstPersonTrevorRNG",
 "FirstPersonTrevorScope",
   "Franklin",
 "Gang",
 "Gang1H",
   "GangFemale",
   "Hillbilly",
    "MP_F_Freemode",
    "Michael",
  "SuperFat",
 "Trevor"

---

### GET_WEAPON_DAMAGE_TYPE

**Hash:** `0x3BE0BB12D25FB305` | **JHash:** `0x013AFC13` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_WEAPON_DAMAGE_TYPE(Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_WEAPON_DAMAGE_TYPE, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** enum class eDamageType
{
	UNKNOWN = 0,
	NONE = 1,
	MELEE = 2,
	BULLET = 3,
	BULLET_RUBBER = 4,
	EXPLOSIVE = 5,
	FIRE = 6,
	COLLISION = 7,
	FALL = 8,
	DROWN = 9,
	ELECTRIC = 10,
	BARBED_WIRE = 11,
	FIRE_EXTINGUISHER = 12,
	SMOKE = 13,
	WATER_CANNON = 14,
	TRANQUILIZER = 15,
};

Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_EQIPPED_WEAPON_START_SPINNING_AT_FULL_SPEED

**Hash:** `0xE4DCEC7FD5B739A5` | **JHash:** `0x64646F1D` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_EQIPPED_WEAPON_START_SPINNING_AT_FULL_SPEED(Ped ped)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_EQIPPED_WEAPON_START_SPINNING_AT_FULL_SPEED, ped);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |

**返回值:** `void` → `void`

---

### CAN_USE_WEAPON_ON_PARACHUTE

**Hash:** `0xBC7BE5ABC0879F74` | **JHash:** `0x135E7AD4` | **Build:** `323`

**C++ 原型:**
```cpp
BOOL CAN_USE_WEAPON_ON_PARACHUTE(Hash weaponHash)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.CAN_USE_WEAPON_ON_PARACHUTE, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `BOOL` → `bool`

**说明:** this returns if you can use the weapon while using a parachute
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### CREATE_AIR_DEFENCE_SPHERE

**Hash:** `0x91EF34584710BE99` | **Build:** `573`

**C++ 原型:**
```cpp
int CREATE_AIR_DEFENCE_SPHERE(float x, float y, float z, float radius, float p4, float p5, float p6, Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_AIR_DEFENCE_SPHERE, x, y, z, radius, p4, p5, p6, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Both coordinates are from objects in the decompiled scripts. Native related to 0xECDC202B25E5CF48 p1 value. The only weapon hash used in the decompiled scripts is weapon_air_defence_gun. These two natives are used by the yacht script, decompiled scripts suggest it and the weapon hash used (valkyrie's rockets) are also used by yachts.

---

### CREATE_AIR_DEFENCE_ANGLED_AREA

**Hash:** `0x9DA58CDBF6BDBC08` | **Build:** `1011`

**C++ 原型:**
```cpp
int CREATE_AIR_DEFENCE_ANGLED_AREA(float p0, float p1, float p2, float p3, float p4, float p5, float p6, float p7, float p8, float radius, Hash weaponHash)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.CREATE_AIR_DEFENCE_ANGLED_AREA, p0, p1, p2, p3, p4, p5, p6, p7, p8, radius, weaponHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `p0` | `float` | `float` | |
| `p1` | `float` | `float` | |
| `p2` | `float` | `float` | |
| `p3` | `float` | `float` | |
| `p4` | `float` | `float` | |
| `p5` | `float` | `float` | |
| `p6` | `float` | `float` | |
| `p7` | `float` | `float` | |
| `p8` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `weaponHash` | `Hash` | `uint` | |

**返回值:** `int` → `int`

**说明:** Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### REMOVE_AIR_DEFENCE_SPHERE

**Hash:** `0x0ABF535877897560` | **Build:** `573`

**C++ 原型:**
```cpp
BOOL REMOVE_AIR_DEFENCE_SPHERE(int zoneId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.REMOVE_AIR_DEFENCE_SPHERE, zoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### REMOVE_ALL_AIR_DEFENCE_SPHERES

**Hash:** `0x1E45B34ADEBEE48E` | **Build:** `573`

**C++ 原型:**
```cpp
void REMOVE_ALL_AIR_DEFENCE_SPHERES(void)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.REMOVE_ALL_AIR_DEFENCE_SPHERES);
```

**返回值:** `void` → `void`

---

### SET_PLAYER_TARGETTABLE_FOR_AIR_DEFENCE_SPHERE

**Hash:** `0xECDC202B25E5CF48` | **Build:** `573`

**C++ 原型:**
```cpp
void SET_PLAYER_TARGETTABLE_FOR_AIR_DEFENCE_SPHERE(Player player, int zoneId, BOOL enable)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_PLAYER_TARGETTABLE_FOR_AIR_DEFENCE_SPHERE, player, zoneId, enable);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `player` | `Player` | `int` | |
| `zoneId` | `int` | `int` | |
| `enable` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### IS_AIR_DEFENCE_SPHERE_IN_AREA

**Hash:** `0xDAB963831DBFD3F4` | **Build:** `1103`

**C++ 原型:**
```cpp
BOOL IS_AIR_DEFENCE_SPHERE_IN_AREA(float x, float y, float z, float radius, int* outZoneId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.IS_AIR_DEFENCE_SPHERE_IN_AREA, x, y, z, radius, outZoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |
| `radius` | `float` | `float` | |
| `outZoneId` | `int*` | `int*` | |

**返回值:** `BOOL` → `bool`

---

### FIRE_AIR_DEFENCE_SPHERE_WEAPON_AT_POSITION

**Hash:** `0x44F1012B69313374` | **Build:** `573`

**C++ 原型:**
```cpp
void FIRE_AIR_DEFENCE_SPHERE_WEAPON_AT_POSITION(int zoneId, float x, float y, float z)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.FIRE_AIR_DEFENCE_SPHERE_WEAPON_AT_POSITION, zoneId, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `void` → `void`

---

### DOES_AIR_DEFENCE_SPHERE_EXIST

**Hash:** `0xCD79A550999D7D4F` | **Build:** `678`

**C++ 原型:**
```cpp
BOOL DOES_AIR_DEFENCE_SPHERE_EXIST(int zoneId)
```

**C# 调用格式:**
```csharp
bool result = Function.Call<bool>(Hash.DOES_AIR_DEFENCE_SPHERE_EXIST, zoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |

**返回值:** `BOOL` → `bool`

---

### SET_CAN_PED_SELECT_INVENTORY_WEAPON

**Hash:** `0xB4771B9AAF4E68E4` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_CAN_PED_SELECT_INVENTORY_WEAPON(Ped ped, Hash weaponHash, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_PED_SELECT_INVENTORY_WEAPON, ped, weaponHash, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `weaponHash` | `Hash` | `uint` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disables selecting the given weapon. Ped isn't forced to put the gun away. However you can't reselect the weapon if you holster then unholster. Weapon is also grayed out on the weapon wheel.
Full list of weapons by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/weapons.json

---

### SET_CAN_PED_SELECT_ALL_WEAPONS

**Hash:** `0xEFF296097FF1E509` | **Build:** `1103`

**C++ 原型:**
```cpp
void SET_CAN_PED_SELECT_ALL_WEAPONS(Ped ped, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_CAN_PED_SELECT_ALL_WEAPONS, ped, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `ped` | `Ped` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

**说明:** Disable all weapons. Does the same as 0xB4771B9AAF4E68E4 except for all weapons.

---


---

## ZONE

**包含 9 个 Native 函数**

### GET_ZONE_AT_COORDS

**Hash:** `0x27040C25DE6CB2F4` | **JHash:** `0xC9018181` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ZONE_AT_COORDS(float x, float y, float z)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ZONE_AT_COORDS, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `int` → `int`

---

### GET_ZONE_FROM_NAME_ID

**Hash:** `0x98CD1D2934B76CC1` | **JHash:** `0x8EC68304` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ZONE_FROM_NAME_ID(const char* zoneName)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ZONE_FROM_NAME_ID, zoneName);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneName` | `const char*` | `string` | |

**返回值:** `int` → `int`

**说明:** 'zoneName' corresponds to an entry in 'popzone.ipl'.

AIRP = Los Santos International Airport
ALAMO = Alamo Sea
ALTA = Alta
ARMYB = Fort Zancudo
BANHAMC = Banham Canyon Dr
BANNING = Banning
BEACH = Vespucci Beach
BHAMCA = Banham Canyon
BRADP = Braddock Pass
BRADT = Braddock Tunnel
BURTON = Burton
CALAFB = Calafia Bridge
CANNY = Raton Canyon
CCREAK = Cassidy Creek
CHAMH = Chamberlain Hills
CHIL = Vinewood Hills
CHU = Chumash
CMSW = Chiliad Mountain State Wilderness
CYPRE = Cypress Flats
DAVIS = Davis
DELBE = Del Perro Beach
DELPE = Del Perro
DELSOL = La Puerta
DESRT = Grand Senora Desert
DOWNT = Downtown
DTVINE = Downtown Vinewood
EAST_V = East Vinewood
EBURO = El Burro Heights
ELGORL = El Gordo Lighthouse
ELYSIAN = Elysian Island
GALFISH = Galilee
GOLF = GWC and Golfing Society
GRAPES = Grapeseed
GREATC = Great Chaparral
HARMO = Harmony
HAWICK = Hawick
HORS = Vinewood Racetrack
HUMLAB = Humane Labs and Research
JAIL = Bolingbroke Penitentiary
KOREAT = Little Seoul
LACT = Land Act Reservoir
LAGO = Lago Zancudo
LDAM = Land Act Dam
LEGSQU = Legion Square
LMESA = La Mesa
LOSPUER = La Puerta
MIRR = Mirror Park
MORN = Morningwood
MOVIE = Richards Majestic
MTCHIL = Mount Chiliad
MTGORDO = Mount Gordo
MTJOSE = Mount Josiah
MURRI = Murrieta Heights
NCHU = North Chumash
NOOSE = N.O.O.S.E
OCEANA = Pacific Ocean
PALCOV = Paleto Cove
PALETO = Paleto Bay
PALFOR = Paleto Forest
PALHIGH = Palomino Highlands
PALMPOW = Palmer-Taylor Power Station
PBLUFF = Pacific Bluffs
PBOX = Pillbox Hill
PROCOB = Procopio Beach
RANCHO = Rancho
RGLEN = Richman Glen
RICHM = Richman
ROCKF = Rockford Hills
RTRAK = Redwood Lights Track
SANAND = San Andreas
SANCHIA = San Chianski Mountain Range
SANDY = Sandy Shores
SKID = Mission Row
SLAB = Stab City
STAD = Maze Bank Arena
STRAW = Strawberry
TATAMO = Tataviam Mountains
TERMINA = Terminal
TEXTI = Textile City
TONGVAH = Tongva Hills
TONGVAV = Tongva Valley
VCANA = Vespucci Canals
VESP = Vespucci
VINE = Vinewood
WINDF = Ron Alternates Wind Farm
WVINE = West Vinewood
ZANCUDO = Zancudo River
ZP_ORT = Port of South Los Santos
ZQ_UAR = Davis Quartz

Full list of zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/zones.json

---

### GET_ZONE_POPSCHEDULE

**Hash:** `0x4334BC40AA0CB4BB` | **JHash:** `0x20AB2FC9` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ZONE_POPSCHEDULE(int zoneId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ZONE_POPSCHEDULE, zoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |

**返回值:** `int` → `int`

---

### GET_NAME_OF_ZONE

**Hash:** `0xCD90657D4C30E1CA` | **JHash:** `0x7875CE91` | **Build:** `323`

**C++ 原型:**
```cpp
const char* GET_NAME_OF_ZONE(float x, float y, float z)
```

**C# 调用格式:**
```csharp
string result = Function.Call<string>(Hash.GET_NAME_OF_ZONE, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `const char*` → `string`

**说明:** AIRP = Los Santos International Airport
ALAMO = Alamo Sea
ALTA = Alta
ARMYB = Fort Zancudo
BANHAMC = Banham Canyon Dr
BANNING = Banning
BEACH = Vespucci Beach
BHAMCA = Banham Canyon
BRADP = Braddock Pass
BRADT = Braddock Tunnel
BURTON = Burton
CALAFB = Calafia Bridge
CANNY = Raton Canyon
CCREAK = Cassidy Creek
CHAMH = Chamberlain Hills
CHIL = Vinewood Hills
CHU = Chumash
CMSW = Chiliad Mountain State Wilderness
CYPRE = Cypress Flats
DAVIS = Davis
DELBE = Del Perro Beach
DELPE = Del Perro
DELSOL = La Puerta
DESRT = Grand Senora Desert
DOWNT = Downtown
DTVINE = Downtown Vinewood
EAST_V = East Vinewood
EBURO = El Burro Heights
ELGORL = El Gordo Lighthouse
ELYSIAN = Elysian Island
GALFISH = Galilee
GOLF = GWC and Golfing Society
GRAPES = Grapeseed
GREATC = Great Chaparral
HARMO = Harmony
HAWICK = Hawick
HORS = Vinewood Racetrack
HUMLAB = Humane Labs and Research
JAIL = Bolingbroke Penitentiary
KOREAT = Little Seoul
LACT = Land Act Reservoir
LAGO = Lago Zancudo
LDAM = Land Act Dam
LEGSQU = Legion Square
LMESA = La Mesa
LOSPUER = La Puerta
MIRR = Mirror Park
MORN = Morningwood
MOVIE = Richards Majestic
MTCHIL = Mount Chiliad
MTGORDO = Mount Gordo
MTJOSE = Mount Josiah
MURRI = Murrieta Heights
NCHU = North Chumash
NOOSE = N.O.O.S.E
OCEANA = Pacific Ocean
PALCOV = Paleto Cove
PALETO = Paleto Bay
PALFOR = Paleto Forest
PALHIGH = Palomino Highlands
PALMPOW = Palmer-Taylor Power Station
PBLUFF = Pacific Bluffs
PBOX = Pillbox Hill
PROCOB = Procopio Beach
RANCHO = Rancho
RGLEN = Richman Glen
RICHM = Richman
ROCKF = Rockford Hills
RTRAK = Redwood Lights Track
SANAND = San Andreas
SANCHIA = San Chianski Mountain Range
SANDY = Sandy Shores
SKID = Mission Row
SLAB = Stab City
STAD = Maze Bank Arena
STRAW = Strawberry
TATAMO = Tataviam Mountains
TERMINA = Terminal
TEXTI = Textile City
TONGVAH = Tongva Hills
TONGVAV = Tongva Valley
VCANA = Vespucci Canals
VESP = Vespucci
VINE = Vinewood
WINDF = Ron Alternates Wind Farm
WVINE = West Vinewood
ZANCUDO = Zancudo River
ZP_ORT = Port of South Los Santos
ZQ_UAR = Davis Quartz

Full list of zones by DurtyFree: https://github.com/DurtyFree/gta-v-data-dumps/blob/master/zones.json

---

### SET_ZONE_ENABLED

**Hash:** `0xBA5ECEEA120E5611` | **JHash:** `0x04E21B03` | **Build:** `323`

**C++ 原型:**
```cpp
void SET_ZONE_ENABLED(int zoneId, BOOL toggle)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.SET_ZONE_ENABLED, zoneId, toggle);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |
| `toggle` | `BOOL` | `bool` | |

**返回值:** `void` → `void`

---

### GET_ZONE_SCUMMINESS

**Hash:** `0x5F7B268D15BA0739` | **JHash:** `0xB2FB5C4C` | **Build:** `323`

**C++ 原型:**
```cpp
int GET_ZONE_SCUMMINESS(int zoneId)
```

**C# 调用格式:**
```csharp
int result = Function.Call<int>(Hash.GET_ZONE_SCUMMINESS, zoneId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `zoneId` | `int` | `int` | |

**返回值:** `int` → `int`

**说明:** cellphone range 1- 5 used for signal bar in iFruit phone

---

### OVERRIDE_POPSCHEDULE_VEHICLE_MODEL

**Hash:** `0x5F7D596BAC2E7777` | **JHash:** `0x3F0A3680` | **Build:** `323`

**C++ 原型:**
```cpp
void OVERRIDE_POPSCHEDULE_VEHICLE_MODEL(int scheduleId, Hash vehicleHash)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.OVERRIDE_POPSCHEDULE_VEHICLE_MODEL, scheduleId, vehicleHash);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scheduleId` | `int` | `int` | |
| `vehicleHash` | `Hash` | `uint` | |

**返回值:** `void` → `void`

**说明:** Only used once in the decompiled scripts. Seems to be related to scripted vehicle generators.

Modified example from "am_imp_exp.c4", line 6406:
/* popSchedules[0] = ZONE::GET_ZONE_POPSCHEDULE(ZONE::GET_ZONE_AT_COORDS(891.3, 807.9, 188.1));
etc.
*/
ZONE::OVERRIDE_POPSCHEDULE_VEHICLE_MODEL(popSchedules[index], vehicleHash);
STREAMING::REQUEST_MODEL(vehicleHash);

---

### CLEAR_POPSCHEDULE_OVERRIDE_VEHICLE_MODEL

**Hash:** `0x5C0DE367AA0D911C` | **JHash:** `0x7A72A24E` | **Build:** `323`

**C++ 原型:**
```cpp
void CLEAR_POPSCHEDULE_OVERRIDE_VEHICLE_MODEL(int scheduleId)
```

**C# 调用格式:**
```csharp
Function.Call(Hash.CLEAR_POPSCHEDULE_OVERRIDE_VEHICLE_MODEL, scheduleId);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `scheduleId` | `int` | `int` | |

**返回值:** `void` → `void`

**说明:** Only used once in the decompiled scripts. Seems to be related to scripted vehicle generators.

Modified example from "am_imp_exp.c4", line 6418:
/* popSchedules[0] = ZONE::GET_ZONE_POPSCHEDULE(ZONE::GET_ZONE_AT_COORDS(891.3, 807.9, 188.1));
etc.
*/
STREAMING::SET_MODEL_AS_NO_LONGER_NEEDED(vehicleHash);
ZONE::CLEAR_POPSCHEDULE_OVERRIDE_VEHICLE_MODEL(popSchedules[index]);

---

### GET_HASH_OF_MAP_AREA_AT_COORDS

**Hash:** `0x7EE64D51E8498728` | **JHash:** `0xB5C5C99B` | **Build:** `323`

**C++ 原型:**
```cpp
Hash GET_HASH_OF_MAP_AREA_AT_COORDS(float x, float y, float z)
```

**C# 调用格式:**
```csharp
uint result = Function.Call<uint>(Hash.GET_HASH_OF_MAP_AREA_AT_COORDS, x, y, z);
```

**参数:**

| 名称 | C++ 类型 | C# 类型 | 说明 |
|------|---------|--------|------|
| `x` | `float` | `float` | |
| `y` | `float` | `float` | |
| `z` | `float` | `float` | |

**返回值:** `Hash` → `uint`

**说明:** Returns a hash representing which part of the map the given coords are located.

Possible return values:
(Hash of) city -> -289320599
(Hash of) countryside -> 2072609373

C# Example :

Ped player = Game.Player.Character;
Hash h = Function.Call<Hash>(Hash.GET_HASH_OF_MAP_AREA_AT_COORDS, player.Position.X, player.Position.Y, player.Position.Z);

---

